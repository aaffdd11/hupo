# 38 · 把已建成的功能按用户切开（三人讨论）

> 主人 2026-09-21 原话（这一篇就是回答它）：
> *"原来就是我没有做这样的隔离，然后做了很多的功能，那现在呢我就需要把这些功能进行隔离，成为不同的用户，
> 所以有**一个主功能是登录功能和创建这个容器**，然后**子功能就是针对不同用户、各个用户他们自己的容器里面
> 的一些设置和那个这种对话聊天的修改**。"*
>
> 三位工程师独立作答（控制面 / 数据面 / 客户端+运维，互相看不到），**下面每条都经我自己复核过**。

---

## 一、边界：主功能（控制面）与子功能（用户那台）

| | 控制面（主功能） | 用户那台（子功能） |
|---|---|---|
| **拥有** | 登录（手机号+验证码）· 令牌（`sub`=真 userId）与撤销 · **`sessionId → userId` 路由**（与 `#agents` 同生共死）· 准入与配额 · **容器的起停与卷分配** · 审计 | 现在那串单例**全部各一份**：`Store`/`Timeline`/`Trash`/`Notice`/`Say`/`Dispatcher`+`Runtime`/`Ledger`+`sock`/`turn-status` · **`DSH_HOME`** · 对话正文 · 账本 · **他自己的 key** |
| 🔴 **不许** | **存任何对话正文、key、`DSH_HOME`** | 决定别人的事（登录、配额、谁能进） |

⚠️ **`tenants.js`（我刚写的）是同进程的过渡层，不是边界** —— 它那十条绿闸证明的是"数据分开取"，
**证明不了跨 uid 的边界**（控制面原话）。真边界是**每租户一个 OS 用户 + 容器**（已建好地基）。

---

## 二、切 / 不切（逐个模块）

| 项 | 判 | 理由 |
|---|---|---|
| `main.jsonl`（`Store`+`Timeline`） | **切** | `tenants.get(uid)` 已做（owner→`data/`、别人→`data/users/<uid>/`） |
| `Trash` · `Notice` 限频账 · `Say` 去重表 | **切** | 三者都 **seed 同一份日志** ⇒ 日志切了它们必须跟着切 |
| `ledger.jsonl` + `Ledger` + **域套接字** | **切** | 套接字路径由 `dataDir` 派生 ⇒ 天然跟走；**但 env 必须按 uid 注入** |
| 🔴 **agent 的键** | **切** | 见 §三①（**今天必撞**） |
| `status.json`（忙不忙） | **聚合** | 每人一份会让 `restart-core.sh` 读不懂 |
| `users.json` · `auth.json` · 令牌 · 临时码 | **不切** | 账号与认证天生全局 |
| 代码 · 镜像 · 人格 · 能力层 · `mcp-ledger-server.mjs` | **不切（只读）** | 无状态 |
| `DSH_HOME` · `agentCwd`/workspace | **切（硬要求）** | N21（键是绝对路径）+ §2.2 三条 |
| 客户端一屏缓存 · 草稿 · 档位设置 | **切** | 见 §八（**设备级 ≠ 用户级**） |

---

## 二·补、✅ 接线落地了（2026-09-21，第 2–3 步）

> §五 那六步里的**第 2 步（单例 → 按 userId 取一份）**与**第 3 步（路由按 `claim.sub` 取世界）**
> 已经落地。**判据 1/2/3/6/7 真机验过**，两条关键修复**都做了变异验证**。

### 补.1 落成了什么

| 件 | 在哪 | 一句话 |
|---|---|---|
| **`Worlds`**（一人一份完整世界） | `src/worlds.js`（新） | 按 userId 各配一份：`store`/`timeline`/`notice`/`say`/`trash`/`ledger`+`ledgerSocket`/`dispatcher`，外加每人的 `cfg`（`DSH_HOME`/`agentCwd`/账本口） |
| **agent 进程池的键按人不同** | `dispatcher.js` 的 `agentKey` | 🔴 **§三① 那个"今天必撞"修掉了**。⚠️ **没有动 `scopeId`** —— 它是写到事件里的字段（盘上已有的是 `'main'`），动它 = 动历史字节 |
| **`DSH_HOME` 按人** | `agent-runtime.js` 的 `cfgFor` | 🔴 少了它就是"甲乙共用一个 DSH_HOME"（N21：会话键是绝对路径编码的 ⇒ 换个 home 就是换个账号） |
| **路由按身份取世界** | `server.js` 的 `worldFor(claim.sub)` | 一句话取一次，**身份只从验过签的令牌来**（不读 URL / body / 头） |
| **流也按人** | `server.js` 的 `onStream` | 补发与订阅都走**他那一份**时间线（否则甲的流会补发出乙的话） |
| **状态聚合** | `worlds.busySnapshot()` | §三③：**任一忙就算忙**（宁等不切）。⚠️ `restart-core.sh` 那行判据**代码没改、语义变了**：原来"那一个人忙不忙"，现在"**N 个人里有没有人忙**" |
| **收尾也按人** | `serve.js` 退出那一段 | 派发器 / 账本口 / "好好走了"标记**每人各收一份**（只收主人那份 = 切断别人的话 + 留孤儿进程） |

**主人那一份原地不动**（这是硬要求，测试盯着）：`DSH_HOME`/工作目录/数据目录**都还是原来的值**，
`owner` 的数据仍然在 `data/` 根下 —— 实测线上重启后**他的 86 条事件原样**。

### 补.2 ⚠️ 还没做的（明说，不许含糊）

| # | 没做 | 为什么 / 什么时候做 |
|---|---|---|
| 1 | **进程级兜底只通知主人**（`installProcessGuard`） | 进程崩是**宿主级**事件，而那条兜底只能往一份时间线上写。别人不会被叫醒。人真的多起来再定按人各装一条 |
| 2 | **按人清 `$DSH_HOME/sessions/`**（欠账 #9 那一套） | 现在**只有主人那一份**在开机清（`serve.js` 顶上那段）。租户的 `sessions/` 会只涨不降 ⇒ 要跟"按人配额"一起做 |
| 3 | **登录白名单 / 老令牌作废**（第 4–6 步） | 现在**新号当场就有自己那份空白世界**了（主人要的正是这个）——但"两人同时在线"那道闸还没开 |
| 4 | **容器**（真正的跨 uid 边界） | 见 `39-PERMISSIONS.md`。⚠️ **这一层不是边界**（控制面原话）：它证的是"服务内部不串号" |

### 补.3 判据与**变异验证**（这一步真正的成绩单）

`test/multitenant.test.js` —— **9 条**，每条都带负向对照。**两条变异验证过**：

| 变异 | 结果 |
|---|---|
| 把 `agentKey` 退回 `timeline.id`（= 接线之前） | 🔴 **2 条红** |
| 把 `cfgForAgentKey` 换成"永远返回全局 cfg" | 🔴 **1 条红** —— ⚠️ **但第一版抓不到**（见下） |

🔴 **第一版判据有个真漏洞，是变异验证抓出来的**：我原来只比
`world.cfg.dshHome` 相不相等 —— 那测的是"**世界算出来的值**"，
而"**agent 实际拿到**哪个 DSH_HOME"是**另一条路**（`runtime.cfgFor`）。
⇒ 变异注入之后 **9 条全绿**（边界已经不在，闸却看不出来）。
修法：判据改成**看真 spawn 出去的 `env.DSH_HOME`**（测试里记下每一次 spawn 的 env），
并加"主人必须还是拿到原来那个"当负向对照。改完再变异，**红了**。

> ⚠️ 这条记在这里是因为它**会重演**：凡是"算出来的值"与"实际用上的值"分两条路的地方，
> 只比前者就是在给自己发假绿。

**真机也验过**（独立进程 + 临时数据，走**真手机号登录**）：
两个号分别登进来 ⇒ 令牌里的 `sub` 是 `u1`/`u2`；
各自说一句话 ⇒ `data/users/u1/main.jsonl` 里**只有甲那句**、`u2` 里**只有乙那句**；
两个目录都是 `0700`。线上重启后主人那一份 **86 条事件原样**、公网 200。

---

## 三、🔴 三个实测卡点（**接线之前必须先解决**）

### ① 所有人会撞**同一个 agent**（我自己验过代码）

> ✅ **2026-09-21 已修**（见 §二·补）：键改成 `<uid>/main`，落在 `dispatcher.js` 的 `agentKey`
> 与 `worlds.agentKeyFor()`。**变异验证过**（退回 `timeline.id` ⇒ 2 条红）。
> ⚠️ 下面这段保留为**当时怎么发现的**，**不是待办**：
> 修的时候**特意没有改 `scopeId`** —— 理由见 `二·补.1` 那张表。

```
dispatcher.js:310   const id = this.#timeline.id;        ← agent 会话的键
dispatcher.js:435   if (sessionId !== this.#timeline.id) return;   ← onEvict 也按它比
serve.js:200        scopeId: timeline.id,
```

我写 `tenants.js` 时**刻意**让每个人的 `timeline.id` 都叫 `'main'`（注释里写的理由是"目录已经把两人分开了"）
—— **那个理由只对了一半**：数据分开了，**键没分开**。共用一个 `AgentRuntime` 时
`runtime.get('main')` 对所有人返回**同一个 agent** ⇒ **甲说的话进乙的窗口**（静默串号，两边日志都"正常"）。

⇒ **修法**：键改成 **`uid/main`**（目录名与日志名都不用动），或让 timeline id 按用户取。
⇒ **判据（带负向对照）**：`runtime.snapshot()` 里必须**同时**看得到 `u1/main` 与 `u2/main`；
**把键改回 `main` ⇒ 只剩一个**（那就是"今天必撞"的证明）。

### ② 🔴 服务**没法以租户身份干活**（两个视角都实测到）

```
$ sudo -n -u hupo-a true
sudo: a password is required
$ sudo -n -l            → sudoers 里没有任何条目
```

⇒ 我契约 `37-MULTITENANT.md` §四写的"起法 `sudo -u hupo-a podman run`"**在服务身份下不成立**。
**这是"谁去建/谁来删容器"的洞**（见 §四）。

### ③ 宿主级的两个**全局**还得单独处理（控制面点名）

* **`ledger.sock`**：同进程过渡期它是全局的 ⇒ 甲的工具调用可能落到乙的账本（**串账**）；
* **`status.json`**：每秒写"忙不忙"，全局一份 ⇒ **甲忙会让乙也"忙"**（→ 重启脚本会等错人，或切错话）。
  ⇒ 聚合成"**任一忙就算忙**"（**宁等不切**），并同步改 `restart-core.sh` 的等待判据。

---

## 四、容器生命周期（**谁建、谁起、谁删**）

| 动作 | 谁做 | 形态 | 代价 |
|---|---|---|---|
| **起停**已存在的容器 | 服务（`deploy`） | **systemd 模板单元** `hupo-tenant@.service`（`User=%i`、`XDG_RUNTIME_DIR=/run/user/%i`）+ **polkit 规则**只放行 `deploy` 对 `hupo-tenant@hupo-a\|hupo-b.service` 的 start/stop ⇒ **免 sudo、免 root 助手** | polkit 规则**本身是把锁**（**要主人签**）· 要 `loginctl enable-linger hupo-a` · 镜像 tar **每租户各 `podman load`** |
| **建 / 删**用户与卷 | **只能 root** | (甲) root 小助手（socket-activated、白名单 uid、四个动词）＝**能自动开通**，代价是 `deploy` 手里多一把 root 钥匙；(乙) 主人**签一批**，人工开通 | ⚠️ 这正是 P1/P2 的那条线：**"能自动开通"和"助手拿不到 root"不能同时要** |

---

## 五、迁移与顺序（**不热切、不做一半**）

控制面给的六步（我认同，且与"主功能/子功能"的分法一致）：

1. `server.js` 里那处 `sub:'owner'` 改成**真 userId**（已经做了）；
2. **单例 → "按 userId 取一份"**（`tenants.js` 已备好取用层，**owner 仍走原来那一份、`data/` 原地不动**）；
3. **路由表**：每个 handler 首行按 `claim.sub` 取世界（14 条路由里 10 条要）；
4. **登录白名单先只放 owner**（此时线上行为与今天**逐字一致** —— 这是"可回退点"）；
5. **用第二个真号走完 §13.4 三道台阶**（容器能起 → 交叉读不到 → 搬家逐字节一致）；
6. 才开"两人同时在线"那道闸。

⚠️ **切换 = 整进程重启，不许热切**（热切时内存里的世界与盘上的会不一致）。
⚠️ **owner 不搬**也自洽；真要搬家作为**单独一次**、用**挂载**不复制 ⇒ 天然逐字节一致。

---

## 六、判据（**可执行 + 负向对照**）

| # | 判据 | 负向对照（**不能省**） |
|---|---|---|
| 1 | 甲说话 ⇒ `data/users/u1/main.jsonl` 有、`data/main.jsonl` **无** | owner 走后者（证明不是"文件根本没在写"） |
| 2 | 乙拿**甲**的令牌读 ⇒ 拒；拿自己的 ⇒ 通 | —— |
| 3 | `runtime.snapshot()` = `u1/main` + `u2/main` | **键改回 `main` ⇒ 只剩一个**（"今天必撞"的证明） |
| 4 | `sudo -u hupo-b cat /home/hupo-a/tenant/…` ⇒ **Permission denied** | 同一条命令读 **hupo-b 自己**的必须成功；再故意 `chmod 755` 甲的目录 ⇒ **变成能读**（证明判的是**权限位**，不是"路径不存在"式空跑） |
| 5 | **以 `hupo-a` 身份**重跑 `tenants` 那十条闸 | **同进程里跑不算**（那证明不了跨 uid） |
| 6 | 拿甲的令牌发**乙的** sessionId ⇒ 403，且**乙的文件字节不变** | —— |
| 7 | 甲忙乙闲 ⇒ `busy=true`；都闲 ⇒ `false` | —— |

---

## 七、🔴 要主人定的（**五条**）

| # | 问题 | 选项 | 备注 |
|---|---|---|---|
| ① | **起停容器**走哪条 | (甲) systemd 模板 + **polkit**（免 sudo）· (乙) root 小助手 | 我倾向**甲**；polkit 规则要你签 |
| ② | **建/删用户与卷**走哪条 | (甲) root 小助手 ⇒ **能自动开通** · (乙) 你签一批、人工开通 | ⚠️ **"自动开通"= 助手手里一把 root 钥匙**（P1/P2 的线）——这条只能你定 |
| ③ | **"新号当场开容器"要不要自动** | 自动 / 先人工 | 与 ② 绑在一起 |
| ④ | **owner 也进容器吗** | 留在宿主 / 也进容器 | 留下最省事；进容器则要处理 N21 的绝对路径（挂载不复制） |
| ⑤ | **容器里跑什么** | 只有 agent / 整个调度器 | `35-CONTAINER-DESIGN.md` §四① 未拍 |

⚠️ 另有一条**还悬着、且挡住下一步**的：**用户的 key 走"网页终端"（不经过我们）还是"表单"（经过我们）**。

---

## 八、客户端 + 运维那一份（第三位）

> 一句话：**把"我是谁"做成缓存键与路由的第一个参数，并让"容器没就绪"挡住聊天** ——
> 否则切完，**每个人看到的还是同一个世界**。

### 8.1 读到的代码事实（他自己核过，我照收）

| 事实 | 意味着 |
|---|---|
| `chat_controller.dart:37-39`：两个 store **已经有 `namespace` 参数，但默认 `'single'`** | ⇒ **今天全站共用一份缓存**；改成传 `sub` 就行，**不用重写** |
| `ProcessLevelStore` 的键**写死、没有 namespace** | ⇒ 档位设置要么按人要么按设备，**要写明** |
| `logout()` **已经**走 `_invalidateLocal()`（`chat_controller.dart:278-290 / 497-502`） | ⇒ 清缓存那条路已经有了，接上 namespace 即可 |
| 🔴 客户端**没有任何地方解令牌的 `sub`** | ⇒ 得加：**只当缓存键用，不验签、不当权限**（验签是服务端的事） |
| 🔴 `Auth` **只能按 `jti` 撤**（`auth.js:366`），**没有"按用户撤全部"** | ⇒ 注销/改号时**撤不干净**（老令牌还能用） |
| `tenants.js` 的 `OWNER_ID='owner'` ⇒ **主人的目录就是 `data/` 根** | 🔴 **凡是按 uid 拼路径的代码（备份/删除/统计）都会"静默跳过主人"**（`data/users/owner` 不存在） |

### 8.2 不分命名空间的后果（他说得很具体，我认同）

**B 登录后会先看到 A 的整屏 + A 打了一半的草稿**；他一发，**那句话就进了 B 的账本**。
⇒ **退出（含 401）要撤令牌 + 清掉全部人的缓存键**（不是只清"自己那份" —— 共享设备上别人还看得见）；**档位不清**。

### 8.3 新用户"等容器"那一段 ✅ **做完了**（2026-09-21）

> 服务端的字段与两屏都落地了。**下面那段原文保留**（它是当时"还没有"的记录）。
> * 服务端：登录回执带 `space`、另有 `GET /api/space`；
>   **缺字段 / 认不出来 ⇒ 按"就绪"**（老客户端行为逐字不变）。
> * 客户端：`lib/models/space.dart`（纯函数决定进哪一屏）· `space_words.dart`（文案）·
>   `screens/waiting_screen.dart` · `screens/model_key_screen.dart`。
> * 判据：`test/unit/space_test.dart` **9 条**（宽容解析 · 四种情况逐条对表 ·
>   **刚填完钥匙不许退回填钥匙那屏** · 🔴 **文案里一个百分号都没有** · 每句过禁用词表）；
>   两屏都**进了可访问性硬闸**（五档不溢出 + 命中区 ≥44）。
> * ⚠️ **`模型` 是禁用词** ⇒ 界面上说的是"**钥匙**"。

### 8.3（原文）新用户"等容器"那一段（**写这一节时服务端还没有这个字段**）

登录响应要**带上容器状态**（缺字段按"**就绪**"兼容 —— 别把老客户端挡在门外）：

| 状态 | 界面说 |
|---|---|
| 在建 | **如实说"正在开一个只属于你的空间"** + 可重试，**不许假进度条** |
| 太久 | **如实说"比平常久了"** + 可重试（"多久算太久"这个数**住代码**） |
| 失败 | **如实说"没建成，是我这边的问题"** + 可重试 |
| 就绪 | 才进聊天 |

### 8.4 运维（注销"少一步就是假删除"）

按 `sub` **撤全部令牌**（今天做不到）→ 容器 → 卷 → `data/users/<uid>/` → 他的 `DSH_HOME`（含 `sessions/`）→ `users.json` 那条（⚠️ **两个写者**：命令行删了、服务内存里还在 ⇒ **他还能登**）→ **踢掉已连的连接**。
⚠️ **`--dry-run` 先打印清单，主人 `sudo` 执行**（`deploy` 免密不了，删不掉）。
⚠️ 备份按人打包；磁盘 `N × 2.4M` + 每人一份 `DSH_HOME` + `sessions/` **只涨不降** ⇒ 要**按人配额**。

### 8.5 迁移顺序（他给的，与控制面那份**互相印证**）

1. **服务端先接线**：路由只从请求的 `sub` 取，未就绪明确回"未就绪"；
2. **再发客户端**：命名空间 + 清缓存 + 等待态 + 缺字段兼容；
3. **最后翻开关并作废旧令牌**（旧客户端撞 401 ⇒ 走既有的"清缓存回登录页"）；
4. **翻之前先备份**。

### 8.6 判据（他给的，**都很可执行**）

* 正：以 A 的令牌启动，缓存键里只出现 `…v1.<subA>`；
* **负**：换 B 启动 ⇒ 断言行上没有 A 的 seq 与文本、A 的键已清；再跑 `check-web-browser --shot` ⇒ **截图里没有 A**；
* 服务端：A 的时间线里没有 B 的 seq；用户改成别人 ⇒ 403；
* 等待态：造 `creating` ⇒ **有等待页、没有输入框**；改 `ready` ⇒ 消失；**断言没有百分比**；
* 注销：`--dry-run` 项数 = 实测项数；删后**登不进、目录不在**、`df` 回收 ≥ 卷大小（**删之前先能登** = 负向对照）；§13.4 搬家前后 **sha256 一致**。

### 8.7 ⚠️ 他与**已冻契约**冲突的一处（要主人裁）

他说 key **"可填，只经服务端写进他的空间"** —— 而契约 `37-MULTITENANT.md` §二② 冻的是
**"网页终端，用户自己在容器里配 ⇒ 不经过我们"**。**两条对不上**，这一条**只有主人能定**
（第三位的方案会让 key 经过我们；已冻的方案不经过）。

---

## 八·补、✅ 客户端那一半也落地了（2026-09-21，§八.5 的第 2 步）

> §八 那几条判据现在**有闸了**：`test/unit/cache_namespace_test.dart`（9 条）+ 既有那 500 多条。

### 补.1 落成了什么

| 件 | 在哪 | 一句话 |
|---|---|---|
| **从令牌里读出"这是谁"** | `lib/models/token_sub.dart`（新，纯函数） | 解 `<base64url(JSON)>.<签名>` 的**前一段**，取 `sub`。🔴 **不验签**（验签是服务端的事）⇒ 所以把它当**不可信输入**，形状卡死（`^[A-Za-z0-9_-]{1,64}$`，与服务端 `safeUserId` 同一套） |
| **两个缓存都按人分** | `timeline_store.dart` / `draft_store.dart` | `namespace` 不再是 `final`（换个人登录要能换一份）；绑定时机 = **拿到令牌之后、读缓存之前**（`ChatController._bindNamespace`） |
| 🔴 **退出时清"全部命名空间"** | 两个 store 的 `clearAllNamespaces()` | 只清自己那份 = **等于没清**：共用设备上，他退出了，下一个用这台机器的人照样能把那个世界画出来 |
| **档位不清** | `process_level_store.dart`（没动） | 它是**设备级偏好**，按账号分反而让同一个人换台设备就丢设置 |

### 补.2 ⚠️ 一个**兜底值**上的取舍（这一条是踩出来的）

`sub` 读不出来时用哪个命名空间？我第一版选了 `'unknown'`（"谁都不属于"听起来更干净），
**结果把 6 条既有判据弄红了** —— 那些用例里令牌是假的（`'tok'`），
它们**直接拿裸 `TimelineStore()` 去读控制器写的那份**，两边命名空间对不上。

重新想过之后改成 **`'single'`**（= 多租户之前那个默认值），理由三条：

1. 它**不是任何真人的键**（真人的键是 `sub`）⇒ "读不出身份"不会撞上别人的缓存；
2. 它让"读不出"**退回旧行为**，而不是让老设备上的旧缓存凭空消失；
3. 退出登录会把它一起清掉。

🔴 **但它有一个必须配着的闸**：万一客户端哪天**解不开真令牌**了（服务端换了编码），
所有人会**静默退回同一个命名空间** —— 那正是多租户要修的那个串号。
⇒ 所以第一条判据专门盯"**真令牌的形状（不带 padding 的 base64url）解得开**"。
**那条红了 = 兜底正在被大规模使用，不是"兜底很安全"。**

### 补.3 变异验证（4 处，全被抓住）

| 变异 | 结果 |
|---|---|
| 退出时只 `clear()`、不清全部 | 🔴 抓住 |
| 不卡 `sub` 的形状（把不可信输入直接当键） | 🔴 抓住 |
| 解 base64 不补 padding（Node 不打 `=`） | 🔴 抓住 |
| `_bindNamespace` 变成空操作（退回默认那一份） | 🔴 抓住 |

### 补.4 判据跑在哪

* **硬闸**：`bash scripts/check-client.sh` —— 静态分析 / 单元 **258 条** / 可访问性，**三道全过**（提示档也过）。
* **部署**：`scripts/deploy-web-v2.sh` ⇒ 公网 `6926dc6d2819`，11 个引用 0 个 404。
* 🔴 **浏览器那条路（V13）**：`check-web-browser.mjs` ⇒ **页面自己建的 WS 1 条 / 87 帧 / 认出 5 类事件 / 续期换新了**。
  ⚠️ 截图里中文还是方块：这台机器**没有中文字体缓存**（老问题，不是这次改出来的）；
  截图这一眼验的是**结构**（对话在、输入框在、没卡在空白屏），不是字。

### 补.5 ⚠️ 还没做的

* **"新号进来，容器还没好"那个等待屏**（§8.3）—— 登录响应还**没有**容器状态那个字段；
  按契约缺字段要**按"就绪"兼容**，别把老客户端挡在门外。⇒ 这是第 3 件。
* **注销那六步**（§8.4）：按 `sub` 撤**全部**令牌 —— `Auth` 今天**只能按 `jti` 撤**，
  没有"按用户撤全部"⇒ 注销**撤不干净**（老令牌还能用）。这是一处**真欠账**。



---

## 九、✅ 欠账收口（2026-09-21 晚）

### 9.1 按用户撤全部令牌（§8.4 那条"撤不干净"）

`Auth` 原来**只能按 `jti` 撤** —— 也就是"只能撤我知道的那一个"。
注销要撤的是"**这个人签过的所有**"（别处还登着的那些，我不知道 `jti`）。
⇒ 加 `revokeUser(sub)`：**判据在 `verify()` 里**（`iat <= 撤销时刻` 的一律不认）。

⚠️ 两处**语义细节**（都是被测试逼出来的）：
* 用 **`<=`** 不是 `<`：`iat` 是**毫秒**，"签发"和"撤销"完全可能**同一毫秒**；
* **重新签发时把 `iat` 顶到撤销时刻之后** ⇒ **撤完还能重新登进来**
  （否则注销完就把人永久锁在门外）。

### 9.2 🔴 这一条测试**挖出两个既有 bug**（都是"假删除"那一类）

| # | bug | 现象 |
|---|---|---|
| 1 | `#maybeReload()` 从**持久那份**重读状态时**整个覆盖**了 `#revoked`/`#revokedUsers` | **改了密码（触发那次重载）之后，之前撤掉的令牌又活了** |
| 2 | `#load()` **根本没读回** `revokedUsers`（写进去了，读不回来） | **重启一次，被注销的人又活了** |

⇒ 两条都修了（改成**合并**、把运行时的读回来）。
**这正是"假删除"** —— 而它只有在"写一条会失败的判据"时才露出来。

### 9.3 另外两个窄口（§二·补.2）

* **进程级兜底原来只通知主人** ⇒ 改成**发给每一个人**（宿主级事件谁都会被波及）；
* **`sessions/` 只清主人那一份** ⇒ 改成**按人各清各的**（`worlds.all()` 逐个清、
  各自 `try`、清不动不影响别人）。

### 9.4 ⚠️ 收尾时我自己**把线上弄停了两分钟**（如实记）

改 §9.3 第二处时，我把那段"按人清 sessions"的代码留在了 `worlds` **初始化之前**
⇒ 启动直接 `ReferenceError: Cannot access 'worlds' before initialization`
⇒ **`w.stalkerai.cn` 502 了约两分钟**。是 `tail serve.log` 看出来的，已挪到 `worlds.warmUp()` 之后。

⚠️ 教训：**动"启动路径上的顺序"时，改完必须立刻在隔离目录里先跑一次**
（我这次跑完才回头重启线上，本来应该先跑）。
