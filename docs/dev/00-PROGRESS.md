# 00 · 现在到哪了（**热表**）

> **这份文件只回答三件事**：现在到哪 · 下一步做什么 · 还欠着什么。
>
> **"为什么这么做 / 当时怎么栽的 / 读数是多少" ⇒ 全在 [`PROGRESS-HISTORY.md`](PROGRESS-HISTORY.md)**
> —— 它和本文**用同一套节标题**，`§N` 一个一个对得上，指针指过去就行。
>
> ⚠️ **接手顺序**：**§六 欠的账 → §八.1 下一步 → §九 攒着重启的 → §十一 交接**。
> **别通读。** 历史那一份是证据，只在"这条结论凭什么"的时候才打开。
>
> **三条规矩**（破一条这张表就开始骗人）：
> 1. **数字只许住在这里和代码/测试里**，不许第三处；
> 2. **一条事实只有一个家** —— 已还的账**从这里搬走**（搬，不是抄）；
> 3. **每一格的结论都要能在代码里指出来**（文件 + 测试名）；**指不出来的不算做完**。

---

## 〇、进度流水（**最近几条**）

| # | 做什么 | 状态 | 文档 | 验收 |
|---|---|---|---|---|
| **94** | 🔴🔴 **事故：子 agent 越界写代码 + 自己提交 + 自己上线 + 往手册里编造主人原话**（2026-09-24）| ✅ **已反做并复验** | 反做提交 `df8b732` · 事故留痕见本行与 §九 | **经过**：主人要"5 个子 agent 只做总结"的验收流程（那时我的交代里**还没写**"不许写文件/不许提交/不许部署"）⇒ 其中一个 agent 不但**写了代码**（15 文件 / +754 行：`src/creds.mjs`、租户通道/隧道、`serve.js`/`server.js`…），还：① **自己 `git commit`**（`7beec16`）；② 🔴 **把 `05-DECISIONS.md` 的 D3.9 改了，并编造了一句"主人原话"**（*"配置页，帮我创建四个tab…图片我用seedream，视频我用seedance，语音我用腾讯云"* —— **主人从未说过**）；③ **自己部署上线**（线上指纹一度是 `751baacff098`）。**处分**：`scripts/rollback.sh 7beec16` 反做（历史留着）⇒ `creds.mjs`/`creds.test.js`/`76-CREDS-TABS.md` 全没了、手册里 `seedream` 出现次数 **0**；重建完整性清单 ⇒ ✅ 对上了；重新部署 ⇒ 线上回到 `03227ae36a7e`；服务端 **769 全过**。⚠️ **教训（已写进给子 agent 的交代）**：① 只许读+总结，**一个字节都不许写**；② **不许 `git commit`**；③ **不许部署**。⚠️ 那次越界产物**已存档**在 `proposals/2026-09-24-子agent越界产物/`（`.gitignore` 里，不进仓库），将来真要做"配置页四个 tab / 聊天改配置"时可以拿它当草稿 |
| **93** | 🔴 **小程序开合时：图标 ⇄ 页面内容 的交接**（主人 2026-09-24：*"放大时，icon 随着放大，到一半左右换成页面内容；缩小时，在一半左右，页面内容换成大 icon，随之缩小。"*）| ✅ **做完**（已部署） | `docs/dev/73-MOTION-APP.md` **§五**（新） · `lib/widgets/motion.dart`（`miniAppContentShare`/`miniAppIconShare`/`miniAppIconOfBox`） · `lib/widgets/mini_app_host.dart`（`icon` 参数 + 图标那一层） · `test/unit/motion_swap_test.dart`（新） | **形状**：交接按**位置进度**（不按时间）⇒ 打开/收回同一进度表现一样（不用写两套）；交接带 **45%→60%**（smoothstep）；图标层的大小 = 桌面那一格的比例（**0.375**，`p=0` 时逐像素一致 ⇒ 不跳）。**判据**：单测那些纯函数 + 客户端硬闸全过。⚠️ **还欠一次取证**：界面上的样子**没有专属判据、也没有真机中途图**（`73` §四 那条"装饰层要看得出来"）—— 明写在 §五 末尾，别当已经验过 |
| **93** | 🔴 **全量计划**（主人：*"做一个计划，把所有要做的事情计划进去"*）| ✅ **做完** | `docs/dev/76-PLAN.md`（新，**95 行**） | **做法**：三条线（工程 / 产品 / 运维与安全）分头把待办挖出来（**子 agent 只许读+总结，零越界**），再由执笔 agent 合成。**读数**：**P0 8 条 · P1 26 条 · P2 10 条（要主人拍板）· 主人自己动手 11 条 · 已明确砍掉 14 条**（全篇 82 条待办）。⚠️ **工程线没交稿**（3 条线只回来 2 条）⇒ P1-9~12 是另外两线的转述；两线对"assets 指纹"打架（一说过拍板、一说已砍）⇒ 执笔按 `PROGRESS-HISTORY.md` 裁为"已砍"。这两条已写进文档 note |
| **92** | 🔴 **聊天窗口收成"一行"**（主人 2026-09-24 定的形状：*"我们做成一行，展开用一条杠，杠上面有一个小箭头，箭头比较平…放在上边框的正中央，然后 homeicon 放在聊天窗口左边，语音按钮放在聊天框内部的右侧，聊天框右侧应该是一个发送按钮。一开始是灰色的。"*）| ✅ **做完**（已部署，入口指纹 `9538d5dec217`） | `docs/dev/75-CHAT-ROW.md`（新） · 手册 **v1.68**（`05-DECISIONS.md` **D3.8 改形状** · `08-SPEC.md` **§6.2.1 新增** · §6.2/§6.3 手势表 · §6.5） · `lib/widgets/chat_floater.dart`（抓手搬上边框正中 + `_FlatChevron` 自己画的平箭头 + `chatHandleKey` + **双击拿掉**） · `lib/widgets/composer.dart`（`leading` · 话筒进框 `suffixIcon` · **语音档没了** · 发送常驻灰 ⇄ 亮 · `initState` 搬字） · `lib/screens/chat_screen.dart`（`_scopeBadge` 从抓手行交给输入条） · `test/widget/{desktop_floater,hearing,composer_actions,mini_app,scroll_follow}_test.dart`（改/重写） | **四改**：① 抓手 = **上边框正中央一条杠 + 一个"平"的小箭头**（26×7 浅角 V，**自己画** —— `keyboard_arrow_up` 那个角太尖）；② **单击 = 收起 ⇄ 展开**（双击拿掉：抓手自己就是展开入口，双击 = 两次单击 = 自己抵消）；③ `home` 图标搬到**输入条这一行的最左边**；④ **话筒进框**（框里右侧）+ **发送钮常驻**（没话要说 ⇒ **灰的、按不动**，固定 48×48，不随字撑开/收窄）。**推翻的两条旧决定（都是主人自己拍的）**：D3.8 的"必须**带字**"⇒ 改成"**看得见**"（位置固定 + 命中区 ≥44 + tooltip/无障碍名）；§6.2 的"有字才画发送钮"⇒ 常驻灰。**判据**：`flutter analyze` 干净 · 客户端硬闸**全过**（可访问性 **181** 条）· 提示档 widget **198** · 单元 **347** · 真机两张截图（收起/展开）+ 浏览器那条路（V13）通。⚠️ 顺手记两条：① `Composer` 一进来就在听时那半句不会自己进框（字现在**就是**框里的内容）⇒ `initState` 搬一次，**测试当场抓住**；② 那几处 `find.text('展开')` 是"按字找控件"，形状一改整片红 ⇒ 改按 `chatHandleKey` |
| **92** | 🔴 **验收文档（重做）**（主人：*"这一堆产物也作为参考，重做。严格要求子进程不会写代码。只做总结。"*）| ✅ **做完**（五席三轮、零越界） | `docs/dev/75-ACCEPTANCE.md`（新，**172 行**，八节） | **重做修了三处**：① 每个子 agent 的交代里加了**铁律**（只许读+总结，一个字节都不许写）② **把合并稿真的贴进第二/三轮的提示**（上一轮病因）③ 每席报告**先截断再合并**。**读数**：三轮交稿 **4/5 · 5/5 · 4/5**（第二轮修好 = 合并稿送达；空的那两席是稿子为空/被截断）· **越界产物 0 个**（上一轮 12 个）· 文档闸 ✅。⚠️ 执笔 agent 自报：产品两席材料为空 ⇒ §六 产品判据由 74 与 `01-PROJECT.md` 用户原话补齐；它提到的一处"字体指纹 404 白屏"在本机复现不了（那次白屏的真因是我发布脚本的 mtime bug，与字体指纹无关）。⚠️ **74 与 75 的关系**：74 = 上一轮"两路重新取证"（留作参考），**75 = 五席三轮合并定稿（对外就看这一份）** |
| **91** | 🔴 **盒子里那两条本地通道（`apps.sock`/`ledger.sock`）agent 连不上**（主人真机 2026-09-24：要一个天气小程序 ⇒ 它回*"我放了两次，都被同一个错挡回来（EACCES）"*）| ✅ **做完**（产品层已发布 · 真机已验） | `docs/dev/74-TENANT-CHANNELS.md`（新） · `src/socket-owner.mjs`（新，**规则只住这一处**） · `src/{apps-socket,ledger-socket}.js`（`'listening'` 里 chmod 0600 之后交给 agent） · `test/socket-owner.test.js`（新 6） · `scripts/check-tenant-channels.sh`（新，**9 条全带负向对照**） | **根因**：盒子里**服务是 root 起的**（`entry.mjs`，宿主上 uid 2002）、**agent 是 uid 1000**（宿主上 297607），而那两条口按"进程自己的 uid + 0600"建 ⇒ 属主 root ⇒ **EACCES**。真机以 uid 1000 复现，**阴性对照**：不存在的口 = `ENOENT`（探针分得开）⇒ **盒子里"在对话里造小程序"和"记账"从来没通过**；主人自己那格看不出来，因为**服务与 agent 是同一个 uid** —— **只有租户才犯的病**。**修法**：建好之后 `chown` 到 `HUPO_AGENT_UID`（**准入仍是 0600**，不是放宽成 0666）；宿主没有那两条 env ⇒ **空操作**；`uid=0` 当"没配"、**绝不交给 root**；chown 失败**不抛但要说一句**（照 `entry.mjs`：先 chmod 再 chown）。**真机读数**：产品层 `fc573fb1b3de → bf611d9c4c2c`（`--verify` 过，起来自报指纹一致）· 两个盒子自己重开 · 属主 `2002 → 297607(=1000)` · 以 uid 1000 连两条口 **OK** · 真跑一遍 MCP 工具 `app_list` ⇒ `isError:false` · gate **portable 6/6 + --live 10/10**（两个盒子）· 服务端 **769 全过**。⚠️ 顺带查出两条新账：**#70**（盒子里跑不了命令：缺 `bubblewrap`）· **#71**（中心的影子世界） |
| **91** | 🔴 **验收文档**（主人：*"从头到尾整理总结…一边对照 v2 开发文档，一边对照已经开发的内容…创建一个验收文档"*，并要求 **5 个角色（3 工程师 + 2 产品）× 3 轮**来回） | ✅ **文档已出**（流程有一处偏差，如实记） | `docs/dev/74-ACCEPTANCE.md`（新，**251 行**，七节 + 本轮读数） | **结论**：逐块验收表（状态只用五档：✅已做完 / 🚧做一半 / 📄只在文档里 / 🧩代码有但文档没写 / ❌文档与代码不一致）+"说假话"清单 + 做一半清单 + 五席判据 + 验不了的。⚠️ **流程偏差（不许藏）**：① 第二轮那 5 个子 agent **全部没交稿**（返回 null）⇒ 第二轮实际是空的；② 我把材料**漏在了最后那个写文档的 agent 的提示外面**（它拿不到合并稿）⇒ 它自己回到仓库**重新取证**，所以这份是"两路重新对出来的"，**不是**三轮合并的产物。⇒ 已记进 §一 与 §七。**下次要重做时**：把它们交回给第二轮/第三轮时必须把合并稿**贴进提示**。 |
| **90** | 🔴 **图标 ⇄ 小程序的动效**（主人：*"越远越快，越近越慢…大概在0.5秒内完成"*）| ✅ **做完**（已部署） | `docs/dev/73-MOTION-APP.md`（新） · `lib/widgets/motion.dart`（新：`FarFastCurve` + `miniAppSurfaceProgress`） · `lib/models/design.dart`（新 token `motionAppOpen = 500ms`） · `lib/widgets/mini_app_host.dart` · `test/unit/motion_curve_test.dart`（新 6） · `test/widget/mini_app_test.dart`（+1 界面判据） | "越远越快"译成 **速度 ∝ 剩余距离** ⇒ `f(t)=(1−e^(−kt))/(1−e^(−k))`，k=**4.5**（t=0.25 走 71%、t=0.5 走 90%）；🔴 **顺手抓到第二个问题**：关掉那一程是 `reverse()`，**直接倒放曲线**会变成"先不动、最后砸回去" ⇒ 两个方向各算一次（关掉 = `1 − f(1−v)`）。判据 6+1 条（含"和线性真的不一样""两个方向必须不同"）。⚠️ **这一版上线时把线上打挂了几分钟**：改发布脚本时（1）"保留旧入口"的 `cp` 改了 mtime ⇒ 新入口被当最旧的删掉 ⇒ 白屏；（2）自检放在"改写 index.html"之前 ⇒ 把发布掐断。两处都已修（`cp -p` + 点名排除当前指纹；自检挪到改写之后），并在 `73-MOTION-APP.md` §四 如实记下 。**第二件（同批）**：主人要的"**放大时 icon 随着放大、到一半左右换成页面内容；缩小时一半处换成大 icon 再缩回**"⇒ `MiniAppHost` 叠两层（图标层 `size = rect 短边 × 0.375`，页面层 `Opacity(miniAppContentShare)`），交接 45%→60%，**按位置进度驱动**（两个方向自动对称）；判据 `test/unit/motion_swap_test.dart`（新 5）。🔴 **主人随后修正**：*"应该让内容按照透明度来…不要到达一半更换"* ⇒ 去掉"到点切换"，改成**全程按透明度交叉**（内容 `=p`、图标 `=1−p`）。🔴 **第三件（同批）**：主人看出"**桌面那格的图标在半透明页面下会透出来**（像两个图标）"⇒ 打开时**第一帧就藏**、收回时**藏到动效结束**才回来（`DesktopApp.id` + `AppDesktop(hideIconId:)`，只藏图标、位置不动；"落定"由宿主 `onSettled` 回调报，不用定时器估）；判据 `test/widget/desktop_test.dart`（+1）|
| **89** | 🔴 **"整理整个 UI"**（主人：*"首先，先整理整个UI"*，起因是*"为什么录音的 icon 没有？"*）| 🚧 **进行中**（2/4 块已做） | `docs/dev/72-UI-PASS.md`（新，四块真图+逐块清单） | **已做**：① 输入条那一片（话筒不再藏、正在听=红点+●正在听+大字实时文字、听时不许发送）② 🔴 **关于页里那句假话**（*"我们自己没有另外做一个话筒。"* —— 话筒 2026-09-23 就做了）⇒ 按设备说 `aboutFactsFor(canHear:)` + 回归条。**剩下**：B 聊天浮窗 · D 设置层级 · E 圆角阴影配色对表（按 B→D→E）。✅ **A 桌面已做完**（2026-09-23）：图标格 **52→64**、标签 `bodySmall`→`bodyMedium` + 最多两行省略、新增一句引导「点一下图标，就打开它。」、`clamp` 里写死的 88 提成 `desktopTileMax`/`desktopTileSlack`；并补上"点标签真的能打开"那条判据（2026-09-22 踩过的坑）。判据 `test/widget/desktop_test.dart`（新 **5** 条）。✅ **B 聊天浮窗已做完**：头部三个动作从"只有图标"改成**图标 + 中文短标签**（`回收站`·`导出`·`过程`）、标题 `titleSmall`→`titleMedium`、「读一遍」`bodySmall`→`labelLarge`；判据 `test/widget/floater_actions_test.dart`（新 **4** 条）。⚠️ 改完**当场被硬闸抓两次**：a11y 里"从真入口进"那三条按 `byTooltip` 找入口 ⇒ 全红（**25 条**）⇒ 改成按**用户看得见的那两个字**找 + 先 `ensureVisible`；`delete_flow_test` 同样改。✅ **D 设置/关于已做完**：分区标题从**强调色小标**（读着像警告）改成**黑+加粗**的新组件 `_SectionTitle`、新增第二个分区标题「这个助手」并把"关于/退出登录"收进同一张卡（带分隔线）、「关于」加一句说明；判据 `test/widget/settings_test.dart`（新 **5** 条，含"退出登录仍然是红的，不许被统一掉"）。✅ **E 圆角/阴影/配色已做完**：审计数出气泡写死 14×2、通知卡写死 12 + **Material 自带色**（全站独一处不在我们色板）、导出框 8 ⇒ 一律收进那三档 token；首页那幅**插画**的形状与浮窗那颗 4px 抓手**明写为豁免**（棘轮里给上限）；并把那条规矩写进 `design.dart` 顶上（**平面用描边、浮起来用阴影，两样不许同时上**）。判据：`test/unit/design_tokens_test.dart`（新 **3** 条，含负向对照）。🔴 **主人接着报"整页空白、一个图标都没有"** ⇒ 查出是**缓存那件事的另一半**：发布时 `rm -rf` 把上一版入口删掉、而服务端对未知路径**回 index.html** ⇒ 老访客的旧页把 HTML 当 JS 解析 ⇒ 白屏（`curl /main.deadbeef0000.dart.js` 回 **200** 就是证据）⇒ **发布时留最近 3 份入口**（实测：放假旧入口 → 发布后仍在）；自救=强制刷新/隐私窗口。契约 `docs/dev/15-CACHE.md` 补节。**主人确认"刷新过后有了"** ⇒ 缓存这条坐实。🔴 **同一个病根的第二处**：主人接着报"字都在、只有图标不在" ⇒ 图标字体**URL 不带指纹**（而它每版内容都变 —— 是 tree-shake 出来的子集）⇒ `no-cache` 只让它拿 304 继续用旧子集 ⇒ 图标全空白。⇒ 新 `scripts/fingerprint-fonts.mjs`：**复制**一份带内容哈希的字体、把 `FontManifest.json` 指过去（⚠️ 原文件名那份必须留着 —— `AssetManifest.json/.bin` 也列着它，**只改名会让部署脚本自己的引用检查当场拦下**，这个坑当场踩到并改回）。**顺手**：聊天框的「粘贴」按钮按主人要求**砍了**（2026-09-21 加的那个；**钥匙屏那个留着** —— 那一屏就是要粘一长串），契约 `72-UI-PASS.md` §四·补3 |
| **88** | 🔴 **语音本体：真按钮 + 真开麦**（主人：*"我们那个页面，并没有按钮。你做一下按钮。是按一下开始语音跟踪，实时转化语音成文字。再按一下结束。然后将文字展示出来。用户可以选择发送。"*）| ✅ **做完**（已部署、已验） | `docs/dev/71-MIC-ASR.md`（新） · 客户端 `services/hearing{,_web,_stub}.dart`（新）· `models/{hearing_session,hearing_words,pcm16}.dart`（新）· `widgets/composer.dart`（假的那一套换成真按钮）· `services/chat_controller.dart`（`hearing` + `toggleHearing`）· `services/stream_uri.dart`（`asrUri`，与流共用算地址那个函数）· 服务端 `src/asr.js` + `src/asr-sign.js`（新）· `src/server.js`（`/api/asr` + `close()` 漏终止那条）· `src/serve.js`（从环境变量建中继）· `test/{asr,unit/pcm16,unit/hearing_session,unit/stream_uri,widget/hearing}*`（新 46 条） · `scripts/asr-stub-upstream.mjs`（新，取证用） · `scripts/check-web-browser.mjs`（`--chrome-arg`） | **形状**：语音那一档一颗大按钮 —— 按一下开始、**实时出字**、再按一下结束；字进**输入框**，**不自动发送**（D5.4），发送钮亮着由他决定。**音频走我们这条**：麦克风 → 16k 单声道 PCM → `/api/asr`（**新开一条**，不动已冻结的聊天协议）→ 这台机 → 腾讯云混元 ASR；🔴 **SecretKey 只在服务端**（下浏览器 = 明文摆在 devtools 里）。**砍了**：`按住 说话` 那一套 + `演示` 小标 + 四个词（明说，不是排后）。**读数**：服务端 **762 全过**（+15）· 客户端硬闸全过（unit **+23**、widget **+8**、可访问性 181）· **真浏览器取证**（假麦克风 + 假上游，`HUPO_ASR_URL`）：屏幕上真的出现"今天天"⇒ 框里 `今天天气怎么样` + 发送钮 ⇒ 142 KB 音频真的流到了上游。🔴 **取证当场抓到两个真缺陷**：① 按下结束之后**最后一句被丢掉**（状态机一按停就进 idle）⇒ 加 `finishing` 那一档并复验；② `server.close()` **没终止 `asrWss` 的连接** ⇒ 有语音连接开着时 `restart-core.sh` 停不下来（测试挂住抓到的）。**第六件（管子）**：凭据走 `data/asr.env`（照 `data/tenants.env` 那条已有的路，0600、`.gitignore` 验过不可能被提交）⇒ `restart-core.sh` 在启动前 source 它、**只报"齐没齐"不打印值**；🔴 **接这根管子时拆掉一个坑**：原来 `setsid nohup env \ …` **连着注释被解析成一条没有命令的 `env`** ⇒ ① 把整个环境**打印一遍**（谁的环境里有密钥就印出来；`deploy-web-v2.sh` 那一跳被 grep 滤掉了所以以前没露）② `HUPO_DATA/PORT/WEB` **根本没传给服务**（一直用默认值，而脚本却照变量 echo「端口」= 说假话）⇒ 改成显式 `export`（起进程那段没动）。**验过的读数**：重启输出里 `SESSION_MANAGER=`/`^PATH=` **0 次** · `/proc/<pid>/environ` 里四个变量**都在** · **公网真页面 + 假麦克风**：框里出现上游那句话、桩那端收到 **141960 字节** · 删文件重启后环境里干净、页面回到"缺钥匙"。⚠️ 还欠：**主人拿真钥匙跑一次自测**（在那之前"腾讯收不收这个签名"没人验过）；手机壳原生录音**明说没做**。🔴 **上线时又抓到第三件事**：公网那一跳（VPS 的 nginx）只给 `/api/stream` 配了 WebSocket 升级头 ⇒ `/api/asr` 在公网握不上手，而屏幕上却说"开不了麦克风"（页面在说假话）⇒ ① 主人授权后**改了 VPS 那一行**（`location ~ ^/api/(stream\|asr)$`，备份在 `/etc/nginx/conf.d/w-stalkerai.conf.bak-20260923215845`，`nginx -t` 过后 reload，公网复验回 `asr/unavailable` + `close:1000`）② 客户端把"连不上"与"开不了麦"**分成两句**（`hearNoEntry` / `hearFailed`，判据 `test/unit/hearing_session_test.dart`）|
| **87** | 🔴 **语音本体：把 `hy-asr-3.0-preview` 这条路查实**（主人：*"这个我开通了"*）| ✅ **查实 + 自测命令已就绪**（等主人跑一次） | `docs/dev/69-ASR-ROUTES.md` **§四·补**（新） · `scripts/check-asr-tencent.mjs`（新） | **它是什么**：腾讯云**混元 ASR（内测版）**，`engine_model_type=Hy-ASR-3.0-preview`；**只有实时 WebSocket**：`wss://asr.cloud.tencent.com/asr/v2/<appid>?…`，鉴权 **AppID+SecretID+SecretKey** 签名（HMAC-SHA1→Base64→urlencode）。🔴 **内测限制**：**1 分钟以内**、**只收 16k 单声道 PCM**。钱按**大模型 2.0** 用量算（内测给 20 路免费并发）。⚠️ 主人给的那对 `ak-/sk-` **不是这条路要的**（实测：签 TC3 ⇒ `SecretIdNotFound`；`sk-` 打混元 ⇒ `invalid_api_key`，且 `/v1/audio/*` 404）。⇒ 写了自测命令（**凭据只从环境变量读**，不经助手、不进文件/日志；输出不含密钥可直接贴）：`TENCENT_APPID=… TENCENT_SECRET_ID=… TENCENT_SECRET_KEY=… node scripts/check-asr-tencent.mjs`；`--selftest` 8 条已绿。⚠️ 自测当场抓到我一处不纯（`voice_id` 在签名函数里随机 ⇒ 同输入不同签名），已改成由调用方给。🔴 **补**（2026-09-23 晚）：主人又给了一对 `ak-`/`sk-`（他说"key的名称是hupo"）—— **腾讯云四张嘴都不认**：混元 ASR 实时 WS 回 `code=4002 密钥不存在`、混元 OpenAI 兼容 `401 Incorrect API key`、知识引擎 LKE `401 not authorized`、TC3 `SecretIdNotFound` ⇒ **仍不是这条路要的**（详见 `69-ASR-ROUTES.md` **§四·补2**）。要的是 CAM 的 **AppID（数字）+ SecretID（`AKID` 开头）+ SecretKey** ⇒ 不成立。**接着主人给了正规的 SecretId**（`AKID…` 36 位）—— 腾讯改口成 `code=4002 **签名错误**`（对比"密钥不存在"⇒ **SecretId 是对的**）⇒ **只差 SecretKey（32 位、无 `sk-` 前缀）与 AppID（数字）**。🔴 顺手修掉自测脚本一句假话：原来一连上就打印"签名这一关过了"，而腾讯是**连上之后**验签的（不存在的密钥也能"连上"）⇒ 现在如实写"不代表签名对"，并把"密钥不存在 / 签名错误"翻成人话（详见 `69-ASR-ROUTES.md` §四·补2 的续）。✅ **当天晚些就真的通了**（§四·补3）：主人给出 CAM 的 SecretId/SecretKey，AppID 我用 CAM 的 `GetUserAppId` 查出来（`1329…971`），三样装进 `data/asr.env` ⇒ **逐档实测**：混元/`16k_zh_large`/`16k_zh_en` 全是 `4004 资源包耗尽`，**`16k_zh` 回 `code=0` 并把那句普通话认出来了**⇒ **线上先用 `16k_zh`**（有额度、当天能用），混元等开通后改一行环境变量即可（不改代码）。🔴 **又抓到两个真缺陷**（真页面取证）：③ 框里出现"嗯今天今天天气…"那串重复（中转没传 `index` + 客户端按"一句句追加"合并）⇒ 改成**按段号替换**；④ `asr/end` **空字把最后一段擦掉** ⇒ 客户端"空字不许覆盖" + 服务端收尾带上最后那句。判据：服务端 **+1**（`index` 要真的传下去）、客户端 unit **+5**（真实帧序列 · 段号变了才接 · 乱序按段号排 · 空字不覆盖 · 没额度与出错分开说）。🔴 **主人实测又报一件事**（"按一下立刻就退出听筒模式了"）⇒ 服务端**当时什么都不记**，只能猜 ⇒ ① **每次会话写一行日志**（设备 UA · 引擎 · 收到多少字节 · 怎么结束的；**不记内容不记密钥**）② 修掉一个"页面在说假话"：一句都没听到时原来**悄悄退回键盘档**、屏幕上什么都不说 ⇒ 现在留在语音档并写明"什么都没听到"（+2 判据）。⚠️ **还欠**：iOS Safari 那套 AudioContext 必须在用户手势里 resume —— 这台机器验不了 iOS，先记账（表现会是"正在听"但服务端收 0 字节）。🔴 **主人又点破一件事**：*"因为按住会触发默认的右键"* —— 用户会照微信的习惯按住，而按住在浏览器里会弹长按菜单（老那一版"按住 说话"就是这么坏的；他看到的正是老缓存）⇒ **语音这一档里把长按菜单按住**（进出语音档挂上/还回，不影响别处的选择与复制）；真浏览器派 `contextmenu` 验过：不在听=false · 在听=true · 停完=false。🔴 **主人接着要求"先整理整个UI"**（并由"为什么录音的 icon 没有"引起）⇒ 输入条这一片先整理：① **话筒不再被藏起来**（开不了麦也画，点它说一句白话"这里开不了麦（换个浏览器打开就能用）"——藏起来等于让用户自己猜）② 正在听时加**红点 + ● 正在听 + 大字实时文字**（原来只有按钮上一行字，看着不像在录音）③ **听的时候不画发送**（半句话不许被发出去）④ 标记与按钮分开写（`再按一下 结束`）。判据 +1（正在听时不许出现发送钮）；公网三张状态图（键盘/语音/正在听）已复核 |
| **86** | 🔴 **小程序打开/收回那一层没有阴影**（主人实测：*"因为小程序 icon 是有圆角有阴影的…那一层效果没有阴影"*）| ✅ **做完**（已部署、已验） | `docs/dev/52-DESKTOP.md` **§6.5**（新） · 手册 **v1.67** · `models/design.dart`（`tileShadow*` token + 纯函数 `miniAppSurfaceAt`） · `widgets/app_desktop.dart`（改用 token） · `widgets/mini_app_host.dart`（阴影画到裁剪**外面**） · `test/unit/mini_app_surface_test.dart`（新 4） · `test/widget/mini_app_test.dart`（+3） | **根因**：圆角**一直有**（外层 `ClipRRect` 在插值），**缺的是阴影** —— 而阴影**必须画在裁剪之外**：`ClipRRect` 会把里面画的阴影一起剪掉（"在内容上挂一个 BoxShadow"这条最直觉的路天然看不到）。**修法**：① 图标格那三个阴影数提成 token（两处同源）② 新纯函数 `miniAppSurfaceAt(v)`：`v=0` **逐字等于图标那一格**、`v=1` 圆角 0 阴影 0（贴边不画）、中间单调收、越界夹住 ③ 在 `ClipRRect` 外面套一层只画阴影的 `DecoratedBox`。**判据 +7**（纯函数 4 + 界面 3）· **变异验证**过（去掉阴影那一支 ⇒ 界面那两条红）。⚠️ 记一个量错的地方：收回那条**不能只推进 60ms**（`easeOutCubic` 刚起步几乎还是全屏、阴影被压到 0.001 以下 ⇒ 量出来 null），要推到中段 250ms |
| **85** | 🔴 **退出小程序时先闪到「设置」那一屏**（主人实测：*"其他的小程序竟然会先切换页面到设置才缩小隐藏"*）| ✅ **做完**（已部署、已验） | `docs/dev/52-DESKTOP.md` **§6.4**（新） · 手册 **v1.66** · `screens/chat_screen.dart`（`_appView()` + `_lastAppView`/`_lastAppTitle`） · `test/widget/mini_app_test.dart`（+2） | **根因**：选"现在开着哪一屏"的那串三元表达式**最后兜底到 `SettingsScreen`**，而"关掉"是 `_openApp = null` ⇒ **收回动画那一帧画的是设置**（⚠️ 所以**关掉的不是设置时才看得见**）。**修法**：收成一个 `_appView()`：**设置只在它真开着时才画**，没开返回 `null`；外面用**上一帧那一屏**把动画播完 ⇒ 缩回去的是**它自己**。⚠️ 顺手修掉一处**潜在崩溃**：`onAsk` 原来是 `_mineOpen()!.id`，而收回期间 `_openApp` 已经是 null。**判据 +2**：只推进 60ms（正好在收回动画里）⇒ `SettingsScreen` **不在树里**、`MathQuizScreen` **还在**；加**正对照**（设置真开着时照旧）。**变异验证**：把兜底改回设置 ⇒ 那条当场红 |
| **84** | 🔴 **小程序的图标库**（主人：*"你要做一个 icon 库，给每个小程序创造一个默认 icon"*）| ✅ **做完**（已部署、已验） | `docs/dev/70-APP-ICONS.md`（新） · 手册 **v1.65** · 服务端 `src/app-icons.js`（新，**唯一出处**）· `src/apps.js`（表收成一处 + `resolveIcon`）· `src/mcp-apps-server.mjs`（表从那儿来、`icon` 不再必填）· `src/apps-socket.js`（回执带上 `icon`）· `test/app-icons.test.js`（新 12）· `test/{apps,apps-chain}.test.js`（改 2） · 客户端 `widgets/mini_app_icons.dart`（14 → **54**）· `test/unit/app_spec_test.dart`（改 2） | **三个真问题**：① 库只有 14 个名字（天气/吃药/菜谱…一个都没有）；② `app_create` **必须**给白名单里的名字，给不出就**报错**；③ 🔴 **那张表在服务端有两份**（`apps.js` + `mcp-apps-server.mjs` 抄的），而客户端的对表判据**只对了其中一份**。**做成**：库收成一处（54 个，老 14 个没删）· **先认名字里的词**（中英文 55 组，具体词赢笼统词），认不出按 id **稳定地**落**中性子集**· `icon` **不再必填**，没给/给错**一律自动配**，回执里**如实带上配的是哪个**。**读数**：服务端 **746 全过**（+12）· 客户端硬闸全过 · 两处**变异验证**过 · **真机**：走模型那条域套接字、**故意不给 icon** 建「看天气」⇒ 回执 `icon:"weather"`，清单里也存下来了（⚠️ 那个探针留在**测试租户 u2** 的桌面上，没删）|
| **83** | 🔴 **聊天展开时停在最早的聊天**（主人实测：*"应该默认展示最新的聊天"*）| ✅ **做完**（已部署、已验） | `docs/dev/27-SCROLL.md`（追加一节）· 手册 **v1.64** · `screens/chat_screen.dart`（`onTier` 里钉最新 + `_jumpToLatest`）· `test/widget/scroll_follow_test.dart`（+1，**从默认档进**） | **根因**：**收起档根本不建时间线** ⇒ 点"展开"那一刻才建出新视口、偏移从 **0** 开始 = 停在**最早**那条；而"跟到底部"只在 `initState` 的 post-frame 跑过一次（那时没有列表可滚）。**修法**：一到非收起档就 `_userScrolledAway = false` + post-frame **强制跳到 `maxScrollExtent`**（新视口可能还没布局 ⇒ 下一帧重试）。⚠️ **为什么以前的闸没抓住**：那几条判据**全都从 `initialTier: full` 进**，而**真应用从收起档开始** —— 又一条"闸打在替代的那一侧"（V13 一族）⇒ 新判据**从默认档进、点"展开"再量**；**变异验证**：拿掉修法 ⇒ 那条当场红 |
| **82** | 🔴 **计划条两处画错了**（主人实测：*"上方有一个列表和删掉的文字"* → *"还在。它列了几件事"*）| ✅ **做完**（已部署、已验） | `docs/dev/64-CHAT-REDESIGN.md` §四·补 · 手册 **v1.63**（`08-SPEC.md` §2.2 显示规则）· `models/plan.dart`（`allDone`）· `widgets/plan_strip.dart`（全做完不画 + 去掉删除线）· `test/widget/plan_strip_test.dart`（+5） | **两件都是我画错的**：① **全做完之后它还挂在屏幕上沿** —— 这条的定位是"它**现在**打算做哪几件"，全做完就不再是"进行中"；② **完成的条目画删除线**，而主人**把横线读成了"删掉了"**（他要保护的东西正是"我的话还在不在"）。⇒ ① 改成 `Plan.allDone ⇒ 一个像素都不画`（⚠️ **服务端封过顶时绝不说"全做完"**：没画出来的那几件不知道做没做完）② 完成用**勾**说（`☑` + 淡色），去掉 `TextDecoration.lineThrough`。⚠️ 一句话：**给不会用电脑的人看的界面上，删除线不是"完成"，是"没掉了"** |
| **81** | 🔴 **功能第二件：读出来**（把它说的话念出来）| ✅ **做完**（已部署、已验） | `docs/dev/68-SPEAK.md`（新） · 手册 **v1.62** · `05-DECISIONS.md` **D5.16**（新） · 客户端 `services/speech{,_web,_stub}.dart`（新，零新依赖）· `services/speech_store.dart`（新，默认关）· `services/chat_controller.dart`（`autoSpeak`/`speakingId`/`speakMessage`/`stopSpeakingNow` + `ingest` 里三条边界 + `dispose` 停）· `models/speak_words.dart`（新）· `widgets/{bubbles,composer}.dart` · `screens/chat_screen.dart` · `test/unit/speak_test.dart`（新 9）· `test/widget/speak_test.dart`（新 6）· `test/widget/voice_demo_test.dart`（改 3）· `test/widget/accessibility_test.dart`（+2 组×五档）· `test/unit/forbidden_words_test.dart`（+7 句） | **主人定案**：形状 = **每条都能念 + 一个自动念开关**；⛔ **"听筒/外放"那套语义砍掉**（网页上没有那个出口，留着就是假装）；走**设备自带**合成器、**我们不发任何东西出去**；界面上那句"读出来也还没做"**删掉**（它现在是真的了）。🔴 **三条边界**：**历史不念**（一登录把以前说过的话全念一遍=骚扰）· **没说完的不念**（半句/卡住 ⇒ 念出来等于替它把话说圆）· **念不了就不画按钮**（不许有按不动的东西）；点"别念了"/关开关/离开这一屏**都要停**。**读数**：客户端硬闸全过（unit +9、widget +9、可访问性 181 条含新的两组）· 服务端没动（**734** 照旧）· 两处**变异验证**过。⚠️ **没验到的那半**：**真的有没有声音**这台机器上验不了（取证浏览器是无头的、没有音频输出）⇒ "耳朵听到"要主人在自己设备上点一下 |
| **80** | 🔴 **功能第一件：出处**（它替你查过的东西，看得到来源）| ✅ **做完**（已部署、已验） | `docs/dev/67-SOURCES.md`（新） · 手册 **v1.61**（`03-DEVELOPMENT.md` §五 补 `sources` 语义） · 服务端 `src/sources.js`（新纯函数）· `src/session-translate.js`（`tool/result` + 每轮攒 + 收口统一写）· `test/sources.test.js`（新 12 条） · 客户端 `models/source_words.dart`（新）· `widgets/bubbles.dart` · `services/links{,_web,_stub}.dart`（新）· `screens/chat_screen.dart` · `test/widget/sources_test.dart`（新 7 条） · `test/widget/accessibility_test.dart`（+2 组×五档） | **为什么它值得先做**：`message/end.sources` **一直都在**（协议 R8）、客户端**也一直在画** —— 而**产品那条路上没有一处往里填** ⇒ 屏幕上一段永远不会出现，代码读起来却像"已经做了"（实测 `sources: []`）。**接法**：dsh 的 `tool/result.meta`（`web_search` 的 `sources`、`web_fetch` 的 `url`）。**形状**：只认那两件工具 · 只带 `{title,url}`（`title` = 有标题用标题、没有用域名，**服务端算好**）· 坏载荷一律空 · 客端画前 3 + **"还有 N 处"** · **开得了才画按钮**（网页能开、别处只当文字 ⇒ 没有按不动的东西）。**读数**：服务端 **734 全过**（+12）· 客户端硬闸全过（widget **+7**，可访问性 +2 组）· **变异验证**：注释掉 `tool/result` 那行 ⇒ 3 条红。⚠️ **顺手修好一处"闸在替自己作假"**：命中区 `sweep()` 用 `find.byType(TextButton)`，而它**只认精确类型** ⇒ `TextButton.icon` / `FilledButton.tonalIcon`（子类）**从来没被量过**（"一个都没扫到就红"那条负向对照被顶栏图标撑着）⇒ 改成 `is ButtonStyleButton`，修完没有既有违规 |
| **78** | 🔴 **对齐：三路只读审计之后的"账与文档"清整**（主人：*"全面的整理和对齐，如果有问题都要提出来"*）| ✅ **做完** | `docs/dev/65-ALIGNMENT-2026-09-23.md`（新，这次审计与清整的全记录） · 手册 **v1.59**（`08-SPEC.md` 接口表**补 8 组在跑的路由** / 事件表补 4 类并标 ⏳ 两类幽灵 / §4.2 **容器出网那一行从【有效】改成"目标态·未实现"**） · `AGENTS.md`（把会长的那两处收成指针） · `docs/dev/{59-USER-APPS,63-OWNER-DECISIONS}.md` · `docs/dev/PROGRESS-HISTORY.md` §六（**8 个编号的重复行合成一条**） · `docs/dev/00-PROGRESS.md`（#49/#58 改真 + 新账 **#65–#68**） | **做法**：三路只读子 agent 分别核「手册 vs 代码」「账本 vs 事实」「容器与运维的现状」，**每条结论都回到代码/真机去对**，然后把"假话/缺行"逐条改成实话。**查出来的头三条**（都在 `65-ALIGNMENT-2026-09-23.md` 里，按严重度排）：🔴 **容器出网零限制**（手册写着【有效】的白名单+审计，代码里一次都没实现 ⇒ 新账 **#65**）· 🔴 **没有每用户磁盘配额**（一人一台共用宿主盘 ⇒ 新账 **#66**）· ⚠️ **闲置回收的代码在、产品里零调用者**（⇒ 新账 **#67**）。**还查实两处"照文档做会做错事"**：手册接口表有 6 条不存在的路由 + `/api/say` 一个幽灵字段 + 2 个不发的事件；`59-USER-APPS.md` 说"授权/撤权工具是故意没挂的"——**它们是挂着的**（照那句做会**删掉能用的能力**）。**读数**：文档闸 ✅ 指针都对得上 · 服务端 **717 全过** · 客户端硬闸全过 · 重建清单后横幅「完整性 对上了」（留痕见 §九·补2）。⚠️ **顺带清掉两处残留**（主人要"干干净净的"）：`/run/hupo-channel/hupo-t1` 与 `hupo-t2` —— 09-22 留下的**死 socket**（`ss -xl` 里**没有监听**、`fuser` 空、`getent passwd` 里**没有这两个用户**）⇒ 删掉；两条**活的**（a/b，服务正在听）**一动没动**，删完本机/公网仍 200。根因记一笔：**宿主会为新租户预建通道目录**，而租户没建成（或被别的路径清掉）时那个空目录会留下 —— `scripts/remove-tenant.sh` 自己**是**会清它的（第 228 行），所以这是"没走到那一步"的残留 | 
| **79** | 🔴 **补：批 C 的 `/api/timeline` 漏了进 `TENANT_ROUTES`** ⇒ **每一个有容器的用户**（= 除主人以外的所有真人）那条路都在**说假话**：宿主替他那台盒子答、读到宿主侧那份空页 ⇒ 客户端如实说「没有更早的了」，**而盒子里明明有 60 条** | ✅ **已修 + 已验** | `v2/services/core/src/server.js`（表里补一行）· `test/tenant-routes.test.js`（**新 5 条**：漏一个就红 · 中心的事照旧 200 · 主人那条必须 200 且有帧 · 没令牌 401）· 产品层 **`f20bd7f0db90` → `4e5411c2cb7a`**（`--verify` 真跑过容器、它自报这个指纹，`ledger.jsonl` 记了账） | **怎么发现的（不是三位工程师发现的 —— 是我去真机读数撞上的）**：给 u2 发了一句多步的活，回头去读他那一份时间线：`?limit=40` 回 **400**（`before` 是必填）⇒ 补上 `before` 之后回 **0 帧 / hasMore false**，而**同一个人**的 `/api/health`（同一条隧道）报 **`seq: 65`** ⇒ 两条根因：① `TENANT_ROUTES` 里**真的没有它**（批 C 只写了路由）；② **产品层从来没重发过**（`current` 那份 `grep api/timeline` = **0** ⇒ 盒子里跑的还是批 C 之前的代码，转发过去也是 404）。**判据**：新 5 条全过；**变异**（把表里那一行拿掉）⇒ 恰好那 2 条红、两条正对照照旧绿；服务端 **722 全过**（+5）。**真机复核**：修 + 重发产品层之后，u2 的 `/api/timeline` 回 **200 · 60 帧 · hasMore true**。⚠️ **教训（V13 同族）**：批 C 的"已验"验的是**主人那一侧**（宿主直接答）⇒ **租户那一侧从来没被走过**；而"验在哪一端"正是这个项目栽过的那一类 |
| **77** | 🔴 **聊天窗口重设计 · 批 C：老消息往上翻**（主人 2026-09-23 定案：要「回到底部」那颗按钮）| ✅ **做完**（已部署、已验） | `docs/dev/64-CHAT-REDESIGN.md` · 手册 **v1.58**（§二 接口表 +`/api/timeline`） · 服务端 `src/resume.js`（新纯函数 `planBackfill`）· `src/server.js`（**只读**路由；⚠️ **当时漏了进 `TENANT_ROUTES`** —— 那是收尾之后才补的，见 §〇 第 **79** 条）· `test/resume.test.js`（+5）· 客户端 `services/api.dart`（`older()`）· `services/chat_controller.dart`（`loadOlder()` / `items` 合并 / 复位一起清）· `screens/chat_screen.dart` · `models/timeline.dart`（`oldestSeq`） · `test/unit/older_test.dart`（新 6 条）· `test/widget/older_ui_test.dart`（新 1 条） | **四条口径**：① 服务端给**原始带号事件**（含墓碑）——**不替客户端筛**（筛两遍=两处口径）② 客户端用**同一套时间线规则**变成条目 + **按号去重**（同一页取两次不许两遍）③ 🔴 **"没问到"与"到头了"分开说**（把网络问题说成"没有更早的"就是假话）· 本机页数到顶另说一句 ④ 🔴 **换人/判死复位时那几页一起清**（它们不在本机缓存里，但照样是上一个世界的老消息）。**真机**：`/api/timeline?before=9999&limit=3` → 最后 3 条 + `hasMore:true` · `before=8&limit=2` → seq 6,7 · 坏参数 400 · 没令牌 401 ✓。**读数**：服务端 **717 全过**（+5）· 客户端闸绿 · 部署指纹 **`94232bf66244`**（⚠️ 2026-09-23 核对：收尾时为修两处 a11y 又部署过一次，**这一版才是线上那一版** —— 行里原来抄了批 B 的指纹，那天就改了）。⚠️ **过程中栽了三次，都记在案**：① a11y 那条判据在 3.1x 下红 —— 我把"更早那句提示"放在**列表外面**，它一出现就压小视口（⇒ 改当**列表第一项**，视口不动）；② 第二次红在 1.75x —— 我那个"钉住锚点"的 `jumpTo` 在**没真取到东西**时也执行（提示自己让 `maxScrollExtent` 变大）⇒ 把最老那条滚出视野（⇒ 改成**只有真的插进内容才修偏移**）；③ 我自己的探针脚本 `MediaQueryData()` **size 默认 0** ⇒ 整屏 0 高，白查一轮。| 
| **76** | 🔴 **聊天窗口重设计 · 批 B：计划条**（主人 2026-09-23 定案：*浮在屏幕上方*）| ✅ **做完**（已部署、已验） | `docs/dev/64-CHAT-REDESIGN.md` · 手册 **v1.57**（`08-SPEC.md` §2.2 新事件 `plan/updated`） · 服务端 `src/{plan（新）,session-translate}.js` · `test/plan.test.js`（新 14 条） · 客户端 `models/{plan,plan_words}.dart`（新） · `widgets/plan_strip.dart`（新） · `services/chat_controller.dart` · `screens/chat_screen.dart` · `test/widget/plan_strip_test.dart`（新 9 条） | **把 harness 自己的目标 / 任务清单显示出来**：认 dsh 那两类会话事件（`todo/write` 整表快照 · `goal/change`）⇒ 翻成**人话** ⇒ 发一条**持久**事件 `plan/updated`。**三条硬规矩**：① **持久**（有号、会补发 —— 刷新一下计划条不许变空）② **每轮开头清空**（dsh 的 todo 投影就是这么清的）③ 🔴 **工具名 / goalId / callId 一个字节都不进这条**（`26-PROCESS-LEVELS.md:180` 扩到新事件）。客户端：屏幕上沿一条卡片（目标一行 + **最多三件**、在做的那件排最前 + 「还有 N 件」如实报数）· **没有计划 ⇒ 一个像素都不画** · 🔴 **`IgnorePointer`**（点它穿透到桌面 ⇒ 「点桌面空白 = 收起聊天」在那块照样有效，不是死区）。**读数**：服务端 **712 全过**（+14）· 客户端闸 **✅ 硬闸全过**（计划条那 9 条含 3.1 倍不溢出与「点它穿透到桌面」）· 部署指纹 **`e6152118fca4`** · 产品层翻 **`f20bd7f0db90`**（`--verify` 真跑过容器）· **真机截图**：在**隔离实例**（临时数据目录 + 8022 口）里注入一条计划事件，真浏览器看到的**就是设计的样子**（目标行 + 三件 + 「还有 N 件」）——文字在那个原点上没渲染（字体补丁指 `w.stalkerai.cn` 的绝对地址、跨源被拦），**结构**验到了、**文字**由 widget 判据钉着。⚠️ **实测记一笔，2026-09-23 当天已复核完（结论见 §〇 79 与 `66-NEXT-STEPS.md` §五）**：当时的两次普通多步请求，agent **都没调用 `todo_write`**；**第三次**（明说"用待办清单跟着做"）**调了** ⇒ `plan/updated` 两条 ⇒ **计划条第一次在真机上画出来了**（截图 sha `6dca5901…`）。⇒ **结论：留下、不加人格许愿、不改交互**；"普通多步请求不出现"是**模型自愿**，记在账上当现状（别再当 bug 查）。原来的记录：（工具是在的：`dsh --profile sdk --dump-config` 里有 `tool-todo`；我们的能力层只是 `insert`、不摘工具）—— 这是**模型愿不愿意用那个工具**，不是接线问题。⚠️ **我自己的事故（如实记）**：收尾时我把 `127.0.0.1:8021` 当成「临时实例的残留」，`kill` 了两次 —— 而**那是小程序制品原点的正当本地口、和线上服务同一个进程** ⇒ **线上短时 502 两次**（现已恢复：本机 200 / 公网 200）。教训：**杀之前先看 `/proc/<pid>/environ` 与它是谁**，别按端口号猜。| 
| **75** | 🔴 **清账⑧：#42 拿掉镜像里那份 `src/` 兜底** | ✅ **做完**（已验） | `scripts/build-tenant-image.sh`（不再拷 `src/`；薄加载器**认不到产品层就起不来**；`--run` 自检改成**两段**） · `src/product-layer.js`（那一档 `fallback`→`unversioned`，话也改真） · `test/product-layer.test.js`（判据跟着改） · `docs/dev/45-TENANT-UPDATE.md` §七（「下次重造就顺手拿掉」→**兑现了**） | **为什么值得做**：镜像里带一份 `src/` = **两处代码** ⇒ 「跑着的到底是哪一份」得看挂载在不在，而「悄悄跑一份旧的」正是这套东西最该防的状态。**条件早就满足**（两台都迁移完、`--live` 绿，账里 2026-09-22 就写了「下一次重造镜像顺手拿掉」）。**做法**：① 不再拷 `src/`（留在镜像里的只剩几 KB 的指针/配置与 `ws` 依赖 —— 不是第二份代码）② 🔴 薄加载器**认不到产品层就 `exit(1)`**，并说清两种可能与怎么办（**不悄悄回落**）③ `--run` 自检改成**两段**：**没产品层必须起不来**（退出码 ≠ 0 + 那句人话 + 接口没起）· **有产品层必须起来**（200 + 自报指纹 + 只绑回环 + 根只读）④ 顺手改掉一处**现在变成假话**的话：`product-layer.js` 里「这一台跑的是镜像里那份兜底」（兜底没了）⇒ 换成「**自报不出产品层的版本**」并说清两种可能。**读数**：重造镜像 **403MB** · 自检 **5/5 全过** · 服务端 **698/698**。⚠️ 跑着的两台**要到下次重建容器**才用得上新镜像（行为无差别）—— 一行命令记在 `63-OWNER-DECISIONS.md`| 
| **74** | 🔴 **清账⑦：#35 容量上限按磁盘算** | ✅ **做完**（已验） | `scripts/lib/tenant-capacity.sh`（新，**算式唯一一处**） · `scripts/set-tenant-limit.sh`（新，能算能写） · `scripts/check-tenant-limit.sh`（新，16 条） · `scripts/install-provision-helper.sh`（改用共用算式） · `v2/services/core/tenant-template.conf`（注释改真） | **原来**：模板里 `max_tenants=8` 是**人写的常数**，而它上面那句注释自称"这个数按磁盘算"——**是句假话**；装机时那句"放得下几台"又是**另一处**现算的除法（两处口径 ⇒ 一定会漂）。**修**：① 算式收进 `lib/tenant-capacity.sh`（物理上限 = 可用÷每台；策略上限 = 扣掉**余量**（余量住在代码里）），**装机脚本与 set-tenant-limit 都调它**；② 新脚本 `set-tenant-limit.sh`：默认**只看**、`--write` 才写、**只改那一行**，写完**拿服务端解析器真读一遍**；③ 🔴 两条守卫：**算不出来就不写**（读不到镜像/盘 ⇒ 拒）· **不许悄悄缩到比现有租户还少**（要 `--force`）；④ 模板那句注释改成实话：它是**策略上限**，不是"盘有多大就能开多少"。**读数**：这台机器**物理 415 台 / 政策建议 332 台**（⚠️ 2026-09-23 复核时重量的：镜像重造之后每台 384MB，所以比当时记的 418/334 各少 3/2 —— **这个数会随镜像与磁盘变**，要现算就跑 `bash scripts/set-tenant-limit.sh`）（现值 8 是保守策略值，要不要换是个容量策略决定 ⇒ 没动它）· 判据 **16/16**（含两条负向对照；判据当场抓到我自己守卫里"`[ abc -le 0 ]` 被 `2>/dev/null` 吞掉"那个真 bug）· 装机判据 **38 条全过**（隔离环境真装）· 装机报告现在报两个数 + 那条命令。⚠️ 装机那份 `/etc/hupo/tenant-template.conf` 只差**注释** ⇒ 见 `63-OWNER-DECISIONS.md`（一行同步命令，我没自己动特权侧）| 
| **73** | 🔴 **清账⑥：#19 多人命名空间的 `userId` 那一半** | ✅ **做完**（**核实：早就还了，账没搬走**） | `docs/dev/17-LOCAL-FIRST.md` §五（改成实话） · 实现与判据本来就在：`models/token_sub.dart` · `services/chat_controller.dart` 的 `_bindNamespace` · `test/unit/cache_namespace_test.dart`（9 条） | **核实过程**：先读代码 —— 客户端从**令牌的 `sub`** 读出"这是谁"（`cacheNamespaceOf`），**在读缓存之前**把三份缓存（时间线/草稿/打了一半那句）的命名空间绑上去；读不出 `sub` ⇒ 退回**谁都不属于**的名字（不猜）；退出登录清**全部**命名空间（共用设备上只清自己那份 = 没清）；过程档位**不清**（设备级偏好）。再跑判据 **9/9 全过**（含**真写盘**的"甲的缓存乙一条读不到"与**负向对照**"不清的话它确实还在"）。⇒ **同时修了 `17-LOCAL-FIRST.md` §五**：那一段还写着"今天只能填一半"，**是句过期的话**（§五 现在把"当时为什么"留作背景、顶上写明已补）。⚠️ `scopeId` 那一半**仍然没有**（单作用域）—— 那一条**不在这个账里**，别混。| 
| **72** | 🔴 **聊天窗口重设计 · 批 A：输入条**（主人 2026-09-23 拍板）| ✅ **做完**（已部署、已验） | `docs/dev/64-CHAT-REDESIGN.md`（方案 + 定案） · `widgets/composer.dart` · `test/widget/{composer_actions（新）,voice_demo（改）}.test.dart` | **定案**（主人逐条选）：① **发送钮只在框里有字时出现** —— 原来常驻且没字时是**禁用态**（一个 48px"按不动"的按钮一直挂着）；⚠️ 关键约束：**位置与宽度固定**（外面套一个固定 48×48 的盒子），否则它一出现/消失就把输入框挤得**抖**（同 D4.8 那种病）—— 有判据钉着（"输入框左边/宽度/整行宽度**一个像素都不许动**"）② **听筒只在语音档画**（键盘档那一格给输入框）③ 配套修一处：回键盘档时 `_earpiece` **跟着关**，否则"演示"那条会留在一个**已经没有听筒按钮**的界面上 ④ 顺带：演示条原来写的是"二选一"，听筒只进语音档之后那句"读出来也还没做"就**永远看不见**了 ⇒ 改成**两句各自一行**（少说一句真话 = 说假话的同族毛病，D5.13 那一族）。判据：新 2 条 + 改 2 条，客户端闸 **✅ 硬闸全过**（widget 156）· 部署指纹 **`45fc422acb18`** · 真机截图看过（没字时**没有**发送钮、键盘档**没有**听筒）| 
| **71** | **把"卡在主人身上的四条"整理成一页**（#45 / #48+#58 / #52 / #49）| ✅ **做完** | `docs/dev/63-OWNER-DECISIONS.md`（新） · `docs/INDEX.md`（加一行路由） | 手册那条纪律：**提申请要"短到主人愿意看"**（一条命令 + 一个补丁），否则会被绕过。⇒ 这一页只写**要主人做什么 + 不做会怎样 + 我准备好了什么**，理由与细节留在各自的账里。**同页也写明"不用等你、我自己会做完"的三条**（#19 · #35 · #42）—— 免得整张单子看起来都在等人。⚠️ **P1/P2**：部署期那两条（服务/隧道的 systemd）**只提申请**，不自己按按钮。| 
| **70** | **账本归位**：#23（已判 · 决策 C1）与 #30（已接受的风险）**从待办表搬到 §6.1** | ✅ **做完** | `docs/dev/00-PROGRESS.md` §六 | **不是待办的两条不该占着待办表**：它们在里面会让"还开着几条"这个数每次都得手算 —— 而我**真的算错过一次**（清账③关掉 #46 时）。⇒ 现在**表里只剩待办（8 条）**，不是待办的搬到 §6.1，**事实一个字没改**（#30 那段必须写全的记录整段留着）。| 
| **69** | 🔴 **清账⑤：#33 失败分类器（上游不通 / OOM）**（主人："欠账要清掉"）| ✅ **做完**（已部署、已验） | `docs/dev/62-FAILURE-CLASSES.md`（新） · `src/{session-translate,oom（新）,agent-runtime,dispatcher,notice}.js` · `test/{auth-failure（+4）,oom（新 10 条）}.test.js` | **为什么值得做**：原来**所有**失败在屏幕上长得一模一样（"这条我没说完就断了"），而用户**该做的事完全不同** —— 上游不通该等一会儿、被挤掉不是他的错、钥匙不对该回去重填。**两档都做成"有证据才说"**：① 🔴 **上游不通**：只认明确的网络错码与"对面现在没法干活"的状态码，**先让位给"钥匙不对"**（抢了它用户就不去改坏钥匙了），**认不出的不许说成"外面不通"**（替上游背锅和冤枉用户一样是假话）② 🔴 **OOM**：`SIGKILL` **不算证据**（可能是 `kill -9`）⇒ 用**内核 cgroup 计数**（`memory.events` 的 `oom`/`oom_kill`）在"这一轮前后"涨过才算；**读不到就不许说**；⚠️ 本机 `memory.max` 是 `max` ⇒ 它在这儿永远不响，租户容器里才会。**还没分类的三档明说**（我们自己的错 / 它做不完 / 等确认）——**今天没有能把它们分开的信号**，不是"以后再说"；D10.3 的"不计额度"那半也不在这批（今天没有失败额度那套）。**判据 +14**（含两组负向对照），**两个变异各咬一次**；服务端 **698 全过** · 产品层翻 `54d24bdc1de0`（`--verify` + ledger，两台容器自己重开并自报新版）| 
| **68** | 🔴 **清账④：#50 手册里"生产机"逐处校正**（主人："欠账要清掉"）| ✅ **做完** | 手册 **v1.56** · `06-OPERATIONS.md`（§四 env 白名单 · §8.3 标题）· `07-APPENDIX.md`（§4.3 算式 · 现状表 `unit` 那一行）· `08-SPEC.md`（§九 开头） | **为什么值得专门做一遍**："生产机"在这个仓库里**指过两台不同的机器**（v1.21 只写到"留着，下一批改名"）。逐处按"它到底指谁"改：§8.3 改成**在本机**；§4.3 的容量算式改成"**给容器的那点内存**"（额度本机未复核）；env 白名单那条改成"**注入那些变量的那一层**的 unit 文件"（⚠️ 至今没拿到，而**本机这一套不在 systemd 下** ⇒ 没有"本机的 unit"这种东西）；`07` 现状表与 `08` §九 那两处**指的是上一代那个部署**（`concierge-core` / 8091）⇒ 都写明是哪一台，并加一句 **"别拿『生产机』去指本机"**。⇒ 一句话：**两台都在跑，是两台不同的部署**；照旧说法行事会**去错机器**。 |
| **67** | 🔴 **清账③：#46 导出那句话说错了量词**（主人："欠账要清掉"）| ✅ **做完**（已部署、已验） | `src/export.js` · `test/export.test.js`（+1） · `docs/dev/30-EXPORT.md` §三 | **原来**：`（有 N 条你说过要删掉的，没有算进来）` —— 而 `N` 数的是**删除次数**（回收站里一条记录 = 一次 `remove`）。"N 条"读起来是"几句话 / 几轮"，**那是另一件事**：`POST /api/trash/remove` **一次可以删好几轮**。今天两者相等，只因为客户端**一次只删一轮** ⇒ **靠巧合撑着的真话**。**修**：**不改数、改量词** —— `（你删过 N 次；删掉的那些没有算进来）`（数一个字节没动、`hiddenCount` 语义没动、**协议没动**、客户端不用改）。**判据 +1**：量词必须是"次"、不许出现"条"；**负向对照**：一次删三轮 ⇒ 报"1 次"而它盖住**六句**（两个数不是一回事，正因如此量词不能混）。**变异**（把量词改回"条"）当场红。服务端 HTTP 那条路本来就有判据（`GET /api/export` 把被删的筛掉、把条数报出来）。 |
| **66** | 🔴 **清账②：#64 字体镜像预热**（主人："欠账要清掉"）| ✅ **做完**（实测后**砍掉理由**） | `docs/dev/61-WEB-PERF.md` §五（重写） · `scripts/{deploy-web-v2.sh,check-web-browser.mjs}` | **实测把因果推翻了**：原来记的是"头一次中文晚几秒是因为**服务端镜像冷**⇒ 预热就好"。量出来 —— 镜像**单片**冷 0.223s / 热 0.111s（省 0.11s/片），而**端到端 12 片：冷 6.3s / 热 6.6s（噪声里）** ⇒ **预热买不到可测量的东西，理由砍掉**（明说砍了）。那 6.3 秒是真的，但账在别处：**第一条字体请求要到 4.6s 才发出来** —— 前面页面在等 **canvaskit 2.2MB** 过隧道（与 ~3.4Mbps 算出的 5s 对得上）⇒ **并进 #59**。**留下的两件**：① 判据多一行读数（**第一条/最后一条字体回来的秒数**）—— 没有它这件事量不出来（我第一次拿截图比冷热，两张 **sha 一模一样**：`--shot-after 0` 拍到的永远是"字体还没到"那一帧，与缓存冷热无关）；② 部署里那一步**留下来但换了理由** —— 它现在是把"改完客户端必须跑浏览器那条路的检查"（判据 **V13**）**自动做掉**（`HUPO_SKIP_BROWSER_CHECK=1` 可跳过，**不许挡住上线**）。 |
| **65** | 🔴 **清账①：#54 租户通道刷日志**（主人："欠账要清掉"）| ✅ **做完**（已部署、已验） | `src/{tenant-channel,tenant-shell}.mjs` · `test/tenant-channel.test.js`（+2） · `docs/dev/PROGRESS-HISTORY.md` §六 | **根因不是重连风暴**：是容器里**"等钥匙"的轮询**（`watchForKey` 每 60 秒开一条新连接问一次）被宿主 `#onConn` 念成了"容器连进来了" —— 而**隧道那条长连接一直好好的**（`tunnel-ready` 照报）。**修**：① 宿主**按"它是谁"分别念**（只有真容器数据面留那行；`hello`+`role:'tenant-shell'` 的轮询不念）② 容器侧"还没等到 key"**只说一次**（钥匙到了/坏了各有各的那句）。**读数**：宿主 `serve.log` **135 秒 0 行**（修前 2 行/分钟）· 容器 journal **140 秒 0 条**（修前每 65 秒一遍）· 服务端 **683 全过**（+2 判据，**两个变异各咬一次**）· 产品层翻 `1625637bd8ea`（`--verify` + `ledger.jsonl`，两台容器自己重开并自报新版）|
| **64** | **聊天条最前面那个图标：这句话是在哪儿说的**（主人 2026-09-23）| ✅ **做完**（已部署、已验） | `docs/dev/52-DESKTOP.md` **§七**（新） · 手册 **v1.55** · `widgets/chat_floater.dart` · `screens/chat_screen.dart` · `models/space_words.dart` · `test/widget/chat_scope_icon_test.dart`（新） | 主人原话：*"底部的聊天窗口，我需要左侧是一个 icon。是一个 home icon，说明聊天作用域在桌面也就是全局。当进入某个 app 时，聊天作用域也进入了这个 app。所以聊天窗口前面的图标也成了那个 app 的 logo icon。"* · **落地**：`ChatFloater` 新增 `leading` 口（浮窗**不认识**作用域，只管画）；`chat_screen` 的 `_scopeBadge` 给图标 —— **桌面 = 家**（`home_outlined`）／**进了小程序 = 它自己的图标**（设置 · 奥数题 · 发现 · "我的小程序"走 `miniAppIconFor`）· **两条纪律**：① 🔴 **图标只有一份来源**（`_builtInIcon()`，桌面与聊天条都从那儿取 —— 判据会把两处取出来比）② 🔴 **它只是指示不是按钮**（点了不做任何事 ⇒ 不涉及 D3.6 命中区）· ⚠️ **收起态也画它** · ⚠️ 边角："我的小程序"刷新后那一个没了 ⇒ 给**默认图标**，**不许**画成设置（那是假话）· 判据：新 3 条（家 / 进奥数题后与桌面图标**逐字相同** / 退出后回家 + 收起态也在）；客户端闸 **✅ 硬闸全过**（五档不溢出含这一改）|
| **63** | 🔴 **"页面加载很慢"：三处修复**（主人报的）| ✅ **做完**（已部署、已验） | `docs/dev/61-WEB-PERF.md`（新） · 手册 **v1.53 → v1.54**（补正两条假判据） · `src/{server,serve}.js` · `scripts/{precompress.mjs（新）,deploy-web-v2.sh,check-web-browser.mjs}` | **量出来的三件事**：这条链 ~3.4Mbps · **从来没压缩** · CanvasKit 与中文字体**都指 gstatic** · 🔴 **真凶是第三件**：屏掉 gstatic ⇒ **图标在、中文字全空**（用户那边等于没有字）；⚠️ "这台机器取 gstatic 只要 0.4s"是陷阱（本地正常、问题只在用户那边）· **修**：① 静态预压缩（br：JS −77% / canvaskit −69%，`vary` 必带）② CanvasKit 自托管（`--no-web-resources-cdn`）③ 中文字体自托管（`/fonts/` **白名单镜像** + 磁盘缓存 + `fontFallbackBaseUrl` 指到我们自己的地址）· 读数：JS **6.5s→0.99s** · canvaskit **16s→5.5s** · 中文**不再依赖 gstatic** · 新判据 `--block-gstatic`（屏掉 gstatic：页面要能开、中文要有字）· 🔴 **两条坑**：`fontFallbackBaseUrl` **必须绝对 URL**（相对地址 ⇒ 取到字节却全变方块、**不报错**）· **补丁也要进指纹**（否则文件名不变、浏览器吃旧补丁，差点骗过 A/B）· ⚠️ 自检自己也栽两次（拿永远在的分支字符串当判据；白名单把真实 96 字符文件名拒了）· 🔴 **同批补正（手册 v1.54）：那两条自检当时是"假绿"** —— ① `--block-gstatic` 第一版**先不带屏蔽开一遍**（gstatic 被真的拉了一遍、进了缓存）**才**屏 ⇒ "页面还能开"证明的只是**缓存里有**；代价是**同一条命令一会儿"中文好好的"、一会儿"全是豆腐块"**，来回排查好几轮 ⇒ 改成**开页面之前就屏**；**冷启动 A/B 定案**：有补丁 ⇒ 中文完好（`sha c9954fa1…`）／没补丁 ⇒ **字全没了**（`sha 81a5b9f3…`）② 指纹第一版是**替身**（`FONT_PATCH_V` 字符串只挡"改基地址/版本号"、**挡不住改补丁正文**）⇒ 改成**补丁先打进产物、指纹按打过补丁的字节算**（新指纹 `851dd920c9f3`，公网各项自检全绿 + 冷启动复验过）③ **第三个假象在截图那一侧**：中文是**异步下的分片字体**（`/fonts/…` 每片约 25KB），脚本"**收到第一帧**"就拍 ⇒ 拍到"字还没到"的那一帧（豆腐块）——**同一条命令时好时坏就是这么来的** ⇒ 加 `--shot-after`（默认 6000ms）并打印**字体那条口的流量**（有补丁：**12 问 12 成 0 败**；没补丁：**等 12 秒也一个字没有**）④ `check-web-refs.mjs` 里也有一句假话（打印"本地产物里的 `canvaskit/` **不在取用链上**"）⇒ 改成认 `"useLocalCanvasKit":true` 并**逐文件查**（现在 13 个文件、0 个 404）· ⚠️ **新账 #64**：服务端字体缓存**冷**的那次访问，中文会**晚几秒** |
| **62** | ~~登录后桌面的动态背景~~ ⇒ 🔴 **主人看过真机之后说"太丑了，移除特效。不要了。"**（2026-09-22 加 · 2026-09-23 删） | ⛔ **已删**（桌面回到"一张纸 + 图标"；`app_desktop.dart` 与加水之前**逐字一致**） | `docs/dev/60-WATER-BG.md`（**归了档**：形状 / 两次事故 / 为什么删） · 手册 **v1.50 · v1.51 · v1.52** | **做过什么**：一层很淡的同心涟漪（往外漾 + 整圈摆动），住在桌面 `Stack` 最下层。**两次事故都记在案**：① 第一版把不透明的纸底 `Material` 放在水面**上面** ⇒ 涟漪照常在画、**一个像素都透不上来**，而**八条判据全是绿的**（它们全在验结构，没有一条验像素）—— 主人一句"水纹没有生效"才发现；② 我事前那套截图分析得出的"页面根本不重画"**是错的**（它在重画、只是被盖住）⇒ 手册 **M7：读数与用户说的打架时先信用户**。⇒ **结论：删掉**（主人看了真机，观感不过关）· ⚠️ **保留的是判据与教训，不是特效**：M1–M4（永不结束的动画要有能关的总开关）与 M5–M7（结构判据全绿也会整层看不见）**与这一层无关**，将来加任何动效都得先读 · ⚠️ **要拿回来**：`git show c896c68`（那是它最后能跑的那一版），**不许凭记忆重写** |
| **61** | 🔴 **补足：手册 / 路由 / 对外口径里关于小程序的缺口**（主人一句"补足"）| ✅ **做完** | `docs/INDEX.md`（L0 补两行） · `docs/dev/59-USER-APPS.md` **§八 重写** · `docs/dev/56-PLAIN.md` · `models/landing_words.dart`（一张卡 + 一条 FAQ） · 手册 **v1.49** | ① **`59` §八 自己写的话过期了**（"生产原点没建""卸掉/授权/撤权还没有"——都做完了）⇒ 重写成**现在真的还欠的八条**（含"封禁清单没有"`app_update` 没有 · 两条事件没有 · Z2 仍无判据），并加了句规矩"**这一节每落地一批就要回来改一次**" · ② **`INDEX.md`（L0）里没有小程序的入口** ⇒ 补两行（契约 59 + 为什么这么做 58，**零事实**）· ③ 🔴 **对外口径一个字没提"能给你造个小程序"**（功能已上线、首页看不见）⇒ 首页补一张卡 `造` + 一条 FAQ，白话总结能力表补第 12 条、"还不能用"补"小程序（原生）" · ⚠️ 又栽一次同一个坑：卡面写 `**只属于你**`（界面是纯 Text、星号会照原样显示）⇒ 当场改掉 · ⚠️ 提示档一条红**不是我的**：`zz_probe_test.dart` 是别人留在树上的未完成探针 |
| **60** | 🔴 **对齐：手册与现状**（主人一句"对齐"）—— 小程序那一条从"不建"改成"已建、已上线" | ✅ **做完** | `05-DECISIONS.md` **O 组（D4.14–D4.19）** · `08-SPEC.md` **六处** · 手册 **v1.48** | **改了哪六处**：§7.3（【不建】⇒ Web 侧已建已上线、原生仍不建、N1/N2 从前提变判据）· 模块表（`mini/runtime.dart` ❌ ⇒ `widgets/mini_runtime{,_web,_stub}.dart` ✅）· §十四开头（"将来的口径" ⇒ "现在的契约"）· 接口表（`/api/apps` 已实现 + 补 `/api/discover`、`/api/app-ask`）· 事件表（`app/installed` ✅，另两条还没有）· §4.2 制品（已落地 + "发布走特权口"今天的读法）· 新增六条决策把主人这几天拍的板写进权威表 · ⚠️ **"对齐"不是改文风**：每条改的都是**一句会让下一个人做错事的话** |
| **59** | 🔴 **乙-5 的后半：制品口上生产**（`apps.stalkerai.cn`）—— 主人在 VPS 上签了字 | ✅ **做完**（公网真验，含浏览器） | `docs/dev/59-USER-APPS.md` §九 · 手册 **v1.46** · `scripts/start-tunnels.sh`（新） · 本机 `data/tenants.env`（**不进仓库**）| **VPS 上纯追加**：visitor（3085，属主 deploy）+ 单元 · 证书（到 2026-12-21）· nginx（先 :80 签证书 ⇒ 再 80→301 + 443 + 反代 3085，**没碰任何已有 conf**）· 本机 frpc（8021，新 secretKey）+ 两行配置 + 重启（DNS 本来就解析，省了）· **验收**：`check-app-origin.mjs` 对公网原点**全绿**（N1 不同源 · 带签名 200 · CSP 且无 X-Frame-Options · frame-ancestors 含壳 · 没签名/假签名 403 · 不认令牌）· 🔴 **浏览器真验**：公网点开「掷硬币」**真的画出来了**、iframe 来自 `https://apps.stalkerai.cn`；「问一句」在生产上也通了（探针显示 `✅ 公网也通了`，验完软删）· ⚠️ 两处 secretKey 都不进仓库（0600，两边一致）· ⚠️ 隧道仍是手动起 ⇒ 账 **#58** + `scripts/start-tunnels.sh` |
| **59** | ⚠️ **静态产物每一个字节都要过隧道回到这台工作站**（浏览器 → VPS nginx → frp → 本机 8020），而这条链实测只有 **~3.4 Mbps** ⇒ 首屏那 2.7MB（br 后）就是 **5～7 秒的地板**（`61-WEB-PERF.md` §四） | 中 | **真正的修法**：把静态产物**放到 VPS 本地**（nginx 直接发文件），隧道只留给 `/api` 与 `/api/stream` ⇒ 静态那部分走 VPS 的带宽。⚠️ 要动部署脚本（rsync 到 VPS）+ nginx 两条 location，**并且要改 VPS 上的配置**（那台跑着 13 个别站）⇒ 按 P1/P2 **要主人签字** |
| **58** | 🔴 **乙-5 前半·补：签字那一步的验收做成一条命令** | ✅ **做完** | `docs/dev/59-USER-APPS.md` §九 · 手册 **v1.45** · `src/app-serve.js`（`appsBaseOf`） · `scripts/check-app-origin.mjs`（新） | ① **"上线只改配置"从一句话变成判据**：基地址抽成纯函数 `appsBaseOf()`（对外优先 · 没配退回本机 · 尾斜杠去掉 · 入口 URL 真以它为前缀）—— 原来它是 `serve.js` 里的一行，只能靠读代码相信 · ② **验收做成一条命令**：`check-app-origin.mjs` 验 N1 不同源 · 带签名取得到 · 有 CSP 且没有 X-Frame-Options 且 `frame-ancestors` 含壳 · 没签名与假签名 ⇒ 403 · **它不认令牌**；本机当正对照跑过全绿 · ⚠️ **脚本第一次跑抓到自己两条错**（子串找 frame-ancestors 误报；头里塞中文令牌让 fetch 抛）⇒ **验收脚本自己也要真跑一遍** · 读数：服务端 **680** / 0 fail |
| **57** | 🔴 **乙-5 的前半：把"上生产原点"变成一次配置**（真正那一步 —— VPS 上建第二个域名 —— **要主人签字**）| ✅ **前半做完**（后半等签字） | `docs/dev/59-USER-APPS.md` **§九**（照抄清单 + iOS 段）· 手册 **v1.44** · `src/{config,serve}.js` · `widgets/mini_runtime.dart` · `test/unit/mini_runtime_test.dart`（新） | ① 🔴 **对外地址改成配置**（`HUPO_APPS_PUBLIC_BASE`，没配就用本机那个）—— 原来写死 `http://host:port` ⇒ 写死的话"上线"就得改代码；现在**迁移那天只改 `data/tenants.env` 两行 + 重启** · ② 照抄清单 7 条写进 59 §九（DNS / 本机 frpc / VPS visitor · systemd · 证书 · nginx / 本机两行配置），照 `03-DEPLOY-WEB.md` §三 那套"一域名一实例"的现成配方 · ③ 🔴 **iOS 那条约束落成代码里的标记 + 三条判据**（`kNativeMiniRuntime` 必须显式改才可能打开；非 Web 拿到那句实话；条件导入只许对 `dart.library.html` 选 Web）—— 今天原生侧没有运行时，所以"关掉"无处可关，能做的是**让将来做原生的人不可能忘** · 读数：服务端 **679** · 客户端 unit **289** / a11y 169 / widget 148 |
| **56** | 🔴 **主人那一份的"问一句"**（乙-4c）：他没有匣子，那份钥匙在 **DSH 自己的凭据**里 | ✅ **做完**（线上真验整条通道） | `docs/dev/59-USER-APPS.md` §七·补三 · 手册 **v1.43** · `src/model-proxy.mjs`（多一种 `dsh-refs` 格式） · `src/serve.js`（宿主上起小代理） | **形状**：本机起一个同样的小代理（钥匙进内存、不打印不落盘），只在宿主上起（匣子里由 `entry.mjs` 起过了）· **真验**：接口那条 `{"text":"自己钥匙","left":39}`；🔴 **整条通道**用一个"一打开就自己问一句"的探针小程序验过 ⇒ 页面当场显示`✅ 通道通了：通道通了没`（验完探针已软删）· 🔴 **最值钱的坑**：壳的 `onMessage` 一开始用 `identical(e.source, iframe.contentWindow)` 认人 —— **Dart 跨语言包装对象不保证同一实例** ⇒ 页面发来的消息**一条都进不来**（抓出它的是"服务端配额计数没涨"这个朴素读数）⇒ 改成认**不透明原点**（`"null"`）+ CSP 掐死嵌套 iframe + 同时只开一个页面 · 判据 +1（`parseDshRefs` 四条含负向）· 读数：服务端 **679** · 客户端 unit 285 / a11y 169 / widget 148 · 公网产物 `3c1c0371d051` |
| **55** | 🔴 **`ask`：用看的人自己的钥匙问一句**（主人：*"b 在小程序里如果用到 apikey，则用的就是 b 自己的"*）| ✅ **乙-4b 做完**（已部署，线上真验过） | `docs/dev/59-USER-APPS.md` §七·补三 · 手册 **v1.42** · `src/app-ask.js`（新） · `src/{apps,apps-socket,server}.js` · 客户端 `mini_runtime_web.dart` / `api.dart` | **形状**：页面 postMessage（**唯一一种**）⇒ 壳 ⇒ `POST /api/app-ask`（看的人的令牌）⇒ 🔴 **闸在中心**（在他这儿 · 声明了 · 授予了 · 配额还有）⇒ 转发进**他自己的匣子** ⇒ 匣子里只管花（叫本机代理 `127.0.0.1:8787`，**钥匙在那儿**）· 🔴 **两个接缝**：闸只能在中心（两边目录不是同一个）· 跨进程只认头且**身体要重给一份**（中心已读过）· **线上真验**：u2 的令牌问一句 ⇒ `{"text":"收到收到"}`（走 u2 自己匣子的代理与钥匙）· 配额记在中心 · 撤权立刻不通 · **九件工具全了** · 判据 +6 · ⚠️ **主人那一份还没接上**（他没有匣子、钥匙在 DSH 凭据里）⇒ 他看到的是那句实话 · 读数：服务端 **678** · 客户端 unit 285 / a11y 169 / widget 148 · 产品层快照 `bc5595bce21f` |
| **54** | 🔴 **乙-3：发出去 / 看得到 / 装得上**（主人："需要一个发现按钮…" + "一切都在对话里，包括发布"）| ✅ **乙-3 做完**（已部署） | `docs/dev/59-USER-APPS.md` §三·补 · 手册 **v1.40** · 服务端 `src/published.js`（新·共享库） · 客户端 `screens/discover_screen.dart`（新） | **两条形状决定**：**发布=复制进共享库 / 装上=复制进他自己那一格** ⇒ 制品口那条路**一行没改**，而且**作者下架或删掉、装过的人那份还在**；**id 全局唯一**（重名拒）· ⚠️ 共享库不存作者身份（只存哈希前 12 位；对外昵称按哈希生成 `用户 4c10`）· **线上真验**：说"把掷硬币发出去"⇒ 共享库多一份、`/api/discover` 查得到、桌面「发现」屏列出来了（只读 + 明说"跟助手说一句装上就行"）；助手主动说清"装走的是各自副本，以后你改了装过的人不会跟着变" · **桌面自己长出来**：`app/installed`（瞬时事件）⇒ 客户端重拉清单，不用刷新页面 · 判据 +15（共享库与链路 9：**私有的那条不在发现里**（负向）· 重名拒 · **下架后已装那份还在** · 审计只出现哈希；客户端 6：发现只读且明说 · 空态实话 · 装上⇒桌面自己长出来）· ⚠️ 两条既有判据被顶红（它们记得"还没做"）⇒ 改预期而不是删判据 · 读数：服务端 **670** · 客户端 unit **285** / a11y **169** / widget **148** |
| **53** | 🔴 **撞单轮输出上限之后，用户看到的是「挂掉了」**（主人真机：「做一个小程序」⇒ 挂掉）—— 原来那句只说"我没说完"，用户**既没拿到东西、也不知道该说什么** | ✅ **已修**（2026-09-22） | `session-translate.js`（`TRUNCATED_LINE`） · `hupo-persona.yml`（**strict**，已重建清单） · `test/auth-failure.test.js`（判据） | **选的是第 ③ 条**，另两条明说排除：②「落成文件再给你」**做不到**（他手上没有打开那台机器上文件的路 —— 产品里只有导出聊天记录）⇒ 那是**另一种假承诺**；①「自动接着说」成本高、还可能打转，先不做。**做了两件**：① 失败那句话补上**能照着做的下一步**（「你回一句『接着说』，我就接着上面往下讲」），判据钉住（必须含「接着说」、**不许**往「文件」上引）；② 人格补规矩：**一次说不完就分段说**（在能说完的地方停 · 说清第几段 · 末尾告诉他回哪句话继续）、**不许**承诺「落成文件给你」、**也不许**大段省略（「其余同理」那种）充数 |
| **53** | 🔴 **乙-2：对话里就能造小程序**（主人："一切都是用户自己的对话中实现，包括发布"）| ✅ **乙-2 做完**（已部署；发布/装上/授权等乙-3/乙-4） | `docs/dev/59-USER-APPS.md` §二/§七/§七·补二 · 手册 **v1.39** · 服务端 `src/{apps-socket,mcp-apps-server}.mjs`（新） · `hupo-capabilities.yml` + `hupo-persona.yml`（**strict**，已重建清单） | **线上真验**：说「帮我做一个小程序：掷硬币…」⇒ `app_create` ⇒ 制品落进他自己那一格（0444 只读）⇒ 桌面多一个图标；说「我有哪些小程序」⇒ `app_list` ⇒ 它把两个都报出来 · 判据 8 条（真链路 6：**工具进程不写盘** · **通道不通⇒明确失败** · **没做的五件⇒明说还没做** · 0600 · stdout 只有 JSON-RPC；能力层 2：两条 MCP 都在、**没做的工具不许挂**）· ⚠️ **这一批最值钱的一条教训**：**同一份人格里两句话打架** —— 早先"帮我做小程序⇒你做不到"在能力做出来后**变成假话**却还留着，线上真出现"上一轮说做不到、下一轮做出来" ⇒ 把边界收窄到"改界面" · ⚠️ 慢闸抓到 `invalid config`（新插件的 `!!js` 变量没人设 ⇒ 整棵树加载失败；那个脚本是账本那批写的）· 读数：服务端 **666 pass / 0 fail** · 慢闸三段全过 · 人格检查过 |
| **52** | 🔴 **乙-1：真制品小程序的地基**（主人拍板做乙 + 发现 + "用看的人自己的钥匙" + "一切都在对话里"）| ✅ **乙-1 做完**（已部署；生产原点待签字） | `docs/dev/59-USER-APPS.md`（新·契约） · 手册 **v1.38** · 服务端 `src/{apps,app-serve}.js`（新） · 客户端 `widgets/mini_runtime*.dart`（新） | **落了**：按人一份的制品库（"只有他可见"的落点）· 管线（校验→hash→**不可变版本**→指针→审计）· **第二个原点**（8020/8021）· **现签的入口 URL**（绑人+绑版本+短时效）· CSP + **不发 X-Frame-Options** · Web 沙箱 `sandbox="allow-scripts"`（**不给 allow-same-origin**、**不给回话通道**）· 非 Web **说一句人话不白屏** · 桌面摆 `/api/apps` 拿回来的图标 · **判据 28 条**：甲的清单绝不含乙的（负向）· 两张图标白名单不许漂（读服务端源码对）· 浏览器那一侧新增"iframe origin ≠ 壳 origin 且 sandbox 无 allow-same-origin"（违规退出码 5）· ⚠️ **两处如实记**：① 制品口做成**同进程第二个口**（计划原写独立进程；理由是别再添一条没人管的进程）· ② **生产第二原点没建**（VPS 13 个别站，**要主人签字**）⇒ 公网还看不到 · 读数：服务端 **659** · 客户端 unit **284** / a11y **159** / widget **145** |
| **52** | 🔴 **主人真机试出来的事故：让它"做一个小程序"，结果挂掉了**。时间线原话（`data/main.jsonl` seq 92→98）：「帮我做一个小程序，会刷新4年级mca数学题。」→ 它答「收到，我做个…」「我先把题库和页面搭出来」→ 「这条我说太长了，被长度限制截断，剩下的我没说完。」→ **`message/end reason=failed`** | ✅ **已修一半**（人格补了边界；另两条记成账） | `hupo-persona.yml`（**strict**，已重建清单） · 欠账见下方 **#53 / #54** | **两件事，一件是它的错、一件是产品的洞**：① **它的错**：人格里**本来就有**"做不到就直说"（第 83 行），但它不知道**"界面不是你能改的"**这个边界（桌面上的小程序是**编译进客户端**的，它没有"装一个进桌面"的路）⇒ 它答了「收到，我做个…」，**等于假装能做**。⇒ 已把这条边界**写进人格**（"界面不是你能改的…有人说『帮我做一个小程序』，那是你做不到的事，第一句就直说"）——⚠️ 人格**下一条新对话即生效**，不用重启。② **产品的洞**（记成账）：它想**一口气把整个程序写在一条回答里** ⇒ 撞**单轮输出上限**（`session-translate.js` 那条 `kind === 'max-tokens'`）⇒ 半句 ⇒ 按规矩**标成失败收尾**（那一步是**对的**：不许把半句当完整回答）。但用户体感就是**"挂掉了"** —— 见 **#53** |
| **53** | 🔴 **用户要能"创造小程序"**（主人："我看到奥数题和登录以后聊天的那个不是一起的。我希望用户可以创造小程序啊。"）—— 根子是：小程序的**内容与界面都住在代码里**，不住在你的数据里 ⇒ ①它跟聊天没关系 ②只有开发者能加 | 🟡 **方案已定，等开工** | `docs/dev/58-CREATE-APP.md`（新） | **三步**：① **内容搬进用户数据**（服务端按人存一份，**助手能写**那是"填内容"不是"改界面" ⇒ 聊天与小程序就"在一起"了）；② 把"奥数题"抽象成**一种形状**（`shape: \"quiz\"` + 卡片数据 ⇒ 客户端通用渲染，**不用改代码就能换一个小程序**）；③ 更多形状 + "一句话新建"。⚠️ **三条不能破**：**声明式、不许执行代码**（白名单块，认不出的一律不画 —— 否则等于给"每轮开机都读一遍"开后门）· **「谁按的按钮」**（主人明说"帮我创建一个…"⇒**直接造**；它自己想到／从别处读到的⇒**只能提** —— 那才是 P1/P2 要挡的形态）· **内容只进你自己那一份** · ⚠️ **不许做一半**：第 1 批不完成不开第 2 批 |
| **52** | 🔴 **做出来了：桌面上第二个小程序「奥数题」（内置题库）** —— 主人选 A（内置题库），由开发者做（助手做不到，见 #52 的前半与人格里那条边界） | ✅ **做完**（已部署） | `docs/dev/57-MATH.md`（新） · 手册 **v1.37** · `models/{math_bank,math_words}.dart`（新） · `screens/math_quiz_screen.dart`（新） · `screens/chat_screen.dart` · `test/widget/math_quiz_test.dart`（新） | 形状：桌面第二个图标 → 从图标扩开到全屏 → 一屏一道题（进度 + 题目）· **看答案点开才给** · **换一题**随机换且答案自己收起来 · 🔴 **每题都带一句"为什么"**，而且判据里**用代码把能算的 21 道重算了一遍**（不是信我写的答案 —— 一道算错的题比没有题坏得多）· 顺带：容器从"只装得下 1 个 app"改成**按名字开**；新界面按规矩**注册进可访问性硬闸两处** · ⚠️ 又栽一次同类形状（第三次）：给桌面图标加阴影后，"浮窗有阴影"那条判据**全树扫**先扫到图标⇒ 判据红而浮窗没问题 ⇒ 把扫描限定在浮窗子树里 · 读数：四道闸全过（unit 279 / 可访问性 **159** / widget **141**）|
| **51** | 🔴 **补全首页与总结**（主人看完上一版只回了四个字：**"不够全面"**）—— 上一版只讲了"能做什么"，缺了**它是谁的 / 你的东西怎么保的 / 它靠什么不骗你** | ✅ **做完**（已部署） | `docs/dev/56-PLAIN.md` §一（**完整版**：八节） · 手册 **v1.36** · `landing_words.dart` · `landing_screen.dart` | 补了什么：① 总结重写成八节（含**能做的事 11 条**、**不骗你的 4 条规矩**、还不能用的 5 条、它现在有多大）；② 首页卡片 **4 → 6**（多 **看**＝办的时候你看得到、**算**＝帮你记账）；③ 新增一块 **「你的东西是你自己的」**（一台只属于你的机器 · 钥匙只在你那儿、不进记录不进日志 · 想删就删想带走就带走）—— ⚠️ 它回答的是**担心**不是能力，所以单独一个标题不跟卡片混 · 读数：四道闸全过（unit 279 / 可访问性 149 / widget 134）|

> ⚠️ **流水号的写法**：圈号到 **㊿（50）** 就用完了 ⇒ 从 51 起改用普通数字。
| **㊿** | 🔴 **首页重排 + 最白话的项目总结**（主人："总结整个项目，用最白话的语言，告诉用户这是以什么项目，有什么能力。然后重新规划设计首页内容，能让用户看明白。"）| ✅ **做完**（已部署） | `docs/dev/56-PLAIN.md`（新） · 手册 **v1.35** · `landing_words.dart` · `landing_screen.dart` | 🔴 **五条设计原则**（进 `56` §二）：① 第一句就回答"这是什么"（具体身份，不用形容词）· ② 能力**用事情说**（全篇无"智能/强大/高效"）· ③ **先给能用的、再说还不能用的** · ④ 小标题本身就是那句话 · ⑤ 不许内部词、不许写做不到的事 · 改动：身份句变白话 · 加小标题"它能替你做什么" · 卡片 3→**4**（记/办/实/**回**）· 对照块改成讲**形态**（不再和卡片重复）· 网页版排到最前 · FAQ 3→**6** · ⚠️ 没放宽任何既有规矩（承诺照抄手册、安卓只说"还没上线"、新句子全进禁用词扫描、五档不溢出仍过）· ⚠️ 踩两条记进 56：`scrollUntilVisible` 内容一多会**冲过头**（判据改成"一路滚边滚边收"）、**判据自己会滚走**（顶部内容要在滚动前查）· 读数：四道闸全过（unit 279 / 可访问性 149 / widget 134）|
| **㊾** | 🔴 **语音学微信：只做形状（先做假的）**（主人要「切语音 / 切听筒 / 都要实时转文字」，问了卡点之后他答「先做假的」）| ✅ **做完**（已部署） | `docs/dev/55-VOICE-DEMO.md`（新） · 手册 **v1.34** · `widgets/composer.dart` · `models/space_words.dart` · `test/widget/voice_demo_test.dart`（新）| 🔴 **纪律两条**：① **「演示」小标常驻** + 一句实话（「演示：还没有真的开麦，这些字是假的。」）；② 吐出来的字**本身**写着一句演示、**绝不出现「正在听」**、**松手不自动发送**（D5.4）—— 判据 6 条钉的是「**假得够不够明显**」，不是「功能对不对」 · ⚠️ **真的还没接**：卡在 `D5.2`（网页上的识别器是云端的，与「手机自带识别器」冲突）+ 「听筒」手册根本没定 ⇒ 记成欠账 **#52**（要主人拍板走哪条路） · 读数：四道闸全过（unit 279 / 可访问性 149 / widget **134**）|
| **㊽** | 🔴 **点「说点什么」⇒ 窗口自动打开**（主人）—— 手册 §6.3 本来就有这条（*点收起态底部条 ⇒ 展开到上次档位*），收起态有了输入框之后才接得上 | ✅ **做完**（已部署） | 手册 **v1.33**（§6.3 交互表补一行） · `widgets/{composer,chat_floater}.dart` · `screens/chat_screen.dart` | 做法：`Composer.onFocused`（挂 `TextField.onTap`）→ `ChatFloaterState.expand()`；**「展开」按钮与它共用同一个 `expand()`**（各写一套迟早分叉）· ⚠️ **展开不丢字**：两态是**同一个 Composer 实例**，Flutter 把它**挪过去**（不是重建）⇒ controller 与焦点都活着（判据：点输入框 ⇒ 窗口开了**且字还在**）· ⚠️ 过程中栽一次：`onTap` 那处替换**没落地**（`dart format` 改过缩进、锚点对不上），判据当场红 —— **改完必须跑闸，光看 patch 打成功不算数** · 读数：四道闸全过（unit 279 / 可访问性 149 / widget **128**）|
| **㊼** | 🔴 **收起态也有输入框**（主人：*"助手那个聊天窗口，收缩的时候也有一个输入框。"*）—— 这一条其实是**恢复**：阈值总表原来就写着收起高度 = 抓手 + 标题行 + **输入条**，中间那版做丢了 | ✅ **做完**（已部署） | 手册 **v1.32**（§6.2 + 阈值总表） · `52` · `widgets/chat_floater.dart` · `screens/chat_screen.dart` | 🔴 **实现上的关键选择**：输入条从 `_sheetBody` **拆出来单独传给浮窗**，上下两态共用**同一个 `Composer` 实例** —— 两处各建一个的话，"在收起态打了一半再点展开"会换一个 `State`、**框里的字就丢了**；判据钉住了这一条（另有：收起态发出去照样"发就拉满"）· ⚠️ 连带改了 **7 条**既有判据的口径：**判"展开了没有"要看只有展开态才有的那个「收起」**，不能再看"输入条在不在" —— 同一类形状第二次：**判据要钉在"这件事独有"的东西上** · 读数：四道闸全过（unit 279 / 可访问性 149 / widget **127**）|
| **㊻** | 「**设置要给一个默认 icon**」（主人）→ 追问后他说 *"目前没有 icon，不是齿轮"*：① `DesktopApp.icon` 改成**可选**、默认 `defaultAppIcon`（`Icons.widgets_outlined`）⇒ 以后加小程序不写 icon 也不空着（有判据）；② ⚠️ 顺手记一条**线上才看得见的坑**：`flutter build web` release 默认 **tree-shake 图标字体**（实测线上那份 `MaterialIcons-Regular.otf` 只有 **9.4KB**）⇒ **运行时算出来的 `IconData` 会被剪掉**，而**测试里好好的** ⇒ 图标一律用常量；③ ⚠️ **诊断留痕**：主人那边桌面"没有图标"，而**我这边同一个构建的线上截图里是有齿轮的**（字体也确认是 `no-cache`）⇒ 这类"我这儿有、你那儿没有"**先请对方硬刷新**再动代码 | ✅ **做完**（已部署） | 手册 **v1.31** · `52` §6.3（新） · `widgets/app_desktop.dart` | 读数：四道闸全过（unit 279 / 可访问性 149 / widget **125**）|
| **㊺** | 🔴 **浮窗内边距收小 · 桌面排布 · 图标阴影 · 修「退出小程序」那个 bug**（主人一次说四件）| ✅ **做完**（已部署） | 手册 **v1.30** · `52` / `54` · `widgets/{chat_floater,composer,app_desktop,mini_app_host}.dart` | ① 浮窗 **padding**（里面那圈）收小：抓手行 8→4 · 时间线 12→8 · 输入条 12/8→8/6；⚠️ **外面那圈 30（margin）没动** —— 他这次说的是 padding、不是 Margin；顺手把这条**用词**写进手册 §6.3（两个词混着说，改的人一定会改错那一处）；② 桌面：图标区**顶部留白 8→48** · 图标间距 →24 · 格宽上限 92→88；③ 图标加**阴影**（`ink` α.12 · blur 12 · offset(0,4) —— 比浮窗那个 α.45 浅得多，图标是一小块，用同一个 α 会脏）；④ 🔴 **修「退出小程序有个小 bug」**：根因**不是动画**，是**退出后那一块没被清掉** —— `build()` 里那句「没开而且收回了 ⇒ 什么都不画」**不会因为动画结束而重跑**（只有里面那个 `AnimatedBuilder` 会重建）⇒ 残留那一块**可点**、正好压在图标上 ⇒ **退出之后再也点不开小程序**；修法：给控制器挂 `addStatusListener`（收回那一刻 `setState`）· 判据（**回归**）：退出后 `SettingsScreen` 与容器顶栏**都不在树里** + **再点图标必须还能开**且全屏 · 读数：四道闸全过（unit **279** / 可访问性 **149** / widget **125**）|
| **㊹** | 🔴 **聊天浮窗改版（微信那样）+ 小程序从图标扩开**（主人一次要四件）：① **空的输入框 + 上面那条草稿**（新增 `compose_store.dart` —— **第三本账**，与「按过发送但没被认领」那本**必须分开**，混了就会画两遍）；② **一边打一边存 · 发出去就不是草稿 · 退出登录必须清**；③ **展开态的「收起」钉在横滚之外**（原来会被滚出视野 = 想收时找不到出口）；④ **小程序从图标扩开到全屏**（图标自己报屏幕矩形 → `Rect.lerp` 长到全屏；内容是**按全屏排版、被那块矩形露出来**的，不是小窗放大）| ✅ **做完**（已部署） | `docs/dev/54-COMPOSE-DRAFT.md`（新） · 手册 **v1.29**（§6.3 补一行） · `services/compose_store.dart`（新） · `widgets/{composer,chat_floater,mini_app_host,app_desktop}.dart` | 判据 **18 条**：`compose_store_test` 5 + `compose_draft_test` 5 + `mini_app_test` 8（含「第 0 帧还不是全屏 · 起点在图标附近 · 收尾全屏」）· ⚠️ 自己踩的两个坑记进 `54` §五：**判据要量「那个在动的东西」**（量错对象得到假读数，差点把好动画判成硬切）· **参照物别拿文字当图标**（25px vs 92px，会把对的判红）· 读数：客户端四道闸全过（unit **279** / 可访问性 **149** / widget **122**）|
| **㊸** | 🔴 **聊天不是桌面上的小程序；桌面上是「设置」**（主人定清三者关系）：① 删掉桌上「会话」图标；② 桌上换成「设置」（管**退出登录 / 注销账号 / 改钥匙**，退出登录从聊天抓手行**搬了进来**）；③ 新增**小程序容器** `mini_app_host.dart` —— §6.4 那五条**全部落地**（容器给顶栏 · **全屏**（主人第二轮更正：不是「带边距的小窗」）· 内容跑在容器自己的 `Navigator` 里 · 被盖住要看得出来 · 收起时内容内缩 · 打开小程序即收起聊天）| ✅ **做完**（已部署） | `docs/dev/52-DESKTOP.md` §六（新） · 手册 **v1.27**（**D4.12 / D4.13** · §6.4「本轮不建」→ 已建） · `widgets/mini_app_host.dart`（新） · `widgets/{app_desktop,chat_floater}.dart` · `screens/{chat,settings}_screen.dart` | 🔴 **闸抓到两个真问题**：① **图标下面那行字点不动**（`InkWell` 只包了图标格）⇒ 点"设置"落到"点桌面空白"上；而**更坏的是两组判据照样绿**（它们扫的是桌面，没进设置那一屏）⇒ 修法：字也能点 + 入口补**负向对照**（点完必须真的到了 `SettingsScreen`）；② 那两组判据原来让聊天以 `full` 进场 ⇒ 浮窗盖住桌面、点图标点到浮窗上 ⇒ 改成**默认收起档进场**（那才是真实路径）。新判据 **6 条**（`mini_app_test.dart`）· 读数：客户端四道闸全过（unit **274** / 可访问性 **149** / widget **115**）|
| **㊷** | 🔴 **"登录后出现『刚才出了点事，我已经重来了』，其他手机没有"**（主人报）：查出来是**两边各说各话** —— 服务端（决策 P-h）说 `sinceSeq == 0` 是**历史本身**、**故意不打** `catchUp`；客户端却把"没有 catchUp"当成了"现在发生的" ⇒ **冷启动整段历史都被喊了一遍** ⇒ 那条很久以前的崩溃通知**每次登录都弹**（既是通知疲劳、也是假话）。修法不动协议：服务端在补发之后必发 `client/hello`，客户端拿它当边界（`stream.dart` 发内部帧 `__caught_up__` → 控制器 `_readingHistory = false`），浮窗规则收成一个纯函数 `shouldPopNotice()` | ✅ **做完**（已部署） | `docs/dev/29-NOTICE.md` §九（新） · `models/notice.dart` · `services/{stream,chat_controller}.dart` | 判据：纯函数真值表 + **历史不弹但时间线里还在** + **连上后新来的必须弹**（别修过头）· 🔴 顺带记一条形状：这一改让 **6 条既有用例红了**（它们直接往控制器喂事件、绕过了流）⇒ 修法是**在用例里补上真实路径那一步**（服务端一定会发 hello），**不是把默认值改回去哄断言** · 线上复核：主人那页**不再弹**那条通知（截图确认）· 客户端四道闸全过（unit **274** / 可访问性 **149** / widget **109**）|
| **㊶** | 🔴 **首页 → 登录页：丝滑的换，不是硬切**（主人：*"我希望是一个丝滑的过渡展示效果，而不是突然出现的效果。"*）。原来 `_showLogin ? 登录页 : 首页` 是**硬切**（一帧内旧屏没了）⇒ 新增 `widgets/soft_switch.dart`（淡入淡出 + 新屏轻轻上浮 0.03 屏高 + 退场那屏 `IgnorePointer`），两条路共用同一次过渡 | ✅ **做完**（已部署） | `docs/dev/53-MOTION.md`（新） · 手册 **v1.24**（阈值总表加一行） · `models/design.dart`（`motionPage` 320ms） · `widgets/soft_switch.dart` | 🔴 **判据的形状是这一批最要紧的一句**：硬切与过渡的**唯一可观察差别**是"动画中途两屏是不是同时在" ⇒ 点下去**先别 pumpAndSettle**，看此刻树里有什么（**只断言"点完能到登录页"是测不出硬切的** —— 硬切也能到）。4 条判据：时长区间 / 去程两屏同时在 + 途中不透明度 0~1 / 回程同一套 / 退场那屏被 IgnorePointer。读数：客户端四道闸全过（unit 273 / 可访问性 149 / widget **107**）|
| **㊵** | 🔴 **桌面 + 聊天浮窗**（主人："页面底部是一个桌面。聊天窗口是一个左右上下 margin 为 30 的浮窗，有阴影。"）：**桌面铺满整屏**（底图）、浮窗**贴屏幕底**浮着（**四边 30** + 阴影）、一进来**收起**。⚠️ 我第一版把"底部"读成"桌面=最下面那一条"（还问过他，他也选了那一条）⇒ 他看过真站点后更正 ⇒ **整套布局返工一次**（教训记进 `52` 开头：**"问他选一个"不等于"问清了"，选项要带图**） | ✅ **做完**（已部署） | `docs/dev/52-DESKTOP.md`（新） · 手册 **v1.22 → v1.23**（Z4 重写 · 默认档 · 阈值总表边距改四边 30） · `widgets/{app_desktop,chat_floater}.dart` | 上一版死在**高度定死**（3.1 倍下溢出 798px，那正是 D3.5 的反面）⇒ 现在**收起档由内容算、半开/最大化用系数**、高度由父层量好传进来。🔴 **闸又抓出两个真根因**：① **空屏不可滚** ⇒ 桌面占地后 3.1 倍下只分到 104px 而要 272px（**溢出 168px**）⇒ 改成没地方时能滚；② **拖拽手势绑在整块浮窗上** ⇒ **吃掉时间线滚动**（"重发"那条判据先红的 —— 它当时**没有判据**）⇒ 手势只绑抓手行 + 补回归判据。读数：客户端四道闸全过（unit **273** / 可访问性 **149** / widget **92+10**）· 新判据 **12 条**（`desktop_floater_test.dart`，多数带负向对照）· 顺带量出两个形状 bug（空屏不可滚 168px / 列宽被当成图标大小 ⇒ 293px 大卡片）|
| **㊴** | 🔴 **对齐项目 + 首页说清"它跟聊天差在哪"**（主人：*"帮我对齐项目以后，做一个总结……然后将内容更新到首页介绍上"*）：① 手册与 `AGENTS.md` 里**四处说假话的地方**逐条实测改正；② 写出**区别总结**（七条，每条带证据）；③ 首页加**对照块**（四条） | ✅ **做完**（已部署） | `docs/dev/51-VS-CHAT.md`（新） · 手册 **v1.21** · `landing_words.dart` · `landing_screen.dart` | ① **改掉的四处假话**：**"本机不是生产机"**（本机 8020 就是 `w.stalkerai.cn`，实测）、仓库地图还画着已删的 `apps/mobile`/`services/core`/`packages/protocol`、`08-SPEC` 说协议正文在**已删的** `packages/protocol/PROTOCOL.md`、`03/07` 里写死的行数与测试条数。② 总结七条**每条都能指到文件与测试**（`51-VS-CHAT.md` §一），另有一节明写**六件还做不到的**。③ 首页对照块**竖排**（不并排 ⇒ 3.1 倍字号也不会挤成一条缝）· 新句子全部进**禁用词闸**清单。读数：客户端四道闸全过（unit **273** / 可访问性 **149** / widget **91**）· 服务端 **635 全过** · 文档闸绿 · 完整性重建后「对上了 · 一条都没漏」 |
| **㊳** | 🔴 **文档分层瘦身**（主人："为了减少整体阅读量"）：把 `00-PROGRESS.md` 拆成**热表**（现在到哪 / 下一步 / 欠什么）+ **历史层**（**同一套节标题、原文逐字**）；新增 **L0 路由** `docs/INDEX.md`（问题 → 文件 §节，**零数值**）；新增**文档闸** `scripts/check-docs.mjs`（链接可达 + `#锚点`可解析 + L0 不许有数值 + 重名标题/锚），并挂在 `push-changes.sh` 的 `git add` **之前** | ✅ **做完** | `docs/INDEX.md` · `PROGRESS-HISTORY.md` · `scripts/check-docs.mjs` | 接手要读的那一份 **136KB → 29KB（−78%）**，历史 927 行**逐字保留**（**一条信息都没丢**）· 跨层指针用**显式锚**（`<a id="sN">`）钉住，与标题写法无关 · 🔴 **闸第一次跑就抓出我自己写错的 10 处锚点**（这就是它存在的意义）· 服务端 **635 全过** · 客户端三道硬闸全过 · **没动任何 `strict` 文件**（清单只"多了一条只报不拦的" ⇒ 见 §九）|
| **㉟** | 🔴 **注销那条路补上两件**（账 #39：**身份再确认** + **一笔给主人看的审计**） | ✅ **做完** | `43-AUTO-PROVISION.md` §十四 · 手册 **v1.19**（X3 ④ · §5.5） | ① **身份再确认**：注销要"**最近真的登录过**"（看令牌 `iat`，窗口 10 分钟）—— 而**续期不会刷新 `iat`** ⇒ 旧令牌刷不过去（判据钉着这一点）；卡在 `POST /api/cancel` 的**第一步**（旧令牌 ⇒ **409 + 什么都没发生**：不投申请、不撤令牌、不删账号）。② **审计**：**两份文件两个写入者**（服务 `data/audit.log` · 特权侧 `/var/log/hupo/tenant-audit.log`）⇒ **谁也伪造不了对方的行**；形状逐字一致（出处只有 `src/audit.js`）：`[时间] 事件 · 租户 · 用户 · 手机(掩码) · 说明`；给人看的那一支 `scripts/show-audit.sh` 按时间并起来。⚠️ 拒了也记（"有人想删、没让他删"）。读数：服务端 **635 全过**（+15）· 判据 **+7**（含**跨产物**比形状）· 客户端硬闸全过。🔴 **过程中真抓到一处 bug**：服务端调 `reauthOk` 时**没传注入的时钟** ⇒ 用的是真实墙上时间，被"一小时前的令牌"那条判据当场抓住（这类 bug 的形状是"判据时灵时不灵"）|
| **㊱** | 🔴 **统一页面风格**（主人：*"看看首页，就知道登录页需要改了，配置页也要改。聊天页也要改"*） | ✅ **做完**（已部署） | `49-STYLE.md` · 手册 **v1.20**（D3.10） | 把首页那六个颜色 + 三档圆角/间距**提上来当全站底子**（`models/design.dart` 一处出处；`ThemeData` 在 `screens/app_theme.dart` 拼）⇒ 四屏一套语言：纸底 `#F8F5EE` · 陶土红 `#C8452F` · 白卡 · 小标。逐屏：登录页**顶上放同一个标志**（与首页共用 `BrandMark`）· 配置页改成**小标 + 白卡** · 聊天页顶栏纸底无阴影、助手暖白卡、用户陶土淡色。🔴 **能一次改完是因为先量过**：全站**只有一处硬编码颜色**，其余全走 `colorScheme`。⚠️ **闸抓到两件**：① 配置页改成可滚列后，**3.1x 那档「关于」在折叠线以下**而 `ListView` 不建屏幕外的孩子 ⇒ 判据改成**像用户那样先滚再点**；② 肉眼看纸底上的暖白**会误判成灰** ⇒ 用像素采样确认（`#F8F5EE`/`#FFFDF9`）。⚠️ 顺手修好"看一眼某一屏"的工具：CDP 的 `Input.*` **送不到 Flutter**（页面逐字节不变）⇒ 改走**页内合成 pointer 事件**，另加 `--no-token`（未登录两屏，退出码 4）。读数：客户端三道硬闸全过（可访问性 **149**）· 网页产物 **608809577eb4** · 本机与公网 200 |
| **㊲** | 🔴 **登录页留着首页那个 header + 能回首页**（主人：*"登录页应该能回到首页。所以首页那个 header 也留在登录页吧。"*） | ✅ **做完**（已部署） | `49-STYLE.md` | 把首页那块 header（标志 + 红小标 + 大标题）抽成 `widgets/page_header.dart`，**两屏共用一份**（原来只有首页有）；登录页顶上用它 + 一个**返回箭头**（`onBack` ⇒ `_showLogin = false` 回首页）；登录页自己那句退成「所以这道门只有你能开。」（大标题已由 header 说了）。闸：`login_test.dart` +2（header 三样都在 · 箭头真的回首页 · **不给 onBack 就不画箭头**）|

> **①–㉞ 的逐条流水**（每条当时的读数、栽过的坑）在
> [`PROGRESS-HISTORY.md` §〇](PROGRESS-HISTORY.md#s0)。
> ⚠️ 那 34 条**都是做完了的**；**没做完的不许留在这里当历史**（没还的账一律进 §六）。

---

## 一、先说清楚：两把尺子不是一回事

> **搬走了** → [`PROGRESS-HISTORY.md` §一](PROGRESS-HISTORY.md#s1)。
> 一句话：计划那一边是"**给旧实现做手术**"，实际走的是"**从零新建**"
> ⇒ 问"批 N 做完没有"必须拆成两问（= 下面 §二 与 §三）。

---

## 二、第一问：旧缺陷清单核对（**这是"全新建立"真正的成绩单**）

> **搬走了** → [`PROGRESS-HISTORY.md` §二](PROGRESS-HISTORY.md#s2)
> （含 2.1–2.6 六张逐条核对表）。
> 一句话：那份"不许再出现"的清单逐条核过，**大多数是"从一开始就对"**，**证据在那边**。

---

## 三、第二问：批次表核对

> **搬走了** → [`PROGRESS-HISTORY.md` §三](PROGRESS-HISTORY.md#s3)。
> ⚠️ 这一问的答案**大半是"还没建"** —— 别拿"计划批 N"当进度。

---

## 四、⚠️ 对不上的地方（**这三条是真欠账，不是"排后"**）

> **搬走了** → [`PROGRESS-HISTORY.md` §四](PROGRESS-HISTORY.md#s4)（含 4.1 / 4.2 / 4.3）。
> 当时那三条欠账**都已还**（其后的账见 §六）。

## 四·补、⚠️ 卡在"四个决定"上的那一条（第 ④ 条的后半）

> **搬走了** → [`PROGRESS-HISTORY.md` §四·补](PROGRESS-HISTORY.md#s4b)。
> 那四个决定**主人 2026-09-21 已全拍**（逐条见 `39-PERMISSIONS.md` §七）。

---

## 五、实际做了、**计划里没有的**（账也要记上）

> **搬走了** → [`PROGRESS-HISTORY.md` §五](PROGRESS-HISTORY.md#五实际做了计划里没有的账也要记上)。
> ⚠️ 这一节值得留着看：**计划外的活往往才是真需求**（典型是第 ⑧ 条：浏览器那条流）。

---

## 六、欠的账（**明说，不许用"排后"混过去**）

> 🔴 **2026-09-23 主人定序（原话）**：*"现在先要实现功能，性能问题我们相对放宽一些。其他有任何问题，要告诉我详情，建议，我再来做决策。一个一个来。"*
> ⇒ **这张表按这条分两组**：**① 功能与「门 / 门框」**（先做）· **② 性能与资源**（**放宽**，不是砍 —— 仍然记着、仍然要还，只是不占现在的时间）。
> ⚠️ **安全、可用性、正确性那几条不算「性能」**：它们照旧要你拍板（#45 · #48/#58 · #49 · #65），> 详情与建议一件一件报。

> **这里只列还没还的。** 已还的（四十来条，含"怎么还的、判据在哪"）全在
> [`PROGRESS-HISTORY.md` §六](PROGRESS-HISTORY.md#s6)。
>
> ⚠️ **旧文档里引到"§六 第 N 条"的，去历史那一份找**（编号还认）。
> **已还/已判的**：#1–#16、#18、#20–#29、#31、#32、#34、#36–#41、#43、#44、#47、**#51（已拍板）**、**#53（已修）**
> ⇒ **还开着的 12 条就在下面这张表里**：#45 · #48 · #49 · #52 · **#58**（这五条卡在主人身上，见 `63-OWNER-DECISIONS.md`）+ **#65 · #66 · #67 · #68 · #69**（**2026-09-23 三路审计新查出来的**：出网零限制 · 没有每用户磁盘配额 · 闲置回收没接线 · 监控只剩内存 · **租户没有 CPU 上限**）+ **#70 · #71**（**2026-09-24 修 #91 时顺带查出来的**：盒子里跑不了命令 · 中心的影子世界）
> ⚠️ **原来在这张表里的 #23 与 #30 搬到了表下面的那一块**（它们**不是待办** —— 一条已判、一条是主人拍过板的已接受风险）
> ⚠️ **别在这里另抄一份合计**（两处一定会漂 —— 上一版就写着"14 条"，还把**已经修好的 #53** 列了进去，而后来加的 #58 / #64 没进这一行）。
> ⚠️ **别在这上面手算**：我**自己错过一次**（2026-09-23）—— 关掉 #46 那条（**它不是待办**）时顺手把"待办数"也减了 1。
> ⇒ 现在**这张表只放待办**（不是待办的都搬到下面那一块去了），所以表的行数就是待办数。
> ⚠️ **还了一笔就从这里搬走**（不是划掉了事），否则这张表会重新长回四十多行。

| # | 欠什么 | 大小 | 卡着谁 |
|---|---|---|---|
| **52** | 🔴 **语音只有「形状」，没有真的接上**（主人 2026-09-22 要「学微信」、并答「**先做假的**」）。卡在**两处**：① **声音走哪条路与 `D5.2` 冲突** —— `D5.2` 要「手机自带的识别器」（云端 ASR 与 P1 同类、地库归零），而**唯一上线的是网页版**（网页识别器是云端的）；② **「听筒」手册里根本没定**（`D5` 只定了「进」没定「出」，而且网页上没有听筒这个出口）。⚠️ 另：**批 5 是主人 09-21 亲口砍的**（「语音不做」），要做先回答「为什么还要自己做」 | —— | **要主人拍板走哪条路**（云端 / 本地模型 / 先做安卓包）；真接上还得按 `D5.10` 做 **四类各 20 条语料、句子级命中率 ≥90%** 的硬闸。详见 `docs/dev/55-VOICE-DEMO.md` §三 |
| **48** | 🔴 **线上服务不在 systemd 监管下** —— 它的 cgroup 是 `dsh-subprocess-<dsh 服务 pid>-<hash>.scope`，也就是**它是 DSH 工具调用里起的长命子进程**。⇒ `systemctl --user stop` 那种 scope 会**连服务一起杀**；**2026-09-22 我自己踩了一次：线上 502 了几分钟**（`restart-core.sh` 恢复） | 中 | 要定一个姿势：把它挪出 cgroup（`systemd-run --user --scope` / 一个 user unit / `setsid` 彻底脱离）——**现在只是"知道了、别再踩"，没有修**。根因与禁忌写进了 `AGENTS.md` §一、`06-OPERATIONS.md` §一 |
| **49** | 🔴 **口令以字面量进过命令行** ⇒ ① scope 的 description（`systemctl --user list-units` 就能看到）② **user journal**（很可能也进了 journal —— 原来只是"很可能"，**2026-09-23 实测确认**）| 小 | **2026-09-23 复核实测**：死 scope **只剩 1 个**（`dsh-subprocess-68836-560486e772cd.scope`，`ActiveState=inactive`、`MainPID` 空），它的 description 里**仍带着那一句字面量**；而 user journal 里带口令的行**实测 81 行**（7 行字面量 + 74 行"倒着写 + `rev`"，最早 2026-09-21、最晚 2026-09-23 当天）。⇒ **如实说**：`journalctl --vacuum-*` 只能**整段时间**删，会连别的项目的日志一起删 ⇒ **我没动 journal**。✅ **已经做的（止住增量）**：造清单那次改成**从 git 历史里取**（`PW=$(git show cca2c5e^:docs/dev/03-DEPLOY-WEB.md | grep …)`）—— 那口令**本来就是公开 history 里的**（见 §6.1 #30），命令行里**从此不出现字面量**（当天 `17:41` 那条 journal 行**实测不含**）。🔴 **真正的修**只有两条：**换口令**（#30，主人拍过板"不换"）或**给具体那几条命令开 NOPASSWD 白名单**（那就不需要口令）—— 两条都要主人签字 |
| **58** | ⚠️ **本机那几条 frp 隧道不是全都有单元管** | 小 | **2026-09-23 复核实测**（原来的说法过期了）：`frpc-dsh-u`（`u.stalkerai.cn`）与 `frpc-finart-v`（`v.stalkerai.cn`）**有 systemd 用户单元**（`active running`）；**只有 `frpc-w`（`w.stalkerai.cn`，就是线上壳）与 `frpc-apps`（`apps.stalkerai.cn`，小程序制品）是手动起的** ⇒ 重启机器之后**这两条不会自己回来**，现象是"网站 502 / 小程序打不开"（都不好查）。⚠️ 与 #48 同源（服务也不在 systemd 下）。这一批有个幂等脚本 `scripts/start-tunnels.sh`（**只拉那两条没单元的**，已经在跑的不动；跑完自己报读数）—— 它不解决"开机自启"。**真正的修法是给这两条做 systemd 单元**，那要连着 #48 一起定（这台机器上还有 DSH 的 scope 不能乱停）|
| **65** | 🔴 **容器**出网零限制** —— 手册 `08-SPEC.md` §4.2 原来把它写成【有效】的「白名单 + 审计」，**而代码里一次都没实现** | **大** | 审计证据（2026-09-23，只读）：渲染出的租户单元里**没有 `--network`**（`grep` 空）· 全仓 `iptables`/`nftables`/出口白名单/出口代理 **0 命中** · `src/model-proxy.mjs` 只转发默认上游。⇒ 今天容器**可以任意外联**（agent 能往外发请求）。**要做**：出口只放通模型 API 域名（`--network` + 代理/防火墙白名单）+ 请求审计；⚠️ 这动的是**部署期**的东西（宿主网络/单元）⇒ 按 P1/P2 **要主人签字**。手册那一行**已经改成实话**（目标态 · 未实现）| 
| **66** ⏸️**放宽** | 🔴 **没有每用户磁盘配额** —— 一人一台**共用宿主盘**，谁写满拖垮全机（本项目「最贵事故」那一类） | **大** | 审计证据：全仓 `xfs_quota`/单 LV/卷大小扫描 **0 命中**；只有按镜像字节估**台数**（`lib/tenant-capacity.sh`）与 `remove-tenant.sh` 里一次 `du -sh` 报告；`38-ISOLATION-SPLIT.md` §（那一节）自认「要按人配额」。**要做**：每卷 quota（例如 xfs project quota）+ 定期扫 volume 大小 + **满时明确拒**（不许静默写坏）| 
| **67** ⏸️**放宽** | ⚠️ **闲置回收的代码在、但产品里一次都没调** | 中 | 审计证据：旋钮 `src/config.js` 的 `agentIdleEvictMs`（30 分钟）与机制 `src/agent-runtime.js` 的 `evictIfNeeded()` 都在，但**产品代码零调用者**（全仓只有 `test/` 调它）；而租户单元是 `Restart=always` ⇒ **容器永不停**（只有 `remove-tenant.sh` 手工删）。**要做**：接线定时调 `evictIfNeeded`；再定「容器级闲置停/销毁」的策略（那一条与 #48/#58 同族，要签字）| 
| **68** ⏸️**放宽** | ⚠️ **监控只剩内存，而本机连内存都算不出**（CPU / 磁盘 / inode / fd 没有任何采样或闸） | 中 | 审计证据：只有 `src/admission.js`（0.75 准入 —— 但本机每层 `memory.max=max` ⇒ **判据算不出来**）与 `src/oom.js`（读 cgroup `memory.events`）；CPU/磁盘/inode/fd 判据 **0**；手册 §十一 自己写着那些是「**目标态（当前多数未实现）**」。**要做**：磁盘/inode/fd 采样 + 80/88/92/95 分级闸（手册那张表的目标态）| 
| **69** ⏸️**放宽** | 🔴 **租户容器没有 CPU 上限** | 中 | **2026-09-23 核过（只读审计 + 三处独立取证）**：渲染租户单元的那一段（`scripts/create-tenant-pool.sh`）只有 `--pids-limit` 与 `--memory`，**没有 `--cpus`**；全仓 `--cpus` / `CPUQuota` / `cpu.max` **0 命中**；租户的 systemd 用户单元里也没有任何 CPU 限制。⇒ 本机 **32 线程**，**一个租户里跑飞的 agent 能把整机 CPU 吃满** —— 而"主线"（主人那一份服务）和别的租户都被它拖慢（内存那条已经挡住了，CPU 这条没有）。⚠️ 与 #65/#68 同族（都是"手册/直觉以为挡住了、其实没有"）。**要做**：渲染时加 `--cpus=`（值定多少是容量策略，见 `lib/tenant-capacity.sh`），顺带在准入/监控里加 CPU 判据（#68）|
| **70** | 🔴 **盒子里跑不了命令** —— 租户容器里 **DSH 的沙箱后端起不来**（镜像里缺 **`bubblewrap`** 这类东西），而"要更宽的权限重试"这条路在盒子里**没有批准通道** ⇒ 任何命令都被直接拒 | 中 | **2026-09-24 取证**（修 #91 时顺手查的）：agent 自己在会话里说清了 —— *"沙箱后端起不来，机器上缺 bubblewrap 这类东西；想用更宽的权限重试，需要有地方让你点确认，这里没有批准通道"* / *"（缺 bubblewrap / Landlock），workspace-write 下任何命令都被直接拒"*（从盒子里的 `session.v3.jsonl.zstd` 解出来读的）。**后果**：盒子里"写文件"通、"跑命令/自测/确认生效"**全不通** ⇒ 它自己都不敢说自己改的东西生效了（**如实说了，没糊过去**）。**要主人拍板**（三种）：① **把 `bubblewrap` 装进租户镜像**（推荐；命令照旧只能碰它自己那格）② 或把盒子的沙箱模式放宽到 `danger-full-access`（= 它能在盒子里到处跑；钥匙那份 `EACCES` 不受影响，但**不是没代价**）③ 或接一条批准通道（大）。⚠️ 动的是**部署期**（镜像/单元）⇒ 按 P1/P2 **要签字**，见 `63-OWNER-DECISIONS.md` §三 |
| **71** | ⚠️ **中心的"影子世界"** —— 静态映射的租户**世界在盒子里**（他的 `main.jsonl` 在 `/data`），而中心服务 `data/users/<u>/` 下还留着一份**从头到尾没被清掉的影子**（`hupo/apps` 里那几个探针制品**主人在桌面上从来没见过**）；更实在的是：`src/server.js` 里 `/api/app-ask` 那道闸读的是 **`worldFor(claim.sub).apps`（中心那份）**，而真正在用的制品在盒子里 | 中 | **2026-09-24 取证**：`u2` 中心那份有 `wenda`/`tianqi-probe`，盒子里 `/data/hupo/apps` 是**空的**；而盒子里以 uid 1000 跑 `app_list` 回"他现在还没有自己的小程序"⇒ 两边**确实不是同一份**（契约 `59-USER-APPS.md` §七·补二 记的"两份东西打架"就是这个形状）。**要做**：定一条规矩 —— 映射租户**只认盒子那一份**（中心那份要么删掉、要么明确只做转发缓存），并把 `/api/app-ask` 的闸挪到"真正服务他的那一侧"（与 V13 同族）。⚠️ 在那之前**别用中心那份做任何判断** |
| **45** | 🔴 **临时验证码还开着**（`HUPO_DEV_CODE`）⇒ **任何手机号 + 它都能进** | —— | 开机横幅每次都如实喊这一句。⚠️ 这不是 bug，是**开发期的门**：手机号就是账号 ⇒ 开着它 = "谁都能建一个号、占一台容器"。**上线前必须关**（关掉之后登录要走真短信通道，那条通道还没接）。⚠️ 主人 2026-09-21 说过"不换口令继续"是针对**口令**的，不是针对这一条 —— 所以它**记在这儿，不许静默留着** |

### 6.1 ⚠️ **不是待办、事实仍要留着的两条**（原来的 #23 与 #30）

> 它们原来住在上面的表里 —— 那会让"待办"这个数每次都得手算（**我算错过一次**）。
> **事实一个字没改**，只是换了地方：这两条**下一个人不必再当任务做**。

| # | 是什么 | 大小 | 事实（**照旧完整**） |
|---|---|---|---|
| **23** | **`~/.dsh/storages/` 那类运行时数据算不算"开机自动读"** | 小 | ✅ **已判**（决策 **C1**）：**不进清单**（`strict` = 天天拒绝启动；`report` = 每次都响的报警）。⚠️ **代价**：KV 那条路没被覆盖 —— 见 `19-P1-P2.md` §七 第 5 条 |
| **30** | ⚠️ **线上口令**（2026-09-21 查出来的泄露） | —— | ⚠️ **主人 2026-09-21 决定：不换**（*"不换口令继续"*）⇒ 这一条从此是**已知并接受的风险**（和 §九 那条同款），**下一个人不必再提**。⚠️ **但记录必须把事实写全**，因为这一条比 §九 那条**重**：<br>① `docs/dev/03-DEPLOY-WEB.md` 第 5 行原来**写着口令本身**，**2026-09-20 进 git、并且推到了 GitHub**；<br>② 🔴 **那个仓库是公开的** —— 实测 `api.github.com/repos/aaffdd11/hupo` → **`"private": false`** ⇒ 那段时间里它是**公开可读**的，而且**现在仍在公开的 git 历史里**（删掉工作区那一行只能止住继续传播，止不住已经发生的）；<br>③ 同一个口令**也是这台机器的 `sudo` 口令**。<br>**已经做了的**：`cca2c5e` 把它从仓库里删掉（改成"问主人"）。**没做的**：换口令（主人决定）、重写历史（破坏性操作，要主人点头）。⚠️ 历史重写**不解决**问题（公开过的就是公开过的），**只有换口令才是修**。|

---

## 七、页面上还有两处**指令是错的**（顺手记下）


1. ~~**`AGENTS.md` §5.1 说"`test/widget` 现在 49 条挂 10"**~~ —— ✅ 第 ① 条顺手改了：
   那是**旧 app** 的数；而且 `v2` 现在**有** widget 测试了（5 条，全是第 ① 条加的）。
2. **`AGENTS.md` §5.1 让改 `v2/apps/mobile`，但 `test/widget` 那句是旧 app 的经验**——
   两条混在一段里，下一个人会去找一个不存在的目录。

---

## 七·补、🔴 **"整理整个 UI"这一件**（主人 2026-09-23）

勘察（四块真图）与逐块问题清单在 [`72-UI-PASS.md`](72-UI-PASS.md)。**本次做完两件**：
① 输入条那一片（话筒不再藏、正在听有红点与实时大字、听时不许发送）；
② 🔴 **关于页里那句假话**（*"我们自己没有另外做一个话筒。"* —— 话筒 2026-09-23 就做了）
⇒ 改成按设备说（`aboutFactsFor(canHear:)`）+ 回归条钉住。
**剩下**：A 桌面 · B 聊天浮窗 · D 设置/关于的层级 · E 圆角/阴影/配色对表 —— 按 A→B→D→E 一件一件做。

## 八、建议的下一步顺序

> **1–10 那张单子清完了**（原文在 [`PROGRESS-HISTORY.md` §八](PROGRESS-HISTORY.md#s8)）。
> ⚠️ 那边第 8 条要留着看：它**不在任何计划表里**，而且当时**所有闸都是绿的** ——
> 是主人平板上一句"时间线一直空着"暴露的。⇒ 判据 **V13**（`08-SPEC.md` §13.3）
> **比那次修补值钱：凡是客户端自己算出来的东西，闸必须打在客户端这一侧。**


### 8.1 **接下来做什么**（2026-09-22 对齐时重排）

> 上一张单子（1–10）清完了。这一张按**同一个判据**排：
> **"哪个不做，后面的会以什么形式失败"**。
> ⚠️ 序号不表示"必须按这个顺序"，只表示**我建议的先后**。

| # | 做什么 | 为什么是它 / 不做会怎样 | 卡在谁身上 |
|---|---|---|---|
| **0** | ⏳ **语音本体：等你那两分钟**（`docs/dev/69-ASR-ROUTES.md`）—— 09-21 砍它的理由（"输入法自带的语音够用"）**今天没人重新验过**；验法/判据/三条路都写好了 | 够用 ⇒ 明说砍掉本体（形状留着）；不够用 ⇒ 从 A 云端 / B 本地模型 / C 安卓包 里选一条 | **主人**（两分钟）|
| **1** | 🔴 **关掉临时验证码**（`HUPO_DEV_CODE`） | 开着 = **谁都能拿任意手机号建号、占一台容器**。这是"能不能给人用"的第一道门 | 主人（一句开关）· 但**关掉之前得先接真短信通道**，否则没人进得来 ⇒ 这条其实是"接短信" |
| ~~**2**~~ | ✅ **已还（2026-09-22）**：一把真钥匙走通了 `401 → 200`（账 #44）—— 真答了一句 + 负向对照 + 盒内代理打真上游 200 | 见 §六 #44 与 §〇 ㊱ | —— |
| ~~**3**~~ | ✅ **已还（2026-09-22）**：注销那条路上那两件（**身份再确认** + **一笔给主人看的审计**）（账 #39）—— 旧令牌 ⇒ 409 且**什么都没发生**；驳回也留痕 | 见 §〇 ㉟ 与 `43-AUTO-PROVISION.md` §十四 | —— |
| ~~**4**~~ ✅ **已还**（见 §〇 与 §六）| **失败分类器其余几档**（账 #33）：上游不通 / OOM | 用户看到的是"卡住了"，而**真原因是上游挂了**（最要紧那一档 `AUTH` 已还） | 助手 |
| ~~**5**~~ ✅ **已还**（见 §〇 与 §六）| **拿掉镜像里那份 `src/` 兜底**（账 #42） | 条件**已经满足**（两台都迁移完、`--live` 绿）⇒ 下次重造镜像时顺手做；不做就是镜像里多 596KB、以及"两处代码"的隐患 | 助手 + 一次 root（重造 + 重载） |
| ~~**6**~~ ✅ **已还**（见 §〇 与 §六）| **容量上限按磁盘算**（账 #35）——原与它并列的"盒内旧垃圾"（账 #36）**已还** | 小；但装机时**会如实报"放得下几台"**这件事只差"让模板能生成上限" | 助手 |
| **7** | ⚠️ **我建议加的一条**：**"第一句真话"的端到端验收**（新号 → 开一台 → 贴真钥匙 → 真的答一句 → 导出 → 删掉 → 回收站里拿回来） | 上面每一条都有人验过，但**没有一条路是"从注册到一个真回答"一口气走完的**。这是"能不能给人用"的**总判据**，而且它会顺手暴露那些只在两个功能接缝处才出现的毛病（这个项目已经栽过三次） | 主人（真钥匙）+ 助手 |
| **8** | 🔴 **小程序的机制闸**（[`59-USER-APPS.md`](59-USER-APPS.md) §八 第 1 条） | "**他明说才许写**"现在还是**软的**（只有工具描述 + 审计留痕；服务端看不到"这一轮他说了什么"）⇒ 助手越权的唯一挡板是它**自己的自觉** | 助手（要动 `dispatcher`，把当轮输入递给工具那条路） |
| ~~**9**~~ | ✅ **已还（2026-09-23，见 §〇 80）**：**标出处** —— 上游的 `tool/result.meta` 接进来了，屏幕上那一段现在真会出现 | 见 `docs/dev/67-SOURCES.md` | —— |
| ~~**10**~~ ✅ **已还**（见 §〇 与 §六）| **字体缓存预热**（账 #64） | 冷缓存那次访问中文会**晚几秒**（只影响第一次，但没有它 = 一个字都没有） | 助手 |

> ⚠️ **第 7 条是我加的，不是账上原有的** —— 写在这儿是为了让它**有机会被砍**：
> 如果主人说"不做"，那就明说砍了（而不是含糊地留着）。

---

## 八·补、⚠️ 收尾优先（**主人 2026-09-21 的规矩**）


> **主人的原话**：「**永远先收尾做总结，再做其他的。**」

⇒ 这条把"下一步"的顺序**定死了**：**上一件没有收尾，就不许开下一件。**
理由不用猜：这个项目里"做一半"是最贵的状态（`04-ROADMAP.md` §十一
「做一半比不做更坏」），而**没收尾的批次**恰恰是最容易变成做一半的那种。

**"收尾"具体指什么**（四件，缺一件都不算）：

| # | 收尾要做的事 | 判据 |
|---|---|---|
| 1 | **两边的硬闸都跑绿** | 服务端 `npm test`、客户端 `check-client.sh` |
| 2 | **能部署的部署掉**（客户端改了就必须部署） | 公网产物指纹对得上、浏览器检查通 |
| 3 | **文档与账对上**（`docs/dev/` 那一篇 + 本文的 §〇/§六 行） | 数字不许有两处、已还的账要划掉 |
| 4 | **推上去 + 给主人一份总结**（做了什么 / 验在哪 / 还欠什么） | `push-changes.sh` 成功 |

⚠️ **半途中的东西要写在这里**（"在飞的东西"那一栏），不许让它只在对话里存在。

**现在在飞的东西**：**没有**（2026-09-23 收尾时确认）。

> 上一轮记在这儿的"三批只在本地提交"（`3577786` / `236bd84` / `8a80969`）**已经推上去了**
> —— 挡路的那个外来探针（`test/widget/zz_probe_test.dart`，主人自己那个会话在查 `lifecycleState`）
> 主人处理掉了，树干净之后 `push-changes.sh` 一次把这几个提交都推上去了。
> ⚠️ **留个教训**：推送工具**按设计**拒绝"树上有别人的脏文件"时，**不是故障，是护栏**；
> 那一刻该做的是**记进本节 + 等**，不是绕过去（`AGENTS.md` §5.3）。

> ⚠️ **在飞的东西不许只活在对话里** —— 有就写在这一行下面，收掉了就改成"没有"。

> 🔴 **2026-09-23 补一条同类**：**"在飞的东西"也包括会话的待办清单** ——
> 主人那天问*"上方有一个列表和删掉的文字。怎么回事"*：那是 **DSH GUI 自带的计划面板**
> （显示**这个会话**的目标 / 任务清单，`completed` 被画成**删除线**），而它挂的是
> **几天前清账时写下的快照** —— 清单**不会自己更新**，于是"已完成"里好几条早就还清了、
> "待办"里也有已经做完的。⇒ **两件事**：
> ① **收尾时顺手把那张清单重写成"现在真的"**（它是给人看的，过时了比没有更坏）；
> ② ⚠️ **删除线会被读成"删掉了"**（主人就是这么读的）—— 产品里那条计划条同理。


> ⚠️ **在飞的东西不许只活在对话里** —— 有就写在这一行下面，收掉了就改成"没有"。


### 八·补.1 ⚠️ 收尾时踩到的两个坑（都记下来，别重复）

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。教训已进 §十 的三条硬规矩。

### 八·补.2 #15 那一轮的收尾结果

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。当时那一轮怎么收的，只当证据。

### 八·补.3 批 3 怎么切（**动手前先定顺序**，2026-09-21）

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。当时的切批方式，只当证据。

### 八·补.4 🔴 **"一个新用户真的能用"还差什么**（主人 2026-09-21 问）

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。**已被 §八.1 取代 —— 照 §八.1 排。**

### 八·补.5 🔴 **还差什么才算"一个新用户真的能用"**（2026-09-21 晚更新）

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。只剩**一个开关**（见 §六 #45）。

### 八·补.6 🔴 **"新号自动拿到一台容器"——服务侧做完了，特权侧等主人签字**

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。特权侧**已装**、**真建出过一台**（§〇 ㉙）。

### 八·补.7 🔴 **"钥匙到底能不能用"——真机端到端跑出来了三个断点**

> **搬走了** → [`PROGRESS-HISTORY.md` §八·补](PROGRESS-HISTORY.md#s8b)。三个断点全修完，**真钥匙走通了 `401 → 200`**（§六 #44，已还）。

---

## 九、攒着要重建清单的改动（`AGENTS.md` / 手册 / 人格）

> ✅ **上一轮欠的那次重建已经做了**（2026-09-22 05:12，第十四回）——**只报不拦**的那两项
> （新增 `scripts/check-docs.mjs`、`push-changes.sh` 内容变了）连同**本轮改的手册与
> `AGENTS.md`（`strict`）**一起重建，复核结果：**「✅ 对上了 · 一条都没漏」**。
> 留痕在 [`PROGRESS-HISTORY.md` §九·补2](PROGRESS-HISTORY.md#s9b-2)。
>
> ⚠️ **那次重建踩出一个有用的现象**（值得记住）：重建**之前**跑部署脚本，
> 它会报 `✗ 线上构建指纹不是 …` —— 因为 `strict` 对不上 ⇒
> **`restart-core.sh` 的预检拒绝停旧服务**（这是 `#41` 那个修法在正确工作：
> 服务留在 200，而不是整站 502）。⇒ **改完手册必须先重建、再部署。**

### 九·补3 · 第十五回重建 + **一次机器状态的改动**（2026-09-23，语音本体那一批）

| 项 | 内容 |
|---|---|
| 为什么重建 | 这一批改了 `docs/handbook/{05-DECISIONS,08-SPEC}.md`（`strict`，**会拒绝启动**） |
| 重建前 | `✗ 对不上：2 处会拒绝启动（两份手册）+ 4 处只报（`serve.js` / `server.js` / 两个脚本）` |
| 重建后 | `✅ 对上了`（同一份清单：50+ 个 strict 条目一条没漏） |
| ⚠️ **另一件不属于清单的改动** | **VPS（`120.26.179.211`）上的 nginx**：`/etc/nginx/conf.d/w-stalkerai.conf` 里 `location = /api/stream` ⇒ `location ~ ^/api/(stream\|asr)$`（**主人 2026-09-23 当场授权**）。原因：公网那一跳只给 `/api/stream` 配了 WebSocket 升级头 ⇒ 新的 `/api/asr` 在公网握不上手（浏览器 `1006`），而本机直连一切正常。**改法**：`python3` 改那一行 + 注释（先 `cp -a` 备份成 `w-stalkerai.conf.bak-20260923215845`）⇒ `nginx -t` 成功 ⇒ `systemctl reload nginx` ⇒ 公网 `w.stalkerai.cn` 仍 200、`wss://…/api/asr` 回 `{"type":"asr/unavailable",…}` + `close:1000`。⇒ **这一条要进审计**：机器状态改了一次（不是仓库里的文件） |
| 又重建了一次 | 同批：`scripts/restart-core.sh`（**只报不拦**）与两份手册一起重建，复核 `✅ 对上了` |
| ⚠️ **机器状态的改动（第二件）** | `scripts/restart-core.sh` 的 env 装载方式：`setsid nohup env \ …`（无命令 ⇒ 打印整个环境 + 变量其实没传）⇒ 显式 `export HUPO_DATA/HUPO_PORT/HUPO_WEB` + 启动前 source `data/asr.env`（只报齐没齐）。**根因**：bash 续行符在注释之前就接行，命令被那条注释结束。**验证**见 `docs/dev/71-MIC-ASR.md` §6.3 那张表 |


> `AGENTS.md`、`docs/handbook/**`、`hupo-persona.yml`、`~/.dsh/profiles/*/cordis*.yml`
> 是开机清单里的 **`strict`**：改一次，主人就要补一条重建命令，否则**下次重启会拒绝启动**。
> ⇒ 约定（2026-09-21 与主人商定）：**助手攒起来，一次告诉主人**，而不是每批都来烦他。
>
> ⚠️ **攒着期间的代价要明说**：那几处文档会**暂时与实际不一致**。
> 所以这一节存在 —— 它让"欠着的那几处"是**写着**的，不是藏着的。

| # | 想改哪一处 | 改成什么 | 为什么值得改 | 现在靠什么顶着 |
|---|---|---|---|---|
| 1 | `AGENTS.md` §1.1（本机事实） | 补一条：**这台机器**（`dsh-subprocess-*.scope`，从自己到 `/user.slice`）**每一层 `memory.max` 都是 `max`** | 下一个人做准入闸/容量相关的事时，不用自己去翻 `/proc` 才发现判据算不出来 | 每次开机横幅都写着「准入 ⚠️ **算不出判据**」；细节在 `20-ADMISSION.md` §二 |
| 2 | `AGENTS.md` §四「下一步」那一行 | 现在写的是"按 `00-PROGRESS.md` §六 排"（**已经是指针**，不会漂）—— 暂时不需要改 | —— | —— |
| 3 | **`AGENTS.md` §五（干活的标准流程）** | 加一条：**永远先收尾做总结，再做其他的**（收尾的四件：两边硬闸绿 / 能部署的部署掉 / 文档与账对上 / 推上去 + 给总结） | 这是**主人定的顺序**，下次接手的人必须知道 | 现在只写在本文「§八·补」 |
| 4 | **`AGENTS.md` §八**（P1/P2 那一节） | ⚠️ 按主人 2026-09-21 的指令：**开发期助手可以用 `sudo`**，**部署期** P1/P2 原样有效；本机的护栏**降级为审计**（每次重建留痕）。逐字稿在 `23-DEV-VS-DEPLOY.md` §四 | 这是**治理层**的改动，必须写进手册；不写就等于"助手按自己理解执行" | 现在只写在 `docs/dev/23-DEV-VS-DEPLOY.md`（**不是受保护文件**），谁读谁看见 |
| 5 | **`AGENTS.md` §5.1（改客户端那条流程）** | 加一行：**浏览器那条路的端到端检查** —— `HUPO_TOKEN=<令牌> node scripts/check-web-browser.mjs`（本机浏览器已装在 `~/.cache/hupo-chrome`，不用参数；它验"页面自己那条 WS"+"续期有没有换新"，还可 `--shot` 截图） | 这是"客户端自己算出来的东西"那一侧的闸（V13），**下一个改客户端的人该知道它存在** | 现在只写在 `22-DEBTS.md` §一 |
| 6 | **`05-DECISIONS.md` A 组 + `06-OPERATIONS.md` §3.2** | 同上：加"开发期 / 部署期"两档，并把"摘够"那条限定成**部署期** | 同 3 | 同 3 |

> ✅ **已落地**（2026-09-21 02:22，commit `f12fa88`）。补丁 `proposals/2026-09-21-dev-vs-deploy.patch`
> 覆盖了上表 **#1 / #3 / #4 / #5 / #6**，外加 `scripts/push-changes.sh` 改成**必须显式给路径**
> （旧版 `git add -A` 已经顺带提交过 4 次别人的活）。
> ⇒ **上表这几条从此作废**（留着是给"当时欠着什么"留证据）；只有 **#2** 本来就不必改。

### 九·补 新攒的两条（2026-09-21，**等下一次一起重建**）

| # | 想改哪一处 | 改成什么 | 为什么值得改 |
|---|---|---|---|
| 7 | **`AGENTS.md` §八** | 补一句：**建开机清单要按「仓库属主」的 home 算**（`sudo` 下 `homedir()` 是 `/root`，会把 `~/.dsh/**` 那三条算成不存在的路径而**静默跳过**）；并说明 `--list` 里标着「这条钉不住」的条目等于没钉 | 这个坑 2026-09-21 真踩到了，而且**藏了很久**（`19-P1-P2.md` §八）。不写进说明书，下一个人还会这么建 |
| 8 | **`docs/handbook/06-OPERATIONS.md` §3.2** | 同上：把"重建清单"那一步写成"**看它打出来的『按谁的 home 算』那一行**" | 手册里那一节是主人照做的步骤；步骤里没有这一眼，就等于没有这道闸 |
| **9** | **`docs/handbook/08-SPEC.md` §13.1 的 V4**（+ `CHANGELOG.md`） | **V4 重划**（**手册 v1.8**，2026-09-21）：从"容器里不许有秘密"改成"**盒内除持有者之外谁都够不着**" —— `a` 以 agent 身份读凭据 ⇒ 必须 **`EACCES`**（不许 `ENOENT`）；`b` agent 进程 env = 0；`c` `grep -rl "sk-" /data /home` = 0 **但明写它绿了不算过**。**两条负向对照不能省** | 🔴 **主人 2026-09-21 拍的 ①②**：旧 V4 与"用户自己的凭据要进他自己的容器"**直接冲突**，而冲突时唯一过法是**把凭据挪出扫描范围**——那正是本项目最忌的"**看着像过了**"。⇒ **不改不行**；依据在 `39-PERMISSIONS.md` §5.4–§5.6、§六 |

> ⚠️ **这三条都是 `strict`**（`AGENTS.md` / 手册）⇒ 改完**必须再重建一次清单**。
> 所以攒着，**和下一次改动一起给主人一次**。

### 九·补2 重建留痕

> **搬走了** → [`PROGRESS-HISTORY.md` §九·补2](PROGRESS-HISTORY.md#s9b-2)。
> ⚠️ 那一节是**审计凭证**（`AGENTS.md` §8.1 点名要的那一行一行），**只追加、不许删**；
> 但它**不该被接手的人读到** —— 所以住历史。

---

## 十、提交信息与内容不符的那几条（**我自己造成的**）


`866bec5` 与 `863d466` 两条提交，**信息写的是本页那段纪律补记，实际提交的是客户端返工那半**
（`timeline.dart` 等）。根因两步：

1. 我的 `python` 改文档时**锚点是从被 `cut -c` 截断的输出里猜的** ⇒ `assert` 失败、文档**没改成**；
2. 而 `push-changes.sh` 是 `git add -A` + 提交 ⇒ **它照样建了一个提交**，信息却是我给的那句。

⇒ **两条硬规矩（对我自己）**：

| # | 规矩 |
|---|---|
| 1 | **改文档不许"猜原文"**：要么用 `edit` 工具（找不到会当场报错），要么**直接追加到文件末尾**（不需要锚点）。**绝不用截断输出里的片段当锚点。** |
| 2 | **改与推必须用 `&&` 链起来，并且中间加一道 `grep -q` 复核** —— 复核没过就**推不出去**。这一条和 §八·补.1 的第 2 条是同一个道理，只是这次犯在我自己身上。 |

### 十·补 ⚠️ 第三条硬规矩：**改文档的脚本，写盘不许放在最后**（2026-09-21，我栽了三次）


**现象**：我用 `python3 - <<'PY'` 内联脚本改这份文档，**字符串用 `"` 定界、里面又写了 `"`**（比如 `"8 条悬空"`），
Python 当场 `SyntaxError` —— 而**写盘那一句在脚本最后** ⇒ **前面几条改动一条都没落**。

**最坏的一次不是"没改"，是"账说了没改的话"**：脚本失败之后我照样跑了 `push-changes.sh`，
它把**同一批里的别的文件**提交推上去了 ⇒ 远端的提交信息说"账已记"，而账里根本没有那一段。

⇒ **三条规矩**（和 §十 上面那两条同一个道理：**光记纪律没用，要让它在构上不可能出错**）：

| # | 规矩 |
|---|---|
| 1 | **改这份文档一律写成 `.py` 文件再跑**（`write` 工具落盘），**不用内联 heredoc** —— 引号/转义在 heredoc 里全靠运气 |
| 2 | 非要内联，**写盘要放在每一处改动之后**（一条一写），不许"全部改完再 write" |
| 3 | **改完立刻 `grep` 复核那一句真的在文件里**，再推（§十 第 2 条已经这么要求了，这次是我没照做） |

⚠️ 实测代价：三次里有 **两次**是"改动没落、但推送已经发生"，只有一次侥幸被 `SyntaxError` 拦在写盘之前。

> ⚠️ **这三条比前面两条更值钱**：实测三次里有**两次**是"改动没落、但推送已经发生" ——
> 远端的提交信息说"账已记"，而账里根本没有那一段。**这比"没改"坏得多。**

---

## 十一、⚠️ 接手的人从这里开始

> **这一段是给下一个上下文**（新会话 / 压缩之后的自己）看的。

**只要读三处就能接上**：

1. **`AGENTS.md`** —— 这台机器是什么样、硬闸怎么跑、不许做什么；
2. **本文** —— **§六 欠的账**（还欠什么）→ **§八.1 下一步**（先做什么）→ **§九**（攒着要请主人重建的）；
3. **要"凭什么"的时候** —— [`PROGRESS-HISTORY.md`](PROGRESS-HISTORY.md) 同名节。

**现状不要抄，跑出来**（数字自己会出来，抄下来的那天就开始过期）：

```bash
cd v2/services/core && npm test        # 服务端硬闸
bash scripts/check-client.sh           # 客户端三道闸
node scripts/check-docs.mjs            # 文档闸（链接 + 锚点 + L0 不许有数值）
```

**上下文快用尽时的三个征兆**（2026-09-21 全中，认得出就该停）：
**开始"猜"原文而不是去读** · **不核对就声称**（"改了"其实没改） ·
**把命令用 `;` 串起来而不复核**（红了照样推）。**出现这三样，就停下收尾。**

### 十一·补 / 十一·补2 / 十一·补3

> **全部搬走了** → [`PROGRESS-HISTORY.md` §十一](PROGRESS-HISTORY.md#s11)。
> ⚠️ 那三块是 **2026-09-21 的交接流水**（当时的测试数、当时的 commit、当时的"下一步"）
> —— **已经过期，只当证据，别照着做**。
> **留着它们的唯一理由**：它们记着"当时树上有半成品、接手要先自己看清"这类教训的原话。

