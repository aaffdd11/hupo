# 63 · ⚠️ **卡在主人身上的四条账**（欠账 #45 / #48+#58 / #52 / #49）

> 主人 2026-09-23：**"欠账要清掉"**。能自己清的已经清了（见 `00-PROGRESS.md` §〇 65–70）。
> **剩下 8 条里，这 4 条不是我不做，是它们要你一句话**（签字 / 拍板 / 一个开关）。
> ⚠️ 这一页**只写"要你做什么"**，理由与细节都在各自的账里（见下表最后一列）。
> ⚠️ **P1/P2 的规矩**：部署期（服务用户的那一套）**助手只提申请，签字的是主人** ——
> 所以这四条我**不会自己按按钮**。

---

## 一、要你做的四件事（一句话版）

| # | 要你做什么 | 不做会怎样 | 我这边准备好了什么 |
|---|---|---|---|
| **1** | **选一条短信通道**（哪家、花不花这个钱），然后我接线、你**关掉临时验证码** | `HUPO_DEV_CODE` 还开着 ⇒ **任何人拿任意手机号 + 那个开发码就能建号、占一台容器**。这是"能不能给第二个真人用"**唯一那道门** | 接线需要的改动我已经看明白（登录那条路今天走的还是开发码那条分支）；**只差通道与凭据** |
| **2** | **签字**：让线上服务与四条隧道**归 systemd 管**（#48 + #58） | **这台机器一重启** ⇒ 网站 502、小程序打不开，而且**不好查**（服务现在活在 DSH 的 scope 里、隧道是手动起的） | ⚠️ 要动的是**线上那道门**，我按 P1/P2 **只提申请**；你点头我就把"一条命令 + 回滚"写出来（**回滚是 `scripts/start-tunnels.sh` + `restart-core.sh`**） |
| **3** | **拍板**：语音走哪条路（**云端** / **本地模型** / **先做安卓包**），或者**明说砍了** | 页面上那套语音**是假的**（`voiceNotWired` 那句写着"还没有真的开麦"）—— 留着"假的"不算欠账，但**"要不要真做"这件事悬着**就一直是"做一半" | 三条路各自的代价我已经在 [`55-VOICE-DEMO.md`](55-VOICE-DEMO.md) §三 列好；真做要按 `D5.10` 做**四类各 20 条语料、句子级命中率 ≥90%** 的硬闸 |
| **4** | **允许我清一次 journal**（#49 的后半） | 上一轮我用 `sudo -S` 时**整条命令成了 systemd scope 的 description** ⇒ `systemctl --user list-units` 里能看到那串口令，**很可能也进了 journal**。那三个 scope 是死的，但我**停不掉**（挂着当前 DSH 进程，杀了等于杀掉本会话） | ① 等它们随 DSH 退出自动消失 / ② **清一次 journal**（要你允，因为 journal 也是证据）/ ③ **已经落地**：以后口令不许以字面量出现在命令行 —— 所以**这三条里只剩 ② 需要你** |

> ⚠️ **#30（线上口令泄露）不在这张单子上**：那是你 2026-09-21 拍过板的**已知并接受的风险**，
> 下一个人不必再提（记录在 `00-PROGRESS.md` §6.1）。

---

## 二、你不点头，我也能自己做完的那几条（**不用等**）

| # | 是什么 | 什么时候做 |
|---|---|---|
| **#19** | 多人命名空间的 `userId` 那一半（今天靠"退出登录 / 服务端判死时清缓存"兜着不串） | 我下一批做 |
| **#35** | 容量上限按磁盘算（现在模板里是个常数，装机时会如实报但**不替你改**） | 我下一批做 |
| **#42** | 镜像里那份 `src/` 兜底（**"拿掉它"的条件已经满足**：两台都迁移完、`--live` 绿）⇒ 下次重造镜像时顺手拿掉 | 要一次 root（重造 + 重载），我会先跑判据再动 |

⇒ 一句结束语：**这四条你回一句话**（比如"短信用 X"、"systemd 你来做"、"语音砍了"、"journal 清吧"），
我就能把剩下的都清掉；**不回的这四条我会一直记在账上，不会假装做完了**。
