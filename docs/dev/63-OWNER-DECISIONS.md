# 63 · ⚠️ **卡在主人身上的账**（欠账 #45 / #48+#58 / #52 / #49 + **2026-09-23 审计新查出来的 #65–#68**）

> 主人 2026-09-23：**"欠账要清掉"**。能自己清的已经清了（见 `00-PROGRESS.md` §〇 65–70）。
> **剩下 9 条里，这 8 条不是我不做，是它们要你一句话**（签字 / 拍板 / 一个开关）——
> 下面 §一 是**早就有的四条**，§三 是**三路审计当天新查出来的五条**（都是**部署期**的事）。
> ⚠️ 这一页**只写"要你做什么"**，理由与细节都在各自的账里（见下表最后一列）。
> ⚠️ **P1/P2 的规矩**：部署期（服务用户的那一套）**助手只提申请，签字的是主人** ——
> 所以这四条我**不会自己按按钮**。

---

## 一、要你做的四件事（一句话版）

| # | 要你做什么 | 不做会怎样 | 我这边准备好了什么 |
|---|---|---|---|
| **1** | **选一条短信通道**（哪家、花不花这个钱），然后我接线、你**关掉临时验证码** | `HUPO_DEV_CODE` 还开着 ⇒ **任何人拿任意手机号 + 那个开发码就能建号、占一台容器**。这是"能不能给第二个真人用"**唯一那道门** | 接线需要的改动我已经看明白（登录那条路今天走的还是开发码那条分支）；**只差通道与凭据** |
| **2** | **签字**：让线上服务与四条隧道**归 systemd 管**（#48 + #58） | **这台机器一重启** ⇒ 网站 502、小程序打不开，而且**不好查**（服务现在活在 DSH 的 scope 里、隧道是手动起的） | ⚠️ 要动的是**线上那道门**，我按 P1/P2 **只提申请**；你点头我就把"一条命令 + 回滚"写出来（**回滚是 `scripts/start-tunnels.sh` + `restart-core.sh`**） |
| **3** | **只选一件事：语音本体做不做。** 做 ⇒ 从三条路里选一条（**云端** / **本地模型** / **先做安卓包**）；不做 ⇒ **明说"本体砍了"**（我把 #52 从账上划掉） | ⚠️ **先把两句话分开，别读成矛盾**（2026-09-23 核）：主人 **09-21 砍的是「语音本体」**（*"语音不做"*，理由是输入法自带语音已覆盖一部分）；**09-22 要的是「形状」**（*"学微信 / 先做假的"*）—— 而**形状已经上线**（话筒/键盘切、按住说话出字、听筒只在语音档、演示条两句各一行）。⇒ **两条指令今天都已经照办了，不冲突**。真正悬着的只有一件：**本体要不要做**（不做 ≠ 假话：`voiceNotWired` 那句一直在如实说"还没真的开麦"）| 三条路各自的代价在 [`55-VOICE-DEMO.md`](55-VOICE-DEMO.md) §三；真做要按 `D5.10` 做**四类各 20 条语料、句子级命中率 ≥90%** 的硬闸。⚠️ 注意 `55` §3.1 那条：网页版只有"云端/本地模型"两条路（`D5.2` 定的"用手机自带的识别器"要安卓包) |
| **4** | **选一条**：① **换口令**（= #30，你 09-21 拍过"不换"）· ② 给我**具体那几条命令**开 NOPASSWD 白名单（**那就不需要口令**）· ③ 允许我 `journalctl --vacuum-time=` **整段时间清一次** | **2026-09-23 复核把"很可能"变成"实测"**：user journal 里**带口令的行 81 行**（7 行字面量 + 74 行"倒着写 + `rev`"，最早 09-21、最晚当天）；死 scope **只剩 1 个**（description 里仍带着那句话，`ActiveState=inactive`）。⚠️ **只要还在用"口令进命令行"这个姿势，它就会继续长** —— 清一次只买到当下 | ① 🔴 **增量已经止住**：造清单那次改成**从 git 历史里取**（那口令本来就是公开 history 里的，见 §6.1 #30）⇒ 命令行里**不再出现字面量**（当天 17:41 那条 journal 行实测不含）。② ⚠️ **清理这条路我没走**：`--vacuum-*` 只能**整段时间**删，会连**别的项目**的日志一起删 —— 那比你手上这件事的代价大。⇒ 我的建议是**换口令**（一次做完，连 #30 一起了），或者**开白名单**（以后根本不用口令） |

> ⚠️ **#30（线上口令泄露）不在这张单子上**：那是你 2026-09-21 拍过板的**已知并接受的风险**，
> 下一个人不必再提（记录在 `00-PROGRESS.md` §6.1）。

---

## 一·补、**顺手一件**（不是决定，是一条命令）

仓库里 `v2/services/core/tenant-template.conf` 的**注释**改了（`max_tenants=8` 的值**没动**）——原来那句"这个数按磁盘算"是假话，现在写清楚了：它是**策略上限**，按磁盘算的值用 `scripts/set-tenant-limit.sh` 看。

⚠️ **装机那份 `/etc/hupo/tenant-template.conf` 还是旧的**（只差注释）⇒ `--check` 在那之前会报一次"不一致"（无害）。
要同步（一条命令，会顺带把仓库里那份装机脚本也刷新）：

```bash
sudo bash scripts/install-provision-helper.sh --yes
```

⚠️ 我没有自己动 `/etc/hupo`（那是**特权侧**，按 P1/P2 归你签字）。

**还有一件同类的**（清账⑧/#42 之后）：租户镜像**重造过了**（`src/` 兜底已拿掉，403MB，自检 5/5），但**跑着的那两台**要到**下次重建容器**才用得上新镜像（`podman load` 换的是存储里的 tag）。⚠️ 在那之前它们照旧从 `/app/code` 跑产品层，**行为没有任何差别**（所以这不是「坏了」，只是「旧镜像还挂在那两台上」）。

```bash
sudo bash scripts/create-tenant-pool.sh --yes
```

---

## 二、你不点头，我也能自己做完的那几条（**不用等**）

| # | 是什么 | 什么时候做 |
|---|---|---|
| ~~**#19**~~ | 多人命名空间的 `userId` 那一半 | ✅ **已还**（2026-09-23 核实：实现早就在，账没搬走 —— 见 §〇 **73**）|
| ~~**#35**~~ | 容量上限按磁盘算 | ✅ **已还**（`set-tenant-limit.sh` + 共用算式 —— 见 §〇 **74**）|
| ~~**#42**~~ | 镜像里那份 `src/` 兜底 | ✅ **已还**（镜像重造 + 自检 5/5 —— 见 §〇 **75**；**跑着的那两台要用上新镜像得等下次重建容器**，命令在 §一·补）|

⇒ 一句结束语：**上面这些你回一句话**（比如"短信用 X"、"systemd 你来做"、"语音砍了"、"换口令"），
我就能把剩下的都清掉；**不回的这些我会一直记在账上，不会假装做完了**。

---

## 三、🔴 **2026-09-23 审计新查出来的五条**（**全是部署期**，按 P1/P2 **要你签字**）

> 来源：三路只读审计（全记录在 [`65-ALIGNMENT-2026-09-23.md`](65-ALIGNMENT-2026-09-23.md)）。
> ⚠️ 这五条的共同形状是：**手册/代码看起来"已经挡住了"，实际没有** ——
> 所以它们比普通欠账更危险（读它的人会**不设防**）。

| # | 欠什么 | 不做的后果 | 我这边准备好了什么 |
|---|---|---|---|
| **#65** | 🔴 **容器出网零限制** —— 手册 `08-SPEC.md` §4.2 原来写着【有效】的「白名单 + 审计」，**代码里一次都没实现**（租户单元**没有 `--network`** · 全仓出口白名单/代理 **0 命中**） | agent **能往任意地址发请求**（把读到的东西带走、或被外面牵着走）；而读手册的人**以为出不去** | 改动面很清楚（宿主网络 + 单元 + 一个出口代理/防火墙白名单 + 请求审计）。⚠️ **动的是部署期** ⇒ 你点头我执行。手册那一行**已经改成实话**（目标态 · 未实现）|
| **#66** | 🔴 **没有每用户磁盘配额** —— 一人一台**共用宿主盘**，谁写满拖垮全机 | 一个人写满 ⇒ **全机一起坏**（本项目"最贵事故"那一类）。全仓 `xfs_quota`/单 LV **0 命中**，只有估台数的算式与 `du -sh` 报告 | 要定两件：**quota 的种类**（xfs project quota？）与**量**；然后我加"满时明确拒"（**不许静默写坏**）|
| **#67** | ⚠️ **闲置回收的代码在、产品里零调用者** —— `agentIdleEvictMs` 与 `evictIfNeeded()` 都在，**只有测试调它**；租户单元是 `Restart=always` ⇒ **容器永不停** | 没人用的那台**照样占着内存**；而"回收"看起来已经做了 | 定时调 `evictIfNeeded` 是**小改**（我可以直接做）；**容器级**闲置停/销毁**是策略**，要你定（与 #48/#58 同族）|
| **#69** | 🔴 **租户容器没有 CPU 上限** —— 渲染单元那段**没有 `--cpus`**，全仓 `--cpus`/`CPUQuota`/`cpu.max` **0 命中**（本机 32 线程）| **一个租户里跑飞的 agent 能把整机 CPU 吃满**：主人那一份服务与别的租户一起被拖慢（内存那条已经挡住了，CPU 这条没有）| 加一行 `--cpus=` 是**小改**；**值定多少**是容量策略（要在 `lib/tenant-capacity.sh` 里给出口径）⇒ 你定个数或授权我按"活跃并发"估 |
| **#68** | ⚠️ **监控只剩内存** —— CPU / 磁盘 / inode / fd **没有任何采样或闸**；而本机每层 `memory.max` 都是 `max` ⇒ 连内存那条判据都**算不出来** | **盘要满的时候没人知道**（等到用户报错才知道） | 手册 §十一 那张目标态表就是判据；要做的是**采样 + 分级闸**（80/88/92/95）|

> ⚠️ **这五条我一条都没自己动手** —— 它们改的是**宿主网络 / 单元 / 配额 / 采集**，
> 全在 `AGENTS.md` §八 说的"部署期"那一档：**助手只提方案与命令，签字的是主人**。
