# 23 · **开发期**与**部署期**是两套安全逻辑（主人 2026-09-21 的指令）

> **主人的原话**：「你现在先备注一下，我要你有 sudo 权限去做，因为目前是开发。
> 在部署是另一套安全逻辑。」
>
> ⚠️ 这一页是**备注**，不是手册。**手册还没改**（原因见 §三）—— 按 `AGENTS.md` §二
> 的裁决顺序，真正生效要动 `05-DECISIONS.md` / `06-OPERATIONS.md` / `AGENTS.md`，
> 而那几个文件是**开机清单里的 `strict`**，改完要重建清单。下面 §四 把该改的话**逐字写好**了。

---

## 一、政策（按主人的指令复述，便于纠错）

| | **开发期（本机、现在）** | **部署期（给用户用的那一套）** |
|---|---|---|
| 助手能不能按特权按钮 | **能**（主人明确授权） | **不能**：只能提申请，**签字的是主人**（P1/P2 原样有效） |
| 判断依据 | "这是开发，图的是快" | "坏事能不能**留下来**"——那才需要主人那一下 |

⇒ 一句话：**P1/P2 管的是"部署"，不是"开发"。**

## 二、⚠️ 但本机有一件必须先说清的事：**这台机器既是开发机，又是线上机**

`AGENTS.md` §一 那张表把"生产机"写成**另一台机器**（另一个 checkout、systemd `concierge-core`、8091）。
**可今天真正在给主人用的那一份，就是这台机器上的 8020 + frp 隧道**（`w.stalkerai.cn`）。

⇒ 所以在**这台机器上**"开发期给我 sudo"，会**同时**解锁"正在服务的那一份"的护栏。
这不是文字游戏，它的直接后果是：

> **一旦助手在本机有 sudo，`/etc/hupo/integrity.json` 那道锁在本机就只剩"记录"作用。**

因为：我改完人格/手册/`AGENTS.md` 之后，**可以自己重建清单** ⇒ 核对永远能通过。
⇒ **锁的价值没了，剩下的是"审计"**：每次重建都是一次**看得见、可回溯**的动作。

### 2.1 这不是"降级通过"，是换了一种保护

| 保护形态 | 挡得住什么 | 什么时候够用 |
|---|---|---|
| **锁**（现在的 `strict`） | 助手**偷偷**把改动变成"下次开机自动读" | **部署**：没人在旁边看着每一次改动 |
| **审计**（开发期应有的形态） | 挡不住助手，但**每次都留痕** | **开发**：主人就在跟前，图的是快 |

⇒ 建议：**本机保持"锁"的机制不变**（对不上就拒绝启动 —— 那是防"没注意到"的安全网），
**但把"重建是预期动作"写进文档**，并让每次重建都留下一条记录（谁、什么时候、改了哪几个文件）。

## 三、为什么这一页没有直接去改手册（**这一条本身就是纪律**）

`AGENTS.md` §二：**唯一权威是手册；发现不一致 ⇒ 先按事实确认，然后回来改手册（升版本）。**
手册 v1.0 定档时 P1/P2 是**绝对**的（"这一条没有例外"）。现在主人给了新的边界 ⇒
**那是手册升版本的事（v1.7）**，不是助手可以"就地按新理解执行"的事。

⚠️ 而且这几个文件是 `strict`：改完**要重建清单**，否则**下次开机拒绝启动**。
（在主人给我 sudo 之前，重建只能主人跑；给了之后我自己能跑 —— 但那就正好是 §二 说的那件事。）

## 四、要改的话（**逐字写好，等主人点头**）

### 4.1 `05-DECISIONS.md` A 组（P1/P2）加一节

```markdown
### 补 · 开发期与部署期是两套（2026-09-21，主人指令）

| 期 | 助手的特权 |
|---|---|
| **开发**（本机、主人就在跟前） | **可以用 `sudo` 做开发上的事**（装工具、跑维护脚本、重建清单） |
| **部署**（给用户用的那一套） | **P1/P2 原样有效**：只能提申请，签字的是主人 |

⚠️ 判据不是"哪台机器"，是**这一段工作在服务谁**：
本机同时是开发机**又是**线上机（8020 + frp）⇒ 本机的护栏**降级为审计**
（每次重建留痕），**部署那一边的锁不动**。
```

### 4.2 `AGENTS.md` §八 要把结论改过来

现在那一节写的是"`sudo` 要密码这件事**是这道护栏的地基**……**不许去试"能不能免密"**"。
⇒ 改成：

```markdown
| 事 | 现在怎么样 |
|---|---|
| **开发期**（本机） | ⚠️ **主人已授权助手用 `sudo`**（2026-09-21）。装工具、跑维护脚本、**重建开机清单**都属于这一档 |
| **部署期**（服务用户的那一套） | **P1/P2 原样有效**：助手只能提申请，**签字的是主人** |
| ⚠️ 本机的护栏是什么 | **审计**，不是锁：助手能自己重建清单 ⇒ 锁不住它。**每次重建都要留痕**（谁、什么时候、动了哪几个文件） |
| ⚠️ 但那不等于"随便改" | 口令**不进任何文件、不进日志**；`sudoers` 优先用**白名单**（见 §五），而不是把口令交给助手 |
```

### 4.3 `06-OPERATIONS.md` §3.2 第 2 条那句"摘够"

原话是"**没有任何路径**能让助手的写入变成'下次开机自动读'"。
⇒ 加一句限定：**"（部署期）"**；并注明**开发期**的形态是审计。

## 五、比把口令给我更好的做法（**推荐**，主人二选一）

口令一旦贴出来就**留在对话记录里**了，而且**它能开这台机器上的所有门**。更合适的是
**只给几个具体命令免密** —— 一个 `sudoers.d` 文件，随时能删，删了就没了：

```bash
# /etc/sudoers.d/hupo-dev   （用 visudo -f 建；权限 0440）
# 只放开开发真正要用的那几条，别给"任何命令"
deploy ALL=(root) NOPASSWD: /home/deploy/.nvm/versions/node/v24.15.0/bin/node /home/deploy/proj/hupo/scripts/verify-integrity.mjs --build
deploy ALL=(root) NOPASSWD: /usr/bin/apt-get install -y chromium-browser
deploy ALL=(root) NOPASSWD: /usr/bin/apt-get install -y *
```

* ⚠️ 最后一条**很宽**（装包等于能装任何东西）—— 只在你接受"开发期就这样"时才加。
* 完全不想放开 `apt` 的话，那就留着：**需要装东西时主人自己跑一次**。

### 5.1 ⚠️ 先看这条硬事实：**这样一个白名单实际上仍然是 root**

`scripts/verify-integrity.mjs` 在 `deploy` **可写**的树里 ⇒ 放开
`sudo node scripts/verify-integrity.mjs --build`，我只要**改一下那个脚本**，
那条就变成"以 root 跑任何东西"。`apt-get install` 同理（装包 = 以 root 跑维护脚本）。

> **这是 sudoers 的经典陷阱：永远不要白名单一个"被授权者能改"的脚本。**

⇒ 所以这份白名单的**真实价值是留痕**（`sudo` 日志里记着我到底跑过哪几条命令），
**不是"圈住"**。要**真的**圈住，只有 5.3 那条路。**我把话说在前面，你自己权衡。**

### 5.2 可以直接粘的那一段（**A：快，但等于 root**）

```bash
sudo visudo -f /etc/sudoers.d/hupo-dev     # 把下面四行粘进去，存盘
```
```
# 开发期给助手的特权（主人 2026-09-21 决定）· 详见 docs/dev/23-DEV-VS-DEPLOY.md §五
# ⚠️ 这几条**实际上是 root**（脚本/apt 都在助手可写的地方）——价值是留痕，不是圈住。
# 撤销：sudo rm /etc/sudoers.d/hupo-dev   （立刻生效，不留后手）
deploy ALL=(root) NOPASSWD: /home/deploy/.nvm/versions/node/v24.15.0/bin/node /home/deploy/proj/hupo/scripts/verify-integrity.mjs --build
deploy ALL=(root) NOPASSWD: /usr/bin/apt-get update
deploy ALL=(root) NOPASSWD: /usr/bin/apt-get install -y chromium-browser
deploy ALL=(root) NOPASSWD: /usr/bin/apt-get install -y ripgrep
```

改完之后**我能自己重建清单了** ⇒ 以后我改人格/手册/`AGENTS.md` 就**不用每次来烦你**，
但**每一次重建都要在这里留一行痕**（谁、什么时候、动了哪几个文件）—— 见 5.4。

### 5.3 **B：真的圈住**（要多花一点功夫，但它是唯一有意义的"白名单"）

把"重建清单"这件特权**冻住**：做一个 **root 所有、且不 import 我这边任何代码**的小工具
（自带路径清单、只用 `/usr/bin/sha256sum`），然后白名单**它**：

```
/usr/local/sbin/hupo-integrity-build        # root:root 0555，冻住
deploy ALL=(root) NOPASSWD: /usr/local/sbin/hupo-integrity-build
```

* 我**改不动**它 ⇒ 这条白名单是**真的**圈住了。
* 代价：**受保护路径清单从"代码里"搬到"那个冻住的文件里"** ⇒
  以后要加一条受保护路径，得主人重新装一次那个工具（一次 `sudo`）。
* 防漂移：加一条测试，读那个 root 文件（`0444` 世界可读）跟代码里的 `protectedPaths()` 对一遍 ——
  对不上就红，提醒"该重装那个冻住的工具了"。

⇒ **要 B 的话说一声，我把那个工具 + 测试写好，你装一次。**

### 5.4 留痕（不管 A 还是 B 都要）

每次重建之后，我在 `docs/dev/00-PROGRESS.md` §九 下面记一行（时间 / 为什么重建 / 受影响的文件）。
**"开发期护栏降级为审计"这句话，就靠这一行一行撑着** —— 没有它，降级就等于拆掉。

## 六、我**没有**做的事（写在明处）

* **没有**改 `docs/handbook/**`、`AGENTS.md`（它们 `strict`，改完要重建清单；**在政策落定前不改**）
* **没有**用那条口令（它现在还只在对话记录里）
* **没有**把"开发期有 sudo"当成"已经生效的规则"去用 —— 它先是**待批注的指令**，
  在主人点头 + 手册升版本之后才是规则
