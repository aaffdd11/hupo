# 79 · **配置页那四样**（四个 tab · P0-2 / P0-3 / P1-1）

> 主人 2026-09-24 定了形状（原话）：
>
> > *"配置页用来配置模型，语言大模型apikey，语音大模型，图片生成，视频生成。"*
>
> 这一批就是照这句话把配置页做成**四个 tab**，并把"填进去的东西**存到哪、什么时候生效**"
> 说清楚。它同时解开三个卡了很久的阻塞项（`77-BLOCKERS.md` 的 B1/B2 与 P1-1）。

---

## 一、四个 tab ＝ 主人说的四样

| tab（界面上那两个字） | 配的是什么 | 字段（`creds.yaml` 里的名字） | 现在**生效吗** |
|---|---|---|---|
| **聊天** | 语言大模型那一把 | `model` → `HUPO_MODEL_KEY` | ✅ 生效（各人本来那条路） |
| **语音** | 语音大模型那三样 | `voiceAppId` / `voiceSecretId` / `voiceSecretKey` → `HUPO_VOICE_APPID/SECRET_ID/SECRET_KEY` | ✅ **主人这一份真的用它**（2026-09-24 接上）；⚠️ **租户那半还没接**（见 §八·2） |
| **图片** | 画图那一把 | `image` → `HUPO_IMAGE_KEY` | ⏳ **先收着**（那条路还没接上） |
| **视频** | 做片子那一把 | `video` → `HUPO_VIDEO_KEY` | ⏳ **先收着**（那条路还没接上） |

⚠️ **tab 上为什么写"聊天/语音/图片/视频"而不是主人用的那几个词**：
「模型」是**界面禁用词**（`07-APPENDIX.md` §2.2 那条硬闸，用户读不懂它），
所以四屏一律用"**它管什么**"来说。**主人口径与界面上那两个字是同一件事的两种说法**，
不是把它改小了 —— 四屏各有一句"填上它，我才能…"把意思说全。

---

## 二、存到哪（**三种人三个落点**，别读成一套）

| 谁 | 「聊天」那一把 | 其余三样 |
|---|---|---|
| **主人**（`kind:'local'`，他这一份就在本机） | 写 **DSH 自己那份凭据**（`$DSH_HOME/.credentials.yaml` 的 `refs.DEEPSEEK_API_KEY`）—— 见 §三 | `data/creds/<他的 id>.yaml` |
| **租户**（有自己一台容器） | **已有的那条路**：`channel.pushKey` → 他盒子的卷（`46-KEY-DELIVERY.md`） | 同上（**中心这份按人存档**） |
| ⚠️ 为什么其余三样**不进盒子** | 盒子那边 `writeKeyFile` 是**整份重写**（只写 `HUPO_MODEL_KEY` 一行）⇒ 下一次送钥匙就把它们抹掉。要真进盒子得改**产品层**（那是部署期，要主人签字）⇒ **今天不进**，页面也**如实说"先收着"** | |

**存档的形状**（`src/creds-store.js`）：
`<HUPO_DATA>/creds/<sub>.yaml`，一行一个 `名字: 值`，**0600**、原子写（先 `.tmp` 再 `rename`）。
⚠️ 文件名里的 `sub` **要洗**（`^[A-Za-z0-9_-]{1,64}$`）——它虽然来自验签令牌，
但"拿它拼路径"这件事必须自己再挡一道（`../` 那类输入绝不许走到文件系统）。

---

## 三、🔴 「语言那一把」写进 DSH 那份凭据（**主人 2026-09-24 点头的那一条**）

**事实**（读 DSH 自己的代码，不是猜的）：

* 那份文件是 `version: 1` + `refs:`（名字 → 字符串）＋ `records:`（`<scope>/<id>` → `{kind, payload}`）；
* 这台机器实测：`refs.DEEPSEEK_API_KEY` **就是那把钥匙**；
  而 `records` 里那条是 **`client-connection/browser-session` / `kind: grant`**
  —— 那是**浏览器的会话授权**，**与钥匙无关**。

⇒ 换钥匙**只动 `refs` 里那一行**（`src/owner-creds.js`），三条纪律：

| # | 纪律 | 判据 |
|---|---|---|
| 1 | **先备份**（同目录 `.bak.<时间>`，0600）—— 这是全机器最要紧的一个文件 | 备份写不出来 ⇒ **一步都不往下走** |
| 2 | **外科式**：只替换那一行，**`records` 逐字节不许变** | `recordsOf(before) == recordsOf(after)` |
| 3 | **原子写 + 写完自己核**；核不过 ⇒ **把原来那份放回去** 并如实报错 | 判据里用一个"写下去就坏"的假 fs 验它真的还原 |

⚠️ **认不出那份文件的格式**（预发布那种扁平格式 / 空文件）⇒ **宁可不写**，
回一句 `unexpected-format` —— 也不要把主人的凭据文件写坏。

---

## 四、服务端那两条路（**老的协议一个字没动**）

| 路 | 做什么 | 为什么这么设计 |
|---|---|---|
| `POST /api/model-key`（**老路**） | 只写语言那一把 | **协议冻结**：老客户端还在跑，一个字节都不许改 |
| `POST /api/creds`（**新路**） | 一次写**一屏**的字段（`{creds:{…}}`） | 语音那三样**必须一起写**（分开写会出现"填了两样"的半截状态） |
| `GET /api/space` | 多回一块 `creds:{model,voice,image,video}` | 🔴 **只有"有没有"**，**永远不回值**（那是密钥那条路）。加字段安全（**加不破**） |

⚠️ **语音"有没有"＝三样齐了才算有**（只有 appid 一样发不出请求）——
这条规则**只住一处**（`src/creds.mjs` 的 `credStatus`），服务端与页面说的是同一个数。

⚠️ **顺手修掉一处假话**：`/api/space` 对主人原来只回 `{kind:'local'}`（没有 `hasKey`）
⇒ 他那屏只能显示"还没有填"，**而他其实有一把能用的钥匙**。
现在照实报（读那份凭据里那个 ref 在不在）；`keyBad` 恒 `false` ——
**我们不知道**它灵不灵（那要真发一次请求才知道），而"不知道"不许写成"不灵"。

---

## 五、🔴 写下去的顺序是**有讲究的**（不是审美）

盒子那边读文件的是 `model-proxy.js` 的 `parseKey`：这份文件"像 YAML"时，
它取**第一个名字里带 `KEY`/`TOKEN`/`SECRET` 的那一行**。
而语音那三样里就带 `SECRET` ⇒ **把 `HUPO_VOICE_SECRET_KEY` 写在前面，
模型那一把就会被换成一把语音密钥**（现象只是上游回鉴权失败，看日志完全看不出原因）。

两道防线（**都有判据**）：

1. `src/creds.mjs` 写文件时**固定按 `CRED_ORDER`**（语言那一把最先），
   而且**旧文件里顺序错了也会被纠正**；
2. `parseKey` 改成**先认名字**：`HUPO_MODEL_KEY` 在就一定是它 ——
   这样"顺序"不再决定"哪一把是模型钥匙"。

---

## 六、界面（`lib/screens/settings_screen.dart` ＋ `lib/widgets/cred_form.dart`）

* 四个 tab ＝ `TabBar(isScrollable: true)`（字放到最大时**横向能滚**，不挤成一团）；
  每一屏**自己是一个可滚的 `ListView`**（大字下不许溢出）。
* 每一屏四件：**这一样管什么** → **现在有没有**（三种状态分开说）→
  **边界句**（收下了 ≠ 现在就生效）→ **填的那一块**。
* **聊天那一屏是老表单**（`KeyForm`：带粘贴、带"取消注册"）；其余三屏用新 `CredForm`
  （字段可以是**一串**（图片/视频）或**三样**（语音），一次提交写整屏）。
* ⚠️ **关于 / 退出登录挂在第一屏（聊天）的可滚内容底下**，**不做固定页脚**：
  固定页脚在 3.1 倍字下会把上面挤爆（D3.5 那道硬闸当场判红）。
* ⚠️ **每一屏给了一个 key**（`credTab:<名字>`）：`TabBarView` 自己也是一个 `Scrollable`
  （横向翻页那一个）而且排在**前面** ⇒ 判据要"像用户那样滚内容"就必须指得到**这一列**
  （判据当场抓到过一次：滚错了对象 ⇒ "关于"永远滚不出来）。

---

## 七、判据

| # | 验什么 | 在哪 |
|---|---|---|
| 1 | 六个字段的读写：认得出 / 认不出留着 / **空值不算有** / 坏的另存 | `test/creds.test.js` |
| 2 | 🔴 **顺序**：旧文件顺序错了也要纠正；语言那把永远最先 | 同上 |
| 3 | 🔴 `parseKey` **名字优先**（语音密钥写在前面也抢不走模型那把）＋ 老形状不许变 | 同上（负向对照两条） |
| 4 | 按人存档：**分开**、0600、不留 `.tmp`、再写不覆盖别的字段 | 同上 |
| 5 | 🔴 `sub` 拼路径要洗（`../` 那类一律拒） | 同上 |
| 6 | 🔴 主人那份：**只换 `refs` 一行**、`records` 逐字节不变、插行、认不出就不写 | 同上 |
| 7 | 🔴 主人那份：先备份 + 写完自核 + **核不过还原** + 没备份就不动 | 同上 |
| 8 | 🔴 `/api/creds`：坏字段/空值/非可打印 ⇒ 400；成了 ⇒ 200 **且回执里一个字符的值都没有** | 同上 |
| 9 | `/api/space` 带上那四样（**只有有没有**），老字段一个不少 | 同上 |
| 10 | 四个 tab 都在、顺序对、每屏说清管什么、边界句在、语音三样、**一次提交写整屏** | `test/widget/settings_test.dart` |
| 11 | `localOnly`（主人自己那一份）**也能填**；关于/退出还在；退出登录**仍是红的** | 同上 |
| 12 | 四样"有没有"的宽容解析（缺字段/坏类型 ⇒ 一律"没有"） | `test/unit/space_test.dart` |
| 13 | 新文案**过界面禁用词表**（tab 名、"管什么"、边界句、状态句） | 同上 |
| 14 | **五档不溢出 + 命中区 ≥44**（新界面也要过那道硬闸） | `test/widget/accessibility_test.dart`（181 条） |

---

## 八、这一批**没做**的（明说）

1. **图片 / 视频那两条路没接**：今天只是把钥匙**收下**（页面自己这么说）。
2. ✅ **语音那三样已经接到识别路**（2026-09-24 当天接着做完）：
   `/api/asr` 的凭据解析**优先读他自己那份**（`data/creds/<他>.yaml` 里那三样），
   没有才退回部署默认那份（`data/asr.env` 现读）。
   **来源如实报**：日志那一行写 `凭据来源 his-own` / `default`（**不是秘密**，只是"从哪来"）。
   ⚠️ **租户那半还没接**：盒子里的 `/api/asr` 跑的是**盒子里那份** `serve.js`，
   而它读的是**盒子里**的 `data/creds/`（我们写的是**中心**那份）⇒ 租户今天仍是"没配"。
   要接上得改**产品层**（把三样推进盒子 —— 而盒子那边 `writeKeyFile` 是**整份重写**，
   得一起改成合并）⇒ 部署期的事，**要主人签字**（`77-BLOCKERS.md` **B10**）。
   ⇒ 所以页面那句话**跟着事实变**（`credVoiceBoundary`，四种组合四句话）：
   本机＋填了＝"以后就用这三样"；本机＋没填＝"用的是机器上配好的那份"；
   租户一律"**你这台还没接上**"（填了也不许承诺）。
   **线上实测**：真连 `/api/asr` ⇒ `asr/ready` ＋ 服务端日志 `凭据来源 his-own`。
3. **其余三样不进租户盒子**：要改产品层（部署期，要主人签字），见 §二 那一格。
4. **`app_update` 那一类"改"**：主人这次说的是**配钥匙**，不是改小程序。
