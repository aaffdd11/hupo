# 43 · 新号自动拿到一台容器（申请队列 · 安全模型先定）

> 主人 2026-09-21 选的：**真的自动开一台**（不是"只把话说明白"）。
> 同时他点名：**要定安全模型**。⇒ 这一篇**先定模型，再写代码**。

---

## 一、现在是什么样（问题的准确形状）

| 件 | 现在 |
|---|---|
| 谁有容器 | **写死的两台**：`u1→hupo-a`、`u2→hupo-b`（`data/tenants.env` 的 `HUPO_TENANT_MAP`） |
| 第 3 个新号登录 | 建号成功、拿到令牌，但 `tenantOf()` 返回 `null` ⇒ `/api/space` 回 `queued` |
| 客户端 | 等待屏**每 2 秒自己重问**、三步一直不勾 —— **诚实，但永远不会好** |
| 服务有没有特权 | **没有**。`deploy` 不在 docker 组、没 sudoers、没 polkit |

⇒ 所以"自动开一台"这句话的**全部难点**只有一件：
**一个公开的网页登录口，要怎么才能触发一个特权动作，而不等于把 root 交出去。**

---

## 二、三条路（以及为什么选第三条）

| | 做法 | 为什么不行 / 行 |
|---|---|---|
| ① | 给 `deploy` 一条 **sudoers / polkit** 规则，允许它跑建租户的脚本 | ⚠️ 这等于"**服务能执行特权命令**"。边界从"一个整数"变成"那个 bash 脚本必须毫无破绽"—— 而脚本要吃 `PATH`/`IFS`/环境、要处理参数，**每加一个功能就多一个面**。`create-tenant-pool.sh` 自己在文件头就否掉过这一条 |
| ② | 一个**常驻的 root 守护**，服务连它的套接字 | 边界比 ① 好（协议窄），但**常驻 root 进程**本身就是一块要长期盯着的面：它要一直活着、要防协议解析漏洞、要被监控 |
| ③ | ✅ **申请队列**：服务只能"**放一个文件**"，root 侧**按需**（systemd 触发）读它 | **服务永远不能执行任何东西**。申请的**全部内容就是一个整数**（连文件内容都不读）。特权侧只在有申请时被 systemd 拉起、跑完就退。边界小到**能用眼睛看完全部输入** |

**选 ③。** 下面就是它的不变量。

---

## 三、安全模型（不变量 A1–A8）

> 这一节是**判据**，不是描述。每条都写"怎么验"。

### A1 🔴 **特权侧只读一个整数，别的什么都不读**

申请 = `/run/hupo-provision/<n>.req` 这个**文件名**。
**文件内容一律不读**（可以是空的）、**不读环境变量里的任何路径**、**不接受参数**。

* 怎么验：往申请目录里放一个内容是一串 shell 命令的 `<n>.req` ⇒ 那个 `n` 正常建，
  **那一串命令一个字都没被执行**（负向对照：拿掉 `n` 的合法性，必须被拒）。

### A2 🔴 **一切路径/用户名/uid 都由特权侧从 `n` 用固定模板推出来**

```
n            ∈ [1, HUPO_MAX_TENANTS]        （整数，其余一律拒）
OS 用户       hupo-t<n>
uid / gid    3000 + n
home         /home/hupo-t<n>
卷           <home>/tenant
subuid 起点   (3000+n) * 100000
单元         <home>/.config/systemd/user/hupo-tenant.service
通道         /run/hupo-channel/hupo-t<n>/channel.sock
```

* 怎么验：拿 `n=../etc`、`n=1;id`、`n=-1`、`n=1$(id)`、`n=9999`（超上限）、
  `n=`（空）逐个塞进去 ⇒ **全部拒**，而且**目录里一个东西都没多出来**。

> ⚠️ **服务侧必须推出同一个名字**（它要提前去听那条通道）。
> 所以这条模板**只有一处权威实现是被两边共用的纯函数**（`tenants.js`），
> shell 那侧照它写、并有一条闸盯着两边一致（见 §五 判据 5）。

### A3 🔴 **申请目录是 sticky、root 属主、只允许服务"放文件"**

`/run/hupo-provision`：`1733 root:deploy`（服务能进能写、**列不出**别人的、**替换不掉** root 的文件）。
特权侧读到的每一项都必须：`lstat` 是**普通文件**（不是符号链接）、**属主是服务的那个 uid**。

* 怎么验：服务身份放一个**符号链接** `3.req -> /etc/shadow` ⇒ **拒**（负向对照：
  放一个普通文件 ⇒ 正常）。

### A4 🔴 **幂等 + 有上限 + 有最短间隔**

* 同一个 `n` 重复申请：第二次什么都不做（不能变成"重建、清空人家数据"）。
* `n` 超过上限：**不做**，并且**如实**把它记成"满了"（不是假装在开）。
* 两次真建之间有一个最短间隔（防一条申请风暴把机器打满）。

* 怎么验：同一个 `n` 连申请 5 次 ⇒ 只建一次、容器**只重启 0 次**；
  狂塞 200 个申请 ⇒ 建的数量 ≤ 上限，且**每一次拒绝都留了一行日志**。

### A5 🔴 **特权侧删掉申请，且只删它自己处理过的那一个**

处理完（无论成功失败）把这个文件删掉。失败也删 —— 否则一个坏申请会**每次开机**
被 `.path` 单元反复触发（那就是一个死循环）。

* 怎么验：放一个超上限的申请 ⇒ 文件**不见了**、日志里有那一行、系统**没有**反复触发。

### A6 **服务的身份不变**：它**不是** root、不进 docker 组、不新增任何 sudo 权限

* 怎么验：装完之后 `id deploy`、`sudo -l -U deploy`、`getent group docker` 三条
  **与装之前逐字相同**。

### A7 **每一次申请与每一次真建都留痕**

一行：时间 · `n` · 结果（建了 / 跳过了 / 拒了，以及为什么）。
申请本身**不含手机号、不含 key**（只有 `n`）⇒ 这一行日志**不泄露个人信息**。

### A8 ⚠️ **装它本身是"部署期"的动作 —— 要主人签字**

助手**只写代码、只准备方案**；把那一串单元装进系统的命令**由主人跑**
（`AGENTS.md` §八：部署期 P1/P2 原样有效）。

### A9 🔴 **root 要跑的每一份文件，都必须 root 拥有**（写这一篇时差点栽在这）

**这一条是写安装脚本时才想明白的，而它一破，A1–A7 全部作废。**

第一版我是这么想的：systemd 单元的 `ExecStart` 直接指仓库里那个
`scripts/provision-tenant-request.sh`，它再去调 `create-tenant-users.sh` /
`create-tenant-pool.sh`，三个脚本都读仓库里的 `tenant-template.conf`。

🔴 **那等于什么都没防住**：这四个文件都在 `/home/deploy/proj/hupo/` 里，
**属主是 `deploy`（也就是服务自己）** ⇒ 一个被攻陷的服务只要
**改一行那几个脚本**（或者把 `name_prefix` 改成 `../../etc`、把 `max_tenants` 改成 `999999`），
下一次申请被 `.path` 单元触发时，**那段代码就是以 root 跑的**。

> 这就是"**边界画在了一个可写的地方**"——和 `39-PERMISSIONS.md` 里
> "挂载表写错了、那个权限位就兜不住"是同一类错误。
> ⚠️ 而且它**看起来是好的**：路径写对了、脚本也真的跑起来了、日志也漂亮。
> ⇒ 所以这一条必须**当成不变量写下来**，不能靠"我记得当时想清楚了"。

**改正**：安装器把这四份**拷**成 root 拥有的：

```
/usr/local/libexec/hupo/provision-tenant-request.sh   root:root 0755   ← 单元只跑这一份
/usr/local/libexec/hupo/create-tenant-users.sh        root:root 0755
/usr/local/libexec/hupo/create-tenant-pool.sh         root:root 0755
/usr/local/libexec/hupo/tenant-template.conf          root:root 0444
```

> ⚠️ **仓库里的那一份是"源码"，不是"运行的那一份"** —— 这正是今天早些时候
> 栽过三次的那个病（*"改完了"与"生效了"是两件事*）。⇒ 安装器必须提供
> `--check`：**把仓库那份与装着那份逐字节比一遍**，不一样就明说
> "改是改了，但 root 跑的还不是它"。

* 怎么验：`--check` 在**没改**时说明"一致"；改一行仓库里的脚本 ⇒ **必须报不一致**；
  `stat -c '%U %a'` 四份都必须是 `root`；`sudo -u deploy` 改那四份
  ⇒ **`EACCES`**（不是"改成功了"）。

---

## 四、它怎么跑（一次新号登录的完整路径）

```
① 新手机号登录            handleLogin → users.ensure(phone) → id = u3
② 服务问一句"有没有他那台"  tenantOf('u3') → null
③ 服务**申请**（唯一的新副作用）写 /run/hupo-provision/3.req（空文件）
④ 状态照实说             /api/space → state:'provisioning'，assigned ✔
⑤ systemd 看见了          hupo-provision.path（DirectoryNotEmpty + PathChanged）拉起 oneshot
⑥ 特权侧（root，几十行）   校验 n → 调现成的两个脚本（**模板只有那一处**）→ 删申请
⑦ 容器起来了              它连回 /run/hupo-channel/hupo-t3/channel.sock
⑧ 状态照实说             /api/space → state:'ready'，三步全 ✔
```

⚠️ **③ 是这一整套里唯一新增的副作用** —— 所以它是 review 的重点：
它写的是**一个空文件，名字是一个整数**。

### 4.0 🔴 **`.path` 那两行到底是什么语义**（2026-09-21 真机量出来的，**改了两处设计**）

我原来在注释里写的是：

> ~~`DirectoryNotEmpty` 管"从空变非空"那一瞬；`PathChanged` 管"已经有两个东西了、又来了一个"。~~

**那是错的。** 拿一对临时探针单元在这台机器上量了一遍：

| 量到了什么 | 数 |
|---|---|
| 投放口里**留一个文件不动** ⇒ 服务被拉起几次？ | **5 次**（然后撞上 systemd 的**启动限速**：默认 10 秒内 5 次） |
| 所以"又来一个"那一半靠谁？ | **`DirectoryNotEmpty` 自己就覆盖了**（`PathChanged` 不是承重的） |
| "起来时目录里已经有待办"（**开机**那一种）呢？ | `DirectoryNotEmpty` 管 —— 那时候**没有"变化"可听** |
| 收尾之后（把申请删掉）还会重复吗？ | **不会**（目录空了，条件就不成立了） |

🔴 **而它的反面是要命的**：只要投放口里**留下任何东西**，它就会一直触发到限速
⇒ 单元进 `failed` ⇒ **之后的新申请没人管了**。

⇒ **由此改了两处设计**：

| 改了什么 | 为什么 |
|---|---|
| **失败标记搬出投放口**（住 `/run/hupo-provision-state`，不在 `/run/hupo-provision`） | 原来 `<n>.req.failed` 留在投放口里 ⇒ 目录**永远非空** ⇒ **每一次失败**都会把单元触发到限速 ⇒ 之后新号**永远拿不到容器** |
| 单元注释里的解释改对 | 那条错的解释会让下一个人**照着一个错的心智模型**去改它 |

⚠️ 判据：`scripts/check-provision-trigger.sh`（**11 条**，把上面每一格都**量出来**，
包括那个反面；跑完撤干净并自证）。

### 4.1 状态机（`/api/space` 的 `state`）

| state | 什么时候 | 三步 | 客户端说 |
|---|---|---|---|
| `local` | 主人（本机那一份） | — | 直接进 |
| `provisioning` | **申请已受理**，特权侧还没建完 | `assigned` ✔ | "正在给你开一台" |
| `starting` | 那台**已经在跑**（单元活着），隧道还没连上 | `assigned` ✔ | 同现在 |
| `ready` | 隧道通了 | 三步全 ✔ | 进填钥匙 / 聊天 |
| `full` | **超过上限**，不会再开了 | 一条都不勾 | 🔴 **必须明说满员**，不许再让人无限等 |

🔴 **`full` 是这次新增里最要紧的一条**：现在的 `queued` 就是"永远等"，
而屏幕上没有任何一个字说它会永远等 —— 那是**项目最忌的"看着在动、其实到不了"**。

> ⚠️ 老客户端不认识 `provisioning` / `full`：`SpaceInfo.fromJson` 对**认不出的
> `state` 一律当"没就绪"** ⇒ 它们会停在等待屏（**行为逐字不变**，不会误进聊天）。

---

## 五、验（判据 · 每条都带负向对照）

| # | 判据 | 负向对照（**不能省**） | 现在 |
|---|---|---|---|
| 1 | 合法申请 ⇒ 那台真的被建出来、容器起来、隧道通 | 不写申请 ⇒ 什么都不发生 | ✅ **全过（2026-09-21 真机）**：新号登录 ⇒ 助手**真的**建出 `hupo-t3`（uid 3003）· 容器在跑 · 状态 `ready` · **只多了一台**（`check-provision-after-install.sh`）|
| 2 | 文件名里塞 `../`、`;`、`$()`、空、超上限 ⇒ **全拒**、目录里没多出东西 | 同一个位置放合法 `n` ⇒ 建 | ✅ 全过（判据脚本 ②） |
| 3 | 符号链接冒充申请 ⇒ 拒，而且**链接指的地方没被动过** | 普通文件 ⇒ 建 | ✅ 全过（③） |
| 4 | 内容是一串命令 ⇒ **一个字都没执行** | 同一份内容换个合法文件名 ⇒ 照常处理 | ✅ 全过（⑤） |
| 5 | 服务侧推出来的租户名 **与** shell 侧推出来的**逐字相同** | 故意把两边改得不一致 ⇒ 闸红 | ✅ 两边读**同一个** `tenant-template.conf`（`test/provision.test.js` 钉着） |
| 6 | 装完之后 `deploy` 的身份三条**与装之前逐字相同** | —— | ✅ **全过（2026-09-21）**。🔴 **基线是在"装之前"记的**（`v2/services/core/data/provision-identity-baseline.txt`，机器本地）—— 这一条**只有先记才有意义**（装完再记 = 自己跟自己比）|
| 7 | 超上限 / 没装助手 ⇒ 状态是 `full`（**不是**永远 `queued`） | 上限之内 ⇒ `provisioning` | ✅ 隔离启动实测（见 §5.1） |
| 8 | 同一个 `n` 重复申请 ⇒ **不重建**（A4） | 换一个 `n` ⇒ 建 | ✅ **全过**："只投一次"单测钉了；真机那半边：同一个号**第二次**跑 ⇒ 判据报"**没有新建** —— 那台已经在了"，容器没被重启 |
| 9 | 模板被换成坏值 ⇒ **一台都不建**（A2/A9 的纵深防御） | 好模板 ⇒ 认 | ✅ 全过（⑦） |
| 10 | 非 root 跑 ⇒ 拒 | root 跑 ⇒ 认 | ✅ 全过（⑧） |
| 15 | 🔴 **镜像不在时，真装必须拒绝动手**（而不是装一个永远不会工作的东西）· 装机前把"镜像在不在 / 每台多大 / 磁盘放得下几台"**算出来给人看** | 拿一个不存在的镜像名跑真装模式 ⇒ 退出码 3 + 给了 remedy + **这台机器一点没动**（拒绝点在任何写盘之前） | ✅ 全过（`check-provision-install.sh` ⑨·补） |
| 16 | 🔴 **边界是"谁也进不去"，不是某个权限数字** | 正对照（**他自己**进得去）＋ 负向对照（另一个租户进不去，而且必须是 **`EACCES`** 不是 `ENOENT`） | ✅ 全过（`check-provision-after-install.sh`）|
| 17 | 🔴 **不许有不带引号的 heredoc**（它在**注释里**也会执行反引号与 `$( )`） | 有 ⇒ 红（我签字安装时**当场**踩到：`DirectoryNotEmpty=` 与 `failed` 被当命令执行、在写出去的文件里变成空） | ✅ 全过（`check-provision-install.sh` ⑥·补）|
| 14 | 🔴 **三处路径一致**：服务（JS）写哪儿 · 助手看哪儿 · 单元里指哪儿 | 改一处 ⇒ 另外两处必须跟着红（变异验过：把 JS 那边的目录改名 ⇒ 两条当场红并各报一个路径） | ✅ 全过（`check-provision-install.sh` ⑨） |
| 13 | 🔴 **装机后跑得起来吗**：那几份脚本在 **systemd 的最小 PATH** 下能跑 · 链上要用的命令**都解析得到** · systemd **认**那两份单元 | 每条都有对照（"解析得到"对着"故意加一个不存在的命令 ⇒ 指名道姓报出来"；"systemd 认"对着"把 `WantedBy` 写成 `WantsBy` ⇒ 它抓出来"） | ✅ 全过（`check-provision-install.sh` ⑦/⑦·补/⑧） |
| 12 | 🔴 **`systemd` 那一寸**：`tmpfiles` 真的产出 1733 · 从空变非空拉起 · 已非空又来一个也拉起 · 带着待办起来也拉起 · **留了东西会反复触发** | 每条都有对照（"收尾之后不重复"对"留了东西反复触发"） | ✅ 全过（`check-provision-trigger.sh`，11 条，**量出来的**） |
| 11 | 🔴 **A9 那四条**：四份 root 拥有 · 单元指向**拷贝**而不是仓库 · 服务身份**读得到但改不动** · **改了仓库那份 `--check` 认得出** | 每条都有对照（"读得到"是"改不动"的对照；"没改时说一致"是"改了说不一致"的对照） | ✅ **全过（判据脚本 `check-provision-install.sh`，19 条）—— 而且它是在隔离环境里验的（临时根 + `--root`），不用签字** |

✅ **第 1 / 6 / 8 条 2026-09-21 已经在本机真的走过了**（主人签字 ⇒ 装 ⇒ 真建一台）。
⚠️ 而 **A9（第 11 条）不必等** —— 它靠安装器的 `--root` 模式在隔离环境里验掉了
（这一条我一开始以为"验不了"，其实只是**安装器少了一个测试入口**：
👉 **"验不了"常常是"没给它一个能验的形状"，不是"真的验不了"**）。
⇒ 这是这一篇的诚实边界：**没签字之前，验不全。**

### 5.1 已经真验过的那几档（2026-09-21，隔离数据目录里跑的）

```
① 判据脚本：sudo bash scripts/check-provision-refusals.sh
   ⇒ ✅ 全过（**26 条**）—— 而且它自己**一台租户都没建**（最后那条盯着这件事）
   其中 ⑨ 是**跨语言**那一条：**真的把 shell 脚本跑一遍**、把它算的 uid
   与 JS 那边算的比（不是在这儿再抄一遍公式）

② 隔离启动（HUPO_DATA 指向临时目录）里真登录三个新号：
   u1（**在静态表里** u1=hupo-a） → state=starting      申请目录：**空的**
   u2（推出来的 hupo-t2）         → state=provisioning  申请目录：2.req（**0 字节**）
   u3（推出来的 hupo-t3）         → state=provisioning  申请目录：3.req（**0 字节**）
   ⇒ 三件事同时成立：**表优先**没被破坏 · 推出来的那两台**真去申请了** ·
     而且申请**真的是空的**（A1 在真实路径上成立，不只是单测里）

③ 助手没装（申请目录不存在）⇒ 新号拿到 state:full / why:no-helper，
   日志里有一句人话 —— 不是让他对着一个永远不动的屏

④ 🔴 **助手那段编排**（判据脚本 ⑩）：这一节验的是**从来没被执行过一次的那一段** ——
   助手自己的编排（校验 → 调 `create-tenant-users.sh` → 调 `create-tenant-pool.sh` → 删申请）。
   真跑它要建真的 OS 用户 ⇒ 那要签字。**⇒ 把两个脚本换成"桩"**：助手是按
   **自己所在目录**去找它们的 ⇒ 把助手拷进临时目录、旁边放两个桩，它调的就是桩。
   于是**骨架**今天就验掉了：**调谁、什么顺序**（先 users 再 pool）· **传下去的东西**
   （那台的名字 / 模板路径 / `--yes`）· **成功那条路**（申请收掉、不留 `.failed`）·
   **失败那条路**（申请**也**删掉 —— 不然 `.path` 会反复触发它；**留 `.failed`** ——
   不然那个人在等待屏上永远等）· 并且自证**调的是桩**。
   ⚠️ 剩下没验的只有"真那两个脚本跑起来会怎样"，而那是**同一段编排**下面的两个已知量。

⑤ A9 那四条：sudo bash scripts/check-provision-install.sh
   ⇒ ✅ 全过（19 条）。**它在隔离环境里验**（安装器 `--root <临时目录>`：
     同一段代码换一个目的地、跳过 systemd），所以**不用签字就能验**。
   ⚠️ 它自证了两件事：真机上那几处**一处都没动**；真机上那条路**没启用**。
   🔴 **做过变异验证**：把单元改回指向**仓库那份**（正是 A9 那个病）
      ⇒ **正好那两条红**，并打印出 `ExecStart=/home/deploy/proj/hupo/scripts/…`
```

⚠️ **② 里第一个号拿到 `starting` 不是巧合**：它落在了静态表的 `u1` 上
（那台已经建好在跑）—— 这正是"表优先"那条规则在真实路径上的样子。

### 5.2 ⚠️ 我在这件事里犯的一次错（如实记）

第一次"隔离启动"我写的是 `HUPO_DATA_DIR=…`，而**真正的变量叫 `HUPO_DATA`**
（`config.js:50`）⇒ 那次"隔离"启动**其实开的是真数据目录**，
于是它动了线上那两个文件：`data/.crashloop.json`、`data/status.json`。

* 影响：**没有造成假通知**（写进去的是 `clean:true`，不会触发"上次没善终"那条），
  线上服务也一直活着（PID 1006196，`/api/health` 正常）；`status.json` 是心跳，本来就会被重写。
* 但这是一次**真的副作用**，不能因为"没出事"就不写。
* ⇒ 纪律补一条：**"隔离"要验它真的隔离开了** —— 横幅里那行 `数据 <路径>`
  就是那条判据；不看那一行，"隔离启动"这四个字只是我自己说的。

---

## 六、代价（如实写，不藏）
---

## 六、代价（如实写，不藏）

| 代价 | 说明 |
|---|---|
| **公开口能烧资源** | 临时码现在开着（主人点名要的）⇒ 谁都能用任意手机号进来 ⇒ 谁都能触发申请。**上限 + 最短间隔 + 每条留痕**是唯一的护栏；最坏情况是"机器上多出 N 台空容器" |
| **linger 的面变大** | 每多一个租户就多一份"没登录也留在机器上"的进程（`create-tenant-users.sh` 文件头已经写过这一条） |
| **特权侧是 root** | 那一份脚本写错了就是 root 级的错。所以它**必须小**：只做"校验一个整数 + 调两个现成脚本 + 删文件"，**不许**长功能 |
| **磁盘** | 镜像每人一份 + 一个卷 ⇒ 上限**应当按磁盘算**。⚠️ **现在还不是**：上限是模板里一个常数（`max_tenants`），**装机时**拿它跟磁盘比一遍并**如实报出来**（"磁盘 X GB 才够 N 台；可用 Y GB ⇒ 放得下 M 台"，M < N 就警告）。⇒ **这一条是"算给你看"，不是"替你算"** —— 真要按磁盘自动定上限，得让模板能生成（另立一项） |
| **仍然不是"秒开"** | 镜像要 load 进他自己那份存储 ⇒ 第一次要几十秒。等待屏那三步照旧如实报 |
| 🔴 **镜像本身是 `deploy` 造的** | 容器跑的那个镜像是 `deploy` 用 `build-tenant-image.sh` 建出来的，**住在 `deploy` 的 rootless 存储里**。⇒ 一个被攻陷的 `deploy` **可以往镜像里塞东西**（而这台机器上每台新容器都用它）。**这一条这次没有解，也不假装解了**：uid 那道边界挡的是"甲读乙的卷"「`deploy` 读别人的卷」，**不是**"`deploy` 决定容器里装什么"。要真解它得让镜像由**第三方**（root 或另一个 uid）构建 + 内容寻址钉住 —— 那是另一件事，单独排 |

---

## 七、谁来装（一句话方案，给主人）

```bash
sudo bash scripts/install-provision-helper.sh --yes     # ← 一条命令（助手写、主人跑）
```

它会：① 装三个 systemd 单元（`.path` + `.service` + tmpfiles）
② 建 `/run/hupo-provision`（`1733 root:deploy`）
③ **什么都不建**（第一台容器要等真有申请）。

⚠️ 撤销也是一条命令：`sudo bash scripts/install-provision-helper.sh --uninstall`。

---

## 八、与既有文档的关系

* 它**取代** `create-tenant-pool.sh` / `create-tenant-users.sh` 文件头里那句
  "为什么要有池（而不是来一个人现建一个）" —— 那两句在当时是**对的**
  （服务没有特权，所以只能事先建好）；现在**多了一条受控的申请路**，
  **池子照旧有用**（它仍然是"怎么建"的唯一模板，只是名单不再是写死的两行）。
* 模板参数化之后，**加第三个租户**与**自动开一台**走的是**同一段代码** ——
  这正是我们要的：自动那条路**不许**自己长出一套平行的建法。

---

## 十、签字之后只剩两条（**判据已经备好，别现写**）

```bash
# ① 装（主人跑，就这一条）
sudo bash scripts/install-provision-helper.sh --yes

# ② 验那两条（装完立刻跑；`--phone` 给一个测试号，它会真的建一个）
sudo bash scripts/check-provision-after-install.sh --phone 13800000000
#    只验 A6（不建任何东西，随时能跑）：bash scripts/check-provision-after-install.sh
```

它验：

| | 是什么 |
|---|---|
| **⑥ A6** | 身份三条与**装之前**的基线逐字相同（🔴 基线已在装之前记好 —— 装完再记就等于自己跟自己比） |
| **装的样子** | 那几份是 root 拥有 · `.path` active · `--check` 一致 |
| **① 真建一台** | 用那个号登录 ⇒ 等他到终态 ⇒ **只多出一台** · 名字与编号对得上模板 · 卷 `700` · `linger` 开着 · 宿主在替他听通道 · **容器在跑** · `/api/space` 到 `ready` |

⚠️ **它自己会自证"只多了一台"**（拿建之前 / 建之后的租户名单比）——
"多了三台"那种事**不许**混过去。
⚠️ 跳过的那几条会**明写"跳过"**，而且末尾再说一遍 —— **跳过的不是过的**。

### 10.1 一个反过来的教训（值得单独记）

我一开始把 **A9 那四条**写成"要等装完才验得了"。**那是懒**：它们验不了，
只是因为**安装器少了一个测试入口**。加一个 `--root <临时目录>`
（同一段代码换一个目的地、跳过 systemd），那四条立刻能在**隔离环境**里验掉，
而真机上**一处都不动**（脚本自证）。

> 📌 **"验不了"常常是"没给它一个能验的形状"，不是"真的验不了"。**
> 而把最重要的那条判据**推到一个人工动作后面**，等于让它最后才被验 ——
> 本项目已经吃过很多次"闸是绿的、可它打在错的输入上"了。

### 5.3 ⚠️ 又一个我自己的错：**判据"过了"，可它把话说错了**

写 ⑩ 那一段时，我在**双引号**里写了反引号：

```bash
ok "是**真做**那个模式调的（`--yes`），不是只看"
```

bash 会把 `--yes` 当**命令替换**执行 ⇒ 那句话变成空的、stderr 多一行
`--yes: command not found`，而**断言照样"过"**（它判的是 `grep` 的结果，不是那句话）。
⇒ 那种坏法**不报错、只是悄悄把话说错** —— 正是这个项目最忌的形状。

⚠️ 而这已经是**本次会话第二次**犯（第一次在 `check-provision-install.sh` 里）。
⇒ 光"下次注意"没用，所以加了一道 **lint（判据 ⑪）**：
**这几份判据里（注释之外）一个反引号都不许有**（它们压根不需要，一律用 `$( )`）。
⇒ 加完它立刻又抓到一个"它自己"的问题：lint 的模式我写成 `'\x60'`，
而 grep 把 `\x60` 当成了**字面量 `x60`** ⇒ 它抓到了自己那一行。
改成八进制 `printf '\140'` 让 grep 收到**真反引号**，而文件里没有那个字符。
⇒ **变异验证**：故意种一行带反引号的 ⇒ 那道 lint 当场红并指名道姓。

### 5.4 ⚠️ 又一条教训：**"跑一遍"会被"把错误吞掉"骗过去**

我加了两条闸来防"装机之后因为 PATH 不对而跑不起来"：

| 闸 | 它管什么 |
|---|---|
| **跑一遍**：拿 systemd 那条最小 PATH 真跑那几份脚本（只看模式） | 抓真实的 `command not found` |
| **静态**：把脚本里"命令位置的词"抠出来，一个个查在最小 PATH 下解析得到 | 抓**被吞掉**的那种 |

为什么两条都要 —— **变异验证抓出来的**：我先只加了"跑一遍"，然后故意让脚本调一个
不存在的命令，写成 `xxx >/dev/null 2>&1 || true`（**把错误吞掉**）⇒ **那条闸没红**。

> 📌 **教训**：一个靠"看输出里有没有某句话"的闸，**只要那句话说不出来就失效**。
> ⇒ 同一个风险要**两种打法**：一种看**行为**（真跑一遍），一种看**事实**（能不能解析）。
> 这个项目已经栽过很多次"闸是绿的、可它打在错的输入上" —— 这是同一个病的另一种形态。

⚠️ 顺带钉了一条更容易犯的：`systemd-analyze verify` **退出码永远是 0**
（`WantsBy` 那种拼错的键它只**打印一行**、不算失败）⇒ 那道闸必须**看它说了什么**，
不能看退出码。我写探针时真的手滑写出过 `WantsBy`。

---

## 十一、✅ 2026-09-21：**主人签字 → 装上 → 真建了一台**（这一篇的收口）

主人明确授权（原话选的选项是"**你替我跑 —— 这句就是我的签字**"；⚠️
`AGENTS.md` §八 的默认规矩是**部署期命令由主人跑**，所以他这句是**明确要绕过那条规矩**，
不是默认路径）。

```
① sudo bash scripts/install-provision-helper.sh --yes
   ⇒ 四份 root 拥有 · 投放口 1733 · 状态目录 0755 · .path active+enabled · --check 一致
② sudo bash scripts/check-provision-after-install.sh --phone <测试号>
   ⇒ A6 身份三条与**装之前**的基线**逐字相同** ✅
   ⇒ 真建一台：新号登录 ⇒ 助手建出 hupo-t3（uid 3003）· 容器在跑 · /api/space = ready ✅
   ⇒ 边界：他自己的 home 700（**这才是边界**）· 另一个租户进去 ⇒ **Permission denied** ✅
   ⇒ 判据 **16 条全过、0 跳过**
```

### 11.1 🔴 签字安装**当场**暴露的一个真 bug（heredoc）

第一遍装完，输出里有一行：

```
scripts/install-provision-helper.sh: line 232: failed: command not found
```

根因：安装器用 `cat > 文件 <<EOF`（**不带引号**，为了展开变量）写 tmpfiles 与单元，
而我在**注释**里写了反引号 ⇒ **不带引号的 heredoc 会做命令替换**
⇒ `` `DirectoryNotEmpty=` `` 与 `` `failed` `` 被当命令执行、在写出去的文件里**变成空**。

* 指令行没坏（那是**运气**，不是设计）；
* 而**原来那道 lint 结构上抓不到** —— 它排除了注释行，而这个反引号**就在注释行上**；
* 更坏的是：我随后"删残留"时**误删了整个 `.service` 段**，而真机上那份是**第一次装**
  留下的旧文件（内容恰好是对的）⇒ `cat` 看着没问题，`--root`（干净临时根）里才露出来。

⇒ 从根上断掉这类：**这几份脚本不许有不带引号的 heredoc**（值一律走 `printf`
或带引号的 heredoc），并把它做成判据（**⑰ / ⑥·补**）。

### 11.2 🔴 判据判的是**数字**，不是**边界**（对着一个好系统报红）

我第一版写的是"卷必须是 `700`"，真机报红：`卷不对：711 hupo-t3:hupo-t3`。查清楚：

| | 是什么 |
|---|---|
| `/home/<租户>` = **`700`** | 🔴 **这才是边界** —— 别人**连进都进不去** |
| `/home/<租户>/tenant`（卷）= **`711`** | 容器起来之后**它自己**改的 —— 为了让盒里的 agent（**另一个 uid**）走得进去 |

⇒ **判边界（谁也进不去），不判数字**。判数字**既会误报**（好系统报红）、
**又会漏报**（home 松了、卷是 700 照样绿）。现在判的是：
**正对照**（他自己进得去）＋ **负向对照**（另一个租户进去必须是 `EACCES`，不是 `ENOENT`）。
⚠️ 顺带：`create-tenant-users.sh` 文件头那句"卷 0700、别人读不到"只对**建的那一刻**成立，
已改准。

### 11.3 🔴 三条判据的"自证"是按**没装**那个状态写的 ⇒ 装上之后集体假红

`⑪ 真机上那几处一处都没动` / `⑫ 真机上没有投放口` / `⑥ 真那条路仍然是没装的` ——
装上之后全红，而**真机上什么都没有被这次测试改动**。
⇒ 正确的问题是"**这次测试有没有改动真机**"，不是"真机上有没有东西"：
改成**跑判据之前记一份真机状态指纹**（路径 + 属主/权限 + 内容哈希 + 投放口条目数 +
**租户名单** + 单元状态），跑完**逐字比** —— 这样**装之前装之后都成立**。

> 📌 **这一节和 §5.4 是同一条教训的两个面**：
> *"我这段代码是对的"* 与 *"它在真机上真的发生了"* 之间，隔着一整条**接线**，
> 而**判据自己也会有"只在某个状态下成立"的假设** —— 那种假设一过期，
> 好系统就开始报红，然后下一个人会去把闸关掉。

### 11.4 现在的事实（写给下一个人）

| | |
|---|---|
| **那条路是活的** | `.path` active + enabled；投放口 `/run/hupo-provision`（`1733 root:deploy`）；标记住 `/run/hupo-provision-state` |
| **真的建出来过一台** | `hupo-t3`（uid 3003），测试号 `13800001234`（⚠️ 这个号**真的在 `users.json` 里**，而且**没有删除路径** —— 它是一笔测试账，见 `00-PROGRESS` §六） |
| **四份 root 拥有的文件** | `--check` 逐字节核对：**仓库那份 == 装着那份** |
| **A6 基线** | 已用过一次；**要再验一次就得重新记基线**（装之前那种才算），或者直接看 `--check` 与单元 |
| **撤销** | `sudo bash scripts/install-provision-helper.sh --uninstall`（一条命令，**不删租户**） |
