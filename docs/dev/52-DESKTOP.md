# 52 · 桌面（**铺满整屏**）+ 聊天浮窗（**贴底** · 四边 30 · 阴影 · 三档）

> **这一篇回答**：登录之后那一屏长什么样、为什么这么摆、以及**这一刀修掉了哪几个真根因**。
>
> **主人 2026-09-22 两句话（第二句是更正）**：
> 1. *"首先，页面底部是一个桌面。聊天窗口是一个左右上下 margin 为 30 的浮窗，有阴影。"*
> 2. 🔴 **看了真站点之后**：*"桌面是全屏的，聊天窗口是在底部的。"*
> ⇒ 正确的形状：**桌面铺满整屏**（它是底图）、**聊天浮窗贴在屏幕底部**浮着、四边各 30。
> 另外他选过：**一进来是收起档**。
>
> 上一版（`50-DESKTOP-FLOATER.md`）**撞在可访问性硬闸上被回滚**；这一版按 D3.5 重做，
> 并把当时那三个根因都钉上了判据。
>
> ---
>
> ⚠️ **我读错了一次，值得留下**：第一句里的"页面底部是一个桌面"，我读成
> **"桌面=屏幕最下面那一条"**，还专门做了两种摆法去问他——**他当时也选了"那一条"**。
> 直到看了真站点他才说清："桌面是全屏的，聊天窗口是在底部的"。
> ⇒ **教训两条**：① 用户说"底部"时，可能指**桌面**的位置，也可能指**浮窗**的位置，
> 这两个都说得通，而选错的那个代价是**一整套布局返工**；
> ② **"问他选一个"不等于"问清了"** —— 选项要**带图/带截图**，光靠文字描述
> （"整页底图"/"底部一条"）两个人脑子里的画面可以完全不同。

---

## 一、形状（从下往上）

```
┌──────────────────────────────────────────────┐
│  （通知浮条：最上面那一层，仍在 `Stack` 里）      │
│                                              │
│      桌面（**铺满整屏**的底图）                │
│      ┌────┐                                  │
│      │设置│  ← 图标在桌面左上（4 列网格）      │
│      └────┘                                  │
│                                              │
│                     ↕ 30                     │
│  ┌────────────────────────────────────────┐  │
│  │  聊天浮窗（抓手行 + 时间线 + 输入条）     │  │  ← **贴屏幕底**、四边各留 **30**
│  │  三档：收起 / 半开 / 最大化              │  │     阴影 blur 32 · α.45 · offset(0,-6)
│  └────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

**两层是 `Stack`，不是上下排的 `Column`** —— 桌面在**下面那一层**（整页），
不是"下面那一条"。`Positioned.fill`（桌面）+ `Positioned(left/right/bottom: 30)`（浮窗）。
⚠️ `Positioned` 必须是 `Stack` 的**直接孩子**；浮窗能用的高度从 `MediaQuery` 算，
**不在 `Positioned` 里套 `LayoutBuilder`**（那个坑见 `50` §二）。

| 元素 | 在哪 | 数值出处 |
|---|---|---|
| 四边边距 **30** | `FloaterMetrics.margin` | 手册 `08-SPEC.md` §10.1 阈值总表（**这一批改过**） |
| 阴影 | `chat_floater.dart` 的 `BoxShadow` | 同上（沿用：blur 32 · α.45 · offset(0,-6)） |
| 半开比例 | `FloaterMetrics.halfRatio` | 手册 §6.2「高度用**系数**表达」 |
| 图标格 **52**（≥44） | `desktopIconBox` | D3.6 命中区。⚠️ **列宽要封顶**（见 §二 根因 5） |
| 默认档 **收起** | `ChatScreen.initialTier` | 主人 2026-09-22 定 |

**两条铁律没变**：**Z1** 浮窗永远在最上（任何页面盖不住它）；**Z3** 盖住不是铺满（四边留边距）。
新增 **Z4**：**桌面在底部一条**，浮窗浮在它上面，两者之间也是同一个边距。

---

## 二、🔴 上一版死在哪，这一版怎么修的（三个根因，都值得记）

### 根因 1：浮窗高度**定死**，再把字塞进去（`798px` 纵向溢出）

上一版是 `屏高 − 边距` 当高度，里面再放 chrome + 时间线。字一放大，
chrome（抓手行 + 状态条 + 输入条）比容器还高 ⇒ `Expanded` 被挤到负数 ⇒ 溢出。
**那正是 D3.5 的反面**（容器跟字算，不是字跟容器）。

**这一版**：
- **收起档不写死高度** —— 它就是抓手行那点内容，高度由内容算（`SizedBox(height: null)`）；
- 半开/最大化用**比例**（系数，不是像素）；
- 高度由父层 `LayoutBuilder` **量好传进来** —— 所以不用在 `Positioned` 里套 `LayoutBuilder`
  （那个坑会把整棵树报 `ParentDataWidget` 错，见 `50` §二）。

### 根因 2：空屏**不可滚**，被挤扁就溢出（`168px`）

桌面占了 30 + 一条之后，浮窗能用的高度变小了。3.1 倍字号下：
空屏那一块只分到 **104px**，而它自己要 **272px** ⇒ 硬闸当场红。

**修法不是把字写小**，而是让它**没地方时能滚**（有地方时仍然居中）——
和那条 `ListView` 时间线是同一个姿势。⚠️ 这条**只在实测里才看得见**：
探针量出来 `_EmptyState` 的 `constraints: h<=104`、`size: (676, 104)`，而它想要 272。

### 根因 3：🔴 拖拽手势**绑在了整块浮窗上** ⇒ 吃掉时间线的滚动

第一版把 `Listener(onPointerMove: …)` 挂在整块浮窗外面，于是**往上拖时间线**
被当成"把窗口拉高"。这一条不是溢出，是**功能坏了**——
而且它当时**没有判据**：是 `accessibility_test` 里"重发那个入口"那条先红的
（它要把时间线拖到最老一条，拖不动 ⇒ 找不到「重发」）。

**修法**：手势**只绑抓手那一行**（手册 §6.3 的原话就是"竖向拖**抓手 / 标题行**"）；
整块浮窗那层仍然 `HitTestBehavior.opaque`，但它**只负责挡住**（点浮窗内部不许漏到桌面）。
⇒ 判据补了一条**回归**：拖时间线**不许**改浮窗高度。

### 根因 4（**闸没抓到，是量出来的**）：桌面那一条被缩成"内容那么宽"

`Column` 默认 `crossAxisAlignment: center` ⇒ `AppDesktop` 只拿到 **100px 宽**并居中，
而它该是**横跨整宽的底部一条**。⚠️ **截图上看不出来**（桌面底色与页面底色是同一个 `paper`），
是在真视口（1280×614）下量矩形才发现的：`桌面 = Rect.fromLTRB(590, 525, 690, 614)`。
⇒ 外层 `Column` 加 `crossAxisAlignment: stretch`，并补一条判据（`desk.width == screen.width`）。

> 📌 **这一条值得记的原因**：它说明"看一眼"**不能代替"量一下"** ——
> 颜色一样的时候，眼睛看不出一个 100px 的盒子和一个整宽的盒子。

### 根因 5（**也是量出来的**）：宽屏下"列宽"被当成了图标大小

按 4 列算列宽，在 1280 宽的屏上得到 **293px**，而我把这个宽度直接当成了图标格的边长
⇒ 桌面上出现**一张 293×293 的大白卡**（截图里一眼就看见了）。
⇒ 修法：**图标格永远是那个小方块**（`desktopIconBox` 52），列宽只决定它在哪儿，
并且**列宽本身也要封顶**（`clamp(64, 92)`）—— 不然宽屏上图标会被摊开到屏幕各处。
⇒ 判据：宽屏（1280）与手机（390）下**图标位置一致**。

---

## 三、判据（`test/widget/desktop_floater_test.dart`，10 条）

| # | 判的什么 | 负向对照 |
|---|---|---|
| 1 | 一进来是**收起**：桌面在、看得见「会话」、有带字的「展开」、**没有输入条** | 展开态才有的东西一个都不许在 |
| 2 | **四边边距 = 30**（左/右/**下**都量到**屏幕边**）；**桌面铺满整屏**（四边贴屏幕） | 浮窗**不是铺满**（宽高都比屏幕小） |
| 3 | **有阴影**（blur 32 · offset(0,-6) · 颜色来自 `ink`） | —— |
| 4 | 点「展开」⇒ 真的开（输入条出现、「展开」消失） | —— |
| 5 | 点桌面空白（浮窗盖不到的那条边）⇒ 收起；点浮窗**内部** ⇒ **无反应** | 点内部之后**不许**变矮/漏到桌面；边距那条带子**必须**点得到 |
| 6 | 在输入条上按发送 ⇒ **最大化**（"发就拉满"） | —— |
| 7 | 拖**时间线**仍然滚动 | 浮窗高度**不许**变（根因 3 的回归） |
| 8 | 收起态「展开」**命中区 ≥44** | —— |
| 9 | 双击抓手 ⇒ 收起 | —— |
| 10 | 拖抓手向上 ⇒ 变高，松手吸附 | —— |
| 11 | **桌面铺满整屏**（四边贴屏幕，不是「内容那么宽」） | 实测过它曾缩成 100px 居中 |

**硬闸那两条在别处**：五档不溢出 + 命中区 ≥44 扫描 → `accessibility_test.dart`（149 条全过）。
> ⚠️ **这一批的判据一共 12 条** —— 形状改过一次（桌面从「底部一条」改成「铺满整屏」），
> 判据也跟着改过一次；**改形状就要改判据，两件事必须同一批**。

---

## 四、⚠️ 还没做的（明说）

| 还差什么 | 为什么没做 |
|---|---|
| **小程序容器壳**（`50` §四） | 桌上仍然只有「会话」一个图标，而**它就是聊天本身** ⇒ 容器壳**没有东西可装**。摆空壳 = "点了没反应"，这个项目最忌那个。等第二个图标上桌时一起做 |
| **甩**（下甩收起 / 上甩拉满） | **实现了**（速度 + 位移 + 时长三个条件都在），但**没有判据** —— 要造一个"速度够快"的合成拖拽才算验过。**当作欠着的** |
| **400ms 防抖** | 同上：实现了，没判据 |
| **平板上的样子** | 现在只在 800×600 的测试窗口与桌面浏览器上看过；`Z3` 在窄屏/平板上要复看（内容限宽 760 那条还在） |

---

## 六、桌上那个「设置」小程序（2026-09-22 主人定清）

> 主人原话：*"聊天和桌面是独立的，聊天是永续的，永远在底下。所以聊天不是桌面上的一个小程序。
> 桌面上应当有一个设置的小程序，用来退出登录，注销账号，修改 apikey。"*

| 变化 | 为什么 |
|---|---|
| **删掉桌上的「会话」图标** | 聊天**永续、永远在底下** ⇒ 它不需要入口，也不会被"打开/关掉"。⚠️ 与 D4.11 不冲突：图标墙是**第一入口**，入的是**小程序**（决策 **D4.12**） |
| **桌上换成「设置」** | 管**退出登录 / 注销账号 / 改钥匙**（决策 **D4.13**） |
| **退出登录搬进设置** | 它原来挂在**聊天抓手行**上 —— 那一行是"聊天"的地方，退出登录不是聊天的事 |

### 6.1 小程序容器（`lib/widgets/mini_app_host.dart`）

手册 §6.4 那五条，现在**全部落地**（原来那一节写着"小程序本轮不建"）：

| # | 规则 | 落点 |
|---|---|---|
| 1 | 收起时不许压住小程序里可点的东西 | 内容底部**内缩**：浮窗把自己**占多高**报给上层（`onHeight`），容器按它加 padding。⚠️ 这一条正是上一代栽过的"最后一行永远点不到" |
| 2 | 被盖住要**看得出来** | 压暗 `α.55` + 缩小 `0.98` + 圆角加大（`covered`） |
| 3 | 被盖住期间点可见区 = 收起聊天，**不传给小程序** | `IgnorePointer` + 一层吸收 `Listener` |
| 4 | 被盖住期间**照常运行**（不许 pause） | 容器一直在树上 |
| 5 | 打开小程序 ⇒ **聊天自动收起** | 图标 `onOpen` 里先 `collapse()` |
| ⏳ | **Z2**：回到可见不许重建（滚动位置 / 表单内容 / 请求都在） | **结构上成立，但没有判据** —— 明说 |

⚠️ **顶上那条栏由容器给**（返回 + 标题），小程序自己**不许**画 `AppBar` ——
它跑在**容器自己的 `Navigator`** 里，所以它内部怎么跳都**跳不出这个容器**。

🔴 **这一刀被闸抓到一个真 bug**（记在这儿，别再犯）：第一版把 `InkWell` 只包在**图标格**上，
**图标下面那行字在外面** ⇒ 点"设置"这行字落到了"点桌面空白"上（只收起了聊天）。
**更坏的是那时两组判据照样绿** —— 它们扫的是**桌面**，根本没进设置那一屏。
⇒ 修法是两件一起做：**字也要能点** + 给入口补一条**负向对照**（点完必须真的到了 `SettingsScreen`）。

### 6.2 同一天还要的两件（在 `54-COMPOSE-DRAFT.md` 里详记）

| 要什么 | 落点 |
|---|---|
| **展开后要有收回的按钮** | 「收起」原来在抓手行那条**横向滚动**里 ⇒ 窄屏 + 大字号会被滚出视野。⇒ **钉在横滚之外**，永远看得见（`chatCollapse`） |
| **小程序从图标扩开到全屏** | 图标点的时候**自己报出屏幕矩形**（`DesktopApp.onOpen(Rect)`）；容器用 `Rect.lerp` 从那儿长到全屏。⚠️ 内容按全屏排版、被矩形"露出来"，不是小窗放大 |
