# 66 · 下一步怎么走 —— **三位工程师的异步讨论 + 我的合成建议**（2026-09-23）

> **主人原话**：*「来三个工程师异步讨论一下接下来要怎么做。」*
>
> 这一篇是**过程与结论**：三位各自独立出方案 → 互看对方方案再交锋 → 我合成一份
> **带取舍的建议**（谁签字、我自己做什么、什么建议砍）。
> ⚠️ 它是 L3（证据与决策记录）：**要主人拍板的清单在 `63-OWNER-DECISIONS.md`**，
> 账本仍在 `00-PROGRESS.md` §六（现在开着 **10 条**）。

---

## 一、怎么讨论的（**方法**）

| 轮 | 做法 |
|---|---|
| **第一轮** | 三位**互不看对方**，各自只读仓库（不许改文件/git/重启/sudo），各交 ≤500 字：前三件 · 砍或推迟 · 最危险的一处 · 只做一件 |
| **第二轮** | 把别人写的**原文**丢给对方，要求：哪里不同意 · 撤回/修正自己哪一条 · #67 与 #48 谁先 |
| **合成** | 我逐条**独立复核**他们的关键论据（回到 `文件:行` 与真机读数），再定取舍 |

三位分别代表三个角度（**同一个问题，三种尺子**）：

| 代号 | 角度 |
|---|---|
| **安全运维** | 谁能从外面敲到门 · 一次重启会怎样 · 资源上限 |
| **产品客户端** | 用户真的能看到/用到什么 · 没验过的东西算不算做完 |
| **架构质量** | 不做的话后面以什么形式失败 · 哪个闸在替自己作假 |

---

## 二、三份方案（要点）

### 2.1 安全运维

- **前三件**：① **#45 收窄**（今天"任意手机号 × 6 位数字码"，失败只按 IP 计数）② **#48+#58 归 systemd**
  ③ **#69 `--cpus=` + #68 的盘/inode/fd 采样**。
- **砍/推迟**：砍 #52 本体 · **砍 #49 的清 journal 路线**（会连别项目日志一起删）·
  本轮只做 #65 的"可发现"（手册继续写"目标态·未实现"）· #67 只接会话级（容器级不必，内存已被 768m 挡住）。
- 🔴 **他现场查到一条**：`/` 是 **ext4**、`/proc/mounts` 无 `prjquota` ⇒ **#66 账里写的 xfs project quota
  在这台机器上不成立**，得改成"采样 + 满时明确拒"。
- **与账本排序的分歧**：#45 不该垫底（**唯一外人可达的入口**）；#52 不该排第一；#67/#68 不该各占一行。
- **只做一件**：#45 收窄 —— *其余加固都建立在"门后只有自己人"之上*。

### 2.2 产品客户端

- **前三件**：① **拿真租户走一次"多步的活"再截图**（批 B 的"真机验"验的是**注入**，端到端从没走过；
  这一步把"模型没用工具"与"用了但画不出来"当场分开）② **把读数落账 + 给主人总结**
  ③ 把 **#66+#69 打包成一次签字**（数值我替他算好）。
- **计划条怎么办**（他给的是明确主张）：**先真验一次；还不出 ⇒ 收口成"有就画、没有就不画"，不再加码**。
  理由：工具说明原文就写着单步任务跳过（`dsh-tool-todo`），而产品里绝大多数是单步问答；
  接线没问题（`src/plan.js`、`session-translate.js` 都在），**"再改人格"只加一个不发音的许愿**；
  而客户端**不区分**"本来没有"与"这一帧丢了" ⇒ 空转是有代价的。
  🔴 他反对"把 `create_goal` 挂到某句话上"—— 那等于给"模型自己想到的"开真动手的路，正是 P1/P2 要挡的形态。
- **砍/推迟**：砍"等模型自己学会用 `todo_write`" · 推迟语音本体 · 推迟往上翻再加码 · 不做预案性改人格。
- **只做一件**：真租户跑一句多步的活 + 截图（免费、不卡主人、不改一行代码）。

### 2.3 架构质量（**第二轮撤回了自己的第一名**）

- **第一轮**他排：① #67 接线空闲回收 ② #48+#58 ③ #65。
- 🔴 **第二轮他撤回 #67 排第一**，理由是实测出来的两件事：宿主只服务主人一份（`serve.js:622`）、
  而 `evictIfNeeded()` 只在"超上限"时淘汰（`agent-runtime.js:180`）⇒ **今天它永不触发**；
  租户 agent 在容器里、容器 `Restart=always` 不随它停 ⇒ **省不下来**。
  要等 **#48 落地（cgroup 有上限）之后**才谈得上省，而且判据必须读那个 cgroup 的 `memory.current`。
- ⇒ **他的最终排序：#48 第一** —— 它是"证明 #67 真省得下来"的前提，也是**其余资源闸唯一的落点**
  （`MemoryMax`/`CPUQuota` 得有个单元可写）。
- 他删掉了自己原来那条不成立的论据（"下次重启拒启动"—— 他实测 `verify-integrity` ✅、清单不漏）。
- **只做一件**（第二轮后）：**#48**。

---

## 三、他们**一致**的 / **分歧**

### 3.1 一致（三位各自独立得出）

1. **#48+#58 要排在前面**（安全运维第 2 · 架构第二轮第 1 · 产品把它并进"签字那批"）。
2. **#52 语音本体：砍**（形状留着、界面上那句实话一字不改）。
3. **#49 的清 journal 路线：砍**（`journalctl --vacuum-*` 只能整段时间删，会连别的项目一起删）。
4. **计划条的问题不在接线**，不能靠"再写一批代码"解决。
5. **#67 只有会话级值得接**；容器级停/销毁是**容量策略**，不是当下缺陷。

### 3.2 分歧（我判谁对）

| 争点 | 谁 | 我判 |
|---|---|---|
| 谁第一：#45 还是 #48 | 安全运维说 #45（唯一外人可达）· 架构说 #48（一切资源闸的落点） | **两个都对，但性质不同**：#48 **卡在主人签字**、改动面大；**#45 的收窄我这边就能写**（激活只要一句话）⇒ **先做 #45 的收窄（不等你）**，同时把 #48 的方案写出来等你签 |
| #65 出网要不要砍 | 安全运维主张"本轮只做可发现" | **不砍、也不急着一口气做完**：它和 #48 是**同一层**（宿主网络/单元）⇒ 排在 #48 之后一起做，落点现成 |
| #67 该不该现在接 | 架构第一轮说该（后撤回）· 安全运维说只接会话级 | **接，但不为省内存**：它现在是"**绿着没做**"（测试给了一条背书、旋钮零读者）⇒ 接上是**把假绿变真绿**，省内存要等 #48 |
| #68 算不算欠账 | 安全运维说"算不出是读数不是任务" | **半对**：内存那条在本机算不出（`admission.js:14` 自己写着），但**盘/inode/fd 今天连采样都没有** ⇒ 采样该做，判据等 #48 |

---

## 四、我的合成建议（**按"先做谁"排**）

### 4.1 我自己就能做（**不等你**，按顺序）

| # | 做什么 | 为什么是它 | 判据 |
|---|---|---|---|
| **1** | **计划条一次真机判定**（真租户发一句多步的活 ⇒ 看事件 + 截图） | 免费、决定性：它决定计划条是"活的"还是"该收口" | 见 §五 |
| **2** | **#45 的收窄**（白名单号 + **失败按号也计数** + 换掉那个共享 6 位码） | 唯一外人可达的门，而真修（短信）在你手上；收窄是**今天就做得到**的那一半 | 判据：非白名单号 ⇒ 明确拒绝且**不建用户、不投申请**；按号计数与按 IP 并存 |
| **3** | **资源闸那一批**：#69 的 `--cpus=` + #68 的**盘/inode/fd 采样** + #66 **改路线**（ext4 没有 prjquota ⇒ 采样 + 满时明确拒）+ **容量算式把内存/CPU 上限一起算** | 三条同一条链；**最便宜的加固**（一行 `--cpus=` 值待定）；不做 ⇒ 一个租户跑飞吃满 32 线程、把主人那份服务一起拖慢 | 采样有读数 · 超限**明确拒**（不许静默写坏）· 算式两个上限取小 |
| **4** | **#67 会话级接线**（把 `agentIdleEvictMs` 那个零读者的旋钮接上，走 `stop()`） | 现在**测试替它背书**、产品零调用者 = V13 同形的"绿着没做" | 到点真的调 `stop()` + `snapshot()` 不再列它；**负向对照**：不到点一动不动 |
| **5** | **账与文档收口**（这一批的读数落账、§八·补 的"在飞"改回"没有"、本文件归档） | 项目第一条纪律：**数字不许有两处**、说过的话要留痕 | 两闸绿 + 文档闸绿 + 推送 |

⚠️ **第 3 条里"值定多少"要你**（`--cpus=` 给几核、每用户盘给多大）—— 代码我先写，
但**值不许我拍**（那就是给一个你不承诺的数字上锁，`admission.js` 头上写着这条纪律）。

### 4.2 要你一句话的（**我建议按这个顺序签**）

| 顺序 | 事 | 不做会怎样 | 我准备到什么程度 |
|---|---|---|---|
| **1** | **#48+#58 归 systemd**（线上服务 + `frpc-w`/`frpc-apps` 两条隧道） | **这台机器重启一次 ⇒ 网站 502 + 小程序打不开**；而且服务现在活在 DSH 的 scope 里，`systemctl --user stop` 会顺手把它杀掉（**已经 502 过一次**） | 单元 + `MemoryMax`/`CPUQuota` + **回滚一条命令**（`start-tunnels.sh` + `restart-core.sh`）我来写，你签字我执行 |
| **2** | **#69/#66 的值** | 一个租户跑飞 ⇒ 整机慢；一个租户写满 ⇒ 全机坏 | 数值我先算好（按"活跃并发"估），你改一个数就行 |
| **3** | **真短信通道**（关掉临时验证码 = #45 的真修） | 关不掉就永远有一道"猜到 6 位数就能进"的门 | 选型与成本要你定（哪家、花不花这个钱） |
| **4** | **#49**：换口令 **或** 给具体几条命令开 NOPASSWD 白名单 | journal 里那 81 行**已经在了**（增量已止住） | 换成白名单之后**根本不需要口令**（推荐） |
| **5** | **#65 出网白名单 + 审计** | agent 能往任意地址发请求（**手册曾被读成"已经挡住了"**） | 排在 #48 之后做（落点在同一层） |
| **6** | **#52 语音本体：明说砍** | 不砍就一直挂着"等你拍板" | 形状已经在线上，界面那句实话一字不改 |
| **7** | 可选：**"第一句真话"端到端验收**（新号 → 开一台 → 贴真钥匙 → 真答一句 → 导出 → 删掉 → 回收站拿回来） | 从注册到一个真回答**没有一条路被一口气走完过** | 这是"能不能给人用"的总判据（`04-ROADMAP.md` §8.1 第 7 条） |

### 4.3 建议**明确砍掉**的（不许"以后再说"）

| 砍 | 理由 |
|---|---|
| **#49 的清 journal 路线** | 只能整段时间删 ⇒ 会连**别的项目**的日志一起删；代价比问题大。**增量已止住**（口令从 git 历史里取，命令行不再出现字面量） |
| **#52 的语音本体** | 主人 09-21 亲口砍过；真要做得按 `D5.10` 立新账（四类各 20 条语料、句子级命中率 ≥90%） |
| **等模型"自己学会用 `todo_write`"** | 工具说明就写着单步任务跳过；产品里绝大多数是单步问答 ⇒ 等不来 |
| **往上翻再"加码"** | 批 C 刚落地，先真用一阵 |
| **计划条的预案性"改人格"** | 只加一个不发音的许愿；真要给模型开"自己动手"的路，那正是 P1/P2 挡的形态 |

---

## 五、计划条那次真机判定（**这一批唯一的"做"**）

**做了**：拿测试租户 `u2`（= `hupo-b`，真容器、真钥匙）**发了两次**，然后**在真机上读 + 看**。

| 次 | 发的是什么 | 结果 |
|---|---|---|
| 1 | 普通的多步请求（"查三件事再一起告诉我：①北京天气 ②上海天气 ③温差"） | **它真做了**（两次检索 + 汇总，一轮答完）—— 但 **`todo_write` / `goal/change` 一次都没调** ⇒ **没有 `plan/updated`** ⇒ 屏幕上沿**什么都没有** |
| 2 | **明说要清单**（"这件事分三步，请你**用待办清单**跟着做，每完成一步就更新进度…"） | ✅ **调了** ⇒ `plan/updated` **两条**（开头三条未完成、结尾三条全完成）⇒ **屏幕上沿真的画出来了** |

**证据**：
- 事件（`GET /api/timeline` 从**他那台盒子里**读出来的原文）：
  `{"type":"plan/updated","goal":null,"todos":[{"text":"查北京未来三天天气",…},{"text":"查上海未来三天天气",…},{"text":"按天比对，找出两地温差最大的一天",…}],"doneCount":3,"total":3,"more":0,"seq":71}` —— **工具名 / goalId 一个字节都没有**（那条纪律守住了）。
- 屏幕：`HUPO_TOKEN=<u2> node scripts/check-web-browser.mjs --shot /tmp/plan-shot.png --shot-after 9000`
  ⇒ 截图里**上沿就是那条计划条**："ⓘ 它列了几件事"+ 三条打勾的待办（全部完成），浮窗照旧在底下收起态。
  （截图 sha256 `6dca5901951124b92a9e7bcb0312e902edf1e0150d61a77a0c202f733eb952e9`；⚠️ Flutter web 把字画在 canvas 上，
  **只有看一眼才算验过** —— 判据 **V13** 那一课。）

### 结论（我按它定）

1. **计划条留下**：接线是好的、真机上真能画出来（**第一次**，批 B 上线以来）。
2. **不加"改人格"这种许愿**：普通多步请求时**模型自愿不用**它（这是模型的选择，不是我们的 bug）——
   而那也正是三位里产品工程师的主张。
3. **不改交互、不动 `create_goal` 挂靠**：把"模型自己想到的"接到真动作上，正是 P1/P2 要挡的形态。
4. ⇒ 对用户的实话是：**"你明确要清单/进度时它才会列"**。
   ⚠️ 所以那条 ⚠️ 要留在账上：**"普通多步请求不出现计划条"是现状，不是缺陷**（别再当 bug 查一遍）。

---

### 顺带：这一趟**撞出两个真缺陷**（不是三位工程师发现的，是我去真机读数时撞上的）

1. 🔴 **`/api/timeline` 漏了进 `TENANT_ROUTES`** ⇒ 对**每一个有容器的用户**，这个请求由**宿主**代答，
   读到宿主侧那份**空**页 ⇒ 客户端如实说"没有更早的了"，**而盒子里明明有 60 条**。
   ⇒ 已修（表里补一行）+ 判据 `test/tenant-routes.test.js`（5 条；**变异**：拿掉那一行 ⇒ 恰好 2 条红）。
2. 🔴 **产品层从来没重发过**（`current` 那份 `grep api/timeline` = **0**）⇒ 盒子里跑的还是批 C 之前的代码，
   转发过去也是 **404**。⇒ 重发到 **`4e5411c2cb7a`**（`--verify` 真跑过容器、它自报这个指纹、`ledger.jsonl` 记账）。

**教训（V13 同族）**：批 C 的"已验"验的是**主人那一侧**（宿主直接答）；**租户那一侧从来没被走过**。
⇒ 已写进 `00-PROGRESS.md` §〇 第 **79** 条与手册 **v1.60**。

---

## 六、我独立复核过的论据（**没一条是听来的**）

| 谁说的 | 我怎么验的 | 结论 |
|---|---|---|
| `agentIdleEvictMs` 产品零读者 | `grep -rn` 全仓：只有 `config.js:240` + 六份测试构造配置 | ✅ 成立 |
| `evictIfNeeded()` 只看超上限 | 读 `agent-runtime.js:178-180`（`size <= limit` 直接返回）+ `test/agent.test.js:261-278` 钉的正是这条 | ✅ 成立 |
| `stop()` 只被轮次超时调用 | `grep -rn "\\.stop("` ⇒ 只有 `dispatcher.js:341`（`reason:'turn-deadline'`） | ✅ 成立 |
| 租户容器没有 CPU 上限 | 读**活着的** cgroup：`cpu.max = max 100000`、`cpu.weight = 100`；全仓 `--cpus` 0 命中 | ✅ 成立（硬证据） |
| 内存/pids 真设了 | 同一 cgroup：`memory.max = 805306368`（768MiB）、`memory.current ≈ 45.6MB`、`pids.max = 512 / current = 7` | ✅ 成立 |
| `/` 是 ext4 且无 prjquota | `findmnt -no FSTYPE,OPTIONS /` ⇒ `ext4 rw,relatime`；`grep -c prjquota /proc/mounts` ⇒ **0** | ✅ 成立（**#66 要改路线**） |
| 验证码是 6 位纯数字、失败只按 IP 计数 | 读 `/proc/1821763/environ` 只量形状（长度 6、全数字，**不打印内容**）+ `server.js:885,906-907` + `auth.js:76`（`#failures` 注释就是 `ip → …`） | ✅ 成立 |
| 登录成功即建用户 + 投开容器申请 | 读 `server.js:901-930`（`users.ensure(phone)` ⇒ `ensureTenant(who.id)`） | ✅ 成立（**这才是 #45 的重**：不只是"能看"，是"能占一台"） |
| 服务活在 DSH 的 scope 里 | `/proc/1821763/cgroup` | ✅ 成立（而且**每重启一次就换一个 scope**） |
| 只有两条隧道有单元 | `systemctl --user list-units`：`frpc-dsh-u` / `frpc-finart-v` 在，`frpc-w`(519993)/`frpc-apps`(1551914) 裸跑 | ✅ 成立 |
| 容量算式只算盘 | 读 `lib/tenant-capacity.sh`；实测这台机器盘 415 / 策略 332，而按内存倒推同时满载 ≈ 73 | ✅ 成立（**算式缺口**，并进 #68/#69） |

⚠️ **一条我故意没做的验证**：没有拿真码去试登录 —— 那会**当场建出一个用户**（`users.ensure`），
而且失败计数是**按 IP** 的（我们的探针都来自 `127.0.0.1`）⇒ 试错可能把**主人自己的登录**锁 15 分钟。
⇒ 这一条只读代码，不拿真机试。
