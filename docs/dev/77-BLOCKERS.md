# 77 · 执行 `76-PLAN.md` 时**卡住/要主人定**的事（边做边记，不中断）

> 规矩：遇到做不动、或**只有主人能拍**的，**不猜、不跳过** —— 记在这儿一行，
> 然后**继续做下一条**。最后一起汇报。

| # | 计划里的哪条 | 卡在哪 | 要主人给什么 | 不定的后果 |
|---|---|---|---|---|
| **~~B44~~** | ✅ **已解除**（2026-09-26 `#155` **已落地 ＋ 真机验过**：见 `00-PROGRESS.md` §九·补18 —— 同一间说两句 ⇒ 会话目录数**一条没多**、正本那条**长大了**；旧会话也已收口）—— 原描述：🔴 **一个工作区在 DSH 里 N 个对话**（真机读数：`/data/main` **38 条** · `/data/workspaces/aoshu-bank` **16 条**）| **根因**：官方那支 SDK server（`@deepseek-ai/dsh-sdk-jsonrpc-server`）**只能 create、不能 resume**（同一个 id 再进去报 `session "…" already exists`），而 DSH 的会话记录**跨进程活着** ⇒ 旧的调度器只能"每换一个 agent 进程实例就换一个 id"（`<会话>.<bootId>.<第几个>`，见 `agent-runtime.js` 顶上那段历史）。⇒ 界面里一个工作区 N 个对话；**派活到那一间的话**与**那一间自己说的话**不在同一条会话里（各自又开新的）。⚠️ 同一形状还有一条：每一轮都换 id ⇒ 连"同一间连着两轮"都不是同一条会话 | **不用你给什么**（技术账，已修）：① 新 `src/sdk-server-hupo.mjs`（我们自己的 SDK server：先看这条会话在不在 —— 在就 `ctx.agents.resume`、不在才 `create`）＋ `hupo-sdk-server.yml`（同一个 patch 里把官方那条 `disabled: true` —— 两个 server 都读 stdin 会抢帧）；② 新 `src/dsh-sessions.mjs`（一个用户一份 `<他那一格>/dsh-sessions.json`，原子写 `0600`；**坏内容不猜、不盖**）；③ `agent-runtime.js` 的会话 id 改成从映射取（**不再**带 `bootId`）。**落地要重新发布产品层 ＋ 重启他那台容器**（`bash scripts/build-tenant-code.sh` ＋ `sudo bash scripts/create-tenant-pool.sh --yes`）——那一步按 P1/P2 **要你签字**（或你说一声我跑）| 不定 ⇒ 界面里"一个工作区 = N 个对话"、记忆接不上（他 2026-09-25 报的那个形状）。⚠️ **两条如实说**：① 旧的会话文件**一个都没动**（"取最新一条当正本、其余带留痕收起来"是**另一手**，契约 `110` §七）；② `prune.js` 与"正本"的相互作用记在契约 `110` §八·欠账 1（**这一轮没动 `prune.js`**）|
| **B46** | ✅ **已修（判据＋读数）**（2026-09-26 · `#157`）—— **主人原话**：*"我在小程序里说话，他回复我现在接不上话。"* —— 原描述：🔴 **开发者入口把某一间开着的时候，那一间的聊天被 DSH 自己的写租约挡住**（`#156` 之后真机验到的 D4 答案）| **根因（真机原话）**：`session "owner/aoshu-bank.muh709vsibnh.1" is already owned by an active write handle` —— DSH 的持久化层对每条会话有一把**写租约**（`session.lock`，flock；源码原话 *"a second acquirer's zero-timeout wait times out"* ⇒ `SessionAlreadyOwnedError`）⇒ **第二个进程直接被拒**。⚠️ 与内存无关（那一下两台并存：web 392MB ＋ sdk 179MB；`oom_kill` **0**）。⚠️ 只影响**那一间**（入口没开那一间时聊天照旧，多次真机都是好的）。⚠️ 触发条件：主人在浏览器里**真的开着那一间的对话**（web 那次是**写开**了这条会话）——容器里的复现办法见 `110` §八·补4| **不用你给什么**（技术账，已修）—— 🔴 **两条一起做**（都在**那一个**服务进程里，没新开通道、没改协议）：① **让开（首选）**：聊天那条路在某一间起一轮**之前**先问开发者中继"这一间你开着吗"，开着 ⇒ 走 `dev-mode.js` 已有的 `dropCurrent()`/`killChild()` 那条路**收掉那台 `dsh web`**，并如实记一句（哪一间、为什么收）；**接缝是已有的那个形状**（`worlds.js` 把中继的**一个动作** `yieldRoom` 传给调度器 —— 与 `onUsage`/`onEgress`/`scopeExists` 同一条路），**宿主侧没有那个中继** ⇒ 一次都不问（不抛）；**只收"正开着的那一间"**，别的房间一个手指头都不碰。② **说不清就说明白（兜底）**：万一还是撞上写租约（时序/别的进程）⇒ 用户看到的那句换成**能指路**的人话「这一间你正开着看，先把它关掉、或者换一间，再跟我说话。」；**别的失败照旧**走原来那句「我现在接不上活…」（两档不许混成一个）| ✅ **已修**。判据 **L1–L5**（每条都能反着验）：**L1** 那一间正被入口开着 ⇒ 起那一轮**之前**收掉那台且点名是那一间（`asked === [room]`、收台发生在起 agent 之前）· **L2**（真那个中继）**别的房间开着 ⇒ 一发信号都不许有**（`held:false`、进程照活、`room` 不变）· **L3** 宿主侧（没有 `devContainer`／有对象但没那个动作）⇒ 聊天照旧、不抛 · **L4** 写租约那一档 ⇒ 用户看到的是那条"你正开着"的人话（真 stdio ＋ 假 agent 回带 `already owned by an active write handle` 的 error 帧；内部词表逐词扫）· **L5** 别的失败（id 形状）⇒ 照旧走原来那句。**五刀变异各自当场红**（读数在 `#157` 那行）：删掉 `deliver` 里的让开 ⇒ L1 红（`actual []`）· 去掉"只收这一间"的守卫 ⇒ L2 红（`held:true, pid:7501`）· 不守卫宿主侧（直接 `.yieldRoom()` 且不吞错）⇒ L3 红（`TypeError: Cannot read properties of null`）· 写租约档换回旧那句 ⇒ L4 红 · `isWriteLeaseError` 恒 true ⇒ L5 红。🔴 **真机读数（一次性容器 · 同镜像/同 cap（没有 `CAP_KILL`）/`--memory=768m` · 挂**未发布**的新产品层 `14503d3f1e3e` · 不碰 u2、不发布、不重启）**：脚本 `scripts/check-room-yield-container.sh`。盒里真造出那一间的会话 ⇒ 真中继起一台**真 `dsh web`**把那间开着 ⇒ 让 web **真的写开**这条会话（＝浏览器真的在看它；容器里没有浏览器 ⇒ 打它自己那条 `/api/session/selectModel`，第一句 `resolveAgent` 就写开 —— 回什么是 `session/model-unavailable` 不要紧）⇒ 再从**真调度器**那条路往同一间说一句：**修后** `delivered=true`、中继如实记一句（`开发者入口正开着「aoshu-bank」这一间 ⇒ 收掉那台 dsh web…`）、收完 `/proc` 里 **`--profile web`=0 台 / `--profile sdk`=1 台**、**`oom_kill` 0→0**、内存 387MB→188MB；**对照层（修前的 `dev-mode.js`/`dispatcher.js`/`worlds.js`/`serve.js`）同一套操作** ⇒ `delivered=false`、原话就是 **`session "aoshu-bank" is already owned by an active write handle`**（主人那次的原话），`--profile web` 照旧 1 台 · ⚠️ **没验什么**：u2 盒里的后修读数（要先发布 ＋ 容器重开 —— 那一步是父 agent 的）|
| **B45** | ✅ **已修并落地**（2026-09-26：A 形状收 `/` · B 投递被拒落一条人话 · 开机 preflight 拦；真机两间各说一句都答上了 —— 读数见 `00-PROGRESS.md` §九·补18）—— 原描述：🔴 **产品层第一版发到主人的盒子之后：任何一句话都没有答复**（真机 2026-09-26 · `#155·补` 已修；**这一版已经回滚过一次**）| **现场**：盒里那台 DSH 一直活着、什么都不说；`/api/say` 回 200 之后**五分钟里一个新事件都没有**。**根因 A（真根因）**：`SESSION_ID_SHAPE` 只收 `[A-Za-z0-9._~-]`，而映射里（`110` §七"取最新一条当正本"那一手）钉的是 **DSH 自己的 id** `owner/aoshu-bank.muh709vsibnh.1`（**带一个 `/`**）⇒ 服务端如实回 `sessionId 形状不认（只收 [A-Za-z0-9._~-]、最长 200）…`。⚠️ **那个 `/` 是 DSH 自己的写法**：**注册表/会话头里的 `id` 是原始形式，目录名才是转义形式**（DSH 的 `encodeSegment`：`/` ⇒ `~002F`）⇒ 判据必须是"**DSH 自己收什么**"。**根因 B（把一个错误变成一次静默挂死）**：投递失败那条路只"**摘票 ＋ 记 `#lastError` ＋ 发一条瞬态 `error`**"，而收口那句 `forceClose` 收的是 `#turns` 里**已经开过的轮**（由 `turn/start` 建账）⇒ `prompt` 被拒时**一轮都没开**、收 **0** 轮 ⇒ **一个字都没写**；而那条瞬态 `error` **客户端今天不渲染** ⇒ 用户永远等不到答复（N19）。`handoffDelivery` 那个 `catch` 同一形状 | **不用你给什么**（技术账，已修）：① 形状判据挪到新 `src/session-id.mjs`（**一处出处**）：**按 DSH 收什么就收什么** —— 收 `/`，只拒 空 / 超长（常量住代码）/ 控制字符 · NUL / `.`、`..` **整段**；② `sdk-server-hupo.mjs` 只 re-export、`dsh-sessions.mjs` 的 `readSessions` 用同一函数逐条校验、`config.js` 的 `preflight` 开机**逐个 id 过一遍**并硬拦；③ 新 `SessionTranslator.deliveryFailed`：先走既有失败收口，**一轮都没开**时**保证**落一条用户看得见的话（`deliver` 与 `handoffDelivery` 两处都改）。判据 T1/T2/T3 ＋ 变异 M11–M15（`110` §五）。⚠️ **落地要重新发布产品层 ＋ 重启他那台容器**（按 P1/P2 **要你签字**）| 不定 ⇒ 用户说一句、**永远没有答复**（回滚版没这个问题；**不带这两处修法不许再发**）|
| **~~B18~~** | ✅ **已解除**（2026-09-25 主人拍「**甲**」，当天做完并上线）—— **盒里现在真能跑命令了**。下面留的是**诊断**（它的价值在于"原因归错"那一课） | ✅ 根因查清：**不是沙箱、是没有 shell**。`landlock-run --probe` **在盒子里**（root 与 uid 1000 都试过）回 **`landlock: fully enforced`**、还能正常 exec `/bin/node`；真根因是 **`/bin` 里只有 `dsh` 与 `node`**（镜像本来就是 `scratch + node`），而 DSH 的 bash 工具**写死了** `["bash","-c",命令]`（`dsh-bash-local` 的 `run()`/`start()`）⇒ exec 那条命令 `ENOENT` ⇒ DSH 把它当**启动器失败**，报成 **`no sandbox backend is usable on this host`（把原因归错了）**。⚠️ **上一版 B18 那句"盒里现在就能跑命令 / enforcement: full"是量在宿主上的** —— 正是手册反复点的那类假绿：**看到"✅"先问一句"这是在哪一端验的"**。⚠️ 镜像 id 那条结论不变（回滚锚点 `5a66a92fe9a4`）；bwrap 那条也仍成立（uid 1000 无 `CAP_SYS_ADMIN` ⇒ 补 bwrap 是假绿）| ✅ **做了什么**（`scripts/build-tenant-image.sh`）：镜像里补 **`/bin/bash`**（宿主那份 ＋ 它的动态库，沿用 node 那套 `ldd` 拷贝）＋ **`/bin/sh` → bash** ＋ **静态 busybox**（`/bin/busybox` ＋ **122 个**按清单挑的 applet 软链：`ls/cat/grep/sed/awk/env/find/cp/mv/rm/chmod/tar…`）＋ **`/usr/bin/env`**（shebang 那一路）；**不联网、不拉镜像**（纪律①不变）。**没动沙箱**（盒里 landlock 本来就是好的）| **判据与真图**：镜像自检 **10 条全过**（⑤·丙 是新增的、"盒里那条命令真跑得起来"：`bash -c 'echo hi'` → hi（**故意写 `bash` 不写绝对路径** —— 那才是 DSH 那条路）、`/bin/sh` → bash、12 个常用命令都在、`/usr/bin/env node` 通、**负向对照 `python3` 仍然不在**）；`HUPO_CONTAINER_IMAGE=localhost/hupo-tenant:local bash scripts/check-container.sh` ⇒ **V1/V1b/V3/V4 全过**（⚠️ **这是第一次把这几条打在真租户镜像上** —— 以前那镜像里没 shell，`run()` 根本跑不起来）；**真机**（`/api/harness` 抓盒里那台 DSH 的原始事件，`u2` 的盒子）：`sandbox/mode workspace-write` → `tool/call name=bash {"command":"echo b18-shell-ok"}` → **`tool/result "b18-shell-ok\n" isError:false`**，且**不再出现** `SandboxUnavailableError`（与改前那次的原文正好相反）| 镜像 `5a66a92fe9a4 → 8a4ffdc04d8d`；两盒已 `load` ＋ **重开**（`create-tenant-pool.sh --yes`，顺带把 `--cpus=4` 也带上了运行中的那两台）；两盒 `Up`、`bash` 在盒里可用（`podman exec … /bin/bash -c` 实测）、数据无损（`u2` 6 个 app、时间线 maxSeq 170；`u1` 时间线 maxSeq 62）、公网 200 |
| **B27** | ⚠️ **"盒子不通"这条路上，三处的状态码不统一**（手册对齐时查出来的）| 同一个病（租户的库在他盒子里、盒子不通）今天三种回答：`/api/apps` 与 `/api/app-ask` 的预闸 ⇒ **如实 503**（`tenant-not-ready`）；而**制品口**走的是**异步 reject** ⇒ 落到 **403／404**（`app-serve.js` 的 `resolveApps` 抛才 403，`createBoxApps.read()` 过隧道失败是 reject ⇒ 404）。**结果不一样、给人的话也不一样** | **不用你给什么**（技术账）：要么统一成 503（"你那台刚才没应"），要么在手册里把三处**各是什么、为什么**写清。**我建议统一成 503**（同一个病给同一句话）——顺手加一条判据 | 不定 ⇒ 同一个"盒子不通"，三个出口三种说法（读的人会以为它们坏的原因不同）|
| **~~B40~~** | ✅ **已解除**（2026-09-25 主人**批**了"让它认得那些房间"＋拍「甲」看板形状；`#152` 收口）—— 下面留的是诊断。**诊断**：开发者模式那条入口起的是**另一个 harness**（`#150` 主人当场抓到的）| 主人原话：*"你在用户容器里面起了两个 deepseek harness，对吧？这是绝对绝对不允许的，它只允许有**一个** deepseek harness，然后你**所有**的对话都是在这一个 deepseek harness 里面完成的。"* 实测：盒里同时有 `dsh --profile web`（开发者模式那条，**没有人格/没有工具**、cwd=`/data`）与 `dsh --profile sdk`（真那台）。⚠️ **DSH 本来就没有守护进程**（`dsh [options] [command]` 一次调用一个进程）⇒ "一个 harness" = **一个 `$DSH_HOME` ＋ 一套 profile**；按轮起多个 `sdk` 进程是**同一套配置的多次调用**，不算两台 | **做完了**（`#109` ＋ `#152`）：那条入口现在①**同一套 patch**（人格 ＋ 能力层 ＋ 模型那条 —— `agentPatchArgs()`，与调度器那台**一处出处**）②**同一个 `DSH_HOME`**（`/data/dsh`，不另设）③**那一间的 cwd**（房间清单：`main` ＋ 每一间工作区）；开关**已开回来**（主人批的 `workspaceNudge: true` —— 不认得那些房间，界面会把它们的对话整个藏掉）。🔴 **判据改过一次，如实记**：我原先把 D7 写成"源码里不许再出现 `--profile web`"，**那条是错的** —— `dsh --profile sdk --host …` ⇒ `unknown option '--host'`，**浏览器界面只有 `web` 那个 app 提供**；照旧那条改会把这条入口**改坏**。⇒ D7′ = 同源落在 **patch / home / cwd** 三样上（契约 `109` §七.3/§七.4）。⚠️ **还剩一条真机没验**：同一个 cwd 上两个进程会不会抢那份对话（D4）| —— （开关已开；**如实说**：D4 那条真机读数还欠着，见 `#152`）|
| **B42** | 🔴 **写 DSH 的工作区注册表时换了属主 ⇒ 它自己读不了 ⇒ 每一间都起不来**（2026-09-25 真机 · `#154` 已修，**等落地**）| 现象：点开发者入口里**任何一间** ⇒ **502**；盒里那台 `dsh web` 的 stderr 原文：`EACCES: open '/data/dsh/storages/workspace.json'`（`errno: -13`）⇒ DSH **起不来**。根因：那份注册表是**那台 DSH（uid=1000，`agent`）**建/读的，而 `ensureRoomRegistered()` 写它的是**盒里那个服务（root）**，而它按 `mode: 0o600` 写 ⇒ 文件变成 **`root:root 0600`** ⇒ **DSH 从此读不了它**。⚠️ 触发条件：注册表里**没有**被点的那一间（新的一间 / 刚见过的那一间）—— 我为了 D3 打开了 `workspaceNudge`（主人批的），第一次 nudge 就把这个坑踩响了 | **不用你给什么**（技术账，已修）：① 写之前**记下原来的属主与权限**，写到**临时文件**上 → `chown` 回原主 → 再 `rename` 盖上去（**先换手、后改名**：换不过去就什么都没换）；② 换不过手 ⇒ **原文一个字节都不改** ＋ 如实说一句；③ 盒里那份**已经手工修回来**（`chown agent:agent` + `0600`，实测 uid 1000 读得动）。⚠️ **落地要重启你那台容器**（见右侧"要主人给什么"）| —— （已修，等落地）。⚠️ **这一条是"我为了修 D3 反而弄坏了它"的实例**：教训写进 `109` §8.5 —— **改"运行中的东西"以前，先想"它的属主/权限是谁的"** |
| **B43** | 🔴 **换房间撞内存上限：新起的那台 `dsh web` 被内核杀掉**（2026-09-25 真机 · `#154` 已修）<br>🔴 **第二版（2026-09-26 · `#156`）：光"等它退"不够 —— 它**杀不动**。** 主人报的原话：*"dsh19145526557 的 dsh 没有接通 websocket"*（换一间就 502 ⇒ 那一页的 WebSocket 永远连不上；**第一间是好的**）。**新根因**：那台 `dsh web` 是**换手到 uid 1000** 起的（安全决策①，不许改），而盒里那个服务（容器 root）是 `--cap-drop=ALL` ＋ 只加回五条 ⇒ `CapEff=0xcb`、**没有 `CAP_KILL`** ⇒ `c.kill(...)` **静默 EPERM**（真机原话 `kill: (14) - Operation not permitted`，被 `try/catch` 吞掉）⇒ "换房间先收旧的"**从来没真的收掉过**。⚠️ 为什么以前全绿：每次发布 ⇒ 容器重建 ⇒ 旧的都没了，而检查总在"刚重建之后"选第一间（**时机假绿**）| 硬读数（u2 的盒子）：容器 `memory.max = 768MB` · 盒里自己的服务 ~76MB · **一台 `dsh web` 348MB ＋ 它自己那三个 MCP 子进程 ~144MB**；换房间那条路原来是"`SIGTERM` 旧的 ⇒ **立刻**起新的"（旧的要 `killGraceMs` 之后才 `SIGKILL`）⇒ **两台的峰值叠在一起** ⇒ 内核把**新起来的那台**杀掉：`/sys/fs/cgroup/memory.events` 的 `oom_kill` **3 → 5**，正好对应我两次换房间；盒里日志是 `dsh web 起不来（code=—，signal=SIGKILL）`。⚠️ 附带一处真缺陷：**失败的那一台没人收**（`spawnWeb` 原来在 `try` 外面 ⇒ 走不到那个 `catch`）⇒ 它赖在容器里占 ~348MB，而中继以为"什么都没在跑"。🔴 **第二版追加的真机读数**（父 agent · u2）：`/tmp/switch3.mjs main aoshu-bank main` ⇒ `main` 200/101、`aoshu-bank` **取页 502**、再回 `main` **502**；换完内存 299MB · **`oom_kill` 8→13** · `/web` 进程**两台**（旧那台没死）。⚠️ 还有一件：清完孤儿过一会儿又冒出 1/2/3 台，而**没人在点房间** ⇒ 入口那条 WebSocket **自动重连** ⇒ 每次重连/取页都又起一台 = **spawn 风暴** | **不用你给什么**（技术账，已修）：① 起新台之前 **`await` 旧那台真的退出**（有上限 `DEV_EXIT_WAIT_MS`，超时如实说）；② 失败的那一台**按句柄收掉**（不留孤儿）；③ 起台期间 `oom_kill` 涨了 ⇒ 那句 502 里**如实写上"被系统杀过（内存不够）"**（读不到就一个字不编）。🔴 **第二版（`#156`）再加五条**：④ 发信号改走**同 uid 的小 helper**（`killPid` —— 同 uid 之间发信号**不需要** `CAP_KILL`）＋ **杀完核实真的死了**（读 `/proc/<pid>`，有上限）＋ **没杀掉如实说**（点名 pid，不许静默、不许当"收干净了"）；⑤ **起新台前扫孤儿**（**只认 `--profile web`**、**只有盒里那一支才开**（默认关 ⇒ 宿主上主人的 `dsh` 一台都不碰）、不动"现在这一台"）；⑥ **内存闸**：余量不够就等（有上限），到点还不够 ⇒ **如实 503 ＋ 人话**（不硬起、也不说成"没起来"）；⑦ **失败之后的冷却**（参数住代码）：冷却期内不许再起，如实 503（治那条自动重连引起的 spawn 风暴）；⑧ 判据 **K1–K6** ＋ 变异 **M1–M9**（九刀全红，读数在 `110` §八·补3）。⚠️ **长期那条**：这台盒子**同时只装得下一台** `dsh web` —— "一次只开一间"是这个上限逼出来的形状，不是省事 | —— （已修；**第二版的真机读数打在一次性容器里**：同镜像/同 cap/`--memory=768m`/同 `--pids-limit`，**换 3 次房间全 200＋101、每次只剩 1 台且 `cwd` 就是新那一间、`oom_kill` 0→0**；同一套跑**修前那份** ⇒ **502×3 ＋ `oom_kill` 0→2→4→6 ＋ 旧那台（pid/cwd 一直没变）一直活着**。**u2 盒里那三条后修读数由父 agent 在发布后补** —— 子 agent 不许发布/重启）|
| **B39** | ✅ **已做**（2026-09-25 · `#152` —— 按我建议的**③**：**不许问**兜前 ＋ **自动接**兜后；主人那一轮说过"**全部签字**"，所以按推荐项做；**若你不要"自动接"那一段，说一声我把它关掉**）—— 原描述：🔴 **派出去的子进程会"停下来问一句"，而那一间里没人能回答它 ⇒ 活永远停住**（`#147`/`#149` 查那个没做完的奥数 app 时抓到的）| 主人在主对话里让助手做「奥数 app」，助手**派了活**（`where:"math-olympiad"`），子进程在它那一间里留下：`message/text`「收到，我这就动手做奥数小程序。」＋ **`message/text`「这条我说太长了，没说完。你回一句「接着说」，我就接着上面往下讲。」** ⇒ 它在**等一个"接着说"**，而那一间的"用户"是主进程、**没有任何东西会替它答** ⇒ 那一轮就此停住、`job_done` 从没被调用（登记永远 `started`，见 B28）。⚠️ 那句"太长了/接着说"**不是我们代码里的**（`grep` 0 命中）⇒ 是 DSH 那边给"用户"的续写提示。🔴 同一形状的另一半：**子进程问任何问题都等于石沉大海**（它没法等一个真人的回答） | **做完了什么**：① **不许问** —— 派活时那段任务书写死"**自己做、别问、别等**"（`job.js` 的 `jobPacketText`）；② **自动接** —— 那一轮**停住**（子进程只留下一句"接着说"那种）⇒ 调度器**替主人接一句**（`JOB_NUDGE_LINE`："接着上面往下，把那件事做完；做完用一句话交回来。别问任何人、也别等谁回话。"），**有上限**（`JOB_NUDGE_MAX`），**接满还没交回 ⇒ 如实报"没做完"**（而且**只说一次**）。⚠️ 判据 S1–S6/B39-a,b,c 每条都做过变异（见 `docs/dev/108-JOB-ASK-FLOW.md`）| —— （③已按推荐项落地；**要关掉"自动接"那一段只需说一句**）|
| **B41** | 🔴 **子进程说"已经在你桌面上了"，而它其实没上架**（`#152` 复核那一批时抓到的**实例**；与 **B31** 同根）| **日志上的原话**（盒里那条可见日志 `seq 409`，`job/report`）：`where:"math-olympiad-2"` · `name:"奥数知识库·题库"` · summary 开头就是 **"桌面上的「math-olympiad-2」做好了"**；而**去盒里点名查**：`/api/apps`（以盒子为准）里**没有**它、`/data/hupo/apps/math-olympiad-2` 也不存在 ⇒ 那句话是**没根据的**。⚠️ 它不是系统性的：同一天另一次真机（`count-twenty-one-twenty-three`）**真上架了**（`/api/apps` 里点名看得到）⇒ **有的孩子上架、有的只在总结里说上架了**，而宿主两边都不核对 | **不用你给什么**（技术账 ＋ 我自己的纪律）：① **给孩子的任务书写死**"做完**必须自己上架**（`app_publish`）＋ 把**可核对的物证**（那个东西在库里、版本号对得上）写进报告"；② **我这一侧**：子 agent 的"做完了"**必须落到一件查得到的东西上**才算数，**没核实过的一句都不许写进账** —— 这一条是**我的**问题（我差点把一句没核的话抄进账）| 不定 ⇒ 账上会留下"做完了"而实际没有的东西（这个项目最忌的形状）|
| **B38** | ✅ **已修**（2026-09-26 · `#158`：**根因抓到 ＋ 判据 ＋ 七刀变异 ＋ 临时实例真机读数**）—— 原描述：🔴 **核心关不干净：退出要 90 秒 ⇒ 被 systemd SIGKILL**（`#149`／`#152` 两次线上事故的帮凶）| `serve.js` **有**优雅退出（`SIGTERM` ⇒ `worlds.shutdownDispatchers()` ⇒ `runtime.shutdown()` ⇒ `close()` ⇒ `exit(0)`），但**今天又发生了一次**（`#152` 部署客户端那次）：2026-09-26 **00:14:09** 停 ⇒ **00:15:39** `State 'stop-sigterm' timed out. Killing.` ＋ `SIGKILL` ＋ `Failed with result 'timeout'` ⇒ **线上整整 90 秒不可用**（这是**实测第二次**，不是推测）。⚠️ 那一窗**正赶上**：一条 WS（`/api/stream`）连着、而且刚触发过一轮真 agent 的活 ⇒ **嫌疑最大的是"关的时候在等一条没结束的连接／一轮没收口的活"**（`close()` 里注释自己写着"WS 是长连接…否则关闭会一直挂着"；今天那一条是 `/api/stream` —— 它**在**终止名单里，所以要查的是"进 `close()` 之前那两步卡在哪"：`shutdownDispatchers()` 还是 `runtime.shutdown()`）| **不用你给什么**（技术账，已按它收口）—— 🔴 **先回答那个"最后一行"的问题**（我原来漏的那一行）：那两次失败的 `收到 SIGTERM，收工。` 之后**最后一行就是它自己** —— 紧接着那行已经是**下一次启动的开机横幅**。逐字取证：`serve.log` 第 **1610** 行（2026-09-25 22:11:25 那次，`(node:297162)` 就是 journal 里被 SIGKILL 的那个 pid，第 1607 行）与第 **1816** 行（2026-09-26 00:14:09 那次），两次的下一行都是 `⚠️ 开机清单按 /home/deploy 算`／`⚠️ 开机清单里"只报不拦"的条目动过`（＝新进程的横幅）。⇒ **那几段代码本来就不打点**，所以"卡在哪一步"**在日志里根本不存在**，光看日志永远查不出来 —— 这就是当时的实情。🔴 **根因（真机读数，不是推测）**：那一条路**每一步都没有上限**，而 `close()` 里那个 `server.close()` 会**等所有连接自己断开** ⇒ 2026-09-26 在**临时实例**上（另起端口／临时数据目录，**没碰线上那个单元**）量到：**空载收工 17 毫秒**；只要挂着一条**没发完的 HTTP 请求**（半开的连接 ⇒ 不算 idle）就 **40 秒都不退**（线上那两次正是被 systemd 在 90 秒时 SIGKILL）。⇒ **修法**：新 `src/shutdown-budget.js` —— 顺序**逐字没变**，但**每一步带上限**（单步超 ＝ 如实点名那一句 ＋ 「记一笔，接着往下收」）、**整条还有一个总上限**（到点如实说卡在哪一步再退）；上限到了**不掐活**（不许为了快把正在跑的活砍了），只是**不许再打"这次是好好走的"标记**（那句话是"我说出去了的话"，没做到不许说 ⇒ 下次开机横幅如实写"没善终"），而**那把锁照样撤**（与善终无关）。数值住代码里（`src/shutdown-budget.js`，手册纪律 1）。| ✅ **已收口**（判据 ＋ 变异 ＋ 真机读数都在 `#158`）：`test/shutdown-budget.test.js` **9 条** —— **Z1** 某一步永不返回 ⇒ 到上限后**照样往下走完并退出** · **Z2** 卡住时**点名那一句**（正常那次**不许**打）· **Z3** 正常收工**顺序与今天逐字一样**（换位 ⇒ 红）· **Z4** 各步都卡 ⇒ **总上限**兜底（不再 await 任何东西，同步那几步照走）＋「上限到了不许打善终标记」＋「某一步抛了也接着往下收」＋「hooks 缺一步要大声说」＋「上限量级必须宽于合法收口」＋「`serve.js` 走的确实是这份预算」。**七刀变异各自当场红**（去掉单步上限／不点名／换个顺序／去掉总上限／没等完也打善终标记／上限卡得比合法收口还紧／`serve.js` 漏接一步；两个文件 md5 还原自查）。⚠️ **还欠一条真机**（如实说）：**线上那个单元**（`hupo-core`）**没动过** —— 收尾要等父 agent 走 `restart-core.sh` 那一步才能读"真实那次收工几秒"；临时实例上的两条读数见 `#158`|
| **B37** | 🔴 **"整条消息都是图片地址"时，那串地址会被当正文画在图片上方**（`#148` 的子 agent 顺手查出来的）| `lib/models/image_links.dart` 的 `textWithoutImageLines` 在"每一行都是图片地址"时走 `out.isEmpty ? text : out` 兜底 ⇒ **返回原地址**；而 `AnswerBubble` 无论如何都画 `Text(textWithoutImageLines(text))` ⇒ 纯图片回复**把地址当正文画出来**了，与该函数自己的注释（"画出来的图那一行地址不重复显示"）**自相矛盾**。实测：`textWithoutImageLines('https://img.example/dog.png')` = 那个地址本身。⚠️ 对 `#148` 的影响：多选/复制的"正文"取的是**气泡真正画出来的那串**（同一个函数、一处出处）⇒ 纯图片那条会复制到那个地址（"屏幕上确实有那串字"，所以 S2 仍成立），**该修的是显示那一侧** | **要你一句话**（其实是个小修）：兜底改成"整条都是图 ⇒ 正文为空"（气泡只画图、不画地址）—— 那样纯图片那条的复制就会走"复制不了，如实说一句"。**要不要我修？**（它有一条既有测试钉着现在这个兜底，改的时候一起动）| 不定 ⇒ 纯图片回复上会挂着一串 `https://…`（看着像坏了）|
| **B36** | ✅ **已拍：不清**（主人 2026-09-25「可以」按我的建议）—— 原描述（留着做记录）：**回收之后，助手那边还留着一份派生缓存**（`#144` 真机取证时发现的）| “真回收”搬走的是 `$DSH_HOME/sessions/--data-workspaces-<id>--/`（会话的原件），但 `$DSH_HOME/storages/session_projcache/sessions/*.json` 里还有一份**索引/缓存**（本机实测 329 个 json，每条含 `record.identity.cwd`）—— 它**不是那段对话的原件**，但里面找得到“哪一间的会话曾经存在过” | **要你一句话**（其实是个判断）：要不要连这份派生缓存一起清？（我倾向**不清**：它是 DSH 自己的缓存，删它可能连累别的间；而且它不含对话正文。）划清界限后我把口径写进 `103` §七 | 不定 ⇒ 严格抠字眼的话，“那一间一点不剩”还差这一处（正文与原件确实都拿走了）|
| **B35** | ✅ **已做**（2026-09-25 当天：#146）—— 原描述：🔴 **窗口自己跟过去之后，聊天条上那个指示会说假话**（`#142` 真机打中之后当场看出来的）| 那个小图标与它的说明取自 `_openApp`（**点开的那个图标**），而自动跟过去时 `_openApp` **还是 null** ⇒ 屏幕上写着/响着「**在桌面上问（全局）**」，而那句话其实是送到**刚派出去的那一间**去的 —— **页面在说假话**（这个项目最忌的形状）。今天它还只是"看着不对"，将来那一间有了名字/图标之后会**看着更不对** | **要你一句话**（我建议这么改）：那个指示**改成看"现在这间"**（`c.scope`）—— 主对话 ⇒ 家图标；某一间 ⇒ 查得到名字就"在「X」里问"（照旧），**查不到名字**（刚派出去的活，名字在他盒子里 · B20）就说一句**不撒谎的模糊话**（例如「**在另一处问**」）。要不要我照这个改？ | 不定 ⇒ 他会看到"明明说在桌面上问，话却进了另一间" |
| **B34** | ✅ **已做人格那一条**（2026-09-25 当天：#146；真机复验见账）—— 原描述：🔴 **"派活"那条工具模型自己不会调（`#142` 真机实测 —— 机制全在，触发不在）** | 拿**主人的原话形状**试了一次（*"帮我做一个能数 1 到 3 的小东西，放到我桌面上"*）⇒ 它在**主对话里自己把东西做完了**（那一段全在 `main`，`job/start` **0 条**、`scope/open` 一次都没出现）。🔴 **不是"工具没挂上"**：我从盒里把它那一轮的会话文件解出来点名看过 —— **9 个账本工具全在**（`mcp__ledger__job_start` 在），容器也是发布新产品层之后（20:25:47）才起来的 ⇒ 就是**模型不调它**（工具说明写着"主人让你做新东西时调它"，但人格层没有"做新东西 ⇒ 先派活"这条，模型按老习惯自己动手了） | **要你一句话**：要不要在**人格层**（V1 默认层）里加一条 —— 「他说做一个**新东西** ⇒ 先把这活**派到一个新的一处**，别在主对话里自己动手」？⚠️ 那是 `hupo-persona.yml`（**strict**，改完要重建开机清单）。也可以换个做法：把 `app_create` 那条口的说明改成"只在被派到的那一处里用"，主对话里调它 ⇒ 明确拒绝并提示走派活。**你挑一个** | 不定 ⇒ 你在主对话里说"做一个…"，它**还是在主对话里做**（新那一间永远建不出来）—— 那正是你 2026-09-25 报的那个形状 |
| **B33** | **派活那条总结"不用问就记得"今天做不到（`102` 做完时如实报的）** | 主进程那份**跨重启背景**（`buildRecap`）只收 `user/echo` 与 `message/*`，**不收 `notice`** ⇒ 子进程交回的那条 `work-done` 总结**不会自动进主进程 agent 的记忆**。今天能保证的是"**查得到**"：`job_list`（真的那条口）＋ 主进程视图里那一帧 `job/report` ＋ 既有的 `app_list`。⇒ 主人那句"**我未来聊天里能知道自己干过什么**"**只对了一半**（他问得出来，但它不会主动记得） | **不用你给什么**（技术账）：要"自动记得"得动 `recap.js`（把 `notice` 里那几类收进背景，并想清"收哪些、怎么不重复"）。排在这一批之后 | 不定 ⇒ 未来主进程聊天里，它**不主动提**"我做过奥数那个"；主人问才答 |
| **B32** | **派活登记与桌面可能对不上（`102` 报的"存疑待核"）** | `JobBook.list()` 的"有哪些工作区"来自 `AppWorkspaces.list()`。而 `103` 那批删 app 走的是 `Apps.remove()`（挪进 `.removed/`）——**工作区那一格留不留残迹、会不会被 `list()` 列出来**，两批没对齐过 | **不用你给什么**（技术账）：两批都落地后**实测一次**（删掉一个有工作区的 app ⇒ 看登记里还列不列它），不一致就把口径收成一处 | 不定 ⇒ 主人问"我有哪些工作区"，答案里可能多出一个**已经删掉**的 |
| **B31** | **`job_done` 的名字与总结由子进程自述、宿主不核对** | 它说"做成了"就算成了（今天只削长度、去无依据的时间词）。**没有任何可核对的信号**（没有"制品真的建出来了"这种交叉校验） | **不用你给什么**（技术账）：要能核对就得让"回报"带上**可验的物证**（例如那个 app 真在他的库里 ＋ 版本号对得上）。在那之前**不许**把这条总结当验收 | 不定 ⇒ 一个跑歪的子进程说"做完了"，主进程就照说 —— **页面可能在说假话**（这正是本项目最忌的）|
| **B30** | ✅ **已拍：先不做「抢屏」**（主人 2026-09-25「可以」）—— 原描述：**`scope/open` 只到"焦点停在主线"的那条连接（`102` §7.3）** | 服务端实时路由（`eventInScope`）按焦点分拣：另一台设备／另一个房间面朝上的那条连接**收不到**"去这一间"那一帧 ⇒ 那种情形下界面**不会自己切过去**（他自己点那个图标照旧进得去）| **要你一句话**（要不要覆盖）：要覆盖就得动 `server.js`（让这一帧按"这人所有连接"广播）——那是一条**协议级**改动，我建议**先不做**（另一种可能：他本来就在别处忙，屏幕不该被抢走）。**你定"抢"还是"不抢"** | 不定 ⇒ 两台设备同时开着时，只有你正在用的那一台会跟过去（另一台留在原处）|
| **B29** | ✅ **已拍：不做「拿回来」入口**（主人 2026-09-25「可以」；界面上照旧不许承诺可恢复）—— 原描述：**删掉之后**没有任何地方能把它**列出来 / 拿回来**（`103` 写这一批时记下的）| 服务端确实是**软删**（`Apps.remove()` 把制品挪进 `.removed/` ＋ 留审计），但**今天没有任何一处**能列出 `.removed/`、也没有"装回来"的入口（`grep .removed` 只有 `remove()` 写它）⇒ 界面上**不许**说"还能拿回来"（说了就是假话，所以那一版面板上**只写"从桌面上删掉"**）| **要你一句话**：要不要做"**拿回来**"（一个回收站／一句"把我删的那个装回来"）。要做我就先做**列得出来**这一半（有出口才敢在界面上提"可恢复"）| 不定 ⇒ 误删就只能请人从盘上捞；**面板上也不会提"可恢复"**（今天这样是**诚实的**，不是缺功能）|
| **B28** | 🚧 **一半已做**（2026-09-25 · `#153`：**「按房间回收」那条口做出来了** —— `POST /api/room-remove {scope}`，见 `docs/dev/103-APP-DELETE.md` §八；主人说「空的工作区要清理」，那一批空壳已按这条口清掉）。**还欠的一半**：**派出去的活没有清扫 / TTL**（子进程永远不交回时，登记里那一行永远停在 `started`）—— 那要定口径（多久算没做成 / 要不要告诉他），是**设计**不是缺门 | 子进程**永远不交回**（崩了／被硬杀／忘了调 `job_done`）时：登记里那一行永远停在 `started`，那一间里留一个占位 `index.html`，而**没有任何东西**会把它收成"没做完"。P1 那本活账只在那一间收"已停"，**两条账不联动**。⚠️ **加上 D3.11 之后又多半条**（我 2026-09-25 实测）："从桌面上删掉"那条路是**按 app id** 走的（`/api/app-remove {id}`）⇒ **一个还没做出东西的工作区（没有制品）删不到**（那个 id 在制品库里不存在 ⇒ 404）。今天我探针留下的 4 个这样的空工作区就躺在盒里 `/data/workspaces/`（**用户看不见**，因为他没有那个图标；但盘上一直在）| **不用你给什么**（技术账）：要收口得定一条口径（"多久没人交回就当没做成"＋要不要告诉他／要不要连那一间一起回收）。⚠️ 写死数值前先想清判据（手册纪律 1）；要"按工作区回收"就得另开一条口（不是 app 那条）| 不定 ⇒ 主人问"那件怎么样了"时，那件**永远在"正在做"**（而它其实早没了）—— 就是"看起来在跑"那类；而且空工作区只增不减 | ⚠️ （**2026-09-25 真机实证**：主人让助手开一处新地方做「奥数 app」（短名 `math-olympiad`）——
登记里那一行 `status:"started"` 之后再没等到 `reported`，可**制品已经建出来了**
（`/data/hupo/apps/math-olympiad` 在、工作区里 `index.html` 在）⇒ 子进程**做到一半没交回**。
主进程那边**如实**跟他说了「那件事我没做完，你回头看一眼。」（那一句是对的）；
但登记永远停在 `started`、桌上多一个**半成品图标** —— 这正是这一条要收的东西。） |
| **B24** | 🔴 **出界闸与登记面之间没有连线**（实现阶段 4 时查出来的）| `outbound.readPacks` 会把 `<scope>/.data/<pack>/pack.json` 里 `outbound:'one-to-one'` 的包判成"永不出"，而 `Delivery.deliver()` **完全不查** `readPacks`／`adjudicate` ⇒ 那个声明今天**只是装饰**（与 B22"没有生产者"是同一件事的两面）。**风险**：将来会出现"登记说交了、闸说这份根本不该出"的**两套口径** | **不用你给什么**（技术账）：**阶段 5 建"数据交付边"时把这两处接上** —— 交付前先问一次那道闸，口径只有一处 | 不定 ⇒ 两条口径各说各话（而这正是这个项目最忌的形状）|
| **B25** | **声明里有三条口，实际只有一条在跑** | `outbound.js` 声明了 `OUTBOUND_ROUTES = {publish, artifact, deliver}`，但 `src/` 里**只有 `publish` 真有调用方**；`artifact` 与 `deliver` 两条 route **一个调用点都没有** ⇒ "三条口共用一个裁决"今天实际上只有一条口在跑，另外两条**没有任何结构性东西**保证将来有人会调它 | **不用你给什么**：阶段 5／6 接线时把这两条补上，并加一条"声明了口就必须有人调"的判据 | 不定 ⇒ 将来有人新开一条出界路时，不会经过那道闸（判据只扫共享库那两个目录名）|
| **B26** | ⚠️ **`delivery/*` 这些记录会原样进客户端** | 它们经 `/api/timeline` 与 `/api/export` 给到客户端（`planBackfill` 只按 seq 筛、**不按类型白名单**），记录里带 `packId`／`shapeVersion`／人格 key。今天的 Flutter 客户端对未知类型**安静忽略**（`timeline.dart` 的 `default: return`）⇒ 不炸；但**冻协议／将来第三方客户端**要把这条算进去 | **要你一句话**（**冻协议那天**）：这些内部登记记录**要不要上屏**（我建议：`/api/timeline` 继续给、但**客户端不渲染**，并在冻协议时写清"未知类型安静忽略"是**约定**而不是碰巧）| 不定 ⇒ 将来谁渲染未知帧，就会把内部 key 画到界面上（`06` 禁用词那条的同族病）|
| **B21** | **`outbound` 这个字段住哪、谁承重 —— 契约层没裁过（实现阶段 3 时撞出来的）** | `92` §② 第 2 步讲**包级**一个 `outbound`（`share`/`one-to-one`/`never`）说了算，而 `91` §3.2／§7.3.1（**已落地**）里 `pack.json` 只有 `items[].share`（**逐条**）—— 两边**没有一处裁过优先级**。今天按 `98` **§三·补** 那条口径落地了（包级有值就它说了算；没写退回逐条；**有值但认不出 ⇒ 拒**，不许当"没写"）| **要你一句话**（**阶段 6 升手册之前**必须拍）：字段名与形状就这么冻（包级 `outbound` 三值 ＋ 逐条 `share` 收窄）——**还是**只留逐条、把包级那个撤掉？我建议**冻现在这条**（它可反着验，且与 `92` §② 对得上）| 不定 ⇒ `one-to-one`／`never` 一旦真被写出来，形状就**冻在两可之间**（两边都"像"契约）|
| **B22** | **"声明承重"这一侧还没有生产者** | 全 `src/` **没有任何一处写 `pack.json`**（`PACK_FILENAME` 只出现在读侧）⇒ 阶段 3 的闸是完整的，但这套形状今天**只被测试冻住、没有真数据**（模型／服务端都产不出 `outbound`）| **不用你给什么**（技术账）：阶段 4 建登记面之前先确认字段名与形状（与 B21 是同一件事的两面）| 不定 ⇒ 阶段 4 可能照着一个"没人写过的形状"建界面 |
| **B23** | **同一个词两义（轻）** | 制品的外联申报文件叫 `outbound.json`（A16，值是**数组**），而 `pack.json` 的 `outbound` 是**字符串枚举** —— 两个文件不冲突，但冻协议时值得留意 | **不用你给什么**：冻协议时顺手把名字分开（例如 `outbound.json` 保持，包级那个叫 `outboundLaw`）| 不定 ⇒ 将来 grep `outbound` 会同时命中两个意思 |
| **~~B19~~** | ✅ **已解除**（2026-09-25 当天做完并上线）—— **P1 §三④「他随时能问'那件怎么样了'」那条出口补上了**。原描述：判据在、话也在，但**没有出口**（`workReport()` 零调用方 ⇒ 用户从界面问不出逐件答案，真机问的时候回的是**模型自己那句话**）| 做法（**只动产品层**，**没碰能力层** ⇒ 不用主人补重建清单）：在**账本**那条 MCP 上加第 5 个**只读**工具 `work_status`（`op:'work'`；为什么住这一支见 `31-LEDGER.md` §7.3 那条注）＋ 服务端 `handleLedgerOp` 的 `work` 分支 ＋ `Dispatcher.workList()`；`worlds.js` 把调度器**惰性**交给那条通道（造通道时它还没有）；`agent-runtime` 多给一个**非秘密**的 `HUPO_SCOPE`（"我这一轮在哪一间里跑的"）| 🔴 **真机第一版就答错**：他问"那件怎么样了"，回来的是**"那件还在做。（就在这儿）"** —— 而那件**就是这句提问本身**（提问那一轮也在账上）。⇒ 修法：`workList({excludeScope})` 把**正在问的那一间正在跑的那一轮**排掉（`isTurnOpen` ＋ `openTurnRefs` 两档，`waiting`/`running` 都排）；修完真机变成 **"现在没有挂着的活。"** ✅。⚠️ **另一个假绿也记下来**：我第一次拿 `/api/harness` 验这条 —— 那条路是**裸 DSH**（`harness-session.mjs` 只给 `--profile sdk` ＋ 模型 patch，**不给人格/能力层**）⇒ 它**本来就没有**琥珀那些工具，探针里的两条 ✅ 是**正则匹到我自己发的那句话**（假绿）。**正确的仪器是 `/api/stream` ＋ `/api/say` 那条真 agent 路**。**判据**：`test/ledger-chain.test.js`（5 条工具 · 服务端原话转述 · `HUPO_SCOPE` 原样带到 · 没接上 ⇒ fail-closed · 没有活 ⇒ 如实说）＋ `test/backend-work.test.js` T4④c（真 `Worlds`＋真调度器：别的房间的活照报、**问话那一轮的活排掉**、查无此件说实话、不接活账 fail-closed）；**变异**：把"没接上"改成回"没有挂着的活" ⇒ 那条判据当场红。**真机**：真 agent 的**工具清单里就有 `mcp__ledger__work_status`**（39 个工具里点名验过）＋ 两次真实回答（改前"那件还在做。（就在这儿）" ⇒ 改后"现在没有挂着的活。"）。产品层 `ab151faa3dc3 → f8e98e94fac4 → **66f493206ccc**`|
| **B20** | **盒子里的 app，房间"在哪看"那半句取不到名字（小账）** | P1 完成提醒 ② 里那半句「（在「看天气」里）」现在**对主人那一侧修好了**（原来写成 `current(id)?.title` —— `current()` 给的是**版本号** ⇒ **恒 `null`**，那半句从来没出来过；已改成从那一版 `manifest.json` 取，带 T4②b 反例）。⚠️ **租户那半仍是 `null`**：盒里那份库要**过隧道、而且是异步**，而 `roomFor()` 是**同步**的 ⇒ 这一层够不着。后果**不是假话**（认不出就**不带那半句**），只是少半句 | **不用你给什么**（技术账）。要修就得让"名字"变成**惰性的**（报的时候再去问盒子那份）；排在别的前面 | 不定 ⇒ 盒里做完的活，提醒里只有"做完了"，**不说在哪一间** |
| **~~B17~~** | ✅ **已解除**（本轮：宿主预闸改成**经现有隧道请盒子自己判**）—— 原描述：**B15 的尾巴：`/api/app-ask`（"小程序问一句"）的宿主预闸仍读宿主那份库** | 改法：新内部口 `/internal/app-ask-check`（**只在 0600 可信 UDS 上**）＋ `createBoxApps.gateAsk()` ＋ 宿主 `gateAskFor()`；**盒里跑的是同一个 `checkAppAsk()`**（四道闸 ＋ **先记再花**，所以不可能两边分叉）；主人 / 单租户**逐字不变**；盒子不通 / 答的话认不出 ⇒ **如实 503，绝不退回宿主那份**（那正是 B17 要修的那句假话）。判据 `test/app-ask-box.test.js` **7 条**（真起宿主＋盒子两个服务、真 `net.connect`）；**变异验证**：把 `gateAskFor` 改回读宿主那份 ⇒ **7 条里 6 条红** | ——（已完成：**真机 16/16**，见 `00-PROGRESS.md`） |
| ~~**B15**~~ | ✅ **已解除**（`#122`：以盒子为准，已上线真机验过）—— 原描述：**`#121` 收尾时撞出来的真 bug：小程序有"两处库"** | **桌面读的是宿主那份，助手写的是盒子里那份** ⇒ **他现在做的小程序不会出现在桌面上**（他看到的「看天气」「问答小抄」是 9-22/9-23 在宿主上做的旧的）。实测：宿主 `data/users/u2/hupo/apps/` = `tianqi-probe` ＋ `wenda`；盒里 `/data/hupo/apps/` = `city-weather`(v1) ＋ `dice`(v1,2,3 —— 反漂判据真跑出来的)。⚠️ 同一个病在**对话**那一侧已经修好（分家是真机验过的），**只有 app 的库还分着** | 你说一句：**以哪边为准**。我建议 **盒子里那份为准**（他一直的规矩是"数据在他自己的盒子里、盒子只出不进"）⇒ 改法两条：① `/api/apps` 对租户**走隧道进盒子**读它自己那份；② 制品那个口（`apps.stalkerai.cn/a/…`）也要**按人转进他的盒子**取字节；③ 宿主那份存量（看天气/问答小抄）**迁进盒子**（照 `city-weather` 那次的做法）| 不定 ⇒ **他新做的小程序永远不上桌面**（助手说"做好了"，他一刷新什么都没有 —— 页面在说假话）|
| ~~**B16**~~ | ✅ **已解除**（`#122`：四个内置 id 已成保留房间）—— 原描述：**内置磁贴要不要也算"房间"** | 只有 `/api/apps` 里的"我的小程序"有房间；**内置的四个**（设置 / 奥数题 / 发现 /「我自己那台」）不在 `/api/apps` 里、服务端没有它们的 id ⇒ 那期间聊天仍落主线。**我先按"它们是界面、不是他做的 app"定了** | 你说一句：**保持现在这样**（默认）还是**每个图标都要分家**（那要先把内置那几个也登记成 app，服务端加 id，客户端改一行 + 判据）| 不定 ⇒ 在内置那几屏里说的话算作主对话（界面自己的图标说明仍按各屏显示 —— 这是已有的、`chat_scope_icon_test` 钉着的行为）|
| **B14** | **`#118`「我自己那台」—— 你自己那个号走不到这条路**（`#119` 之后有了一条更好的路，但这条**仍然成立**）| 那条 WS（App 里的终端）**只服务"有盒子的人"**：`owner`（你登录用的那个号）是 **`local`、没有容器**，公网口按 H1（**判据反例**）一律拒 ⇒ 你在自己账号上点开那个磁贴会看到「它这边停下了」。**这不是坏了，是那道闸在按规矩工作**。⇒ ✅ **`#119` 给了另一条路：浏览器那条（`dsh19145526557.stalkerai.cn`）已经能用** —— 用 **`19145526557` 那个号**登录 App ⇒ 打开「我自己那台」⇒「**在浏览器里打开**」。| 你说一句：**你自己要不要用 `owner` 这个号就能进**（那要给 `owner` 也开一台盒子，或者让界面上允许你点名某个开发者用户的入口 —— 后者要改一点客户端与一条产品决定）| 不定 ⇒ 你在**自己账号**上点那个磁贴**只会看到"停下了"**（界面说的是实话，但你会以为坏了）；要看那台 DSH 就**用有盒子的号** |
| ~~B1~~ | ✅ **已解除**（主人 2026-09-24 那句原话）| 形状定了：**四个 tab ＝ 聊天 / 语音 / 图片 / 视频**（界面上不写「模型」——它是禁用词）。见 `79-CREDS-TABS.md` | ——（已完成：两闸绿 + 已部署 + 线上取证）|
| ~~B2~~ | ✅ **已解除**（同上那句话）：`/api/space` 现在回 `creds:{model,voice,image,video}`（**只有有没有，永不回值**），新 `POST /api/creds` 一次写一屏 | ——（做完） | 见 `79-CREDS-TABS.md` §四 |
| B3 | **P0-7** 开机清单漏 `src/creds.mjs` | ✅ **已顺带解决**：反做那批越界产物后重建了清单，现在「✅ 对上了」，漏 **0** 条 | ——（无需动手） | 已解除 |
| B4 | **P2 十条**（见 `76-PLAN.md` §四） | 每条都要你拍板 | 逐条拍 | 见计划里每条写的"不拍的后果" |
| **B6** | **仓里有别的会话在动**（要主人确认） | 手册 `CHANGELOG.md` 顶上有一条 **v1.68（2026-09-24）「聊天窗口收成一行：上边框正中一条杠 + 平的小箭头 · homeicon 靠左 · 话筒进框 · 发送常驻（灰）」**，带一段主人的原话 —— **那不是我这段对话里的**；而**工作树里没有这个形状**。另外那次「越界提交」自己的说明里写着「客户端那半源码**被别的会话抹掉**，待重做」。⇒ ⚠️ **我之前说「编造主人原话」可能下重了**：那句话**可能来自另一个会话**（同一个主人在别的窗口交代过）。**要主人确认**：v1.68 那个形状要不要做？如果别的会话正在做，我这边**不要动输入条那一块**（免得互相抹） | 说一句「v1.68 做/不做」；以及「还有没有别的窗口在改这个仓」 | 不定 ⇒ 我可能把别的会话的半成品当成越界产物处理（**已经发生过一次**） |
| B5 | **轮换那对贴进过聊天的密钥**（计划 §五） | 只有你能做 | 作废重发 + 填 `data/asr.env` | 那把钥匙**已经在漏**（进过会话日志） |
| ~~B12~~ | ✅ **已解除**（主人 2026-09-24：*"图片需要打通，视频不需要。"*）⇒ 图片**当天打通**（见下面那行 ✅）；**视频砍掉**（P1-28 不再做，页面明说） | ——（已完成） | 见 `79` §9.5 |
| ~~旧 B12 备查~~ | **"我说一句，它画给我"要不要做？（要你一句话）** | 图片那条路今天已经通了（配置页「图片」那一屏的「试一张」真能画）—— 还差**和聊天打通**：你说一句"给我画个…"，助手就去画。⚠️ 那要**一条工具 ＋ 一条闸**：**"他明说才许生成"**（钥匙是你的、钱也是你的 ⇒ 助手自己想到的不许花 —— 与小程序那条"他明说才许写"同一个道理）。⚠️ 顺带要你定：画出来的图**放在聊天里**（要新做"气泡里显示图"那一块）还是**只给个地址** | 你说一句："做"／"先不做"；做的话顺便说清**图放哪** | 不定 ⇒ 图片这一路**只有配置页那个「试一张」**（能用，但不在聊天里） |
| **B13** | **D5.10 那条硬闸只差"你的声音"（＋一个口径要你确认）** | 语料 **80 条**（四类各 20）与**装置**都做好了（`test/corpus/voice-4x20.json` · `src/voice-corpus.js` · `scripts/check-voice-corpus.mjs`；装置**真跑过**：静音喂进去 ⇒ 0/3 ＋ 如实标"不齐"）。⚠️ **音频必须你自己念**（16k 单声道 PCM16 的 wav，按 `001`.wav…`080`.wav 放一个目录里）—— 本机 TTS **不可用**（espeak 没有中文词典、也没有 ffmpeg/sox 重采样）⇒ **不许拿合成音顶**。⚠️ **顺带一个口径要你一句话**：手册那句"**四类问题各 20 条，句子级命中率 <90% 不上线**"——是**总命中率**（80 条里对 ≥72 条）还是**每一类都要 ≥90%**？我按**总**算的（更宽松），但**分类照报**、低于门槛的点名 | 你定：① 你什么时候方便念那 80 句（我可以先只录**一类 20 句**试节奏）② 口径按"总"还是"每类都要" | 不定 ⇒ 这条硬闸**没有读数**（装置在，但没声音）—— 语音这条路就永远只是"能听见"，谈不上"听得准" |
| **B11** | **图片 / 视频用哪一家？（要你一句话）** | 配置页那两屏现在**只是收着**（页面自己这么说）。要真接上，第一步是定**用哪一家**：图片生成、视频生成各是一家（各家接口/鉴权/计费都不一样），以及**要不要走我们这边中转**（像语音那样由服务端替他去调）。⚠️ 我**没有**任何依据替你选：越界那批代码里写的"seedream / seedance"是**那个子 agent 编的**（它连你的原话都编过）⇒ **一个字都不能当依据** | 你说一句："图片用 X、视频用 Y"（或者"先不做"）；顺带说清**先做哪个** | 不定 ⇒ 那两屏**永远只是收着**（页面会一直如实说"这条路还没接上"） |
| **B10** | 🚧 **只剩"重新发布产品层"这一步了**（2026-09-27 更新 · `#174`）。**这一批查实并修掉两处真缺陷**（详见 [`125-TENANT-VOICE-CREDS.md`](125-TENANT-VOICE-CREDS.md)）：**① 推送的触发** —— 修之前只有"这一包里带 `model`"那一拍才推，而配置页是**一屏一次提交** ⇒ 在「语音」那一屏填的三样**从来没送进盒子**（中心存下了、页面说"有"）。已修＋**已部署**（`setCreds` 任何一次改动都推整包；通道在盒子每次连上来时补送一份）。**② 盒子里的读法** —— `asr-creds.js` / `image-use.js` 调 `credsFor` 时**没把 `env` 传下去**，而"盒子里读那份单文件"那一支只在 `HUPO_ROLE=tenant` 时才走 ⇒ **那条兜底路一次都没执行过**（文件里明明有、识别路照样回「没配凭据」）。已修＋判据补在**产品那一侧**（反做就红）——🔴 **但盒子跑的是 2026-09-26 发布的那一版，要重新发布产品层才生效**。⚠️ **重新发布 = 动两台活着的盒子** ⇒ 按 §8.1 要你点头、我当场盯着做（`scripts/build-tenant-code.sh` ＋ `--publish`）。**原描述**：**租户那半"按人一份的语音钥匙"** | 主人那一份**早就接通**（`凭据来源 his-own`，`123` §九 真浏览器验过）。租户这一半：①✅ 已修已部署 ②🚧 修好了但没进盒子。⇒ **今天租户仍然"填了也用不上"**，页面那句「先收着。你这台还没接上……」**现在还是实话**（所以这一批**没改**那句客户端文案） | 你定：**现在发布产品层**（我出补丁、你签字；发布完你在 `u2` 那屏把三样填上就能录）还是**先记着** | 不定 ⇒ 租户那台的语音/画图一直停在"填了用不上"（页面**自己这么说**，不是假话，但功能缺一半） |

| **B9** | **P1-23 / P1-24 只能等你**（不是我不做，是**计划自己的依赖图**把它们排在后面） | `76-PLAN.md` §八 写着 **P2-1（语音本体走哪条路：云端 / 本地模型 / 先做安卓包）→ P1-25 → P1-24 → P1-23**。⇒ ① **P1-23**（手机壳原生录音 ＋ `RECORD_AUDIO`）要真做就得引一个原生录音依赖、写一条"16k 单声道 PCM 送给 `/api/asr`"的路，而**验它必须在你的手机上**（这个仓库的规矩：判据打在真那一侧 —— 没有设备就只能写"没验过"的代码，那就是**做一半**）；② **P1-24**（D5.10 那条硬闸：四类各 20 条语料、句子级命中率 ≥90%）要**真的开麦喂语料**，还得先有能用的引擎（`Hy-ASR-3.0-preview` 现在是 `code=4004 资源包耗尽`）。⇒ 两件都**先等你一句话** | 你定 **P2-1**（走哪条路）。⚠️ 与它绑在一起的还有：混元那个资源包**开通没有**（决定引擎换不换）＋ 你什么时候能用手机试一次 | 不定 ⇒ 手机壳里**永远开不了麦**（会一直如实说"这里开不了麦" —— 那句话是**真的**，不是坏了） |
| **B8** | 🚧 **机制都在，规则形状你这次没点名** —— 按**保守默认**继续（宁可误拒），要改随时一句话。⚠️ 它现在管**两处**：**写小程序**（`apps-consent.js`）与**画图**（`image.js` 的 `asksToDrawImage`）。**原描述**：**「什么算'明说'」这条产品规则要你拍**（P1-22 的**后半**，机制已落地） | 「他明说才许写」那道闸现在是**硬的**了（服务端写盘前自己看当轮你那句话，见 `59-USER-APPS.md` §八·补）。**但"什么算明说"是我替你先定的**：认得出「帮我做一个…/给我做个…/我想做一个小程序」这类**要东西的说法**（`ask` ＋ `make` 同时出现）才放行；光有「帮我」「做」不算（"帮我查一下天气"、"我今天做了一个梦"都拒）。⚠️ 我取的是**宁可误拒**：认不出就拒、并告诉你该怎么说 —— 代价是**偶尔要你多说一句** | 你定：① 就按这个（**宁可他多说一句，也不许他自己造**）；② 放宽（比如"看到页面里写着可以做一个"也算）；③ 收紧（比如必须**点名**"小程序"三个字才算）。⚠️ 还有一条口径要你定：**"改"（换个颜色、加个按钮）算不算"明说要做"** —— 现在这一版**不算**（现在也没有 `app_update`，见 §八 第 2 条） | 不定 ⇒ 我这版默认会**误拒一些正常说法**（你会看到它回一句"得你亲口说一句'帮我做一个……'"，**不是坏了**，按它说的说一遍就成） |

## 已做完的（这一批）

| ✅ | **P1-26**（**做得到的这一半**）语音凭据"接不到识别路" | ⚠️ 先说清**前提变了**：计划里写的 `VOICE_FIELDS`／`creds.mjs` **已经在反做越界那批时一起没了**（`df8b732`）⇒ 它的"**按人一份**"那一半**做不了**（形状正是你要拍的 **B1/B2**：配置页那几个 tab 各是什么、`/api/space` 回什么）。⇒ 我做了**不依赖那个决定**的那一半：① 中继的凭据从"**开机读一次**"改成"**按连接现取**"（`asr.js` 的 `config` 可以是函数）；② 新 `src/asr-creds.js`：**`data/asr.env` 每次现读** ＋ 身份（`sub`，**从验过签的 `claim` 来**）传进中继 ⇒ **"以后按人取"只差那个缝里的一处实现**；③ 开机横幅**如实加一行**"语音凭据（来源 · 引擎）：APPID 10 位 · …"（**只有长度与来源，一个字符的钥匙都没有**） | `test/asr-creds.test.js` **7 条**（文件解析含引号/注释/坏行 · 🔴 **同一个进程里再取一次就读到新钥匙 ＝ 轮换不用重启** · 没配要**说得出为什么** · 读不出来（EACCES）也要说得清 · 🔴 **日志那句一个字符的钥匙都不带**（带"真的读进去了"的负向对照）· 按人取**今天只能写 `default`**、不许假装是他的 · 中继**按连接各取一份**）。服务端 **795 全过**。🔴 **判据当场抓到一个真缺陷**：`claim` 住在别的块里 ⇒ `onAsr` 里引用它 **`ReferenceError`** —— 那会让**每一条语音连接**在线上炸（9 条判据同时红）。**线上实测**：`语音凭据（来源 file · 引擎 16k_zh）` ＋ 真连 `/api/asr` 拿到 `{"type":"asr/ready","engine":"16k_zh"}`（钥匙不对这一步过不去）⇒ 文件里那份凭据**真的接到了识别路**。⚠️ **还剩下的**：按人一份 ⇒ 等你拍 B1/B2。⚠️ **顺带**：B5 那把要轮换的钥匙，现在**改完文件就生效、不用重启** |
| ✅ | **P1-15** 开机清单**身份耦合**那一处 ＋ 两条"覆盖缺口" | 三件：① `serve.js` 原来自己写 `os.homedir()` —— 用 `sudo` 起服务时那是 `/root`，而清单里几条 `~/.dsh/**` 是**按仓库属主**拼的绝对路径 ⇒ 逐条 hash **照样全绿**，可 `coverageGaps()` 问的那句"盘上有没有东西"在新 home 底下空空 ⇒ **一条漏都不报**（＝"写着有闸、其实没核"）。现在身份**只住 `resolveServiceHome()` 一处**，启动横幅**如实报"按谁的 home 算"**；② 清单里**记下**它是按谁的 home 建的，对不上就在启动时**大声说**（横幅那一行同时改口径：不一致时**不再写"对上了"**）；③ `~/.dsh/storages/**` 与 `data/ledger.jsonl` 那两条**接受的风险**：代码里一直写着，**文档表里只有 storages** ⇒ 给账本补一行（`19-P1-P2.md` §七 第 7 条） | `test/integrity.test.js` **+7**（记 home · 换 home ⇒ 大声说 · 同 home 不许响 · **老清单不算不一致** ＋ 措辞不许吹 · `serve.js` 不许再出现 `homedir()` · **清单里确实没有 storages/账本**（带"该在的确实在"的负向对照）· **两边文档都得写着这两条风险**）。**变异验证**：把 `home` 从清单里拿掉 ⇒ 两条当场红。服务端 **788 全过**。线上横幅实测：`开机清单按 /home/deploy 算（⚠️ 这份老清单里没记…）` |
| ✅ | **P1-27 后半：图片和聊天打通**（主人 2026-09-24：*"图片需要打通"*） | ① **工具**：`image_generate`（⚠️ **借住**在小程序那条 MCP 上 —— 能力层是 **strict**，新开一条要主人重建清单 ⇒ 先借住，下次重建时再拆出去）；② 🔴 **闸**"**他明说才许生成**"：服务端看**当轮他那句话**（`asksToDrawImage` ＋ `dispatcher.turnInput`，与"明说才许写"同一个道理）——**没有"他那句话"⇒ 一定拒**；③ 通道上加 `draw` op（`handleAppsOp` 变 async）；④ **界面上**：回话里带图片地址 ⇒ **画在气泡里**（`imageUrlsIn` 纯函数：后缀/`format=`/`data:image` 才认，**认不出一个字都不动**；整行地址不重复显示、句子里的留着；取不到图就说实话） | 服务端 **+9**（工具清单十件 · `draw` 六种情形 · **拒了就不许花** · 闸真值表）· 客户端 **+6**（认地址四情形 · 气泡真画出来 ＋ 负向对照）· 服务端 **819 全过** · 客户端硬闸 **全过**。**线上实测**（真域套接字 ＋ 真 MCP 子进程）：`tools/list` 里有 `image_generate`；**没人说话时调它当场被拒**（"画一张是**花你的钱**的事…"）⇒ **一分钱没花**。⚠️ **没验到**：真画一张要主人那把 ARK key。⚠️ **视频砍掉**（他 2026-09-24 说*"视频不需要"*）⇒ P1-28 不做，页面明说 |
| ✅ | **P1-27 前半：图片生成那条路接通**（seedream · 火山方舟） | 配置页「图片」那一屏多了**「试一张」**：写一句 → **真打火山**（用**他自己**那把 `HUPO_IMAGE_KEY`）→ **真把图画在屏幕上**。服务端新 `src/image.js`（建请求/解响应**纯函数** ＋ 注入 `fetch` 的调用）＋ 路由 `POST /api/image`（**钥匙不进回执不进日志**；每种不行分开说：没填 409 / 空话 400 / 钥匙不灵 502 / 连不上 / 画太久 / 它没给图）。⚠️ **接口正文取自第三方速查**（官方那两页是前端渲染、抓不到）⇒ 端点与模型名**都能配**（`HUPO_IMAGE_URL/MODEL/SIZE`），并且**真打一次以回话为准** | `test/image.test.js` **7**（纯函数 · **回执没有钥匙** · 四种失败分开 · 人话互不相同且过内部词表 · 路由 404/400/409/200 · 上游说钥匙不灵⇒502 带原因）· `test/widget/image_try_test.dart` **6**（🔴 **失败那句话原样显示**、空输入**一次都不发**、成了**地址就是那个**、没填钥匙/没接线⇒不画那一块、命中区≥44）· **硬闸**：配置页·图片那一屏（含试一张）**五档不溢出**加进 `accessibility_test.dart`（181→**186**）· 服务端 **817 全过** · 客户端硬闸 **全过**。**线上实测**（指纹 `ad83e17bb03f → d35b5f337d6b`）：假钥匙真打火山 ⇒ `AuthenticationError: The API key format is incorrect`（**端点与 Bearer 形状都对**）；线上真路没填钥匙 ⇒ **409** ＋「还没填画图那一把钥匙」；空话 ⇒ **400**。⚠️ **真画出一张图**要主人自己那把 ARK key（填完按「试一张」）。⚠️ **与聊天打通**见 **B12** |
| ✅ | **P1-26 后半：语音那三样真的接到识别路**（2026-09-24，紧接配置页那一批） | ① `voiceCredsFor` **优先读他自己那份**（`data/creds/<他>.yaml` 的 `HUPO_VOICE_*`，**三样齐了才算**——与页面回报**同一条规则**），没有才退回部署默认（`data/asr.env` 现读）；② 中继日志那一行带上**来源**（`his-own` / `default`，**不是秘密**只是"从哪来"）；③ 界面那句边界话**跟着事实变**（`credVoiceBoundary`：本机＋填了＝"以后就用这三样"、本机＋没填＝"用的是机器上配好的那份"、租户一律"你这台还没接上"）；④ 用他自己的钥匙时**不再走"取证中转"**（`HUPO_ASR_URL` 是给没钥匙时验链路用的） | `test/asr-creds.test.js` **+3**（他自己的优先 · **别人那份不许用在他身上** · 三样缺一退回默认 · 不许指向中转）· `space_test.dart` **+1**（四种组合逐条对表 ＋ 四句互不相同）· `settings_test.dart` **+1**（界面上四种组合）· 服务端 **810 全过** · 客户端硬闸 **全过**。**线上实测**：把 `asr.env` 里那三样同样一份写进他自己的存档之后，真连 `/api/asr` ⇒ `asr/ready` ＋ 日志 **`凭据来源 his-own`**；`/api/space` ⇒ `creds.voice:true`。⚠️ **租户那半**见 **B10**。⚠️ 两个小留痕：① 服务端全量有一次**没复现的红**（`sources.test.js` 那条"工具名不进 message/end"；随后单文件 5 次 + 全量 3 次全绿）—— 记着，再犯就当阻塞项查；② **取证工具连点两次**时第二次点击没生效（截图逐字节相同）⇒ 这一屏各 tab 的内容判据走 widget 那一侧 |
| ✅ | **P0-2 ＋ P0-3 ＋ P1-1 配置页那四样**（主人 2026-09-24 定的形状） | 一句话：**四个 tab ＝ 聊天 / 语音 / 图片 / 视频**（"模型"是界面禁用词 ⇒ 用"它管什么"来说）。① **界面**：`TabBar`（字最大时横向可滚）＋ 每屏自成一个可滚列；每屏四件（管什么 → 有没有 → **边界句** → 填的）；聊天那屏**还是老表单**（粘贴 + 取消注册），其余三屏用新 `CredForm`（图片/视频一串、**语音三样一次写完**）；「关于/退出登录」挂第一屏可滚内容底下（固定页脚在 3.1 倍字下会挤爆 —— 硬闸当场判红）。② **服务端**：新 `POST /api/creds`（一次写一屏）；老 `POST /api/model-key` **一字未动**（协议冻结）；`/api/space` 多回 `creds:{model,voice,image,video}`。③ 🔴 **钥匙存到哪**：主人那一把**写进 DSH 自己那份凭据**（他 2026-09-24 点头选的那一档；只换 `refs.DEEPSEEK_API_KEY` 一行、**`records` 逐字节不动**、先备份、写完自核、核不过还原）；租户还是老路（推盒子）；其余三样进**中心按人存档** `data/creds/<id>.yaml`（0600、原子写；不进盒子是因为盒子那边**整份重写**会抹掉）。④ **顺手修掉一处假话**：`/api/space` 对主人原来只回 `{kind:'local'}` ⇒ 他那屏只能显示"还没有填"，**而他其实有一把能用的钥匙** | `test/creds.test.js`（新 **12**，含**顺序陷阱**、`sub` 洗路径、主人那份的外科式写入与还原、`/api/creds` **回执不回显值**）· `test/widget/settings_test.dart`（重写 **11**：四屏都在/顺序/边界句/语音三样/**一次提交写整屏**/localOnly 能填）· `space_test.dart` **+1**（四样宽容解析）＋新文案过禁用词表 · 客户端硬闸 **全过**（可访问性 181）· 服务端 **807 全过**。**线上实测**（指纹 `2cb767664cf8 → b9cbd1f1bba0`）：真截图看得到四个 tab；`/api/space` 实测 `hasKey:true · creds:{model:true,voice:false,image:false,video:false}`。⚠️ **还没做**：图片/视频那两条路没接、语音三样也还没接到识别路（下一步）、其余三样不进盒子（要改产品层，要你签字）—— 页面**自己这么说**（边界句），见 `79` §八 |
| ✅ | **P1-22** 「他明说才许写」原来**只有软的**两道（工具说明 + 事后审计）—— **而说明是给模型看的**：模型被绕进去就照做，那两道一道都拦不住 | 服务端**写盘之前自己看一遍"这一轮他到底说了什么"**：① `dispatcher` 记**当轮输入**（`turnInput` = **引起来这一轮的那句话**，`turn-start` 时从那一轮的票上认领 —— **不是"最后投递的那句"**：他半路又补一句属于下一轮，不该顶掉正在跑的这一轮；turn-end / 超时 / 失败**三处都清**）；② `worlds` 用**取值函数**递给小程序那条通道（调度器建在通道**后面**，只能这么接）；③ 新 `src/apps-consent.js`：纯规则 `asksToMakeApp()`（**ask ＋ make 同时出现**才算"他要我做"）＋ 一句人话 `NEEDS_ASK`。🔴 **判据只读服务端自己记的那句话** —— 请求里塞 `turnInput`/`said`/`consent` **一个字段都不看**（看请求＝把闸交给要过闸的人）；拒了**盘上一点东西都不许有**；**没有"他那句话"⇒ 一律不许造**（助手自己发起的那一轮、开机那几秒都在这档） | `test/apps-consent.test.js` **6 条**（真值表 7 正 9 负 · fail-closed · 真链路拒/成 · **伪造"他明说了"不作数** · **真 `Worlds` ＋ 真域套接字的端到端接线判据**）。**变异验证两次**：① 把接线写死成一句"帮我做一个小程序"；② 把"跟着这一轮的票走"改回"最后投递的那句" ⇒ 两次都**只有那一条红**、其余 5 条不受影响。服务端 **781 全过**（+6）。⚠️ 剩下要主人拍的见 **B8** |
| ✅ | **P1-18 / P1-19 后半** 形状类过期文档 | 逐处回填**四处**（都属 `docs/dev/`，**不动 strict** ⇒ 不需重建）：① `73-MOTION-APP.md` 里「交接带 45%→60%」是旧口径 ⇒ 改成**全程按透明度交叉**（并注明 `_swapFrom/_swapTo` 已不存在）；② `64-CHAT-REDESIGN.md` 的「发送/听筒」那一行 ⇒ 按代码回填（**听的时候不画发送** · 话筒一直在 · 听筒那三个字早砍了）；③ `48-SETTINGS-KEY.md` 的「配置里只有钥匙+关于」⇒ 现在是**两个分区**（你那串钥匙 · 这个助手〔关于/退出登录〕），并注明四个 tab 那件事**还没定**；④ `71-MIC-ASR.md` 判据表的条数 ⇒ 按实测改成 **17 / 22** | 文档闸 ✅ |
| ✅ | **P1-17** "线上 ↔ 仓库"没有对表 | 新增 `docs/dev/78-OUT-OF-REPO.md`：把**不在仓库里的那几样**列清（VPS nginx · frp 隧道 · 开机清单 · `data/asr.env` · 租户表 · `~/.dsh/**`），每样写**住在哪 / 谁改 / 怎么验**；并把 VPS 上那份 nginx **照实抄进仓库** `deploy/nginx-w-stalkerai.conf`（含两处历史：`/api/asr` 那一行为什么要加 · 为什么不能用 http 级 `map`） | 文档闸 ✅。⚠️ 这一条计划里写的是"**助手提 ＋ 主人签**" ⇒ 材料已出，见本文件 B7 |
| 🔴 | **B7：nginx 抄本要不要成为"唯一事实源"？（要主人签）** | 我把 VPS 上那份 nginx **抄进了仓库**（`deploy/nginx-w-stalkerai.conf`），但它**仍是"那台机器的状态"**：真正的生效文件在 VPS 上，仓库这份只是抄本 ⇒ **两边可能漂**（而漂了没人会发现） | 你定：① 就按现在这样（抄本 + 改完手动同步）；② 还是要我做一条"**对表命令**"（`ssh` 读 VPS 那份、与仓库这份 diff，不一致就报红）？ | 不定 ⇒ 下次改 nginx 时，仓库这份会过期（而它是"唯一写下来的那份"） |
| ✅ | **P1-20** 闲置回收**从来没接过线** | `evictIfNeeded()` 原来**只有 `test/` 在调** ⇒ `maxProcesses` 那个上限**从来没生效过**（代理会一直攒）。现在接在**取用那一刻**（`agent()` 里新建实例之后）：池子只可能从那儿被突破 ⇒ 那才是该淘汰的时机。⚠️ 它自己的三条安全边界没动：**只淘汰空闲的** · **先收口再卸**（`onEvict` 回调）· **不 await**（`agent()` 是同步的，淘汰在后台跑） | 新判据 `test/agent.test.js`：上限 1 ⇒ 取第二个之后 `count<=1` 且**淘汰的是最久没用那个**（LRU）。服务端闸绿 |
| 🔴 | **P1-20 的另一半（要主人签）** | 现在只接了**代理进程池**那一层（进程级的，安全、可回退）。**容器级**的停/销毁策略（闲置的租户容器停掉/回收）**一个字都没动** —— 那会动到别人的数据面，属于部署期 ⇒ **要你签字** | 你定：**要不要做容器级闲置回收**？（不做就说"不做"，我把它从 P1 挪到"已砍"；要做就说清"闲置多久算闲置、停了之后怎么回来"） | 不定 ⇒ 代理池那层已经收敛（本轮做完），但**容器一直占着资源**（`#66/#67` 那两笔账还挂着） |
| ✅ | **待核三处（已核完）** | ① `01-PROJECT.md:300`「宽 >900 时…」—— 代码里**没有 900 那个断点**（`ConstrainedBox(maxWidth: 760)` 一律限宽）⇒ 已改成「一律限宽」；② `01-PROJECT.md:305`「甩要能关」—— **真的存在**：`chat_floater.dart` 里有 `flingVelocity=700` / `flingDistance=60` / `flingMaxMs=300` 与拖拽速度处理 ⇒ **那句不用改**（已核，属"仍成立"）；③ `68-SPEAK.md` —— 通页**没有**"网页上一定画得出来"那种话，只有两处提到 `canSpeak` ⇒ 补了一句新口径（**现在是真问浏览器有没有音色**）+ 指向判据 | 手册改了 `01-PROJECT.md`（strict）⇒ **重建了清单**、✅ 对上了；文档闸 ✅ |
| ✅ | **P1-25** `D5.2` 还写着「用手机自带的识别器」 | 改成**实际走的那条路**：经我们自己的服务端转给腾讯云（`/api/asr` → `wss://asr.cloud.tencent.com/…`），并把**代价照实写下来**（🔴 **声音出境到腾讯云** —— 当初反对的正是这一条；换来的是「网页上真能用」）；配套两条（钥匙只在服务端 · 没配钥匙如实说）写在同一行 | 手册口径已改 · **重建了清单**（strict）· 文档闸 ✅ |
| ✅ | **P1-19**（前半）`AGENTS.md` 里写死「50 个文件」 | 实测 **124 条 / 17 strict**（那句是写死的旧值）⇒ **换成指针**：条数不写在那儿，`--build` 的横幅会报（**本文件第一条纪律：不写数值**） | 文档闸 ✅ · 重建后 ✅ 对上了 |
| ✅ | **P1-16** 重建留痕只写了一条 | `00-PROGRESS.md` 新增 **§九·补4**：把 09-24 这几次重建**逐次**记成表（时间 / 为什么 / 动了哪几个 `strict` 文件 / 复核结果），并写清"为什么必须记"（strict 对不上 ⇒ 拒绝启动） | 文档闸 ✅ |
| ✅ | **P1-21** 逃生绳口径只在 `start-tunnels.sh` 里有 | `restart-core.sh` 顶上补同一句实话：**这个脚本不解决"开机自启"**（机器重启后服务不会自己回来，那要 systemd = #48 还没做）；两边口径现在一致 | `bash -n` 通过 · 文档闸 ✅ |
| ✅ | **P1-3** 说到一半断线，能"接着刚才那句说" | ① **服务端**收尾那条带上机器可读的 `reason`（`user-stop` / `upstream` / `engine` / `capped`）；② **客户端**据此分两种走法：**按停** ⇒ 正常收尾（切回键盘、字落框里）；**半路断了** ⇒ **字留着 + 说明白（`hearCutOff`）+ 不切走**（停在失败档 ⇒ 再按一下就是接着说）。🔴 **写判据当场抓到两个真缺陷**：(a) `broke('cut')` 原来 new 了个空 Hearing ⇒ **他刚说的话被丢掉**；(b) `asr/capped` 之后那条**迟到 1.5 秒**的 `asr/end` 收不下字（因为已经不在"听"这一档）⇒ 正是"最后一句被丢掉"那一族 | 客户端 unit **+4**（按停/断了/到点/断了但无字）· 服务端 **+1**（收尾必须带 reason） |
| ✅ | **P1-10** `stream.dart` 本体判据 | **全做完了**。① 退避提成纯函数 `models/retry.dart`；② **给 `StreamClient` 加了可注入的探针**（`probe`）⇒ 判 401 那条路：探针说 401 ⇒ 状态如实报 `unauthorized` 且**之后不许再出现"重连中"**（B1 那个"永远转圈"），正向对照（探针 OK ⇒ 该走退避重连）；③ **游标判据**：`streamUri(... sinceSeq: _sinceSeq)`（重连不许把游标写成 0）+ 收到帧要推进游标 + 退避不许改回写死的算式。⚠️ 写它时踩了两个小坑：局部变量名 `probe` **遮住字段**（Dart 报"先引用后声明"）· 还有一行 `probe == TokenProbe.ok` 没跟着改名（analyze 当场红） | `test/unit/stream_retry_test.dart` 3 条 + `retry_test.dart` 3 条，全过 |
| 🚧 | ~~**P1-10** 退避那一半~~（已完成，见上）：算式从 `stream.dart` 提成纯函数 `models/retry.dart` 的 `retrySeconds()`（原来是一句写死的 `(_attempt*2).clamp(1,8)`，没人判过）+ 3 条判据（逐档对表 · 永不为 0 · 单调不降 · 乱传参数不许炸）。**还没做**：401 那条（要给它一个可注入的探针）与**续传游标**（`sinceSeq` 的补发顺序）—— 那两条要动 `StreamClient` 的构造，留下一条清晰的接力点 | 无（不是卡点，是还没做到） | 不做的后果：401/游标仍只有集成测试覆盖 |
| ✅ | **P1-4** 手机壳里"画不画话筒"口径 | 文档 `71-MIC-ASR.md` 那一条**改成与代码一致**：话筒**照画**，点它出一句白话「这里开不了麦（换个浏览器打开就能用）」，**不进语音档也不装开麦**（原文写"界面上不画话筒"是旧口径）；判据在 `test/widget/hearing_test.dart` | 文档闸 ✅ |
| ✅ | **P1-7** 沙箱三属性**一条判据都没有** | 落源码级判据 `test/unit/mini_sandbox_test.dart`：`sandbox=allow-scripts`（**不给** `allow-same-origin`）· `referrerpolicy=no-referrer` · `allow=''`；**三条负向对照**（给了 allow-same-origin / 不设 sandbox / 放开 allow ⇒ 都必须抓住） | 2 条全过。⚠️ 写它时先被自己判红一次：**注释里**就写着"故意不给 allow-same-origin" ⇒ 先剥注释再扫 |
| ✅ | **P1-5** Z2「回到可见不许重建」原来只验了「没被销毁」 | 补强成**同一个 Element**：盖住 → 回到可见后，`MiniAppHost` 与小程序的 `Element` 必须 `identical`（重建 ⇒ 用户填了一半的东西会没） | `mini_app_test.dart` 新判据（2 个 identity 断言） |
| ✅ | **P1-6** 图标⇄内容交接只有纯函数判据 | 补**界面**判据：在那一层的 `Stack` 里读两个 `Opacity` —— 起点（图标 1 / 页面 0）· **半路两层都在且加起来 = 1** · 终点（0 / 1） | `mini_app_test.dart` 新判据（三点各若干断言）。⚠️ 写它时先判错了一次结构（图标层在 `ClipRRect` **外面**，是兄弟不是后代）⇒ 改成从外层 `Stack` 找 |
| ✅ | **P1-8** 写死尺寸棘轮只扫圆角 | 扩到**间距/尺寸**（`EdgeInsets.all/symmetric/fromLTRB/only` 与 `SizedBox(height/width)` 里的**数字字面量**），用**基线计数**（2026-09-24 实测 **161 处 / 25 个文件**）：每个文件**只许少、不许多**，总数也不许涨 | `design_tokens_test.dart` 两条（棘轮 + **负向对照**：`EdgeInsets.all(12)` 数得出、`d.gapM` 数不出）；**变异验证**：塞一处 `SizedBox(height: 9)` ⇒ 当场红，还原 ⇒ 5 条全过 |
| ✅ | **P1-12** `/api/health` 形状（手册六字段 vs 代码三字段） | 手册那一行**改成代码事实**（`ok`/`timelineId`/`seq`），并把原来那六项明标 **⏳ 从来没实现过**（`grep upstream\|cert` = 0）；手册升 **v1.69** + CHANGELOG 记一笔；**重建了开机清单**（手册是 strict） | 新判据 `test/health-shape.test.js`：**两边都读**（手册那一行 vs 代码那道分支），字段必须一致；提到那六项时必须带 ⏳（否则又成假话）。服务端 **773 全过** |
| ✅ | **P1-9** `/api/model-key` 一条测试都没有 | 补三条路：没开这条路 ⇒ **404** · 空值/非可打印字符 ⇒ **400** · 正常 ⇒ **200**，并断言**回执里不许回显钥匙**、身份只来自令牌（给 `boot()` 加了 `setModelKey` 注入口） | `test/server.test.js` 新判据（1 条，三断言） |
| ✅ | **P1-13** 字体指纹只有 shell 级验证 | 落 **Dart 级**判据 `test/unit/web_artifacts_test.dart`：① 字体清单里每个文件**都在盘上**且文件名带 **8 位内容指纹**；② `index.html` 指的入口**也在盘上**（就是那两次白屏的两条根因） | **变异验证过**：把清单里的字体名改成 `deadbeef` ⇒ **当场红**；还原 ⇒ 2 条全过 |
| ✅ | **P1-11** 公开面常量少一条（常量在说假话） | `PUBLIC_ROUTES` 补上 `/api/send-code`（它真实可达：那道分支在鉴权之前，登录前就要用它发码） | **运行期**判据：常量逐条对上（4 条）＋"不带令牌真的够得着"（`send-code` 不是 503）＋**负向对照**（`/api/health` 仍被 503 挡）。🔴 **顺手让新闸挡住了一次真部署**：那条「公开路由只有三个」的旧护栏当场红了 ⇒ 部署**中止**（线上仍是上一版）—— 这是 P0-8 那道新闸**在真路径上的第一次实战：红了真的上不去**。随后把那条护栏更新成四条（逐条写明为什么必须公开） |
| ✅ | **P1-14** 像文件的路径不许回 index.html | `serveStatic` 里加一条：带资源后缀（js/css/json/wasm/字体/图…）的路径**如实 404**，只有**页面路由**才走 SPA 兜底 | 新判据 3 断言：假入口(`/main.deadbeef0000.dart.js`) ⇒ **404** · 真资源 ⇒ **200**（负向对照）· 页面路由 ⇒ 200 HTML |
| ✅ | **P1-2** `canSpeak` 写死 `true` | `speech_web.dart` 改成**真的问浏览器有没有音色**（`getVoices().isNotEmpty`）；问不出来就当念不了 | `source` 级判据：不许再出现 `const bool canSpeak = true`、必须有 `getVoices`；speak 测试 **10 条全过** |
| ✅ | **P0-8** 部署脚本不跑硬闸 | 构建**之前**先替跑两道硬闸；红 ⇒ **一步都不往下走**（线上仍是上一版）。逃生口 `HUPO_SKIP_GATES=1`（会大字打在输出里，事后查得到）；`HUPO_GATE_CLIENT/SERVER` 给变异验证用 | **变异验证** `scripts/check-deploy-gate.sh`：把客户端闸换成 `false` ⇒ 非零退出 + 明说"不部署" + index.html/入口文件**一个字节没动**（4/4 过） |
| ✅ | **P0-1** 收尾那批的闸（上一轮一道都没跑） | 走**新的部署前闸**跑了两道 + 部署脚本自己的浏览器自检（真 Chrome：页面开、那条流通、字体取得到） | 客户端 **354/181/199** · 服务端 **769 全过** · 线上 `901e0b843bcb` · 浏览器自检 ✅ |

| # | 计划里的哪条 | 怎么做的 | 判据/读数 |
|---|---|---|---|
| ✅ | **P0-4** 禁用词 `'服务器'` 真上屏 | `chat_controller.dart` 两句改成「这边对不上了，正在重新同步」/「没收下：…」；**并加判据扫源码里那两句** | `test/unit/forbidden_words_test.dart` **5 条全过** · analyze 干净 |
| ✅ | **P0-5** 收窄「正在听」的豁免 | 词表那条从"一刀切禁"改成"**只在真的在录音时才可以**"（D5.13 原意），并写清唯一合法落点 | 新判据扫**整棵 lib/**：字符串字面量只许出现在 `hearing_words.dart`（连"词表自己"都显式排除）；6 条全过 |
| ✅ | **P0-6** 热表自相矛盾（#89 写 2/4，其实四块全做完） | `00-PROGRESS.md` 那一行改成实话 | 文档闸 ✅ |
