# 71 · 语音本体：**真开麦**（按一下说 · 再按一下停）

> **主人 2026-09-23 定案**（原话）：
> *"我们那个页面，并没有按钮。你做一下按钮。是按一下开始语音跟踪，实时转化语音成文字。
> 再按一下结束。然后将文字展示出来。用户可以选择发送。"*
>
> 这一份说清：**界面上长什么样 · 音频怎么走 · 判据打在哪 · 什么砍了 · 什么还欠**。
> 上游那条路是什么（腾讯云混元 ASR 内测版、签名怎么算）在
> [`69-ASR-ROUTES.md`](69-ASR-ROUTES.md) §四·补；"读出来"那一半（TTS）在
> [`68-SPEAK.md`](68-SPEAK.md)。**这一份只管"说给它听"。**

---

## 一、形状（屏幕上）

语音那一档（话筒图标切过去）现在是**一颗大按钮**：

| 状态 | 屏幕上 | 按下去会怎样 |
|---|---|---|
| 闲 | `按一下 说话` + 一句怎么用 | 开始听（会弹浏览器的**麦克风权限**框） |
| **在听** | `● 正在听`（红点 + 三个字）+ **大字实时文字**（最多 3 行）+ 按钮 `再按一下 结束` | 说"这边完了"，等最后一句回来 |
| **收尾中** | 实时文字区 + `收尾中…（把最后一句接住）` | 这一档**不画按钮**（按它没有意义） |
| 拿到字 | **自动切回键盘那一档**，字在输入框里 | 发送钮出现 —— **发不发由他**（D5.4：绝不自动发送） |
| **一句都没听到** | `什么都没听到，再按一次试试` | 按钮还在，**留在语音档**（不悄悄退回键盘） |
| **这里开不了麦** | 话筒**照画**；点它出一句 `这里开不了麦（换个浏览器打开就能用）` | 不进语音档、不开麦、不出假字 |
| 没配钥匙 | `语音这条路还没配好（缺钥匙）` | **不画按钮**（做不了的不许摆出来） |
| 没给权限 | `没拿到麦克风权限 —— 在地址栏那个锁里放开麦克风，再按一次` | 按钮还在（他能重试） |
| **这条连接没接上** | `这条路暂时连不上，等会儿再按一次` | 按钮还在 |
| 开不了麦 | `开不了麦克风，再按一次试试` | 按钮还在 |
| 识别那头出错 | `识别那一头出错了，再按一次试试` | 按钮还在 |

🔴 **"连不上"和"开不了麦"必须分开说**：2026-09-23 在线上真的踩到过 ——
公网那一跳（nginx）不认这条路径，而屏幕上写的是"开不了麦克风"（**页面在说假话**）。
⇒ 两句话各有各的出处，判据 `test/unit/hearing_session_test.dart` 里那条分开钉着。

🔴 **"按住"这个动作在浏览器里是坏的**（主人 2026-09-23 实测并点破）：
*"因为按住会触发默认的右键"* —— 手机上按住会弹系统/浏览器的长按菜单。
这一版本来就是"点一下开始 / 再点一下结束"，但**用户会照着微信的习惯按住**
⇒ 所以在**语音这一档里把长按菜单按住**（进出语音档时挂上/还回去，别影响别处的选择与复制）。
判据（真浏览器，`--eval` 派一个可取消的 `contextmenu`）：**不在听 = 不拦 · 在听 = 拦住 · 停完 = 还回去**。

⚠️ 三件**刻意**的事：

1. **字进输入框、不自动发送**。它停在框里，和用户自己打的字一样 ——
   这就是主人那句"用户可以选择发送"。
2. **开麦前框里已有的字留着**：识别结果**接在它们后面**（绝不擦掉他打了一半的字）。
3. 🔴 **"正在听"这三个字只在真的在听时出现**（D5.13 那条纪律的正面用法）。

### 砍掉的（明说，不是"排后"）

| 砍了 | 为什么 |
|---|---|
| `按住 说话` / `松手 发送` 那一套 | 主人要的是**按一下开始、再按一下结束**；"按住"那套语义（微信的）**删掉** |
| `演示` 小标 + `演示：还没有真的开麦，这些字是假的。` + 那串假字 | 它是"先做假的"那一版的产物；现在这一半**是真的**了，留着才是假话 |
| `voiceHoldToTalk` / `voiceReleaseToSend` / `voiceDemoChip` / `voiceNotWired` 四个词 | 同上（`test/widget/voice_demo_test.dart` 整份删掉，换成 `test/widget/hearing_test.dart`） |

---

## 二、协议（`/api/asr` · 一条**新的** WebSocket）

🔴 **为什么另开一条**：聊天那条（`/api/stream`）的协议**已经冻结**，
而音频要的是**二进制帧**。往里塞新东西 = 动一条上线过的协议。⇒ 另开一条，
身份那一套**完全照抄**（同一个 `bearer` 子协议、同一个 `claim`、同一套租户转发）。

| 方向 | 帧 | 内容 |
|---|---|---|
| 浏览器 → 服务端 | **二进制** | 16k · 单声道 · 16bit **小端 PCM**（一个字节都不加壳） |
| 浏览器 → 服务端 | 文本 | `{"type":"asr/start"}` · `{"type":"asr/stop"}` |
| 服务端 → 浏览器 | 文本 | 见下表 |

| 事件 | 意思 |
|---|---|
| `asr/ready` | 上游握手成了（`code:0`）——**这一条才算真开起来了** |
| `asr/partial{text,index}` | 一段的**中间结果** |
| `asr/final{text,index}` | 一段的结果（`slice_type` 1 或 2） |
| `asr/end{text,index}` | 整段收尾（**带着最后听到的字**，不是空字） |
| `asr/capped` | 到点了（上游内测版一条连接最多 1 分钟）⇒ 我们**提前**收手 |
| `asr/error{reason,message,code?}` | 上游回错（鉴权 / 没开通 / 参数）——**只说它那句原话** |
| `asr/unavailable{reason}` | 这台部署**没配钥匙**（如实说） |

🔴 **`index` 是这份协议里最容易漏掉的一个字段**（2026-09-23 抓真帧才定下来）：
腾讯**同一段**会连着发好几条，每一条的 `voice_text_str` 都是**这一段到目前为止**的字
（实测：`嗯` → `今天` → `今天天气` → `今天天气怎么` → `今天天气怎么样？`，**`index` 全程是 0**）。
⇒ 客户端必须**按段号替换**、段号变了才接新的。少了 `index` 就只能"一条条接起来"，
屏幕上会出现"嗯今天今天天气今天天气怎么今天天气怎么样？"那一串重复
（这正是公网真页面上抓到过的）。判据：`test/asr.test.js` 的「index 要真的传下去」
与 `test/unit/hearing_session_test.dart` 的「真实帧序列」那两条。

⚠️ **服务端提前收手的那个点是 `ASR_MAX_MS`**（`src/asr.js`），**比 1 分钟小**：
等它半路把连接掐掉，用户看到的是"说到一半没了"。

---

## 三、音频怎么走（🔴 密钥**不许**下浏览器）

```
麦克风 ──getUserMedia──▶ AudioContext ──降采样 16k──▶ WebSocket `/api/asr`
                                                          │
                                                （这台机器的 serve.js）
                                                          ▼
                                      wss://asr.cloud.tencent.com/asr/v2/<appid>?…签名…
```

**为什么不让浏览器直连腾讯云**：那样子 `SecretKey` 就得出现在页面里 ——
不是"可能被猜"，是**明文摆在 devtools 里**，谁都能拿它烧我们的额度。
代价是这台机要中转音频：16k 单声道 ≈ 32 KB/s，一次最多一分钟，很小。

- **签名**在 `v2/services/core/src/asr-sign.js`（纯函数）。自测脚本
  `scripts/check-asr-tencent.mjs` **import 同一份** —— 脚本自己再写一遍
  = 判据打在另一侧（V13 那条教训）。
🔴 **顺序是有讲究的**（2026-09-23 在线上实测到才改的）：
**先连上问一句"这台能不能听"，能听才去要麦克风权限。**
原来的顺序是先 `getUserMedia` —— 于是在**没配钥匙**的部署上，
用户先看到一个麦克风权限框、点了允许之后才被告知"没配好"：**白打扰一次**。
⇒ 现在的判定点是上游那句 `asr/ready`（只有它才算"能听"）。

- **换上游**：环境变量 `HUPO_ASR_URL`（给了它就直接连它，不算签名）。
  它是给**取证**用的：没有钥匙也能把整条链验一遍。⚠️ **生产里不许设**。
- **凭据**只从环境变量读：`TENCENT_APPID` / `TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY`
  （引擎名可用 `TENCENT_ASR_ENGINE` 覆盖，默认 `Hy-ASR-3.0-preview`）。
  ⚠️ 多租户下客户端是连**他自己那台容器**的 `/api/asr` ⇒ 容器里也要有这三样。

---

## 四、判据（**每一条都打在真那一侧**）

| 在哪 | 多少条 | 钉住了什么 |
|---|---|---|
| `v2/services/core/test/asr.test.js` | **16** | 签名纯 / `voice_id` 缺了要抛 / 路由之外仍 404 / 没令牌**握手就 401** / 没配钥匙如实说 / **音频一个字节不改地过去** / 半句与定稿映射对 / 连上之前推的音频不丢 / 上游回错**原话转达且不带密钥** / 到点收手 |
| `test/unit/pcm16_test.dart` | **8** | 48k→16k **按窗口平均**（不是隔点丢）、截幅、长度、小端、空输入 |
| `test/unit/hearing_session_test.dart` | **17** | 按一下/再按一下、**按段号替换（真实帧序列：同一段不重复）**、段号变了才接、乱序也按段号排、**按停之后来的最后一句必须接住**、**空字不许擦掉刚听到的**、没配/没权限/没额度/连不上各停在哪句话上 |
| `test/unit/stream_uri_test.dart` | **+3** | `/api/asr` 的地址与流**共用同一个算法**（https 页面**不许降级**那条金丝雀） |
| `test/widget/hearing_test.dart` | **8** | 开不了麦不画话筒 / 三档按钮文案 / 没配钥匙**不画按钮** / **说完了字进框 + 发送钮出现** / 命中区 ≥44 / 2.0 倍不溢出 |
| `test/widget/accessibility_test.dart` | 既有 **181** | 语音那一档也在五档扫的范围内（硬闸） |

### 真浏览器取证（**这一批最强的那条证据**）

假麦克风（Chrome 的 `--use-fake-device-for-media-stream` +
`--use-file-for-fake-audio-capture=<16k 单声道 wav>`）+ **假上游**
（`scripts/asr-stub-upstream.mjs`），于是**除了腾讯云自己那台**，每一环都是真的：

```
▶ 假上游：收到收尾请求；一共收到 147420 字节音频（≈4.6 秒）
🧪 【听的时候】… | 今天天 | 发送 | 键盘 | 正在听…再按一下 结束 | …
🧪 【输入框】[{"v":"今天天气怎么样"}] 发送钮在=true
📷 截图里：输入框里就是「今天天气怎么样」，发送钮亮着
```

🔴 **取证抓到四个真缺陷**（这才是"真跑一遍"的价值）：

| # | 缺陷 | 根因 | 怎么修 | 判据 |
|---|---|---|---|---|
| 1 | 按下"结束"后**最后一句被丢掉**（屏幕上只剩半句「今天天」） | 状态机一按停就进 `idle`，而收字只在"正在听"时进行 | 加 `finishing`（收尾中）那一档，等 `asr/end` 才算完 | `hearing_session_test.dart`「按了停之后来的最后一句必须接住」 |
| 2 | **`restart-core.sh` 停不下来**（有一个语音连接开着就卡住） | `server.js` 的 `close()` 只终止聊天那条 `wss`，**没管新加的 `asrWss`** | 两条都终止、两条都关 | 测试第一次跑就挂在那儿（55 秒那条） |
| 3 | 公网**握不上手**，而页面上说"开不了麦克风" | VPS nginx 只给 `/api/stream` 配了升级头；客户端又把"连不上"与"开不了麦"混成一句 | nginx 那一行改成 `location ~ ^/api/(stream\|asr)$`（主人授权）+ 客户端分成 `hearNoEntry` / `hearFailed` 两句 | §6.1 的公网复验 + 「连不上与开不了麦分开说」 |
| 4 | 🔴 屏幕上**一串重复**："嗯今天今天天气今天天气怎么今天天气怎么样？" | ① 中转**没把 `index` 传下去**；② 客户端按"一句一句追加"合并 | 服务端带 `index`；客户端改成**按段号替换**（真帧抓出来才看清） | `asr.test.js`「index 要真的传下去」+ `hearing_session_test.dart`「真实帧序列」 |
| 5 | `asr/end`（**空字**）把刚听到的那一段**擦掉** | 腾讯 `final:1` 里没有 `result` ⇒ 中转出来是空字；客户端照单全收 | 客户端：**空字不许覆盖**；服务端：收尾带上**最后听到的字** | 同上那条真实帧序列的第二个断言 |

---

## 四·补、🔴 排查"按一下就没声了"这一类投诉（2026-09-23 加的）

主人手机上反馈：*"按一下立刻就退出听筒模式了"*。而服务端**当时什么都不记** ⇒ 只能猜。
⇒ 现在**每次会话都写一行日志**（**不记内容、不记密钥**，只记设备、引擎、收了多少字节、怎么结束的）：

```
▶ asr：会话开始 · 引擎 16k_zh · 来自 Mozilla/5.0 (…Chrome/154…)
▶ asr：会话结束 · 收到 139230 字节 ≈ 4.4 秒 · 原因：上游说整段说完了
▶ asr：浏览器那一边断了（收到 139230 字节）
```

一句话读法：
* **日志里一行都没有** ⇒ 那台设备**根本没走到服务端**（老缓存的页面 / 开不了麦 / 不是网页那套）；
* **收到 0 字节** ⇒ 走到了服务端、但**音频一个字节都没上去**（权限 / 音频图 / 移动端把 AudioContext 挂起）；
* **有字节、很快就结束** ⇒ 看那一行的"原因"（上游说什么）。

⚠️ **同时修掉一个"页面在说假话"**：一句都没听到时它原来**悄悄退回键盘档**、
屏幕上什么都不说 —— 用户看到的就是"按一下、什么都没发生"。现在**留在语音档**并写明
"什么都没听到"，判据 `test/widget/hearing_test.dart` 那条。

## 四·补2、输入条这一片的**一次整理**（2026-09-23，主人：*"先整理整个UI"*）

| 原来 | 现在 | 为什么 |
|---|---|---|
| 开不了麦就把**话筒藏起来** | **话筒一直在**，点它说一句 `这里开不了麦（换个浏览器打开就能用）` | 藏起来等于让用户自己猜 —— 主人当场就问"为什么录音的 icon 没有" |
| 正在听时只有按钮上一行字，**看着不像在录音** | 红点 + `● 正在听` + **大字实时文字**（最多 3 行，超出省略） | "正在听"要**一眼看得见**（D5.13 的正面用法） |
| 正在听时**发送钮已经出现**（框里有字） | 听的时候**不画发送**，停下才出现 | 那会儿字是"还在长"的半句，发出去等于替他做决定 |
| `正在听…再按一下 结束` 一句话塞在按钮里 | 标记归标记、按钮归按钮（`再按一下 结束`） | 一行里两件事，读起来费劲 |

⚠️ 红点那一点是**文字画出来的**（`●` 跟主题字号缩放），不是写死的尺寸。
判据：`test/widget/hearing_test.dart`（含"正在听时不画发送"那条）。

## 五、还欠的（**明说，不许用"排后"混过去**）

| 欠什么 | 现在什么样 |
|---|---|
| **主人跑一次真自测**（真钥匙） | `TENCENT_APPID=… TENCENT_SECRET_ID=… TENCENT_SECRET_KEY=… node scripts/check-asr-tencent.mjs`；在那之前，"腾讯收不收我们的签名"这一条**没人验过**（取证用的是桩） |
| **手机壳里的原生录音** | **没做**。这一批只做网页；手机壳里那份 `hearing_stub.dart` 如实返回"开不了"（界面上不画话筒） |
| **说到一半断线/权限被收回** | 如实报错、字留着，但**没有"接着刚才那句继续说"** |
| **一次最多一分钟** | 上游内测版的限制。到点由**服务端**说 `asr/capped`（客户端不自己猜），字留着 |
| **`canSpeak` 写死 `true`**（"读出来"那个开关） | 浏览器一个音色都没有时它照样画（2026-09-23 取证时实测 `0 音色` + `err:not-allowed`）⇒ 记账，没动 |
| **音量波形 / 说多久了** | 没做（主人没要；而且周期定时器会把 `pumpAndSettle` 顶住，不值当） |
| **移动端（iOS）音频图可能被挂起** | `AudioContext` 在 iOS Safari 上首次必须**在用户手势里** `resume()`，而我们的链路是"点了之后异步走几步才建图" ⇒ **可能在手机上一直采不到音频**（表现：屏幕上"正在听"，但服务端收到 0 字节）。⚠️ 这台机器验不了 iOS，先记成欠账 |

---

## 六、生产怎么配

### 6.1 ⚠️ **公网那一跳也要放行这条路径**（2026-09-23 已改，主人授权）

VPS 上 `/etc/nginx/conf.d/w-stalkerai.conf` 原来只有 `location = /api/stream`
带 WebSocket 升级头 ⇒ `/api/asr` 落进普通 HTTP 反代、**握手被丢掉**
（浏览器只看到 `1006`，而这一头一切正常）。改法**一行**：

```nginx
-    location = /api/stream {
+    location ~ ^/api/(stream|asr)$ {
```

改完 `nginx -t` ⇒ `systemctl reload nginx` ⇒ 公网复验：
`wss://w.stalkerai.cn/api/asr` 回 `{"type":"asr/unavailable",…}` 再 `close:1000`
（和本机直连 `127.0.0.1:8020` 一模一样）。备份留在
`/etc/nginx/conf.d/w-stalkerai.conf.bak-20260923215845`。
⚠️ **以后再加 WS 路径，别忘了这一跳**（漏了的表现是"本机好好的、公网握手就断"）。

### 6.2 凭据怎么进服务（**主人自己动手** · 2026-09-23 已把管子接好）

服务从**环境变量**读那三样。而环境由 `scripts/restart-core.sh` 从
**`v2/services/core/data/asr.env`** 装进去（**照 `data/tenants.env` 那条已有的路**）：

```bash
cd /home/deploy/proj/hupo/v2/services/core
umask 077                     # 只有自己能读
cat > data/asr.env <<'EOF'
TENCENT_APPID=你的AppID
TENCENT_SECRET_ID=你的SecretId
TENCENT_SECRET_KEY=你的SecretKey
EOF
chmod 600 data/asr.env        # 0600
cd /home/deploy/proj/hupo && bash scripts/restart-core.sh
```

重启时它**只报"齐没齐"，不打印任何值**：

```
   （读到了 data/asr.env：APPID 有 · SECRET_ID 有 · SECRET_KEY 有）
```

⚠️ **三条**：
1. `data/` **在 `.gitignore` 里**（`git check-ignore` 验过）⇒ 这个文件**不可能被提交**；
2. 这个文件**只有你自己写**（我不碰那个值，也不打印它）；
3. 多租户：容器里那台也要有这三样（客户端连的是**他自己那台**的 `/api/asr`）。
   今天**没配** —— 别人的语音会如实说"没配好"。

### 6.2.1 现在的实际状态（2026-09-23 晚，已生效）

主人给了 CAM 的 **SecretId / SecretKey**，AppID 我从 **CAM 的 `GetUserAppId`** 查出来
（`cam.tencentcloudapi.com`，TC3 签名，回 `AppId`）—— 三样齐了，装进 `data/asr.env`
（`0600`、`data/` 在 `.gitignore` 里），重启后横幅报 `APPID 有 · SECRET_ID 有 · SECRET_KEY 有`。

⚠️ **引擎这一步有个坑**：主人说开通了混元（`Hy-ASR-3.0-preview`），但实测：

| 引擎 | 回话 |
|---|---|
| `Hy-ASR-3.0-preview`（混元内测） | `code=4004 **资源包耗尽**，请开通后付费或者购买资源包` |
| `16k_zh_large` · `16k_zh_en` | 同上（都是"大模型2.0"计费那套） |
| **`16k_zh`（标准实时语音识别）** | ✅ **`code=0`** —— 送进去的 3 秒普通话**认出来了**：`今天天气怎么样？` |

⇒ **线上先用 `16k_zh`**（那一档免费额度还在，**当天就能用**）。
等混元的资源包/后付费开通了，把 `data/asr.env` 里那一行改掉重启即可（**不用改代码**）：

```bash
# 想切回混元：把 TENCENT_ASR_ENGINE 那行改成这个（或整行删掉 —— 默认就是它）
TENCENT_ASR_ENGINE=Hy-ASR-3.0-preview
```

⚠️ **换钥匙**：主人把 SecretId/SecretKey **贴进过对话**（= 进过会话日志）⇒
**建议作废重发一把**，然后把新的三样填进同一个文件、重启（步骤同上）。
🔴 顺手记一条：这次我差点把 SecretId 整串写进文档，**被 GitHub 的密钥扫描挡下**（推送被拒），
改成只留前 8 位才推成功 —— 那条"密钥不进文件"的纪律连 GitHub 都替我们守着。

### 6.3 🔴 接这根管子时顺手拆掉的一个坑（`restart-core.sh`）

原来那一段长这样：

```bash
setsid nohup env \
  HUPO_DATA="$DIR/data" \
  HUPO_PORT="$PORT" \
  HUPO_WEB="$DIR/web" \
# ← 紧跟一条注释，**它把这个命令结束了**
if [ -f data/tenants.env ]; then … fi
  HUPO_BUILD_ID="$BUILD" \
  node src/serve.js > serve.log 2>&1 < /dev/null &
```

bash 的续行符在注释**之前**就把行接起来了，于是那其实是一条
**没有命令的 `env`**：

- ① 它把**整个环境打印一遍** —— 环境里但凡有密钥（比如我正要接进去的
  `TENCENT_SECRET_KEY`），就会**当场印在屏幕上**（`deploy-web-v2.sh` 调它时那一段
  被 `grep` 滤掉了，所以以前没露出来；**直接跑就不会**）；
- ② `HUPO_DATA / HUPO_PORT / HUPO_WEB` **根本没传给服务** —— 服务一直在用默认值，
  而脚本却照着变量 echo「端口 $PORT」⇒ 谁把 `HUPO_PORT` 改了，**脚本就在说假话**。

⇒ 改成显式 `export`（一个字节都不打印，值真的进服务）。⚠️ **起进程那一段没动**：
线上服务的 cgroup 是 DSH 的 subprocess scope（`AGENTS.md` §一 记着），
换 detach 方式会动到它。

**验过的读数**（2026-09-23，用**不含任何密钥**的 `HUPO_ASR_URL` 当文件内容）：

| 验什么 | 读数 |
|---|---|
| 不打印环境 | 重启输出里 `SESSION_MANAGER=` / `^PATH=` **0 次**（修之前会出现） |
| 只说齐没齐 | `（读到了 data/asr.env：APPID 没有 · SECRET_ID 没有 · SECRET_KEY 没有）` |
| 值真进服务 | `/proc/<pid>/environ` 里 `HUPO_DATA / HUPO_PORT / HUPO_WEB / HUPO_ASR_URL` **都在**，`HUPO_DATA` 正是 `…/core/data` |
| **真页面走通** | **公网** `w.stalkerai.cn` + 假麦克风：框里出现上游那句话、桩那头收到 **141960 字节**音频 |
| 拆完不留痕 | 删文件 + 重启 ⇒ 环境里 `HUPO_ASR_URL` / `TENCENT_SECRET_KEY` **都不在**，页面回到"缺钥匙" |
