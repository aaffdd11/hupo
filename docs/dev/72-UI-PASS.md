# 72 · **整个 UI 的一次整理**（主人 2026-09-23：*"先整理整个UI"*）

> 起因：主人问 **"为什么录音的 icon 没有？"** → 追下去发现是**三个不同的原因**
> （原生包把话筒藏起来了 / 老缓存 / 正在听时看不出在录音），
> 而他说 *"首先，先整理整个UI"* ⇒ 先把界面这一层收拾干净，再往下加功能。
>
> **范围**（主人四块都要）：**A 桌面** · **B 聊天浮窗** · **D 设置/关于** · **E 配色·圆角·阴影统一**。
> **做法**：一块一块来，每块**先拍真图**（`scripts/check-web-browser.mjs --shot`），
> 改完再拍一张对比；判据照旧进 `test/widget` + 可访问性硬闸。

---

## 〇、已经做完的两件（本次）

| 件 | 原来 | 现在 |
|---|---|---|
| **输入条这一片** | 开不了麦就把话筒**藏起来**；正在听只有按钮上一行小字；听的时候发送钮已经出现 | 话筒**一直在**（点它说明白话）；正在听 = **红点 + ● 正在听 + 大字实时文字**；听的时候**不画发送**。契约 `71-MIC-ASR.md` §四·补2 |
| 🔴 **关于页里的一句假话** | *"**我们自己没有另外做一个话筒。**"* —— 话筒 2026-09-23 就做了 | 改成**按设备说**（`aboutFactsFor(canHear:)`：网页一套、开不了麦一套），并把那句假话钉成**回归条**（`test/unit/about_facts_test.dart`） |

⚠️ 那句假话是**在"整理 UI"时当场读出来的**（勘察截图里就写在屏幕上）——
这也是"整理"这件事本身的价值：**它逼你把每一屏重新读一遍**。

---

## 一、逐块勘察结果（真图在 `/tmp/ui-*.png`，本机可开）

### A · 桌面那一片 — ✅ **已做完**（2026-09-23）

| 原来 | 现在 | 判据 |
|---|---|---|
| 图标格 **52**（手机上像一粒纽扣） | **64**（`desktopIconBox`，仍是 ≥44 的硬要求之上） | `desktop_test.dart`「格子够大：>= 64（不许缩回去）」 |
| 标签 `bodySmall`(≈12) | `bodyMedium`(≈14)，**最多两行、超出省略** | 同上那条 + 「名字长了不许把格子撑破」 |
| **一句引导都没有**（六个陌生图标 + 一片空白） | 桌面上多一句 `点一下图标，就打开它。` | `desktop_test.dart`「桌子上有一句引导」 |
| `clamp(…, 88.0)` 里那个 88 是**写死的** | 提成 `desktopTileMax` + `desktopTileSlack` | 同上 |
| 🔴 点**标签**能不能打开（2026-09-22 踩过的坑：`InkWell` 只包了方格 ⇒ 点字变成"收起聊天"） | 行为没改（本来就是对的），**这次补上判据** | `desktop_test.dart`「点**标签**真的能打开（不是点桌面空白）」 |

仍留给后面两块的：
* **三种卡片外观**（桌面格子 / 聊天卡片 / 设置行）⇒ **E** 统一；
* 格子下面那一大片空白 ⇒ 桌面本来就是底图（B/E 里再看要不要放东西）。

### B · 聊天浮窗（展开态）— ✅ **已做完**（2026-09-23）

| 原来 | 现在 | 判据 |
|---|---|---|
| 头部三个动作**只有图标**（🗑 / ▣ / ⚙），手机上没法 hover 看 tooltip ⇒ 只能瞎点 | **图标 + 中文短标签**：`回收站` · `导出` · `过程`（D3.8：不许只有无字图形） | `floater_actions_test.dart`「三个动作都有中文」 |
| 标题「助手」`titleSmall`(≈14)，和那排图标一样大 | **`titleMedium`(≈16)** | 同上「标题 >= 16」 |
| 「读一遍」`bodySmall`(≈12) 又小又淡 | **`labelLarge`(≈14)** + 淡色，命中区仍 ≥44 | 同上「读一遍的字 >= 14 且命中区 ≥44」 |
| 收起态只有一道缝 | **没动**（主人要的"贴边"）；提示留给下回 | —— |

⚠️ **改完当场被硬闸抓住两次**（这就是闸的价值）：
1. `accessibility_test.dart` 那三条"**从真入口进**"（过程面板 / 回收站 / 导出）是按
   `find.byTooltip(...)` 找入口的 —— 改成带字按钮后 tooltip 没了 ⇒ **25 条全红**。
   ⇒ 判据改成"按**用户看得见的那两个字**找"，并且**先 `ensureVisible`**
   （窄屏 + 大字号下它们在横滚条里，用户也得先滚过去）。
2. `delete_flow_test.dart` 同样按 tooltip 进回收站 ⇒ 一起改。

### D · 设置 / 关于 — ✅ **已做完**（2026-09-23）

| 原来 | 现在 | 判据 |
|---|---|---|
| 「你那串钥匙」是**强调色 + 加宽字距**的小标 ⇒ 读起来像**警告** | **黑 + 加粗**的普通分区标题（`_SectionTitle`，一处定、两个分区都用它） | `settings_test.dart`「分区标题是黑+加粗，不是强调色」 |
| 「关于 / 退出登录」**光秃秃挂在最下面**，一页里三块东西读不出结构 | **第二个分区标题**「这个助手」+ 两条收进**同一张卡**（中间一条分隔线） | 同上「两个分区都有标题」 |
| 「关于」只有两个字 | 加一句小字说明 `它的能力、记忆和边界` | 同上「关于那一条有一句说明」 |
| 退出登录（红色） | **没动**（判据钉住它**不许**被"统一"成黑的） | 同上「退出登录仍然是红的」 |
| 关于页里那句假话 | ✅ 上一轮已修（按设备说 + 回归条） | `about_facts_test.dart` |

### E · 配色 · 圆角 · 阴影

| 看到的问题 | 说明 |
|---|---|
| **三种卡片外观** | 桌面格子（浅底大圆角）/ 聊天卡片（白底）/ 设置行（白底 + 描边）—— 圆角与阴影各用各的 |
| token 有，但**各屏挑着用** | `models/design.dart` 里已经有那几档；需要一次"**一屏一屏对表**"，而不是再造一套 |

---

## 二、顺序（**一个一个来**，每件都留前后对比图）

1. **A 桌面**（一进来的第一眼）
2. **B 聊天浮窗**（用得最多的一屏）
3. **D 设置/关于**（这次的起因之一）
4. **E 统一 token**（前三个做完，E 就是"把剩下的对一遍表"）

⚠️ 每件做完都要：两道硬闸绿 · 真图复核 · 文档与账跟上 · 部署。
