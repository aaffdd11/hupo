# 96 · 主人拍的 16 条（2026-09-25，一条一条问出来的）

> ⚠️ 这是**决策登记**：实现照它走；与它冲突的旧文档以本篇为准（并去升那几份的版本）。
> 金额、比例、阈值**一律不写数值**（住代码/配置）。

| # | 问题 | **拍了什么**（主人原话/选项） | 落到哪一步 |
|---|---|---|---|
| 1 | 审核主体是谁 | **运营方保存的那个 agent 读整份项目源码** ⇒ 做总结 · 找风险点 · **给评级**（不是"作者自报为准"）| 上架闸（93 的 R 系列改成"运营方评审"为主）|
| 2 | 评级算不算数 | **乙：低风险自动放行，高风险找你**（哪一档算低 —— 阈值住代码）| 上架闸放行规则 |
| 3 | 还要不要作者申报 | **要**；而且**运营方会在容器里预置"审核能力"让用户先预审**（容器里跑的是用户的 agent ⇒ **算力用户出**）| 91 创建侧 + 93 上架侧 |
| 3b | （我补的要害）| 🔴 **预审规则必须住产品层**（只读挂载＋指纹，母体升级）—— **绝不许用户能改**，否则"自己审自己"是笑话 | 产品层 / 防篡改 |
| 4 | 预审什么时候跑 | **甲：上架流程第一步自动跑**（他随时也能手动再跑一次）| 上架流程第一步 |
| 5 | 申报住哪 | **甲：写在制品里，随 `rootHash` 冻结**（改申报＝换起点）| 制品 / A16 |
| 6 | 用量给谁看 | **丙：发现页直接显示完整曲线** + （我补的口径）公开的是**作者/运营方那份**；**每个装上的人自己的曲线仍私密**；**安装者少时退化成量级档**（阈值住代码）| 发现页 / 市场 |
| 7 | 申报与实测对不上 | **用量的真相在运营方这边**（模型调用经我们中转 ⇒ **无法造假**）⇒ **以运营方实测为准**；申报是"预告"，对不上进**评级** | 93 的 C2 / 结算地基 |
| 8 | token 算到谁头上 | **甲：都算到这个 app（含它触发的子任务）**；内部另存拆分备查，但**对外只一个数** | 93 的 `usage.jsonl` |
| 9 | 钱怎么走 | **甲：先只做授权＋结算的账，不碰真钱**（账要**可复算**，对不上就红）；真钱独立一档，默认砍 | 95 付费层 |
| 10 | 免费与定价 | **用户自己报价给市场**；**自创自用永远免费** ⇒ 平台不干预定价、只记账。（**平台抽成你没提 ⇒ 先按 0 记**，要抽改一处配置）| 95 |
| 11 | 经验包卖什么 | **乙：卖这份文件**（买家才能看到全文）⇒ 发现页只给预览；买到＝**交付进他盒子**（授权同一把钥匙）；**撤回：新请求拒、已交付收不回** | 94 |
| 12 | 数据包怎么交 | **"我会做一个市场，帮助交易"** ⇒ 交付走**市场**（挂牌→发现→交易→交付进盒子→回执/登记→撤回）| 计划新增**市场**一档 |
| 13 | 市场信用怎么立 | **乙：加用户评分与评论** + （我补的口径）评分**绑版本**、**作者不能删评**、**评论过隐私闸**、**一账号一版一次**、评分**不参与"能不能上架"**、只作排序参考 | 市场 |
| 14 | 署名与手册冲突 | **甲：改手册承认现状**（保留署名；如实写"显示名是哈希前缀、可反推"；两处明文 `sub` 换成**不可 join 的引用**）| 手册升版 + 清单重建 |
| 15 | "说法深浅漂移"怎么管 | **甲：先只量、只报警，不拦**（每轮把术语密度/句长/模板句占比落审计；同一用户同一情景"双峰"就报警）| P2 |
| 16 | 他看着 A 却说 B 的事 | **丙：不确定就先反问一句**（"你是说 B 那间吗？"）| P3 / 84-C 焦点路由 |

## 我自己定的（**不占主人决策额度，写明以便他随时推翻**）

1. **授权登记统一成一份** `entitlements.jsonl`，用 **`layer` 字段**区分三层（94 的 `exp-licenses.jsonl` 与 95 的 `entitlements.jsonl` 合并）。
2. **平台抽成先按 0 记**（Q10 未定；改一处配置即可）。
3. **评论防刷与隐私闸**的具体口径（见 Q13 那行）。
4. **预审与复评的结论对不对得上**，本身就是一条**反着验的判据**（差太多 ⇒ 红）。

## 还欠着的（记着，别让它烂掉）

- **`80-P2-DECISIONS.md` 那 14 条**（最早那批，仍未答）；要的话我按同样方式**一条条**问。
- 94/95 各自还留了几条细分（默认已按它们的"建议"取），实现时若真撞上再单独问。


---

## 附：`80-P2-DECISIONS.md` 那批（主人 2026-09-25 起**一条条**问，这里记已答的）

| 条 | 问题 | **拍了什么** | 后果/要做的事 |
|---|---|---|---|
| **P2-10** | 登录用的是**临时开发码**（任何手机号+那个码就能建号占容器）| **③ 暂时就这样**（记账，不改）| 🔴 风险照旧写明：知道那个码的人能给自己建号、占一台容器 |
| **P2-7** | 服务与两条隧道**不自启**（重启即整站 502）| **① 启用**（草案已就绪，`systemd-analyze verify` 过）| 我先把三个单元逐行讲给主人听 ⇒ 他点头后 `systemctl --user enable --now` ⇒ 当场验"重启后公网仍 200" |
| **P2-8** | 盒里 agent **能往任意地址发请求**，而手册读起来像"出不去" | **② 先只做出网留痕**（不改网络）| 记"它往哪发"（域名/时间/量）可查；手册升版时写明"能出网" |
| **P2-1** | 语音走哪条路 | **① 云端（现状）**；**手机录音另算一件** | 声音**出境到腾讯云**（`D5.2` 已如实写）；安卓录音要主人的手机才能验 ⇒ 先写好等着 |
| **P2-4** | `src/**`/`scripts/**` 是"只报不拦" | **② 继续只报不拦**，理由写进手册 | 人格/手册/DSH 配置仍 strict（真保护没减）；收口期再谈升 |

**已答完（12/12）**：
| 条 | 问题 | **拍了什么** | 后果/要做的事 |
|---|---|---|---|
| **P2-5** | 盒子里跑不了命令（缺 `bwrap`）⇒ 它改完自己没法验 | **① 改镜像补 `bwrap`** | ⚠️ **安全相关**：盒里从此能执行命令（靠 `bwrap` 收着）⇒ 要**重建镜像 + load + 重开容器**，单列一次带说明的部署 |
| **P2-6** | 宿主上也有一份小程序目录（权威已在盒里）| **乙 把宿主那份删掉** | 只删**租户那几格**（`data/users/<id>/hupo/apps/`）；**主人自己那份 `data/hupo/apps` 是权威，不许动**；删前逐条核对盒里 `rootHash`，删完留审计一行 |
| **P2-2** | 语音钥匙的"部署默认那份"要不要当兜底 | **② 去掉兜底**（没填＝没配）| ⚠️ 只对"别人"去兜底；**主人自己那份不动**（免得把他自己的语音关掉）|
| **P2-9** | 收起态那道缝：补提示还是砍 | **② 明说砍掉** | 改 `72:48` 并记账（那道缝本来就能点开）|
| **P2-3** | 花钱的活（画图/语音）要不要做成可见的账 | **① 现在就跟 token 一起做** | 一个账本三个计数器（token/张数/分钟）；对外仍是一个数 |
| **P2-11** | 租户容器没有 CPU 上限 | **① `--cpus=4`** | 改 `scripts/create-tenant-pool.sh`；**只影响以后新建的容器** |
| **P2-12** | 监控只剩内存（磁盘/inode/fd 没采样）| **① 接线** | 规则那半已就位 ⇒ 只做接线（周期采样 + 按手册四档告警），不改运行时 |
