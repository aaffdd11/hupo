# 76 · 计划：所有要做的事（一份就够）
> 执笔 2026-09-24。材料＝**产品线**＋**运维安全线**两份清单（工程线**没交稿**，见 §八）＋ `75-ACCEPTANCE.md`（§五/§六/§七）· `74-ACCEPTANCE.md` · `00-PROGRESS.md` §六/§八 · `63-OWNER-DECISIONS.md` · `72/73/15/71` · `proposals/2026-09-24-子agent越界产物/`（只读）。裁决顺序：手册 → 代码 → 其他；每条只写有证据的结论。
## 一、怎么用这一份（排序原则：先收尾 · 先修"说假话" · 再做功能 · 一件一件来）
- 顺序：先收尾（§二）→ 修"说假话"（§二）→ 本批工程（§三）→ 主人拍板（§四）→ 主人亲自动手（§五）；**新功能排最后**（主人 2026-09-23 定序见 `63-OWNER-DECISIONS.md:32`）。
- 每行格式：**做什么** —— 判据（`文件:行`／测试名／一次操作）· 规模（小/中/大）· 谁动手 · `[来源]`（原清单编号）。
- 一件做完就按 `AGENTS.md` §5.0 四件收尾（两闸 · 部署 · 文档与账 · 推送），上一件没收尾不许开下一件；`76-CREDS-TABS.md` §八 的教训：同一棵树上不许两个会话同时动手。
## 二、P0 —— 立刻做（不做会出事 / 一行就能修完的）
- **P0-1 收尾 #94 那批（上一轮只读，一道闸都没跑）**：`cd v2/services/core && npm test`（777 绿）· `bash scripts/check-client.sh` · `HUPO_TOKEN=… node scripts/check-web-browser.mjs --shot …`（V13）；小；助手。[产品 E7/E8 · `75:157`]
- **P0-2 客户端"配置页四个 tab"重做，或明说丢弃**：`git apply --check proposals/2026-09-24-子agent越界产物/客户端改动.patch`（现通过＝未入树）；重做完 `grep -rn TabBar v2/apps/mobile/lib` 不再为 0 ＋ `check-client.sh` 绿 ＋ 部署指纹变；中；助手。[产品 A1/A4＝运维 A1 · `75:87`]
- **P0-3 配置页服务端半与客户端半合批收尾**：`/api/space` 回 `creds:{model,image,video,voice}`（`76:89`）＋ 服务端 777 绿；小；助手。[产品 A3 · `76:114`]
- **P0-4 禁用词三处同步（`'服务器'` 真上屏＝缺陷）**：改 `chat_controller.dart:664,976`（→`conn_state.dart:48`→`chat_screen.dart:680,1044`），`forbidden_words_test.dart` 清单收这两句；小；助手。[产品 C1＝运维 B5 · `75:60`]
- **P0-5 收窄 `'正在听'` 的豁免**：词表按 D5.13（`05-DECISIONS.md:200`"只在真听时"）收窄并写清豁免，测试钉住；小；助手。[产品 C2 · `75:61`]
- **P0-6 热表自相矛盾：#89 写"2/4"而四块全做完**：`00-PROGRESS.md:26` 改成实话；小；助手。[产品 D2 · `75:53`]
- **P0-7 开机清单漏 `src/creds.mjs`**：`verify-integrity.mjs` 报"漏 0 条"；小；主人一条 `sudo` 重建（见 §五-5）。[产品 H8＝运维 B1/E1 · `75:96`]
- **P0-8 部署脚本不跑硬闸**：`deploy-web-v2.sh:47` 只 `flutter build web`（0 处 analyze/test）⇒ 部署前替跑 `check-client.sh` ＋ 变异验证（故意红 ⇒ 中止）；小；助手。[运维 A5/A6 · `75:51`]
## 三、P1 —— 这一批做完（工程线）
- **P1-1 四个 tab 各写一句边界（图片/视频/语音"还只是收着"）**：`configImage/Video/VoiceBoundary` 上屏，`settings_test.dart` 钉住；小；助手；依 P0-2。[产品 A2 · `76:18-20`]
- **P1-2 修 `canSpeak` 写死 `true`**：`speech_web.dart:15` 改成查音色 ＋ `test/unit/speak_test.dart`；小；助手。[产品 B6 · `71:201`]
- **P1-3 补"说到一半断线，接着刚才那句说"**：`hearing_session` 新态 ＋ 判据；中；助手。[产品 B7 · `71:199`]
- **P1-4 对齐"手机壳里画不画话筒"口径**：改 `71-MIC-ASR.md:198` 或改 `composer.dart:385-404`，二选一 ＋ 判据；小；两边。[产品 B8 · `71:198`]
- **P1-5 补小程序 Z2"回到可见不许重建"判据**：`mini_app_test.dart:347` 现只验"没被销毁"；小；助手。[产品 E1 · `08-SPEC.md:553`]
- **P1-6 补图标⇄内容交接的界面取证**：`73:84-86` 自认欠真机中途图 ⇒ 补像素/结构判据或两张中途截图；小；助手。[产品 E2]
- **P1-7 补小程序沙箱三属性自动判据**：`mini_runtime_web.dart:40-43`；小；助手（V13 族，待工程线确认）。[产品 E3 · `75:36`]
- **P1-8 写死尺寸棘轮扩到 `EdgeInsets`/`SizedBox`**：`design_tokens_test.dart:26-33` 现只扫 `BorderRadius.circular`；中；助手。[产品 E5 · `75:94`]
- **P1-9 给 `/api/model-key` 补路由级 test**：`grep -rln model-key v2/services/core/test/` 非空（坏 field⇒400 · 非 model⇒404 · 正常 200）；小；助手。[产品 H9＝运维 B3 · `75:91`]
- **P1-10 补 `stream.dart` 本体判据（退避/401/游标）**：现只 `import_rules_test` 命中，地址算法有 `stream_uri_test`；中；助手。[产品 H10 · `75:92`]
- **P1-11 公开面常量与真实 4 条对上**：`PUBLIC_ROUTES`（3 条 ＋ `server.js:20` 死导入）vs `server.js:395-445`（含 `/api/send-code`）＋ 判据；小；助手。[产品 H11＝运维 B4 · `75:22`]
- **P1-12 修 `/api/health` 形状**：手册 `08-SPEC.md:173` 六字段 vs 代码 `server.js:512-514` 三字段 ⇒ 形状闸或改手册；小；待服务端契约线。[运维 B6 · `75:50`]
- **P1-13 给字体指纹/入口改名补 Dart 级判据**：落 `v2/apps/mobile/test/unit/`，变异（改坏 `FontManifest.json` ⇒ 红）；小；助手。[运维 A3 · `75:42`]
- **P1-14 像文件的路径回 404**：`server.js` 加分支（`/main.deadbeef0000.dart.js`⇒404 ＋ 真入口仍 200 负向对照）；小-中；助手。[运维 A4＝产品 H1 · `15-CACHE.md:244-246`]
- **P1-15 完整性两处小账**：`serve.js:72` 身份耦合自检／打印＋ `~/.dsh/storages/**`、`data/ledger.jsonl` 覆盖缺口给方案或明说"接受的风险"；小；助手。[运维 E4/E5 · `integrity.js:113-115,188-189`]
- **P1-16 重建留痕写进 `00-PROGRESS.md` §九**（时间/为什么/动了哪几个文件）；小；助手。[运维 F2 · `AGENTS.md` §8.1]
- **P1-17 "线上 ↔ 仓库"对表 ＋ nginx 落仓库**：写清唯一例外（客户端 tab 源码 ＋ VPS nginx），把 `/api/asr` 升级头片段落进 `deploy/` 或 `docs/dev/`；小；助手提 ＋ 主人签。[运维 C1/F3 · `75:158`]
- **P1-18 回填形状类过期文档**（`68:27` · `64:135` · `73:76` · `01-PROJECT.md:300/305` · `48:83` · `71:16-30,131,133` · `android_manifest_test.dart:53`）：逐处改成代码事实；中；两边。[产品 D1 · `75:57-67`]
- **P1-19 文档 strict 那批 6 条 ＋ `AGENTS.md:340`"50 个文件"**（实测 123 条/17 strict）：6 条改完 ＋ **一次**重建 ＋ 横幅"对上了"；中；助手提/主人重建。[运维 F1/E3＝产品 H7 · `75:97`]
- **P1-20 闲置回收接线**：产品代码调 `evictIfNeeded`（现只有 `test/` 调它、单元 `Restart=always`）；中；助手（容器级停/销毁策略要主人）。[运维 D3＝#67]
- **P1-21 逃生绳回滚口径写进文档**：`scripts/start-tunnels.sh` ＋ `scripts/restart-core.sh`，写明"不解决开机自启"；小；助手。[运维 C4]
- **P1-22 小程序机制闸"他明说才许写"**：动 `dispatcher` 把当轮输入递给工具那条路 ＋ 判据；中；助手。[产品 G3 · `00-PROGRESS.md:227`]
- ⚠️ **P1-23 与 P1-24 都排在 P2-1 后面**（见 §八 依赖图）⇒ **2026-09-24 确认：它们卡在主人那句话上**，见 `77-BLOCKERS.md` **B9**（不做半件的理由写在那儿）。
- **P1-23 手机壳原生录音 ＋ `RECORD_AUDIO`**：`hearing_stub.dart:11` 不再恒 `false` · `AndroidManifest.xml:16` 加权限 · `check-apk.sh:70-74` 置 `bad`；中-大；两边；要主人设备。[产品 B4＝D5.11 · `75:140`]
- 🚧 **P1-24（语料＋装置已就位 · 2026-09-24）**：四类（天气/药/公交/发消息）在**手册里就定死了**（`04-ROADMAP.md:189`）⇒ 我先把**语料 80 条** + **装置**做完了；**只剩声音**（要主人念，见 `77-BLOCKERS.md` B13）。
- ~~**P1-24 落 D5.10 硬闸（四类各 20 条、句子级命中率≥90%）**~~：语料 ＋ 命中率测试（`v2/services/core` 里 `命中率|语料` 现 0 命中）；中；两边；要主人开麦。[产品 B3 · `75:55`]
- **P1-25 回填 D5.2"手机自带识别器"改云端中继**：`05-DECISIONS.md:189` 改 ＋ `node scripts/check-docs.mjs` 退 0；小；两边（strict 要主人补重建命令）。[产品 B2＝运维 G8 · `75:54`]
- ⚠️ **P1-26 的前提已经变了**（2026-09-24）：`creds.mjs` / `VOICE_FIELDS` **随越界那批一起反做了** ⇒ "**按人一份**"那一半要等你拍 **B1/B2**；**不依赖那个决定的那一半已经做完**（凭据按连接现取 ＋ 现读 `data/asr.env` ⇒ 轮换不用重启），见 `77-BLOCKERS.md` 那张表。
- **P1-26 语音三样（`VOICE_FIELDS`）接进识别路**：`VOICE_FIELDS` 除 `creds.mjs` 无消费方（送得到容器、接不到识别路）⇒ 接上一条 resolver ＋ 判据；中；助手。[运维 B2 前半 · `75:79`]
## 四、P2 —— 主人拍板才能动（列出要拍什么，并写清"不拍板的后果"）
- **P2-1 语音本体走哪条路（#52）**：云端/本地模型/先做安卓包 三选一（拍板前先做 §五-1）；小（一句话）；主人。→不拍：真链路线与 D5.2 继续对撞，P1-23/24/25 全悬着。[产品 B1 · `63:50`]
- **P2-2 语音钥匙归属**：仍全机一份 `data/asr.env`，还是每人一份三格（现两处并存）；中；两边。→不拍：`76:97-98` 的过渡态永久留着，两处口径各说各话。[产品 F2＝运维 B2]
- **P2-3 三条"拿钥匙去干活"的路先定接口**（Seedream 生图/Seedance 生视频/腾讯云语音）：再补端到端判据（含真实模型计费）；大，**要开新批**；主人定接口→助手。→不拍：四个 tab 永远只收钥匙，用户以为填了能生图。[产品 F1 · `76:13-20,95`]
- **P2-4 收紧开机完整性那档"只报不拦"**：`src/**`/`scripts/**` 现 `report`（`integrity.js:71-76`），至少 `src/**` 升 `strict` 或写明"为什么不升"；中；主人签。→不拍：改了 `src/` 开机照样起，留痕靠人。[运维 E2 · `75:96`]
- **P2-5 #70 盒子里补 `bubblewrap`（三选一）**：`podman exec hupo-a sh -c 'command -v bwrap'` 有输出；中；主人三选一/助手改镜像。→不拍：盒内任何命令被直接拒，它改完自己没法验。[产品 H3＝运维 D6 · `63:114`]
- **P2-6 #71 中心影子世界 / `/api/app-ask` 闸挪到真那一侧**：定"只认盒子那一份"＋ 判据；中；主人拍板/助手。→不拍：小程序账目两头打架，中心那份不能用于任何判断。[产品 H4＝运维 D7 · `75:24`]
- **P2-7 #48/#58 线上服务与两条隧道归 systemd**：`systemctl --user list-unit-files | grep frp` 出现 `frpc-w`/`frpc-apps` 且 `enabled`，重启后公网仍 200；中；主人签/助手写单元。→不拍：一重启＝整站 502、小程序打不开。[产品 H5＝运维 C2+C3 · `63:49`]
- **P2-8 #65 容器出网白名单 ＋ 审计**（安全，不属性能）：单元加 `--network` ＋ 出口白名单 ＋ 请求留痕，白名单外⇒拒；大；主人签/助手。→不拍：agent 能往任意地址发请求，读手册的人以为出不去。[产品 H6＝运维 D1 · `63:114`]
- **P2-9 收起态"只有一道缝"**：做提示（补 `desktop_test`）或明说砍（改 `72:48` 并记账）；小；主人拍。→不拍：按 D3.8 记账悬着，下一个人不知道要不要做。[产品 E6 · `72:48`]
- **P2-10 #45 选短信通道并关 `HUPO_DEV_CODE`**：`/proc/<pid>/environ` 里没有该变量、没通道时 `/api/send-code` 回 503（`server.js:417-422`）；中；主人选通道＋助手接线。→不拍：任何人拿任意手机号建号占容器。[产品 G1 · `75:43`·`63:48`]
## 五、主人自己动手的那几件（钥匙 · 额度 · 手机验 · 轮换）
- **M1 那两分钟**：用自己手机输入法说三句话（家常／带数字专名／中间停顿），回"够用/不够用（哪儿）"；小；主人。[`63-OWNER-DECISIONS.md:18-24` · 产品 B1/G4]
- **M2 拿真钥匙跑语音自测**：`TENCENT_APPID=… TENCENT_SECRET_ID=… TENCENT_SECRET_KEY=… node scripts/check-asr-tencent.mjs`（输出不含密钥可直接贴）；小；主人。[产品 B5/G5 · `71:197`]
- **M3 真机自测一次**（腾讯收不收这个签名／权限框／识别质量／额度）；小；主人。[产品 B5 · `75:155`]
- **M4 TTS"真的有声音"**：自己设备听一次（无头浏览器没有音频输出）；小；主人。[产品 B9 · `00-PROGRESS.md:37` #81]
- **M5 重建开机清单一条 `sudo`**：照 `verify-integrity.mjs` 打出的绝对路径命令粘（本机没有系统 `node`）；小；主人。[P0-7 · `75:96`]
- **M6 文档 strict 批改完后的**一次**重建**（同上命令）；小；主人。[P1-19 · `AGENTS.md` §八]
- **M7 口令卫生三选一**：换口令／给那几条命令开 NOPASSWD 白名单／允许整段时间清 journal（现 journal 带口令 81 行、死 scope 1 个）；中；主人。[#49 · `63:51`]
- **M8 跑两条机械维护命令**：`sudo bash scripts/install-provision-helper.sh --yes` · `sudo bash scripts/create-tenant-pool.sh --yes`；小；主人。[产品 G7 · `63:58-75`]
- **M9 "打开 6 秒没第一个字就上划杀掉"时间闸**：要主人平板量一次；中；主人＋助手。[产品 E4 · `01-PROJECT.md:54`]
- **M10 "第一句真话"端到端走一次**（新号→开容器→真钥匙→真答一句→导出→删→回收站取回）；中；主人＋助手。[产品 G2 · `00-PROGRESS.md:226`]
- **M11 以 root 跑一次容器判据**：`check-container.sh`（含 V4a/V4b/V4c 两条负向对照，`deploy` 不在 docker 组）；中；主人。[运维 D8 · `08-SPEC.md:1213,1224-1226`]
## 六、已明确砍掉的（明说砍了，不是待办）
- 语音"按住说话／松手发送"那一套（`71:53`）。
- "演示"小标 ＋ 那句假字 ＋ `voiceHoldToTalk` 等四个词 ＋ `voice_demo_test.dart`（`71:54-55`）。
- "语音档／听筒档"这个档本身（`composer.dart:135-137,261`）。
- 桌面水面特效（`00-PROGRESS.md:56` #62，主人"太丑了，移除特效。不要了。"）。
- TTS"听筒／外放"那套语义（`00-PROGRESS.md:37` #81）。
- 聊天框"粘贴"按钮（钥匙屏那个留着）（`00-PROGRESS.md:29`）。
- 抓手"双击"（`00-PROGRESS.md:24` #92）。
- 浮窗"自动弹配置屏"——"没定就不做"（`48-SETTINGS-KEY.md:80-82`）。
- 音量波形／说多久了（`71:202`，主人没要）。
- Service Worker／自毁 SW（`15-CACHE.md:161`，一直 `--pwa-strategy=none`）。
- `assets/`／`canvaskit/` 加指纹（`PROGRESS-HISTORY.md:353,957`；产品 H2 写"待拍板"、运维 A2 写"已砍"⇒ **按已砍**，不再当待办）。
- "部署时预热字体"（实测理由不成立，#64 并进 #59，`61-WEB-PERF.md` §五）。
- 上一代实现（`services/`、`apps/mobile/`、`packages/protocol/` ＋ 四个旧脚本）。
- 小程序**原生**（iOS）运行时（手册 §7.3 明说不建，`75:36`）。
## 七、已放宽 / 暂时不做的（如实说，别写成待办）
- #66 磁盘配额 · #67 闲置回收 · #68 采样与分级闸 · #69 CPU 上限 ＋ 容量算式：⏸️**放宽**，不是砍（`00-PROGRESS.md:142-143,165-168`）；主人说"回头弄性能"再排。
- 准入／容量判据**算不出来**：本机每层 `memory.max=max`，如实说（`75:156`；`admission.js:13-19` 自认）。
- #30 线上口令泄露：主人拍板"不换"＝已知并接受的风险，下一个人不必再提（`00-PROGRESS.md:181`）。
- 语音一次最多一分钟：上游内测限制，服务端说 `asr/capped`（`71:200`）。
- iOS 音频图可能被挂起：本机验不了 iOS，先记账（`71:203`）。
- "字体指纹 404、白屏级"：本机与公网都 200（`web/assets/fonts/MaterialIcons-Regular.082c3415.otf`），**复现不了**，不是待办（`75:159`）。
- 真实模型调用／计费：要有额度的真钥匙 ⇒ 只作 P2-3/M2/M3 的观察项，不单列（`75:155`）。
- VPS nginx 那一跳／"线上＝仓库某版"不可复核：仓库外、`web/` 被 `.gitignore:30` 忽略 ⇒ 只按 P1-17 记账（`75:158`）。
- 批 5"语音本体"：09-21 砍过；要不要继续，等 M1 一句话（`63:50`）。
## 三·补、配置页那四样**定下来之后**新长出来的活（2026-09-24 主人定了四个 tab）
> ⚠️ 这一节是**执行中发现的**，按规矩补进计划（不许只活在对话里）。它与 §四（要主人拍板）连着。

- ✅ **P1-27 前半（图片生成那一条路）**：2026-09-24 主人定"**图片用 seedream（火山）**"之后**当天做完** —— 配置页「图片」那一屏有「试一张」，真打火山、真把图画出来；端点与模型名可配，线上真打一次已取证。▶ 见 `79-CREDS-TABS.md` §九。
  ⚠️ **后半（与聊天打通）**：要一条工具 ＋ **"他明说才许生成"**那条闸 ⇒ **B12** 等主人一句话。
- ❌ **P1-28 视频生成那条路 —— 砍掉，不做**（主人 2026-09-24：*"图片需要打通，视频不需要。"*）。
  那一屏的话已改成**明说"这条路不做"**（不许说成"还没接上" —— 那听着像在排队）。
  ⚠️ 备查（万一以后要做）：
- ~~**P1-28 视频生成那条路接通**（主人 2026-09-24 已定：**seedance · 也是火山**）~~：`HUPO_VIDEO_KEY` 今天仍然只是收着。
  ✅ **形状已查证**（2026-09-24）：火山方舟把它做成**异步任务** ——
  官方 API 参考里有「**创建视频生成任务**」（`/docs/ark/create-video-generation-task-api`）、
  「**查询视频生成任务**」（`/docs/ark/get-video-generation-task-api`）、
  「查询任务列表」、「取消/删除任务」四页 ⇒ 与图片那条**不是同一个形状**（图片是"一次请求一张图"，
  视频是"交个任务 → 轮着问它好了没有"）。
  ⚠️ 那几页和图片那页一样是**前端渲染**的（抓回来只有目录）⇒ 参数以**真打一次的回话**为准
  （与 P1-27 同一套做法：端点/模型名**可配** ＋ 真打取证）。
  ⚠️ **展不展示**：客户端今天**没有任何播放能力**（`Image.network` 之前也是 0 命中）⇒
  第一批先给**"做好了，点开看"**（一个链接 ＋ 一句实话），**嵌入播放**另记一条。
  中-大；助手。
- **P1-29 租户那半"按人一份的语音钥匙"**：把语音三样也推进盒子 ＋ 盒子那边 `writeKeyFile` 改成**合并**（现在整份重写）⇒ 要改**产品层**并重新发布；中；**助手出补丁 ＋ 主人签字**。[B10]
- ⚠️ **优先级**：P2-1（语音本体走哪条路）→ P1-23/P1-24 那条链仍然**排在前面**；上面三条**不挤压**它。

## 八、这一份与 75/74 的关系 · 依赖图（谁必须在谁之前）
- **关系**：`75-ACCEPTANCE.md`＝五席三轮合并定稿（对外只看它）· `74-ACCEPTANCE.md`＝上一轮参考（读数已过期，`75:9-10`）· 本份＝把 75 的 §五/§六/§七 ＋ 产品线/运维线两份清单合成"谁先谁后"，不新增事实。
- **必须先于**：P0-1 收尾之前不许开新批；**P0-2 必须先于 P1-1**（tab 边界句依 tab）；P2-1（走哪条路）→ P1-25 → P1-24 → P1-23；P0-7/P1-19/P2-4 都要"改完一次重建"，重建命令只有主人能跑。
- **重复合并**（同一件事在两线各出现一次）：产品 A1/A4＝运维 A1 · C1＝B5 · B2＝G8 · H9＝B3 · H11＝B4 · H3＝D6 · H4＝D7 · H5＝C2+C3 · E7/E8 合成 P0-1。
- **缺口**：**工程线没交稿** ⇒ P1-9/P1-10/P1-11/P1-12 只有产品/运维线的转述，缺工程线自己的排期与判据；H2 vs 运维 A2 的口径冲突已按"已砍"裁决（见 §六）。
