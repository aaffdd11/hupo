# 手册变更记录

> **规则**：`docs/handbook/` 是**唯一权威**。任何结论的改动都要在这里记一笔。
> **定档之后**（见 v1.0）：改动必须**升版本**，并在下面写清"改了什么、为什么"。
>
> 为什么要有这一页：这套手册的价值全在"**它是唯一说法**"。
> 一旦同一个问题在两处有不同答案，它就退化成又一份参考文档。

> ## v1.88 · **过程四档 ⇒ 两档：砍掉「安静」与「步骤流水」**（2026-09-26 · 主人原话 *「名不副实的要去掉。」*）
>
> **改了什么**：
> ① `01-PROJECT.md` **§6.3** 与 `03-DEVELOPMENT.md` **§4.4** 的可见性表：**四档 ⇒ 两档**
>    （留 **在做什么**（默认） / **推理原文**；砍 **安静** / **步骤流水**），并写明各为什么；
> ② `03-DEVELOPMENT.md` **§3.2** 那三行的「什么意思」列跟着改（`step/detail` 注明它
>    **今天只为老客户端存在**）；
> ③ `05-DECISIONS.md` **D7 组**新增 **`D7·补`** 一行（本次收窄的原文与边界）；
> ④ `04-ROADMAP.md` 提到「过程四档」的地方**保留当年读数**，各加一条**带日期的更正**；
> ⑤ 契约新增 [`dev/122-TWO-PROCESS-LEVELS.md`](../dev/122-TWO-PROCESS-LEVELS.md)；
>    旧的 [`dev/26-PROCESS-LEVELS.md`](../dev/26-PROCESS-LEVELS.md) 顶上加了指向它的更正。
>
> **为什么**：重做之后的**工具行**（`tool/call`：名字 · 人话标题 · 成败 ·
> 展开看入参输出，而且**落盘、切回来还在**）已经把「步骤流水」承诺的事说得
> **更准更全**，那一档只剩一串粗粒度、瞬态的重复行；而「安静」**只掐掉
> 「它正在做…」那一行**、**工具行不受档位管** ⇒ 它做不到它名字说的事。
>
> 🔴 **协议一个字都没改**（手册纪律 2：上线即冻结）：`?level=quiet|doing|steps|reasoning`
> 四个 wire token 仍然冻结，服务端 `PROCESS_LEVELS` / `LEVEL_EXTRA_TYPES` 与
> `step/*` 的语义**一个字节没动** —— 砍的只是**客户端菜单上的两个入口**，
> 新客户端不再发 `quiet` / `steps`；**老客户端照旧**。
> 🔴 **`D7.4` 一条没松**：推理原文仍然**只有 `reasoning` 档**服务端才发
> （隐私闸不能挪到客户端判）。
> 🔴 **老设备迁移**：盘上存着 `steps` / `quiet` ⇒ 读出来**归一到 `doing`**
> （留着它，菜单上就没有任何一项是选中的 = 页面在说假话）。
>
> **判据**：`test/unit/process_levels_test.dart`（两档 · 四个 token 冻结 ·
> 老档归一 · 坏值不抛）· `test/unit/process_level_store_test.dart`（盘上老档 ⇒ `doing`）·
> `test/unit/stream_uri_test.dart`（只发两个字面量）·
> `test/widget/process_levels_test.dart`（菜单恰好两项 · 老档选中「在做什么」·
> `reasoning` 档下 `step/*` **收了不画**、推理原文照旧画）·
> `test/widget/accessibility_test.dart`（切换面板只有两行 ＋ 五档不溢出）·
> `v2/services/core/test/process-level.test.js`（**原样没改**：老客户端 `steps` 仍收 `step/*`）。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.87 · **排队看得见、撤得掉：§2.2 加 `queue/changed` 与客户端帧 `unsay`**（2026-09-26 · 接着「聊天窗口重做」第二批）
>
> **改了什么**：
> ① `08-SPEC.md` **§2.2** 新增一行：下行**瞬态**事件 `queue/changed`
>    （`{type, items:[{messageId,text,at,truncated}], count}` ＋ `scopeId = 那一间`）
>    ＋ 上行客户端帧 `{"t":"unsay","messageId":"m_…"}`（**走 `/api/stream`，不新开 HTTP 路由**）；
> ② 服务端新 `src/queue.js`（那一帧的**形状与认法**唯一出处）＋ `src/dispatcher.js` 的
>    `queueItems` / `onQueueChanged` 出口 / `unsay()` ＋ `src/worlds.js` 把它接到**那一间的视图**上
>    ＋ `src/server.js`（认那一帧、并在 `client/hello` 那一刻现发一份**快照**）；
> ③ 客户端 `lib/models/chat_queue.dart` · `lib/models/queue_words.dart` · `lib/widgets/queue_strip.dart`
>    ＋ `timeline.dart` / `chat_controller.dart` / `stream.dart` / `chat_screen.dart` 接线。
>
> **为什么**：主人在琥珀还在做上一件事的时候又发一句时，服务端**早就**把它排队了
> （`dispatcher.js` 的 `#delivered` 票；DSH 实测 `agent/inbox/spliced {target:"next-turn"}`），
> 可屏幕上**一个字都没有** —— 他既看不见自己还排着什么，也撤不掉。
> 研究（`docs/dev/115-DSH-WINDOW-PARITY.md` §一.5 · `115-raw/B-render.md` §3.3）里 DSH 的形状是
> **QueueDock**：一条内联、两条以上折在计数抬头后面、每行可撤 —— 这一版把它补上。
>
> 🔴 **瞬态 ＋ 进程内**：队列是**按房间、住在进程里**的 ⇒ 进程/host 一重启就没了。
> 这是**如实的行为**（DSH 自己的文档也写着：控制面那个基线**不能跨宿主重启重建**），
> 不是缺陷；⇒ 刷新/重连之后靠 `client/hello` 那一刻的**快照**重建。
> 🔴 **撤一句只在"还没被认领"时有效**：已经在跑的那一句**没有错误面**（什么都不做、回一份新快照）。
> 🔴 **这一批只报不改**：FIFO、票/认领、`pendingDeliveries`、N19 收口**一个字都没动**。
>
> **判据**：`v2/services/core/test/queue.test.js`（Q1–Q7，每条带反例）·
> 客户端 `test/unit/chat_queue_test.dart` · `test/widget/queue_strip_test.dart` ·
> `test/widget/accessibility_test.dart`（五档不溢出 ＋ 命中区 ≥44）· `docs/dev/117-QUEUE-VISIBLE.md`（契约）。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.86 · **「首先全部开放，聊天窗口的设计也要重做」：§2.2 加四条事件（工具行 / 结果 / 每轮用量 / 系统提示词）＋ D1.1 在聊天窗口上放开**（2026-09-26 · 主人亲口定的产品形状）
>
> **改了什么**：
> ① `08-SPEC.md` **§2.2** 新增四条**持久**事件 —— `tool/call` · `tool/result` · `turn/usage` · `system/prompt`；
> ② `05-DECISIONS.md` **D1.1·补** —— 那条「界面上出现内部词＝缺陷」在**聊天窗口**这一格上**放开**（其余界面一个字没松）；
> ③ 服务端 `src/tool-rows.js`（新，纯函数：标题 / 有界入参 / 有界输出 / 用量折叠）
>    ＋ `src/session-translate.js`（把这四帧接上，**持久**）。
>
> **为什么**：主人 2026-09-26 原话 —— *「首先全部开放，聊天窗口的设计也要重做。」*
> 背景是同一轮的研究（`docs/dev/115-DSH-WINDOW-PARITY.md`）：DSH 的窗口把**每一次工具调用**都摊成一行
> （名字 · 一句话标题 · 成败 · 原样输出），而我们改前只发「在查资料 / 在写」这么一个词
> —— 那是**信息损失**，不是文风。⇒ 这一版把它补回来。
>
> 🔴 **有界而且如实**：标题/入参/输出都有上限，超了**截断并且如实报** `bytes`/`truncated`（不许假装完整）。
> 🔴 **用量宁可不画也不许给半个**：任何一次尝试没报准 ⇒ `usage:null, complete:false`（DSH 同一条规矩）。
> ⚠️ **推理原文与 `step/*` 仍然一个字节都不落盘**（那条泄漏闸**只改了另一半**：工具行从"不许落盘"改成"必须落盘"）。
>
> **判据**：`v2/services/core/test/tool-rows.test.js`（新 6 条）· `test/process-level.test.js`（泄漏闸改成**两件事同时成立**）。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.85 · **用户端没有"包"：§十四 加一条 —— 他自己那一份不落版本快照、不查尺寸/文件数**（2026-09-26 · 主人亲口定的产品形状 D4.21）
>
> **改了什么**：`08-SPEC.md` **§十四** 新增 **14.6「用户端 vs 包：上限只在『包』那一侧』**；
> `05-DECISIONS.md` O 组新增 **D4.21**。（协议一个字没动：`/api/apps` 的字段语义照旧。）
>
> **为什么**：真机现场 —— 助手在主人的「奥数小站」里撞上一堵墙，停在岔口上等他挑
> （*"这一页现在 246,522 字节，离 256KB 的那条线只剩 15,622 字节……一条页面根本装不下"*）。
> 追下去：我们把**"包"的规矩**（不可变版本 ＋ 单文件 256KB／总 2MB／40 个文件）
> **挂在了用户端**。主人 2026-09-26 原话：
> *"所谓的版本快照，只在市场中存在。不在用户端。用户 a 创建的，在他自己那里就是一段源代码部署……
> 所以不需要什么压缩。"*
>
> 🔴 **用户端**（他自己那一份）：内容住在**工作区**（一个图标 = 一间房），桌面点开的就是它
> ⇒ 写工作区 ＋ 登记（`hupo/apps/<id>/app.json`）**不查**尺寸、不查文件数、**不落** `versions/`；
> 小程序那几条工具的 `files` 变成**可选**（他/助手写在那一间目录里的就是它）。
> 🔴 **包那一侧**（发到市场 / 从市场装来）：那三道上限**原样有效**，一个都没放宽
> —— 发布时才把"他现在那一份"打成一版，打不成如实拒（而且**他自己那份照旧能用**）。
> ⚠️ **旧的（只有包的那一份）行为一个字没变**：`meta()` 两者都读，装来的 / 发过一版的照旧按包走。
>
> **判据**：`v2/services/core/test/app-user-side.test.js`（V1／V1·反着验／V2／V2·反着验／V3／V4 · **四刀变异各自当场红**）·
> `test/app-live-update.test.js`（"没做成要有一条人话"改成**发布那一步**触发 —— 用户端那三句不该再出现了）·
> `docs/dev/114-APP-USER-SIDE-NO-LIMIT.md`（契约 ＋ 真机读数）。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.84 · **§2.2 把 `app/update-available` 从"还没有"改成事实：制品换了一版 ⇒ 正开着它的界面自己换上**（2026-09-26 · 主人亲口定的产品形状）
>
> **改了什么**：`08-SPEC.md` **§2.2** 那一行里 `app/update-available` 从"**另两条还没有**"
> 改成 ✅ 已实现（`app/error` 仍然标着还没有）。该帧：`{type, id, version, at}`，
> **持久**（有号、落盘、重连与切回那一间都补得上），`scopeId` = 那个 app
> ⇒ 只有正开着这一间的那条连接实时收得到。
>
> **为什么**：主人 2026-09-26 原话 ——
> *"小程序那边，我希望是一种前后端完全同步的模式，也就是说后端增加内容前端显示是一一匹配的。不需要刷新。"*
> 这一版做的是**制品版本**那一半（"后端数据实时取"那一半见 v1.83 的 `app/workspace-changed`）。
>
> 🔴 **帧里没有签名 URL**：`entryUrl` 是短时效的（绑人 + 绑版本 + 现签），
> 落进那条日志就是一句**迟早变假的话**；客户端拿 `id` 自己去 `/api/apps` 现取。
> 🔴 **触发点是 `Apps.create()` 那一个漏斗**（所有写路都经过它）＋ **写完之后**才喊
> （早一步喊，客户端可能拿到上一版 ⇒ 屏幕上什么都不会换）；第一版不喊。
> ⚠️ 顺带补的：**小程序那几条工具"没做成 / 被拒"必须有一条他看得见的话**
> （真机现场：助手撞上单文件上限、说了句"我试几种压法"就没了 ⇒ 他那边一点提示都没有）——
> 走现成那条通知通道（`kind: failed`），文案**只从一张表里出**（工具原文有内部短词，不许上屏）。
>
> **判据**：`v2/services/core/test/app-live-update.test.js`（U1 ＋ F1–F3）·
> `v2/apps/mobile/test/{unit,widget}/mini_live_update_test.dart`（U2–U5）·
> `docs/dev/111-APP-LIVE-UPDATE.md`（契约 ＋ 真机读数）。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.83 · **桌面那一格打开的是"他正在改的那一份"：§2.1 加 `/w/<id>/<path>` ＋ §2.2 加 `app/workspace-changed`**（2026-09-26 · 主人亲口定的产品形状）
>
> **改了什么**：
> ① `08-SPEC.md` **§2.1** 加一行 `GET /w/<id>/<path>`（**另一个原点**）—— **那一间工作区里"现在那一份"**的字节；
>    并把 `/api/apps` 那一行的 `entryUrl` 写成"**活地址**"（字段语义没变：仍是"打开这个 app 的那条签名 URL"）。
>    `/a/<id>/<version>/<path>` **一行没动**（某一版不可变快照 = 发布 / 血缘 / 回滚那一侧）。
> ② `08-SPEC.md` **§2.2** 加一帧 `app/workspace-changed`（**瞬态**，只推给正开着那一间的那条连接）。
>
> **为什么**：主人 2026-09-26 原话 —— *"小程序做好了，发布到桌面，这个对于用户自己用的应该是不需要这样的。
> 因为他自己开发过程原则上就是对**当前这个版本**进行内容修改、功能修改等等……只不过它跟原来的那个版本
> 不一样的话呢，对于市场来说它可能是有更新，用户可以重新去更新，否则的话用户不需要走这条发布的通道。"*
> ⇒ **他自己那一份**（桌面图标）打开的是**活的工作区**；**"发布 / 版本快照"只留给"市场 / 别人"那一侧**
> （`app_publish` / 血缘 / 回滚 /「发现」照旧）。真机读数也逼着做：主人那三个老 app
> （`dice`/`wenda`/`coin`）**根本没有工作区目录** ⇒ 不给"先从当前那一版落成一次"这一刀，它们一点开就取不到东西。
>
> 🔴 **签名照旧**（同一把键 / 同一个 TTL / `signEntry`+`verifyEntry` 四道验都不变），只是 payload 里
> "取的是哪一份"那一格用**哨兵 `live`**（顺带域分离）。🔴 **白名单两道**（验签后碰库前一道、`readLive` 里再一道）：
> `.` 开头的与 `build/` 一律拒。🔴 **以盒子为准**：租户经现有隧道读他盒子里那份；盒子不通 ⇒ **如实 503**。
>
> **判据**：`v2/services/core/test/app-live.test.js`（V1–V6 ＋ `/api/apps` 现签那半 ＋ 接线那一根线）·
> `test/apps-route.test.js` / `test/apps-box.test.js`（跟着改成活地址那条路）· 客户端
> `test/unit/mini_workspace_change_test.dart` ＋ `test/widget/mini_workspace_live_test.dart`（V5）·
> `docs/dev/112-OWN-APP-IS-LIVE.md`。
> ✅ **客户端那半已经接上**（复用 `#159` 的 `MiniAppFrame`）：`mini_update.dart` 加 `AppWorkspaceChange` ·
> `chat_controller.dart` 加一条**瞬态**分支（举手、`return`、不喂时间线）· `chat_screen.dart` 加
> `_onAppWorkspaceChanged`（**不判版本**，只重取"正开着的那一个"）。客户端改了 ⇒ **要 `deploy-web-v2.sh`**，
> 而且它与服务端**一起发**（只发服务端的话，老客户端打得开活地址但不会自己换帧）。
> ⚠️ 改的是 **strict** 文件（`docs/handbook/**`）⇒ **要重建开机清单**。
>
> ## v1.82 · **§2.1 加一条 `/api/room-remove`：按房间回收一间"没有制品"的工作区（B28）**（2026-09-25 · 主人说"空的工作区要清理"）
>
> **改了什么**：`08-SPEC.md` **§2.1** 加一行 `POST /api/room-remove`（body `{scope}`）。
> 它按 **scope**（不是 app id）把那三样拿走 —— **工作区目录** · **那一间的对话记录** ·
> **助手那边那一间的会话记录** —— 并写 `reclaimed.json` 留痕（N22 的唯一例外）＋ 审计一行；
> **不动制品库**（留痕里 `items.app` 如实 `false`）。
>
> **为什么**：`/api/app-remove` 按 **app id** 走 ⇒ 一间**还没做出东西**的工作区
> （没有制品，或制品早被删了）**删不掉** —— 只能永远躺在盘上，而**开发者入口那条清单
> 会把它如实列出来**（`#152` 真机一口气见到十来个）。主人 2026-09-25 说 **"空的工作区要清理"**
> ⇒ 补这一条口（B28 记的正是"要按工作区回收就得另开一条口"）。
>
> **四道闸**（缺一条就有"按错名字把别人的房间删了"的形状）：`main` 拒 ·
> **内置那几间**（`settings`/`discover`/`harness`）拒 · **制品库里还有它** ⇒ 拒
> （那条要走 `/api/app-remove`，它连制品一起搬）· **认不出这一间** ⇒ 404 ＋ 一句人话。
> ⚠️ **以盒子为准**：租户这一下在他盒子里落，盒子不通 ⇒ 如实 503。
>
> **判据**：`v2/services/core/test/room-reclaim.test.js` · `test/route-shape.test.js`（新路由进表）·
> `docs/dev/103-APP-DELETE.md` §七 · `docs/dev/77-BLOCKERS.md` B28。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.81 · **人格加第 11 条（守正出奇）＋ §2.2 把"派活 / 问话"那几帧补进表**（2026-09-25 · 主人亲口加的）
>
> **改了什么**：
> ① `08-SPEC.md` **§12.4 第 11 条**：**守正出奇** —— 按主人最正的那句话办，**不许绕开它去找一件更好做的**
>    （不许偷换 / 不许只做一半 / 不许拿旁边那一步顶上）；**发现自己偏了当场说**；**做不到就直说**；
>    ⚠️ **不许因为怕他不高兴就不说**（"报坏消息不挨骂，瞒才挨骂"）。条数三处对齐（**11**）。
> ② `08-SPEC.md` **§2.2**：把**已经在跑但表里没有**的那几帧补上 —— `job/start` · `job/report`
>    （**持久**，落在**那条**可见日志上）· `scope/open`（**瞬态**，界面自己切过去）·
>    `job/ask` / `job/ask-expired` / `job/answer-ack` ＋ 客户端帧 `{"t":"job-answer",…}`（**问一句才动手**：
>    **没答之前什么都不建**；超时**作废**、**不是默认答应**）· `app/open`（**做完自动打开**，只推给正开着那一间的那条连接）。
>    ⚠️ **没有新增任何 `/api/…` 路由** ⇒ §2.1 那张表不动。
>
> **为什么**：① 主人 2026-09-25 亲口说 *"你有一个需求，你做不到的时候，你就会绕开这个需求去找其他东西，
> 我需要的是守正出奇……如果你做的行为跟他有偏差，你要能发现……你千万不要害怕告诉我真话"* —— 那是**人格**，
> 不是一次性的交代。② 那几帧 2026-09-25 已经上线（`#142`/`#108`），**协议字段一旦上线就冻结** ⇒
> 手册里缺着它们，下一个人就会照"表里没有"去改第二遍。
>
> **判据**：`docs/dev/09-PERSONA.md` · `v2/services/core/test/persona.test.js` · `bash scripts/check-persona.sh`
> （**两道新检查**：`守正出奇` 在 ＋ `不许因为怕他不高兴就不说` 在）· `docs/dev/102-APP-BIRTH-SCOPE.md` ·
> `docs/dev/108-JOB-ASK-FLOW.md`。
> ⚠️ 改的是 **strict** 文件（`docs/handbook/**` ＋ `hupo-persona.yml`）⇒ **要重建开机清单**。

> ## v1.80 · **「奥数题」这个内置功能从产品里去掉（D4.20）**（2026-09-25 · 主人拍「乙」）
>
> **改了什么**：`05-DECISIONS.md` 新增 **D4.20** —— 桌上那一格 · 它那一屏 · 它那几句文案 ·
> 客户端那一套常量 · 服务端 `BUILTIN_SCOPES` 里的 `'math'` **一起去掉**；
> ⇒ **内置那几格只剩三个**（设置 / 发现 /「我自己那台」），`D4.14` 那份清单按这条读。
>
> **为什么**：主人 2026-09-25 说 *"删除奥数题这个小程序。"*；问准之后他选的是
> **从产品里去掉这个功能**（不是"收起来"、更不是删数据）。
>
> ⚠️ **不动的**：盒里 `math` 那一间**已有的数据一个字节都不动** —— 那是**他说过的话**；
> 移除功能**不等于**销毁历史。代价如实认下：那一间**从此进不去**（要清得单独说）。
>
> **判据**：`docs/dev/105-DROP-MATH.md`（M1 源码级 0 命中 · M2 桌上只剩三格 · M3 `math` 不再是合法房间 ·
> M4 两闸绿 · M5 数字收成一处）· `node scripts/check-docs.mjs` · `cd v2/services/core && npm test` · `bash scripts/check-client.sh`。
> ⚠️ 改的是 **strict** 文件 ⇒ **要重建开机清单**。

> ## v1.79 · **桌面那个面板多两项：改个名字 / 复制一个（§2.1 加两条路由）＋ 手机那条"按住"**（2026-09-25 · 主人实测后定）
>
> **改了什么**：`08-SPEC.md` **§2.1 接口表**加两行 —— `POST /api/app-rename`（`{id,title}`）与
> `POST /api/app-copy`（`{id}` ⇒ 新的一格：新 id · 字节一模一样 · 标题「X 副本」· **新那一间是空的**）。
> 两条都照 `/api/app-remove` 那条已有的规矩：**以盒子为准** · 盒子不通 ⇒ 如实 503 · 认不出 ⇒ 404 ＋ 人话 · **各留审计一行**。
>
> **为什么**：主人 2026-09-25 在 `103` 上线之后实测，要这个形状 ——
> *"一个选项，跟右键点击效果类似，但是只有重命名，复制小程序，删除小程序。这三个选项。"*
> ⇒ 那一格右键/按住出来的是**三项菜单**（契约 `docs/dev/104-APP-MENU.md`）。
> ⚠️ **"删除**小程序**"不能照字面写上屏**：「小程序」是上屏禁用词（`06` 禁令 4，硬闸一直扫它）
> ⇒ 菜单上仍是「**从桌面上删掉**」。
> ⚠️ 同一批修掉主人报的**真缺陷**：**手机上按住那一格，它反而被点开了** ——
> `InkWell.onLongPress` 要"满 500ms 且手指不挪出 18 逻辑像素"，真手指两样都容易失手，
> **一失手松手就照旧算点了一下** ⇒ 改成**自己按住**（`Listener` ＋ 计时器 ＋ 更宽的容差），
> 到点出菜单、并把这一次点击**吞掉**。这是**只在真手指上现形**的一条（合成事件测不出来）。
>
> **判据**：`docs/dev/104-APP-MENU.md` §四（客户端 C11–C16 · 服务端 S10–S13，每条要变异验证）。
> ⚠️ 这次改了 **§2.1**（strict）⇒ **要重建开机清单**。

> ## v1.78 · **从桌面上删掉 ＝ 真回收 ＋ 二次确认（D3.11）＋ N22 的唯一例外（回收留号洞，必须留痕）**（2026-09-25 · 主人拍板）
>
> **改了什么**：① `05-DECISIONS.md` 新增 **D3.11**（主人原话：*"点击删除，也会提醒用户，回收相应的工作区、经验、数据。需要用户二次确认。"*；
> 三个追问全拍「甲」：真回收 ·"经验"＝那一间的对话记录 · 二次确认照回收站那个弹窗）；
> ② `02-ARCHITECTURE.md` 新增 **§5.1·补** —— **N22 的唯一例外**：主动回收会把那一间的行从日志里拿走 ⇒ **留号洞**，
> 而且**只有这一种**允许有洞，且**每个洞都要在 `reclaimed.json` 里找得到解释**；
> ③ `03-DEVELOPMENT.md` 的 N22 那一行跟着改（"无空洞"后面点明这个例外与它的判据）；
> ④ `08-SPEC.md` **§2.1 接口表**里 `/api/app-remove` 那一行按新语义重写（它是一次**真回收**：一起拿走什么 · 留下什么 · 会留号洞 · 界面上要**二次确认**且必须说清）。
>
> **为什么**：主人 2026-09-25 把"删掉"的语义定死了 —— 一个图标 ＝ 那一间（工作区 ＋ 对话 ＋ 数据），
> **删它就得把那四样一起拿走**，而且要**先提醒、再要一次确认**（破坏性动作不许手滑就触发，照 §8.2 的老规矩）。
> ⚠️ **为什么必须动 N22**：那一间的对话就住在**同一条** `main.jsonl` 上（P-l：一条日志、作用域只是标签）
> ⇒ "真回收"与"无空洞"**只能二选一**；用"重编号"绕开会让所有客户端的游标错位（要么漏、要么重放一屏），
> 用"清空正文留空壳"又不是真回收 ⇒ 取**允许留洞 ＋ 强制留痕**，并让校验器**能带留痕地判**。
> ⚠️ **留下的两样**（审计一行 · 用量/记账）**不许跟着没** —— 那是审计底线。
>
> **判据**：`docs/dev/103-APP-DELETE.md` §七（客户端 C7–C9 · 服务端 S6–S9，每条都要变异验证）；
> `node scripts/check-docs.mjs` · `cd v2/services/core && npm test` · `bash scripts/check-client.sh`。
> ⚠️ 这次改了两份 **strict**（`05-DECISIONS.md` · `02-ARCHITECTURE.md` · `03-DEVELOPMENT.md`）⇒ **要重建开机清单**。

> ## v1.77 · **§6.7 加第 8 条：桌面图标上的右键菜单是我们自己的 ⇒ 网页上要关掉浏览器那份**（2026-09-25 · 主人签字"关掉"）
>
> **改了什么**：`08-SPEC.md` **§6.7 实现约束**加第 8 条 ＋ 底下一段边界说明。
>
> **为什么**：`103` 让桌面图标右键／长按能"从桌面上删掉"。而**引擎默认开着浏览器自己那份
> 右键菜单**（`browser_context_menu.dart` 原话：*"On web, by default, the browser's context
> menu is enabled and Flutter's context menus are hidden."*）⇒ 右键图标时它**盖在我们面板上面**，
> 右键删除在网页上**根本点不到**。主人 2026-09-25 拍"关掉"。
> ⚠️ 边界：**只关这一层壳**（小程序那层是独立 iframe，它里面的右键照旧）；
> 代价是输入框里右键粘贴没有了（`Ctrl+V` 照旧）。判据 = `test/unit/web_shell_test.dart`（配置断言）。
>
> ⚠️ 顺带**修一处排版缺陷**：`v1.76` 那一条原来插在 `v1.73` 与 `v1.72` **之间**
> （破了"最新的在最上面"）⇒ 挪回它该在的位置（`v1.77` 之下、`v1.75` 之上）。
>
> **判据**：`node scripts/check-docs.mjs` · `cd v2/apps/mobile && flutter test test/unit` · `bash scripts/check-client.sh`。

> ## v1.76 · **删掉一个小程序：`POST /api/app-remove`（接口表加一行）**（2026-09-25 · 主人点名的典型情况）
>
> **改了什么**：`08-SPEC.md` **§2.1 接口表**加一行 `POST /api/app-remove` —— 从桌面上删掉一个小程序。
>
> **为什么**：主人 2026-09-25 要"右键／长按桌面图标就能删"。服务端其实**早就有**软删
> （`Apps.remove()`：挪进 `.removed/` ＋ 审计，MCP 那边是 `app_uninstall`），但**客户端没有路**。
> ⇒ 加这一条路由，并且照 B15／B17 那条既有的规矩：**以盒子为准**（租户这一下在他盒子里落，
> 盒子不通就**如实 503**，**绝不**去动宿主那份）。
>
> ⚠️ **一条诚实的边界写进了表里**：它是**软删**，但**今天没有"拿回来"的入口**
> ⇒ 界面上**不许**说"还能拿回来"（想恢复就先做入口）。契约 `docs/dev/103-APP-DELETE.md`。

> ## v1.75 · **§3.5 的「未决」按 D10.4 收口 · 人格硬规则条数收成一处 · §12.4 第 9 条按 D10.1 改成实话**（2026-09-25 · 助手审计后对齐）
>
> **改了什么**：① `02-ARCHITECTURE.md` **§3.5** 里那句「**【未决】见 §七 不一致 3**」删掉 ——
> 按 `05-DECISIONS.md` **D10.4** 写成**已拍板**（「挪走」＝**先收口** · 「转交」＝**不收口、同一条继续写**；
> 两者**语义相反**，但**共用一个状态标记**这件事实现时**必须拆开**，契约 `docs/dev/100-DISPATCHER-D.md`）；
> **§7.3** 那张"未决"表**随之作废**，并补一句 **D 期在建**（`handoff_to` 加在**账本那条 MCP** 上）。
> ② **人格硬规则条数三处收成一处**：数 `08-SPEC.md` §12.4 正文 = **10 条** ⇒
> `hupo-persona.yml` 文件头那行注释「九条」改「十条」（**只动那一行注释，人格正文一个字没动**）·
> `docs/dev/09-PERSONA.md` 里的「9 条 / 九条」改「10 条 / 十条」。
> ③ `08-SPEC.md` §12.4 **第 9 条**原来写「被打断的活会自己接着做」——
> **与 D10.1 相反**，按事实改成**两种说法**（**只碰过只读工具的** ⇒ 外面会自己重做；**动过东西的** ⇒ 不会自己重来）。
> ④ `08-SPEC.md` §12.2 补一段：账本那条 MCP 上挂的工具（含**只读**的 `work_status` ＋ 🚧 在建的 `handoff_to`）。
>
> **为什么**：§3.5 那句是**过时**（D10.4 早拍过，只是手册没跟着改）·
> 条数三处不一 = 「**同一份数字不许有两处**」那条纪律被破 ·
> 第 9 条与 D10.1、与人格里"动过东西的不会自己重来"**互相矛盾** ⇒
> 手册与代码冲突时**先把事实核清，再改手册**（`AGENTS.md` §二）。
>
> **判据**：`node scripts/check-docs.mjs` · `bash scripts/check-persona.sh` · `cd v2/services/core && npm test`。
> ⚠️ `hupo-persona.yml` 是 **strict** ⇒ 改它的那一行注释**要请主人补一条重建命令**
> （命令别手打，照 `verify-integrity.mjs` 打出来的那条粘）。

> ## v1.74 · **小程序「以盒子为准」进手册 ＋ 盒里真能跑命令**（2026-09-25 · 助手审计后对齐）
>
> **改了什么**：① `08-SPEC.md` **§2.1 接口表**给 `/api/apps`、`/api/app-ask` 补上**以盒子为准**
> （租户读**他盒子里那一份**；**盒子不通 ⇒ 如实 503**，**绝不退回宿主那份**），
> 并**新增一行**制品口 `/a/<id>/<version>/<path>`（**独立原点** · **先验签再碰盒子** · 失败也**不退回宿主那份**）。
> ② **§14.4 制品管线**加一行同一条铁律。③ **§13.1 容器验收**加 **V1c**：
> **盒里那条 `bash -c` 真跑得起来**（＋负向对照 `python3` 仍不在），并写明**沙箱一个字没动**
> （盒里 landlock 本来就是 `full`）。
>
> **为什么**：小程序曾经有**两处库**（桌面读宿主、助手写盒里）⇒ 主人新做的**永远不上桌面**
> （页面在说假话）；主人 2026-09-25 拍定**以盒子里那份为准**（B15/B17）。
> 而 B18 的上一版"盒里能跑命令"是**量在宿主上**的假绿 ⇒ 真根因是盒里**没有 shell**
> （补 `/bin/bash` ＋ 静态 busybox；**不是**动沙箱）。两条都属"不写进手册、下一个人就不知道"。
> ⚠️ **一处如实标出**：制品口盒子不通时今天回 **403/404**（`/api/apps` 与 `/api/app-ask` 是 503）——
> 各出口状态码**不统一**，按代码写进表里，**没有替它统一**。
>
> **判据**：`cd v2/services/core && npm test`（含 `apps-box.test.js` / `app-ask-box.test.js`）·
> `bash scripts/check-container.sh`（真镜像自检）· `node scripts/check-docs.mjs`。

> ## v1.73 · **焦点路由上线：协议加的那几个字段进接口表 ＋ `/api/say` 的「归处提示」与 `/api/timeline` 的旧语义按代码写清**（2026-09-25 · 助手审计后对齐）
>
> **改了什么**：① `08-SPEC.md` **§2.1 接口表**里三行改成**落地的样子** ——
> `WS /api/stream`（`&scope=` **收窄成"初始焦点"** · 客户端→服务端帧
> `{"t":"focus","scope":"<id>","sinceSeq":<n>}` · `client/hello` **加**可选字段 `focus` ·
> 服务端回 `client/focus` · **切焦点不重连**）·
> `POST /api/say`（`scope` = 「**这一句的归处提示**」· **归处提示 ≠ 焦点 ⇒ 409 ＋ `ask:true` ＋ 一句人话**：
> **先反问、不落盘、不投递** · 重发不判）·
> `GET /api/timeline`（`?scope=` = 只看这一间；**不带 ⇒ 只看主线**）。
> ② `03-DEVELOPMENT.md` **§3.2 增补字段**补三行（`focus` 帧 · `client/hello.focus` · `client/focus`）。
>
> **为什么**：这三条都是**已经上线**的协议面，而接口表还停在 v1.72 那版（把 `&scope=` 写成"连接级"）。
> **协议字段一旦上线就冻结** ⇒ 表必须与代码逐字对得上，后来人才知道哪个字段是承重的。
> `?scope=` 那两行**原先写错过**（契约 `84` §四把 timeline 写成"不带 ⇒ 全部"）——
> 主人 2026-09-25 明确拍过"**保持旧语义**" ⇒ 按代码与那句拍板改回来。
> ⚠️ 切焦点不重连、一条连接服务所有房间，是 `02-ARCHITECTURE.md` §四 核心原则 3 与
> `05-DECISIONS.md` **P-l** 早就定过、而**代码到这一轮才落地**的两条。
>
> **判据**：`cd v2/services/core && npm test`（含 `focus.test.js`）· `node scripts/check-docs.mjs`。

> ## v1.72 · **一个图标 = 一个工作区 = 一条对话（把 `workspaces/<scope>` 那句接上）**（2026-09-25 · 主人定的形状）
>
> **改了什么**：`08-SPEC.md` **§2.1 接口表**给三条已有路由补上**可选的 `scope`** ——
> `POST /api/say`（body 里的 `scope`）· `GET /api/timeline`（`?scope=`）· `WS /api/stream`（`&scope=`，**连接级**，照 `level` 那先例）。
> **不带 = 主线 `main`**（旧客户端照旧；只加可选字段，不改任何已有字段的语义）。
>
> **为什么**：`02-ARCHITECTURE.md` §2.1 早就写着 `workspaces/<scope-name>/ ← 子工作区（= 桌面上的一个图标）`，
> 但从来没接上（实测：`/data/workspaces` 空、小程序文件躺在 `/data/main` 里、事件的 `scopeId` 恒为 `'main'`）。
> 主人 2026-09-25 定死：**一个图标 = 一个工作区 = 一条对话**，且**对话也是分家的**。
> 契约与判据（A1–A7）见 `docs/dev/83-APP-WORKSPACE.md`。
>
> **同一批还钉了"不漂"**：人格层（`hupo-persona.yml`）与 `app_create` 的工具说明里都写明
> 「**小程序 / app / 应用 只有一个意思：hupo 桌面上那个图标**」，并**点名**三种漂移
> （微信/支付宝小程序、iOS/安卓原生、给外人访问的网站）；判据两条（字面＋**真行为** `scripts/check-app-drift.mjs`）。

> ## v1.71 · **接口表补三条"从常量来"的路由 ＋ 把防漂闸扩到常量**（2026-09-24 · 助手执行 76 计划）
>
> **改了什么**：① `08-SPEC.md` **§2.1 接口表补三条在跑的路由** —— `WS /api/harness`、
> `GET /api/dev-harness`、`POST /api/dev-mode`（各带契约指针与"公网口/可信口"的边界）。
> ② `v2/services/core/test/route-shape.test.js` 的**代码侧**从"只扫 `server.js` 字面量"
> **扩成也认常量**：`ASR_PATH` · `HARNESS_PATH` · `DEV_MODE_PATH` · `DEV_HARNESS_PATH`
> —— **取不到常量名就当场红**（改名/删掉时不许静默漏掉）。
>
> **为什么**：这三条路是最近两批加的，**路径只住在常量模块里**（`src/harness-session.mjs` /
> `src/dev-mode.js`），而那条闸只扫 `server.js` 的字面量 ⇒ 它们**既没进手册、也没被闸盖住**：
> **闸是绿的，却漏了三条** —— 正是"看起来有闸、其实没有"的形状（同族：V13）。
>
> **判据**：`cd v2/services/core && npm test`（含 `route-shape.test.js`）。
> **变异验证**：临时把手册里 `/api/dev-mode` 那一行删掉 ⇒ 闸当场红并点名 `missing: /api/dev-mode`
> （还原后复绿）；另把 `harness-session.mjs` 里 `HARNESS_PATH` 改名 ⇒ 闸在"取不到常量"那一句红。

> ## v1.70 · 配置页**四个 tab** 落进手册 ＋ 接口表补三条 ＋ 画图那一支拆出来（2026-09-24 · 助手执行 76 计划）
>
> **改了什么**：① `05-DECISIONS.md` **D3.9·补** —— 主人 2026-09-24 定的形状写进决策记录：
> 四屏＝聊天 / 语音 / 图片 / 视频（界面上不写「模型」）；图片**已接通**（配置页「试一张」 ＋
> 聊天里说一句也能画，带「**他明说才许生成**」那道闸）；**视频不做**（主人说不需要，页面明说）；
> 语音三样**已接到识别路**（他自己的优先）。⚠️ 钥匙落点也写清：主人那一把在 **DSH 自己那份凭据**里，
> 其余进中心按人存档。② `08-SPEC.md` §2.1 接口表补三条：`POST /api/creds`、`GET /api/space` 的
> `creds` 那一块（**只有有没有**）、`POST /api/image`。⚠️ 老的 `POST /api/model-key` **一字未动**。
> ③ `hupo-capabilities.yml` 把**画图那一支**从小程序那条上拆成独立的 `mcp-image`
> （工具面向分开，**通道还是同一条**；要单独停画图，去掉那一块就行）。
>
> **为什么**：这三处都是「手册/能力层是唯一权威」的地方 —— 产品形状定了、接口加了、能力面变了，
> 不写进手册，下一个只看手册的人就不知道；而能力层那一处**改了必须重建开机清单**（strict）。

> ## v1.69 · `/api/health` 那一行改成**实话**（2026-09-24 · 助手执行 76 计划 P1-12）
>
> **改了什么**：§2.1 接口表里 `/api/health` 原来写「结构化健康：disk / store / agents / memory / upstream / cert」，
> 而**代码只回三个字段**（`ok` / `timelineId` / `seq`）。`grep -n "upstream\|cert" server.js` = **0**
> ⇒ 那六项**从来没实现过** —— 这一行是「文档在说假话」。
>
> **为什么这么改**（而不是去实现那六项）：手册是「要什么」，但那六项**各自是什么、谁在用**一个字都没写；
> 照空想的形状去实现 = 又一份假话。⇒ 改成**代码事实**，并把六项明标 **⏳ 没做**；
> 真要那六项就**单开一批**（新功能，每个探针都要有自己的判据）。
>
> **判据**：`v2/services/core/test/health-shape.test.js` —— 它**读手册那一行、也读代码那道分支**，
> 两边点名的字段必须一样（漂了就红）。

> ## v1.68 · **聊天窗口收成"一行"：上边框正中一条杠 + 平的小箭头 · homeicon 靠左 · 话筒进框 · 发送常驻（灰）**（2026-09-24 · 主人定的形状）
>
> **主人原话**：*"我们给聊天窗口做优化。我们做成一行，展开用一条杠，杠上面有一个小箭头，
> 箭头比较平，所以不会显得那么突兀，放在上边框的正中央，然后 homeicon 放在聊天窗口左边，
> 语音按钮放在聊天框内部的右侧，聊天框右侧应该是一个发送按钮。一开始是灰色的。"*
>
> **改了四处**：
>   1. **抓手**从上边框左端那一小截（收起态还挂着一个带字的「展开」按钮）改成
>      **上边框正中央的一条杠 + 一个"平"的小箭头**（自己画的浅角 V：26×7，不是
>      `keyboard_arrow_up` 那种尖的）；**单击 = 收起 ⇄ 展开**，竖向拖照旧改高度。
>   2. **「在哪儿说话」那个图标**（桌面 = `home`）从抓手行搬到**输入条那一行的最左边**。
>   3. **话筒**从左边那颗"话筒/键盘切换"搬进**框里的右侧**（`suffixIcon`），
>      「读出来」那个开关跟着一起住进框里；**"语音档"整个没有了** ——
>      按一下就开始听、**听到的字直接落进这个框**、再按一下结束。
>   4. **发送钮常驻**（原来"有字才画"）：没话要说时**灰的、按不动**，
>      位置与大小固定 48×48（不许随字撑开/收窄 —— D4.8 那种"界面自己抖"）。
>
> **被这一版推翻的两条**（⚠️ 都是主人自己拍的板，所以按他的新话改，**不是**我顺手改的）：
>   · **D3.8 的"必须有带字的展开入口"** ⇒ 改成"**看得见**的展开入口"：
>     位置固定 + **命中区 ≥44** + tooltip 与无障碍名（`展开`/`收起`）；
>   · **§6.2 那条"框里有字才画发送钮"**（2026-09-23）⇒ 常驻 + 灰。
>   · 顺手**拿掉了双击抓手**：它现在自己就是展开入口 ⇒ 双击 = 两次单击 = 自己抵消
>     （真机上看起来就是"点了没反应"）。
>
> **判据**：`test/widget/{desktop_floater,hearing,composer_actions,mini_app,scroll_follow}_test.dart`
> 跟着重写/改点（抓手按 `chatHandleKey` 找、命中区与"在不在正中"两条新判据；
> 语音那一份改成"字进框 / 发送钮灰 ⇄ 亮"）。**硬闸**：五档不溢出 + 命中区 ≥44 全过 ·
> `flutter analyze` 干净 · 真机截图两张（收起 / 展开）。

> ## v1.67 · **小程序打开/收回那一层：补齐阴影，起点和图标那一格一模一样**（2026-09-23 · 主人实测报的）
>
> **主人原话**：*"小程序 icon 是有圆角有阴影的。而打开和关闭的时候，那一层效果没有阴影，
> 所以开启和打开的效果并不如意。"*
>
> **根因**：圆角其实一直有（外层 `ClipRRect` 在插值），**缺的是阴影**；而阴影**必须画在
> 裁剪之外** —— `ClipRRect` 会把里面画的阴影一起剪掉。
> **修法**：图标格那三个阴影数提成 token（`design.dart` 的 `tileShadow*`，两处同源）·
> 新纯函数 `miniAppSurfaceAt(v)`（`v=0` 逐字等于图标那一格；`v=1` 圆角 0、阴影 0）·
> 在 `ClipRRect` **外面**套一层只画阴影的 `DecoratedBox`。
> 判据 **+7**（纯函数 4 · 界面 3：扩开那一帧有阴影 / 全屏没有 / 收回中段又有）
> · **变异验证**：去掉阴影那一支 ⇒ 界面那两条当场红。契约 `docs/dev/52-DESKTOP.md` §6.5。

> ## v1.66 · **退出小程序时不许先闪到「设置」那一屏**（2026-09-23 · 主人实测报的）
>
> **主人原话**：*"在小程序退出的时候，其他的小程序竟然会先切换页面到设置才缩小隐藏。"*
>
> **根因**：`chat_screen.dart` 里选"现在开着哪一屏"的那串三元表达式**最后兜底到 `SettingsScreen`**，
> 而"关掉"就是 `_openApp = null` ⇒ **收回动画那一帧画的是设置**（⚠️ 关设置本身时看不出来）。
> **修法**：收成一个 `_appView()` —— **设置只在它真的开着时才画**，没开返回 `null`，
> 外面用**上一帧那一屏**把收回动画播完（缩回去的是它自己）。
> ⚠️ 顺手修掉一处潜在崩溃：`onAsk` 原来是 `_mineOpen()!.id`，收回期间 `_openApp` 已是 `null`。
> 判据 +2（收回动画中间那一帧：`SettingsScreen` 不许在树里、原来那一屏还在；加正对照）
> · **变异验证**：把兜底改回设置 ⇒ 当场红。契约 `docs/dev/52-DESKTOP.md` §6.4。

> ## v1.65 · **小程序的图标库：每个小程序都一定有一个默认 icon**（2026-09-23 · 主人点名要的）
>
> **主人原话**：*"你要做一个 icon 库，给每个小程序创造一个默认 icon。"*
>
> **治了三个真问题**：① 库只有 **14 个名字**（日常那些事一个都没有）；
> ② `app_create` **必须**给白名单里的名字，给不出/给错就**当场报错**；
> ③ 🔴 **那张表在服务端有两份**（`apps.js` 一份、`mcp-apps-server.mjs` 抄一份），
> 而客户端那条"两张表不许漂"的判据**只对了其中一份**。
>
> **做成什么**：图标库收成**一处出处**（新 `src/app-icons.js`，**54 个名字**，老 14 个一个没删）·
> 挑图标：**先认名字里的词**（中英文 55 组关键词，具体词赢过笼统词），认不出按 id **稳定地**
> 落在**中性子集**里（不是在整库乱落）· `app_create` 的 `icon` **不再必填**，
> **没给/给错一律自动配**（桌面上出现一个空白图标，比换一个相近的图标坏得多），
> 而且**回执里如实带上配的是哪个**（否则模型以为它挑的生效了）。
> 判据 **+19 条**（服务端 12 新 + 2 改 · 客户端 2 改），两处变异验证过；
> **真机验过**：走模型自己那条域套接字、故意不给 `icon` 建一个小程序 ⇒ 回执 `icon:"weather"`
> （按"看天气"配的），清单里也存下来了。契约 `docs/dev/70-APP-ICONS.md`。

> ## v1.64 · **展开那一刻也要停在最新**（2026-09-23 · 主人实测报的）
>
> **主人原话**：*"聊天展开的时候默认显示了最早的聊天。这是不对的。应该默认展示最新的聊天。"*
>
> **根因**：**收起档根本不建时间线**（`chat_floater.dart` 的 `if (collapsed) … else Expanded(child: …)`）
> ⇒ 点"展开"那一刻时间线才被建出来，新视口从偏移 **0** 开始 = 停在**最早**那条；
> 而"跟到底部"只在 `initState` 的 post-frame 跑过一次（那时还是收起档、没有列表可滚）。
> **修法**：非收起档一到就 `_userScrolledAway = false` + post-frame **强制 `jumpTo(maxScrollExtent)`**，
> 而且**连补 4 帧**（展开是动画、列表又是懒加载：跳完还会长，只跳一次**差一条** —— 真机截图量出来的）。
> ⚠️ **"懒加载那半"没有判据钉住**（拿掉连补时那几条判据照样全过）；证据是真机截图，见 `27-SCROLL.md` §5.1。
> ⚠️ 而且那几帧补跳**一发现"用户动过手"就收手**：不然会把用户刚做的上滑又拽回底部
> （已有的硬闸 D3.6「主界面（含"重发"那个入口）」正是这么红的 —— 它先上滑到顶去找"重发"）。
> ⚠️ **为什么以前的闸没抓住**：那几条判据**全都从 `initialTier: full` 进**（"一上来就展开"的入口），
> 而**真应用是从收起档开始的** ⇒ 又一条"闸打在替代的那一侧"（V13 那一族）。
> 新判据**从默认档进**、点一下"展开"再量；**变异**：拿掉修法 ⇒ 那条红。
> 改了 `docs/dev/27-SCROLL.md`（追加一节）与 `test/widget/scroll_follow_test.dart`（+1）。

> ## v1.63 · **计划条：全做完就收起来 · 完成的条目不画删除线**（2026-09-23 · 主人实测反馈）
>
> **主人原话**：*"现在上方有一个列表和删掉的文字。怎么回事"* → *"还在。它列了几件事"*。
> ⇒ 两件**画错了**的，当天改掉：
> ① **全做完之后它还挂着** —— 这条的定位是"它**现在**打算做哪几件"，全做完就不再是"进行中"；
>    改成 **`Plan.allDone` ⇒ 一个像素都不画**（⚠️ **服务端封过顶时绝不说"全做完"**：
>    没画出来的那几件我们不知道做没做完）；
> ② **完成的条目画删除线** —— 主人**把那道横线读成了"删掉了"**；改成完成用**勾**说（`☑` + 淡色）。
> ⚠️ 一句话记住：**给不会用电脑的人看的界面上，删除线不是"完成"，是"没掉了"。**
> 判据 **+5**（纯函数 3 · 界面 2）· 改了 `08-SPEC.md` §2.2 那条的显示规则与 `64-CHAT-REDESIGN.md` §四·补。

> ## v1.62 · **读出来：把它说的话念出来**（2026-09-23 · 功能第二件）
>
> **主人定案**：功能清单里选「听筒 / 读出来」；形状选**"每条都能念 + 一个自动念开关"**。
> **改了哪几处**：`05-DECISIONS.md` **D5.16**（新）· 这一页 · `docs/dev/68-SPEAK.md`（新）·
> `docs/dev/00-PROGRESS.md` §〇。
>
> ⛔ **砍掉的**：那个「听筒 / 扬声器」演示按钮与它那三个词 —— 网页上**没有"听筒"这个出口**
> （浏览器只有扬声器/耳机，`setSinkId` 在 iOS 上无效），留着它就是假装有一个做不到的东西。
> ✅ **替上的是**：**每条回答一个"读一遍"**（正在念时变**"别念了"**）+ **一个"读出来"开关**
> （自动念新说的话，**默认关**）。走**设备自带**合成器，**我们不发任何东西出去**；
> 界面上不再有"读出来也还没做"那句（它现在是真的了 —— 留着就是假话）。
>
> 🔴 **三条边界**（每条都有判据）：**历史不念**（一登录把以前说过的话全念一遍 = 骚扰）·
> **没说完的不念**（半句/"这条没说完"/卡住 —— 念出来等于替它把话说圆）·
> **念不了就不画那个按钮**（界面上不许出现按不动的东西）；点"别念了"/关开关/离开这一屏**都要停**。
> 判据 **+19 条**（控制器 9 · 界面 6 · 那个开关 3 · 可访问性 2 组×五档 · 禁用词 7 句），两处变异验证过。

> ## v1.61 · **出处：它替你查过的东西，看得到来源**（2026-09-23 · 功能第一件）
>
> **主人 2026-09-23 定序**（*"现在先要实现功能，性能问题我们相对放宽一些"*）之后的第一件功能。
> **改了哪几处**：`03-DEVELOPMENT.md` §五 表后补**`sources` 的语义**（`title` = 给人看的名字、只认两件工具、
> 只认 `http(s)`、工具名/查询词一个都不进）· 这一页 · `docs/dev/67-SOURCES.md`（新）·
> `docs/dev/00-PROGRESS.md` §〇 与 §8.1。
>
> **为什么值得做**：`message/end.sources` 这个字段**一直都在**（协议 R8）、客户端**也一直在画**——
> 而**产品那条路上没有任何一处往里填** ⇒ 屏幕上一段永远不会出现，代码读起来却像"已经做了"。
> 这一批把上游的 `tool/result.meta.sources`（`web_search`）与 `meta.url`（`web_fetch`）接进来。
>
> **形状**：服务端只认那两件工具、只带 `{title, url}`、坏载荷一律空；客户端画前 3 条 +
> **如实报"还有 N 处"**；**平台开得了才画按钮**（网页能开、别处只当文字——界面上不许有按不动的东西）。
> 判据 **+19 条**（服务端 12 · 客户端 7 · 可访问性硬闸 +2 组×五档）。
>
> ⚠️ **顺手修好一处"闸在替自己作假"**：量命中区的 `sweep()` 用 `find.byType(TextButton)`，
> 而它**只认精确类型** ⇒ `TextButton.icon(...)` / `FilledButton.tonalIcon(...)`（子类）**从来没被量过**；
> 之所以一直没露馅，是因为那条"一个都没扫到就红"的负向对照被**顶栏那几个图标**撑着。
> ⇒ 改成 `is ButtonStyleButton`（子类也认），修完**没有既有违规**，而新加的那几行真的被量到了。

> ## v1.60 · **补：`/api/timeline` 漏了进 `TENANT_ROUTES`**（2026-09-23 · L2 表格修正）
>
> **改了哪一处**：`08-SPEC.md` §2.1 表后那句"只有**五**组会转发进租户容器" ⇒ "**六**组"，并把
> `/api/timeline` 列进去。
>
> **为什么**：批 C 新加的只读路由**只写了路由、忘了进转发名单** ⇒ 对**每一个有容器的用户**，
> `GET /api/timeline` 由**宿主**代答（读宿主侧那份空的 per-user 盘）⇒ 客户端如实说"没有更早的了"，
> **而他盒子里明明有 60 条**。⇒ 这属于本项目最当回事的那一类：**页面在说假话**。
> 另有一层：**产品层当时也没重发** ⇒ 盒子里跑的还是批 C 之前的代码（转发过去也是 404）。
> 两处都修了（`src/server.js` + 产品层 `f20bd7f0db90 → 4e5411c2cb7a`），并加了
> `test/tenant-routes.test.js`（5 条，含"漏一个就红"的负向对照与"主人那条必须真有帧"的正对照）。
>
> ⚠️ **它为什么能溜过批 C 的收尾**：那一批的"已验"验的是**主人那一侧**（宿主直接答得好好的），
> **租户那一侧从来没被走过**。⇒ 纪律重申（判据 **V13** 同族）：**凡是"某一端自己算出来/自己答的"东西，
> 闸必须打在那**一端**上** —— 这里就是"有容器的那一端"。

> ## v1.59 · **对齐：把手册里"说了假话 / 缺了行"的地方逐条改真**（2026-09-23 · 三路只读审计之后）
>
> **改了哪几处**：
> · `08-SPEC.md` **§2.1 接口表** —— 🔴 **补上 8 组在跑的路由**（`send-code` · `renew` · `revoke-all` · `audit` ·
>   `space` · `model-key` · `cancel` · `export` · `trash` 四条）· **6 条今天不存在的路由**（`health/local` ·
>   `receipt` · `message/:id/cancel` · `conversations` 三组 · HTTP 上的 `apps` POST/rollback · `debug/*`）标 ⏳ ·
>   `/api/say` 的 **`conversationId` 幽灵字段删掉** · 表后写明**哪五组会转发进租户容器**；
> · `08-SPEC.md` **§2.2 事件表** —— `miniapp/load-failed` 与 `audit/notice` 标 ⏳（今天不发）·
>   补上一直没进表的 **`task/resumed` · `notice/urgent` · 三条墓碑**；
> · `08-SPEC.md` **§4.2** —— 🔴 **"出网"那一行从【有效】的「白名单 + 审计」改成「目标态 · 今天没实现」**
>   （代码里没有 `--network`、没有出口白名单、也没有请求审计 ⇒ 新账 **#65**）· **水波那一行整行删掉**
>   （主人看过真机说不要了）· **§10.1** 三处"表里有、代码里没有"的（内阴影 · 档位 200ms · 宽屏 720）标 ⏳，
>   甩的阈值标明**代码里是 700**（表里 650 过期）· **§10.3** 标明那两个内存值是**上一代那个部署的**；
> · `04-ROADMAP.md` §十二 三处标已实现；`AGENTS.md`（§五/§八 收成指针，**进度数字只住 `00-PROGRESS.md`**）；
> · 🔴 `docs/dev/59-USER-APPS.md` —— 原来写着"授权/撤权工具**故意没挂**"，**事实是它们是挂着的**
>   （有判据）。**照那句做会删掉能用的能力** ⇒ 改成实话。
>
> **分量**：这一版改的不是文风，全是**会让下一个人做错事**的句子 —— 照着幽灵路由写客户端、
> 照着幽灵事件等一个永远不来的信号、把已实现的写成没实现、把没实现的写成【有效】。
> **最容易害人的那一条是出网**：手册说"白名单 + 审计【有效】"，而代码里**一次都没实现** ⇒
> 读它的人会以为"容器出不去"，于是**不设防**。
>
> **为什么以前没发现**：这些行是"照上一代的设计"抄进来的，而**抄完之后没人拿代码对过**。
> ⇒ **纪律加一条**：写"现状"的行，**写的时候就对一次代码**；对不上的写进 `docs/dev/`，**别写进手册正文**。
>
> 全记录（三路审计怎么做的、查到什么、哪些是**好的**）见 `docs/dev/65-ALIGNMENT-2026-09-23.md`。

> ## v1.58 · **聊天记录：老消息往上翻着加载 + 「回到最新」**（2026-09-23 · 批 C）
>
> **改了哪几处**：`08-SPEC.md` **§二 接口表**（新增 `/api/timeline`）· 这一页 ·
> `docs/dev/64-CHAT-REDESIGN.md`（批 C 状态）· `docs/dev/00-PROGRESS.md` §〇。
>
> 服务端：`src/resume.js`（新纯函数 `planBackfill`）· `src/server.js`（那条**只读**路由；
> 它也在 `TENANT_ROUTES` 里 —— 租户用户的那一份在盒子里答）· `test/resume.test.js`（+5）。
> 客户端：`services/api.dart`（`older()` + `OlderPage`）· `services/chat_controller.dart`
> （`loadOlder()` / `items` 合并 / 复位时一起清）· `screens/chat_screen.dart`（滚到顶自动取 +
> **钉住视觉锚点** + 顶部那句状态 + 「回到最新」）· `models/timeline.dart`（`oldestSeq`）·
> `test/unit/older_test.dart`（新 6 条）· `test/widget/older_ui_test.dart`（新 1 条）。
>
> **定案（主人 2026-09-23）**：要「回到底部」那个按钮；一页多少条 / 最多留几页**按代码常数**定
> （`chat_controller.dart` 的 `olderPageSize` / `olderMaxPages`）。
>
> **四条口径**（都写进了判据）：
> ① 服务端给的是**原始带号事件**（含墓碑）——**不替客户端筛**（筛两遍 = 两处口径，迟早会漂）；
> ② 客户端用**同一套时间线规则**把那一页变成条目（去重/分组/隐藏照旧）+ **按号去重**（同一页取两次不许出现两遍）；
> ③ 🔴 **"没问到"与"到头了"必须分开说**（把网络问题说成"没有更早的了"就是假话）·
>    本机页数到顶另说一句（**是我们不留了，不是服务端没有**）；
> ④ 🔴 **换人 / 服务端判死复位时，那几页一起清掉**（它们不在本机缓存里，但照样是"上一个世界的老消息"）。

> ## v1.57 · **计划条：把助手自己的目标 / 任务清单显示出来**（2026-09-23）
>
> **改了哪几处**：`08-SPEC.md` **§2.2 事件表**（新增一条 `plan/updated`）· 这一页 ·
> `docs/dev/64-CHAT-REDESIGN.md`（方案 + 定案 · 批 B）· `docs/dev/00-PROGRESS.md` §〇。
>
> 服务端：`src/plan.js`（新：把 `todo/write` 快照与 `goal/change` 翻成人话）·
> `src/session-translate.js`（认那两类会话事件 · 每轮开头清空 · 发**持久**事件）·
> `test/plan.test.js`（新，14 条）。
> 客户端：`models/{plan,plan_words}.dart`（新）· `widgets/plan_strip.dart`（新）·
> `services/chat_controller.dart`（`Plan? get plan`）· `screens/chat_screen.dart`（挂到屏幕上沿）·
> `test/widget/plan_strip_test.dart`（新，9 条）。
>
> 主人 2026-09-23 定案：**浮在屏幕上方**（聊天窗口一个像素不动）· 显示 **目标 + 任务清单** ·
> 作用域图标留在抓手行 · **发送只在有框里有字时出现**（批 A 已落）· 聊天记录要"回到底部"（批 C）。
>
> **为什么这条值得进手册**：它是**新的一条 L2→L1 事件**，而且带着三条硬规矩：
> ① **持久**（有号、会补发）—— 刷新一下计划条不许变空；
> ② **每轮开头清空**（dsh 的 todo 投影就是这么清的：新的一轮上不该挂着上一轮的计划）；
> ③ 🔴 **工具名 / goalId / callId 一个字节都不许进这条**（`26-PROCESS-LEVELS.md:180` 那条规矩，扩到新事件）。
>
> ⚠️ **实测记一笔（不许当已经做完了）**：真机上我发了两次话（拿测试租户 `u2` / `hupo-b`），
> 那个 agent **两次都没有调用 `todo_write`** ⇒ 计划条**没在真机屏幕上出现过**。
> 工具**是在的**（`dsh --profile sdk --dump-config` 里有 `tool-todo`；我们的能力层只是 `insert`，不摘工具）
> ⇒ 这是**模型愿不愿意用那个工具**的问题，不是接线问题。⇒ 判据覆盖的是**我们这一侧**
> （事件认得出 · 翻得对 · 发得持久 · 界面画得对），**模型那一侧没法自动化**。

> ## v1.56 · **"生产机"这个称呼逐处校正**（账 #50 · 2026-09-23）
>
> **改了哪几处**：`06-OPERATIONS.md`（§四 那条 env 白名单 · §8.3 标题）·
> `07-APPENDIX.md`（§4.3 那段容量算式 · 现状表里 `unit` 那一行）·
> `08-SPEC.md`（§九 开头那句）· 这一页 · `docs/dev/00-PROGRESS.md` §〇/§六。
>
> 为什么：**"生产机"这个词在这个仓库里指过两台不同的机器**——而 **v1.21** 那时只来得及写
> "留着，下一批逐处改名"（那一行现在兑现了）。这次逐处按**它到底指谁**改：
>
> | 地方 | 原来 | 现在说的 |
> |---|---|---|
> | `06` §8.3 标题 | "重启自己的正确姿势（生产机上）" | **在本机** —— 本机就是跑 `w.stalkerai.cn` 的那一台 |
> | `07` §4.3 容量算式 | "带界面的宿主在生产机上装不下" | **给容器的那点内存**里装不下（额度见 `06` §1.3 —— ⚠️ **那张表是从上一代抄来的，本机未复核**） |
> | `06` §四 env 白名单 | "先盘点生产机 unit 文件" | **注入那些变量的那一层**的 `unit` 文件（⚠️ **至今没拿到**；而**本机这一套不在 systemd 下** ⇒ 根本没有"本机的 unit"这种东西） |
> | `07` 现状表那一行 | "生产机 `unit` 文件" | **上一代那个部署**（跑 `concierge-core` / 8091 的那一台）的 |
> | `08` §九 开头 | "用前先在生产机上核一遍" | **在那一台（上一代那个部署）上**核；并加一句 **"别拿『生产机』去指本机"** |
>
> ⇒ 一句话：**两台都在跑，是两台不同的部署** —— 本机 `127.0.0.1:8020` = `https://w.stalkerai.cn`；
> 那一台 8091 = `https://hupo.stalkerai.cn`（**上一代**）。**照旧说法行事会去错机器。**

> ## v1.55 · **聊天条最前面那个图标：这句话是在哪儿说的**（2026-09-23）
>
> **改了哪几处**：`08-SPEC.md` **§6.2**（抓手行新增一条）· 这一页 ·
> `docs/dev/52-DESKTOP.md` **§七**（新）· `docs/dev/00-PROGRESS.md` §〇。
>
> 客户端：`widgets/chat_floater.dart`（新增 `leading` 口 —— 浮窗自己**不认识**"作用域"，
> 它只把上层给的东西画在标题前面）· `screens/chat_screen.dart`（`_scopeBadge` / `_scopeIcon` /
> `_builtInIcon`）· `models/space_words.dart`（那两句说明）·
> `test/widget/chat_scope_icon_test.dart`（新，3 条）。
>
> 主人原话：*"底部的聊天窗口，我需要左侧是一个 icon。是一个 home icon，说明聊天作用域在桌面
> 也就是全局。当进入某个 app 时，聊天作用域也进入了这个 app。所以聊天窗口前面的图标也成了
> 那个 app 的 logo icon。"*
>
> | 现在在哪儿 | 那个图标 |
> |---|---|
> | **桌面**（没进任何小程序 = 全局） | 一个**家**（`Icons.home_outlined`） |
> | **某个内置小程序** | **跟桌面上那一个完全相同**（设置 / 奥数题 / 发现） |
> | **"我的小程序"**（乙-1） | `miniAppIconFor(它的 icon)` |
>
> **两条纪律**：
> ① 🔴 **只能有一份来源** —— 内置那三个的图标现在只写在 `_builtInIcon()` 里，桌面与聊天条
> 都从那儿取（两处各写一套**迟早**会分叉，而分叉的症状是"进去了"在两处长得不一样）。
> 判据钉住了它：`chat_scope_icon_test.dart` 会把聊天条那个图标与桌面上那个**取出来比**。
> ② 🔴 **它只是指示、不是按钮**（点了什么都不做）—— 真让它可点就等于多一个出口，
> 那要另配行为 + ≥44 的命中区（D3.6），**不在这一刀里**。⇒ a11y 的命中区扫描照旧与它无关。
>
> ⚠️ **收起态也画它**（收起那条照样能把话发出去 ⇒ "在哪儿说"同样得看得见）。
> ⚠️ 一个边角：**"我的小程序"清单刷新之后正开着的那一个可能不在了** ⇒ 那时给**默认的小程序
> 图标**，**不许**掉进内置那个 `switch`（那会把"我的某个小程序"画成**设置** —— 一句假话）。

> ## v1.54 · 🔴 **补正 v1.53 的判据**（它们当时是**假绿**）（2026-09-23）
>
> **改了哪几处**：`scripts/deploy-web-v2.sh`（**补丁先打、再按字节算指纹**）·
> `scripts/check-web-browser.mjs`（`--block-gstatic` **开页面之前**就屏 + 新增 `--shot-after`
> 与字体那条口的流量读数）· `scripts/check-web-refs.mjs`（canvaskit 那一支改成**真话**）·
> `docs/dev/61-WEB-PERF.md`（判据的两次错 + 冷启动 A/B 定案 + 新欠账 §五）·
> `docs/dev/00-PROGRESS.md` §〇/§六（新账 **#64**）/§九。
>
> **为什么值得单独升一版**：v1.53 那几条话**都写着"已经做到了"，而代码里其实没做到**。
> 这种地方最贵 —— 它的代价不是一次故障，而是**下一个排查的人信了它**。
>
> ① **`--block-gstatic` 当时是假判据，而且它错了两次**：
>    * **第一次：屏蔽得太晚** —— 第一版**先不带屏蔽开一遍页面**
>      （`fonts.gstatic.com` 与 `www.gstatic.com` 都被**真的拉了一遍**、进了同一个
>      profile 的缓存），**然后才**屏掉再开第二遍 ⇒ "页面照样开"证明的是**缓存里有**，
>      **不是**"我们自托管了"。⇒ 现在**屏蔽在第一次导航之前**生效。
>    * **第二次：拍得太早** —— 修完上一条**仍然时好时坏**，根因在**截图那一侧**：
>      中文用的是**异步下载的分片字体**（引擎遇到缺字才去 `/fonts/…` 要，每片约 25KB），
>      而脚本等到"**收到第一帧**"就往下走 ⇒ 拍到的是**还没字**的那一帧（`.notdef` 方块）。
>      ⇒ 现在**截图前固定等 `--shot-after`（默认 6000ms）**，并把**字体那条口的流量**
>      （问了 / 成了 / 失败）打出来 —— **CanvasKit 把字画在 canvas 上，查不到文本**，
>      "取没取到字体"与"屏幕上有没有字"**对着看**才判得准。
>    ⚠️ 教训一句话：**同一条命令一会儿好一会儿坏，先怀疑"判据自己"**（这次错在屏蔽时机与拍摄时机）。
>
> **冷启动 A/B 定案**（每次一个全新 profile、`--block-gstatic` 从第一个请求就屏，**两边都等够**）：
>
> | 条件 | 结果 |
> |---|---|
> | **有**补丁 + 冷启动 + 屏 gstatic | **中文完好**（`sha c9954fa1…`，多次跑出来一致；字体 **12 问 12 成 0 败**） |
> | **没**补丁 + 冷启动 + 屏 gstatic | **字全没了**（`sha 81a5b9f3…`，**等 12 秒也还是没有**） |
>
> ⇒ 镜像 + `fontFallbackBaseUrl` **确实在起作用**，不是"看着像好了"。
>
> ② **指纹当时是"替身"**：v1.53 写"补丁也要进指纹"，而实现是**先算指纹、后打补丁**，
>    拿一个 `FONT_PATCH_V` 字符串当替身 —— 它只挡得住"改基地址 / 改版本号"，
>    **挡不住"改补丁正文"** ⇒ 那个坑（浏览器 `immutable` 一直吃旧补丁）**还在**。
>    ⇒ 现在**补丁先打进构建产物、指纹按打过补丁的字节算**，替身字符串取消；
>    改名/复制之后再**复核一遍**补丁还在。
>
> ③ **`check-web-refs.mjs` 里也有一句假话**：它原来打印"本地产物里的 `canvaskit/`
>    **不在页面的取用链上**（只是备份）" —— 实测**是反的**：屏掉 gstatic 冷启动能开，
>    靠的正是它。⇒ 现在那一支**认 `"useLocalCanvasKit":true`**，并且**逐个查那两个文件在不在**
>    （跟显式 `canvasKitBaseUrl` 那一支同等对待）。
>
> **判据（现在才算数）**：
> `HUPO_TOKEN=<令牌> node scripts/check-web-browser.mjs --block-gstatic --shot /tmp/x.png`
> ⇒ ① 页面照常打开、流照常通；② **中文有字**（不是方块、不是空白）。
> ⚠️ **新账 #64**：服务端字体缓存**冷**的那一次访问，中文会**晚几秒**才出现
> （在那之前是方块/空白）—— 只影响"第一次"，怎么还写在 `61-WEB-PERF.md` §五。

> ## v1.53 · **"页面加载很慢"：三处修复（压缩 / CanvasKit 自托管 / 中文字体自托管）**（2026-09-23）
>
> **改了哪几处**：`docs/dev/61-WEB-PERF.md`（新）· `docs/dev/15-CACHE.md`（那句过期了）·
> 这一页 · `docs/dev/00-PROGRESS.md` §〇/§六 · 服务端 `src/{server,serve}.js` ·
> 脚本 `scripts/{precompress.mjs（新）,deploy-web-v2.sh,check-web-browser.mjs}`。
>
> 主人：*"页面加载很慢，怎么回事"*。**先量再猜**，量出来三件事：
> ① 这条链只有 **~3.4 Mbps**（`main.dart.js` 2.73MB 走 6.5s）· ② **从来没压缩**
> （响应头里没有 `content-encoding`）· ③ 产物里 **CanvasKit 与中文字体都指向 gstatic**。
>
> 🔴 **第三件才是真凶，而且它有个更坏的面**：屏掉 gstatic 之后 **图标在、中文字全空**
> —— 也就是说用户那边（国内）取不到那个 CDN 时，**页面等于没有字**。
> ⚠️ 而"这台机器取 gstatic 只要 0.4s"是个**陷阱**：本地一切正常，问题只在用户那边。
>
> **修了三处**：① **静态预压缩**（br：JS −77% · Canvaskit −69%）+ 必带 `vary`；
> ② **CanvasKit 自托管**（构建加 `--no-web-resources-cdn`）；
> ③ **中文字体自托管**：服务端加一条**白名单镜像** `/fonts/…`（带磁盘缓存），
> 部署时把 `fontFallbackBaseUrl` 指到我们自己的地址。
>
> 读数（走公网、带 br）：`main.dart.js` **6.5s → 0.99s** · `canvaskit.wasm` **16s → 5.5s** ·
> 中文字体**不再依赖 gstatic**（屏掉 gstatic 也有字）。
> 新判据：`check-web-browser.mjs --block-gstatic`（**屏掉 gstatic：页面要能开、中文要有字**）。
>
> 🔴 **这一批的两条坑（都很值得记）**：
> ① **`fontFallbackBaseUrl` 必须是绝对 URL**：写相对地址 `/fonts/` 时，引擎**取到了字节
> （200 / 24780 字节）却对不上号** ⇒ 中文**全变方块、而且不报错**；
> ② **补丁也要进指纹**：入口名字按产物字节算，而 patch 改的是**部署出去的那一份** ⇒
> 不把补丁算进去，改了补丁**文件名不变**、浏览器（`immutable`）一直吃旧补丁 ——
> 这次**差点被它骗过一次 A/B**（两次截图字节相同才察觉）。
> ⚠️ **这一条当时的实现只是个"替身"（`FONT_PATCH_V` 字符串），当天在 v1.54 补正** ——
> 替身挡不住"改补丁正文"，坑还在。
> ⚠️ **上面那条新判据（`--block-gstatic`）当时也是假绿，同样在 v1.54 补正。**
>
> **自己写的自检也栽过两次**（都记进 `61`）：拿"产物里有没有 gstatic 字符串"当判据
> （那是永远在的分支，误报）；白名单把**真实文件名**（96 字符）拒了
> （判据里用的是短假名字，所以没抓到，线上表现是"字体全 404、中文还是不显示"）。

> ## v1.52 · 🔴 **删掉：桌面那个动态背景**（主人：*"太丑了，移除特效。不要了。"*）（2026-09-23）
>
> **改了哪几处**：`08-SPEC.md` **§6.1 Z5 / §6.1.1 / §6.1.2** 各补一句"**这一层已经删掉**"
> （那三节**留着**，但降级成"将来要加时先读的前提"）· 这一页 · `docs/dev/60-WATER-BG.md`（归档）·
> `docs/dev/00-PROGRESS.md` §〇。
>
> 客户端：`lib/widgets/water_bg.dart`（删）· `app_desktop.dart`（回到加水之前，**逐字一致**）·
> 那三份只为它存在的测试与 `test/flutter_test_config.dart`（删）。
>
> ⚠️ **这是"要砍就明说砍了"，不是"排后"**（`04-ROADMAP.md` §十一）。所以：
> 1. **代码删干净**，不留注释掉的开关（那种"以后再说"等于永远不做，而且会让下一个人纠结）；
> 2. **教训留下**：M1–M4（永不结束的动画要有能关的总开关）+ M5–M7（结构判据全绿、
>    整层却看不见）**与那一层无关** —— 将来加任何动效/装饰都先读这两节；
> 3. **要拿回来**：`git show c896c68`（它最后能跑的那一版），**不许凭记忆重写**。
>
> 读数：客户端四道闸全过（unit **289** · 可访问性 **169** · 其余界面 **150**）。

---

> ## v1.51 · 🔴 **补一条：装饰层"结构判据全绿，也可能整层看不见"**（2026-09-23）
>
> **改了哪几处**：`08-SPEC.md` **§6.1.2 新增 M5–M7** · 这一页 · `docs/dev/60-WATER-BG.md` §六
> （重写成"事故与根因"）· `docs/dev/00-PROGRESS.md` §〇。
>
> 主人验过线上一句：*"水纹没有生效。"* —— **他是对的**，而且**八条判据全是绿的**。
>
> **根因一句话**：水面画在桌面 `Stack` 的**最底层**，而它上面那层
> `Material` 画的是**不透明的纸色** ⇒ 它就是**盖在水面上的另一张纸**。
> 涟漪照常在算、ticker 照常在跑、**一个像素都透不上来**。
>
> ⇒ 手册里要落的是**三条会被下一个人踩的**（`M5`–`M7`）：
> 1. **"看得见的东西"必须有判据打在像素（或那一层的颜色）上。** 出事那版八条判据
>    验的全是**结构**（在不在树上 / 会不会吃点击 / 会不会拖死 `pumpAndSettle`），
>    而"加了背景"这件事**只**在像素上成立 ⇒ 这就是"闸扫的是别的东西、而它照样绿"。
> 2. **一条正、一条反**：① 光栅化画家的输出，必须有一片点和底色不同；
>    ② **源码级**钉住"盖在上面的那层必须透明"。
> 3. 🔴 **读数与用户说的打架时，先信用户。** 我事前做过一整套像样的排查
>    （隔几秒截图 / alpha 提到 100% / 普通 canvas 动图当对照），结论是
>    "**页面根本不重画**" —— 错的：它在重画，只是**被盖住了**。
>
> 读数：客户端四道闸全过（可访问性 **169** · 其余界面 **156**）；水面这一块的判据 **12 条**；
> **真验**：隔几秒两张截图**不再相同**（`8d4411a4…` vs `f2ff13f5…`）⇒ "它在动"这次验过了。

> ## v1.50 · **桌面那一层可以有动的东西，但它必须是"能关的"**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` **§6.1 加一条 Z5** · **§10.1 加两行**（底图 · 动效纪律）·
> 这一页 · `docs/dev/60-WATER-BG.md`（新）· `docs/dev/00-PROGRESS.md` §〇。
>
> 主人 2026-09-22：*"给登录后的桌面增加一个动态背景，有水面波纹状。"*
> ⇒ 桌面那层（Z4 说的"铺满整屏的底图"）多了一层**很淡的同心水波**。
>
> 手册里要落的是**两条会被下一个人踩的规矩**（不是"长什么样"）：
>
> 1. **Z5：桌面底图是装饰 ⇒ 不许接输入、不许说话、不许改排布。**
>    "加一层"最容易顺手破坏的是 **§6.3 的"点桌面空白 = 收起聊天"**
>    （背景把命中吃了，那条会**静默失效**，而且没人会去查背景）；
>    以及 D3.6 的命中区（图标格下移 1px 就破了）。
>    ⇒ 底图必须 `IgnorePointer`，而且**在 `Stack` 最下面那一层**。
> 2. 🔴 **动效纪律：凡是"永不结束"的动画，必须有一个能关的总开关，
>    而且那个开关**默认是开的**。** 理由是**测试的形状**：
>    `pumpAndSettle()` 的出口是"没有下一帧了"，一个 `..repeat()` **永远出不来**
>    ⇒ `test/widget/` 里**一百多处**会集体 `pumpAndSettle timed out` ——
>    那种红**不指向任何真 bug**，而且没人修得动。
>    ⇒ 这个项目里"多加一个一直动的效果"的代价不是几毫秒，是**整套界面闸**。
>    ⚠️ 附带一条：**关掉它 ≠ 删掉那一层**（"不动"与"没有"是两件事）。
> 3. **谁要求少动就得少动**：系统开了"减弱动态效果" ⇒ 纯装饰的动画**不画**
>    （与 D3.5"别假装支持"同一类）。
>
> 读数：客户端四道闸见 `00-PROGRESS.md` §〇 那一条。⚠️ **这一批自己栽了一次**：
> `build()` 里算"动不动"时漏了那个总开关（`_run()` 里算对了）⇒ 7 条判据超时；
> 抓到它的正是"pumpAndSettle 不挂"那条判据 —— **它不读那个标志**，所以自说自话不了。

> ## v1.49 · **补足：手册/路由/对外口径里关于小程序的缺口**（2026-09-22）
>
> **改了哪几处**：`docs/INDEX.md`（L0 路由补两行）· `docs/dev/59-USER-APPS.md` **§八**（重写）·
> `docs/dev/56-PLAIN.md`（能力表 + 不能用的清单）· 客户端 `models/landing_words.dart`（**一张卡 + 一条 FAQ**）·
> 这一页 · `docs/dev/00-PROGRESS.md` §〇。
>
> 主人一句"**补足**"⇒ 把还缺的地方扫了一遍，**三处**：
>
> 1. 🔴 **`59` §八 自己写的话过期了**：原来写着"生产第二原点没建""卸掉/授权/撤权还没有"——
>    2026-09-22 都做完了。⇒ 重写成**现在真的还欠的八条**（"他明说才许写"还是软的 · `app_update` 没有 ·
>    `app/update-available`/`app/error` 两条事件没有 · **封禁清单没有** · `dart:html` 待迁 ·
>    可访问性硬闸没覆盖远端那一屏 · `minShellVersion` 没接线 · **Z2 仍然没有判据**）。
>    ⚠️ 加了句规矩：**这一节每落地一批就要回来改一次** —— 留着旧话就是假话。
> 2. 🔴 **`INDEX.md`（L0 路由）里根本没有小程序的入口** ⇒ 一个新人问"小程序怎么做的"会落到空处。
>    补两行（问题 → 指针，**零事实**，符合 L0 的规矩）：契约 `dev/59` + 当初为什么这么做 `dev/58`。
> 3. 🔴 **对外口径里一个字都没提"能给你造个小程序"**（功能已上线，首页却看不见）⇒
>    首页补**一张卡**（`造`：能给你做个小程序）+ **一条 FAQ**（"我能让它给我做个小程序吗？"），
>    `56-PLAIN.md` 的能力表补第 12 条、"还不能用"里补"小程序（原生）"。
>    ⚠️ 又栽了一次同一个坑：卡面里写了 `**只属于你**` ⇒ **界面是纯 Text，星号会照原样显示**（当场改掉）。
>
> 读数：客户端 unit **289** / 可访问性 **169** · 公网产物见下。⚠️ **提示档有一条红**，
> 但**不是我的**：`test/widget/zz_probe_test.dart` 是**别人留在树上的未完成探针**（见 §十 那条规矩）。

> ## v1.48 · **对齐：手册与现状**（小程序那一条从"不建"改成"已建、已上线"）（2026-09-22）
>
> **改了哪几处**：`05-DECISIONS.md`（新增 **O 组 · D4.14–D4.19**）· `08-SPEC.md`（**六处**）·
> 这一页 · `docs/dev/00-PROGRESS.md` §〇。
>
> 主人一句"**对齐**"（他那句没说完，我按最像的意思做）：这几批落地之后，
> 手册里**还留着已经变成假话的句子** —— 而手册是唯一权威。逐条改掉：
>
> | 在哪 | 原来 | 现在 |
> |---|---|---|
> | §7.3 | 【不建】小程序运行时（本轮） | **Web 侧已建、已上线**；原生侧仍然不建；注明 N1/N2 从"前提"变成**判据** |
> | 模块表 | `mini/runtime.dart` ❌【不建】 | `widgets/mini_runtime{,_web,_stub}.dart` ✅ **Web 侧已建**；原生 `kNativeMiniRuntime=false` |
> | §十四 开头 | "小程序本轮不建……将来做的时候" | **"这一节不再是将来的口径，它就是现在的契约"** |
> | 接口表 | `/api/apps` 一行（规划态） | ✅ 已实现（真实字段 + **现签** `entryUrl`）；补 `/api/discover`、`/api/app-ask` |
> | 事件表 | `app/installed` 等三条 | ✅ **`app/installed` 已实现**；另两条还没有 |
> | §4.2 制品 | 一条规则 | ✅ **已落地**（含"「发布走特权口」今天的读法"） |
>
> **新增 O 组六条决策**（主人这几天拍的板，之前只在 `docs/dev/` 里，没进权威表）：
> **D4.14** 小程序分内外两种 · **D4.15** 独立域名是 N1 的落点 ·
> **D4.16** 发布/装上=复制 + id 全局唯一 + 共享库不存身份 ·
> **D4.17** `ask` 用**看的人**的钥匙（花在他自己的环境里）+ 四道闸 ·
> **D4.18** 一切操作在对话里 + 只读的「发现」 · **D4.19** iOS 商店版不发布运行时。
>
> ⚠️ **"对齐"不是改文风**：上面每一条改的都是**一句会让下一个人做错事的话**。

> ## v1.47 · **上线之后当场补的一条：入口 URL 十分钟过期**（2026-09-22）
>
> **改了哪两处**：客户端 `screens/chat_screen.dart` · `test/widget/remote_app_test.dart` ·
> `docs/dev/59-USER-APPS.md` §七·补五。
>
> 上线之后在服务日志里看到一个**用户一定会撞上**的边角：制品入口 URL **只有十分钟有效**
> （服务端现签、绑人绑版本 —— 这是刻意的）。**页面开着不动、过十分钟再点图标**，那条 URL 已经过期
> ⇒ 点开是空的。⇒ 客户端补一条：**打开之前先看它快到没到**（留 60 秒余量），快到了就**先重拉清单**再开。
>
> 判据两条：**快过期 ⇒ 多拉一次**（而且照样打开）· **还早 ⇒ 不许**多打接口（别每次点都打）。
> 读数：客户端 unit **289** / 可访问性 **169** / widget **150** · 公网产物 `41d4346752b2`。

> ## v1.46 · **乙-5 的后半：制品口上生产了（`apps.stalkerai.cn`）**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §九（上线记录 + 读数）· 这一页 ·
> `docs/dev/00-PROGRESS.md` §六（**#58**）· **`scripts/start-tunnels.sh`**（新）·
> 本机 `data/tenants.env`（**这台机器的部署状态，不进仓库**）。
>
> 主人给了 VPS 的登录 ⇒ 那一步做了（**VPS 上只做纯追加，没碰任何已有 conf**）：
> **visitor**（`/opt/frp/frpc-visitor-apps.toml`，`bindPort=3085`，**属主 `deploy`**）+
> `frpc-visitor-apps.service` · **本机 frpc**（`frpc-apps.toml`，`localPort=8021`，新生成的 secretKey）·
> **证书**（`certbot certonly --nginx -d apps.stalkerai.cn`，到 2026-12-21）·
> **nginx**（先只放 :80 签证书 ⇒ 再换 80→301 + 443 + `proxy_pass 127.0.0.1:3085`）·
> 本机两行配置 + 重启。**DNS 本来就解析** ⇒ 那一步省了。
>
> **验收（对着公网原点，`check-app-origin.mjs` 全绿）**：制品口 ≠ 壳（N1）· 带签名 200 ·
> 有 CSP / **没有 `X-Frame-Options`** / `frame-ancestors` 含壳 · 没签名与假签名 ⇒ 403 ·
> **它不认令牌**。
>
> 🔴 **浏览器里也验过（这才是"你手机上能不能用"的答案）**：
> 公网 `https://w.stalkerai.cn` 上点开「掷硬币」⇒ 页面**真的画出来了**，
> 而 iframe 来自 **`https://apps.stalkerai.cn`**；「问一句」那条也在生产上通了
> （探针当场显示 `✅ 公网也通了`，验完已软删）。
> ⚠️ 以前看不到，是因为浏览器**不肯在 https 页面里嵌 `http://127.0.0.1`** —— 现在两个都是 https。
>
> ⚠️ **两处秘密都不进仓库**：本机与 VPS 各一份 `frpc-apps.toml`（0600）里的 `secretKey`，**两边必须一致**。
> ⚠️ **隧道是手动起的**（`frpc-w` 也一样）⇒ 记成账 **#58**，并加了幂等脚本 `scripts/start-tunnels.sh`
> （重启之后跑一条命令；真正的修法是 systemd，要连着 #48 一起定）。

> ## v1.45 · **乙-5 的前半·补：那一步做完之后怎么算"对"，一条命令**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §九 · 这一页 · `docs/dev/00-PROGRESS.md` §〇 ·
> 服务端 `src/app-serve.js`（`appsBaseOf`）· `src/serve.js` · `test/apps.test.js` **· `scripts/check-app-origin.mjs`**（新）。
>
> 1. **"上线只改配置"从一句话变成判据**：把"用哪个基地址"抽成纯函数 `appsBaseOf()`，
>    判据钉住（对外优先 · 没配退回本机 · 尾斜杠要去掉 · 拼出来的入口 URL 真以它为前缀）。
>    原来它是 `serve.js` 里的一行 ⇒ 只能靠读代码相信。
> 2. 🔴 **那一步做完之后的验收，做成一条命令**：`scripts/check-app-origin.mjs`
>    （① 不同源（N1）· ② 带签名取得到 · ③ 有 CSP / 没有 X-Frame-Options / `frame-ancestors` 含壳 ·
>    ④ 没签名与假签名 ⇒ 403 · ⑤ **它不认令牌**）。本机当正对照跑过，**全绿**。
>
> ⚠️ **这个脚本第一次跑就抓到自己两条错**（都是脚本的，不是产品的）：
> `frame-ancestors` 我拿子串找第二个 origin ⇒ 误报（那一节是**一串**，壳可能排在第二个）；
> 往请求头里塞了中文令牌 ⇒ `fetch` 直接抛（头里的值只能是 ASCII）。
> **教训：验收脚本自己也要真跑一遍**，不然它报的"红"会把人带到错的地方。
>
> 读数：服务端 **680 pass / 0 fail** · 客户端 unit 289 / 可访问性 169 / widget 148。

> ## v1.44 · **乙-5 的前半：把"上生产原点"变成一次配置 + 一份照抄清单**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` **§九**（新：上生产原点的照抄清单 + iOS 那一段）·
> 这一页 · `docs/dev/00-PROGRESS.md` §〇 · 服务端 `src/config.js` / `src/serve.js` ·
> 客户端 `widgets/mini_runtime.dart` · `test/unit/mini_runtime_test.dart`（新）·
> `test/capabilities.test.js`。
>
> ⛔ **真正那一步（VPS 上建第二个域名）要主人签字** —— 这一批做的是**不需要签字、但必须先在**的那些：
>
> 1. 🔴 **对外地址改成配置**（`HUPO_APPS_PUBLIC_BASE`，没配就用本机那个）：
>    原来它是写死的 `http://host:port` ⇒ 写死的话，"上线"就得**改代码**，而那正是要避免的形状。
>    ⇒ **迁移那天只改配置**（`data/tenants.env` 两行 + 重启），不留半行代码改动跟着走。
> 2. **一份照抄清单**写进 `59` §九（7 条：DNS · 本机 frpc · VPS visitor/systemd/证书/nginx · 本机两行配置），
>    照 `03-DEPLOY-WEB.md` §三 那套"一域名一实例"的现成配方 —— 签字那天是**机械操作**。
> 3. 🔴 **iOS / 原生那条约束落成代码里的标记 + 三条判据**：
>    「iOS 商店版不发布小程序运行时」今天**无处可关**（原生侧根本没有运行时，
>    `mini_runtime_stub.dart` 就是它的全部行为）⇒ 把它落成 `kNativeMiniRuntime`
>    （**必须显式改才可能打开**）+ `test/unit/mini_runtime_test.dart`（标记为 `false` ·
>    非 Web 拿到那句实话 · **条件导入只许对 `dart.library.html` 选 Web 实现**）。
>
> 读数：服务端 **679 pass / 0 fail** · 客户端 unit **289** / 可访问性 **169** / widget **148**。

> ## v1.43 · **乙-4c：主人那一份的"问一句"也接上了**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §七·补三 · 这一页 · 服务端 `src/model-proxy.mjs`
> （多一种钥匙格式 `dsh-refs`）· `src/serve.js`（宿主上起一个小代理）·
> 客户端 `widgets/mini_runtime_web.dart` · 判据 `test/app-ask.test.js`。
>
> **为什么还要做这一件**：主人**没有匣子**，他这份钥匙在 **DSH 自己那份凭据**里
> （实测形状：`refs.DEEPSEEK_API_KEY`）⇒ 在本机起一个**同样的小代理**（钥匙进内存，不打印不落盘），
> 只在**宿主**上起（匣子里由 `entry.mjs` 起过了，两边都起会撞端口）。
>
> **线上真验（整条通道）**：造了一个"一打开就自己问一句"的**探针小程序**，页面上当场显示
> `✅ 通道通了：通道通了没` —— 也就是**页面 → 壳 → 接口 → 本机代理 → 模型 → 回到页面**这一整条通了
> （验完探针已软删，不留在主人桌面上）。接口那一条单独验过：`{"text":"自己钥匙","left":39}`。
>
> 🔴 **这一批最值钱的坑（写进 `59`）**：壳那条 `onMessage` 一开始用
> `identical(e.source, iframe.contentWindow)` 认人 —— **Dart 跨语言包装对象不保证同一实例**
> ⇒ 页面发来的消息**一条都进不来**。抓出它的读数很朴素：**服务端那边的配额计数没有涨**。
> ⇒ 改成认**不透明原点**（沙箱页面的 `event.origin` 恰好是 `"null"`，外面伪装不出来），
> 再加两条保险：页面自己的 CSP 把嵌套 iframe 掐死 + 同一时刻只开着一个页面。
>
> 读数：服务端 **678 pass / 0 fail** · 客户端 unit **285** / 可访问性 **169** / widget **148** ·
> 公网产物 `3c1c0371d051`。

> ## v1.42 · **乙-4b：小程序问一句，花的是你自己的钥匙**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §七·补三 · 这一页 · `docs/dev/00-PROGRESS.md`（#55 结案）·
> 服务端 `src/app-ask.js`（新）· `src/{apps,apps-socket,server}.js` · `src/mcp-apps-server.mjs` ·
> 客户端 `services/api.dart` · `widgets/mini_runtime_web.dart` · `screens/chat_screen.dart` ·
> `test/app-ask.test.js`（新）· `test/{apps,apps-chain,capabilities}.test.js` · `test/widget/remote_app_test.dart`。
>
> **主人定的调子**：*"b 在小程序里如果用到 apikey，则用的就是 b 自己的。"*
> ⇒ **花这个动作发生在 b 自己的环境里** —— 这一批就按这个做了。
>
> **形状**：页面说"我要问一句"（**唯一一种** postMessage）⇒ 壳 ⇒ `POST /api/app-ask`（带**看的人**的令牌）
> ⇒ 🔴 **闸在中心**（这一条在他这儿 · 清单里**声明了** `ask` · 他**授予了** · 配额还有）
> ⇒ 转发进**他自己的匣子**（带"闸过了"的头 + 重新给一份身体）⇒ 匣子里**只管花**：
> 叫本机代理（`127.0.0.1:8787`，**钥匙在那儿**）⇒ 上游 ⇒ 原路回来。
>
> 🔴 **两个当场查出来的接缝（都写进契约了）**：
> ① **闸只能在中心**：租户的数据在两边**不是同一个目录**（中心 `data/users/<id>/`，匣子里 `/data`）
> ⇒ 匣子自己判不了"他授予了没有"；
> ② **跨进程只认头、身体要重给一份**：中心为了判闸已经读过一遍身体，而转发是流式的。
>
> **线上真验**（u2 的匣子里有钥匙）：用 **u2 的令牌**问一句 ⇒ `{"text":"收到收到"}`
> —— 走的是**u2 自己匣子里**那个代理、**u2 自己的钥匙**；配额记在中心（`{"day":…,"n":2}`）；
> **撤权之后立刻** ⇒ `{"error":"你还没允许它用你的钥匙"}`。
>
> **九件工具全了**（造/看/发/撤/发现/装/卸/授权/撤权）—— 授权与撤权这一批才挂上
> （前两批**故意不挂**：那条路没定的时候，挂上去就是给他一个按了没用的开关）。
>
> **判据 +6**（`app-ask.test.js`）：四道闸各说各的 · 请求真打到本机代理的 `/chat/completions`
> 且页面那句话原样进去 · 没钥匙时如实说 · **先记再花** · 撤权立刻不通 ·
> 🔴 **租户那条接缝**（闸没过**一个字节都不转发**；过了才带"闸过了"的头、身体重给一份）。
> 读数：服务端 **678 pass / 0 fail** · 客户端 unit **285** / 可访问性 **169** / widget **148** ·
> 产品层快照翻到 `bc5595bce21f`（`--verify` 真跑一个容器、它自报同一指纹）。
>
> ⚠️ **主人那一份还没接上**：他那台机器上没有匣子，钥匙在 DSH 自己的凭据里 ⇒ 他会看到
> `这台设备上还没接上问话那条路`。**要接上得单独立一条**（记在 `59` §八）。

> ## v1.41 · **乙-4 的前半：同意与告知的地基**（`ask` 那条路要你点名）（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §七·补三/四 · 这一页 ·
> `docs/dev/00-PROGRESS.md` §六（**#55**）· 服务端 `src/{apps,apps-socket}.js` ·
> `src/mcp-apps-server.mjs` · `hupo-persona.yml`（**strict**）· 客户端
> `screens/discover_screen.dart` · `models/app_words.dart` · 判据四份。
>
> **主人点名要的是**：*"使用的时候，涉及到的 apikey 是自己的，不是开发者的。"*
> ⇒ 查过代码之后，**"用他自己的钥匙"没有现成的路**（三条都要动"本轮不建"的东西或改容器）：
> ① 走 agent（钥匙天然对，但**没有只读档** ⇒ 别人写的话进全权执行体，正是最怕的形状）；
> ② 走直连模型（天生安全，但 **L2 拿不到钥匙**：租户那条要改容器镜像）；
> ③ 给 dsh 配"这一轮不给工具"的 mode（ACP 有 `session/set_mode`，profile 没配 ⇒ strict 文件 + 新决策）。
> ⇒ **记成 #55，要主人点名**。
>
> **在等这个决定的同时，这一批把不依赖它的那一半做完了**：
> · **权限白名单开门**（`ask` 可以声明，清单与装上都会带着）；
> · 🔴 **如实告知**：「发现」那一屏上，要"用你自己的钥匙"的那条**写着这句话**（装上之前就看得见）；
> · **授予状态**（`grant.json` —— **他**的决定，不是作者写进清单就生效）；
> · **配额常量**（每天每 app 次数 + 两次最小间隔，数字只住 `apps.js`）；
> · **卸载**：`app_uninstall`（**软删**：挪进 `.removed/`，能拿回来）+ 审计；
> · **人格直说"它现在还不能问话"**（今天这是实话 —— 不许嘴上先答应）。
>
> ⚠️ **授权/撤权的工具故意没挂**：那条路还没定，挂了就是给他一个"按了没用"的开关
> （判据钉着"这两件不许挂"）。**这就是"做一半"的反面**：没做的那一半**明说在等决定**，
> 而不是摆一个假的开关上去。
>
> 读数：服务端 **672 pass / 0 fail** · 客户端 unit **285** / 可访问性 **169** / widget **148** ·
> 人格检查过 · 文档闸 11 份全绿。

> ## v1.40 · **乙-3：发出去、看得到、装得上**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §二/§三·补/§七 · 这一页 ·
> `docs/dev/00-PROGRESS.md` §〇 · 服务端 `src/published.js`（新，共享库）·
> `src/{server,worlds,apps-socket,mcp-apps-server}.*`（路由 / 按人挂 / 四条新工具）·
> 客户端 `screens/discover_screen.dart`（新）· `models/app_spec.dart` · `services/api.dart` ·
> `services/chat_controller.dart`（`app/installed`）· `screens/chat_screen.dart` ·
> `test/{apps-chain,capabilities}.test.js` · `test/{unit/app_spec,widget/remote_app,widget/accessibility}_test.dart`。
>
> **主人两句话**：*"我还需要一个发现按钮，可以看到其他人发布出来的小程序"* +
> *"一切都是用户自己的对话中实现，包括发布。"*
>
> **两条形状决定**（写进 `59` §三·补，这一批全靠它们）：
> ① **发布 = 复制一份进共享库**；**装上 = 复制一份进他自己那一格**
> ⇒ **制品口那条路一行都没改**（还是乙-1 那套签名/校验），而且**作者下架或删掉、装过的人那份还在**；
> ② **id 全局唯一**（重名直接拒）：客户端那个图标按 id 认人，两个人都发 `dice` 会让"装哪个"说不清。
> ⚠️ 共享库里**不存作者身份**，只存 `sha256(sub)` 前 12 位；对外昵称也**按哈希生成**（`用户 4c10`）
> —— **手机号那种东西绝不上这儿**。
>
> **现在真的能**（线上真验过）：他说「把『掷硬币』发出去」⇒ 共享库里真多一份、`/api/discover` 能查到；
> 桌面上的「发现」那一屏列出来了（`⭐ 掷硬币 / 用户 4c10 发的 · 第 1 版`），
> 并**明说**"看到想用的，跟助手说一声「装上」就行"（那一屏**只读**：装/发/改都在对话里）。
> 助手还主动说清了那件重要的事：*"他们装走的是各自的副本，以后你改了自己这份，装过的人那份不会跟着变。"*
> ⇒ **桌面自己长出来**：装上了 ⇒ 服务端往流里推一条 `app/installed`（瞬时、不写盘）⇒
> 客户端收到就重拉清单（**不用刷新页面**），有判据钉住。
>
> **判据新增 15 条**：共享库与链路 9 条（发布进发现 · **私有的那条不在发现里**（负向）·
> 重名明确拒 · 装上=复制进他自己的那一格 · **下架之后已装的那份还在** · 下架后再装⇒拒 ·
> 审计三行且**只出现作者哈希**）+ 客户端 6 条（发现只读且明说 · 空态实话 ·
> `app/installed` ⇒ 桌面自己长出来 · 发现条目解析）。
> 读数：服务端 **670 pass / 0 fail** · 客户端 unit **285** / 可访问性 **169** / widget **148**。
>
> ⚠️ 两条既有判据被这一批**顶红**了（它们记得"发布/装上还没做"）—— 那正是判据该有的反应：
> 一条改成"只剩卸载/授权不许挂"，一条改成"现在是六件工具"。**改了能力就要回头改判据的预期**，
> 但**不许为了让闸变绿而删判据**。

> ## v1.39 · **乙-2：在对话里就能造小程序了**（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md` §二/§七/§七·补二 · 这一页 ·
> `docs/dev/00-PROGRESS.md` §〇 · 服务端 `src/{apps-socket,mcp-apps-server}.mjs`（新）·
> `src/{config,worlds,agent-runtime}.js` · `hupo-capabilities.yml`（**strict**）·
> `hupo-persona.yml`（**strict**）· `test/{apps-chain,capabilities}.test.js` ·
> `v2/services/core/scripts/check-capabilities.mjs`。
>
> **主人**：*"一切都是用户自己的对话中实现，包括发布。"* ⇒ 这一批把"**造**"这条路接通了。
>
> **现在真的能**（线上真验过，不是推演）：
> ① 他在对话里说「帮我做一个小程序：名字叫掷硬币…」⇒ 助手调 `app_create` ⇒
> **制品落进他自己那一格**（`data/hupo/apps/coin/versions/1/`，0444 只读）⇒ **桌面上多一个图标**；
> ② 他说「我有哪些小程序」⇒ `app_list` ⇒ 它把两个都报出来了（掷硬币 / 掷骰子）。
>
> **判据 8 条新增**：真链路 6 条（测试 → stdio JSON-RPC → 域套接字 → 盘；**工具进程不写盘** ·
> **通道不通 ⇒ 明确失败**（不是"好了"）· **还没做的那五件 ⇒ 明说"还没做"** ·
> 坏输入只让那一条失败 · 通道 0600 · stdout 只有 JSON-RPC）+ 能力层 2 条
> （两条 MCP 都在、**没做的工具不许挂**）。
> 读数：服务端 **666 pass / 0 fail** · 慢闸（真起 dsh）三段全过（录音 6 条）· 人格检查过。
>
> ⚠️ **当场踩到两条，都记进 `59` §七·补二**：
> ① 🔴 **同一份人格里两句话打架** —— 早先那条"帮我做小程序 ⇒ 你做不到"在能力做出来之后
> **变成了假话**，而它**还留在人格里** ⇒ 线上真出现"上一轮说做不到、下一轮做出来了"。
> 修法：把边界**收窄到"改界面"**，并明写旧那句现在只对"改界面"成立。
> **教训：加一条能力，要回头查一遍有没有旧话在否认它。**
> ② 🔴 **慢闸抓到 `invalid config`**：`hupo-capabilities.yml` 里两条 `!!js process.env.…`
> 在没设变量时求值成 `undefined` ⇒ dsh 判 invalid config、**整棵树加载失败**；
> 而那个慢闸脚本是账本那批写的，只给账本设了变量。
> **教训：加第二条同类插件，所有"给第一条设环境"的地方都要跟着改。**

> ## v1.38 · **乙-1：真制品小程序的地基**（独立原点 + 沙箱 + 不可变版本 + 审计）（2026-09-22）
>
> **改了哪几处**：`docs/dev/59-USER-APPS.md`（新，契约）· 这一页 · `docs/dev/00-PROGRESS.md` §〇 ·
> 服务端 `src/{apps,app-serve}.js`（新）· `src/{config,server,serve,worlds}.js` ·
> 客户端 `models/app_spec.dart`（新）· `widgets/{mini_runtime,mini_runtime_web,mini_runtime_stub,mini_app_icons}.dart`（新）·
> `screens/chat_screen.dart` · `scripts/check-web-browser.mjs`。
>
> **主人三句话**：*"我要做乙"* + *"还需要一个发现按钮……apikey 是自己的，不是开发者的"* +
> *"一切都是用户自己的对话中实现，包括发布"*。⇒ **本批是乙-1**：先把"真制品"的地基立起来。
>
> 🔴 **它同时推翻 `08-SPEC.md` §7.3「【不建】小程序运行时（本轮）」** —— 这一批在建。
> **N1 / N2** 从"将来做时的前提"变成**现在的判据**。
>
> **这一批落地的**：① 按人一份的制品库（`<他那一格>/hupo/apps/`）——**这就是"只有他可见"的落点**；
> ② 管线（校验 → hash → **不可变版本** → 指针 → 审计；**校验不过盘上一个字节都不动**）；
> ③ **第二个原点**（壳 8020 / 制品口 8021 ⇒ 浏览器眼里就是两个原点）＋
> **现签的入口 URL**（绑人 + 绑版本 + 短时效）＋ CSP（`default-src 'none'`、`connect-src 'none'`、
> `frame-ancestors 壳`）＋ **绝不发 `X-Frame-Options`**；
> ④ Web 沙箱：`<iframe sandbox="allow-scripts">`，**故意不给 `allow-same-origin`**，
> **不给任何回话通道**（"制品拿不到能力"是结构上成立的）；非 Web **说一句人话**、**不许白屏**；
> ⑤ 桌面把 `/api/apps` 拿回来的一条条摆成图标，点开跑在另一个原点的沙箱里。
>
> **判据 28 条**（服务端 19 + 客户端 9）：其中三条最要紧 ——
> **甲的清单里绝不会有乙的**（负向对照）· **两张图标白名单不许漂**（客户端读服务端源码逐字对）·
> **iframe 的 origin ≠ 壳的 origin 且 sandbox 里没有 `allow-same-origin`**（浏览器那一侧的新检查，
> 违规直接退出码 5）。读数：服务端 **659 pass / 0 fail** · 客户端 unit **284** / 可访问性 **159** / widget **145**。
>
> ⚠️ **两处如实记下**：① 计划里写"制品服务是独立进程"，**这一批做成同进程第二个口**
> （理由：这台机器已经有一笔"服务不在 systemd 下"的账，不再多一条没人管的进程；
> 对浏览器而言它仍然是另一个原点）；② **生产第二原点没建**（VPS 上 13 个别站，**要主人签字**）
> ⇒ 公网还看不到小程序，**本机能真验**。其余欠的写进 `59` §八。

> ## v1.37 · **第二个小程序：奥数题（内置题库）**（2026-09-22）
>
> **改了哪几处**：`docs/dev/57-MATH.md`（新）· 这一页 · `docs/dev/00-PROGRESS.md` §〇（52）·
> `models/{math_bank,math_words}.dart`（新）· `screens/math_quiz_screen.dart`（新）·
> `screens/chat_screen.dart` · `test/widget/math_quiz_test.dart`（新）· `accessibility_test.dart`。
>
> **起因**：主人 *"我让它做一个四年级 mca 奥数题库小程序。结果挂掉了。"* —— 查清是两件事
> （`00-PROGRESS.md` #52/#53：助手**假装能做** + 撞单轮输出上限）。
> 我把三条路摆出来，**主人选 A：内置题库**，由我（开发者）来做。
>
> **这一刀的形状**：桌面上第二个图标（奥数题）；点开从图标扩开到全屏；
> 一屏一道题（进度 + 题目）· **看答案**点开才给 · **换一题**随机换且**答案自己收起来**。
>
> 🔴 **最要紧的一条 —— 每道题都必须能被人验**：
> 题库每一条是 `(题目, 答案, 一句话"为什么")`，**界面上永远把"为什么"一起给**；
> 而且判据里**用代码把能算的 21 道重算了一遍**（等差和 · 鸡兔同笼 · 植树 · 盈亏 · 相遇 ·
> 追及 · 火车过桥 · 归一 · 容斥 · 方阵 · 锯木头 · 敲钟 …）。
> ⚠️ **理由**：**一道算错的奥数题，比"没有题"坏得多** —— 这个项目最忌"看着像真的"。
>
> **顺带做的两件**：① 容器从"只装得下一个 app"（一个布尔）改成**按名字开**（同时仍只开一个）；
> ② **新界面按规矩注册进了可访问性硬闸的两处**（D3.5 五档不溢出 + D3.6 命中区）。
>
> ⚠️ 过程中又栽了一次**同一类形状**（第三次）：给桌面图标加了阴影之后，
> `浮窗有阴影` 那条判据**全树扫**，先扫到了**图标**那个阴影 ⇒ 判据红，而浮窗其实没问题。
> ⇒ 修法：把扫描**限定在浮窗子树里**。**判据要钉在"这件事独有"的东西上。**
>
> **判据**：`math_quiz_test.dart` 7 条（题库形状 + 21 道重算 + 禁用词 + 那一屏的两条）；
> 可访问性 **+10**（五个字号档 × 两条闸）。读数：客户端四道闸全过
> （unit 279 / 可访问性 **159** / widget **141**）。

> ## v1.36 · **补全：首页多了"看得见它在做""帮你记账"和"你的东西是你自己的"**（2026-09-22）
>
> **改了哪几处**：`docs/dev/56-PLAIN.md` §一（**完整版**）· 这一页 ·
> `docs/dev/00-PROGRESS.md` §〇（51）· `landing_words.dart` · `landing_screen.dart`。
>
> **主人看完 v1.35 的回话只有四个字**：**"不够全面"**。对——那一版只讲了**能做什么**，
> 缺了三块：**它是谁的 / 你的东西怎么保的 / 它靠什么不骗你**。
>
> **这一版补上了什么**：
> 1. `56-PLAIN.md` §一 重写成**完整版**（八节）：它是谁给谁用 · 一个人一台 ·
>    **能做的事 11 条** · **它靠什么保证不骗你（4 条规矩）** · 现在的样子 ·
>    还不能用的 5 条 · 它现在有多大 · 最本质的一句。
> 2. 首页卡片 **4 → 6**：多了 **看**（它办的时候你看得到）和 **算**（帮你记账）。
> 3. 首页新增一块 **「你的东西是你自己的」**（三条）：一台只属于你的机器 ·
>    你的钥匙只在你那儿（不进记录不进日志）· 你想删就删、想带走就带走。
>    ⚠️ 这一块回答的是**担心**，不是能力 ⇒ 单独一个标题，不跟卡片混。
>
> ⚠️ 老规矩一条没松：承诺照抄手册、安卓只说"还没上线"、新句子全进禁用词扫描、五档不溢出仍过。

> ## v1.35 · **首页重排：用最白话把"这是什么、能做什么"说清**（2026-09-22）
>
> **改了哪几处**：`docs/dev/56-PLAIN.md`（新）· 这一页 · `docs/dev/00-PROGRESS.md` §〇（㊿）·
> `landing_words.dart` · `landing_screen.dart`（加一个小标题）。
>
> **主人**：*"总结整个项目，用最白话的语言，告诉用户这是以什么项目，有什么能力。
> 然后重新规划设计首页内容，能让用户看明白。"*
>
> **五条设计原则**（进了 `56` §二，改首页文案之前先读）：
> ① 第一句就回答"这是什么"（用**具体身份**，不用形容词）；
> ② 能力**用"事情"说**，全篇不出现"智能/强大/高效"；
> ③ **先给"现在能用的"，再说"还不能用的"**（诚实，但别当头泼冷水）；
> ④ 每块只有一个主题，**小标题本身就是那句话**；
> ⑤ 不许内部词、不许写做不到的事（禁用词硬闸 + "说了做不到 = 撒谎"）。
>
> **首页改成了什么**：身份句改成
> *"它是一个替你办事的助理。你说一件事，它记住、去办，办完回来告诉你结果。"*；
> 加小标题 **"它能替你做什么"**；卡片从三张加到**四张**（记 / 办 / 实 / **回** ——
> "删错了能拿回来"以前只在别处说过）；对照块四行**改成讲形态**（不再和卡片重复）；
> 平台那三张把**"网页版"排到最前**（只有它能用）；FAQ 从三条加到**六条**
> （多了"它会不会瞎编""说错删错怎么办""有语音吗"）。
>
> ⚠️ **没有放宽任何一条既有规矩**：那句承诺仍然照抄手册、安卓入口仍然只说"还没上线"、
> 新加的每一句都进了**禁用词扫描清单**、五档不溢出仍然过。
>
> ⚠️ 排这一版踩了两条（记进 `56` 末尾）：**`scrollUntilVisible` 内容一多就会冲过头**
> （判据改成"一路滚、边滚边收"）；**判据自己会"滚走"**（顶部的内容要在滚动之前查）。

> ## v1.34 · **语音只做"形状"（先做假的）**（2026-09-22）
>
> **改了哪几处**：`docs/dev/55-VOICE-DEMO.md`（新）· 这一页 ·
> `docs/dev/00-PROGRESS.md` §六（**#52**）· §〇（㊾）。
>
> **主人**：*"对话框要学习微信。要能切语音，能切听筒。语音和听筒都要实时转文字。"*
> ⇒ 我把**两处卡点**摆出来问他（见下），他答：**"先做假的。"**
>
> **改成了什么**：形状全做真的（话筒/键盘 · 按住说话 · 听筒/扬声器 · 实时文字区），
> **数据明标「演示」**。🔴 **纪律两条，缺一条就是缺陷**：
> ① **「演示」小标常驻** + 一句实话（*"演示：还没有真的开麦，这些字是假的。"*）；
> ② 吐出来的字**本身**写着"这是一句演示"；**绝不出现"正在听"**；**松手不自动发送**（D5.4）。
>
> **两处卡点（都没解决，如实记）**：
> 1. 🔴 **声音走哪条路 = 与 D5.2 直接冲突**：`D5.2` 定的是"**手机自带的识别器**"
>    （原话：云端 ASR = 声音出境给第三方，**与 P1 同类**；地库恰好最需要它，云方案在那归零），
>    而**现在唯一上线的客户端是网页版** —— 网页上的识别器是**云端**的。
>    三条路（云端 / 本地 WASM / 先做安卓包）各有代价，**要主人定**。
> 2. ⚠️ **"听筒"手册里根本没有**：`D5` 只定了"进"（话筒→字），没定"出"（把助手的话读出来）；
>    而且听筒是**手机上的概念**，网页上没有这个出口。
>
> ⚠️ 另外记着：**批 5（语音）是主人 2026-09-21 亲口砍的**（"语音不做"），
> 理由是"输入法自带语音已经覆盖了一部分" ⇒ 真要做，得先回答"**为什么还要自己做**"。
> ⚠️ 真接上不是"加个插件"：`D5.10` 要求**四类各 20 条语料、句子级命中率 ≥90%** 才准上线。
>
> **判据**（`test/widget/voice_demo_test.dart`，6 条）钉的是"**假得够不够明显**"，
> **不是**"功能对不对"（功能还没有）。

> ## v1.33 · **点"说点什么"⇒ 窗口自动打开**（2026-09-22）
>
> **改了哪两处**：`08-SPEC.md` §6.3（交互表补一行）· 这一页 · `docs/dev/00-PROGRESS.md` §〇（㊽）。
>
> **主人**：*"点击说点什么，聊天窗口会自动打开。"*
>
> **这一条手册里本来就有**（§6.3：*点收起态底部条 ⇒ 展开到上次档位*）—— 收起态有了输入框之后，
> 点那个框就是"点收起条"，只是以前没人把它接起来。
>
> **做法**：`Composer` 多一个 `onFocused`（挂在 `TextField.onTap`），上层接
> `_floaterKey.currentState?.expand()`；**「展开」按钮与它走同一个 `expand()`**
> —— 两处各写一套迟早分叉。
> ⚠️ **展开不会丢字**：上下两态用的是**同一个 `Composer` 实例**，Flutter 认得出它、把它**挪过去**
> （不是重建）⇒ `TextEditingController` 与焦点都活着。判据钉着这一条
> （"点输入框 ⇒ 窗口开了 **且字还在**"）。
> ⚠️ **过程中栽了一次**：`onTap` 那处替换**没落地**（`dart format` 改过缩进、我的锚点对不上），
> 而判据当场红了 —— **这就是"改完必须跑闸"的意义**（光看 patch 打成功不算数）。

> ## v1.32 · **收起态也有输入框**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §6.2（补一条）· §10.1 阈值总表（"收起高度"那行写回"输入条"）·
> 这一页 · `docs/dev/52-DESKTOP.md`。
>
> **主人**：*"助手那个聊天窗口，收缩的时候也有一个输入框。"*
>
> **这一条其实是"恢复"**：阈值总表原来就写着收起高度 = **抓手 + 标题行 + 输入条** ——
> 中间那版把输入条只留在展开态，**做丢了一件**。
>
> **实现上有一个关键选择**（值得记）：输入条从 `_sheetBody` 里**拆出来单独传给浮窗**，
> 上下两态共用**同一个** `Composer` 实例。两处各建一个的话，"在收起态打了一半、再点展开"
> 会换一个 `State` ⇒ **框里的字就丢了**（那是最气人的丢法）。判据钉住了这一条。
>
> ⚠️ 连带：**判"展开了没有"要看只有展开态才有的那个「收起」**，不能再看"输入条在不在"
> —— 7 条既有判据因此改了口径（它们原来拿 `Composer` 当档位信号）。这是同一类形状的第二次：
> **判据要钉在"这件事独有"的东西上**，别钉在一个两边都有的东西上。
>
> **读数**：客户端四道闸全过（unit 279 / 可访问性 149 / widget **127**）。

> ## v1.31 · **小程序有默认图标了**；以及一条"线上才看得见"的字体坑（2026-09-22）
>
> **改了哪一处**：`docs/dev/52-DESKTOP.md` §6.3（新）· 这一页 · `widgets/app_desktop.dart`。
>
> **主人**：*"设置要给一个默认 icon。"* + 追问后补一句 *"目前没有 icon，不是齿轮"*。
>
> 1. **默认图标进框架**：`DesktopApp.icon` 变成可选，默认 `defaultAppIcon`
>    （`Icons.widgets_outlined`）⇒ 以后加小程序**不写 icon 也不会空着**。有判据。
> 2. ⚠️ **一条线上才看得见的坑**：那个默认值**必须是写在代码里的常量** ——
>    `flutter build web`（release）默认 **tree-shake 图标字体**（实测：线上那份
>    `MaterialIcons-Regular.otf` 只有 **9.4KB**，是子集）⇒ **运行时算出来的 `IconData` 会被剪掉**，
>    于是**测试里好好的、线上画不出来**。⇒ 图标一律用常量（`Icons.xxx` 字面量，或常量默认值）。
> 3. **诊断记一笔**：主人那边桌面"没有图标"，而**我这边同一个构建的线上截图里是有齿轮的**
>    （`/tmp/desk-icon.png`，那次 `curl` 也确认字体是 `no-cache`、用前先问）。
>    ⇒ 遇到"**我这儿有、他那儿没有**"，**先请对方硬刷新一次**再动代码 ——
>    这与 `53-MOTION.md` §六 那条同类：**先怀疑你看的那一份，再怀疑代码**。

> ## v1.30 · **浮窗内边距收小 · 桌面排布 · 图标阴影 · 修「退出小程序」那个 bug**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §6.3（补一条用词）· 这一页 · `docs/dev/52-DESKTOP.md` ·
> `docs/dev/54-COMPOSE-DRAFT.md` · `docs/dev/00-PROGRESS.md` §〇（㊺）。
>
> **主人一次说了四件**：*"聊天浮窗的 padding 减少一些，桌面排布好一点，
> 我看小程序图标顶部 Margin 可以增加一些。小程序图标要有阴影。另外退出小程序有个小 bug。"*
>
> 1. **浮窗的内边距收小** —— 他说的是 **padding**（里面那圈），不是 **Margin**（外面那圈）
>    ⇒ 外面那个 **30 是 Z4，没动**。里面：抓手行 `8 → 4` · 时间线 `12 → 8` · 输入条 `12/8 → 8/6`。
>    ⚠️ 顺手把这条**用词**写进 §6.3：**里面那圈叫 padding，外面那圈叫 margin** ——
>    两个词混着说，改的人**一定会改错那一处**。
> 2. **桌面排布**：图标区**顶部留白 8 → 48**（他："图标顶部 Margin 可以增加一些"）·
>    图标间距 `22 → 24` · 格宽上限 `92 → 88`（收紧一点）。
> 3. **图标加阴影**：`BoxShadow(ink α.12 · blur 12 · offset(0,4))` ——
>    比浮窗那个（α.45）浅得多：图标是一小块，用同一个 α 会脏。
> 4. 🔴 **修「退出小程序有个小 bug」**：**根因不是动画**，是**退出后那一块没被清掉** ——
>    `build()` 里那句"没开而且收回了 ⇒ 什么都不画"**不会因为动画结束而重跑**
>    （只有里面那个 `AnimatedBuilder` 会重建）⇒ 关掉之后**那一块还留在图标的位置上**，
>    而且**它是可点的** ⇒ 正好压在图标上、**把图标挡住了**：**退出之后再也点不开小程序**。
>    ⇒ 修法：给控制器挂 `addStatusListener`，收回那一刻 `setState` 一次。
>    **判据（回归）**：退出后 `SettingsScreen` 与容器顶栏**都不在树里**，
>    而且**再点图标必须还能开**（且全屏）。
>
> **读数**：客户端四道闸全过（unit 279 / 可访问性 149 / widget **125**）。

> ## v1.29 · **聊天浮窗改版（微信那样）+ 小程序从图标扩开**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §6.3（补"展开态的收起必须一直看得见"）· 这一页 ·
> `docs/dev/54-COMPOSE-DRAFT.md`（新）· `docs/dev/52-DESKTOP.md` §6.2。
>
> **主人一次要了四件**（原话）：*"聊天浮窗要改版，改成微信那样。就是要有一个空的输入框，
> 但如果用户输入过，没发送，则显示在上面作为草稿。草稿也是要记住的。
> 然后展开后要有收回的按钮，另外小程序点开要有效果，就是从哪里打开，就从哪里扩开到全屏的效果。"*
>
> 1. **空的输入框 + 上面那条草稿**：新增 **`compose_store.dart`（第三本账）**。
>    ⚠️ **它不是** `draft_store.dart` 那本（那是"**按过发送**但没被认领"的话，属于时间线）；
>    这一本存的是"**压根没按发送**、只在打字框里的字"。混成一本 ⇒ 刷新后同一句话**画两遍**。
>    规则：框是空的，草稿在**上面**单独一条（"你打了一半" + 原话 + 「接着写」/「不用了」）；
>    点「接着写」⇒ 字回框、那条收起；**一边打一边存**；**发出去就不是草稿了**；**退出登录必须清**。
> 2. **展开态的「收起」钉在横滚之外**（§6.3 新增一行）：它原来在那条横向滚动的动作里，
>    窄屏 + 大字号下会被滚出视野 —— **想收起来时找不到出口**，那就等于没有出口。
> 3. **小程序从图标扩开到全屏**：图标点的时候**把自己在屏幕上的矩形报上来**
>    （`DesktopApp.onOpen(Rect)` —— 图标自己量，上层不用猜），容器用 `Rect.lerp`
>    从那个矩形长到全屏（`motionPage` / `easeOutCubic`）。⚠️ 内容是**按全屏排版、被那块矩形"露出来"**的，
>    不是"一个小窗被放大"。
>
> **判据**：`compose_store_test.dart`（5）+ `compose_draft_test.dart`（5）+
> `mini_app_test.dart` 加到 8（新增"第 0 帧还不是全屏、起点在图标附近、收尾全屏"）。
> ⚠️ 这一批自己踩的两个坑记在 `54` §五：**判据要量"那个在动的东西"**（量错对象会得到假读数）、
> **参照物别拿文字当图标**。

> ## v1.28 · **小程序是全屏的**（2026-09-22）
>
> **改了哪一处**：`08-SPEC.md` §6.4 补一句"它是全屏的" · 这一页 · `docs/dev/52-DESKTOP.md` §六。
>
> **为什么**：主人看过 v1.27 之后说 *"桌面小程序打开后，是全屏显示的，
> 只不过聊天窗口还在底下那里。"* —— 我那版把它做成了**带边距的小窗**（`Padding` 四边 + 常驻圆角），
> 那不是他要的形状。⇒ 现在：**内容全屏**（不要外边距、不要常驻圆角），
> **聊天浮窗照旧浮在底下**（Z1 不变）。
>
> ⚠️ **§6.4 规则 1 的落点跟着挪**：「收起时不许压住小程序里可点的东西」现在是
> 给**内容**加底部内缩（`Padding(bottom: bottomInset)` 包住容器里那个 `Navigator`），
> **不是**给窗口加边距 —— 窗口已经全屏了。
> ⚠️ 圆角只在**被聊天盖住**时出现（规则 2："要看得出来被盖住"）。
>
> **判据**：`test/widget/mini_app_test.dart` 加一条 ——
> **小程序矩形 == 屏幕矩形**（全屏），内容全宽，而**聊天那条还在底下**。

> ## v1.27 · **聊天不是桌面上的小程序；桌面上是「设置」**（2026-09-22）
>
> **改了哪几处**：`05-DECISIONS.md` G 组加 **D4.12 / D4.13** · `08-SPEC.md` §6.4
> （「小程序本轮不建」→ **已建**，并写明落了哪几条、哪条还没判据）· 这一页 ·
> `docs/dev/52-DESKTOP.md`。
>
> **主人把三者的关系定清了**：*"聊天和桌面是独立的，聊天是永续的，永远在底下。
> 所以聊天不是桌面上的一个小程序。桌面上应当有一个设置的小程序，用来退出登录，注销账号，修改 apikey。"*
>
> **改成了什么**：
> 1. **桌面上那个「会话」图标删掉**（D4.12）—— 聊天永续、永远在底下，它不需要一个入口，
>    也不会被"打开 / 关掉"。⚠️ 这与 D4.11（图标墙是第一入口）不冲突：入的是**小程序**。
> 2. **桌上换成「设置」**（D4.13），里面管**退出登录 / 注销账号 / 改钥匙**。
>    **退出登录从聊天抓手行搬了进来** —— 那一行是"聊天"的地方，退出登录不是聊天的事。
> 3. 新增 **`lib/widgets/mini_app_host.dart`**（小程序容器）：**容器给顶栏**、内容跑在
>    **容器自己的 `Navigator`** 里（它内部怎么跳都跳不出这个容器）⇒ §6.4 的
>    **1 / 2 / 3 / 4 / 5 条全部落地**，判据 6 条（`test/widget/mini_app_test.dart`）。
>
> 🔴 **过程中被闸抓到一个真 bug**（值得记）：第一版把 `InkWell` 只包在**图标格**上，
> **图标下面那行字在外面** ⇒ 点"设置"这行字落到了"点桌面空白"上（只收起了聊天）。
> ⚠️ 更坏的是：那时"配置页 / 关于页（从真入口进）"两组判据**照样绿** ——
> 因为它们扫的是**桌面**，根本没进设置那一屏。⇒ 两处一起修：**字也要能点**，
> 并给那个入口补一条**负向对照**（点完必须真的到了 `SettingsScreen`）。
>
> ⚠️ **另一条一起改的**：那两组判据原来让聊天以 `full` 进场 ⇒ **浮窗把桌面盖住了**，
> 点图标会点到浮窗上。⇒ 改成**默认收起档进场**（那才是真实路径：一进来就是收起）。

> ## v1.26 · **通知报给谁：拍板 + 一条真 bug**（2026-09-22）
>
> **改了哪几处**：`05-DECISIONS.md` 新增 **N 组（N1 / N2）** · 这一页 ·
> `docs/dev/29-NOTICE.md` §九（新） · `docs/dev/00-PROGRESS.md` §六 #51 结案。
>
> **起因是主人一条观察**：*"登录后，出现刚才出了点事，我已经重来了。但是其他手机的并没有出现这段话。"*
> 查下来那句话里**藏着两件事**，长得一模一样，但**一件要修、一件要拍板**：
>
> 1. 🔴 **真 bug（已修）**：那条是很久以前的崩溃通知，却**每次登录都弹一次浮窗**。
>    两边各说各话 —— 服务端（决策 P-h）说 `sinceSeq == 0` 是**历史本身**、**故意不打** `catchUp`；
>    客户端却把"没有 `catchUp`"当成了"现在发生的" ⇒ 冷启动整段历史都被喊了一遍。
>    ⇒ **N2**：历史（补发**和**首屏）只进时间线、不弹浮窗；只有连上之后新来的才喊。
>    修法不动协议：拿服务端必发的 `client/hello` 当边界。
> 2. ✅ **设计（已拍板）**：为什么其他手机没有 —— **宿主级崩溃通知只写进主人那一份世界**
>    （`serve.js` 的窄口）。**N1**：保持只报主人。**理由**：宿主重启只打断主人那一份世界
>    （租户的世界跑在自己容器里）⇒ 给租户报就是**无事也报**，而那是 R1.2 要禁的形状。
>
> ⚠️ 记一条形状（进 `29-NOTICE.md` §9.4）：这一改让 **6 条既有用例红了**，它们都**直接往
> 控制器喂事件**（绕过了流）⇒ 修法是**在用例里补上真实路径那一步**，
> **不是把默认值改回去哄断言**（同一天已经栽过一次：`initialTier`）。

> ## v1.25 · **页面切换 320ms → 400ms**（2026-09-22）
>
> **改了哪一处**：`08-SPEC.md` §10.1 阈值总表那一行 · 这一页 · `docs/dev/53-MOTION.md` ·
> `models/design.dart` 的 `motionPage`（**唯一的数值出处**）。
>
> **为什么**：主人看过 v1.24 的动效之后说 *"过渡时间增加到 400ms"*。
> ⚠️ 这是**他的观感判断**，不是我们算出来的 ⇒ **别"顺手调回去"**；
> 判据里那个 200–500ms 的区间仍在，所以 400 合法（它管的是"不许短到等于硬切、
> 也不许长到像没反应"）。
>
> ⚠️ **同一批改了四处**（数值只许有一个家，但**说明它的话**散在手册与文档里 ——
> 改数就得一起改，不然下一个人读到的就是两个数）。

> ## v1.24 · **首页 → 登录页：丝滑地换，不是硬切**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §10.1 阈值总表（新增一行"页面切换 320ms"）· 这一页 ·
> `docs/dev/53-MOTION.md`（新）。
>
> **为什么**：主人 *"从首页，点击开始，到登录页显示，我希望是一个**丝滑的过渡**展示效果，
> 而不是**突然出现**的效果。"* —— 原来 `main.dart` 里是
> `_showLogin ? 登录页 : 首页`，那是**硬切**：一帧之内旧屏没了、新屏整个出现，
> 观感上就是"闪了一下"。⚠️ 这条不只是好看：走查里"**它到底有没有反应**"是这个项目
> 最贵的那个问题，而**没有动画的界面看起来像坏了**。
>
> **改成了什么**：新增 `widgets/soft_switch.dart`（`AnimatedSwitcher`）——
> **淡入淡出** + **新屏轻轻上浮**（行程 0.03 屏高：是"上浮"不是"滑进来"，滑进来要**等**它）；
> **退场那一屏 `IgnorePointer`**（动画那半秒里不许点到已经走了的东西）；
> 两条路（"开始用"过去 / 箭头回来）**共用同一次过渡**。
> ⚠️ **不动 `Navigator`**：这两屏是"同一个页面里的两个状态"，换成路由会把
> "回首页那个箭头"和退出登录那条路一起改掉。
>
> **判据**（`test/widget/landing_transition_test.dart`，**4 条**）：
> **硬切与过渡的唯一可观察差别 = 动画中途两屏是不是同时在** ⇒
> 点下去之后**先别 `pumpAndSettle`**，看此刻树里有什么（硬切的话首页已经没了）；
> 再验"中途不透明度在 0~1 之间"、"收尾时新屏完全不透明"、"退场那屏被 `IgnorePointer` 包住"。
> ⚠️ **只断言"点完能到登录页"是测不出硬切的** —— 硬切也能到。

> ## v1.23 · **更正：桌面铺满整屏，聊天窗口贴底**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §6.1 **Z4**（重写）· §10.1 阈值总表（边距那行的注）· 这一页 ·
> `docs/dev/52-DESKTOP.md`（重写 §一 形状 + 补一条根因）。
>
> **为什么要改**：**v1.22 那一版是我读错了主人的话。** 他第一句是
> *"首先，页面底部是一个桌面…"*，我读成"**桌面 = 屏幕最下面那一条**"，
> 还专门做了两种摆法问他——**他当时也选了"那一条"**。等他在真站点上看过，才更正：
> *"**桌面是全屏的，聊天窗口是在底部的。**"*
> ⇒ 正确形状：**桌面铺满整屏**（底图）、**聊天浮窗贴在屏幕底部**浮着、四边各 30。
>
> **代码怎么改的**：`Column[浮窗, 桌面那条]` → `Stack[Positioned.fill(桌面) , Positioned(左/右/下 30, 浮窗)]`。
> ⚠️ 浮窗能用的高度因此从 `MediaQuery` 算（**不在 `Positioned` 里套 `LayoutBuilder`** ——
> 那会让整棵树报 `ParentDataWidget` 错）。
>
> **顺带修掉一个量出来的形状 bug**：按 4 列算列宽，在 1280 宽的屏上是 **293px**，
> 而我把它直接当了图标格边长 ⇒ 桌面上出现**一张 293×293 的大白卡**。
> ⇒ 图标格固定成 52（≥44），列宽只决定位置并**封顶**；判据加了"宽屏与手机下图标位置一致"。
>
> ⚠️ **教训（记进 `52` 开头）**：**"问他选一个"不等于"问清了"** ——
> 选项要**带图**；光靠"整页底图 / 底部一条"这种文字，两个人脑子里的画面可以完全不同。
> ⇒ 判据也从 11 条改成 **12 条**：**改形状就要改判据，两件事必须同一批**。

> ## v1.22 · **桌面落在底部一条 + 聊天浮窗四边 30（默认收起）**（2026-09-22）
>
> **改了哪几处**：`08-SPEC.md` §6.1（**新增 Z4**）· §6.2（**默认档 = 收起**）·
> §6.7（新增两条实现约束）· §10.1 阈值总表（**浮窗边距 → 四边 30**；收起高度改成跟字算）·
> 这一页 · `docs/dev/52-DESKTOP.md`（新）。
>
> **为什么**：主人 *"首先，页面底部是一个桌面。聊天窗口是一个左右上下 margin 为 30 的浮窗，
> 有阴影。"* —— 问清两件事之后他选：**桌面 = 屏幕最下面那一条**（不是整页底图）、
> **一进来是收起档**。原来那版（桌面 = 整页底图 + 浮窗盖住四边只留 10/10/12）
> **读不出"底部是桌面"**：最大化时你只看得到一圈 10px 的边。
>
> **这一刀修掉了三个真根因**（上一版就是死在第一个上，`docs/dev/50` 留过档）：
> 1. 🔴 **浮窗高度定死**（`屏高 − 边距`）⇒ 3.1 倍字号下 chrome 比容器还高，
>    `Expanded` 被挤成负数 ⇒ **纵向溢出 798px**。**那正是 D3.5 的反面**。
>    ⇒ 现在：**收起档由内容算**、半开/最大化用**系数**、高度由父层 `LayoutBuilder` 量好传进来。
> 2. 🔴 **空屏不可滚** ⇒ 桌面占了高度之后，3.1 倍下它只分到 104px 而自己要 272px
>    ⇒ **溢出 168px**（硬闸抓的）。⇒ 现在**没地方时能滚**（有地方时仍居中）。
> 3. 🔴 **拖拽手势绑在了整块浮窗上** ⇒ **吃掉时间线的滚动**（往上拖列表被当成"把窗口拉高"）。
>    这条不是溢出、是**功能坏了**，而且**当时没有判据** —— 是 `accessibility_test`
>    里"重发那个入口"那条先红的（它要把列表拖到最老一条，拖不动就找不到「重发」）。
>    ⇒ 现在手势**只绑抓手行**；补了一条**回归判据**：拖时间线不许改浮窗高度。
>
> **判据**：`test/widget/desktop_floater_test.dart`（新，10 条，多数带负向对照）——
> 默认收起 / 四边 30（下边距量到**桌面顶**）/ 有阴影 / 点内部无反应 / 点桌面收起 /
> 发送即拉满 / 拖列表不改高度 / 展开按钮命中区 ≥44 / 双击抓手 / 拖抓手变高。
> 硬闸那两条仍在 `accessibility_test.dart`（五档不溢出 + 命中区扫描，**149 条全过**）。
>
> ⚠️ **还没做的明说**（`52` §四）：**小程序容器壳**（桌上只有「会话」一个图标，
> 而它就是聊天本身 ⇒ 壳没有东西可装）、**甩**与 **400ms 防抖**（实现了但**没有判据**）、
> 平板上的样子只在本机看过。
>
> ⚠️ **顺带改了一条既有的测试习惯**：`ChatScreen` 加了 `initialTier` 参数
> （默认 `collapsed`）。要看**展开态**的既有 widget 测试**显式传 `FloaterTier.full`** ——
> 默认值是产品行为，**不许为了哄断言把它改回去**（§6.2 那句话就是为这个加的）。

> ## v1.21 · **对齐：这台机器就是那台服务机；仓库地图换成 `v2/`**（2026-09-22）
>
> **改了哪几处**：`README.md` §二/§三/§四 · `03-DEVELOPMENT.md` §一/§7.4 ·
> `06-OPERATIONS.md` 开头/§2.1–§2.3/§八/§九 · `07-APPENDIX.md` §4.1/§4.2 ·
> `08-SPEC.md` §1.1/§三 附注 · `AGENTS.md` §一/§二 · 这一页。
>
> **为什么要改**：**文档在说假话，而且是会把下一个人带偏的那种**。主人这次说的是
> *"帮我对齐项目"* —— 对完发现四处硬伤，逐条实测（2026-09-22）：
>
> 1. 🔴 **"本机不是生产机"是假话**。本机 `127.0.0.1:8020` 上就跑着
>    `v2/services/core/src/serve.js`，对外就是 `https://w.stalkerai.cn`（frp 隧道 `frpc-w`），
>    两台真租户在跑。⇒ 照旧文档做，人会**在本机到处找服务**，或者**以为线上在别处、改完不部署**。
> 2. 🔴 **仓库地图画的是上一代的树**（`apps/mobile/`、`services/core/`、`packages/protocol/`）。
>    那三处 **2026-09-21 就删了**，仓库里只有 `v2/`。
> 3. 🔴 **`08-SPEC` 说"R1–R14 的正文在 `packages/protocol/PROTOCOL.md`，那份仍然权威、
>    不在被删之列"** —— 而 `packages/` 整个删了。⇒ 改指 `03-DEVELOPMENT.md` §三（现行接口）
>    + git 历史（旧条文）。
> 4. **写死的数值**：`03`/`07` 里的 `wc -l` 表、测试条数表（"72 全过""widget 挂 10"）——
>    既过期，又**违反本手册自己的纪律 1**。⇒ 删掉，改成"跑哪条命令"。
>
> **新增/改动的纪律**：`README.md` §四 加了**第 6 条**（文档分层、指针要过闸：
> `node scripts/check-docs.mjs`，挂在提交前）；§四 第 3 条改成事实
> （`test/widget` 一般是提示档，**但 `accessibility_test.dart` 是硬闸**）。
>
> **新记下的一类事故（值得单列）**：**线上服务不在 systemd 监管下** ——
> 它的 cgroup 是 `dsh-subprocess-<dsh 服务 pid>-<hash>.scope`，也就是**它是 DSH 工具调用里
> 起的长命子进程**。⇒ `systemctl --user stop` 那种 scope 会**连服务一起杀**；
> **本次对齐过程中就出过一次线上 502**（已用 `restart-core.sh` 恢复）。
>
> ⚠️ **还没对齐、明确留着**：`06` §1.2/§8.3 与 `07` 里的"生产机"字样，指的是
> **上一代那个部署（`hupo.stalkerai.cn`）**，不是本机；逐处改名留到下一批
> （**明说留着，不是"以后再说"**）。

> ## v1.20 · **一套外观：首页是基准，另外三屏向它看齐**（2026-09-22）
>
> **改了哪两处**：`05-DECISIONS.md` 加 **D3.10** · 这一页 ·
> `docs/dev/49-STYLE.md`（新）。
>
> **为什么**：主人 *"我们现在统一一下页面风格。看看首页，就知道登录页需要改了，
> 配置页也要改。聊天页也要改。"* —— 首页自己长成了一套（暖纸底 + 陶土红 + 白卡），
> 而**另外三屏是 Material 默认样**（蓝顶栏、蓝按钮、冷灰底）⇒ 四屏摆在一起**不像一个产品**。
>
> **改成了什么**：把那六个颜色 + 三档圆角/间距从首页**提上来**当全站的底子，只有
> `lib/models/design.dart` 一处出处；`ThemeData` 在 `lib/screens/app_theme.dart` 拼。
> 逐屏还改了三处：登录页**顶上放同一个标志**（和首页共用 `BrandMark`）、
> 配置页从"一行行字 + 分隔线"改成**小标 + 白卡**、聊天页顶栏变纸底无阴影
> （助手那一条 = 暖白卡、用户那一条 = 陶土淡色）。
>
> 🔴 **能一次改完的原因**：动手前先量过 —— 全站**只有一处硬编码颜色**，
> 其余**全走 `colorScheme`** ⇒ 把主题拼对，四屏一起变。
>
> ⚠️ **一个字号的数值都没新写**（D3.5 仍然有效：容器跟字算）。
> ⚠️ **闸在这一次改动里抓到两件事**（记进 `49` §4.1）：① 配置页改成可滚列之后，
> **3.1x 那档「关于」被挤到折叠线以下**，而 `ListView` 不建屏幕外的孩子 ⇒
> 判据得**像用户那样先滚再点**，不是把那一行硬塞进屏幕；② 肉眼看纸底上的暖白**会误判成灰**
> —— 我是靠**像素采样**（`#F8F5EE` / `#FFFDF9`）确认主题真生效的。
>
> ⚠️ 顺手修好了"看一眼某一屏"这条工具（`check-web-browser.mjs`）：
> **CDP 的 `Input.*` 送不到 Flutter**（实测页面逐字节不变）⇒ 改走**页内合成 pointer 事件**，
> 于是能一路点进"配置 → 关于"；另加 `--no-token`（看未登录那两屏，退出码 **4** —— 不判那条流）。

> ## v1.19 · **注销之前要再确认一次身份，而且每一件都留一笔账**（2026-09-22）
>
> **改了哪三处**：`05-DECISIONS.md` X3 ④ 的状态 · `06-OPERATIONS.md` §5.5 · 这一页 ·
> `docs/dev/43-AUTO-PROVISION.md` §十四（新）。
>
> **为什么**：账 #39 一直写着"注销这条路上还差两件"——而它是**不可逆**的，
> "只靠令牌"意味着**一个被盗的令牌就能删掉一个人所有的东西**。
>
> **① 身份再确认**：要"**最近真的登录过**"（看令牌里的 `iat`，窗口 10 分钟）。
> 🔴 关键事实：`Auth.renew()`（续期）**保留原来的 `iat`、只把 `exp` 往前挪** ⇒
> **旧令牌没法靠续期刷过去**（要是反的，这条规则就是空的）。
> 不满足 ⇒ **409 + 什么都没发生**（不投申请、不撤令牌、不删账号），
> 界面说「…**现在什么都没动**」并送他回登录那一屏。
>
> **② 给主人看的那一笔账**：**两份文件、两个写入者**（服务写 `<数据目录>/audit.log`，
> 特权侧写 `/var/log/hupo/tenant-audit.log`）—— **谁也写不了对方的文件 ⇒ 谁也伪造不了对方的行**。
> 行的形状两边**逐字一致**（出处只有 `src/audit.js`）：
> `[时间] 事件 · 租户 · 用户 · 手机(掩码) · 说明`，**拒了也记**；
> 给人看的那一支是 `scripts/show-audit.sh`（按时间并起来，读法写在输出末尾）。
>
> ⚠️ **两条如实说**：① 短信通道接上之前，这道门**和"登录"一样强**（挡的是"被盗的旧令牌"）；
> ② 审计里**只有掩码手机号**，而且**钥匙一个字符都不许有**（判据钉着这两条）。
>
> 🔴 **过程中判据当场抓到一处 bug**：服务端调 `reauthOk` 时**没把注入的时钟传进去** ⇒
> 它拿**真实墙上时间**去比 ⇒ 那条"一小时前的令牌必须被拦住"的判据**时灵时不灵**。

> ## v1.18 · **页面上能唤起「配置」，在里面填/换钥匙**（2026-09-22）
>
> **改了哪三处**：`05-DECISIONS.md` 加 **D3.9** · `06-OPERATIONS.md` §5.7（补一句"网页上随时能换"）·
> 这一页 · `docs/dev/48-SETTINGS-KEY.md`（新）。
>
> **为什么**：主人 *"用户可以在页面唤起配置。配置上可以输入 apikey"*。
> 在那之前，钥匙**只有第一次那条流程**能填 —— 填过之后**再也回不去**
> （只有"被判无效"之后刷新页面才碰巧回得去，欠账 #34）。⇒ 想换一把、
> 或者只想看看"我到底填过没有"，用户**没有地方可去**。
>
> **改成了什么**：顶栏加一个**齿轮**（「配置」），那一屏里是**钥匙** + **「关于」**
> （「关于」从顶栏搬进来 —— 顶栏再加一个就是 7 个图标，手机上会挤成一团；**顶栏仍是 5 个**）。
> 表单抽成 `widgets/key_form.dart`，**和第一次那一屏共用同一份**。
>
> 🔴 **顺带补上一个"页面在说假话"**：服务端原来只回 `hasKey` ⇒
> "**还没填过**"和"**填过但被判无效**"在界面上**长得一模一样**，
> 配置那一屏只能对他说"还没有填"。⇒ 加一个 `keyBad` 字段（纯函数 `src/key-state.js`），
> 三种状态**分得开**，而且各有各的话（单测钉着）。
>
> ⚠️ **没做的明说**：被判无效时**不自动弹**配置 —— 现在的形状是"随时能进去看、而且它如实说"，
> 而不是"自动把你推到某一屏"（"什么时候弹"没定就不做）。
> ⚠️ 还有一条**试过但不通**的：想给浏览器检查加"点一下再截图"，而 Flutter 把字画在 canvas 上、
> 按坐标派发鼠标/触摸事件**页面逐字节不变** ⇒ 那个参数**没留**（宁可没有，也不留一个假装能用的）。

> ## v1.17 · **钥匙住进卷里，而且有三个入口**（2026-09-22）
>
> **改了哪五处**：`02-ARCHITECTURE.md` 加 **N30** · `05-DECISIONS.md` M 组加 **T10 / T11** ·
> `06-OPERATIONS.md` 加 **§5.7** · 这一页 · `docs/dev/46-KEY-DELIVERY.md`（新）。
>
> **为什么**：主人 *"容器内也可以设置 key"* ⇒ 追问后拍板 **甲**（落盘到卷里）+
> *"还要能从外面投进去（不动 root）"*。
> 起因很具体：钥匙原来只在容器里的 **tmpfs**，而**容器一重建就没了** ——
> 2026-09-22 那次迁移两台各重建一次，**主人之前贴过的钥匙当场作废**。
> 而"重建容器"恰好是这套东西以后**要经常做**的事 ⇒ 每次都要用户重贴一次，不能接受。
>
> **改成了什么**：钥匙住 `<数据目录>/creds.yaml`（**root 0600**、原子写）⇒
> 重建容器 / 换运行层都**不丢**；三个入口（网页 / 盒内 `put-key` / 本机投递 `.key` 文件）
> 写的是**同一个文件**。
>
> ⚠️ **代价如实记**：钥匙**落盘**了（在**他自己那台**的卷上）。能读到那块盘的人就能拿到它。
> 🔴 **决策 ① 一个字都没动**：agent 是 uid 1000，而文件是 root 0600、`/data` 是 root 0711
> ⇒ **读它得 `EACCES`**（真机验过，用的是**临时**一把、验完就删）。
>
> 🔴 **这一批最有价值的教训**（`46` §七）：**同一个病在**一个小时里连栽三层**——
> ① 规则写在**镜像里**的入口 ⇒ 产品层发布**带不动它**；
> ② 归一写在**默认值**里 ⇒ 被"显式传参"绕过（默认值只在参数是 `undefined` 时生效）；
> ③ 看门拿**第一拍看到的样子**当基线 ⇒ 钥匙在"报到之后、第一拍之前"出现就**永远不提示**。
> ⇒ 结构上断掉：**镜像里只留一个薄加载器**（**N30**），**其余全在产品层**。
>
> ⚠️ 顺带修了两件：投递那条路原来的"送过就算数"会让**推送丢在重连/重建那一下**之后
> 永远等下去（文件不删、钥匙不进容器、**谁都不报错**）⇒ 改成每 30 秒有界重试；
> 以及"我在宿主还是盒里"原来是**猜**的（靠有没有通道）⇒ 冒烟跑因此报了一个
> 盒里不存在的目录（**在说假话**）⇒ 单元现在明确给 `HUPO_ROLE=tenant`。

> ## v1.16 · **容器怎么更新：镜像劈成「运行层 + 产品层」**（2026-09-22）
>
> **改了哪三处**：`02-ARCHITECTURE.md` 加不变量 **N29** · `06-OPERATIONS.md` 加 **§5.6** ·
> `05-DECISIONS.md` M 组加 **T9**。
>
> **为什么**：主人原话 *"现在 docker 里面的系统，是镜像。但是，我核心功能，
> 其实是 docker 里面的系统。所以，你觉得应该如何更新它？"*
> ⇒ 在那之前，"改进产品"= 重造 404 MB 镜像 + root 分发 + 每台重建容器；
> 而且**容器不知道自己跑的是哪一版**（镜像没给 `HUPO_BUILD_ID` ⇒ `buildId` 是 `dev`）
> ⇒ **一台可以在旧代码上静默跑几个月，而两边都以为没事**。
>
> **改成了什么**：按"多久变一次"劈开 —— **运行层**（node + 库 + agent 本体，
> 305 MB / 罕动）留在镜像里；**产品层**（调度器 + 人格 + 能力，596 KB / 每次改）
> 住进宿主上一份目录，**只读**挂进容器 ⇒ **改产品 = 写文件 + 让容器重开**。
> 容器**如实自报版本指纹**，宿主比完不一致**就说话并叫它重开**
> （那一帧 `reload` **不带任何内容**）；容器**先把手上那轮说完**再退。
>
> ⚠️ **主人拍板**：产品层**归 `deploy`**（*"归 deploy，我能自己翻转但每次都记账"*）。
> 这**不新增权力**（镜像本来就是 `deploy` 造的），换掉的是一层人工步骤 ⇒
> **所以每次翻转必须记账**（`ledger.jsonl`：from / to / why / by）。
>
> 🔴 **真机实测抓到三个真 bug**（每一条都记进 `45` §十，而且**每一条都补了带负向对照的闸**）：
> ① 账本被**嵌套 `JSON.stringify`** 写成"装着 JSON 的字符串"（人看着像、程序读不出来）；
> ② 单元里挂的是**解引用后的指纹**而不是那条软链 ⇒ **翻 `current` 对在跑的容器完全没用**
> （宿主看见的永远是旧版 ⇒ 一直叫它重开 ⇒ 来回退，实测两台容器被反复重启到起不来）；
> ③ "当前是哪一版"被**开机缓存**了 ⇒ 翻转之后它变成假话 ⇒ 同样来回退。
> ⇒ 两条规矩写进 §5.6：**单元里挂软链**；**"当前是哪一版"永远现读、不许缓存**。
> 三个都是"看着像对的"那一种 —— 单测全绿，**只有真机走一遍才看得见**。

> ## v1.15 · **"取消注册"：用户自己能把那一台收回去**（2026-09-22）
>
> **改了哪两处**：`05-DECISIONS.md` 的 **X3 ④**（状态改成"两条路都有了、还差身份再确认与审计"）·
> `06-OPERATIONS.md` §5.5（补上"用户自己那一条"）。
>
> **为什么**：主人原话 *"贴 apikey 的时候，也要有个撤回的功能。**隐蔽一点**。
> 就是**取消注册**。这样我就不用浪费资源了。"*
> ⇒ 入口放在"填钥匙"那一屏的**下面**、小字、次要色（**"隐蔽"= 入口不抢眼**，
> **不是"不告诉他就删"**）；点下去**先列清单**再确认 —— 手册 X3 ② 对"删一轮对话"
> 都要求这个，删**整台**更得列。
>
> 🔴 **它没有新增任何特权面**：走的是**同一个申请投放口**（文件名 `<n>.cancel`，
> 与 `<n>.req` 平行），服务能做的仍然只是"放一个空文件、名字里有一个整数"。
> **只能注销自己**（那个整数只来自验签令牌里的 `sub`）；**静态表那两台**服务侧与
> `remove-tenant.sh` 两边都拒；收掉之后**令牌全撤 + 账号那一行也删**。
>
> ⚠️ **做的时候栽了一次**（记进 `43` §13.3）：服务侧写出来的名字与助手认的名字**不一致**
> （`3.req.cancel` vs `3.cancel`）⇒ **用户点了取消注册、什么都没发生**，而两边单测都是绿的。
> ⇒ 补了一条**跨产物一致性**的闸（真跑服务侧那份代码、看名字助手认不认），并**变异验证**过。
>
> 🔴 **还差两件**（`00-PROGRESS` §六 #39）：**身份再确认**（现在一个被盗的令牌就能
> 删掉一个人所有的东西）与**一笔给主人看的审计**。

> ## v1.14 · **"我无法黏贴"**（手机上空输入框长按不弹菜单）（2026-09-21）
>
> **改了哪一处**：`08-SPEC.md` §6.7 新增第 **5** 条约束 ——
> **凡是要求用户"粘一长串"的输入框，必须自带一个「粘贴」按钮**。
>
> **为什么**：主人亲报 *"我无法黏贴，为啥"*。查下去是个**已知的 Flutter 手机端限制**：
> 它把字画在 **canvas** 上，长按弹的是**它自己的**选择菜单 ——
> 而**空输入框里没有可选的文字** ⇒ **菜单不弹 ⇒ 没有"粘贴"**。
> 桌面浏览器按 `Ctrl+V` 没事，**手机没有物理键盘就卡在这儿**；
> 而"钥匙"那一屏要的恰好是"**粘一长串**" ⇒ **没有按钮就是进不去**。
>
> ⇒ 修法：**给一个「粘贴」按钮**（按钮一按就是"用户手势"，能合法读剪贴板）。
> 钥匙屏与聊天输入条**都加了**（同一个病）；读不到剪贴板时**如实说"还能怎么办"**，
> 而且**不许清空他已经填的内容**；聊天那条是**接在光标处**、不是替换掉他打了一半的话。
> 闸：`test/widget/paste_test.dart` **5 条**（含三条负向对照）。
>
> ⚠️ **这一条是"这一类的"，不是"这一屏的"**：以后任何"要粘东西"的新输入框都得带上它。

> ## v1.13 · **"删掉一台租户"有了路**（X3 ④ 的一半）（2026-09-21）
>
> **改了哪两处**：`05-DECISIONS.md` 的 **X3 ④**（注明现状：容器那一半有路了、网页那一半没有）·
> `06-OPERATIONS.md` §五 新增 **5.5**（删/验各一条命令 + 五条不许破）。
>
> **为什么**：手册 X3 ④ 写着"**注销账号立刻清空**"，而实现里**一条路都没有** ——
> 用户注销之后，他的 OS 用户 / 一段 subuid / 一个卷 / 一份镜像**会永远留在机器上**。
> 而这件事是**验"真建一台"时自己撞上的**：留下的那台测试租户一量 **449M**，
> 当时只能说"要清的话得先做那条路（现在没有）"。
>
> 🔴 **五条不许破**里最要紧的是第一条：**静态表那两台（`hupo-a`/`hupo-b`）永远不许删**
> —— 那是**老用户正在用的世界**，所以是**硬拒，没有绕过开关**；
> 其次"**没有'删全部'这种参数**"（一个参数能删掉所有租户，本身就是个事故）。
>
> ⚠️ **网页上"注销账号"那条路还没做**，而且**不该顺手做**：它比"开一台"危险得多，
> 四个问题要先定（**身份再确认** / **删前列清单** / 要不要复用申请队列 / **留痕**）——
> 已记进 `docs/dev/00-PROGRESS.md` §六（#38）。

> ## v1.12 · **"新号自动拿到一台"真的通了**（2026-09-21）
>
> **改了哪两处**：`04-ROADMAP.md` §10.1（"怎么算做完"三条 —— 从"还没做完"改成**三条全过**）·
> `06-OPERATIONS.md` §5.4（验的清单补上"装完之后那两条"，并记下签字安装的事实）。
>
> **为什么**：主人**明确授权**之后装上了（⚠️ 那是**明确绕过** `AGENTS.md` §八
> "部署期命令由主人跑"的例外，不是默认路径），并且**真建出一台**：
> 一个新号登录 ⇒ `hupo-t3`（uid 3003）· 容器在跑 · 状态 `ready`。
>
> ⚠️ 装的过程里**当场暴露两个真问题**，这一版的判据把它们钉住了：
> ① **不带引号的 heredoc 会在注释里执行反引号**（`DirectoryNotEmpty=` 与 `failed`
>    被当命令跑、写出去的文件里少了字）；② 有一条判据判的是**权限数字**（卷 700）
>    而不是**边界** —— 而那台**是好的**（真边界是 `$home` 的 700；卷是 711，
>    是容器自己改的，为了让盒里的 agent —— 另一个 uid —— 走得进去）。
>
> 🔴 还有一条**判据自己的假设过期**：三份判据的"自证"原来写的是
> "真机上那几处**一处都没有**"，那只在**还没装**的时候成立 —— 装上之后集体假红，
> 而真机上**什么都没有被这次测试改动**。⇒ 改成**跑判据之前记一份真机状态指纹**、
> 跑完逐字比（这样装之前装之后都成立）。

> ## v1.11 · **"验不了"其实只是"没给它一个能验的形状"**（2026-09-21）
>
> **改了哪一处**：`06-OPERATIONS.md` §5.4 —— 把"装之前能验什么"写成一张表，
> 并新增一条判据脚本 `scripts/check-provision-install.sh`。
>
> **改的原因**：v1.10 里我把 **A9 那四条**（四份 root 拥有 · 单元指向**拷贝**而不是
> 仓库 · 服务那个身份**读得到但改不动** · **改了仓库那份 `--check` 认得出**）
> 写成了"要等装完才验得了"。**那句话是我的懒**——它们验不了，只是因为
> **安装器少了一个测试入口**。给它加一个 `--root <临时目录>`（**同一段代码换一个
> 目的地**、跳过 systemd），那四条就能在**隔离环境**里验掉，而真机上**一处都不动**
> （脚本自证这一点）。
>
> 🔴 **为什么值得单独记一笔**：A9 恰恰是"整套模型会不会作废"的那一条
> （让单元去跑仓库里那个脚本 = 把边界画在一个服务自己写得到的地方）。
> 让它**卡在一个人工动作后面**，等于把最重要的那条判据推到最后才验 ——
> 而本项目已经吃过"闸是绿的、可它打在错的输入上"很多次了。
>
> ⚠️ **判定**：仍然只有**"真建一台"**与**"装完之后服务身份不变"**这两条
> 必须等主人签字；其余全部**装之前就有答案**。

> ## v1.10 · **多租户的安全模型写进手册**（N26–N28 · A1–A9 · T1–T8）（2026-09-21）
>
> **改了哪几处**：
>
> | 文件 | 改了什么 |
> |---|---|
> | `02-ARCHITECTURE.md` §五 | **新增不变量 N26–N28**（公开入口不许执行特权动作 / 一切从模板推出且 root 拥有 / 上限与"给不了"必须明说） |
> | `08-SPEC.md` §四 | **新增 4.3**：A1–A9 九条不变量，**每条都写"怎么验"**，并写明"没签字之前不许把真建那一半算成绿的" |
> | `06-OPERATIONS.md` §五 | **新增 5.4**（装 / 撤 / 核对各一条命令）；⚠️ **§5.3 更正**（见下） |
> | `05-DECISIONS.md` §一 | **新增 M 组 T1–T8**（多租户与"新号自动拿到一台"） |
> | `README.md` | 去掉三处"76 条"的**重复数字**（数字只留在 `05-DECISIONS.md`） |
>
> **为什么**：主人 2026-09-21 选了"**真的自动开一台**"，并点名 **"要定安全模型"**。
> 难点的准确形状只有一件 —— *一个公开的网页登录口，要怎样才能触发一个特权动作，
> 而不等于把 root 交出去*。三条路（sudoers / 常驻守护 / **申请队列**）选了第三条，
> 因为它的边界**小到能用眼睛看完全部输入**：一个整数。
>
> 🔴 **写安装脚本时才发现的一条**（它会让整套模型作废，所以写成了不变量 **A9 / N27**）：
> 第一版让单元直接跑**仓库里**那个脚本，而**仓库属主就是服务自己** ⇒
> 一个被攻陷的服务改一行，下次申请触发时**那段代码就是以 root 跑的**。
> ⚠️ 而它**看起来是对的**（路径对、脚本真的跑了、日志漂亮）。
> ⇒ 运行的那一份必须是 **root 拥有的拷贝**，并且要有"仓库那份 vs 装着那份"的**逐字节核对**。
>
> ⚠️ **§5.3 的更正（这是一条"手册在说假话"，必须单独记）**：
> 它原来写"**本机做不了容器实验**"（依据是"不在 docker 组 + apparmor 挡无根容器"）。
> **那句话现在是错的** —— 本机用**无根 podman**，租户各有自己的 OS 用户 / rootless 存储 /
> systemd 用户单元，**两台真容器在跑**。
> 依它行事会得出"容器只能去生产机验"的结论，而**那不是事实**。
>
> **判定为"有效"的部分**：`08-SPEC.md` §4.3 与 N26–N28。
> **尚未启用的部分**：那套单元**还没装**（装它属于部署期，要主人签字）；
> ⇒ 手册里对应地**明写了"没签字之前，真建那一半不算绿的"**，没有把它混成"已完成"。

> ## v1.9 · **批 5（语音）砍了** + **验证码是掩码**（2026-09-21）
>
> **改了哪两处**：`04-ROADMAP.md` 批 5（标"砍了"）。⚠️ **不是** `05-DECISIONS.md` 的 D5 —— 那个 D5 是"一步 + 回显"，**与语音无关**；批 5 那一行原来写的"D5"是个**引用错误**（2026-09-21 核对时发现）。
>
> **为什么**：主人 2026-09-21 原话 —— *"语音不做"* · *"验证码为掩码，不能写在上面。
> 所以人们并不知道如何登录。因为找不到获取验证码的按钮。"*
>
> 🔴 **第二句反转了一条既有判据**：原来（`test/widget/login_test.dart`）钉的是
> "**临时验证码必须如实说**（写在屏上）"。主人推翻了它 ——
> 写在屏上 = **谁看见谁就能进**，"验证码"三个字就变成了摆设。
> ⇒ 新的判据是：**屏上不许出现那串码**，而且**要有【获取验证码】那个按钮**
> （没有按钮 = 人们不知道该怎么登录）。
> ⚠️ 那条判据的**反转**记在 `docs/dev/40-LOGIN-CODE.md`。

## v1.8 · **V4 重划**：从"容器里没有秘密"改成"**盒内除持有者之外谁都够不着**"（2026-09-21）

> **改了哪一处**：`08-SPEC.md` §13.1 的 **V4**（判据本身 + 一整套说明块）。
>
> **为什么**：主人 2026-09-21 拍板两件事 ——
> ① **agent 读不到自己的 key**（"原则上就是 agent 读不到"）；
> ② **按此重划 V4**。
>
> **旧 V4 的病**：它把"容器里不许有秘密"当成**整台容器**的属性，于是和"用户自己的凭据要进他自己的容器"
> **直接冲突** —— 而后者是**必须**的（不进容器，它就调不了模型）。
> 冲突时唯一"能过"的办法是**把凭据挪到 `/data`、`/home` 之外**（tmpfs）——
> 那是**骗判据**：`grep` 扫不到**只说明它不在扫描范围里**，不说明**agent 拿不到**。
> 本项目已经把这种"看着像过了"记成事故过，所以**不许**留着。
>
> **新 V4 钉在哪**：`a` **以 agent 的身份去读** ⇒ 必须失败，**且必须是 `EACCES` 而不是 `ENOENT`**；
> `b` agent 进程的 `env` 里 **= 0**；`c` `grep -rl "sk-" /data /home` **= 0**（管"别处漏了副本"）。
> 并**明写**：`c` 绿了**不算** V4 过 —— 凭据按契约住 tmpfs，`c` 本来就该是 0。
>
> **两条负向对照（不能省）**：root 读同一份**必须成功**；同目录一份 `0644` 对照项、agent 读**必须成功**。
> 没有这两条，"读不到"可能只是**路径拼错 / 挂载没进去 / 文件不存在**。
>
> ⚠️ **依据不在手册里，在 `docs/dev/39-PERMISSIONS.md`**：§5.4 能力表的实测推导 ·
> §5.4.1 成对负向对照（含 setuid 副本：带 `no-new-privileges` ⇒ `euid=1000`，去掉 ⇒ `euid=0`）·
> §5.6 判据 1/2/3 实测 · §六 —— **DSH 自己怎么做的、以及它为什么挡不住 agent**（别拿"DSH 也这样"来省掉边界）。

## v1.7 · "改自己"分两期：**开发期可用 `sudo`，部署期 P1/P2 原样**（2026-09-21）

> **改了哪三处**：`05-DECISIONS.md` A 组（加「补 · 开发期与部署期是两套」+ P1.2 限定"部署期"）·
> `06-OPERATIONS.md` §3.2（第 2 条同样限定 + C5 收口）· `../AGENTS.md` §八（表头 + §8.1 四行 + 末尾那段）。
>
> **为什么**：主人 2026-09-21 的指令。原话与**逐字补丁文案**在 `docs/dev/23-DEV-VS-DEPLOY.md` §四。
>
> **判据不是"哪台机器"，是"这一段工作在服务谁"**：本机**同时是开发机又是线上机**（8020 + 隧道），
> 所以"换台机器就安全"这种读法是错的。⇒ 本机的护栏**降级为审计**（每次重建留痕）；
> **部署那一边的锁不动**。
>
> ⚠️ **和 C5 的关系要说清，否则会被读反**：C5 记的是"手册写着 `sudo` 免密，而本机实测要密码"
> —— 那是**事实错误**，已经改掉。这一次**不是把那句话装回来**，是**主人在知情下明确授权**，
> 而且**只到开发期**。两句话都留在 `06-OPERATIONS.md` §3.2 里，免得下一个人把授权读成"当年那句是对的"。
>
> **落地**：`scripts/apply-change.sh <补丁>`（一个补丁 → 一个 commit）+
> `sudo node scripts/verify-integrity.mjs --build`（重建开机清单，否则下次重启拒绝启动）。

---

## v1.6 · 人格硬规则加第 10 条：**被拦住时不许把话题带走**（2026-09-21）

> **改了哪一处**：`08-SPEC.md` §12.4（人格硬规则，9 条 → **10 条**）。
>
> **为什么**：主人问了一句「拦住的是否提示换个话题？」——查下来，
> 「换个话题吧」这一族话**第 2 条只覆盖了一半**：它管"把话头留给用户"（留客），
> 管不到"**把话题从这件事上引开**"。
> ⇒ 而被拦住时把话题带走，对 **H1 那位用户**是最坏的形状之一：
> 他分不清是**这件事不行 / 它没听懂 / 要他自己按一下** ⇒ **再问一遍**，或者不敢再问。
>
> ⇒ 加第 10 条，并把"被拦住时只说哪三种话"写进同一条的注解
> （①要你签字 ②能力上做不到 ③内容上不该做）。依据 **N11** + **D2.1**。
>
> ⚠️ 同批还修了人格里一处**自相矛盾**：「主人让你改这个系统的时候」第 2 条要求
> "把**改动和命令**准备好"，第 5 条却说"回话**不要贴命令**"——
> 读字面就成了"我得跑一条命令，但它不肯告诉我是什么"。⇒ 第 5 条补上
> "**但要一句话说清他该跑哪一步**"。
>
> **落地**：`v2/services/core/hupo-persona.yml` · `test/persona.test.js`（九条 → 十条）。

---

## v1.5 · §13.2 那三条回潮断言**改成真能跑的**（2026-09-21）

> **改了哪一处**：`08-SPEC.md` §13.2（防回潮断言）。
>
> **为什么**：原文写的是 `grep -rn usermod .` **命中 = 0**、`grep -rn "HEAD~1" .` **命中 = 0**。
> 实测：那三个词**就写在定义它们的文档里**（这一节自己就有 `usermod` 和 `HEAD~1`），
> 而"回退不许用 `HEAD~1`"这句话本身也得在 `06-OPERATIONS.md` 出现；
> 更要命的是**断言自己必须写出那个字符串**才能去搜它。
> ⇒ **照字面执行，这三条永远绿不了。**
>
> 另外第 2 条指向 `services/core/hupo-persona.yml` —— **那个文件已经删了**（v1.3），
> 所以它是**空跑通过**（grep 一个不存在的路径 = 0 命中）。**假的绿比红更坏。**
>
> ⇒ 改成：**扫哪儿 / 不扫哪儿 / 为什么**都写死，并加第 4 条"**不许空跑**"
> （文件数要够，且几个关键文件必须在扫描列表里）。落地在
> `v2/services/core/test/reverse-drift.test.js`（第 19 批），
> 里面有一条测试专门把"必须排除 `docs/` 与 `test/`"这个理由**跑出来**。
>
> ⚠️ 这是**判据的修正**，不改任何结论：三条禁令本身一条都没变。

---

## v1.4 · 加一条验收判据 V13（**不许绕过被测代码**）（2026-09-21）

> **改了哪一处**：`08-SPEC.md` §13.3 加 **V13**。
>
> **为什么**：客户端把实时那条流的地址算成了 `ws://`，在 https 页面上被浏览器
> 按混合内容拦掉 ⇒ **那条路从来没通过一次**；而当时**所有闸都是绿的**，
> 因为验收用的是 node 探针，**探针把 `wss://` 写死了**，绕过了被测的那行代码。
>
> ⇒ 补一条判据：**凡是客户端自己算出来的东西，闸必须打在客户端这一侧；
> 旁路工具的绿灯不算客户端的验收。** 全过程见 `docs/dev/16-STREAM.md`。
>
> ⚠️ 这一条是**判据**，不是新结论——它没有改动任何既有决定，只是把
> "怎么证明做到了"补严。**V13 现在是硬要求。**

---

## v1.3 · 上一代实现删掉了（2026-09-21）

> **删了什么**：`services/`（旧调度器）、`apps/mobile/`（旧客户端）、`packages/protocol/`，
> 以及 `scripts/` 里旧项目的四个脚本。**仓库里现在只有 `v2/`。**
>
> ⚠️ **连带的后果，必须记着**：`07-APPENDIX.md` §一 那份缺陷清单里写的
> **全是当年的文件行号**（`dispatcher.js:617`、`conversation.js:72`…）——**那些文件不存在了**。
> ⇒ **那些行号从此是"历史坐标"**：要翻原文用 `git show f93f296:<路径>`。
> **结论本身仍然有效**，而且已经收敛进 `docs/dev/` 的各篇。
>
> ⚠️ 顺带清掉一笔账：那份清单里"旧人格文件里写着「直接动手」"这条**违反 §13.2 第 2 条**
> 的回潮断言 —— **树删了，断言天然成立**。
>
> **它修掉了什么真问题**：那份旧实现是"照它的样子改一改"的唯一诱惑来源，
> 而它**带着已经判定为缺陷的做法**（`AGENTS.md` 反复点名的那个错）。

---

## v1.2 · 续做那几处【未决】关掉了（2026-09-21）

> **改了哪几处**：`02-ARCHITECTURE.md` §7.2 的"续做/并发上限【未决】"、
> §7.3 的"挪走收口语义【未决】"、`08-SPEC.md` §15.3 里没展开的"失败分类五类"。
> **结论进 `05-DECISIONS.md` §二·补（J 组，D10.1–D10.5）。**
>
> **为什么不改生效处、只加一节**：手册纪律 1（不写数值）与"改了哪处要留痕"。
> 生效处那几行 **【未决】仍然留着**——它们是"当时确实是悬空的"的证据；
> 裁决在 D10，**冲突时以 D10 为准**。
>
> ⚠️ **一处必须先认的代价**：D10.1/D10.2 取的是**保守白名单**（`bash` 不在里面），
> 所以**绝大多数活不会被自动续做，只会通知主人**。
> 这是为了不让"续做"变成"重复下单 / 重复发信"——**宁可少做，不可做错**。

---

## v1.1 · 语音的前提变了：**"不会拼音用不了"改成「按设备如实说」**（2026-09-20）

| 项 | 内容 |
|---|---|
| **触发** | 主人说：**"我会在 pad 上开网站，输入法自带语音。"** |
| **改了什么** | **`05-DECISIONS.md` D3.3**：从"必须明写『不会拼音的人用不了』"改成 **"按当前设备如实说"** |
| **连带改了** | `04-ROADMAP.md` 批 2 的验收、批 5 的必要性、`H1` 的措辞、`docs/dev/03-DEPLOY-WEB.md` §五 |
| **为什么这算改了结论** | 原那条**是在"完全没有语音输入"的前提下写的**。前提变了，同一句话就从"诚实"变成"**另一种谎言**" |

### 具体怎么变

| 设备情况 | 事实 | 关于页 |
|---|---|---|
| **输入法自带语音** | **能用**（走输入法，不是我们的话筒） | ❌ **不许写"用不了"**——那是假话，**而且会劝退一个其实能用的人** |
| 输入法没有语音 | 真的用不了 | ✅ 必须明写 |

⇒ **新规则**：**按「当前这台设备」如实说**；检测不到就**说得保守**，**不许一刀切**。

### 同时降级了 D5（但不等于取消）

输入法自带的语音**提前兑现了 D5 的一部分价值**。但要记清一件事：

> **D5 的立论从来不是"步数要少"，而是「不许要学新的」。**
> 输入法麦克风成立，是因为**用户已经会用它**——不是因为它只有一步。

⇒ **D5 降级为"在真机上复核之后再说"**，而不是"不用做了"：
**只验证过主人的平板**；那一类千元机上是不是也这样，**没有验证过**。

### ⚠️ 由此新增一条**必须尽早验**的风险

**这条路整条都压在"Flutter web 里的中文输入法能不能正常工作"上。**
Flutter web 的文本输入在中文 IME 上历史性地出现过合成/光标问题。
⇒ **在真实平板上用真输入法打一遍**，是第一件要验的事——
它要是不行，**"打不了字的人能用"这个结论整个不成立**。

### 教训（写进纪律）

**凡是"因为某能力缺失"而推出的文案，能力一到位就要回来改它。**
一句本来正确的诚实说明，**会在前提变了之后变成另一种谎言**。

---

## v1.0.1 · 实施落点与顺序（**不改任何结论，只记两条实施决定**）

| 项 | 决定 |
|---|---|
| **日期** | 2026-09-20 |
| **落点** | **新代码进 `v2/` 平行目录**；旧代码**原地保留**，继续可读，直到新的被验证过再删 |
| **顺序** | **先建地基**：时间线内核（落盘 + 编号契约 + 协议核心），跑通"一条消息进来 → 落盘 → 推出去"，再往上长 |

**为什么要记这一笔**：`04-ROADMAP.md` 的批 1 是按"**修既有代码**"排的，
而 `03-DEVELOPMENT.md` 的文件索引写的是**原地路径**。
"全新建立 + `v2/`" 与这两处**字面不符** ⇒ **不默默偏离，记在这里**。

⚠️ 由此产生的两条**读法变更**（本版生效）：

1. **批 1 那四件不再是"去旧代码里改"**，而是"**在新代码里从一开始就对**"。
   它们的**验收判据不变**（V-a / V-e / V-f / V-g），只是实现位置改到 `v2/`。
2. **`03-DEVELOPMENT.md` §七 的文件索引**按"原地替换"写的；
   在 `v2/` 方案下，它读作"**目标模块清单**"，路径前缀换成 `v2/`。
   ⇒ 等新实现在 `v2/` 长齐、旧代码删掉之后，会**再升一版**把索引改回最终路径。

---

## v1.0 · 基线冻结 —— ✅ **定档**（2026-09-20）

**这一版是开发的起点。** 从这一版起：
**代码按它写，不按旧代码写；旧的一切只当参考。**

| 项 | 值 |
|---|---|
| **定档日期** | **2026-09-20** |
| **冻结 commit** | `dff9557` |
| **git tag** | `baseline-v1.0` |
| **规模** | 9 份，**4029 行** |
| **状态** | **规划完成、实现未开始** |

### 这一版包含什么

| 文件 | 内容 |
|---|---|
| `README.md` | 索引 · 三条读法 · 五条维护纪律 · "已在手册里"与"只在历史里"的对照 |
| `01-PROJECT.md` | 产品定义 · 十个用户 · 十个放弃点 · 五根因 · 五原则 · 产品声音 |
| `02-ARCHITECTURE.md` | 三层+旁路 · 多租户 · 作用域 · 调度器职责 · **不变量 N12–N25** |
| `03-DEVELOPMENT.md` | 仓库地图 · 模块依赖 · 协议 v2 · 状态机 · 接口契约 · 测试地图 · 文件索引 |
| `04-ROADMAP.md` | 批 0–6（含 1.5）· 验收判据 · 规模 · "做一半比不做更坏" |
| `05-DECISIONS.md` | **76 条决策** · 最强理由 · 被否决的选项 |
| `06-OPERATIONS.md` | 部署 · 权限模型 · 供给上限 · 容器 · 备份 · 故障处理 |
| `07-APPENDIX.md` | 现状缺陷清单 · 术语与禁用词 · 评审史 · 实测事实 |
| `08-SPEC.md` | 15 节规范与判据（详见下） |

### `08-SPEC.md` 的 15 节

不变量 N1–N11（含 v6 代仍有效的规范与**三起"页面在说假话"事故**）·
接口契约 · 数据与存储 · 安全模型 · 成本与可观测性 · 界面与浮窗铁律 ·
已取代/不建 · iOS 与 Web 构建 · 部署形态 · **阈值与数字总表** ·
**运行机制（崩溃守护/回退锚点/备份/审计/保留期）** · **L3 契约（工具矩阵/记忆闸门/人格）** ·
**容器与上线验收** · 小程序沙箱与制品 · L2 实现约束

### 这一版**不含**什么（**明确记下来，免得以后以为漏了**）

| # | 不在这一版 |
|---|---|
| 1 | **任何实现**。批 1 一件都没做 |
| 2 | **工作区/作用域的运行时**（`scopeId` 今天在服务端与客户端零出现） |
| 3 | **记忆 KV**（本轮不建，只保留两条规则的定义） |
| 4 | **小程序运行时与制品管线**（本轮不建，但 N1/N2 是前提） |
| 5 | **六个模块**：成本三级 / trace / 来源策略 / 审计 / 制品库 / 记忆 KV |
| 6 | **容器**（第二个用户的开门条件，不是第一步） |
| 7 | **语音本体**（要加 Flutter 插件） |
| 8 | **iOS 分支**（只发内置应用、不发作用域运行时） |

### 这一版里的**已知未决**（**不许当成"已知"来引用**）

| # | 未决 |
|---|---|
| 1 | 进程上限默认值 **3 vs 4**（且一处说"不是准入判据"、一处拿它当拒否条件） |
| 2 | **备份**三处冲突：对象存储 vs 本地第二目录 / 每小时 vs 每日 / 季度演练 vs 每月 |
| 3 | "挪走"（先收口再另起）与"转交"（不收口、同一条继续写）的**收口语义相反**，却共用一个状态标记 |
| 4 | 记忆存放位置的三套说法 |
| 5 | 一个 agent 的内存口径：文档 172MB vs 实测 195MB（**取实测**） |

### 定档之后怎么改这份手册

| 改什么 | 怎么办 |
|---|---|
| **代码与手册不一致** | **先按代码确认事实，然后回来改手册**（不要反过来）——并在本页记一笔 |
| **结论变了** | **升版本**（v1.1 / v1.2…）+ 写清改了什么、为什么 |
| **只是补细节** | 升**次版本**（v1.0.1）+ 一行说明 |
| **想删一条结论** | ⚠️ **先确认它已经在别处有家**——上一轮删旧文档时，靠的就是"先把 N1–N11 搬进 `08-SPEC.md`，再删"。**顺序反了就会丢东西** |
| **想加数值** | 先想清楚它会不会过期。**会过期的数值只进 `08-SPEC.md` §十，或留在代码里** |

### 旧文档在哪

**删除前的完整快照是 commit `f93f296`**（那之后的第一个 commit 就是删除）。
取法见 `README.md` §六 的"主题 → 路径"对照表。
