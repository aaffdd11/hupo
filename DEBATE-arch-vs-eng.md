# 两两对辩 · 资深架构师 对 工程实践师（DISCUSS-eng.md）

> 我的题目：**边界、契约、不变量、可回溯性**。他的题目：**投递、时序、中止、竞态**。
> 两份稿子没有立场冲突的地方我一句不复述；凡是能被他驳倒的我当场认，驳不倒的顶住。
> 本稿里所有"已核实"的事实都是我**自己读源码读出来的**，不引用他的转述。

---

## 〇、先摆我在本轮新核实的事实（后面每条判决都踩在这上面）

| # | 事实 | 出处（我实读） |
|---|---|---|
| **F1** | `interruptedBlocks()` 不是"生成全文"，是**过滤重建**：丢弃全部 tool-call 块、丢弃 `trim()` 后为空的块、**保留 reasoning 块**、按 stream order 合并 | `dsh-llm/lib/types/assembler.js:146-168` |
| **F2** | 中止点在 chunk 边界成立：`for await` 顶部 `throwIfAborted()`，`chunkSeqs` 只含中止前已 append 的 chunk；中止消息带 `surfaceOp:'append'` + `sourceEventSeqs: chunkSeqs`。**精确的是 chunk 序列，不是文本** | `dsh-agent-loop/lib/index.js:616-648` |
| **F3** | replacement 事件**进日志**（`foldSurface` 带 `replaceGeneration` 重放它）→ replace 是 durable 的**视图修复**，不是 hack；但 append-origin 才是"人类转写的耐久源"，replacement 只对模型可见；`sourceEventSeqs` 被宿主强校验（必须 densely 覆盖被遮蔽节点、不得重复、必须引用更早事件） | `dsh-session/lib/types/surface.js:36-44, 130-180` |
| **F4** | **"用户实际看到的文本"今天没有生产来源**：`HostAdapter.spokenText()` 直接 `return ''`，全仓库只有 `FakeHost`（模拟器）有实现 | `src/host.js:22-24, 64-66` |
| **F5** | `deferWhileThinking` 已在 v1 落地，且已有一个真实语义缺口：谓词 `spokenChars === 0` 在真实宿主上（F4）**恒真** | `src/handoff.js:37` |

**三条事实的联合推论**：他 §5 V-B 关心的"拿不到固化的已输出文本怎么办"、他 §1 row1 说的"三份文本不一致"，**都不是待验假设，是今天的代码现状**。他要去"验证"一件已经在纸面上成立的坏事。

---

# 甲、逐条回应

### 他的主张 1：宿主的中止消息（`interruptedBlocks()` + `sourceEventSeqs`）意味着"生成到哪"是可精确重建的，因此它足以充当"生成全文"这一份文本

**判定**：部分成立（载体对，结论越界）
**我的回应**：F1 摆在这里：`interruptedBlocks()` 是一次**有损过滤**——tool-call 块全丢、空白块丢、reasoning 块保留并与 text 块按 stream order 合并。所以它**不是"生成全文"**，它是"宿主认为可以安全固化的子集"。三个后果：
1. 他 §1 row1 的断言①「`delivered` 是 `generated` 的前缀」在**工具调用轮次必然失败**（文本块被过滤掉了），在**含 reasoning 的轮次**会拿到一段混着推理的"全文"——前缀关系要么不成立，要么语义错乱。
2. `sourceEventSeqs` 精确的是 **chunk 序号集合**，不是文本。所以"生成到哪"要重建，得自己把 chunk 按宿主的过滤规则重新装配一遍——**那等于在控制器里再写一份宿主装配器，两份实现必然漂移**。
3. 更本质的：他之所以需要一个"generated"载体，是因为他想做"三份文本一致性断言"。**而这三份里只有一份是需要被定义的（权威），另外两份应该是从它导出的**——见主张 6。
**谁来保证它不被破坏**：给宿主消息降级为**只读证据**（`chunkSeqs` 用于对账"生成侧"），禁止它出现在任何前缀断言的左值上；唯一权威是控制器 commit 流。
**如果他是对的，我的架构要改什么**：无。要改的是他 watermark 模块的三条断言载体（`generated` 必须定义为"控制器按 chunk 顺序重装配的、未经投递裁切的字节序列"，而不是宿主消息的 content）。

---

### 他的主张 2：`surfaceOp:'replace'` 不能擦用户的字，但可以合法地用来把"没投递的尾巴"从模型可见面遮蔽掉；这在"让模型别引用用户没看见的话"上是**唯一正解**

**判定**：部分成立（读法成立，定位越界）
**我的回应**：F3 证明他的读法是对的，而且比我原来的理解更细：replacement **进日志、会被 `foldSurface` 重放**、并且宿主对 `sourceEventSeqs` 做强制校验（必须覆盖每一个被遮蔽的节点）。所以它是一次**宿主背书的、可校验的视图修复**——这一点我原来漏了，认。
但"唯一正解"不成立，而且把它当"对齐机制"是**层次错误**：
- replace 只改变**走同一条投影路径的消费者**看到的东西。而 append-origin 消费者——模块自己声明的"人类转写的耐久源"、DSH 自带 UI（REVIEW-eng 炸点 2 已核实它按 `isAppendSurfaceEvent` 渲染）、审计、记忆抽取、以及将来 T4 的 `originTurnId` 归属——**读到的仍然是那条含未投递尾巴的 `assistant/message`**。
- 所以 replace 不是"消除分叉"，它是**把分叉从「模型 vs 用户」搬到「模型 vs 全部 append-origin 消费者」**。对 R5 而言这是改善（模型确实不再看到用户没看见的话），对"审计与用户视角"而言这是恶化（耐久源里那句尾巴永远在）。
- 由此得到我要钉的那条：**"用户看到了什么"必须是一个落盘字段，不能是一个投影态。** 投影只有走同一投影路径的人才尊重；字段是每个消费者都绕不过去的。
**谁来保证它不被破坏**：`emittedText` 逐轮落盘 + 装配入口只接受 committed 前缀 + 一条断言"外发流前缀逐字节 === 客户端渲染文本"。replace 只允许作为**视图修复**登记在 R2 例外清单里，**禁止作为 `emittedText` 的来源**（这是可与 F3 共存的分工：replace 管模型视角，落盘管事实）。
**如果他是对的，我的架构要改什么**：R2 的例外从"整块重建"一项扩为三项，且**共用同一套形态**：`rebuild(reason)` / `replaceSurface(range)` / `crossTurnAppend(taskRef)`，全部需要控制器签发的窗口令牌。理由：**今天为了让 replace 合法而开的口子，就是 T4 明天进来的门。**

---

### 他的主张 3：v6 §三 的时间轴只覆盖简单档；难题档两块间隔 5–7s；处理层必须吐出可外发的进度信号，这是 schema 字段，"事后补要动协议"

**判定**：**成立**，而且这是他全部文字里最强的一条
**我的回应**：他论证出了"这是协议/schema 字段"，却没有把它放进任何"必须现在做"的清单（§5 的三条验证里没有它）。而判据本该是：**字段属"历史形状"——后加 = 历史轮次永远没有这份素材，于是"接收层当时到底能说什么"这件事不可回放**，无缝衔接的质量也就不可回溯。
**他这条同时证伪了 v6 自己的时序表**：`ARCHITECTURE-v6.md:73-80` 画的 `t≈1.5s 深思回答` 在难题档不存在，而"两块拼成一条无缝的话"这个核心承诺恰恰在最需要的档位失效。
**谁来保证它不被破坏**：`ProgressSignal` 进协议类型 + 每轮落盘；接收层的素材做**能力门控**（它只拿得到「用户原话 + 粗查记忆 + 分类标签 + 进度信号」，签名里根本没有处理层产物）。
**如果他是对的，我的架构要改什么**：**L0 从八项改为九项**（新增 `ProgressSignal` schema + 落盘），且 v6 §三 时间轴必须**分档重写**（简单档 / 难题档 / 级联档）。这两条我照改。

---

### 他的主张 4：R2 与"一小时会议的滚动摘要"不可兼得；解法不是妥协 R2，而是**显式豁免**（被动语料不进主上下文，滚动摘要跑独立短上下文、跑完即弃、不依赖跨调用缓存），且这条豁免必须写进架构

**判定**：**成立**，而且它比他自己以为的更严重——它打到的不是 R2，是**我的"单一装配入口"**
**我的回应**：他说"不改 R2，而是豁免"，严格说这**根本不是 R2 的例外**（独立上下文根本不碰主链的 append）。真正被打到的是我 §4.2 的 L0#2：如果"独立短上下文"的实现方式是"另写一个函数拼 prompt"，它就直接命中我自己列的红旗 #1（出现第二处构造 prompt 的地方）。**我的契约形态有漏洞，不是原则错。**
**谁来保证它不被破坏**：装配入口唯一 = **唯一装配函数 + 显式「上下文域」（context domain）维度**。每域有自己的 append-only 链、自己的前缀指纹、自己的 `promptVersion`；跨域读在类型上不可表达。于是"会议摘要域""T4 任务域"是**同一入口的合法参数**，而不是第二个入口。
**如果他是对的，我的架构要改什么**：L0#2 改写（我照改）；装配入口签名里 `domain` 与 `promptVersion` 必填（这也顺手解决他主张 5 的灰度问题）。

---

### 他的主张 5：共享 profile 一变（`prompt_version+1`）→ 所有用户缓存同时作废，全员首字一起变慢；上线纪律是分批灰度 + 避开高峰

**判定**：成立（运维纪律），但这不是"矛盾"，是**这个决定的必然代价**
**我的回应**：前缀首段放用户标识是 R1 提示词层要的，代价（跨用户永远 0 命中）是刻意的、可算的。他补的那条运维后果是对的：**persona 变更是一次"全员性能事件"**，必须排队。架构上能做的不是消除它，而是让它**可归因、可分批**——分批灰度意味着"同一时刻两个 `promptVersion` 并存"，而当前装配入口没有版本维度。
**谁来保证它不被破坏**：`promptVersion` 作为装配入口必填参数 + 每轮 trace 落它 + 灰度期断言"同一用户同一会话内版本不变"。
**如果他是对的，我的架构要改什么**：与主张 4 合并——装配入口必须参数化 `(domain, promptVersion)`。这条我照改。

---

### 他的主张 6："两块回答"的难点不是提示词问题，是**三份文本一致性**问题（生成全文 / 用户实际看到的 / 交接包里的已输出全文），并给出三条字节级断言；20 行代码就能把最贵的不一致顶出来

**判定**：部分成立（问题识别正确，**份数与载体都错**）
**我的回应**：
1. **不是三份，是四份**：原始 chunk 流 / 宿主过滤重建的 `interruptedBlocks`（F1）/ 控制器已放行 / 客户端已渲染。而且这四份**不是干净嵌套**。
2. **"交接包里那份"是不是独立一份，取决于他选谁当权威**：选"控制器已放行"，它就是同一份的拷贝，断言②退化成恒真；选"宿主消息"，它才独立，于是断言①在工具轮次失败（主张 1）。
3. **最硬的一条**：F4——今天仓库里**没有任何生产来源**能给出"用户实际看到的文本"（`HostAdapter.spokenText()` 直接返回空串，只有 `FakeHost` 有实现），F5 说明 `deferWhileThinking` 的谓词在真实宿主上恒真。**所以"顶出最贵的不一致"这件事，用 20 行代码顶不出来：它顶的是一个空值。**
**谁来保证它不被破坏**：不要用"三条断言"去守护三份文本——要**让第二份和第三份在类型上不存在**：`emittedText` 是唯一权威并逐轮落盘；交接包里的"已输出全文"是**同一对象的引用/导出**，不是可独立构造的字符串。
**如果他是对的，我的架构要改什么**：无（这正是我 L0#1 的加强版）。要改的是他的断言清单：`released ⊆ generated`（**按 chunk seq 序列比，不按文本比**）、`handoff.text := released`（同一对象）、`turn 关闭后 released 不再增长`（除显式 ack 补投）。"生成全文"这个载体废除。

---

### 他的主张 7：ETA 在物理上不可测，"ETA × 时效性"第一版无法实现，只能退化为按已耗时查历史分布；判据 p90 误差 > 1.5s → 接管改成纯定时器（"到 3s 没结论就直接接"）

**判定**：部分成立（降级正确，**兜底错误**）
**我的回应**：ETA 不可测，认——LLM 没有剩余时间信号，唯一可得的是已耗时。"按已耗时查历史分布"比"ETA 乘法"可实现，这条我采纳为接管判决的 L1 输入。
但"p90 误差 > 1.5s → 纯定时器硬接"这条兜底是**在批量制造 S4**：3 秒到点硬接，等于把"接管点落在半句中间"变成一个**由时钟驱动的必然事件**。而他自己在 §1 row1 认定 S4 是最贵的失败模式。用时钟去制造你最贵的失败，这不是降级，是把一个设计缺陷制度化。
正确兜底是**定时器 + 句边界窗口**（主张 14/18，议题 2）：到点后最多再等一个句边界（≤400ms）。成本有上界，且只在"深答已就绪"这个**已发生事实**上生效——不需要预测（v1 的 `deferWhileThinking` 就是这个机制，F5）。
**谁来保证它不被破坏**：接管动作只有一个出口（控制器）；`isBoundary` 单点实现；`DeferWindow ≤ 400ms` 是配置常量，改动留 trace。
**如果他是对的，我的架构要改什么**：无；我采纳他的"概率查表"，并把它抬进 P2 的 L1 层。

---

### 他的主张 8：被点名检测不能作为唯一依据；链路固有延迟 ≥1.7s，判决到达即过期；要求段级 FP ≤0.5% / recall ≥85%；双条件 + LLM 二次意见 + 800ms 静默；L1/L2/L3 降级阶梯，**L2（状态提示 + 主人一键"回答"）应该是第一版形态**

**判定**：**成立**（§4.3 整节我接受，含代价不对称的算法）
**我的回应**：只有一处要补，而且它正好用他自己的方案证明：**阈值与降级阶梯必须由控制器的 `decide()` 输出，识别器不得自带门限。** 他的 L2 就是"把出声权交回控制器"的最干净形式——主人点一下 = 人类替代了不可靠的检测器。如果识别器内部还有一套"低置信就不响应"的规则，你就有了两个 owner，而没有任何测试能钉住行为（我 §1.4）。他的双条件规则是对的，但它属于**判决表**，不属于识别器。
**谁来保证它不被破坏**：`decide(signals, mode) → Decision` 纯函数 + 穷举表（表与代码同源）+ **出口断言** `assert(mode==='active' || pointedAt === true)` + 低置信 fixtures 回放断言"外发流 0 条"。
**如果他是对的，我的架构要改什么**：把 L2 写进 speaker 专文的**演进第一版形态**（我原来只写了"接口留、实现后加"，没给第一版形态）。这条我照改。

---

### 他的主张 9：主被动状态机是最值得先做的一条；纯函数 + 滞回 + 置信门限 + 可撤销默认值（**默认主动**，宁可多答不可漏答）；12 条场景回放

**判定**：成立，但"默认主动"我要**反对一半**
**我的回应**：代价不对称在两个模式里是**反的**——主动模式下漏答 = 用户觉得它坏了；被动模式下误答 = 会议事故（他自己 §4.3 算过：段级 FP 2% → 每小时 1–4 次误答）。所以"默认主动"不能是全局常量，必须是**按模式取值的默认**：active 域默认出声，passive 域默认沉默。这恰恰是 `decide(signals, mode)` 里 `mode` 参数存在的理由——**他的纯函数签名如果只有 `signals`，这个默认值就无处安放。**
**谁来保证它不被破坏**：判决表按 `(signals × mode)` 穷举；被动域默认值写死在表里并单测；出口断言。
**如果他是对的，我的架构要改什么**：无实质改动（签名早就带 mode）。

---

### 他的主张 10：T4 第一版 ❌；难点不是生成应用，是**失败是异步的、没有回滚路径**；所以 T4 第一段代码必须写失败路径，而不是生成器

**判定**：部分成立（失败路径优先我认，但"❌ 第一版 → 不用管"是错的，见议题 4）
**我的回应**：失败路径优先，认。但他漏了一个前置形状：**"时间承诺"必须落盘**。v6 §五 要求"时间承诺来自实测分布，误差 >50% 主动更正"——要能主动更正，你必须知道**当时承诺了什么**。`promise / promiseAt / promiseBasisVersion` 是**历史形状**：今天不记，将来每一条历史承诺都不可更正、不可归因。同理"产物回收"需要 `retentionUntil`（REVIEW-eng 炸点 5 的磁盘水位就是靠这个治的）。
**谁来保证它不被破坏**：任务表 schema（`taskId / originTurnId / promise / promiseBasisVersion / retentionUntil / state`）+ 落盘 + 失败态用户可见文案的固定枚举。
**如果他是对的，我的架构要改什么**：无（我补的是他没提的字段）。

---

### 他的主张 11：级联第一版不做；瓶颈恰好长在最贵最慢的位置（融合是父、专家是子 → 父子 0 命中、融合全价冷启动，而融合决定反馈层何时开口）；用现成的 `CONCIERGE_EXPERT_ANGLES` 2→4 旋钮做 1 小时实验拿到斜率

**判定**：**成立**（结论、依据、旋钮实验我全部同意并采纳）
**我的回应**：唯一补充，而且是纯收益的补充：**"第一版不做"不等于"限制不用现在设"**。`maxDepth` 默认 3、子 agent 并发无上限、`maxRetries=5` 且含 `EMPTY_RESPONSE`——这三条**不需要级联就会被撞上**（任何一次子 agent 使用），改动是**三行配置**。他的验证清单里没有它们，而它们恰好是"不改就静默失控"的那一类（REVIEW-eng E7/E8）。
**谁来保证它不被破坏**：显式配置 + 启动断言（`maxDepth` 未显式设置则拒启）+ trace 落 `nodes_per_depth / total_llm_calls / retry_count`。
**如果他是对的，我的架构要改什么**：无。

---

### 他的主张 12：记忆隔离的难点不是"隔离"，是**"共享面的清单化"**（除 `DSH_HOME` 外还有共享只读 profile、插件配置、日志目录、`~/.dsh/sessions`）；做法是白名单 + 强制只读 + 启动可写即拒启；测试要断言**共享目录在整个测试期间 mtime 不变**

**判定**：**成立**，而且这是他对我的 R1 保证者的**必要加强**
**我的回应**：我原来只写了"能力式句柄 + 隔离字符串断言 + 部署脚本断言 `basename(DSH_HOME)`"。他补的 **mtime 断言**是唯一能测出"共享面被写"的手段——字符串断言测不出"写了个空文件""改了个索引""追加了一行日志"。这是我该补的第四条保证者。
**谁来保证它不被破坏**：共享路径白名单（配置即代码）+ `chmod a-w`/`ro` 挂载 + 启动自检（可写即拒启）+ mtime 断言 + 无 `userId` 读接口的签名 grep。
**如果他是对的，我的架构要改什么**：R1 保证者从三条扩为四条（我照改）。

---

### 他的主张 13：说话人识别第一版 ❌（缺接口或缺前提数据），只做自用 VAD

**判定**：**混淆了层次**（"实现不做"对，"❌ 缺接口"错）
**我的回应**：他给的三条理由我全同意（输入端本身就是 1–2 个月攻关、输出的动作不可撤销、P4 还没证明主动使用）。问题在他的表格判据：他定义 ❌ = "第一版做不了（**缺接口或缺前提数据**）"。而说话人识别的**信号位与判决形状**既不缺接口也不缺数据——`SpeakerSignals` 类型、`decide()` 纯函数、`Decision.action='silent'`（= `noReply` 可表达）是**现在花 0 成本、以后满仓库返工**的形状。写成 ❌ 会让读者合理地推断"连信号位也不用留"。
**判据（我的）**：**"实现不做"与"接口不留"是两个不同的判定**，他的一张表只有一个格子，装不下两个判定。这就是他把架构问题当实现问题处理的典型位置之一。
**谁来保证它不被破坏**：L0#6（`Decision`/`Utterance` 分离 + `noReply` 可表达 + `SpeakerSignals` 第一版填常量 `{speaker:'owner',confidence:1,pointedAt:true}`）。
**如果他是对的，我的架构要改什么**：无（结论一致，只是表述要拆成两栏）。

---

### 他的主张 14：A/B 不是全局二选一，而是**按角色分配**——接收层用 B（按单元投递）、反馈层用 A（逐字流式）；若只能选一个，选 B。判据：优先消除不可逆的失败

**判定**：部分成立（分类直觉对，**结论混淆了层次**）
**我的回应**，四点，其中两点用他自己的论据：
1. 他把 A/B 从"全局单点决策"改成"**按角色两个决策**"——这**恰恰是我反对的"由层决定投递语义"**。我 §3.1 说的是"不该由任何单一层做"；他的方案是"由两个角色各做一次"，结果是客户端必须**同时实现两条渲染路径**（单元缓冲 + 打字机回放 AND 逐字流），两套验收、两套 bug。这比他批评的"全局二选一"更贵，不是更便宜。
2. **用他的 C6 打他的 §2.1**：C6 说"以单元 seq 为地址的续传"是 B 的免费收益，A 要"按字节偏移续传"（他嫌贵）。但按他的分配，**走 A 的恰恰是长文那一层**（100–300 字、最可能横跨一次重连）——**单元寻址覆盖不到的那一层，正是最需要它的一层。**
3. **他 C6 的成本前提混了层次**：220 字节/52k 事件/11MB 是 REVIEW-eng E12 里 `dsh-host-apiproxy` **跨进程读日志**的成本；控制器在进程内（他与 REVIEW-eng E6 都锁死了），维持一个 8KB 尾部环形缓冲是**零成本**。他把跨进程读日志的账，记到了进程内缓冲的头上。
4. **正确的切分轴不是"角色"，是"commit 权在谁手上"。** 把放行调度交给控制器，"永不露半句"这个**效果**就能在没有协议单位、没有第二渲染路径的前提下拿到——**"按单元投递"不是一种数据格式，是一种放行调度。**
**谁来保证它不被破坏**：通路唯一（逐字流式）+ 控制器独占 commit 与放行调度；协议只带 `turnId / block: fast|deep`，**不带句边界**。
**如果他是对的，我的架构要改什么**：若 PM 坚持"任意时刻被打断都停在完整句"，我接受 B，但按我 §3.2 的**单点切分**执行，并要他书面承认三条代价（协议引入 `Segment`、客户端多一条回放路径且必须承认抖动空档、首字 p50 指标变更）。我的立场不是拒绝 B，是**拒绝在没有 PM 的指标变更确认前，把 B 做成协议**。

---

### 他的主张 15：句边界三条规则（硬边界含 `；` / 累积 ≥25 字强制切 / 首 token 后 600ms 兜底 / 强制切时回退最近 8 字内的 `，、` / 逗号本身不算边界）；"这个我熟，先看连接池"= 一个投递单元

**判定**：**不成立**（作为投递协议）；作为**接收层的生成纪律**成立
**我的回应**，三条，全是他的规矩反噬他自己的目标：
1. **兜底切分制造"伪完整句"**：600ms 到点强制切，切出来的是一个语法上不完整的单元，但协议上它**必须被当作完整单元**（否则客户端会继续等它）。于是 B 的唯一收益（"用户看到的永远是完整句"）在它自己的兜底路径上失效；记账上它还标成 `soft_end`，反馈层被要求"接着它说"——**模型被派去接一个不完整的句子。**
2. **逗号身份二义 + 投递结果依赖时序**：他 §2.2 花整节论证"逗号不做边界"，兜底又回退到逗号。同一段文本，负载轻时按句号切成 3 个单元、负载重时按 600ms 兜底切成 5 个单元 → **投递结果依赖时序（非确定性），而它又被提升为"用户看到了什么"的事实源**。他在 §3.2 警告我的那种"偶发、不可复现、无法定位"的 bug，就长在他自己的兜底规则里。
3. **"语义自足"没有 owner**：§2.5 说"完整从句（≥8 字、语义自足）→ 发，标 `soft_end`"。语义自足谁判？说不出 owner，它就会在四层各出现一次。
**谁来保证它不被破坏**：这三条规则**降级为接收层的生成纪律**（短句、自足、禁跨句指代开头如"这个/它/另外"），由提示词 + 场景测试约束；判定权不进投递协议。控制器只保留一个 `isBoundary()`，服务于**放行调度**，不服务于投递格式。
**如果他是对的，我的架构要改什么**：无——他的数值（25 字 / 600ms / 8 字回退）我**全盘采纳**为生成纪律的参数，只是不放进协议。

---

### 他的主张 16：首单元延迟增量必须钉死在 ≤0.35s，不做长度上限 p90 会到 +0.9s；这条估算必须实测——用现有 `session.jsonl` 的 chunk 事件按三套规则离线重放

**判定**：部分成立（测量该做，**但它是"基线"，不是"营运点"，不能当 go/no-go**）
**我的回应**：**循环依赖**。单元延迟 = f(接收层的输出分布)；输出分布 = g(提示词)；而提示词恰恰是他 §2.3 自己要改的对象（"提示词约束 + 兜底切成 15 字"）。**用当前提示词下的历史输出，去裁决改过提示词之后的投递策略**——测出来的只是上界，不是营运点。这次测量**不能是 B 的 1 天 go/no-go**，但"最坏情况有多坏"仍然值钱，且零风险，**我支持排进 S2 与地基并行做**。
**谁来保证它不被破坏**：作为指标变更依据的测量，必须在生成纪律生效之后以同一批问题重跑一次。
**如果他是对的，我的架构要改什么**：无。

---

### 他的主张 17：barge-in 下 B 更好（视觉完整 / 记账成一个整数 seq 水位 / TTS 必需）；前提是客户端"瞬时 snap 到已收单元末尾" + 打字机回放 + `playback_rate ≥ gen_rate×1.15`；缓冲残词**默认丢、且只能在服务端丢**；丢掉的文本仍进记录但标 `delivered:false`

**判定**：部分成立
**我的回应**：
1. **"TTS 一开就必需按句投递"——成立，我认，这是他最锋利的一条**（合成器不能合成半个词）。我采纳它，但把它变成**按输出模态分档**的合法轴：`tts=true ⇒ 必须单元投递`；文本档走 A + 窗口。**注意这两条轴的区别：模态是客观属性，角色不是。**
2. 客户端 snap + 打字机回放 + 回放速率 ≥ 生成速率×1.15：成立，但代价要算清——**它引入了抖动缓冲的经典两难**：回放速率跟不上就出现"播完单元 1 等单元 2"的空档，而空档正是他 §2.2 说的"打字停了"。**B 的沉默期摊在每一句之后；我的窗口把它压在"换人"那一次。** 这才是两种方案真正的成本差（不是 0.25s vs 0.4s）。
3. **"丢只能在服务端发生"——在 B 通路下成立，在 A 通路下"丢"是无操作**：已放行的字节撤不回来（他自己的 §3.0 原则），未放行的字节本来就没人看见。所以这条纪律的正确表述是：**丢弃的效果不是"让屏幕变干净"，而是"让 `emittedText` 与屏幕分叉"**——在 A 通路下它只破坏不变量，不改善观感。把它当成通用硬纪律，是过度归纳。
4. `delivered:false` 标记：**成立，采纳**（与我的落盘相容）。我加一条：**未放行字节一律保留在记录里，绝不删除**，否则"我当时到底生成了什么"不可回放。
**谁来保证它不被破坏**：记录里两个独立字段 `released:boolean` / `superseded:boolean`；`released` 水位由控制器独占；客户端禁止自行切分。
**如果他是对的，我的架构要改什么**：新增"语音档必须单元投递"这条策略档（我照改）。

---

### 他的主张 18：贯穿原则"已显示的字撤不回来，但可以追加"；**权威顺序：客户端 ack 水位 > 网关投递日志 > 宿主 assistant/message**；归纳三条（单一写入者 / 权威顺序 / 每个终态都要有超时兜底）

**判定**：原则**成立**；**权威顺序不成立**——这是本轮最重要的一处不成立
**我的回应**：
- 原则"纠错只能追加"：**成立，我采纳为 S4 / 打断 / 误答 / 双答竞态的通用形态**（我原来只写了 S4，没把它升格为贯穿原则）。这条是他对我报告的真实增补。
- **权威顺序错，且是架构级**。权威必须同时满足三性质：**随时可得、单调、单一**。ack 水位满足后两个，**不满足"随时可得"**——它恰恰在最需要它的时候（断线、客户端崩溃、重连窗口内，也就是他的 C6）**不存在**；而且它**每设备不同**（他的 C9），于是 R5 的内容变成**设备相关**：同一轮对话在两台设备上给反馈层喂两份不同的"已输出全文"，同一个问题得到两个不同的承接。这不是"多一跳"的代价，是把 R5 从**服务端不变量**降级成**客户端上报量**。
- 正确顺序：**唯一权威 = 控制器 commit 流**（进程内、单调、落盘、断线时依然可得）；**ack 是派生证据**，只用于"哪些需要重传"，不用于"用户看到了什么"。
- **这个选择有一个漂亮的副作用：竞态的数量是权威选择的函数。** 权威一换：**C1** 不再是竞态，只是"读一次 commit 水位"（他 §C1 的原子三步与 150ms 超时兜底整节消失）；**C2**"服务端以为投到 12、客户端渲染了 13"消失（两边读同一个单调水位）；**C9** 从**语义问题**降级为**重传问题**（第一版单设备优先不再是妥协，只是"重传窗口按最慢设备对齐"）；**C10** 保留（replace 例外仍需令牌）。
- **C3 / C4 / C7 / C8 我全部采纳**：C3 首句情绪无关（这条同时救了我的窗口，见议题 2）、C4 turn 血缘 + `superseded`、C7 `deep_ready` vs `handoff_allowed`、C8 打断后不 cancel 处理层（沉没成本已付）。两处补充：C7 的 `handoff_allowed` 是我 §3.3 窗口的**必要前置闸门**（我漏了，认）；C8 必须补一句**预算**——"继续算完"的算力必须计入当轮 `total_llm_calls`，否则 REVIEW-eng 的 `≤12` 熔断会被 superseded 的轮次绕过（他的 C8 与熔断断言在实现上会打架）。
- "每个终态都要有超时兜底"：**成立**，且它是我该补进 L0 的工程纪律（无超时的等待就是死锁）。
**谁来保证它不被破坏**：权威定义进 ADR + 装配入口只接受 committed 前缀。**这条可以用类型强制**：上下文装配的入参类型里**根本没有 ack 水位这个类型**——ack 只出现在重传路径的签名里。这就让"用 ack 当权威"变成编译不过。
**如果他是对的，我的架构要改什么**：无（反而更精确）。但若他坚持 ack 权威，我要他回答一个问题：**断线重连的那 1–10 秒里，R5 的"已输出全文"取什么？** 回答不了，"ack 是权威"就不成立。

---

### 他的主张 19：单一写入者——只有控制器能 `steer / inject / followup / cancel`，任何其他组件想影响对话必须走它

**判定**：**成立**（与 REVIEW-eng E6、我 §1.2 完全一致）
**我的回应**：补一条类型级强制，否则它只是纪律：把这四个方法收进一个 **capability 凭据**（只有控制器持有），其他组件**在类型上拿不到**它。"代码评审说只有控制器能调"是可以被绕过的，拿不到方法则不可表达。
**谁来保证它不被破坏**：capability 令牌 + CI grep 断言"除控制器外无 `steer|inject|followup|cancel` 调用点"。
**如果他是对的，我的架构要改什么**：无。

---

### 他的主张 20：延迟预算是固定盘子，并行只是换成对上游并发数的需求；按句投递从首字预算拿走 250–350ms，**这是产品指标变更（首字 p50 0.9→1.15s），必须 PM 拍板**，不是实现细节

**判定**：**成立，而且这是他对我的报告最有力的一击**
**我的回应**：我在 §3.3 的代价表里写"接管时刻最多多等一句 ≈200–400ms（用户体验，**可忽略**）"——**"可忽略"是工程判断，"是否接受首字指标变更"是产品判断，我越权了。** 他这句我不辩解，当场认。推迟窗口的代价必须写成**指标变更提案**交给 PM：附上他 §2.3 那张表 + 我的窗口上界，由 PM 在两个数之间选。
**谁来保证它不被破坏**：任何影响 `first_token_p50` 的机制，其 PR 必须附"指标影响"一栏，由 PM 签。
**如果他是对的，我的架构要改什么**：§3.3 代价表删掉"可忽略"，改为"需 PM 拍板的指标变更：首字 p50 −Δ / 接管点完整率 +X"。我照改。

---

### 他的主张 21：给架构师的三条物理边界——①跨 agent 缓存 0% 是物理事实，所以**协调/融合角色必须是最便宜的那个**，而 v6 里最贵最慢的融合节点恰恰决定用户何时听到结论；②**只有能被安全中止的角色才配拥有开口权**，"这一条比'谁说什么'更硬"；③"用户看到的"和"模型看到的"是两份不同文本，而架构里没有任何一处指定谁是权威

**判定**：①**成立**；②**混淆了层次**；③**成立，且是共识**
**我的回应**：
- **①成立**，与 REVIEW-eng §4.3 同源。架构推论我接受：**"融合节点要快"这个期望不能进设计**；v6 §三 的"t≈1.5s 深思回答"在级联档位下不成立（他主张 3 已证）。→ v6 §三 时间轴分档重写，写进我的修改清单。
- **②不成立**：**"能否被干净地停下"不是角色的属性，是"stop 权归属"的属性。** 接收层之所以停得干净，是因为（按我 §1.1）我们把停的时机交给控制器、禁止它自我收尾；把它当成角色的固有属性，会得出"所以接收层天生适合 B"，而真实因果是"**所以接收层不该自己决定停**"。**他的 §6② 与 §2.1 之间是循环论证：先用投递属性推出角色分层，再用角色分层证明投递属性。**
- **③是共识**（= 我的 L0#1）。而且我要补一刀：**③ 这个洞的存在本身就证伪了他 §5 的优先序**——既然"不补它所有承接都建在流沙上"，它就不是"1 天可验证的假设"，而是"必须先钉住的地基"。**他给出了本轮最强的一条 L0 论证，却把它排在验证之后。**
**谁来保证它不被破坏**：①靠"融合角色不进关键延迟路径"这条设计约束 + 准入规则；②靠出口断言（不靠角色自觉）；③靠落盘 + 装配入口签名。
**如果他是对的，我的架构要改什么**：v6 §三 时间轴分档重写（我照改）。

---

### 他的主张 22：三条技术验证 V-A/V-B/V-C，每条 1 天内完成，**失败会推翻一条已写进架构的设计**

**判定**：V-A 部分成立 / **V-B 归类错误** / V-C 成立但只推翻策略常量（详见议题 1）
**我的回应**：
- **V-A**：离线部分测的是"**量**"（延迟分布）——失败推翻的是 B 的成本假设，即一条产品权衡，**不推翻架构**。在线部分（"能不能扣住 chunk"）他写的失败分支是"B 的执行点挪到客户端"——**这只是他的框架下的结论**。在我的框架下，同一个失败（客户端能直连宿主 WS）推翻的是**外发通路的唯一性**，也就是 G3 与我 L0#1/#3 的物理前提：只要存在一条不经控制器出口的路径，`emittedText` 与"唯一出口"在结构上就不可实现，而且**此后所有验证结论都不可复现**（你不知道验的是哪条通路）。→ **V-A 的第一条判据不该是"能不能按单元投递"，而是"外发通路是不是唯一的"**，且它必须排在 L0 的 `emit()` 之前，作为 G3 的验证。
- **V-B 归类错误，且有方法论漏洞**：它断言的四件事（不出现两段重叠 / 不出现未投递文本 / 不串味 / 还是同一条消息）**全部是 L0#1/#3/#4 的产物**。要一天做完 V-B，你必须先有一个假宿主 + 一套 emit/水位——**那就是 L0 的最小切片**。所以 V-B 不是"验证探针"，它是 **L0 的验收测试**，必须排在 L0 之后。
  更硬的一刀：V-B 用**假宿主 + 真模型**，而 V-B 的全部失败模式都来自**真实宿主的具体装配与事件语义**（F1 的过滤、F2 的 chunk 边界、在途 chunk 窗口）。用假宿主测出的"不出现两段重叠"，只在假宿主的语义下成立。**正确配置是反过来的：真宿主 + 可控（脚本化/限速）上游。**
- **V-C**：成立，但两个分支性质不同。(a)"尾部追加块是否仍保缓存"测的是**宿主/上游行为**，可证伪，值钱，排 S2 并行；(b)"首句等情绪是否把首字推到 1.6s"测的是**策略**，结论是 R3 的降级选项，**不推翻边界**，属 S4。
- **统一点**：**可测的是"量"，不可回填的是"形"。** 一天的实验能裁决延迟、缓存命中、误报率；裁决不了 schema、类型、权威归属。他的清单三条里**没有一条**对应我 §4.1 的三条判据（历史形状 / 公共必经之路 / 让 R 结构性不可实现）——**这就是优先序漏掉的东西。**
**谁来保证它不被破坏**：验证清单必须带一栏"它测的是量还是形"；凡"形"一律进 L0，不得写成验证。
**如果他是对的，我的架构要改什么**：无（我把 V-A 在线部分升格为架构验证、V-B 降格为 L0 验收）。

---

### 他的主张 23：三方共识——说话人只做自用 VAD / 级联第一版不做 / 先落"投递水位 + 三份文本一致性 + 每轮 trace"；他最坚持的是**先把"用户实际看到的文本"变成单一、可断言、可续传的真源**

**判定**：**成立**（与我 L0#1 一致，这是本轮两稿唯一的硬共识）
**我的回应**：共识成立。补一句他可能没意识到的推论：**他最坚持的这条，恰好不能靠验证拿到，只能靠先建**——它是形，不是量。**他 §6③ 与结论 3 联合起来，已经论证了他 §5 优先序的反面。** 我不需要另找理由，用他自己的两段话就够了。
**谁来保证它不被破坏**：L0#1 落盘 + 装配入口签名 + 外发流前缀断言。
**如果他是对的，我的架构要改什么**：把 `emittedText` 落盘写进 **v6 §十 的 P0**（现 P0 只有"修工具链 + 单 agent + 状态条 + trace + 首句规则 + 假前提检测"）。**这是我对 v6 的计划级修改。**

---

### 他的主张 24：C2 首版做法——`turn/stop` 后服务端停止转发 + 客户端在收到 stop 后丢弃未 ack 单元 + 对已显示部分追加止损语

**判定**：部分成立（方向对，**"客户端丢弃"是错的 owner**）
**我的回应**：在 A 通路下让客户端"丢弃未 ack 单元"，前提是**客户端能识别"单元"**——那等于客户端实现一份切分规则，正是他 §3.2 自己反对的事。而且已渲染的字节撤不回来，能丢的只是尚未渲染的缓冲，收益不确定而复杂度确定。正确做法：**服务端冻结放行水位，客户端只渲染已放行前缀**；"单元"这个概念在客户端**不存在**。
**谁来保证它不被破坏**：客户端禁止自行切分（CI grep + 协议里不出现句边界字段）；止损语一律走追加出口。
**如果他是对的，我的架构要改什么**：无（首版做法按 A 通路重写）。

---

# 乙、四个议题的正面裁决

## 议题 1：他的"1 天验证"清单，是架构问题还是实现问题？失败推翻什么？

**逐条定性**

| 验证 | 测的是量还是形 | 失败推翻什么 | 我的定性 |
|---|---|---|---|
| V-A 离线（切分规则重放） | 量 | B 的成本假设（一条**产品权衡**） | 实现层成本探针，该做，可与地基并行 |
| V-A 在线（能不能扣住 chunk） | **形**（通路唯一性） | 按他的框架→"B 挪到客户端"；按架构→**外发通路唯一，即 G3 与 L0#1/#3 的物理前提** | **他框架下的"实现问题"，实为架构问题** |
| V-B 五种时序 | 形 | 落在我的 L0#1（他已经把失败分支写在纸上） | **L0 的验收测试被误标为前置验证探针** |
| V-C 缓存分支 | 量 | R2 的落地方式 | 该做，排 S2 并行 |
| V-C 情绪分支 | 策略 | R3 的降级选项 | 策略比较，属 P2/S4，不动边界 |
| 旋钮实验（ANGLES 2→4） | 量 | 级联延迟斜率的自有数据 | 采纳，成本 1 小时 |

**结论**：他的清单**九成是"量"**，是工程成本探针——这本身没有问题，问题在**标注**：他把其中唯一一条真架构问题（外发通路唯一性）标成"投递机制问题"，把其中唯一一条真架构验收（V-B）标成"前置探针"。**这是"把架构问题当实现问题处理"的第一处，也是最贵的一处。**

**地基项能不能被 1 天验证覆盖？不能。**
- `emittedText` 落盘 = **历史形状**。没有"实验失败→改设计"的形式，只有"现在不记→以后按历史赔付"的形式。而且证据是计划级的：v6 的 P0–P3 共 15 天，P4 是 **21 天真人日志**——**那是全项目唯一不可替代的证据来源。如果 P4 跑在 `emittedText` 落盘之前，这 21 天在"承接是否锚定在用户看到的文本上"这个轴上是零信息**，而那正是 P4 要判的核心之一。
- `noReply` 可表达 = **类型可表达性**，不是可测量。它只能靠"试着加被动模式，发现满仓库假定'一定会说话'"来发现——**三个月后才发现**。
- `openMemory(userId)` 能力句柄 = **调用面形状**，只能靠"后来 grep 出几十个调用点"来发现。
- `turnId` + 协议占位 = **历史形状**。

**他的优先序漏了什么**：漏掉了"不可回填的形状"这整整一类。具体四项：`emittedText` 落盘、`Decision`/`Utterance`/`noReply` 形状、协议占位（`turnId` / `attachments` / `taskRef` / `progressSignal` / `originTurnId`）、`SpeakerSignals` 信号位。而其中**第一项他自己已经在 §6③ 与结论 3 论证过了**——他的论证支持我的优先序，不支持他的清单。

---

## 议题 2：他坚持"四层各自发明标点规则"，PM 坚持按句——我的 ≤400ms 窗口能否两头都满足？

**能，但只满足一半，我把那一半精确划出来。**

**先对齐一个前提**：他的"按句投递"和我说的"推迟窗口"不是同一类东西。他的 B 是**数据格式**（单元作为协议单位，客户端按单元渲染、按单元 ack）；我的窗口是**放行调度**（通路仍是逐字流式，只有"什么时候把接管动作发出去"被推迟）。**"按单元投递"不是一种数据格式，是一种放行调度**——这一点定了，他 §3.2 担心的"四层各自发明标点规则"就不可能出现，因为**客户端和协议里根本没有"句"这个概念**。

**精确契约（`DeferWindow`）**

| 项 | 定义 |
|---|---|
| **归属** | 控制器独占。`isBoundary()` **单一实现**，规则不外泄给接收层/协议/客户端 |
| **前置闸门** | `deep_ready === true` **且** `handoff_allowed === true`（采纳他的 C7）。**不做预测**——窗口只在"深答已就绪"这个已发生事实上生效（v1 的 `deferWhileThinking` 就是这个机制，F5：谓词升级只是换一个布尔条件） |
| **触发条件** | 仅当"当前已放行文本的末字节**不是**硬边界"时窗口才有意义；末字节已是硬边界 → Δ=0，立即接管 |
| **判句边界** | `isBoundary(tail)`：命名边界字符 ∈ `{。！？…\n}`。**我反对把 `；` 列为硬边界**（分号后是同层并列分句，接管点在那里同样是断裂；降为软切点，不参与放行判决） |
| **窗口内行为** | **继续逐字放行，不扣留**（这是与 B 的根本区别：B 扣留 → 沉默期对用户可见；我不扣留 → 无沉默期） |
| **超时（Δ_max=400ms）** | **立即接管**，屏幕可能停在半句 |
| **半句：丢弃还是补发** | **都不。** 已放行的半句留在屏幕上（撤不回来，采纳他 §3.0 原则）；**未放行的字节一律补发、绝不丢弃**——丢弃在 A 通路下是**无操作**，唯一效果是让 `emittedText` 与屏幕分叉 |
| **半句的性质** | **正常态，不是失败态。** 兜底不是补发，是**深答首句的承接口径（S4-lite：补完而非纠偏）**：首句必须与 `emittedText` 末句共享至少一个实词，且不得重复末句已说内容 |
| **客户端契约** | 只渲染 `released` 流；**不得自行切分、不得自行缓冲整句**（与他 §3.2 的结论一致） |
| **断言** | ① `client.renderedText` 是 `emittedText` 的前缀（逐字节）；② `emittedText` 单调增长；③ `handoff.text := emittedText`（同一对象）；④ turn 关闭后 `released` 不再增长（除显式补投） |

**满足度裁决**

| 要求 | 是否满足 | 说明 |
|---|---|---|
| 他（工程可行） | ✅ | 单点判句、**无协议单位**、**无第二渲染路径**、不引入可感知沉默期、不引入客户端缓冲 |
| PM"接管点不落在半句中间" | ✅ | 每次换人（每一轮难题都发生）都被保护，成本 ≤400ms 且藏在"换人"这个本来就有的停顿里 |
| PM"任意时刻被打断都停在完整句" | ❌ | **只有 B 能给。** 我的窗口保护的是"接管时刻"，保护不了"barge-in 时刻" |

**真正的取舍点**：**不是延迟数字，是"完整句"的保证范围**——"接管路径的完整"还是"任意打断时刻的完整"。而且我要指出一个 owner 问题：**他 §2.1 已经替 PM 做了这个决定（选 B），他 §6 又说这是产品指标变更必须 PM 拍板。同一个决定不能有两个 owner**——这正是我 §1 那句"两头都能做的地方就是 bug 的存放处"。**这是本议题里他必须自己解决的自相矛盾。**

**如果 PM 选"任意时刻完整"**：我接受 B，条件三条：① 按我 §3.2 的**单点切分**（切分只在控制器算一次，输出 `Segment` 作为协议字段，其余三层不切）；② 客户端回放路径必须承认**抖动空档**（"播完单元 1 等单元 2"）；③ 首字 p50 的指标变更书面确认。另保留一条反建议：**按模态分档是合法的（`tts=true ⇒ 单元投递`，他 §2.4 论据我全盘接受），按角色分档是不合法的**（他 §2.1）——前者是客观属性，后者是循环论证（主张 21②）。

---

## 议题 3：他的 3 个验证与我的 L0 八项冲突吗？

**结论**：**不冲突的占多数，冲突的只有一条半，但那条半是致命的。**

- **不冲突**：V-A 离线、V-C 缓存分支、旋钮实验**都不依赖 L0**，可与 L0 并行（它们读现有日志、改一个配置）。
- **冲突**：**V-B 与 L0#1/#3 是同一件事的两种叫法**。把它排在 L0 之前，等价于"先验收再施工"。V-A 在线部分同理——它的判据依赖 `emit()` 的唯一性。

**不做就无法回填（必须排在所有验证之前）**

| 序 | 项 | 为什么不可回填 | 判据 |
|---|---|---|---|
| **0** | 显式 `maxDepth=2` / 子 agent 并发上限 / 重试预算（去掉 `EMPTY_RESPONSE` 可重试） | 三行配置；不需要级联就会撞上；静默失控 | 不是形状，是**今天 2 小时就能关掉的洞** |
| **1** | `emittedText` 逐轮落盘 | 历史形状；**P4 的 21 天不可替代** | 判据 1 |
| **2** | `turnId` + 每轮 trace 字段（`prompt_version / prefix_tokens / first_token_ms / retry_count / handoff_path / turn lineage / superseded`） | 历史形状 | 判据 1 |
| **3** | 协议占位：版本字段 + `attachments[]` + `taskRef` + `progressSignal` + `originTurnId` | 历史形状 + 客户端消息模型 | 判据 1 |
| **4** | 唯一出口 `emit()` + `Decision`/`Utterance` 分离 + `noReply` 可表达 + `SpeakerSignals` 信号位 | 公共必经之路 + 让 R4/被动模式结构性可实现 | 判据 2+3 |
| **5** | 装配入口唯一化 + **上下文域 `(domain, promptVersion)`** | 公共必经之路；后加=全部 prompt 结构重写 + 缓存全废 | 判据 2 |

**可以后加**：`openMemory(userId)` 能力句柄（但**不晚于网关上线**，REVIEW-eng 说"多用户第一步是不裸奔"）；`rebuild()/replaceSurface()/crossTurnAppend()` 与令牌（在第一个例外出现前）；T4 任务状态机 / 说话人识别 / 级联 / T2/T3 渲染器的**实现**——只要形状先有。

**→ 合并后的统一顺序见下一节。**

---

## 议题 4：T4 与 R2/R5 的冲突——"第一版不做所以不用管"不成立

**核心反驳一句话**：**"T4 第一版不做"是关于实现的真话；"不用管"是关于形状的假话。第一版每一轮对话，都在为 T4 写一段不可回填的历史。**

**不留就会返工的接口（7 项，全部与"今天"有关，没有一项是"T4 的实现"）**

| # | 接口 | 不留的后果 |
|---|---|---|
| 1 | `originTurnId`（轮次归属） | 历史形状。今天不加，**历史轮次永远无法与任何任务关联**，T4 落地时"这个任务属于哪一轮"只能猜 |
| 2 | `attachments[]` / `taskRef` 占位 + 客户端"一条消息可带附件" | 客户端消息模型一旦是纯文本字符串，后加 = 改数据层 + 兼容历史消息 + 改渲染 |
| 3 | **R5 的条件化**（承接形态才要求含 `emittedText`；T4 完成通知属**事件档新形态**，其上下文是"任务材料 + 承诺原文"，不是接收层全文） | 不条件化 → T4 落地时 R5 物理不可满足 → **实现者会静默放宽 R5**。**纪律被静默降级，比纪律被违反更坏** |
| 4 | 跨轮追加的**显式 API** `crossTurnAppend(taskRef)` + 令牌，**与 `replaceSurface` 的例外共用同一套形态** | 不显式化 → 任务完成时的追加会**伪装成尾部追加**溜过 R2；且它会借今天为 replace 开的口子进来 |
| 5 | `Utterance.turnId` → **`originTurnId`**，且允许指向**已关闭**的轮次 | T4 的完成通知天然不属于当前活动轮。这是今天 0 成本、以后要动**所有 `emit()` 调用点**的契约形状。**（这一条打的是我自己的契约，我当场改）** |
| 6 | 任务状态表 schema（`taskId / originTurnId / state / promise / promiseBasisVersion / retentionUntil`）+ **落盘真源的投影** | 我的风险 3：进程内是执行位置，不是状态位置。任务状态留在进程内存 = 重启即失；保留期不落盘 = 磁盘水位不可治（REVIEW-eng 炸点 5） |
| 7 | 承诺账（承诺值 + 承诺时刻 + 依据分布版本） | v6 §五 要求"误差 >50% 主动更正"——**不知道当初承诺了什么，就无法主动更正** |

**反向一问（请他回答）**：T4 第一版不做，那**"用户请求交付形态"的入口**也第一版不做吗？如果用户在第 40 轮说"帮我做个小工具"，而系统回"这个我做不到"——**能力可以后加，承诺不能。**

---

# 丙、合并后的统一顺序（验证与地基混排）

> 判据：**先定形状（不可回填）→ 再测量（可与形状并行）→ 最后验通路（依赖形状的唯一性）。**
> 他的顺序是"先测量 → 后定形状"；代价是**形状漏了要用历史赔付，而历史不接受补写**。

| 段 | 内容 | 工期 | 为什么在这个位置 |
|---|---|---|---|
| **S0 · 关洞（今天）** | ① 修 `npm test` 入口（REVIEW-eng E10）；② 显式 `maxDepth=2` + 子 agent 并发上限 + 重试预算（去 `EMPTY_RESPONSE` 可重试）；③ 写一份 ADR 钉死"**唯一权威 = 控制器 commit 流，ack 是派生证据**" | 2 小时 | 全是零风险的关洞动作；③ 是后面每一项的判据来源，必须先在纸上定 |
| **S1 · 形状（不可回填）** | ① `emittedText` 单一真源 + **逐轮落盘**；② `turnId` + 每轮 trace 字段；③ 协议占位（版本 / `attachments` / `taskRef` / `progressSignal` / `originTurnId`）；④ 唯一出口 `emit()` + `Decision`/`Utterance` 分离 + `noReply` 可表达 + `SpeakerSignals` 信号位；⑤ 装配入口唯一化 + `(domain, promptVersion)` 维度；⑥ 记忆提交幂等键 `turnId` | 3–4 天 | 命中我的三条判据（历史形状 / 公共必经之路 / 让 R 结构性可实现）。**必须在 P4 之前完成**，否则 21 天真人日志在承接质量轴上为零信息 |
| **S2 · 测量（与 S1 并行）** | ① 他的 V-A 离线（首单元延迟分布 + 首句长度分布 + 按单元投递的文本差异率）——**只作基线/上界，不作 go/no-go**；② 他的 V-C 缓存分支（尾部追加是否保缓存）；③ `CONCIERGE_EXPERT_ANGLES` 2→4 旋钮（他提的，1 小时）；④ 首字预算实测：情绪/假前提并行的在途请求数与 429 放大 | 各半天–1 天 | 全是"量"，全不依赖 L0，全部零风险，**并行做不占关键路径** |
| **S3 · 通路（依赖 S1 的唯一出口）** | ① **V-A 在线部分，判据改为"外发通路是否唯一"**（= G3 的可测形式）；② V-B 五种时序，**配置改为真宿主 + 可控上游**（不是假宿主 + 真模型），并定位为 **S1 的验收测试**而非前置探针 | 1–2 天 | 它们的断言对象是 S1 的产物；排前面就是"先验收再施工" |
| **S4 · 策略（消费 S1 接口）** | 推迟窗口 ≤400ms + `isBoundary` 单点 + S1–S4 形态表 + 首句锚点校验 + 六种失败模式场景 + C3/C4/C7/C8 的防御 + V-C 情绪分支（R3 的降级选项） | 5 天（≈v6 的 P2） | 只消费 L0 接口，延后一周只是晚一周（我 §4.3） |
| **S5 · 真人日志** | 21 天，0 代码（v6 的 P4，不改） | 21 天 | 前提是 S1 已完成 |
| **S6 · 条件触发** | 级联 / 交付形态 / 说话人识别 / 多用户（v6 的 P5，不改） | — | — |

**与他清单的对账**：他的三条验证没有被删掉一条，只是**归位**——V-A 离线进 S2、V-A 在线进 S3、V-B 进 S3（且改为 S1 的验收）、V-C 拆成 S2（缓存）与 S4（情绪）、旋钮进 S2。他在 S0 少了三行配置，在 S1 少了全部六项。

---

# 丁、我改变了的立场

1. **我承认"推迟窗口的代价可忽略"是越权。** 被 **DISCUSS-eng §6 第一段**说服（"这是一个需要 PM 拍板的产品指标变更，不是实现细节"）。§3.3 代价表改为"需 PM 签字的指标变更"。
2. **L0 从八项改为九项。** 被 **他的矛盾 1** 说服：处理层的**可外发进度信号**是协议/schema 字段，属历史形状，我漏了。
3. **我的"单一装配入口"形态有漏洞。** 被 **他的矛盾 2（独立短上下文）**逼出：装配入口唯一 ≠ 全局只有一条链。改为**唯一装配函数 + 显式上下文域 `(domain, promptVersion)`**，否则"会议摘要域"会被实现成第二处构造 prompt，命中我自己的红旗 #1；`promptVersion` 参数同时解决他矛盾 3 的灰度。
4. **`Utterance.turnId` 必须改名 `originTurnId` 且允许指向已关闭轮次；R5 必须条件化。** 这是在议题 4 自查中被打出来的（也受他 T4 一节启发）：不条件化，T4 落地时实现者会静默放宽 R5——纪律被静默降级比被违反更坏。
5. **我漏了 `handoff_allowed` 这道闸门。** 被 **他的 C7** 说服：`deep_ready ≠ handoff_allowed`；我的推迟窗口必须挂在这道闸门之后。
6. **"纠错只能追加"应从 S4 升格为贯穿原则。** 被 **他的 §3.0** 说服（我原来只把它写成 S4 的形态，没意识到它能统一 S4/打断/误答/双答竞态四件事）。
7. **B 的第一后果我说漏了。** 我原来只说"四层各自发明标点规则"；他 §2.2 的兜底规则（600ms / 8 字回退）让我看到**更硬的一条**：B 的投递结果**依赖时序**（同文本在不同负载下切成不同单元），而它又被当作"用户看到了什么"的事实源。这条比我原来的理由更硬，我收编进自己的论证。
8. **replace 的定位我改了。** 他读 `surface.js` 的读法成立（F3：replacement 进日志、`foldSurface` 重放、宿主强校验 `sourceEventSeqs`），我原来只写"与 R2 冲突"，漏了它作为**宿主背书的视图修复**这一层。改法：接受它进 R2 例外清单，但**必须与 `rebuild` / `crossTurnAppend` 共用同一套"显式 API + 令牌"形态**，且**禁止作为 `emittedText` 的来源**。
9. **R1 保证者从三条扩为四条**，加他的 **mtime 断言**（唯一能测出"共享面被写"的手段）。
10. **v6 §三 时间轴分档重写 + `emittedText` 落盘进 P0。** 前者被他的矛盾 1 与主张 21① 逼出；后者被 P4 的"21 天不可替代"逼出。

---

# 戊、僵持不下的

1. **权威归属（不可调和，且必须现在定）。**
   我：唯一权威 = 控制器 commit 流；ack 是派生证据，只用于重传。
   他：ack 水位 > 网关日志 > 宿主消息。
   我不退让的理由：**权威必须同时"随时可得 + 单调 + 单一"**。ack 在最需要它的时候（断线重连，他的 C6）**不存在**，且每设备不同（他的 C9）→ R5 变成设备相关，同一个问题在两台设备上得到两个不同的承接。
   **能让我动的是**：他能回答"断线重连的那 1–10 秒里，R5 的已输出全文取什么？"——回答不了，这个顺序就不成立。
   顺带指出：选对接地气的那一个，**C1 整节消失、C2 消失、C9 从语义问题降级为重传问题**。竞态的数量是权威选择的函数。

2. **A/B 的落点（分歧的实质是"沉默期放在哪里"）。**
   我：通路唯一（逐字流式）+ 控制器放行调度 + ≤400ms 只在 handoff 那一次；沉默期藏在"换人"这个本来就存在的停顿里。
   他：接收层按单元投递（协议单位 + 客户端缓冲/回放）；沉默期摊在每一句之后。
   **能让我动的是**：PM 明确选择"任意时刻被打断都必须停在完整句"（而不是"接管点不落半句"）。那时我接受 B，并按我 §3.2 的单点切分执行。
   我同时要求他先解决他自己的 owner 矛盾：**§2.1 替 PM 做了决定，§6 又交回 PM。**

3. **验证 vs 地基的优先序（可收敛为一个可判命题）。**
   我：形状先行、测量并行、通路验证最后。
   他：能推翻设计的实验先做。
   **分歧可收敛为一句可判的命题**：**V-B 的四条断言（不重叠 / 不出现未投递文本 / 不串味 / 还是同一条消息）是不是 L0 的产物？** 若他承认是，V-B 就不能排在 L0 之前，他的优先序自行崩塌；若他否认，请他指出这四条里哪一条**不需要** `emittedText` 与唯一出口就能断言——**他指出不来，因为那四条断言的左值全部是 L0 定义的量。**

---

# 净结果

1. **共识**：说话人只做自用 VAD、级联第一版不做、先把"用户实际看到的文本"做成单一权威并落盘、纠错只能追加、决策权（含出声权与停止权）归控制器。
2. **他被驳倒的**：ack 作为权威（断线时不存在、每设备不同）；按角色分叉 A/B（客户端两条渲染路径 + 长文层失去单元寻址）；兜底切分制造伪完整句并把时序不确定性提升为事实源；V-B 当"前置验证"（它就是 L0 的验收）；用假宿主测真宿主的失败模式。
3. **我被驳倒并已改的**：推迟窗口的代价不能自称"可忽略"（PM 的指标变更）；L0 漏了进度信号 schema；装配入口漏了上下文域；`Utterance.turnId` 必须是 `originTurnId`；R5 必须条件化；R1 要加 mtime 断言。
4. **必须现在定的三件**：权威归属（commit 流）、`noReply`/`SpeakerSignals`/`Decision` 形状、协议占位五字段——**都是形，都不能靠验证拿到。**
5. **最大的分歧只剩一个**：沉默期放在每一次换人（我）还是放在每一句之后（他）——**这个由 PM 拍板，不由我们两个拍板。**
