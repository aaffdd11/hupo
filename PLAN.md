# 项目计划：链路完整性收敛（PM 视角）

> 目标：把"个人 AI 助理"的链路**打通并验证完整**，而不是先建更多组件。
> 图：[`docs/diagrams.html`](docs/diagrams.html)（浏览器打开，含 4 张图）

---

## 一、项目定义（已按多用户 + 移动端 + 级联修订）

| 项 | 内容 |
|---|---|
| **要交付什么** | 一套 **iOS/Android App + 云端服务**，跑通"用户 ⇄ 接收/反馈层 ⇄ 级联处理层"的完整链路 |
| **交付形态** | Flutter（双端）+ 云端 DSH Harness + 自建网关 + 每用户隔离的 DSH 进程 |
| **完成标准** | 12 场景验收全绿 + 真机弱网/切后台连续 3 天不崩 + 多用户互不串数据 |
| **最大风险** | ① DSH 无多租户身份（已核实）→ 网关是硬前置 ② 移动端网络与生命周期全空白 |
| **首要原则** | **先闭环，再扩面**；但"多用户"从第一天就是目标，不做单用户过渡 |

**修订说明（相对上一版）**
- ❌ 原"不做多用户" → ✅ **多用户是目标形态**，网关/隔离/限额从阶段 1 就在范围内
- ❌ 原"客户端本地缓存" → ✅ **不做**（客户端打开就是聊天，只需会话列表元数据）
- ⚠️ 原"处理层按类别多 agent" → ✅ 改为 **灵活级联处理层**（横向 fan-out + 纵向 subagent 递归）

**当前状态诊断**：单轮主路径完整；多轮/并发/用户主动覆盖 6/12；
**云端与移动端整块缺失**（12 个新缺口，见第三节 D9–D20）。

---

## 二、整体流程图（文字版；图见 docs/diagrams.html）

### 图 1 系统分层（4 角色 + 控制器 + 12 组件）

```
应用层 APP：控制器 + ETA + 续话器 + 一致性守卫 + 降级兜底 + 衔接改写* + 任务队列*
     │
路由层：常驻分类器（只判定，不进对话）
     │
处理层：技术类 / 写作类 / 生活类 / 事务类 / 通用兜底（多 agent，按类别，不扇出）
     │
对话层：接收层 ⇄ 反馈层（共用声音，角色指令分开）
     │
记忆层：档案 / 品类树 / 经验包 / 指标 / 证据链
     │
   用户（只感知对话层）                       * = 本次新识别出的缺口
```

### 图 2 单轮时序

```
用户 ─① 说话─► 控制器 ─② 简报─► 接收层 ─④ 文本流─► 控制器
                  └③ 任务─► 处理层 ─⑤ 结论──────► 控制器
                                                   │
                              ⑥ 判决（ETA×时效性×禁区）
                                                   │
                    ┌──────────────┬───────────────┴────────────┐
                 等待           软打断                        硬打断
              （自然收尾）   （说完当前句即停）            （cancel + 换人）
                                                   │
                                    ⑦ 承载包 ─► 反馈层 ─⑧ 收尾─► 控制器
                                                   │
                                        ⑨ 放行 ─► 用户看到一条消息
                                                   │
                                        ⑩ 观察反应 → 记忆提交/回滚
```

### 图 3 跨轮与并发状态机

```
A 正常轮        用户 Q1 → 接收层开口 → 处理层深研 → 判决 → 反馈层说结论 → 落账
B 用户插话      Q1 深研中，用户又发 Q2
                ├ 路径B1：接收层立即应 Q2，任务队列登记，处理池起第 2 个会话
                └ 路径B2：Q1 结论到达 → 入队不抢占 → 与 Q2 结论合并成一条说
C 品类切换      技术类 → 写作类 → 再切回技术类
                恢复方式 = 摘要【尾部追加】，前缀不动 → 缓存保留、经验可直接引用
```

### 图 4 记忆层级与注入位置

```
记忆层级（每用户一份，控制器独占写权）
  热窗口（每品类独立，8–12 轮原文）
    └溢出压缩→ 温摘要（每品类一份）
         └再压缩→ 冷经验（进记忆库，有界，带衰减）

请求组装顺序（固定，不可重排）
  [system 前缀] persona + 角色指令 + 工具 + 品类方法论   ← 全静态
  [历史消息]    只追加，永不改写
  [尾部注入]    记忆简报 / 上下文摘要 / 本轮要点          ← 每轮变
  [用户消息]    原话（原文进，不能只给摘要）

实测：记忆插到最前 → 命中 18.5%、未命中 3383 tokens；追加尾部 → 命中 95.6%、未命中 184
```

---

## 三、工作分解（WBS）与依赖

### D0 · 地基（阻塞一切）

| ID | 任务 | 为什么是地基 | 依赖 |
|---|---|---|---|
| **D1** | **事实源 + 角色视图** | 三角色对同一轮认知互不相通；用户说"那个东西"时三个角色都在猜。**其余 5 个缺口全挂在它上面** | 无 |
| D2 | 衔接改写器 | 处理层的结论是给控制器的结构化数据，不是能直接说出口的话 | D1 |
| D3 | 任务队列 + 轮次归属 | 用户并发/插话时，未完成任务需要跨轮存活并标明何时发 | D1 |
| D4 | 双通道拦截落地 | 按 `source` 标记分路；系统注入的回应永不外发 | D1 |
| D5 | 轮际状态载体 | 上轮说了什么/接管点/一致性，跨轮保存 | D1 |
| D6 | 发言出口采集 | 账本改记"实际对用户说出的文本"，否则纠正的证据链是断的 | D4 |
| D7 | 用户打断语义 | 区分"控制器打断 agent"与"用户打断 agent"两套语义 | D3 |
| D8 | 上下文恢复协议 | 品类切回时用什么载体、追加到哪、旧窗口怎么回收 | D1 |

### D1 为什么是唯一的关键路径

```
D1 ──┬── D2 ──┐
     ├── D3 ──┼── 端到端最小闭环（可验收）
     ├── D4 ──┤
     ├── D5 ──┘
     └── D8
```
**除 D6/D7 外，所有任务都等 D1。** 所以排期上 D1 必须第一个动，且不能并行开工其他——否则会返工。

### D9–D20 · 云端与移动端（新增，多用户形态必需）

| ID | 任务 | 说明 | 依赖 |
|---|---|---|---|
| **D9** | **自建网关** | 鉴权（登录/令牌/设备管理）+ 用户路由 + 限流。**DSH 无多租户身份，这层不可省** | — |
| **D10** | **DSH 进程池 + 每用户隔离** | `userId → DSH_HOME=data/u_xxx → 独立进程 → 独立 socket`；空闲回收 | D9 |
| **D11** | **三段 ID 映射** | `客户端 convId ↔ DSH sessionId ↔ 角色实例 id`，持久化在服务端 | D9 |
| **D12** | **流式传输与重连** | WebSocket 长连接 + SSE 回退 + 心跳 + 断点续传 + 消息去重 | D9 |
| **D13** | **移动端生命周期** | 切后台服务器继续跑；回前台拉取错过内容；杀进程后恢复位置 | D12 |
| **D14** | **级联处理层** | 见下"级联设计"；横向 fan-out + 纵向 subagent 递归 | D1 |
| **D15** | **成本限额与熔断** | 每用户 token 配额、429 退避、上限触发降级 | D9 |
| **D16** | 会话列表与标题 | 复用 DSH 的 `session-title` + SQLite 查询 | D11 |
| **D17** | 轮级可观测性 | trace：接管路径 / 缓存命中率 / 各段耗时 / 成本 | D9 |
| **D18** | 客户端版本协商 | App 更新与服务器 API 不同步时的兼容策略 | D9 |
| **D19** | 推送通道 | APNs + FCM（处理层 10s+ 等待场景，锁屏后需通知） | D13 |
| **D20** | 凭证与合规 | 密钥只留云端、绝不下发客户端；账号删除入口（商店审核） | D9 |

### D14 级联处理层设计（含实测依据）

**形态**：不是"每个品类一个 agent"，而是**一个灵活的处理层**，按问题难度动态展开：

```
横向 fan-out（同层多节点并行）
  分类节点 ──┬── 专家节点 A（可行性）
             ├── 专家节点 B（风险与边界）
             └── 专家节点 C（历史经验对比）
                     │
纵向递归（DSH 原生 subagent，可继续往下）
  专家节点 A ──► 子任务 1 ──► 子子任务 …（受 maxDepth 限制）
                     │
              融合节点 ──► 一句话结论 + 置信度
```

**DSH 已核实支持的级联能力**：

| 能力 | 机制 |
|---|---|
| 子 agent 可再调子 agent | `delegationDepth` 单调递增 + `maxDepth` 上限（超限抛 `SubagentDepthError`） |
| 每级独立 persona | `SubagentStartRequest.persona`（provider 能力位 gated） |
| 每级独立工具集 | `toolFilter` |
| 每级结构化输出 | `outputSchema`（**属于单次激活，不写进持久化描述符**） |
| 子 agent 可续话 | `descriptor.mode: 'one-shot' \| 'continuable'`，continuable 支持**冷恢复**（持久化了恢复所需的组合信息） |
| 深度随冷恢复保持 | 恢复时以持久化 header 的 `delegationDepth` 作单调下限，不会越权加深 |

**⚠ 级联与缓存的硬约束（本轮实测得出）**

| 做法 | 实测命中 |
|---|---|
| 每个节点各自独立 persona（system 不同） | **跨节点 0%**（节点内部重复才 90%） |
| **共用同一 system 前缀，节点差异放在尾部标记** | **跨节点 90%** |

**结论**：级联节点要**共用一个大 system 前缀（规则 + 工具 + 该用户的记忆块），角色差异写在尾部**。
这是"级联省钱"与"节点各自专精"之间唯一的兼容解 —— 想两者都要，就必须把专精信息放到尾部的 user 消息里，而不是各自的 system。

**级联的三条纪律**
1. **先判难度再展开**：简单问题直接走单节点（实测：第 3 次同类问题 1.5s、0 专家），绝不无脑 fan-out
2. **深度有上限**：`maxDepth` 建议 2–3，且必须显式设置（否则递归失控）
3. **融合节点必须有**：多个节点的输出必须收敛成一句话，否则反馈层拿到的是一堆要点

| 里程碑 | 内容 | 验收方式 | 依赖 |
|---|---|---|---|
| **M0 · 云化地基** | D9+D10+D11+D12 | 两台设备登录不同账号，各自对话互不串；断网重连不丢消息 | — |
| **M1 · 事实源** | D1：控制器持有唯一事实源，能向 3 个角色投影不同视图 | 给定一轮对话，三个视图各自正确且能追溯到同一份事实 | — |
| **M2 · 单轮闭环** | D2+D4+D14 | 场景 1/2/3/4/11 全绿；级联按难度展开正确 | M0,M1 |
| **M3 · 多轮并发** | D3+D5+D13 | 场景 5/6/7/10/12 全绿；切后台回前台不丢内容 | M2 |
| **M4 · 链路完整** | D6+D7+D8 | 12 个场景全绿 | M3 |
| **M5 · 可运营** | D15–D20 | 限额生效、trace 可查、推送到达、凭证不外泄 | M0 |
| **M6 · 真机验收** | 全部 | 真机连续 3 天：崩溃/丢消息/重复话说 0 次 | M5 |

---

## 四、里程碑与验收

| 里程碑 | 内容 | 验收方式 | 依赖 |
|---|---|---|---|
| **M1 · 地基** | D1 完成：控制器持有唯一事实源，能向 3 个角色投影不同视图 | 单元测试：给定一轮对话，三个视图的字段各自正确、且都能追溯到同一份事实 | — |
| **M2 · 闭环** | D1+D2+D4 完成：单轮跑通，且系统注入的回应不外发 | 场景 1/2/3/4/11 全绿 | M1 |
| **M3 · 多轮** | D3+D5 完成：跨轮状态不丢、并发任务有归属 | 场景 5/6/7/10/12 全绿 | M2 |
| **M4 · 完整** | D6+D7+D8 完成：证据链闭合、两种打断语义分开、品类恢复可用 | 12 个场景全绿 | M3 |
| **M5 · 可用** | 真实使用 3 天，无链路级故障 | 崩溃/卡死/重复话说 0 次 | M4 |

---

## 五、12 场景验收清单（完整性的可测量定义）

| # | 场景 | 当前 | 验收标准 |
|---|---|---|---|
| 1 | 正常一轮（处理层后到） | ✅ | 接收层开口 → 反馈层承接，无重复 |
| 2 | 正常一轮（处理层先到） | ⚠️ | 接收层只说简短承接语，结论由反馈层完整说出 |
| 3 | 处理层超时 | ✅ | 降级话术出现，且标明"初步判断" |
| 4 | 处理层失败 | ✅ | 出现可执行的下一步，绝不沉默 |
| 5 | **用户插话打断接收层** | ❌ M4 | 半截话被正确收尾或标注，新消息优先被响应 |
| 6 | **原任务未完成时问新问题** | ❌ M3 | Q1 结论不丢，Q2 被立即响应，两者合并或按轮次发 |
| 7 | 接收层说完但结论 30s 后才到 | ⚠️ | 话题耗尽后走降级，不重复填充 |
| 8 | **反馈层说到一半用户插话** | ❌ M4 | 同场景 5，但接管状态要正确交接 |
| 9 | 连续同类问题（第 3 次） | ⚠️ | 走经验直出，且不再重新走完整流水线 |
| 10 | **品类切换后再切回** | ❌ M4 | 上下文被正确恢复，接收层第一句能引用历史 |
| 11 | 用户说"别想了"/取消 | ✅ | 处理层被 cancel，接收层收尾 |
| 12 | **两个问题并发** | ❌ M3 | 两个会话各自出结论，不串味、不合并错 |

**当前覆盖：4 ✅ / 3 ⚠️ / 5 ❌**

---

## 六、范围冻结（本阶段明确不做）

| 不做 | 理由 |
|---|---|
| **客户端本地缓存对话** | 客户端打开就是聊天；只需拉会话列表元数据（标题/时间/未读数），不做本地消息库与同步 |
| 成本优化 / 缓存策略深化 | 已明确暂不考虑成本（但"级联共用前缀"这条要遵守，它是架构纪律不是成本优化） |
| 品类专属 agent 升格 | 已由"灵活级联"替代，不再需要静态的品类 agent 池 |
| 上下文压缩管线 | 与"只追加"策略冲突 |
| 更复杂的续话策略 | 先验证基础链路 |
| 语音输入 / 图片理解 | 后续版本；框架预留（接收层输入多模态） |

## 六之二、客户端呈现结构（Flutter，双端一致）

**只有三个界面，刻意保持少** —— 客户端是"哑终端"，一切智能在云端：

| 界面 | 内容 | 关键点 |
|---|---|---|
| **会话列表** | 标题 + 最后一条 + 时间 + 未读 | 数据来自服务端 `session-title` + SQLite 查询；**不在本地存消息** |
| **对话页** | 消息流（流式打字）+ 输入框 | 承载"接收层先开口 → 反馈层承接"两段文本，**用户看到的是一条连续消息** |
| **设置** | 账号 / 账号删除 / 版本 | 账号删除是应用商店硬要求 |

**对话页必须处理的六件事**（这些是工程重点，不是 UI 美化）：

1. **流式渲染**：SSE/WebSocket 增量到达，逐字显示；重连后按消息 ID 去重与续接
2. **两段文本的自然衔接**：接收层的话与反馈层的话要拼成一条，中间不能露出"换人了"的破绽
3. **长等待的在场感**：等待中显示"正在查…"这类状态（内容由接收层的续话提供，不是本地假动画）
4. **弱网降级**：发送失败要可重试，且重试不能产生重复消息（客户端生成幂等 messageId）
5. **切后台回来**：拉取错过的内容，补在正确位置
6. **中断输入**：用户想在 AI 说话时插话，输入框必须始终可用，不能锁死

---

## 七、风险登记

| 风险 | 影响 | 应对 |
|---|---|---|
| **D1 设计过度**（做成 12 组件的巨型控制器） | 工期失控 | D1 只做"事实源 + 3 个视图投影"，不许顺手加机制 |
| 用户感知"两句话很碎" | 架构价值归零 | M2 完成后立刻做 A/B：单 agent 被 steer vs 双 agent 承接，真人判断 |
| 场景在实现中继续膨胀 | 永远完不成 | 场景清单冻结，新增场景必须走变更流程 |
| 文档与实现脱节（已发生过一次） | 决策丢失 | **决策只认落盘文档**；每次对话产生的决策当天落文档 |

---

## 八、下一步（本周）

1. **确认本计划的场景清单与范围冻结**（第七节两条风险需要你表态）
2. 动 D1：设计"事实源 + 角色视图"的数据结构（这一步做完，其余 5 个缺口才有落点）
3. D1 完成后立即做 M2，用 A/B 验证"承接质量"这个核心假设
