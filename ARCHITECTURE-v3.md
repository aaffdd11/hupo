# 三层架构正式定义与控制器设计（v3 · 定稿规划）

> 你已正式定名：**接收层 / 反馈层 / 处理层**。
> 本文做三件事：① 把三层与它们的伴生进程正式定义清楚；② 深度论证"监控什么、怎么估、怎么接"；
> ③ 给出可落地的状态机与协议。仍然只规划，不写假实现。

---

## 第一部分 · 正式定义

### 1.1 三层（按你的定名）

| 层 | 正式名 | 只做一件事 | 绝对不做的事 |
|---|---|---|---|
| 第一层 | **接收层 Receiving Layer** | **接收用户信息，并直接回应** | 不判断、不分发、不管处理层死活 |
| 第二层 | **反馈层 Feedback Layer** | **把处理层的结论说给用户** | 不思考、不猜测、不自己发挥 |
| 第三层 | **处理层 Processing Layer** | **真正思考并产出结论** | 不直接对用户说话 |

**你指出的物理约束是整个设计的钥匙：接收层没有办法分发。**

这句话的推论比它看起来更重要：

> **因为接收层不能分发，所以"分发"必须发生在接收层之外。**
> 于是必然存在一个**在它之外、看着它**的进程 —— 这就是控制器。
> 同理，处理层的进度也不能自报（它不面向用户），所以**也必须由外部进程去观察**。

**结论：不是"我们想加一个监控"，而是三层结构在物理上必然长出一个监控进程。**
控制器不是可选组件，它是这个架构能成立的前提。

### 1.2 四个角色 + 一个进程（正式定义）

| 编号 | 角色 | 类型 | 职责 | 输入 | 输出 |
|---|---|---|---|---|---|
| **A** | 接收层 | Agent | 接收 + 直接回应，维持在场感 | 用户原话 + **发言简报** | 说给用户的实时文本流 |
| **B** | 处理层 | Agent（独立会话） | 深度思考，产出结论 | 用户原话 + 上下文快照 + 品类经验 | 结构化结论（不对用户说话） |
| **C** | 反馈层 | Agent | 承接并说出结论 | B 的结论 + **A 的已输出全文** + 一致性判定 | 说给用户的收尾文本流 |
| **D** | **控制器 Controller** | **进程**（非 Agent） | 监控 A 与 B、估算进度、决定谁在什么时候说 | A 的流事件 + B 的状态事件 + 用户输入 | 发言简报 / 续话通知 / 打断指令 / 接管指令 |

控制器是**进程而不是 Agent**，这点必须守住：
- Agent 会"想说话"，控制器不能有表达欲，否则它会变成第四个声音。
- 控制器的所有输出都是**结构化的指令**（简报、通知、指令），语言由 A 和 C 生成。

### 1.3 两层闭环

```
┌─ 快环（在场感，必须立刻成立）──────────────────────────┐
│  用户说话 ──► 控制器捕获 ──► 接收层 A ──► 用户看到回应   │
└────────────────────────────────────────────────────────┘

┌─ 慢环（答案，允许慢但必须到）──────────────────────────┐
│  控制器捕获 ──► 处理层 B（自主工作）──► 结论             │
│                     ▲                                  │
│              控制器持续观察：在推理？在调工具？停滞了？   │
└────────────────────────────────────────────────────────┘

       两环交汇点 = 控制器（唯一知道两边状态的地方）
```

**为什么必须是两个环而不是一条流水线**：因为快环的节奏由用户决定（用户说一句就要有回应），
慢环的节奏由任务决定（研究一个复杂问题就是要 8 秒）。把它们串成一条线，慢环会拖死快环。

---

## 第二部分 · 深度论证：控制器到底监控什么

### 2.1 监控接收层 —— 监控的是"因果"，不是"时间"

你原话说"监控第一层接到的东西"。这里有一个容易被忽略的要点：

> **控制器不需要监控"接收层收到了什么"（它自己就是分发者，它知道）。**
> **控制器需要监控的是：接收层"是否已经把这件事回应了"，以及"它还要说多久"。**

所以对接收层的监控，本质上是在观察三件事：

| 观察项 | 来源 | 用途 |
|---|---|---|
| 是否已经开口 | 第一个 `chunk` 到达 | 判断要不要催它（没开口=还没接住，用户在等） |
| 说了多少、多快 | `chunk` 序列的字数与速率 | 估算剩余量 |
| 是否在收尾 | 最后一句是否命中收尾模式（总结/提问/安抚） | 判断"现在打断会不会切在句子中间" |
| 是否停了 | `assistant/message` 终态 | 判断缺口，决定要不要续话 |

**关键判断：漏接（gap）的定义不是"时间过去了"，而是"接收层停下来了，而处理层还没好"。**
这两个条件同时成立才叫缺口。时间本身不构成问题——用户在读、在思考的时候，安静是正确的。

### 2.2 监控处理层 —— 监控的是"状态转移 + 停滞"，不是"百分比"

处理层不是"进度条"型的任务，你无法知道它想完了 60%。硬报百分比是伪精确。
正确的是**状态机 + 停滞检测**：

```
进程状态： 未开始 ──► 已受理 ──► 推理中 ──┬──► 已就绪
                                    │
                                    ├──► 停滞（无新活动超过阈值）
                                    └──► 失败/超时
```

**为什么"停滞检测"比"百分比"重要**：因为停滞会**改变策略**，而百分比不会。
- 正常推进 → 继续等，即便慢一点
- 停滞 → 立刻启动续话，并考虑降级（先给个初步判断）
- 停滞且已超时 → 走兜底通道

状态转移从哪来？处理层作为一个 Agent，天然会暴露可观察的活动：

| 处理层内部阶段 | 可观察信号 | 历史时长统计的用途 |
|---|---|---|
| 分类 | 第一步开始 | 已实测约 0.5–1.4s |
| 召回经验 | 一次工具/检索调用 | 接近 0ms |
| 专家深研 | 工具调用 + 子任务 | 已实测 2.5–5.7s |
| 提炼 | 最后一步 | 已实测 1.1–1.6s（换快模型后） |

> 用上一轮原型实测出来的数值做先验，比任何猜测都靠谱。这也是为什么 P0 应该先把
> "每个阶段的真实耗时分布"采下来 —— 没有这些先验，ETA 就是瞎猜。

### 2.3 怎么估算 —— 三档判断，而不是秒数

你问"怎么样预估是什么时间"。我的结论是：**不要输出秒数，输出相对判断。**

理由：
1. 大模型的耗时不服从稳定分布（同一个问题两次可能差 3 倍），秒数必然不准；
2. 秒数一旦报给用户，就成了承诺，不准就要解释；
3. 真正需要的是"决定做什么"，而决定只需要粗粒度。

所以 ETA 输出三档：

| 档位 | 判定条件（接收层） | 对应动作 |
|---|---|---|
| `快说完` | 已说字数接近该类回复的历史均值，或最后一句命中收尾模式 | 等它自然收尾 |
| `还要一会` | 仍在稳定输出，距均值尚有距离 | 软打断（把它引向收尾） |
| `还很久` | 刚开始 / 在工具调用中 / 速率骤降 | 硬打断，反馈层接管 |

处理层同样三档：`即将就绪` / `正常推进` / `停滞或超时`。

**ETA 的实际计算（接收层）**：
```
剩余字数 ≈ 该类问题的历史回复长度中位数 − 已输出字数
剩余时间 ≈ 剩余字数 ÷ 最近 300ms 的实测输出速率
```
**ETA 的实际计算（处理层）**：
```
若当前状态 S 的历史 p50 为 T(S)，已在该状态停留 t：
  t < T(S)         → 正常推进
  T(S) ≤ t < 2·T(S) → 偏慢，准备续话素材
  t ≥ 2·T(S)        → 判定停滞，启动降级评估
```

### 2.4 处理层"想得久"时，接收层该说什么（续话素材的硬约束）

这是整个设计最容易做砸的地方。续话绝不能是"稍等""我还在想"这类填充语。

**接收层的三条硬约束**（必须写进它的 persona，由人格保证，而不是靠事后过滤）：

1. **只说自己正在做什么，不说结论。** 结论是处理层的产出，接收层抢答会制造"打脸"风险。
2. **不承诺时间。** 不说"马上就好""再等 3 秒"。
3. **不重复。** 每次续话必须提供新信息（新角度/新确认/新问题）。

**控制器发给接收层的"续话通知"是结构化的，不是一句话**：

```
{
  类型: '续话通知',
  剩余安全话题: ['复述确认', '说明当前在做什么', '提一个推进性问题'],
  已用过: ['复述确认'],
  禁止: ['给出结论', '承诺时间'],
  本轮要点: '用户的问题涉及两个可能的品类，正在确认是哪一个'
}
```

注意 `本轮要点` —— 接收层说的是**控制器提供的真实信息**，不是编的。
这样"我还在思考"就变成了"我在确认它属于哪一类问题"，用户能感觉到是真在干活。

### 2.5 为什么不用"次数上限"

你说得对，固定的"两次"是死板的。**正确的停止条件是"话题耗尽"，而不是"次数用尽"。**

安全话题是个有限集合（复述确认 / 说明在做什么 / 提一个推进性问题），每用掉一个就少一个，
用过的不能再用（否则就是重复，违反硬约束 2）。所以：

- 话题还有 → 可以续话
- 话题耗尽 → **不是"硬撑到期"，而是自然进入降级**：由接收层如实说明"这个我需要更久，先给你一个初步方向"

**这个设计的妙处**：降级不是一个外挂的异常分支，而是续话策略的自然终点。
不需要任何计数器，也不会出现"第十二次说稍等"的荒谬情况。

**但需要一个安全阀**：如果话题耗尽得太快（比如第一轮就用完三个），说明处理层遇到了硬骨头，
此时应立即触发**降级交付**：让处理层交出当前最好的半成品，而不是继续耗。

---

## 第三部分 · 控制器状态机

### 3.1 控制器持有的全部状态

```
focus          : 谁在说话        receiving | feedback | none
recv.state     : 接收层状态      idle | opening | speaking | closing | stopped
recv.chars     : 已输出字数
recv.topics    : 已用过的续话话题
proc.state     : 处理层状态      idle | accepted | reasoning | ready | failed
proc.since     : 当前状态已持续多久
lastActivityAt : 最近一次收到任一环的有效活动
pendingUserMsg : 处理中用户又发来的新消息（排队）
delivered      : 本轮结论是否已经说过
```

### 3.2 状态转移表（核心逻辑）

| 触发 | 条件 | 动作 |
|---|---|---|
| 用户说话 | 处理层空闲 | 启动快环（接收层开口）+ 启动慢环（处理层受理） |
| 用户说话 | 处理层忙 | 新消息入队；快环照常回应；告知处理层有新输入 |
| 接收层停顿 | 处理层未就绪 且 仍有安全话题 | 发续话通知（附未用话题 + 本轮要点） |
| 接收层停顿 | 处理层未就绪 且 话题耗尽 | 触发降级交付（处理层交半成品） |
| 接收层停顿 | 处理层已就绪 | 进入接管判定 |
| 处理层就绪 | 接收层仍在输出 | 接管判定：等 / 软打断 / 硬打断 |
| 处理层就绪 | 接收层已停 | 反馈层直接接管（无需打断） |
| 处理层失败/超时 | 任意 | 走兜底话术，由反馈层或接收层收尾 |
| 用户新消息 | 任意 | 优先级高于一切未完成工作（见 3.4） |

### 3.3 接管判定（回答"等还是打断"）

判决输入只有两个：**接收层剩余量**（ETA 三档）× **结论是否有时效性** × **是否允许打断**（禁区检查）。

| 接收层 ETA | 允许打断？ | 结论有时效性？ | 判决 |
|---|---|---|---|
| 快说完 | — | — | **等**：让接收层自然收尾，反馈层接着说 |
| 还要一会 | 是 | 是 | **软打断**：通知接收层"结论已到，立刻收尾"，随后反馈层接 |
| 还要一会 | 是 | 否 | **等**：自然交接最顺，没人喜欢被打断 |
| 还要一会 | 否（禁区） | 任意 | **等**：等它离开禁区（通常 <1s） |
| 还很久 | 是 | 任意 | **硬打断**：`cancel` 接收层，反馈层承接接管 |
| 还很久 | 否（在工具调用中） | 任意 | **等一个边界**，再做一次判定 |

**打断禁区（查表，不现算）**：
1. 正在输出代码块 / 数字 / 列表项中间
2. 正在调用工具且结果尚未说完
3. 用户刚发新消息 1.5s 内（新消息优先级更高）
4. 接收层当前句已进入"一句话的最后一个分句"（等 0.3s 就能自然结束）

**软打断 vs 硬打断的本质区别**：
- 软打断 = **改指令，不改流程**。接收层还是一轮，只是被通知"立刻收尾"。无半截话。
- 硬打断 = **`cancel` + 换人**。已输出 token 撤不回来，会产生半截话，所以反馈层**必须承接**。

### 3.4 接管协议（保证"一个声音"）

反馈层出手时必须收到完整的承接包：

```
{
  用户原话,
  接收层已输出全文,          ← 关键：没有它就会重复
  接管点: '截断在第 N 字',
  一致性: agree | extend | conflict,
  处理层结论: { 一句话, 要点≤3, 下一步动作, 置信度 },
  说话风格提示: 最近 3 句的语感（避免"换了个人"）
}
```

**硬性规则**：
- 一致性为 `conflict` 时必须显式纠偏（"我刚才那个说法不准确…"），不许装作没说过。
- 置信度低时必须标记为初步判断，不许用确定语气。
- 接管的第一句必须**承接**而非另起（"我刚说到 X，结论是 Y"）。

### 3.5 冲突与重复的处理

| 情况 | 处理 |
|---|---|
| 接收层的猜测 与 结论一致 | 直接说结论，视为自然延伸 |
| 接收层是补充关系 | 说一下"补充一点…"，顺滑 |
| 接收层与结论矛盾 | 显式纠偏（代价是承认自己说错了，但比让用户按错信息行动好得多） |
| 接收层已经说过的内容，结论里又出现 | 反馈层跳过重复部分，只说要新增的 |

---

## 第四部分 · 边界与异常（容易被漏掉的部分）

| 场景 | 设计 |
|---|---|
| 用户在处理中又发新消息 | 新消息优先级最高；接收层立刻回应新消息；处理层把新输入并入当前任务（或拆成两个任务）；反馈层最后统一收口 |
| 用户连续问 3 个问题 | 接收层逐条确认；处理层可并发处理；反馈层按顺序合并回答 |
| 处理层中途"想岔了" | 控制器据一致性守卫发现结论与用户原话无关 → 触发重算，同时让接收层先给一个有用的初步方向 |
| 多个结论就绪（拆分的任务） | 反馈层合并成一条，不连发多条 |
| 用户明确说"别想了" | 硬打断处理层（`cancel`），接收层收尾 |
| 处理层比接收层还快 | 常见（实测：处理层 ~0.9s，接收层首字 0.8–1.5s）→ 走"等接收层自然说完再交"或软打断 |
| 处理层失败 | 兜底：反馈层如实说明 + 给可执行的下一步，绝不留白 |

**一个反直觉但重要的实测结论**（来自 v0.1 原型的真实测量）：
> **处理层经常比接收层的第一个字还快。**
> 因为接收层要过一次大模型才有首字（0.8–1.5s），而处理层在简单问题上 0.9s 就能出结论。
> 这意味着大多数轮次的实际形态是**"接收层说了一句话，反馈层紧接着接管"**，
> 而不是"接收层说很久，反馈层来救场"。
> 所以**"等还是打断"的判定必须以实测 ETA 为准，不能预设"接收层一定说很久"**。

---

## 第五部分 · 落地映射（已核实的宿主能力）

| 需要 | 宿主接口 | 备注 |
|---|---|---|
| 建三个角色 agent | `ctx.agents.create({ sessionId, agentOptions, setup })` | `setup` 里可按角色挂 persona / 工具 |
| 角色人格隔离 | agent preset + `dsh-persona`（scope 级） | 三个角色三套 persona，互不污染 |
| 读接收层输出增量 | `assistant/chunk`（token 级） | 驱动 ETA 与 UI |
| 判断"是否收尾/是否被打断" | `assistant/message`（`interrupted: true`） | |
| 观察处理层状态 | 处理层的逐步活动 + 工具调用事件 | 用于停滞检测 |
| 硬打断 | `agent.cancel(cause, { keepInbox })` | 已输出 token 不可撤 |
| 续话通知 / 发言简报 | `agent.steer()`（在跑）/ `followup()`（已停） | 载体用 `inject` 更合适：不唤醒、只喂上下文 |
| 唯一对话真源 | 控制器自己的记录（不依赖任何 agent 日志） | 这是"应用层拥有记忆"的落点 |
| 经验账本 / 品类树 / 指标 | 复用 v0.1 已验证实现 | p50 12.0s→6.5s、降级率归零的调参结论继续有效 |

**关于"发言简报"**：控制器不应该把接收层要说的话写死（那就成了控制器在说话），
而应该给**意图与约束**（说什么类型的话、禁说什么、本轮要点），语言由 agent 自己组织。
这样既可控，又保留 agent 的表达能力。

---

## 第六部分 · 待确认

1. **反馈层与接收层是否共用同一套 persona**：同一个"声音"，还是允许反馈层更权威？（决定观感）
2. **硬打断的默认策略**：我倾向"仅当结论有时效性且接收层还很久时启用"，其余一律等。是否同意？
3. **处理层是否允许中途向接收层"要更多信息"**：例如处理层发现用户没说清，它可以请求接收层去问用户。
   这会让接收层变成"双向通道"（既是输入也是输出），能力更强但复杂度上一个台阶。做不做？

---

## 附：本轮规划相对 v2 的变化

| 变化 | 原因 |
|---|---|
| 层名正式定为 接收 / 反馈 / 处理 | 你的定名 |
| 明确"四角色 + 一进程" | 反馈层必须是独立 Agent，才能承接接收层已说的话 |
| 控制器从"可选"上升为"结构必然" | 因为接收层无分发能力 |
| 废除续话次数上限，改为"话题耗尽即降级" | 你的意见；且逻辑上更自洽 |
| ETA 从"三档时间"细化为"状态机 + 停滞检测" | 百分比与秒数都是伪精确 |
| 新增"接收层三条硬约束" | 防止续话变成废话、防止抢答造成打脸 |
| 新增第 4 部分边界表 | 真实对话一定会遇到 |
