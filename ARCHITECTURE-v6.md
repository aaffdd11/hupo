# 个人 AI 助理 · 总架构定稿（v6）

> **本文是唯一权威版本**,取代 v5 及之前所有版本。
> 配套专文(本文引用,不重复):
> - `ARCHITECTURE-seamless.md` —— 快答→深答的无缝衔接
> - `ARCHITECTURE-speaker.md` —— 说话人识别与主被动对话
> - `ARCHITECTURE-delivery.md` —— 四档交付形态(H5/页面/应用)与记忆隔离
> - `REVIEW-pm.md` / `REVIEW-eng.md` / `REVIEW-appdev.md` —— 三份独立评审
> - `docs/diagrams.html` —— 5 张架构图

---

## 〇、一句话定义

> **一个逻辑型个人助理**:默认只对主人回应,先给快速回答、再给深思回答,
> 两块拼成一条无缝的话;需要时交付页面或小应用;记性越来越好但不顺从、不越界。

---

## 一、四个角色 + 一个进程(骨架)

| 角色 | 定位(已纠偏) | 绝不做的 |
|---|---|---|
| **接收层** | **快速回答**:回应 + 方向 + 能立刻确定的部分 | 不给未经研究的结论 |
| **处理层** | 深度思考,产出结论(可级联) | **永不直接对用户说话** |
| **反馈层** | **深思回答**:研究之后的结论(有最终发言权) | 不重复已说内容、不另起话题 |
| **控制器** | 决策进程:谁说话/何时抢话/记什么/要不要答 | **不能有表达欲** |

> ⚠ **本轮最重要的纠偏**:反馈层不是"第二个接话的角色",**它就是"深思熟虑的那块回答"**。
> 一轮对话用户应收到两块内容 —— 快速回答 + 深思回答 —— 目标是让它们像一个人一次说完。

---

## 二、分层全景

```
客户端层（Flutter，iOS + Android）
  ├ 对话页（两块同一条消息渲染）  ├ 会话列表  ├ 设置
  ├ VAD + 音频采集（被动模式）    ├ H5 容器（T3/T4 交付物）
  └ 状态提示（不在场感 → 用状态代替语言）

应用层（控制器所在，不产生语言）
  ├ 控制器：模式状态机 / 谁说话 / 打断判决 / 交接包 / 上限熔断
  ├ 说话人判定：主人 vs guest-N ｜ 被点名检测 ｜ 主人是否在等回答
  ├ 情绪与前提检测：情绪强度 / 是否发泄 / 假前提（实测 6/6 检出）
  ├ 交付形态判定：T1 对话 / T2 卡片 / T3 页面 / T4 小应用
  ├ 上下文装配：只追加 + 前缀不变量断言
  └ 记忆检索：二级（粗查给接收层 / 精查给处理层）

路由层
  └ 常驻分类器：大类/中类/小类 + 置信度 + 难度 + 交付形态

处理层（可级联，节点 ≤ 同 depth 共享前缀）
  └ 分类 → 召回 → 专家 fan-out → 融合（maxDepth=2）

对话层
  └ 接收层 ⇄ 反馈层（共用 voice 段，role 段分开）

记忆层（每用户一份，控制器独占写权）
  └ 档案 / 品类树 / 经验包 / 指标 / 证据链 / 会话语料（隔离区）

云端支撑
  └ 网关（鉴权/路由/限流）→ DSH 进程池（每用户一进程 + 共享只读 profile）
```

---

## 三、一轮对话的完整链路(含两块回答)

```
t=0      用户/主人说话（或被动模式下多人说话）
t≈10ms   控制器：说话人判定 → 模式判定 → 粗查记忆
         └ 并行发起：情绪与前提检测 ｜ 分类器 ｜ 处理层
t≈0.7s   情绪信号回来 → 决定"怎么答"（不阻塞"开始答"）
t≈0.9s   处理层出结论（简单问题走经验直出）
t≈1.2s   接收层开口 = 【快速回答】("这个我熟，先看连接池，我去确认")
t≈1.3s   接管判决（ETA × 时效性 × 打断禁区）→ 软打断/等
t≈1.5s   【深思回答】由反馈层生成，第一句天然承接
         └ 上下文必含：接收层已输出全文 + 语气样本 + 一致性判定
t≈3s     同一条消息内追加完成 → 落账 → 观察用户反应
```

**关键实测(支撑设计)**:
- 快层 p50 0.8–1.0s vs 深层 p50 6.5–7.9s(9 倍)
- 处理层常比接收层首字更快(0.9s vs 1.2s)→ 多数轮次是"快答一句,深答紧接着接管"
- 记忆/摘要**只能尾部追加**:插最前 → 命中 18.5%/未命中 3383 tokens;追加尾部 → 95.6%/184(差 18 倍)
- 级联节点**各自 system → 跨节点命中 0%**;共用前缀 + 尾部标记 → **90%**

---

## 四、五条不可协商的纪律

| # | 纪律 | 违反的后果 |
|---|---|---|
| **R1** | **记忆隔离三层**:数据层(`search(userId,…)` 无无参重载)、进程层(每用户一 DSH_HOME)、提示词层(**用户标识放最前**,断言校验) | 静默的数据事故,不报错 |
| **R2** | **只追加,不改前缀**:唯一例外是整块重建,且必须在用户思考间隙做 | 成本差 18 倍且无感知 |
| **R3** | **情绪只影响"怎么说",绝不影响"说什么"**;安抚绝不等同顺从 | 顺着用户说 → 问题解决不了 |
| **R4** | **默认只对主人回应**;"被点名"是硬例外,必须答 | 喊了不理 → 判定为坏掉 |
| **R5** | **反馈层上下文永远含接收层已输出全文** | "不重复""接得上"都无法实现 |

---

## 五、四档交付形态

| 档 | 形态 | 载体 | 何时用 |
|---|---|---|---|
| T1 | 几句话 | 对话流 | 问答、判断、建议(默认档) |
| T2 | 结构化展示 | SDUI 卡片(Flutter 原生) | 表格/指标/图表 |
| T3 | 动态页面 | H5 容器(WebView + 白名单) | 多维筛选、下钻 |
| T4 | 完整小应用 | H5 容器 + 持久化 + **异步任务** | 工具类 |

判定标准不是"内容多少",而是**"用户是否需要做一次操作才能得到他要的"**。
默认 T1;T4 引入任务状态机 + 时间承诺(必须来自实测分布,误差 >50% 主动更正)。
合规依据:Apple 审核指南 **4.7** + [Mini Apps Partner Program](https://developer.apple.com/news/?id=06212016b#2)。

---

## 六、主/被动对话与说话人识别

| 模式 | 谁在说 | 助手行为 |
|---|---|---|
| **主动对话** | 主人对助手说 | 完整闭环(快答 + 深答) |
| **被动聆听** | 多人互相聊 | **不出声**,只记 + 结构化,状态提示代替语言 |
| **被动但被点名** | 任何人直接对助手说 | **必须回应**(简短,不喧宾夺主) |

**三个必须判定的问题**:
1. **这句话是不是主人在说?**(声纹,输出置信度;低置信→保守不回应但记录)
2. **主人是不是在等助手回答?**(对他人说 / 对助手说 / 自言自语)
3. **有没有人被点名?**(称呼词 + 指向性 + 情绪强度 + 停顿等待)

粒度只到**"主人 / guest-N"** —— 识别具体身份对"该不该回应"无额外价值,却把合规负担放大。
**法律红线**:录制他人语音须告知并取得同意(提示音/提示灯/可停止/可删除)。
被动语料**不进主人记忆**、**不逐轮注入**、**强保留期(建议 7–30 天)**。

---

## 七、用户情绪与前提(逻辑型人格)

| 规则 | 内容 |
|---|---|
| **默认逻辑型** | 不顺着说;发现错误前提必须指出 |
| **情绪极差才安抚** | 强度 ≥ 阈值(建议 0.7)先接情绪再讲逻辑;低于阈值直接讲逻辑 |
| **安抚 ≠ 顺从** | 安抚情绪,不默认事实 |
| **不贴标签** | 绝不说"我注意到你很焦虑";情绪信号**只用不用嘴**(缩短首句、降密度、先给小结论) |

**实测**:假前提检测 **6/6 全中**(技术误解/因果倒置/错误前提/幸存者偏差/无效对比/归因错误),
每次 33–35 token、关思考、395–1077ms;情绪检测 p50 758ms、33–35 token。
→ 两者**必须并行发起**,串行会把首字延迟推到 1.6–2.3s。

---

## 八、部署形态(工程总监实测推荐)

```
Flutter App(iOS/Android)
   │  HTTPS + WebSocket/SSE
   ▼
自建网关(鉴权 / 三段 ID 映射 / 限流 / trace)
   │  按 userId 路由
   ▼
DSH 进程池：每用户一进程 + 共享只读 profile + systemd 模板 + 空闲回收
   │  控制器作为【进程内 cordis 插件】（关键，见下）
   ▼
数据层：会话(session.jsonl + SQLite) / 记忆库(每用户) / 会话语料(隔离) / 配额
```

**为什么控制器必须进程内**(已核实):
- 远程注入的 `source.kind` **被硬编码为 `"user"`** → 进程外无法区分"系统注入"与"用户输入",
  R4 与"系统注入永不外发"无法实现,且控制器的指令会**显示成用户自己说的话**
- 进程外读 token 代价极高:50 消息页 = **52k 事件 / 11MB**(99% 是 chunk)
- 进程外拿不到 `inject`、`cancel` 默认清空 inbox

**容量(本机实测)**:4 vCPU / 7.2GB(可用 3.1GB)/ 磁盘已用 80% → 单机同时在线 **8–12 人**;
全新 `DSH_HOME` 冷启动 25s / 203MB / 1.9MB 磁盘 → 每用户一进程今天就能落地。
**>12 人加机器,不改架构。**

---

## 九、工程红线(实测得出,必须写成断言)

| 红线 | 手段 |
|---|---|
| 前缀只追加 | 装配入口只暴露 `append`/`truncateOldest`;每次装配断言前 n 块字节不变;20 轮字节稳定性单测 |
| 前缀指纹同 depth 相同 | 不同则**抛错拒启**(不是告警) |
| 记忆不跨用户 | 前缀首段必须是本用户标识段;断言 prompt 不含他人 uid |
| 级联不失控 | `maxDepth=2` 显式设置;子 agent 并发上限显式设置(框架默认无上限) |
| 重试不雪崩 | LLM 默认 `maxRetries=5` 且含 `EMPTY_RESPONSE` → 必须改成带全局预算的退避 |
| 采样而非扇出 | 控制器先判定再派发,不"跑几个再挑" |
| 每轮 trace | 落 `prompt_tokens / cached_tokens / 接管路径 / 各段耗时 / 成本` |

---

## 十、落地分期(合并三位评审的收敛项)

| 阶段 | 内容 | 工期 | 验收 |
|---|---|---|---|
| **P0** | 修工具链(✅ 已完成)、单 agent + 状态条 + trace + 首句规则 + 假前提检测接入 | 3 天 | 能跑、能看 trace |
| **P1** | 记忆最小版(经验条目 + 尾部追加简报,复用 v1) | 4 天 | 首句能引用历史 |
| **P2** | 两块回答的**无缝衔接**(接收层 steer → 反馈层承接)+ voice/role 分离 | 5 天 | 六种失败模式场景通过 |
| **P3** | "它记住了什么"页面 + 纠正闭环 + 成本上限 | 3 天 | 纠正后 20 轮不复现 |
| **P4** | **创始人自己每天用,记 21 天日志** | **21 天,0 代码** | ≥30% 会话首句引用历史;催促语 ≤5% |
| **P5** | **条件触发**:P4 判据达成才启动 → 云化/多用户/级联/交付形态/说话人识别 | — | — |

**P0 必须先做的两个测量**(都是 20 分钟级,却决定 60% 工程量):
1. **"深层 > 3 秒"的轮次占比** —— <20% 则接收层价值太小;>40% 则值得做
2. **两块回答的盲评** —— 用 v1 产出手工渲染两版,问"从哪句开始换人了?"

---

## 十一、待你拍板(7 项)

| # | 决策点 | 我的建议 |
|---|---|---|
| 1 | 说话人粒度 | 只分"主人 / guest-N" |
| 2 | 被动模式是否留音频 | 只留文本转写,留 7 天,音频不留 |
| 3 | 被动模式怎么结束 | 自动(静默一段时间)+ 主人手动,两者都要 |
| 4 | 非主人被点名的回应程度 | 简短回应 + 引导给主人 |
| 5 | 硬打断默认 | 默认关闭,仅"结论有时效性 且 接收层还很久"时启用 |
| 6 | T4 产物能否长期保存 | 只做临时交付物(否则等于自建应用商店) |
| 7 | 级联是否第一版就做 | 不做(P4 条件触发);3 节点 fan-out + 融合 ≈ 8s,比单节点 6.5s 更慢 |

---

## 附:本版相对 v5 的变化

| 变化 | 原因 |
|---|---|
| **反馈层定位纠偏** | 它是"深思回答",不是"第二个接话的" —— 创始人纠正 |
| 新增无缝衔接专文 | 快答→深答怎么像一个人说的 |
| 新增说话人识别与主被动 | 主人识别、被动聆听、被点名 |
| 新增交付形态专文 | 四档形态 + H5 容器 + 合规 |
| 新增情绪与前提纪律 | 逻辑型人格 + 假前提检测(实测 6/6) |
| 新增记忆隔离三层约束 | 创始人要求严格隔离,缓存也不共享 |
| 控制器确定放进程内 | `source.kind` 硬编码 + 进程外代价实测 |
| 多用户确定为"每用户一进程" | 工程总监实测容量与冷启动数据 |
| 落地分期改为 P0–P5 | 合并三位评审:P4 用 21 天真人验证,不写代码 |
| 修正:级联共用前缀口径 | 改为"同 depth 逐字节相同"(父子之间永远 0%) |

---

## 附二:实现现状(实测数字,随代码更新)

以下是**已经跑起来并在公网验证过**的部分。数字来自 `services/core/watch.mjs`
（`BASE=https://hupo.stalkerai.cn`，走真实 WS + 真实模型，不是本地 mock）。

**处理层 = 真 agent（不是固定 LLM 调用）。** 判据是创始人给的：

> "果然,跟我聊天的无法接入搜索功能,说明没有在走 agent 路线。"

所以专家这一步必须能**自己决定要不要查、查什么、查几次**：

| 情形 | 走法 | 模型档位 | 实测 |
|---|---|---|---|
| 不需要外部信息 | 直接判断 | `flash` + 关思考 | expert 755ms / 整轮 2684ms |
| 需要外部信息 | **带联网搜索的 agent**（模型自定查询，最多 3 次） | `flash` | search 2831–3543ms，带回 5 条来源 |
| 判定"要很久" | 先答一声 → 建任务 → 后台 `v4-pro` + 低思考 → **主动打断回来报结果** | `v4-pro` | 整轮 1115ms 收口，任务 ~25s 后回报 |

搜索走 Anthropic 格式端点 + 服务端工具 `web_search_20250305`，复用同一个 key，
不额外接第三方搜索服务。

**来源是协议的一部分，不是正文的一部分**（PROTOCOL.md R7）：
`message/start.sources[]` 带上标题与 URL，客户端渲染成正文下方的可点小块；
没联网就传空数组，界面**一个字都不显示**，也绝不解释原因。

---

## 附三：处理层 = 一个真 agent（2026-09-14 转向）

创始人的原话：

> 「我的聊天窗口包括我触发器所对接到的处理层的所有的对话以及反馈层的这些对话，
>  我都应该是以一个 agent……你本身调用的不是什么直接跟模型交流，你是跟 agent 交流。」

在那之前，处理层是**手搓的四次模型调用**（receiver / classify / expert / feedback）
加一个自己写的 `search.js`。那等于把一个人劈成四个分身，还让它们互相复述。
现在改成：**一个会话 = 一个真 DSH agent 进程**，那四层就是同一个 agent 的四段输出。

### 怎么接的

DSH 自带一个 `sdk` profile（`dsh --profile sdk`），用 stdio 换行分隔 JSON-RPC 说话：

| 方向 | 方法 | 说明 |
|---|---|---|
| 客户端→服务端 | `initialize` | cwd / provider / model / effort / maxTokens |
| 客户端→服务端 | `session/prompt` | `{sessionId, contentBlocks}`；**未知 sessionId 会惰性新建** |
| 客户端→服务端 | `shutdown` | 收工 |
| 服务端→客户端 | `session.event` | **完整会话日志事件**（turn/step/assistant/tool） |
| 服务端→客户端 | `session.status` | `running` / `idle` |
| 服务端→客户端 | `subagent.*` | 子 agent 起落 |

人格不是改 profile，而是启动时挂一层 `--patch`（官方支持的额外覆盖层）：
`services/core/hupo-persona.yml`。profile 保持原样，升级 DSH 不会把我们的人格改坏。

### 你的五层，在 DSH 里本来就是什么

| 架构里的层 | DSH 原生对应物 |
|---|---|
| 接收层（先应一声） | agent 本轮**第一段文本**（排在工具调用之前） |
| 处理层（真去想、可级联） | **agent 工具循环 + `subagent`**（25 个工具，含 web_search/web_fetch） |
| 反馈层（说结论） | agent 工具跑完后的**最终文本** |
| 控制器/调度器 | 只剩**节奏**：太久就挪走、到点收口、失败也要有合法收尾 |
| 监控层 | 另起一个 agent 会话读记录做总结（不污染用户会话） |
| 每用户隔离记忆 | **一个 sessionId**（DSH 自己持久化，重启后还记得） |

### 实测数字（`services/core/watch.mjs` / `browser-check.mjs`，热进程）

| | 旧（四次手搓调用） | 新（一个真 agent） |
|---|---|---|
| 第一句话 | 626–824ms | **1127–1651ms** |
| **用户看到**第一句（真浏览器口径） | 未测过 | **2207ms**（中位） |
| 整轮（简单问题） | 2576ms | **2053–2816ms** |
| 整轮（要查外部信息） | 4706ms | 9500–13678ms（但它自己搜了 3 轮、还核对了时间） |
| 冷启动 | — | **2.7–5.1s**（WS 一连上就预热，用户不用等） |
| 常驻内存 | ~30MB | **155–195MB / 会话**（空闲 30 分钟回收） |
| 工具 | 手搓 1 个搜索 | **25 个**，含 subagent / bash / 读写文件 / web_search |
| 会话记忆 | 手拼最近 6 轮进提示词 | **DSH 自己持久化**，跨轮记得（实测它自己说"跟刚才一模一样"） |

第一句话变慢是唯一真实的退步，原因是 agent 要先出一段思考才说话。
换来的是：它真的会查、会自己决定查几次、会引用真实观测、**记得上一轮聊了什么**。

### 迁移收尾：删干净

不只是删掉"四段调用"，还把**所有"直接跟模型说话"的路径**清掉了：

| 文件 | 原来干什么 | 现在 |
|---|---|---|
| `agents.js` | 四段手搓调用（receiver/classify/expert/feedback） | 删除 |
| `search.js` | 手搓联网搜索 | 删除（用 agent 自带的 `web_search`） |
| `listener.js` | 另起一次调用做总结 | 删除（改用另一个 agent 会话） |
| `llm.js` | 裸的 DeepSeek 客户端 | 删除（成了孤儿，留着就是留了第二条路） |
| `config.js` 的 `baseUrl` / `fastModel` / `deepModel` | 模型路由 | 删除（agent 进程自己带 provider 配置） |

现在 `src/` 里**没有任何一处**出现 `api.deepseek.com` 或 chat/completions ——
唯一跟模型说话的地方是那个 `dsh --profile sdk` 子进程。

### 踩过的坑（都写进代码注释与测试了）

1. `spawn dsh ENOENT` —— systemd 的 PATH 里没有 nvm 的 bin，而且没接 `child.on('error')`，
   一次 spawn 失败把整个服务带崩、systemd 反复重启，用户看到的是"整个应用没反应"。
   现在：自动找可执行文件 + 接住 error 事件 + unit 里补 PATH。
2. **人格要求"第一步只说话别调工具"会让这一轮直接结束** —— DSH 的循环是
   "没有工具调用就收轮"。所以"先应一声"必须和工具调用在**同一轮**里。
3. `session.event` **没有 token 级 delta**，事件按 step 整段到达。
   所以"接收层快"靠的是 agent 把应声那句话放在 step 1，不是靠流式。
4. markdown 链接必须在**服务端**洗成人话（客户端是纯文本气泡），
   链接不能丢 —— 转成结构化来源走 sources 通道。

---

## 附四：自动刷新（前端是哑的）

创始人的要求：

> 「前端只是一个接受和回应的东西。你当然还要做一个自动化的刷新页面机制。
>  就是如果系统改变了需要刷新，你会去刷新页面。」

所以"要不要刷新"**不是客户端判断的**，是服务端说了算：

```
部署脚本算出客户端源码指纹  ──写──▶  /var/www/hupo/client-build.json
                                          │
                            服务端每 5s 看一次，变了就
                                          ▼
              给所有在线会话推 client/reload（emitTransient：只在现场，不进日志）
                                          │
                          客户端：手上没说完的话就先说完 → 刷新自己
```

三条设计取舍：

1. **指纹按客户端源码内容算**，不是时间戳。内容不变指纹就不变 ——
   不会出现"明明没改却让用户刷新"。
2. **推送只在现场（`emitTransient`）**：刷新令描述的是"此刻"，
   迟到就没有意义。断线期间部署的新版本靠**每次连上都对一次 `/api/version`** 补上。
3. **同一个指纹只自动刷一次（sessionStorage 护栏）**。
   防的是：服务端记的指纹和实际部署的包对不上（文件写错、部署到一半）
   → 刷 → 还是旧包 → 又对不上 → 再刷……无限循环。
   宁可停在"版本对不上"，也不能让用户的页面自己抽风。

### 实测（`services/core/reload-probe.mjs`，真浏览器）

```
当前构建指纹：3ea0d4ae71b58ae9
打开页面…  [ws] 第 1 次连接
把 client-build.json 改成 probe-xxx（模拟部署）…
  [6s] ws 连接次数 = 2        ← ✅ 页面自己刷新了
盯着 probe-xxx 不变，观察 20s…
  仍是 2 次                   ← ✅ 护栏生效，没有反复刷
```

判据不是"事件发出去了"，而是**浏览器真的重新加载了一次页面**
（页面重载会重连 WebSocket，数连接次数就知道）。

### 顺带发现的另一个坑

`handleHttp` 里引用了 `createServer` 的局部变量 → `/api/version` 一被请求就
`ReferenceError` → **未捕获异常把整个服务带走**，systemd 反复重启。
而部署后的客户端每次连上都会调这个接口 —— 等于一上线就自杀。
现在 HTTP handler 外面套了兜底层：任何请求处理抛异常只回 500 + 记日志，**绝不打死服务**。

---

## 附五：聊天是贴底浮窗

创始人要求：「聊天窗口变成一个浮窗，在底部显示。」

所以界面从"整屏聊天"改成 **Stack + 贴底浮窗**：

```
┌─────────────────────────┐
│ 开发者卡片（仅 dev，浮层）  │
│                         │
│      背景（_Backdrop）    │  ← 浮窗后面**留得出东西**，
│                         │     以后首页/卡片/网页铺这里
├─────────────────────────┤
│ ═══ 抓手（可拖可点）        │
│ 🎙 最新一句话预览  ⏱1 📋 ⌄ │  ← 收起时这行就是全部信息
│ 消息列表（ListView）       │
│ [还有件事在处理…]          │
│ [说点什么            ↑]   │
└─────────────────────────┘
```

三档高度（拖抓手吸附）：**收起(124px) / 半开(50%) / 最大化(100%)**。

### 两条不能丢的性质（创始人补的要求）

> 「每次对话，浮窗就是最大化的。但因为是浮窗，所以会有一个阴影和边缘。」

1. **每次对话都最大化。** 用户开口拉满，**助手开口也拉满**（手动拖到半档也照样拉回）。
   判据是"有没有新的一条调度器消息"，而不是"是不是被收起了"。
2. **最大化**不等于**铺满**。四边永远留边距（左右 10、下 10、上 12），
   四个角都圆（18），阴影两层：外面一层散的把浮窗从背景上抬起来，
   里面一层紧的勾出边缘 —— 只有一层会像"画上去的"，不像浮着。

这两条互相拉扯：铺满就没有"边缘"，不铺满又不够"最大化"。
所以做法是**最大化到只剩一圈边距**，而不是铺到屏幕边。

### 还有一条

**收起时状态条不占输入条的地方**，但"还有件事在处理"仍以标题行上的小标存在
—— R6 不能被浮窗吃掉。

### 开发者卡片要让位

开发者卡片画在浮窗**上面**（不然被盖住），浮窗顶部按**量出来的卡片高度**让位 ——
卡片能收起/展开，高度不是常数，写死就会错位。

### 一个量出来的数

收起高度第一版写 104px，界面上没看出来，**widget 测试直接报 `RenderFlex overflowed by 6.5 pixels`**
—— 抓手 14 + 标题行 32 + 输入条（图标按钮默认最小 48）已经顶满。
现在输入条的按钮压扁（`VisualDensity.compact` + `iconSize: 20`），收起高度改成 124。
这就是为什么这个数写在注释里还标了"量出来的"。

---

## 附六：监控层 —— 一个看着这个对话到底好不好的 agent

创始人要求：

> 「你另外有个 debug 的 agent，用来监控用户说的和用户看到的反馈，你要去分析合理性
>  并且如果有修改就要变成任务。尤其注意，所有对话要有时间戳。
>  这样你才能看到反馈和响应从时效性来说对不对。」

### 一、地基：每个事件都有时间戳（R11）

放在 `Conversation.emit()` 里**统一加**，不在各个调用点补：

```js
const withSeq = { at: Date.now(), ...event, seq: ++this.seq };
```

这样**将来新加的事件类型也不可能漏**（之前 `message/text`、`message/status`、
`message/end` 就都漏了）。调用方自己给了 `at` 就以调用方为准 —— 客户端时钟能借此带进来。

### 二、看两样东西

| 看什么 | 从哪来 | 口径 |
|---|---|---|
| **用户说了什么** | `user/echo` | 服务端 `at` + **客户端 `clientAt`**（他按发送的时刻） |
| **用户看到了什么** | `message/*` | 服务端 `at` |
| **用户什么时候看到的** | `message/seen` | **客户端自己的钟**（`clientFirstSeenAt` / `clientEndedSeenAt`） |

为什么必须让客户端报一次：服务端只知道"我什么时候发的"。
**用户按下发送 → 请求到服务器**这一段、以及**服务端发出 → 屏幕画出**这一段，
只有客户端自己的钟能测。而且两个值同钟相减，**不受两端时钟偏差影响**。

### 三、两级判断（既不漏也不贵）

1. **计量**（纯算，不花钱）→ **每一轮都做**，结果直接进开发卡片：
   `时效 首句 1531ms ｜ 整轮 11815ms ｜ 空窗 10284ms`
2. **判断**（叫 debug agent，花钱）→ 只有机械规则报出问题时才叫，且同一会话 60 秒冷却

机械规则抓的：首句 > 6s、整轮 > 30s、**空窗 > 8s**、留客式追问（「要不要我…」）、
失败收尾、一句话不说。都是不要钱的确定性判断。

### 四、发现的问题变成任务，不是日志

`TaskBook`（`data/debug/tasks.json`）。**按"问题类别"立项**：

```
同一类毛病 = 一条任务，seenCount 累加说明它反复发生
```

为什么不能按标题去重：机械规则说「中间有 11792ms 完全没有输出」，
debug agent 说「第一句 959ms 就出来了，但之后到结论之间空窗 11792ms」——
措辞差得远，精确匹配挡不住，任务簿一下堆了几十条同义条目（实测踩过，27 条 → 3 条）。

### 五、实测产出（它真的抓到了东西）

第一轮跑出来就抓到了这些真问题：

- **空窗 9–14 秒**：首句 1 秒就出来了，然后一直静默到结论 —— 用户盯着"我去查…"干等十几秒。
- **只说了"我去查"就收轮**：整轮没有任何结论、来源 0 条，等于把过程当结果交出去。
- **来源条数 0 却给精确数字**：「湿度 92%」「雨量 5 毫米」「日落 18 点 06」。
- **自相矛盾**：一句说"这件事比我想的久，我单独拿去做"，紧接着同一条里把完整结论给全了。
- **时间对不上**：回答里写"现在（清晨 5 点 45 分左右）"，而同一批另一轮说当时是凌晨 5 点 41。

每条都带**能动手的修法**（改哪个环节、加什么硬约束），不是"建议优化"。

### 六、踩过的坑

1. **刷新页面后，客户端把历史补发当成"用户刚看到"** → 感知延迟算成 14 秒（服务端只有 2.7 秒）。
   修法：`/api/version` 返回 `serverNow`，客户端用**服务端时钟**划线，只回报之后产生的消息；
   另外在计量侧加可信度过滤（客户端口径比服务端口径多出 5 秒以上就不采信）。
2. **agent 在 JSON 后面补一段说明文字** → `indexOf('{')…lastIndexOf('}')` 切出来不是合法 JSON，
   审查整个崩掉。改成**按括号配对扫描**取第一个配平对象。
3. **任务按标题去重挡不住改写** → 改成按问题类别立项。

---

## 附七：服务重启时的三种表现

创始人要求：

> 「如果后端服务要重启，聊天窗口要变得灰色不可触达，重启完成要可以自动恢复，
>  并说一句我已经升级好了。」

| 时刻 | 表现 |
|---|---|
| 通着 | 正常 |
| **重启中** | 整块**抽掉颜色**变灰 + 罩一层「正在恢复连接…」+ **不可触达**（`IgnorePointer`） |
| **恢复后** | 自动变回正常（用户什么都不用做），并出现一句 **「我已经升级好了。」** |

### 三个关键决定

**1. 变灰是"抽掉颜色"，不是调透明度。**
用 `ColorFilter.matrix` 的亮度矩阵（R/G/B 三行系数相同），蓝气泡会真的变成灰的。
只调透明度只是"变淡"，用户会以为界面卡住了。

**2. 不可触达 ≠ 隐藏。**
输入框还看得见（用户刚才打的字不丢），但整块被 `IgnorePointer` 包住。
藏起来会让人以为坏了；看得见但点不动 + 一句"正在恢复连接"，才是"我知道，我在处理"。

**3. "服务重启过"由服务端判断，客户端不自己编。**
判据不需要客户端自报任何东西：

```js
sinceSeq > 0                                   // 它之前就在这个会话里，不是全新打开
&& 会话记录里没有任何一条事件发生在本进程启动之后   // 它见过的最后一段历史来自上一个进程
```

说完之后这个会话在本进程里就有"本进程的事件"了，所以**只会说一遍**
（多个设备同时重连也只有第一个触发）。实测：重启一次说一次，再重启不重连就不说。

### 附带修掉的一个隐患

原来的 WebSocket 传输层**只重连 6 次就彻底放弃**（退避 2/4/6/8/10/12 秒 ≈ 42 秒）。
服务重启 + 部署可能超过这个窗口 —— 一旦放弃，用户就得手动刷新。
现在改成**不限次数、退避封顶 8 秒**：服务什么时候回来它就什么时候接上。
"自动恢复"必须是**真的自动**，不能靠用户刷新。

---

## 附八：两起"页面在说假话"的事故

创始人两次来问「页面怎么还在说……」。两次都不是显示 bug，
是**服务端把不真实的状态写进了对话记录**。

### 一、「还有件事在处理」永远挂着

**现象**：页面上那行灰字从 15:31 一直挂到 16:39，用户半小时后还在看它。

**根因**：界面上那句话是客户端**从事件记录重建**的 ——
`task/created` 建、`task/completed` 消。而 agent 改客户端改到一半重启了服务，
**它自己连同那件活一起被杀**，没人补 `task/completed`。

**修法**：启动时**对账**（`Dispatcher.#reconcileTasks`）：

- 扫一遍记录，`task/created` 没有配对的 `task/completed` ⇒ 一定是被打断的
- **一定收口** —— 宁可少显示一条"在处理"，也不能让用户等一个不会来的结果
- 但**只对最近的说话**（6 小时内），两天前的旧账悄悄清掉就行
- 措辞用「可能没做完」而不是「没做完」：系统只知道"我被断了"，
  **不知道**那件活到底做完没有（可能改完了只是没来得及回话）

配了 5 条测试：收口 + 说话 + 旧账不说 + 已完成不重复 + 重启两次只说一次。

### 二、「我已经升级好了。」堆了 7 句

**现象**：用户来问「页面还在说」时，对话里躺着 **7 句一模一样的**「我已经升级好了。」
（06:00、13:01、15:04、15:32、15:46、15:47、16:37 —— 每次服务重启一句）。

**根因**：判断"要不要说这句"的判据**太松**。第一版只看两条：

```
sinceSeq > 0                              ← 它之前就在这个会话里
&& 本次启动还没跟他说过话                    ← 这条历史来自上一个进程
```

于是**每次重启、只要客户端重连就说一句** —— 用户根本没被打断过，
页面刚打开也会收到。**判据里缺了"他确实被打断"这一条。**

**修法**：由客户端自报一个它**确实知道**的事实 ——
「这次连接是断线重连，还是页面刚打开」。

```
GET /api/stream?conversationId=…&sinceSeq=…&interrupted=1
```

传输层记着 `_everConnected`：连成功过一次之后，后面的连接才带 `interrupted=1`。
服务端只在 `interrupted=1` 时才考虑说这句话。

实测（带诊断日志）：

```
WS 连接 conv=c_main sinceSeq=0   interrupted=false   ← 页面刚打开，跳过 ✓
WS 连接 conv=c_main sinceSeq=562 interrupted=true    ← 真被打断
noticeReconnect → true                               ← 说了一句 ✓
```

**历史那 7 句**清掉了（备份在 `/tmp/c_main.bak.*.jsonl`）——
它们是我调试时反复重启堆出来的，不是真实的产品事件。

### 这两件事的共同教训

**客户端的显示是服务端事件的**投影**。服务端一旦写进一条不真实的状态，
页面就会一直替它说那句话 —— 而且说得理直气壮。**
所以：写进对话记录的每一条，都要问一句"这条一直是假的怎么办"。

---

## 附九：反馈必须**按时间说话**（消息不许交叉）

创始人原话：

> 「我发现你在尝试补齐对话。这个不对。不能补齐，反馈完全按时间来说话。」

### 他看到的是什么

按 seq 排（客户端就是按 seq 渲染的）：

```
seq 571  开气泡1
seq 572  写进气泡1  「收到，我去查一下…」
seq 573  开气泡2    「这件事比我想的久，我单独拿去做，完了跟你说。」
seq 578  写回气泡1  ← 答案长回了**上面那个**气泡
```

客户端**一个 messageId 一个气泡、按 seq 排序** ⇒ 上面那个气泡在"我拿去做"
之后还在继续往后长 ⇒ **时间倒流** ⇒ 看起来像在补齐对话。

扫历史记录：**10 处**，每一处都是同一个签名 —— 也就是说**每次挪走都会发生**。

### 根因

"挪走"（`escalateAfterMs` 到点说「我单独拿去做」）只做了两件事：
发一条 dispatcher 消息、建一个任务。**它没有把 agent 手上那条气泡收口。**

于是 agent 后面的输出还写回原来那条 messageId，而那条气泡在时间线上排在
"我拿去做"**前面**。

### 修法

挪走时**先收口，再另起一条**：

```js
// session-translate.js
handoff() {
  if (cur.writer.started) cur.writer.end('completed');   // 手上这条就此收口
  cur.writer = new MessageWriter(conv, {
    messageId: newId('m'),
    origin: 'proactive',        // 这是"做完了回来说"，不是接着说
  });
  cur.textCount = 0; cur.sawTool = false; cur.sources = [];
}
```

修完实测（低阈值跑一轮）：

```
seq 3   开气泡1（agent）
seq 4   写进1  「收到，我先去查上海现在的天气。」
seq 5   收口1                    ← 先收口
seq 6   开气泡2（dispatcher）
seq 7   写进2  「这件事比我想的久，我单独拿去做，完了跟你说。」
seq 9   建任务
seq 10  开气泡3（agent, proactive） ← 另起一条
seq 11  写进3  「搜索结果没给出具体数据，我直接去取实时数据。」
seq 12  写进3  「上海现在 27 度左右…」
seq 16  任务完成
```

三个气泡按时间读：**应一声 → 我拿去做 → 做完了回来说**。
自动判交叉：**0 次**。

### 不变量（写进测试了）

> **消息之间不许交叉。** 一条消息开始之后，就不能再有别的消息的正文
> 写进更早的那条里。

`test/timeline-order.test.js` 把这个不变量形式化了：把事件流折成每条消息的
`[开始seq, 结束seq]`，断言区间两两不重叠。5 条测试覆盖：普通一轮、挪走、
挪走时还没说话（不留空气泡）、重复挪走、来源归属。

**这条不变量比"别自相矛盾"更根本**：自相矛盾只是它的一个症状，
它真正破坏的是"反馈和时间一一对应"。

---

## 附十：鉴权（把"公开无鉴权"这个洞堵上）

### 当时的暴露面（不用任何凭证就能做）

```
① 读你的全部对话记录      GET /api/conversations/<id>/events   → 200
② 以你的身份下指令        POST /api/say                         → 202
③ 读你的调试数据          GET /api/debug/report                  → 200
④ 实时读你所有消息        wss://…/api/stream                      → 连上了
```

而这条链路的尽头是一个**能跑 bash、有免密 sudo、能改自己代码**的 agent。
这不叫"有风险"，这叫"门是开的"。

### 设计取舍（每条都对应一个具体的攻击面）

| 决定 | 为什么 |
|---|---|
| 口令只存 **scrypt 哈希**（Node 自带） | 明文不落盘；不引依赖 |
| 令牌**签名、无状态**（HMAC-SHA256） | 用"服务端会话表"的话，每次改代码重启都把人登出 —— 而服务本来就会反复重启，用户会以为鉴权坏了 |
| 密钥**持久化**在 `data/auth.json`（0600） | 重启不换密钥；权限锁死，泄漏密钥 = 谁都能签合法令牌 |
| HTTP 用 `Authorization: Bearer` | 标准做法，两端都通 |
| WebSocket 用**子协议** `['bearer', token]` | 浏览器不能给 WS 设自定义头；而**令牌绝不能放查询串** —— 那会被 nginx 原样写进 `access.log`，等于把钥匙挂在门口 |
| 登录接口**按 IP 限速**（5 次锁 15 分钟） | 公网上的口令框不限速就是给人爆的 |
| **不做"本机免鉴权"** | ⚠ nginx 把 `/api/` 反代到 `127.0.0.1:8091` —— 所有外部请求的源地址都是 127.0.0.1。加本机豁免 = 鉴权当场失效。这个捷径**永远不能加** |
| 没设口令时**大声喊**（启动横幅） | 不能让它在日志里悄悄裸奔 |

### 公开路由只有三个（白名单要短）

```
GET  /api/auth       客户端问"要不要登录、我算不算登录了"
POST /api/login      口令换令牌
GET  /api/version    没登录只给 buildId（客户端靠它判断要不要刷新自己）
```

其余**一律 401**，包括 `/api/health`。

### 没登录时界面露什么

**只显示登录页。** 对话气泡、会话列表、开发者卡片一个都不建 ——
这台机器上的 agent 能执行命令，露一个字节就等于把主人说过的话给出去了。

### 实测（真浏览器 + curl，两条路都打）

```
匿名读对话记录     → 401
匿名发指令         → 401
匿名读调试数据     → 401
匿名连 WebSocket   → 401（Unexpected server response: 401）
篡改令牌一个字符   → 401
错口令             → 401，连错 5 次 → 429（锁 15 分钟）
正确口令           → 200 + 令牌 → 之后 200
刷新页面           → 仍然登录着（令牌持久化）
```

### 踩到的坑（真浏览器抓出来的）

**登录成功、接口全 200、界面却停在登录页不动。**
根因：登录后控制器通知了，但**外层 App 没重建** —— 我在 `main.dart` 里只挂了个
"把令牌落盘"的监听，它不 setState。

单测 `LoginScreen` 是测不出这个的（那个页面本身完全正确）。
所以补了一条**外壳级**测试：把整个 `HupoApp` 跑起来，断言登录后
`LoginScreen` 消失、`ChatScreen` 出现。为此给 `HupoApp` 开了个
`transportOverride` 口子（只在测试里用）。

教训：**"页面自己没错"不等于"流程对"** —— 跨组件的状态流转必须在外壳层测。

---

## 附十一：修掉监控层报的三个真问题

监控层攒下来的问题里，有三条是**真能修的**，其余是历史遗留或已被前几节的修改覆盖。

### 一、回答硬截断在半句（最实在的一个）

**现象**：回答断在「一个是"避」「临海老」—— 用户拿到一个**看起来说完了**、
其实没说完的残篇。

**根因（实测确认）**：agent 撞上单步输出上限时，`turn/end` 的 reason 是
**`max-tokens`**，而翻译层**完全忽略 reason**，一律写 `completed`：

```js
// 修前
cur.writer.end('completed');   // ← 半句话也被当成完整回答
```

复现方式：把 `maxTokens` 调到 120，问一个长问题 ⇒
`── 第 1 轮结束（max-tokens）`，可见文本只有「收到，我去把上海这几块资料查齐了再讲。」

**两处修**：
1. **识别非正常收尾**：`kind !== 'completed'` 时补一句
   （`这条我说太长了，被长度限制截断，剩下的我没说完。` / `这条我没说完就断了。`），
   并把 `message/end` 标成 `failed` —— **绝不许把半句当完整回答**。
2. **把上限从 4000 提到 16000**（长回答 + 思考都要吃这个预算）。

配了 4 条测试：撞上限必须补句 + 正常收尾不许补 + 被中断也要补 + 一个字没说也要有交代。

### 二、编造具体来源

**现象**：回答里写「上海师范大学 2010 年有一篇硕士论文，访谈了 227 名 3 到 5 岁幼儿」，
而那一轮**来源条数 0**。年份、机构、样本量俱全，听起来特别可信。

**两处修**：

1. **人格勒紧**（主）：点名最容易顺口编的几类 —— 年份、机构名、论文、研究结论、
   样本量、统计百分比、引文 —— 没查到出处就**不给具体数字**，宁可说「这块我不确定」。
   同时加一条：**不许说你打算做什么然后不做**（「我去实测一遍」这种话，要么做完再说）。
2. **机械兜底**（副）：`looksLikeUnsourcedClaim()` 认出这几类断言，
   而本轮**来源为 0** 时，收口前补一句「这条里的具体研究我没查出出处，用之前你核一下。」

检测器刻意写窄（10 例验证全对）：认得出「研究表明」「访谈了 227 名」「样本量 500」，
**不误伤**「气温 24 到 31 度」「2026 年 9 月 14 日」「我们刚才聊了三条」。

**实测对比**（同一个问题"3到5岁小孩对父母的认识是怎么发展的？"）：

```
修前：上海师范大学 2010 年有一篇硕士论文，访谈了 227 名 3 到 5 岁幼儿   ← 编的
修后：真去搜（16 条来源，抓了 Semantic Scholar / Springer / PubMed）
      「不过这篇的全文我没读到，具体是几岁到几岁、转变的判据是什么，
        我不确定，不编给你。」
      「我只看到标题，没查全文。」                                    ← 认边界
```

### 三、来源挂错了气泡（顺手发现的）

挪走时把来源收给了「收到，我去查…」那条，结果**给结论的那条一条来源都没有** ——
正好挂反了。来源属于**结论**，不属于"我去了"。修：应声条 `sources = []`，
来源跟着答案走。加了测试。

### 顺带：验证工具自己也要能进门

开了鉴权之后，所有探针（`watch.mjs` / `browser-check.mjs`）先撞 401。
于是：`auth-cli.mjs --token` 本机签发令牌；`probe-login.mjs` 让浏览器探针
**老老实实走一遍登录**（顺便每次都在验证登录这条路还没坏）。

同时把 `browser-check.mjs` 默认会话改成 `c_probe` —— **探针不许再往主人的对话里灌东西**。
