# 架构讨论稿：v6 的边界、契约与演进路径

> 视角：架构。不问"功能全不全"，只问三件事 —— **这条边界画在哪、谁来保证它不被破坏、现在不做以后要返工吗**。
> 本文不复述 v6/seamless/speaker/delivery 的内容，只对它们做判决与改写建议。
> 我的口头禅：**一个边界如果两头都能做，它就不是边界，是一个 bug 的存放处。**

---

## 〇、我的假设（不成立请直接驳回对应判决）

| # | 假设 | 不成立会怎样 |
|---|---|---|
| **G1** | 客户端（Flutter）是**唯一渲染面**，同一会话不存在第二个客户端同时渲染 | "已输出/已渲染"失去单一权威，R5 与"两块一条消息"同时失效 |
| **G2** | 控制器插件能拿到 chunk 级输出与轮生命周期钩子（`agent/pre-step`、`agent/turn-stopping` 级别） | 第三节的"推迟窗口"与 R5 都要重估；进程内这条论证也就不再充分 |
| **G3** | 所有用户可见文本**全部**经控制器唯一出口（T2/T3 内的文案也不例外） | 边界 5 的重叠点关不上，"一个声音"不可审计 |
| **G4** | 延迟差（首字 0.8–1.0s vs 深答 p50 6.5–7.9s）在接入记忆与工具后仍保持 ≥4 倍 | "两块"退化成一块，本文第一节与第四节可整体简化 |
| **G5** | 本轮不做"同账号多设备并发同会话" | 需要额外的"设备归属"层，SpeakerSignals 与 Decision 都要加维度 |

**一条总纲（本文所有契约都从它派生）**：
> **能力（capability）比禁令强，位置（位置性约束）比提示词强，类型（不可表达）比断言强。**
> 画边界时优先问：能不能让越界**在代码里表达不出来**；不能，退而求其次：能不能让越界**在唯一出口处被拦下**；再不能，才写断言；最后才写提示词与评审。

---

# 一、v6 的边界画得对不对

## 1.1 "两块回答"的边界（接收层 ↔ 反馈层）：**不清晰，而且有一条自相矛盾的谓词**

**现状**：边界按"内容类型"划 —— 接收层给"能立刻确定的部分"，反馈层给"研究后的结论"。
**这是本文要指出的第一个硬问题**：
> "能立刻确定"这个谓词的判据是**研究结果**，而接收层按定义拿不到研究结果。
> 两条禁令（"不许给未经研究的结论" + "要给能立刻确定的部分"）在谓词层面互斥 —— 接收层只能靠**类别先验**去猜。

猜错的产物不是"废话"，是 **S4 级纠偏**。也就是说：**v6 最大的信任风险（S4 做砸）不是实现问题，是这条边界定义自己制造出来的**。

**重叠点（两头都能做的地方）**：
1. **"接收层说到哪停"** —— 现在有三个候选人：控制器（交接判决 + 打断禁区表）、接收层（"末句留扣"的角色指令）、反馈层（首句锚点）。三条都在，等于谁都没负责。
2. **"能立刻确定"的判定权** —— 接收层觉得自己能确定，控制器觉得处理层马上就好，两边都能决定"这句话该不该由接收层说"。

**契约改写（可强制形式）**：

| 项 | 改写 |
|---|---|
| **划界依据** | 从"内容类型"改为**信息来源（能力门控）**：接收层的可用输入 = 用户本轮原话 + 粗查记忆 + 分类标签。**它不持有工具、不持有精查记忆、不持有处理层任何产物。** 想说结论也没料 —— 这才是"不许给未经研究的结论"的硬实现 |
| **停止权** | **接收层只负责"停在哪都不难看"，控制器独占"什么时候停"。** 接收层不得自我收尾（不得输出"我先去确认"后自行结束）—— 那等于把决策权下发给一个非确定性组件 |
| **反馈层的"最终发言权"** | 不等于无限时长。定义为：**必须给出一个可执行结论 + 最多一次显式纠偏（S4）**；不得以"关于你的问题""总的来说"开头，且首句必须与接收层全文共享至少一个实词（v6 已有此校验，保留） |
| **不重复** | 从"提示词指令"升级为**会话级断言**：反馈层输出中不得包含接收层任何完整句（子串级）；命中则重生成一次，再命中落 trace 并放行（不阻塞交付） |

【保证者】：能力门控＝装配入口（唯一构造上下文处，接收层的入参类型里根本没有"处理层产物"这一项）；停止权＝唯一出口 gate；不重复＝会话级断言 + 重生成预算。

**顺带一个必须钉死的定义（这是 R5 的真实缺口）**：
> R5 说"接收层**已输出**全文" —— **"已输出"是谁的视角？**
> 模型已生成 ≠ 控制器已放行 ≠ 客户端已渲染。三者可能不同（生成快于渲染）。

我的判决：**唯一权威必须是"控制器已放行"，并落盘**，契约名从"已输出"改为 `emittedText`（controller-committed）。
否则反馈层会去承接**用户还没看到的字** —— 那正是"重复"与"错位"两个失败模式同时发生的机制。**这条如果不在第一版落盘，以后无法回溯（历史轮次没记过）。**

## 1.2 "谁说话"的边界（控制器 ↔ 角色）：**方向对，但有一个没归属的出口**

**现状**：控制器"不能有表达欲"。这句话是对的，但 v6 自己就留了两个破口：

| 破口 | 谁在写这段文本 | 判决 |
|---|---|---|
| **S3 的"轻量重启信号"**（"…刚查完，**结论是**…"） | 未指定 | 归**反馈层**（它是结论的开头，不是状态播报） |
| **S3 的"轻量续说"**（"还在确认第二处"） | 未指定 | 归**接收层**，且必须计入"续说预算"（每轮 ≤ N 次），**预算写在控制器里、不写在接收层指令里** |

**契约（可强制形式）**：
> 凡用户可见文本，必须携带 `speaker ∈ {receptive, reflective}`。
> **`speaker = controller` 在类型上不存在** —— 控制器的输出类型是 `Decision`，不是 `Utterance`；两者用 branded type 隔离，**没有公共结构、不能互相赋值**。

JS/TS 需要注意一个坑：结构化类型下 `{text}` 与 `{text}` 天然兼容。所以必须用 brand：
```ts
type Utterance  = { readonly __brand: 'utterance';  readonly text: string; readonly speaker: 'receptive'|'reflective'; readonly turnId: TurnId }
type Decision   = { readonly __brand: 'decision';   readonly action: 'speak'|'silent'|'hold'; readonly reason: string }
```
这样"控制器顺手说一句话"就不是纪律问题，而是**编译不过**。

【保证者】：类型系统（brand）+ 唯一出口 gate（所有外发文本都过 `emit()`，`emit()` 只接受 `Utterance`）+ CI 断言"外发流里每条文本都带 speaker 与 turnId"。

**注意一个反直觉点**：控制器越"薄"，越容易偷偷说话。REVIEW-eng 把第一版控制器压到 <400 行是对的，但**薄不等于可以没有 `Decision` 这个类型** —— 类型要先有，行数可以后长。

## 1.3 "记忆写权"的边界（控制器独占）：**把两个权利捆成了一个，必须拆**

**现状**："记忆层每用户一份，控制器独占写权"。
**重叠点**：这句话把两个不同的权利合并了：

| 权利 | 本质 | 谁最适合 |
|---|---|---|
| **决定权**：什么值得记、以什么置信度记 | **语义判断** | 处理层分类节点（它有 confidence、有类别路径）—— 控制器没有语言能力，判不了 |
| **提交权**：写入、幂等、隔离、跨轮一致 | **工程不变量** | 控制器（它持有事实源与 turn 边界） |

把两者捆在一起的实际后果，v1 已经踩过：经验沉淀逻辑写在 orchestrator 里（既判又写），于是"通用兜底路径的经验污染召回"这类问题是靠**补丁**修的（README 坑 1/3），不是靠边界修的。

**契约（可强制形式）**：
> 记忆接口只有一个写入口：`commit(turnId, proposals[])`。
> **不存在 `write(entry)`。** proposal 是处理层/反馈层产出的不可变数据，控制器不解释其语义、只做提交。
> `commit` 幂等键 = `turnId`（这是 REVIEW-eng 炸点 7 的正解，且它天然落在边界上）。

**读权必须单独定义**（现在缺）：三个读者（接收层粗查 / 处理层精查 / 控制器自身），必须**只读已提交的快照**，否则会读到未提交的半成品。
> 契约：读接口按 `(userId, asOfTurn)` 取；运行中禁止读到 `asOfTurn` 之后的 proposal。

【保证者】：类型（无 `write` 入口）+ 幂等单测（同 turnId 提交两次，断言 hits 不变）+ 隔离断言（前缀首段必须是本用户标识段）。

## 1.4 "该不该答"的边界（说话人识别 ↔ 控制器）：**归属含糊，但它最容易做成硬边界**

**现状**：v6 §2 把"说话人判定"与"控制器"并列成两行；speaker 专文 §7 又说这些是控制器新增的能力。**两处不一致**。

**我的判决**：**识别器只出信号，控制器独占判决。**
> `decide(signals: SpeakerSignals, mode: Mode) → Decision` —— **一个纯函数，无 IO，无 LLM，可穷举单测。**

为什么必须是纯函数：这是**整套设计里唯一能被场景测试穷举的判决**（R4 的硬例外、低置信保守策略、"主人在等回答"三态）。一旦识别器也能"决定不回应"（例如低置信直接丢），你就有了两个 owner，而**没有任何测试能钉住行为**。

**契约（可强制形式）**：
1. **判决写成穷举表**（信号组合 × 模式 → 动作），代码里是一个 `match`，表与代码同源。
2. **规则必须全序**："被点名必须答"与"低置信保守"会冲突 —— 谁赢必须显式写死（我建议：**被点名 > 低置信保守**，但**仅当 mode=active 或被点名信号中的"称呼词"命中**；纯情绪强度不足以越过保守默认）。
3. **强制点在出口，不在入口**：被动模式下"接收层不说话"不能靠接收层指令保证 —— 一旦控制器误判而 steer，它就说。所以：

> **断言位置：`emit()` 出口处 `assert(mode === 'active' || pointedAt === true)`。**

【保证者】：纯函数 + 穷举表单测 + 出口断言 + **回放测试**（喂一批低置信 fixtures，断言外发流 0 条）。

**这条边界的最强之处**：它是 v6 里唯一一个"信号可替换、判决不变"的结构 —— 也是为什么声纹可以后加（见第四节）。

## 1.5 "交付形态"的边界（处理层判定 vs 控制器裁决）：**最危险的一条，因为一个 LLM 输出直接决定分钟级资源**

**现状**：档位由处理层的分类节点判定（`{难度, 交付形态}`）。
**重叠点**：分类是**语义判断**，T4 是**资源承诺**（异步任务 + 时间承诺 + 页面仓库 + 合规审核）。让两者同一处产出，等于让一次模型输出直接开门。

**契约（可强制形式）**：
| 阶段 | 字段 | 类型 | 谁产出 |
|---|---|---|---|
| 建议 | `deliveryHint: 'T1'..'T4'` | **建议**，无副作用 | 处理层 |
| 裁决 | `delivery: 'T1'..'T4'` | **裁决**，有配额与准入 | 控制器 |

两条纪律：
1. **T3/T4 必须有可测量的准入条件**（配额、单任务时长上限、域名白名单、当日未超预算），**不是模型自觉** —— 这是 REVIEW-eng §4.4 那条准入规则的同类形态。
2. **交付形态不能替代语言**：每轮必须**有且仅有 1 条以用户可见文本结尾的答复**；T2+ 的产物是它的 `attachments[]`，**不是它的兄弟**。否则会出现"反馈层没说话，只弹了个页面"—— 那等于把"一个逻辑型助理"退化成工具面板。

**一个近乎免费的诊断指标**：trace 落 `deliveryHint` 与 `delivery` 的**不一致率**。
> 这个率本身就是"模型判档是否可信"的唯一证据，且它不需要任何新机制。

【保证者】：两个字段不同名不同类型（写错就编译不过）+ 控制器准入闸门 + trace 不一致率告警。

## 1.6 五条边界一览（判决与保证者）

| 边界 | 清晰度 | 主要重叠点 | 关键契约 | 保证者 |
|---|---|---|---|---|
| 两块回答（接收↔反馈） | ❌ 谓词自相矛盾 | "说到哪停"有三个 owner | 按**信息来源**划界；停止权归控制器；`emittedText` 由控制器落盘 | 装配入口能力门控 + 出口 gate + 会话级断言 |
| 谁说话（控制器↔角色） | ⚠️ 有未归属出口 | S3 的两段文本无人认领 | `speaker ∈ {receptive, reflective}`；`Decision ≠ Utterance` | brand 类型 + 唯一 `emit()` |
| 记忆写权 | ⚠️ 两权捆绑 | 决定权 vs 提交权 | 只有 `commit(turnId, proposals)`；读按 `asOfTurn` | 类型 + 幂等单测 + 隔离断言 |
| 该不该答（识别↔控制器） | ⚠️ 文档不一致 | 识别器可能自作主张 | 判决是纯函数 + 全序规则 + **出口**断言 | 穷举表单测 + 回放 fixtures |
| 交付形态（处理↔控制器） | ⚠️ 建议与裁决混同 | 一次模型输出开门 | `deliveryHint` ≠ `delivery`；形态不替代语言 | 类型 + 准入闸门 + 不一致率 |

---

# 二、R1–R5：哪些能靠机制强制，哪些只能靠人

先给结论表，再逐条说。

| # | 可机制强制度 | 最强手段 | 残留（只能靠人） |
|---|---|---|---|
| R1 记忆隔离 | **95%** | **能力式 API**：`openMemory(userId)` 返回的句柄上**不存在任何接受 userId 的方法** → "跨用户读"在类型上不可表达 | 运维错误（home 目录配错）靠部署脚本断言 |
| R2 只追加 | **90%** | append-only 句柄（无 `set/insert/splice`）+ 字节稳定性单测 + 启动指纹断言 | **"整块重建"这个唯一例外与"改写前缀"长得一模一样** |
| R3 情绪只影响怎么说 | **~60%** | 信号通路唯一（情绪块只能进 voice 段）+ 反驳回归集 | "顺从 vs 安抚"是语义判断 —— **本文点名的"只能靠评审"的那一条** |
| R4 只对主人回应 | **90%** | 纯函数判决 + 穷举表 + **出口断言** + 低置信回放测试 | 识别器本身的准确率（模型精度） |
| R5 反馈层含接收层全文 | **95%** | 装配入口签名强制非空 transcript + sha256 一致性断言 | `emittedText` 的**定义**（已输出/已放行/已渲染）必须先拍板 |

## R1 · 记忆隔离三层

**三层各有对应的强制形态，且第三层（提示词层）最弱、必须补断言**：

- **数据层**：不要靠"签名里必填 userId"。签名可以被绕过（`search(userIdOfOther, …)` 照样过）。
  **要的是能力式句柄**：`openMemory(userId)` 内部闭包捕获 userId，**对外对象上没有 userId 参数**。这才叫"绕不过去"。
  【保证者】类型/模块边界（单例导出面收敛）。
- **进程层**：`DSH_HOME = data/<userId>` 由 systemd 模板注入，**部署脚本断言 `basename(DSH_HOME) === $USER_ID`**；否则一个环境变量拼错就把两个人的 home 指到一处。
  【保证者】部署脚本 + 启动自检。
- **提示词层**：v6 已有两条断言（首段是用户标识段 / prompt 不含他人 uid），保留。
  补一条更硬的：**装配入口只接受 `MemoryHandle`（已绑定 userId），不接受原始条目数组** —— 否则"从别处捞来的块"可以被塞进任意 prompt。
  【保证者】装配入口签名。

## R2 · 只追加，不改前缀

这是 R1–R5 里机制最强的一条，且**唯一需要补的是例外的形态**。

- 强制的三层（建议全都要）：句柄层（无改写 API）、运行时层（每次装配断言前 n 块字节不变，**生产也开**）、CI 层（20 轮字节稳定性单测 + "触及装配入口的 PR 必须附测试证据"）。
- **缺口**："唯一例外是整块重建，且必须在用户思考间隙做" —— 这句话在代码里**与常规改写无法区分**，所以它一定会被当成后门用。
  > 契约改写：重建必须是**显式 API** `rebuild(reason)`，且需要一个**由控制器签发的窗口令牌**（窗口 = 用户思考间隙，由控制器观测空闲时长产生）。没有令牌 → 拒绝。
  【保证者】API 形态 + 令牌（**这是"例外必须有独立的表达形式"的通用手法**）。

## R3 · 情绪只影响"怎么说"

**这条是本文明确认定"无法被机制完全强制、必须靠代码评审"的那一条。** 但要说清楚它的可强制部分与残留：

- **可强制（约 60%）**：
  1. **通路唯一**：情绪信号只能进 `voice` 段，不能进任何"事实/结论/建议"字段 —— 这是装配位置约束，可断言。
  2. **反驳回归集**：构造 N 条"错误前提 + 高情绪"用例，断言输出含显式纠偏标记（"不是 X，是 Y"这类结构可自动判）。**这是可自动化的** —— 假前提检测已有 6/6 的实测，回归集有基础。
- **残留（只能评审 + 抽样盲评）**："安抚"与"顺从"的界线是语义的。你可以测出"说了纠偏词"，测不出"纠偏力度被情绪压软了"。
- 【保证者】回归集（自动）+ **每季度一次盲评**（人工，10 轮样本，判"这次是安抚还是在顺着说"）。
- 语气分级名单（0.7 阈值、缩短首句、降密度）不是机制，是**策略常量**；常量放配置、改动留 trace 版本号，出问题才查得回来。

## R4 · 默认只对主人回应，被点名是硬例外

见 1.4。补充一条**运维向**的强制：**被点名检测的四个依据（称呼/指向/情绪/停顿）必须能被单独开关**，因为它们的误报率差异巨大（称呼词极准，情绪+指向很糙）。
> 契约：`SpeakerSignals` 里每个信号独立且带来源，判决表按**信号来源**分级，而不是按"综合得分"。综合得分会让"情绪强度高"单独构成越权——**这是现场最容易变成事故的一条**。
【保证者】信号结构（分级而非加权）+ 穷举表 + 出口断言。

## R5 · 反馈层上下文永远含接收层已输出全文

机制上最容易的一条（签名 + 断言），但**它有一个前置定义必须先拍板**（见 1.1 末尾）：

> `emittedText` := 控制器已放行的文本（commit 流），逐轮落盘。

三个候选（模型已生成 / 控制器已放行 / 客户端已渲染）里，只有第二个是**进程内可得且唯一**的。选第三个需要客户端回执（多一跳、且弱网下不可靠）；选第一个会让反馈层承接用户没看到的字。
【保证者】装配入口签名 + sha256 一致性断言 + **落盘**（不落盘则历史轮次不可回溯）。

---

# 三、方案 A vs B（逐字流式 vs 按句投递）

## 3.1 这个选择应该在哪一层做？—— **不该由任何单一层做，必须拆成三个不同的决定**

先把问题拆开，因为把 A/B 当成"一个选择"是错误的前提：

| 子问题 | 归属 | 理由 |
|---|---|---|
| **切点算不算安全**（当前句写完没有） | **控制器** | 它已有"何时交接"的独占权（1.1）。切点判据是这条权利的一部分，不是新权利 |
| **投递语义**（客户端能显示到哪） | **协议**（网关承载，但网关**零语义**） | 网关只转发带边界标记的片段；**它不能理解"句"** —— 否则每改一次切分规则都要升级网关 |
| **渲染**（把已 commit 的前缀画出来） | **客户端** | 客户端只做一件事：渲染 commit 流。**它不得自行切分、不得自行缓冲整句** |

**为什么决策不能在客户端**：客户端是唯一**无法知道深答 ETA** 的地方。让客户端做缓冲决策，等于让一个没有信息的一方决定延迟预算 —— 结果一定是"有时白等、有时切在半句"。
**为什么不能在网关**：网关是**多用户共享件**。任何语义放在网关，都会变成"所有用户共用一套投递策略"，而投递策略的正确值依赖**单轮的实时状态**（深答还有多久到）。网关只能承载字段，不能承载判据。

> **一句话**：A/B 不是数据通路的岔口，**是同一条通路上的一个策略旋钮**。做成岔口 → 你会有两套渲染路径、两套验收、两套 bug。

## 3.2 如果选 B，"句子"的定义权归谁？—— **会蔓延，除非单点定义 + 显式字段**

**先看一个事实：宿主模型里没有"句子"这个单位。** DSH 会话模型只有两个粒度：`assistant/chunk`（token 级增量）与 `assistant/message`（`surfaceOp: 'append'` 的整条消息，且替换副本明确是"model-only、不进人类 transcript"）。
> 也就是说：**"句子"是一个需要被发明出来的单位**。凡是需要被发明的单位，如果不在某处被单一地定义，就一定会被每一层各自发明一次。

B 方案下，"句子"的消费者至少四个：

| 消费者 | 它想要什么 |
|---|---|
| 接收层（生成时） | 我该在哪断句 |
| 控制器（判决时） | 现在这一句写完了没有 |
| 协议（投递时） | 这一片是不是一个完整单元 |
| 客户端（渲染时） | 我要不要等下一片 |

四处各写一版标点规则 → **"接管点落在半句中间"会变成偶发、不可复现、且无法定位的 bug**。这正是你担心的"跨层蔓延的隐式约定"，而且它一定会发生。

**唯一的防法（若真做 B）**：
> **切分只在控制器里算一次**，产出 `Segment { index, text, boundary: 'sentence'|'eos', turnId }` 作为**协议字段**下发；接收层不切、协议不切、客户端不切。
> 确定性规则（标点 + 最小长度 + 不切代码块/列表内部）+ **单测覆盖边界样例**。
【保证者】单一实现 + 协议字段（客户端拿不到"如何切"的规则，只拿结果）+ 一条断言"客户端渲染的文本 === 控制器 commit 流的前缀"。

## 3.3 第三条路（我推荐的）：**A 做通路，B 降级为"生成纪律"，中间加一个极短的推迟窗口**

**结构**：
```
数据通路：始终逐字流式（A）—— 不引入第二套渲染路径
可切性  ：变成接收层的【生成纪律】，而不是投递协议
          （短句、每句语义自足、禁跨句指代开头如"这个/它/另外"）
推迟窗口：深答已就绪时，控制器【最多再等一句完成】（≤ ~400ms）再接管
```
**为什么这条路代价最小**：
- **不引入缓冲** → 不引入沉默期（B 方案最大的副作用：为了凑一个句号，用户会看到"打字停了"，这恰恰是 seamless 文档 §4.5 最想避免的东西）。
- **不需要预测** → 不需要"预测到深答即将接管"这个未验证的机制。**"等一句"是不需要预测的**：它只在"深答已就绪"这个**已知事实**上生效（这正是 v1 已有的 `deferWhileThinking` 的判据升级：从"还没开口"改为"当前句未完成"）。
- **不产生跨层契约** → "句子"只服务生成质量评估，不进协议。

**代价（必须诚实列出）**：
| 代价 | 量级 | 谁承担 |
|---|---|---|
| 接管时刻最多多等一句 | 快层生成速率下约 200–400ms | 用户体验（可忽略；且它换来"不切在半句"） |
| 控制器需要**字符级流 + 标点判定** | 进程内极低成本（G2 前提） | 控制器复杂度 +5% |
| 接收层句式被约束（短句自足） | **可能与"给方向"的信息量冲突** | **产品权衡，必须 PM 拍板**：短句会牺牲一点连贯感 |
| 仍非 100% 安全 | 长句/引用/代码块内的切点 | 兜底：**宁可推迟到句号，也不硬切**；超过上限仍硬切时 → 强制走 S4 纠偏模板 |

**判决**：**推荐 A（逐字流式为默认通路）+ 一句推迟窗口**；**B 不做成协议**，只保留其"生成纪律"的那一半。
如果未来真需要 B（例如弱网下频繁出现半句接管），再把 3.2 的单点切分加上 —— 那时它是一层**可回滚的增量**，不是今天就要付的架构债。

---

# 四、演进路径：从今天到 v6 全貌，怎么走才不返工

## 4.1 判据：什么算"必须一开始就有"

一条能力必须现在就有，当且仅当满足下列**任一条**：
1. **它是历史数据的形状**（后面加 = 历史轮次没有这个字段，永远补不回来）；
2. **它是所有调用点的公共必经之路**（后面加 = 满仓库找调用点，必然漏）；
3. **它的缺失会让某条 R 纪律在结构上不可实现**。

## 4.2 L0 · 地基（现在必须做，否则一定返工）

| # | 内容 | 命中哪条判据 | 后加会怎样 |
|---|---|---|---|
| 1 | **事实源 + `emittedText` 落盘** | 1（历史形状）+ 3（R5 的物理前提） | 历史轮次无法判断"反馈层承接的是不是用户看到的"；衔接质量不可回溯 |
| 2 | **单一装配入口 + append-only** | 2（公共必经之路）+ 3（R2） | 全部 prompt 结构重写 + 缓存全废 |
| 3 | **唯一出口 `emit()`（含 speaker 标签与 turnId）** | 2 | 散落的输出点收不回来；"一个声音"与多用户审计同时不可判 |
| 4 | **`turnId` + 幂等 + 每轮 trace** | 1 | 无法回放、无法对账、无法 A/B；成本基线被污染后不可恢复 |
| 5 | **`openMemory(userId)` 能力句柄** | 2 + 3（R1） | 全仓库调用点重写 |
| 6 | **`Decision` / `Utterance` 类型分离 + `noReply` 可表达** | 3（R4 与被动模式） | 见下 §4.4 —— 这是最容易被低估的一条 |
| 7 | **`maxDepth` / 子 agent 并发上限 / 重试预算显式化** | 3 | 现在是静默默认值（depth=3、无并发上限、含 EMPTY_RESPONSE 的 5 次重试）—— **不用等做级联，任何一次子 agent 使用都会撞上** |
| 8 | **协议版本字段 + `attachments[]` / `taskRef` 占位** | 1 | 客户端消息模型一旦是纯文本，后加附件要改数据层 + 历史消息兼容 |

## 4.3 L1 · 现在可做、也可延后一周不返工

- 两块回答的衔接策略（S1–S4 形态表）、一句推迟窗口、首句锚点校验、六种失败模式场景测试。
- 它们**只消费 L0 的接口**，所以延后一周只是晚一周。

## 4.4 你问的三件事，逐件回答

### ① 说话人识别 —— **接口必须现在预留，实现可以完全后加**

**必须现在有（成本≈0，后加必返工）**：
1. **信号位**：`SpeakerSignals` 类型（`{ speaker, confidence, pointedAt, addressedBy }`），第一版填常量 `{speaker:'owner', confidence:1, pointedAt:true}`。
2. **判决是纯函数 + 出口断言**（1.4）。这是最关键的一条：**只要出口唯一，识别器就永远是一个可替换的 signal provider**。
3. **"不出声"必须是一个可表达的状态**（`Decision.action = 'silent'`）。

**为什么第 3 条最容易被低估**：如果第一版到处默认"接收层一定会产出 utterance"（装配时预设 transcript 非空、trace 里 `spoken` 必填、验收标准里"每轮必有一条回复"），那么加被动模式时你要满仓库去找"哪些地方假定了一定会说话"。**这是个典型的、三个月后才会爆的返工。**

**可以干净后加**：VAD、声纹服务、diarization、被点名检测模型、合规 UI、会话语料隔离区。
**唯一的结构性依赖**：被动语料"不逐轮注入"这条 —— 它要求装配入口支持"有上下文但不产生输出"的形态，**这就是第 3 条**。

### ② 交付形态四档 —— **字段必须现在有，渲染器可以后加**

**必须现在有**：`deliveryHint`（建议）+ `delivery`（裁决）+ `attachments[]` + 上行通道字段 + **客户端"一条消息可带附件"的渲染形态**。
**可以干净后加**：SDUI 渲染器、WebView 容器、任务状态机、页面仓库、生成物审核、T2/T3/T4 的实现本身、T4 的 `originTurnId`。
**唯一会返工的点**：客户端消息模型。如果第一版消息是"纯文本字符串"，后加附件 = 改数据层 + 兼容历史消息 + 改渲染。**成本很低但现在做更便宜。**

### ③ 级联 —— **只有"限制"必须现在有，"能力"完全可以后加**（三件里唯一能干净后加的）

- **必须现在**：装配入口唯一（已有）+ **`maxDepth=2` 与并发上限显式设置**（即使第一版不 fan-out）+ trace 落 `nodes_spawned / nodes_per_depth / total_llm_calls`。
- **可以后加**：fan-out、融合节点、准入规则、专家 persona。
- **为什么不返工**：级联只消费装配入口，不改变任何跨层契约。**但有一条必须现在写进设计假设（否则会做出错误期望）**：
  > **融合节点与专家节点之间永远 0% 前缀命中**（父/子结构固定文本块差异，结构性必然）。所以"融合节点要快"这个期望不能写进设计 —— 而融合节点恰恰是决定反馈层何时开口的那个。

## 4.5 "地基破了"的红旗（建议直接做成 CI grep + 评审清单）

出现下列任一形态，说明边界已经被绕过，**必须视为架构事故而不是代码风格问题**：

| 红旗 | 被绕过的不变量 |
|---|---|
| 出现第二处构造 prompt/上下文的地方 | 装配入口唯一（R2 随之失效） |
| 出现控制器产出的用户可见文本 | `Decision ≠ Utterance`（1.2） |
| 出现未携带 `speaker` / `turnId` 的对外文本 | 审计与承接（1.2/R5） |
| 出现向"当前尾部"以外位置追加的块 | R2（唯一例外须走 `rebuild`+令牌） |
| 客户端出现"自行合并/自行切分"逻辑 | 分组键与切分权归属（3.1/3.2） |
| 出现读记忆的接口同时接受 userId 与句柄 | R1 能力式隔离 |
| 出现"跨轮的尾部追加" | R2 与 T4 的冲突未拍板（见第六节） |

---

# 五、我看到的最大架构风险（3 条）

## 风险 1 · "已输出文本"没有单一权威（触发最早、破坏面最广）

- **触发条件**：任何一次接管判决依据"模型已生成"而不是"控制器已放行"的位置。它不需要谁犯错 —— **默认实现就会撞上**：模型吐字快于控制器落盘、客户端渲染慢于两者。
- **爆炸半径**：同时击穿三件事 —— ① R5（反馈层承接用户没看到的字 → 重复/错位）；② S4 纠偏（纠偏对象错位 → 用户看到"我先前怀疑下游"但用户根本没看到那句）；③ **多用户审计**（哪句话是系统注入、哪句话是用户看到的，无从判定）。而且**不可回溯**：历史轮次没记过 `emittedText`。
- **设计阶段如何避免**：
  1. 把状态机的轴从"模型生成"改为 **commit 流**：`generated → committed → rendered`，**只有 `committed` 是事实源**，落盘。
  2. `emit()` 是唯一 commit 点；装配入口只接受 `committed` 前缀。
  3. 验收写成可判定项：**外发流的前缀必须逐字节等于客户端渲染的文本**（一条断言，弱网下用 seq 补齐验证）。

## 风险 2 · "谁说话"的唯一出口被非确定性组件突破

- **触发条件**：任何一次省事 —— 让接收层自己说"我先去看看"然后结束；让 T2 卡片自带标题文案；让错误态由工具直接产出文案；让被动模式的"状态提示"顺手写成一句人话。
- **爆炸半径**：`speaker` 标签失去意义 → "一个声音"不可审计 → **被动模式下必然出卖用户**（会议里助手忽然说了一句话，而没有任何记录说明它是谁说的）。这是产品级事故，且与 source.kind 那条硬事实同源：**一旦系统注入的文本与用户可见文本的边界模糊，你就再也分不出哪句是被注入的**。
- **设计阶段如何避免**：
  1. `Decision` 与 `Utterance` 用 brand 类型隔离，控制器**在类型上没有嘴**。
  2. 所有外发（含 SDUI/H5 内的文案、错误态、状态提示）走同一个 `emit()`；
  3. **状态提示是 UI 事件不是消息** —— 两者在类型与协议上就是两种东西，不能互相转换（否则"状态代替语言"会在第一次方便时变成"状态加一句话"）。

## 风险 3 · "进程内"被误当成"状态在进程内"（最架构的一条）

- **触发条件**：控制器把内存态当作事实源（"唯一真源 = 控制器自己的记录"这种表述会诱使实现这么做），或把审计/指标留在进程内。
- **爆炸半径**：① 进程崩溃 = 记忆双写/丢失（REVIEW-eng 炸点 7，且是**静默**污染 hits）；② **故障边界与隔离边界不重合**：网关按 userId 路由、进程边界隔离凭证，但重启即失忆 —— 于是"每用户一进程"这个隔离承诺在重启时失效；③ 8–12 人的容量上限来自内存，而内存里装的应该是**可再生的工作集**，不是事实源。
- **设计阶段如何避免**：
  1. 把"进程内"限定为**执行位置**，不是**状态位置**：控制器**无内存态** —— 所有决策从落盘事实源 + 本轮输入重算。
  2. 唯一真源 = append-only JSONL + `turnId` 幂等（评测已经给过这个正解，这里补的是**它在边界上的含义**：真源不能在进程内）。
  3. 容量表里明确写"**重启恢复时间**"这一列（现在只有内存/磁盘两列），因为它是这个架构里唯一不可省略的恢复成本。

---

# 六、给另外两位同行的提醒（每人一条）

## 给产品经理：**你要砍掉的东西里，有一半是物理事实**

你的第一刀是"独立反馈层"，论证是"它解决的问题是拆成两个角色自己制造的"。
**这条论证对 v6 已经不成立一半了** —— v6 已把反馈层重新定义为"深思回答"（一块内容），不是"第二个接话的角色"。而你真正要处理的是 **"两块内容"**，它不来自架构选择，**来自延迟差这个物理事实**（首字 0.8–1.0s vs 深答 p50 6.5–7.9s）。

**所以必须更新的是这个区分**：
| 你要砍的 | 判决 |
|---|---|
| 独立的**反馈层 agent** | ✅ 可以砍（工程上可行，eng 也同意）—— 砍掉它之后"承接"变成同一个 agent 的自我延续 |
| **"两块回答"本身** | ❌ 砍不掉。它会在你的 C 臂里**照样出现**：第一句（被 steer）+ 后接结论。你砍掉的是机制，不是那个时间结构 |

而这里有一个你最容易忽略的架构事实：**在 C 臂（单 agent 被 steer）里，那个 agent 是在同一个上下文里先给方向、再被喂进结论的 —— 它比双 agent 更容易前后矛盾**（"我先怀疑是下游超时" → "结论是连接池"），而这正是 S4 纠偏，**现在没有任何组件负责它**。双 agent 至少有"一致性判定"这个位置，单 agent 没有。

**建议你的验收项加一条**：C 臂也要做"从哪句开始换人了"的盲评，而且**专测"方向被推翻"的轮次**（v6 已经把这四种衔接形态列清了，翻到 S4 那条）。
否则你会砍掉机制、保留（甚至放大）破绽，而且**没有任何数字能告诉你这件事发生了**。

## 给工程实践师：**你的不变量全部作用在"内容"上，而新能力作用在"时序"上**

你实测出的三件套（字节稳定性、同 depth 前缀指纹、maxDepth）都是**对上下文内容**的约束 —— 它们的前提是：**"一轮"是一个短的、可关闭的区间**。

本轮新增的两件事打破了这个前提：

1. **说话人识别**：同一段上下文的生成顺序变了（被动模式下**有内容但不出声**）。你的"每轮 trace 必落 spoken"这类验收会立刻产生假失败。
2. **交付形态 T4**：**"一轮"延长到分钟级。** 一个 3 分钟后完成的任务，它的 R5 要求"反馈层上下文含接收层全文" —— **哪一次的接收层全文？**

> **这是 v6 里没有任何文档回答过、但第一版 T4 落地时必然撞上的冲突：**
> 任务完成时若要装配上下文，前缀里已经追加了后面若干轮的内容；而 R2 不许改前缀。
> → 要么违反 R2（往中间追加），要么为任务单开一轮（于是这个结论无法严格承接）。

**这不是实现细节，是需要架构层拍板的取舍**，而且它正好落在你的检查清单之外。我的建议：
- **"一轮"必须被定义为一个可关闭的区间（turn boundary）**，T4 任务携带 `originTurnId` 归属开启它的那轮；
- 任务完成时的追加，**必须走一个显式的跨轮机制**（不是伪装成尾部追加）——否则 R2 会被这一次"例外"永久打开；
- 你的清单里加一列：**"这条门禁在 T4 下还成立吗"**，因为你的门禁全是为"短轮次"设计的。

---

# 七、结论（≤5 行）

1. **我推荐方案 A**（逐字流式为唯一数据通路）**+ 一个"最多再等一句"的推迟窗口（≤400ms）**，把 B 降级为接收层的**生成纪律**（短句、每句语义自足），**不把"句子"做成跨层协议** —— B 作为协议的唯一后果是四层各自发明一次标点规则。
2. **三方最可能达成一致的三件事**：① 单一装配入口 + append-only 的**机器强制**（不是原则）；② 控制器必须是**进程内插件**（已被 `source.kind` 硬编码这一事实锁死，无争辩空间）；③ 第一版先用 **trace + 21 天真人日志**做测量，机制等数据。
3. **我最坚持的一件事**：**`emittedText`（控制器已放行的文本）必须有唯一权威并逐轮落盘** —— 它同时是 R5 的物理前提、无缝衔接的判据、纠偏的对象、多用户审计的边界；没有它，这三件事一起不可判定，而这四样东西以后再补不回来。
