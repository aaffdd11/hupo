# 用户记忆体系与分层 Agent 设计（v4 · 规划）

> 你补上了这个架构缺的最后一块：**记忆不是全局的，是"每个用户一份"的**，
> 而且记忆要**回流到接收层**，让接收层的回应本身就有智能，而不是"我听到了"。
> 本文定义记忆的分层结构、每层对应 Agent 的形态、以及记忆如何改变接收层的第一句话。

---

## 第一部分 · 记忆的归属与结构

### 1.1 一条铁律：记忆属于用户，不属于任何 Agent

四角色都**不拥有**记忆，它们只是记忆的**读写方**：

| 角色 | 对记忆的关系 |
|---|---|
| 接收层 | **只读**（读"简报"，见 2.2） |
| 处理层 | **读 + 写提案**（分类时可读，产出结论后写提案） |
| 反馈层 | **只读**（读结论与前情，用于承接） |
| 控制器 | **唯一写入者**（决定什么进记忆、进哪一层、是否晋升） |

为什么必须是控制器独占写权：
- 三个 Agent 各自写记忆会互相打架（同一个问题被记三次、品类路径不一致）；
- 记忆的"该不该记"需要跨角色信息（处理层的结论 + 用户后续反应），只有控制器有全貌。
- **写提案 / 确认入库**两段式：处理层产出结论时提交提案（状态 `pending`），
  反馈层说出去、用户没有纠正，控制器才提交生效（状态 `committed`）。
  这样"答错了的记忆"不会污染后续所有轮次。

### 1.2 三层品类（按你的定名：大类 / 中类 / 小类）

沿用 v0.1 已验证的三级结构，正式定名：

```
用户
 └─ 大类 Domain        （10 级，如：技术 / 写作 / 生活 / 事务）
     └─ 中类 Category  （60 级，如：编程 / 数据 / 小说 / 健康）
         └─ 小类 Subcategory（300 级，如：接口故障排查 / 睡眠 / 标题打磨）
```

**小类是记忆的最小单元**，也是经验的落地位置（一条经验 = 一个小类下的一个问题模式 + 解法）。

### 1.3 一个用户就一份记忆，一层层长出来

```
记忆 = {
  档案 Profile      : 稳定偏好（说话语言、专业程度、讨厌什么、习惯用什么词）
  品类树 Taxonomy   : 由真实对话长出来的 大类/中类/小类
  经验 Experiences  : 每个小类下的"问题模式 → 有效解法 → 坑"
  对话 Transcript   : 客观流水（不参与推理，只用于审计与检索）
  指标 Metrics      : 每类的访问频次、成功率、纠正率
}
```

**关键设计**：品类树、经验、指标三者构成**同一棵树上的三份数据**，
所以"某个中类值不值得升级成 Agent"这个问题，答案就藏在这三个数里（见 3.3）。

---

## 第二部分 · 记忆如何改变接收层（你最关心的那点）

### 2.1 现状问题

现在接收层的第一句是"我听到了/我知道了"——**因为它手里什么都没有**。
它只有一个近期的对话窗口，没有类别感、没有历史感，所以说不出有信息量的话。

### 2.2 解法：控制器给接收层发"记忆简报"

记忆简报是**受控的、结构化的、有置信度的摘要**，不是把记忆倒给它：

```
{
  本轮分类(初步):  技术/编程/接口故障排查    置信 0.92
  该小类经验:      命中 2 条（最近一次：3 天前，解法是"先查连接池"）
  该中类经验:      命中 1 条（大类级通用做法）
  用户档案相关:    该用户偏好看具体步骤，不喜欢长篇解释
  历史关联:        该用户 5 天前问过同类问题，当时的结论是 X
  可安全引用的:    小类高置信经验（置信 ≥0.8）
  禁止引用:        低置信/未确认的记忆
  本轮要点:        这个问题属于"偶发故障排查"，这类问题的通法是先缩小范围
}
```

**接收层拿到的不是"要说什么话"，而是"我知道什么"** —— 语言仍由它自己组织。

### 2.3 于是第一句话变成了有信息量的样子

| 场景 | 改前（无记忆） | 改后（有记忆简报） |
|---|---|---|
| 该小类有高置信经验 | "我听到了，我看看" | "这个我熟，你上次也是这个接口的问题，我按上次那条路再看一遍" |
| 该小类第一次遇到 | "我听到了" | "这类偶发故障我看过不少，先看是不是高峰期才出现？" |
| 用户是老用户，有偏好档案 | "我听到了" | "收到，我直接给你步骤，不绕弯子" |
| 记忆里有冲突/不确定性 | "我听到了" | "这个问题有两种可能，我先确认是哪一种" |

> 注意第三行：**它引用的不是知识，是"对这个人的了解"**。
> 这是"智能响应"和"复读机"的分界线 —— 用户会立刻感觉到"它记得我"。

### 2.4 三条硬约束（防止记忆变成幻觉）

记忆越强，编造的风险越大。必须写进接收层 persona 并作为控制器的过滤规则：

1. **只引用"可安全引用"清单里的内容**（控制器按置信度筛选后下发），其余一律不许提。
2. **引用必须可追溯到具体记忆**。控制器在下发简报时带 `证据id`，事后可审计：
   如果接收层说了记忆里没有的东西，那就是幻觉，要能查出来。
3. **低置信记忆只能影响"怎么答"，不能出现在"说什么"里。**
   即：可以悄悄把它作为方向偏好（比如更倾向于先问澄清问题），但不能说"我记得你说过 X"。

**一个真实的风险要提前防**：过度拟合。用户偶然提过一次失眠 → 之后每次都被"关心睡眠"。
防法：**记忆引用要有频次与新鲜度门限**，并且**对同一类记忆的引用次数设上限**，
引用过多时控制器主动衰减该记忆的权重。

---

## 第三部分 · "每个类别有自己的 Agent" 怎么落地

### 3.1 直接给 300 个小类各建一个 Agent 是不现实的

成本与启动时间都不是小问题：每个 Agent 有独立会话、独立上下文、独立生命周期。
冷启动建 300 个 = 300 个会话，绝大多数永远用不上。

### 3.2 混合方案：轻量"经验包" + 按需升格为真 Agent

| 形态 | 适用 | 成本 | 能力 |
|---|---|---|---|
| **经验包**（默认） | 绝大多数小品类别 | 极低（就是结构化记录的检索） | 提供"这个类别的既有做法"，由处理层直接使用 |
| **专属 Agent**（升格） | 高频 / 高价值 / 高难度类别 | 一个真 Agent | 该类别有独立方法论、独立工具、可长期积累 |

**这种"经验包 → Agent 升格"的设计，本质上和数据库的"视图 → 物化视图"是同一种思路**：
先用便宜的方式覆盖全部，再对真正被频繁使用的少数做物化。

### 3.3 升格的判定条件（用记忆里的数据说话）

控制器监控每个中类/小类的三个数，达到任一条件就**提议**升格：

| 信号 | 阈值（建议，可调） | 含义 |
|---|---|---|
| 访问频次 | 最近 30 天 ≥ 8 次 | 这个类别是真需求 |
| 反复纠正 | 纠正率 ≥ 0.3 | 现有经验不够用，需要专门积累 |
| 处理时长异常 | p50 超过全局 2 倍 | 这个类别就是难，值得专门优化 |
| 结论复用率 | 同小类第二次提问仍无法走经验捷径 | 经验没沉淀住 |

**升格动作分三步**：控制器提议 → 人工确认（或高置信自动）→ 建 Agent 并继承父子经验。
**降格也要有**：连续 90 天访问 < 2 次 → 冻结为经验包，释放 Agent。

### 3.4 分层 Agent 的经验继承（对应你说的"小类继承大类"）

```
小类 Agent 的上下文 = 小类专属经验
                    + 中类经验（升格时注入）
                    + 大类经验（升格时注入）
                    + 跨类通用经验
```
由**控制器在分配任务时组装**，而不是让 Agent 自己去查（Agent 自己去查会查错路径，
v0.1 实测踩过：分类器输出 `技术/后端开发/…`，树里只有 `技术/编程/…`，一不匹配经验链就断了）。
控制器负责"路径归一化 + 模糊匹配 + 借用策略"，Agent 只负责用。

---

## 第四部分 · 记忆在三个角色间的流动（完整链路）

```
用户说话
   │
   ▼
控制器：① 记忆检索 —— 用关键词/向量粗定位候选类别与经验
   │        （必须快，压住接收层的首字延迟；粗排后精排）
   │
   ├─► 接收层：拿到【记忆简报A（初步）】→ 说出"有信息量的第一句"
   │
   └─► 处理层：拿到【记忆简报B（完整）】+ 任务
            │
            ├─ 分类（大类/中类/小类）→ 路径归一化 → 必要时建新小类
            ├─ 召回经验（小类 → 中类 → 大类 → 通用，带相似度闸门）
            ├─ 深度研究（专家/工具）
            └─ 产出结论 + 【记忆提案】
                   │
                   ▼
       控制器：暂存提案（pending）
                   │
                   ▼
       反馈层：承接并说出结论（携接收层已输出全文 + 一致性判定）
                   │
                   ▼
       控制器：观察用户反应（未纠正/纠正/追问）
            ├─ 未纠正 → 提交记忆（committed）：经验入库、指标更新、品类树按需长叶
            ├─ 纠正   → 标记该经验作废或降权，并回写"修正后的经验"
            └─ 追问   → 该经验权重提升（说明是有效信息），并触发更深一层的检索
```

**两个时序细节很重要**：

1. **记忆检索必须发生在接收层开口之前**，且必须是"粗查"。
   如果记忆检索比接收层还慢，接收层就退化成复读机。所以需要**二级检索**：
   粗查（毫秒级，关键词/类别索引）给接收层；精查（带相似度计算）给处理层。
2. **不要等用户反应再记**。提案先暂存，随后提交，避免"用户没说话就不记"导致记忆永远长不大；
   但**被纠正后必须能回滚**——所以经验条目要有 `状态`（pending / committed / retracted）。

---

## 第五部分 · 数据结构（可直接落地的形状）

```jsonc
// 用户档案（每用户一份）
{
  "userId": "u_xxx",
  "profile": {
    "verbosity": "步骤优先，不喜欢长解释",
    "language": "zh",
    "expertise": { "技术/编程": 0.8, "生活/健康": 0.3 },
    "bannedTopics": [],
    "updatedAt": 0
  }
}

// 品类节点（大树 + 中类 + 小类共用一张表）
{
  "path": "技术/编程/接口故障排查",
  "level": "subcategory",
  "visits30d": 9,
  "correctionRate": 0.11,
  "p50DurationMs": 6400,
  "agentState": "experience-pack",   // experience-pack | agent | frozen
  "agentId": null,                   // 升格后填 agent 的 sessionId
  "createdFrom": "dialogue",         // seed | dialogue
  "updatedAt": 0
}

// 经验条目
{
  "id": "exp_123",
  "path": "技术/编程/接口故障排查",
  "level": "subcategory",
  "pattern": "接口高峰期偶发 500",
  "solution": "先查连接池与线程池是否打满",
  "pitfall": "先别改代码，容易误判",
  "hits": 3,
  "confidence": 0.86,
  "state": "committed",             // pending | committed | retracted
  "evidence": ["turn_18", "turn_42"],
  "updatedAt": 0
}
```

`evidence` 字段是**可审计性的落点**：接收层引用了记忆，就能一路查到是哪几轮对话支撑的。

---

## 第六部分 · 这对已有设计的影响

| 原设计 | 变化 |
|---|---|
| 控制器只做调度 | 控制器同时是**记忆的唯一写入者与检索者** —— 职责明显变重，可能要拆成"调度"与"记忆"两个子进程 |
| 接收层"只说在做什么" | 升级为"用记忆说出有信息量的第一句"，但仍受三条硬约束约束 |
| 处理层直接查经验 | 改为**控制器组装好经验包再交付**，处理层不自己查（避免路径查错） |
| 品类树只是分类 | 品类树同时是**记忆索引 + Agent 升格判据 + 经验继承链** |
| 反馈层只读结论 | 增加读取"接收层已输出全文 + 一致性判定" |
| 经验账本（v0.1 已实现） | **升级**：加 `state` 三态、加 `evidence` 可追溯、加层级继承与升格字段 |

v0.1 原型里已经实测有效的两条纪律继续适用，而且现在更关键：
- **相似度闸门**（覆盖率 0.6 + Jaccard 0.4，阈值 0.18）：防止"归类对了但问题不同"的张冠李戴
- **路径归一化 + 模糊兜底**：防止分类路径与记忆路径不一致导致经验链整条失效

---

## 第七部分 · 待确认

1. **记忆检索的时机**：我建议"粗查给接收层（毫秒级，只给类别与命中与否）+ 精查给处理层（带相似度）"。
   你更倾向哪一种：接收层拿到完整记忆（更有料但更慢），还是只有粗查结果（更快但更保守）？
2. **升格为专属 Agent 的触发方式**：全自动（达到阈值就建）/ 提议后人工确认（我建议）/ 手动。
3. **跨用户共享**：不同用户之间**经验是否可共享**（比如"接口故障排查"的通法对所有用户都成立），
   还是严格隔离？我的建议是**分两层**：通用方法论可共享，个人偏好与隐私严格隔离。
