// **打开一个外面的地址**（出处那几行要能点开）。
//
// 契约：`docs/dev/67-SOURCES.md`。
//
// ── 为什么单独一层，而且这么小 ────────────────────────────
// ① **平台差异只在这一个文件里**：网页上能开新标签页；别的地方（今天的测试环境、
//    将来的安卓包）**打不开**。
// ② 🔴 **打不开的时候不许画一个"按不动"的按钮**（`AGENTS.md` §六 第 4 条那一族：
//    界面上不许出现做不到的东西）。⇒ `canOpenLinks` 是假的时，调用方传 `null`，
//    出处就**只当文字显示** —— 看得见，但不当按钮。
// ③ **没有新依赖**：和 `mini_runtime.dart` 同一条路（条件导出 + 各平台一份），
//    网页那份用 `dart:html`，跟这个仓库里已经在用的写法一致。
//
// ⚠️ **只认 http(s)**：`javascript:` / `data:` / `file:` 一律不开
//    （出处是模型查回来的东西，不能因为上游给了一个怪 URL 就执行它）。

export 'links_stub.dart' if (dart.library.html) 'links_web.dart';
