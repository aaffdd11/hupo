// **把它说的话念出来**（听筒那一半 · 主人 2026-09-23 定案）。
//
// 契约：`docs/dev/68-SPEAK.md`（形状、判据、没做的）。
//
// ── 三条纪律 ──────────────────────────────────────────────
// ① **走设备自带的合成器**（浏览器/系统的朗读）。我们**不把文字送到任何地方** ——
//    这个文件里没有任何网络调用。（⚠️ 有些系统的音色**自己**是云端的：
//    那和"系统读任何一段文字"一样，是那一层自己的事，**不是我们在传**。）
// ② 🔴 **念不出来就别画按钮**（`canSpeak` 为假 ⇒ 调用方传 `null`）——
//    界面上不许出现按不动的东西（`AGENTS.md` §六 第 4 条那一族）。
// ③ **一次只念一段**：新的开念之前先把上一段停掉（`cancel()`），
//    而且**停得掉**（用户点"别念了"、关掉开关、离开这一屏都得停）。
//
// ⚠️ 网页上是 `window.speechSynthesis`；别的平台这一份是桩（`speech_stub.dart`）。
//    与 `links.dart` / `mini_runtime.dart` 同一条路（条件导出 + 各平台一份，**零新依赖**）。

export 'speech_stub.dart' if (dart.library.html) 'speech_web.dart';
