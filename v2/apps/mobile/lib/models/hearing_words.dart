// 语音那一步（**真开麦**）在界面上的字。契约：`docs/dev/71-MIC-ASR.md`。
//
// ⚠️ **界面上的字只有这一处**（D3.10）。别在 widget 里写字符串。
// ⚠️ 不许出现内部词（`AGENTS.md` §六 第 4 条）：这里说的都是用户看得见的事
//    —— "麦克风权限"、"没配好"，不是"连接 / 客户端 / 云端 / 工具名"。
//
// 🔴 这一版把主人 2026-09-22 那个**假形状**（`按住 说话` + `演示` 小标）**砍了**：
//    主人 2026-09-23 定案 —— *"我们那个页面，并没有按钮。你做一下按钮。
//    是按一下开始语音跟踪，实时转化语音成文字。再按一下结束。
//    然后将文字展示出来。用户可以选择发送。"*
//    ⇒ 按一下开始 / 再按一下结束，**没有再"按住"那一套**（声明砍掉，见契约 §五）。

/// 按一下开始（闲的时候）。
const String hearStart = '按一下 说话';

/// **录音标记**那三个字（和那个红点一起显示在实时文字上面）。
/// ⚠️ **"正在听"只在真的在听时出现**（D5.13）—— 而它现在是一个**一眼看得见的标记**：
///    红点 + 这三个字 + 实时文字，2026-09-23 主人问"为什么录音的 icon 没有"之后改的
///    （原来只有按钮上那行字，看着不像"在录音"）。
const String hearListening = '正在听';

/// 正在听时那颗按钮上写的（按一下就是结束）。
const String hearStop = '再按一下 结束';

/// 在听、但还一个字都没认出来时，那一块里显示的（**和按钮上那句不一样**：
/// 同一屏上同一句话画两遍，看着就像坏了）。
/// ⚠️ 它是真的：这会儿麦克风确实开着（D5.13 管的是"没在听时不许说听"）。
const String hearListeningEmpty = '我在听 —— 你说话就行';

/// **半路断了**（不是用户自己按停）—— P1-3。
/// ⚠️ 这时候要：**字留着**（他已经说出来的那半句）、**说明白**、**别切走**
///    ⇒ 他再按一下就是"接着刚才那句说"（新的一句接在已有的字后面）。
const String hearCutOff = '刚才断了 —— 字都留着，按一下接着说我听着';

/// **按了结束、等最后一句**的那一小会儿（通常不到一秒，但**必须说出来**：
/// 屏幕上那几个字还没定下来，这时候什么都不显示，用户会以为说丢掉了）。
const String hearFinishing = '收尾中…（把最后一句接住）';

/// **这里开不了麦**（不是网页 / 不是 https / 原生那个包里没做录音）。
/// 🔴 2026-09-23 主人问"为什么录音的 icon 没有" —— 原来这一档是**把话筒藏起来**的，
///    于是他看到的是"功能没了"。改成**话筒一直在、点它说明白话**：
///    藏起来等于让用户自己猜，摆着说清楚才是"页面说实话"。
const String hearCantHere = '这里开不了麦（换个浏览器打开就能用）';

/// 这一批的形状（说清楚按下去会发生什么，别让用户猜）。
const String hearHint = '说完再按一下，字会留在这儿 —— 你自己决定发不发。';

/// 这台部署还没配钥匙（**如实说**，不装开麦）。
const String hearUnavailable = '语音这条路还没配好（缺钥匙）';

/// 浏览器没给麦克风权限。
const String hearDenied = '没拿到麦克风权限 —— 在地址栏那个锁里放开麦克风，再按一次';

/// 开麦这一步本身失败了（没有麦克风、被系统拒、音频图起不来…）。
const String hearFailed = '开不了麦克风，再按一次试试';

/// **这条连接没接上**（不是麦克风的事：中间那一跳不认这条路径、网断了…）。
/// ⚠️ 它必须和 [hearFailed] **分开说** —— 混成一句就是页面在说假话。
const String hearNoEntry = '这条路暂时连不上，等会儿再按一次';

/// **这条路没额度了**（腾讯回 `4004 资源包耗尽`）。
/// ⚠️ 它和 [hearEngineFailed] **分开说**：没额度是"去开通/买资源包"，
///    识别出错是"再试一次" —— 混成一句，用户就不知道该干什么。
const String hearNoQuota = '语音这条路还没开通好（没额度了）';

/// 识别那一头出错了。
const String hearEngineFailed = '识别那一头出错了，再按一次试试';

/// 到点了（内测版一条连接最多一分钟）——服务端说的，不是我们猜的。
const String hearCapped = '一次最多说一分钟，我先停了';

/// 一个字都没听着。
const String hearNothing = '什么都没听到，再按一次试试';

// ── ★ 配置页「语音」那一屏那颗「试一下」（主人 2026-09-26）────────────────
//
// 主人原话：*"配置页，语音配置上，增加一个测试按钮，点击后会录音，会转文字，
// 并写入一个文本框。"*
//
// ⚠️ 这一组只是**这一屏自己的几行字**；开麦那几句话（`hearDenied` / `hearNoEntry`
//    / `hearNoQuota` / `hearNothing` / `hearCapped`…）**照旧共用上面那一套** ——
//    同一件事不许有两种说法（判据 `test/unit/voice_try_test.dart` 逐条钉着）。

/// 那一块的抬头。
const String voiceTryTitle = '试一下';

/// 按一下开始（闲的时候）。
const String voiceTryStart = '试一下 说话';

/// 正在听时那颗按钮上写的（再按一下就是结束）。**不是**「正在听」那三个字。
const String voiceTryStop = '停下';

/// 收尾中（按了停、还在等最后一句）那颗按钮上写的。
const String voiceTryWorking = '收尾中…';

/// 一句怎么用（**说清字会落在哪儿**，别让人猜）。
/// ★ 2026-09-26：加上"中间停一下也接着听" —— 主人要的是"再点一下才结束录音"，
///    引擎自己在停顿处收尾**不算**他说完了（见 `123-VOICE-TEST-BUTTON.md` §八）。
const String voiceTryHint = '按一下开始，再按一下才结束；中间停一下也接着听 —— 认出来的字会落在下面那个框里。';

/// 下面那个框的标签（**空着的时候也要挂在那儿**）。
const String voiceTryBoxLabel = '认出来的字（可以选、可以复制）';

/// **读不懂的一帧**（识别那一头回了个我们认不出的东西）。
/// 🔴 它和「开不了麦克风」**必须分开说**：麦克风那一步明明成了，混成一句就是假话。
const String voiceTryBadFrame = '那边回了个读不懂的东西，再按一次试试';

/// **这台还没配好语音钥匙**（上面那三样还没送出去）——
/// 说清"上面那三样就是它要用的"（主人要的就是这句）。
const String voiceTryNoKeyEmpty = '这台还没配好语音的钥匙 —— 上面那三样填好、送出去，试一下用的就是它们。';

/// **上面那三样填过、这边却说没接通**（这台还没接上）。
/// ⚠️ 与上一句分开：一个让他去填，一个让他等接好 —— 混成一句他就不知道该干什么。
const String voiceTryNoKeyFilled = '这边说语音还没接通 —— 上面那三样它现在还用不上，得等这台接好。';
