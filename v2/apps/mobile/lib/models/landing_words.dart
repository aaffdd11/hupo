// Landing（未登录时的第一屏）的**全部文案**。
//
// ⚠️ 为什么单独一个文件：这一屏上的每一句话都要过**禁用词硬闸**
//    （手册 `07-APPENDIX.md` §2.2：界面上出现内部词 = **缺陷**，不是文风问题）。
//    把文案集中在这里，闸才有一处可扫（`test/unit/forbidden_words_test.dart`）。
//
// ⚠️ **纯数据，不 import flutter**（同 `trash_words.dart` 那几份的规矩）。
//
// 版式参考：主人 2026-09-21 点名的 **gengshu.me**（暖米色底 + 一个暖红强调色 +
// 药丸按钮 + 柔和卡片 + 居中小标题 + 手机示意图）。
//
// ── 🔴 2026-09-22 主人："总结整个项目，用最白话的语言，告诉用户这是以什么项目，
//    有什么能力。然后重新规划设计首页内容，能让用户看明白。" ──────────
//
// 这一版就是照那句话重排的。**五条设计原则**（改文案之前先读一遍）：
//
//   1. **第一句就回答"这是什么"** —— 用**具体身份**（"替你办事的助理"），不用形容词；
//   2. **能力用"事情"说**，不用形容词（不写"智能""强大""高效"）；
//   3. **先给"现在能用的"，再说"还不能用的"** —— 诚实，但别当头泼冷水；
//   4. **每块只有一个主题，小标题本身就是那句话** —— 用户扫标题就能懂；
//   5. **不许出现内部词**（禁用词硬闸守着），也**不许写做不到的事**。
//
// ⚠️ **唯一那句承诺照抄手册 `01-PROJECT.md` 〇（一个字都不许改）**。
// ⚠️ **下载那几个入口必须如实**：今天**没有**任何安装包，点了就直说没上线，
//    **不许假装开始下载**（"说了做不到 = 撒谎"）。

/// 品牌名。和登录页、聊天页同一个词。
const String landingBrand = '琥珀';

/// 顶部小标（版式里的 kicker）。
const String landingKicker = '你说的事，它真会去做';

/// 产品承诺。**手册原文，不许改字**。
const String landingPromise = '你说的事它真会去做，不只是陪聊。';

/// 承诺下面那句解释。⚠️ 这一句要**第一眼就让人知道这是什么**（不是聊天机器人）。
const String landingLead = '它是一个替你办事的助理。你说一件事，它记住、去办，办完回来告诉你结果。';

/// 两个入口。
const String landingStart = '开始用';
const String landingDownload = '下载安卓版';

/// 副文案：说清下一步要什么 —— 别让他点进去才发现要手机号。
const String landingStartHint = '用手机号登录，第一次要花一点时间给它安个家。';

/// ⚠️ **如实说**：现在还没有安装包。点了就这句话 —— **不许假装开始下载**。
const String landingAndroidNotYet = '安卓版还没上线，先用网页版吧。';

// ── 它能做什么（四张卡）────────────────────────────────────
/// 这一块的标题。⚠️ 标题本身就是那句话 —— 扫一眼就知道这一块讲什么。
const String landingCanTitle = '它能替你做什么';

/// (标记, 一句话, 解释)。标记是单个字 —— 让人一眼记住这一条。
const List<(String, String, String)> landingCards = [
  ('记', '你说过的事，它记着', '关掉、重启、过几天回来，都不用重说一遍。'),
  ('办', '它自己去办', '该查的去查，该记的记上；办的时候你看得到它在做什么。'),
  ('实', '没办成就直说', '查不到就说查不到，不拿猜测凑一个答案。'),
  ('回', '删错了能拿回来', '删掉的东西先进回收站；想把自己的记录带走，能导出一段文字。'),
];

// ── 对照块：它跟"光是聊天"差在哪 ──────────────────────────
//
// 契约：`docs/dev/51-VS-CHAT.md`（**每一条都在那儿带证据**）。
// ⚠️ 这一块讲的是**形态上的区别**，别跟上面四张卡重复（那四张讲"能做什么"）。
const String landingDiffTitle = '跟聊天机器人不一样在哪';

/// 左边那一列的表头（是"别人"）；右边用品牌名（是它自己）。
const String landingDiffLeft = '光是聊天';
const String landingDiffRight = landingBrand;

/// 一行一条：(光是聊天长什么样, 它长什么样)。
const List<(String, String)> landingDiff = [
  ('给你一段回答', '给你一件事办完的结果'),
  ('换个话题就忘了', '你说过的事它留着'),
  ('说不清也猜一个', '没办成就直说'),
  ('东西放在别人那儿', '东西放在只属于你的那台机器里'),
];

// ── 在哪儿用 ──────────────────────────────────────────────
/// ⚠️ **能用的排在最前面**（现在只有网页版），不能用的如实标着。
const String landingDownloadTitle = '在哪儿用';
const List<(String, String, String)> landingPlatforms = [
  ('网页版', '现在就能用', '点上面的「开始用」'),
  ('安卓', '还没上线', '做好会放在这儿'),
  ('苹果手机', '还没上线', '做好会放在这儿'),
];

// ── 常见问题 ──────────────────────────────────────────────
/// ⚠️ 五条，覆盖第一次来的人最会问的五件事（"是什么 / 放哪 / 花钱 / 会不会编 / 弄错了怎么办"）。
/// **每一条都要是他自己会问的那句话**，不是我们想宣传的那句话。
const String landingFaqTitle = '你可能想知道';
const List<(String, String)> landingFaq = [
  ('它跟聊天机器人有什么不一样？', '聊天机器人给你一段回答就完了。它会把你的事记下来、自己去办，办完回来告诉你。'),
  ('我的东西放在哪？', '放在只属于你的那台机器里。别人看不到。'),
  ('要花钱吗？', '现在不要钱。你自己填一把自己的钥匙就能用。'),
  ('它会不会瞎编？', '它的规矩是：查不到就说查不到，办不成也直说，不拿猜的凑。'),
  ('我说错了、删错了怎么办？', '删掉的东西先进回收站，能拿回来；你也能把记录导出成一段文字带走。'),
  ('有语音吗？', '还在做。现在打字就能用。'),
];

/// 页脚那句小字。
const String landingFootNote = '正在一点点搭起来。现在能用的是网页版。';
