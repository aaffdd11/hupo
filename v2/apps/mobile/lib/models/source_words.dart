// 出处那几行上说的字（主人 2026-09-23 选的功能 · 契约 `docs/dev/67-SOURCES.md`）。
//
// ⚠️ **界面上的字只有这一处**（D3.10：一套外观一处出处；文案同一条规矩）。
// ⚠️ 不许内部词（`07-APPENDIX.md` §2.2）：这里**不写**"来源 / 引用 / 检索 / 接口 / 工具"——
//    就写他生活里的话："它查过这些地方"。
// ⚠️ 用户是谁：他**不会拼音**、读不懂术语（`docs/ux/06-retiree.md`）。

/// 出处那几行的抬头。
const String sourcesHeadWords = '它查过这些地方';

/// 气泡里**最多画几处**（住代码里；服务端最多给 8 条）。
///
/// ⚠️ 上限**不是**"剩下的就不说了"：超出的那几处用 [sourcesMoreWords] **如实报数**
///    （和计划条那条"还有 N 件"同一条规矩：宁可少画，不许少说）。
const int sourcesShown = 3;

/// "还有 N 处"。
String sourcesMoreWords(int n) => '还有 $n 处';
