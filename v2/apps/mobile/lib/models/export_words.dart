// 「导出」这一路**用户会看到的字**（契约 `docs/dev/30-EXPORT.md`）。
//
// ⚠️ 为什么文案单放一个 models 文件、不写在界面里：
//    摆在 `screens/` / `widgets/` 里的字符串，**禁用词硬闸够不着**
//    （`test/unit/export_test.dart` 拿**真那份**禁用词表扫这一份）。
//    —— 和 `trash_words.dart`、`notice_words.dart` 同一条理由。
//
// ⚠️⚠️ **"记录"和"时间线"一个都不许出现**：
//    "时间线"是禁用词表里的内部词；"记录"是 ⑳ 踩过一次的那个词
//    （`notice_words.dart` 顶上写着同一条，⑳ 的教训是它藏在说明里也会被读出来）。
//    ⇒ 说"这条对话"、说"记忆"。`test/unit/export_test.dart` 有一条断言单钉这两个词。
//
// ⚠️ 用词纪律：说人话。不许出现"工作区 / 客户端 / 服务器 / 会话 / 工具 / 搜索"
//    那些内部词（那是缺陷，不是文风问题）。
//
// ⚠️ 纯逻辑，**不许 import flutter/material**。

/// 顶栏那个入口，以及那一页的标题。
///
/// ⚠️ 位置（顶栏第几个）**等主人看过再定，不属于契约**（§四）；
///    这一件只保证"有一个能进去的入口"。
const exportTooltip = '导出';
const exportTitle = '导出';

/// 那一段字上面的一句：说清这一框是干什么的。
///
/// ⚠️ 说的是"**可以整段复制走**"，不是"我帮你保存/上传"——
///    这一件**不做文件下载、不做云备份**（§四），别让字比做的事大。
const exportHintLine = '下面这一段可以整段复制走，粘到哪儿都是这些话';

/// 那个复制按钮，以及按下去之后的两句话（**成没成都说** —— N11）。
const exportCopy = '复制';
const exportCopiedLine = '已经复制了';
const exportCopyFailedLine = '没复制成，长按上面那段字自己选一下';

/// 空对话时**唯一**该说的话（§四）。
///
/// ⚠️ 而且此时**不给框、不给复制按钮**：一个空白框会让用户以为"导出坏了"
///    或者"它什么都没记住"——两种都是屏幕在说假话。
const exportEmptyLine = '这条对话还是空的';

/// 没拿到（网 / 服务端）。
///
/// ⚠️ "令牌过期了"**不走这一句**：那一句和回收站是同一件事，用同一句话
///    （见 `trash_words.dart` 的 `trashUnauthorizedLine`），别再新造一个说法。
const exportLoadFailedLine = '没拿到，过一会儿再试';
