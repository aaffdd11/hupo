# 应用层三层架构规划（v2 · 取代 v0.1 的"插件式接管"）

> 前提变更：**在 agent 之上再建一层应用层**，由应用层决定"谁说话"。
> 因此 v0.1 里"一轮只能有一条主回复""steer 只在 step 边界生效"这些约束**不再成立**。
> 本文只规划，不含实现。

---

## 0. 三层定义与一个关键修正

你给的三步：

| 层 | 职责 | 关键约束 |
|---|---|---|
| **L1 接收层** | 收用户的话，并**持续对用户说话**（撑住在场感） | 唯一允许"主动开口"的层 |
| **L3 处理层** | 真正思考用户的问题（分类 → 品类 → 专家 → 提炼） | 唯一"不直接对用户说话"的层 |
| **L2 反馈层** | 把 L3 的结论说给用户听 | 唯一有"最终发言权"的层 |

**一个必须写死的修正**：L2 不该只是"把 L3 的话说出来"的传声筒。
因为 L1 可能**已经说了一部分内容**，L2 必须知道"我刚才已经说到哪里了"，否则会重复。
所以 L2 = **承接层**：它的输入不只是 L3 的结论，还有 **L1 的已输出全文 + 接管点**。

这条如果不写死，第一次上线就会出现"L1 说了一遍，L2 又说一遍"的重复感。

---

## 1. 你问「有没有需要增加其他必要的」——有，缺 6 个

三层是最小骨架，跑起来会立刻缺下面这些。按重要性排：

### ① 仲裁器 Arbiter（最核心，就是你说的"监控"）
**它不生产内容，只决定"谁在说、说什么、何时被抢话"。** 是你三个问题的唯一答案所在地。

持有状态：
- 谁当前持有"发言权"（`focus: L1 | L2 | none`）
- L1 的生成进度（已输出字数 / 估计总字数 / 是否在工具调用中）
- L3 的进度（`pending | researching | ready | failed | timeout`）
- 本次接管点（L1 说到哪个字被接管）

### ② 心跳与续话器 KeepAlive（L1 撑场专用）
职责：**L3 没完成时，主动通知 L1 继续说话**（就是你描述的"通知第一个还没完成"）。
但"继续说话"绝不能是废话填充，否则用户会觉得被敷衍。合法的续话素材只有三种：
1. 复述/澄清用户诉求（确认理解无误）
2. 说明当前在做什么（"我在查你上次那个接口的日志"）
3. 提出一个能推进的问题

> 必须有**续话次数上限**（建议 2 次）。第三次还在说，说明 L3 出了问题，应该走 ⑤ 的降级通道，而不是继续硬撑。

### ③ 进度估算器 ETA（回答你第四个问题的唯一依据）
"L1 还要说多久"必须能估出来。三个来源：
- **L1 的已输出字数 ÷ 该类回复的历史平均字数** → 粗估剩余
- **L1 是否已进入收尾段**（最后一句是否是总结/提问/鼓励语）
- **模型侧信号**：是否已停止输出（`turn/end` 是否临近）

估不准是常态，所以 **ETA 只输出三档，不输出秒数**：`快说完(<1s)` / `还要一会(1–4s)` / `还很久(>4s)`。
三档对应三种动作，避免"估了个 2.3 秒"这种没法执行的精度。

### ④ 一致性守卫 Consistency Guard（防止"自己打自己脸"）
L1 为了撑场说的话，可能与 L3 的最终结论**冲突**（L1 猜了个方向，L3 研究后推翻）。
必须在接管前做一次比对，输出三种之一：
- `agree`：一致 → 直接接管
- `extend`：L3 是补充 → 接管时说"补充一点…"（自然）
- `conflict`：冲突 → **接管话术必须显式纠偏**："我刚才那个说法不准确，实际情况是…"

> 没有这个守卫，L1 的猜测会被当成承诺，用户按它行动，然后 L2 说反话 —— 这是最伤信任的失败模式。

### ⑤ 降级与兜底通道 Fallback
L3 会失败：超时、报错、结论为空。必须预定义四种终态，绝不能出现"用户干等":

| L3 状态 | L2 该说什么 |
|---|---|
| 按时完成 | 正常接管，说结论 |
| 超时但有半成品 | 说半成品 + 明确标记"这是初步判断" |
| 失败/为空 | 承认 + 给一个可执行的下一步（"我去查一下，你先把复现步骤给我"） |
| 不可用（模型挂了） | 由 L1 收尾并说明"深度分析暂时不可用" |

### ⑥ 打断许可表 Interrupt Policy
"能不能打断"不是每次都现算的，应该是**查表**。至少三个禁区：

| 禁区 | 原因 |
|---|---|
| L1 正在输出代码块 / 数字 / 列表 | 切一半会破坏可读性 |
| L1 正在调用工具 + 输出结果 | 切断后用户拿到半截结果更糟 |
| 用户刚发新消息（1.5s 内） | 新消息优先级高于旧任务 |

---

## 2. 你最后那个问题的答案：**等还是打断**

这是整个设计的核心决策，我建议按"**L1 剩余量 × L3 就绪度**"做一个二维表，而不是凭感觉：

| | L3 刚就绪，很着急 | L3 就绪，但不急 | L3 还没好 |
|---|---|---|---|
| **L1 快说完**（`<1s`，已进收尾） | **等** | **等** | 让 L1 说完 + 心跳续话 |
| **L1 还要一会**（`1–4s`） | **软打断**：允许 L1 说完当前这句，然后 L2 接 | **等**（自然交接更顺） | 心跳续话 + 通知 L1"快好了" |
| **L1 还很久**（`>4s`，或在工具调用中） | **硬打断**：`cancel` L1，L2 接管并复述已说部分 | **软打断** | 换续话角度（复述→提问） |

三条支撑规则：

- **"等"永远是默认值。** 打断只在"L3 结论有时效性（比如用户在等一个明确答案）且 L1 还有很久"时才用。
- **打断不是丢弃，是交接。** L2 接管时**必须承接 L1 已说的内容**（"我刚说到 X，结论是 Y"），
  而不是另起一段。用户感知的应该是"同一个人话说完了"，不是"换了个人"。
- **硬打断有个物理下限。** 已经流出去的 token **撤不回来**（宿主会把它固化成 `interrupted: true` 的
  半截消息）。所以硬打断的真实语义是"**L1 就此闭嘴，L2 接着说**"，而不是"抹掉 L1 刚才那句"。
  这条要写进产品预期，否则上线后会被当成 bug。

---

## 3. 运行时序（正常路径 + 抢话路径）

```
用户说话
   │
   ├─► L1 接收层 ── 立刻开始说（"收到，你在问 X，我先应一声"）
   │       │
   │       └─► 同时把原话 + 上下文交给 L3
   │
   ├─► L3 处理层（独立会话，不面向用户）
   │       分类 → 品类 → 召回经验 → 专家 → 提炼
   │       │
   │       └─► 完成 → 通知 Arbiter「结论就绪」
   │
   └─► Arbiter（监控全局）
           │
           ├─ L3 未完成 且 L1 已停 ──► KeepAlive：通知 L1 按素材继续（上限 2 次）
           │
           ├─ L3 完成 ──► 问 ETA：L1 还要多久？
           │                 │
           │                 ├─ 快说完 ──► 等 L1 自然结束 → L2 接管
           │                 ├─ 还要一会 ─► 软打断（说完当前句）→ L2 接管
           │                 └─ 还很久 ───► 硬打断（cancel L1）→ L2 承接接管
           │
           └─► L2 反馈层：携「L1 已输出全文 + 接管点 + 一致性判定」说结论
                   │
                   └─► 收尾 → 回到等待下一条用户消息
```

**交接产物**（Arbiter 交给 L2 的结构）：

```
{
  已说全文:   L1 到目前为止输出的完整文本,
  接管点:     具体截断到哪个字（用于承接话术）,
  一致性:     agree | extend | conflict,
  L3结论:     一句话 + ≤3要点 + 下一步动作,
  置信度:     低置信度时 L2 必须附加"这是初步判断",
  品类路径:   用于写经验账本
}
```

---

## 4. 消息协议（应用层内部事件）

应用层自己应持有一份**唯一真源**的对话记录（不依赖任何 agent 的会话日志）。事件类型：

| 事件 | 发出方 | 语义 |
|---|---|---|
| `user.said` | 输入源 | 用户说话了 |
| `l1.started` / `l1.chunk` / `l1.finished` | L1 | 已输出文本的增量与终态 |
| `l3.started` / `l3.progress` / `l3.ready` / `l3.failed` | L3 | 处理层状态 |
| `arbiter.idle-check` | Arbiter | 检查 L1 是否还有话说 |
| `arbiter.eta` | ETA | 快说完 / 还要一会 / 还很久 |
| `arbiter.decide` | Arbiter | wait / soft-interrupt / hard-interrupt |
| `l2.takeover` / `l2.finished` | L2 | 接管与收尾 |
| `session.recorded` | Ledger | 落账 |

---

## 5. 落地要用的宿主能力（已核实存在，不是设想）

| 需要 | 宿主接口 | 备注 |
|---|---|---|
| 建三个角色 agent | `ctx.agents.create({ sessionId, agentOptions, setup })` | `setup` 里可给每个角色单独挂 persona / 提示段 / 工具 |
| 每个角色不同性格 | agent preset + `dsh-persona` 行（可按 scope 挂） | 已有 preset 目录：code / cordis / minimal / standard |
| 定位"谁" | `ctx.agents.get(id)` / `list()` / `roots()` | |
| 读用户输入 | `session/event` 里的 `user/message` | 含 inject 的上下文，靠 `source` 区分 |
| 读 L1 的输出增量 | `assistant/chunk`（token 级） | 拿它驱动界面与 ETA |
| 读 L1 的终态 | `assistant/message`（带 `usage`，被打断时带 `interrupted: true`） | 判断"这句是否完整" |
| 硬打断 | `agent.cancel(cause, { keepInbox })` | cause 用 `{ kind: 'user' }` 或 `{ kind: 'hook', reason }` |
| 通知 L1 继续 | `agent.steer()`（正在跑）/ `followup()`（已停） | 续话素材由 KeepAlive 生成 |
| 落盘 | 应用层自己的 Ledger（复用 v0.1 已验证的实现） | 唯一记忆归应用层 |

---

## 6. 分阶段（每步都可独立验收，不写假 agent）

| 阶段 | 交付 | 验收标准 |
|---|---|---|
| **P0** | 应用层骨架：三个角色 agent 真实创建 + 只读 transcript 采集 + 唯一对话记录 | 用户说一句，能看到三个 agent 的真实会话各自产生内容，应用层正确记录 |
| **P1** | Arbiter 最小版：只做"L3 未完成 → 通知 L1 续话"，不含打断 | L3 慢时 L1 会主动续话，且续话素材合规、不超 2 次 |
| **P2** | ETA + 决策表：等 / 软打断 / 硬打断 三路真实生效 | 三种路径各复现一次，L2 接管话术正确承接 L1 已说内容 |
| **P3** | 一致性守卫 + 降级通道 | 构造 L1 猜测被 L3 推翻的场景，L2 正确纠偏；构造 L3 超时场景，兜底话术生效 |
| **P4** | 经验账本 + 品类树接入（复用 v0.1 已验证的实现） | 同类问题第二次显著变快 |

---

## 7. 三个待你确认的决策点

1. **L1 的续话上限**：我建议 2 次。超过就走降级通道（承认"还在算"），而不是无限硬撑。你的偏好？
2. **硬打断的默认开关**：我建议**默认关闭**，只对"用户明确在等答案"的场景开启。因为已输出 token 撤不回来，硬打断天然会有半截话。
3. **L1 与 L2 是不是同一个角色**：如果要求"一个声音"，可以让同一套 persona 挂到 L1 和 L2 两个 agent 上（内容不同、语气一致）；
   如果允许"两个声音"，L2 可以是更权威的性格。这直接决定观感，建议先定。

---

## 附：与原 v0.1 的关系

- v0.1 的 **Reflex 快层** → 直接成为本文的 L1
- v0.1 的 **Cortex 皮层**（分类/品类/召回/专家/提炼）→ 直接成为本文的 L3
- v0.1 的 **Handoff** → 升级：从"边界注入"变为 Arbiter 的"真接管（cancel + 承接）"
- v0.1 的 **Ledger / Metrics** → 原样保留，归应用层所有（它才是唯一记忆）
- v0.1 里已验证的调参结论仍然适用：L1 必须关思考（`reasoning_effort=none`）；
  L3 的提炼阶段用快模型（flash/none），实测把 p50 从 12.0s 降到 6.5s、降级率归零
