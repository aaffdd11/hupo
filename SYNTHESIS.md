# 三方讨论与两两对辩 · 合成结论（求同存异）

> 机制:三位角色(产品经理 / 工程实践师 / 架构师)先各写一份讨论稿,再做**两两对辩** ——
> 三个辩题、**双向各辩一次**(因为"A 驳 B" ≠ "B 驳 A"),共 **6 份对辩稿**。
> 产出:`DISCUSS-{pm,eng,arch}.md`、`DEBATE-{pm-vs-eng,eng-vs-pm,arch-vs-eng,eng-vs-arch,pm-vs-arch,arch-vs-pm}.md`
> 本文只写**结论**:收敛项直接定,争议项列清实质并给裁决建议。

---

## 一、收敛项(三方无异议,可直接定)

| # | 结论 | 谁提出的 |
|---|---|---|
| C1 | **`emittedText` 必须有唯一权威并逐轮落盘**,定义 = "控制器已放行的文本" | 架构师提出,工程实践师**改立场接受**(被"选客户端回执需要多一跳且弱网不可靠"说服) |
| C2 | **A 为默认通路 + 推迟窗口**;工程实践师**撤回**"只能选一个就选 B" | 双方各让一步 |
| C3 | **控制器必须进程内** —— 新增一条比 `source.kind` 更硬的论证:进程内逐帧判决是 µs 级,进程外是每帧 185B | 工程实践师新增证据 |
| C4 | **"两块内容"砍不掉**(它来自 9 倍延迟差这个物理事实),但"两个 agent"不是必须的 —— 需要的是**两次装配 + 一个事实源** | 架构师澄清,PM 接受 |
| C5 | **先测量后机制**;盲评(seamless §6)必须进 P2 门禁 | PM 提出,架构师接受 |
| C6 | **级联第一版不做**;`maxDepth` 显式设 2、并发上限、重试预算必须现在配置 | 三方一致 |
| C7 | **说话人识别第一版不做**,只做自用 VAD(端点检测 + barge-in,零合规成本) | 工程实践师提出,**本轮我的实验独立证实** |
| C8 | **声纹与聚类砍掉**(D30/D31 删除);被动模式合规负担因此归零 | PM 加码 |
| C9 | **取消滚动摘要** —— 连带消掉 R2 的"授权例外",架构里不必新增例外条款 | PM 加码,工程实践师认同(主要成本是 30–60 次 LLM 调用/小时) |
| C10 | **只追加、不擦除**是全场最硬的一句纪律 | 三方共识 |

### 六个"我改了的立场"(评审们互相说服的结果,值得记录)

| 谁 | 改了什么 | 被什么说服 |
|---|---|---|
| 工程实践师 | `emittedText` 权威归控制器(原主张客户端 ack 水位) | "选客户端回执需要多一跳,弱网不可靠"(自己的 RTT p99 400–800ms) |
| 工程实践师 | 撤回"只能选一个就选 B" | "A 不吃首字预算,B 要吃 250–350ms" |
| 产品经理 | 撤回"必须选 B" | **真宿主接管粒度是 step 不是字符**(`dsh-agent/README.md`:`next step boundary`、`no step-only abort`) |
| 产品经理 | 认"首句短 → 延迟归零"用了错指标(实际 +250–350ms) | 工程实践师的量化 |
| 产品经理 | "只发完整句"太绝对 → 完整从句该发 | 工程实践师的分档 |
| 架构师 | 承认"推迟窗口代价可忽略"是越权(那是 PM 的指标变更) | PM 的指标论证 |

---

## 二、真争议(辩不出结果,需要你拍板)

### 争议 1：反馈层用混合拓扑还是同栈(最有产品后果)

| 立场 | 主张 | 论据 |
|---|---|---|
| 工程实践师 | 接收层用 B(按单元)、反馈层用 A(逐字) | 接收层只一句,代价 ≈0.3s;反馈层长文,按句投递会把首字推迟 0.6–1.2s |
| 产品经理 | **反馈层也用 B(同栈)** | 工程自己的文档写了"深答 ≤25s 皮层超时" ⇒ 反馈层**也会被系统中止**,而混合方案把"单一真源"劈成两套水位 |

**PM 抓到工程稿的一处内部矛盾**:`:61` 说反馈层"不会被系统中止",`:164` 又写"深答等待 ≤25s 皮层超时"。**被中止的正是最容易露半句的长文。**

**实质**:混合拓扑的代价是"两套投递语义 + 两套水位",而收益只在"反馈层首字快 0.6–1.2s"。**同栈的代价是反馈层首字慢一点,但只有一套语义。**

**我的裁决建议**:**先做能退化的那个(同栈 B)**。理由:两者都能过,但同栈的失败模式更容易修(慢一点 vs 两套水位不一致),而且 PM 指出的矛盾是真的。

---

### 争议 2：投递协议 B 是否成立(技术性最强)

工程实践师的立场:B 作为**协议**(服务端缓冲单元 + 客户端回放 + ack)需要发明一个宿主里不存在的概念。

架构师的反驳:**"句子"在 DSH 宿主里不存在** —— 只有 `assistant/chunk` 与 `assistant/message`。把投递点改到句 = 发明一个被四层共享的一级概念,必须单点切分 + `Segment` 协议字段。

而 PM 引用的 `DISCUSS-eng.md` §2.3 已量化:"不做长度上限,p90 会到 +0.9s" ⇒ **架构师那个 ≤400ms 窗口能否成立,实际由 PM 的首句长度纪律保底,不是由机制保底。**

**三方各自的实证**:
- 工程实践师:`cancel` 只停在 **chunk 边界**(`dsh-agent-loop/lib/index.js:615-648`,`throwIfAborted()` 在 `for await` 内),**没有字符级暂停** ⇒ 要"等一句写完"就必须扣住句尾不投递 = **缓冲**,而那正是他攻击 B 的那个副作用
- 架构师:`interruptedBlocks()` 是**有损过滤重建**(丢 tool-call 块、丢空白块、保留 reasoning 块)⇒ 工程实践师选定的"生成全文"载体不成立
- PM:`src/host.js` 的 `spokenText()` 基类**直接返回空串**,只有 FakeHost 有实现 ⇒ **"用户实际看到的文本"今天根本没有生产来源**

**三方实证合起来指向同一件事**:这一步的三个候选载体(客户端 ack / 网关日志 / 宿主消息)**都不完整** ⇒ C1 那条"唯一权威"不是洁癖,是必需品。

**裁决方式**(架构师给的):**每臂 20 次中止对照测量** —— 量"残骸率"(半句)vs"卡死率"。这是可测的,不该靠辩论定。

---

### 争议 3：误报的代价怎么转移(最像产品问题)

| 立场 | 主张 |
|---|---|
| 架构师 | 出口断言 `assert(mode==='active' \|\| pointedAt===true)` —— 把"宁可漏答"固化成不变量 |
| 产品经理 | 反对:这把误报代价**全部转移给用户且不可纠正**;要求出口放"**确权问句**"而不是断言 |

PM 的替代方案:**把"沉默"变成可撤销的** —— 判为"疑似对助手说话"但置信度不足时**不出声**,但在状态区留**一条可点的一行提示**,用户点一下完成纠正。一次交互同时解决"它坏了"和"它只听别人"。

**我支持 PM**,并且这恰好就是他"不依赖声纹"路线的落点(声纹不可行时,确权问句是唯一可持续的兜底)。

---

### 争议 4：L0 地基地基要打多厚(影响排期最多)

| 立场 | 主张 |
|---|---|
| 架构师 | L0 九项地基,不做就无法回填;`noReply` 必须可表达,否则被动模式后加要满仓库返工 |
| 工程实践师 | **八项里只有 4 项不可回填**(1/4/6/8)+ 2 小时的 7 ⇒ **P0 = 2–3 天,不是 2 周** |

工程实践师的核算:8 项打包 + 全量 `emit()` + 令牌式 rebuild + `asOfTurn` ≈ **多花 6–8 天**,并把 **P4 的 21 天真人验证推迟一周以上** —— "架构洁癖伤的是他自己的验证节奏"。

**而架构师有一条计划级证据很有力**:
> **P4 的 21 天是唯一不可替代的证据源。若它跑在 `emittedText` 落盘之前,这 21 天在"承接质量"轴上零信息。**

**我的裁决建议**:取工程实践师的**范围**(只做 4 项不可回填 + 配置三行),但取架构师的**顺序**(`emittedText` 必须在 P4 之前落地)。两者不冲突。

---

### 争议 5：`emit()` 的覆盖范围

| 立场 | 主张 |
|---|---|
| 架构师 | 所有面向用户的输出必须走**唯一出口** `emit()`,否则"谁在说话"可被绕过 |
| 工程实践师 | `emit()` 只覆盖**人类转写**;**UI 词条/状态提示是不可互转的另一种类型**,不是同一出口;强行合并 = 3–5 天 |

这条与架构师自己的主张冲突(他说过"状态提示是 UI 事件不是消息")。**我支持工程实践师**:类型不同不该塞进同一出口,成本 0.2 天 vs 3–5 天。

---

### 争议 6：`asOfTurn` / 令牌式 rebuild 是否过度设计

工程实践师判**过度设计**(单进程单写者;令牌状态与架构师自己的"控制器无内存态"打架),建议用 `signature 预留 + expectedPrefixHash` 替代。**我支持**,因为"控制器无内存态"若成立,令牌本身无处存放。

---

## 三、工程实践师用真实语料判掉了三个争议（最重要的一节）

它在动笔前扫了本机 `~/.dsh/sessions/**` 全部 **392 个会话**，从逐 chunk 的 `dt` 重建时间线
（连续性校验通过 **n=2466**，散文子集 n=1340）。**这些数字改变了三个结论。**
脚本已落盘：`scripts/va-replay.js`（可重跑）。

### 3.1 「首句 ≤14 字」被实测判为 66% 失败率的赌注

| 指标 | 实测 |
|---|---|
| 自然首单元长度（到第一个硬边界） | p10=7 / **p50=24** / p90=68 字 |
| ≤14 字占比 | **33.8%** |
| ≤25 字占比 | 53.1% |
| 到 14 / 25 字耗时（相对首 chunk） | **p50 74 / 137ms**，p90 149 / 267ms |
| 600ms 时间兜底触发率 | **0.6%** |
| 25 字上限切分结果 | 硬边界 44.7% / 逗号软切 13.4% / **原地硬切 41.8%** |
| 其中末字是功能词的残词 | **仅 1.7%** |
| 整段无硬边界的流 | 9.3% ⇒ **"块结束必须是硬边界"是必需项，否则会死锁** |

**三条结论**：
1. 「首句 ≤14 字」不是纪律，是**服从性赌注 —— 三分之二的轮次做不到**
2. **600ms 时间兜底几乎不触发 ⇒ 真正决定首单元延迟的是字数上限，不是时间兜底**；
   "14 字还是 25 字"不是细节，**它就是机制本身**
3. 用「**不许以功能词结尾**」取代产品经理原来的「永不发半句」（残词率仅 1.7%，比硬切好得多）

### 3.2 A/B 之争被重构：选 B 不是面向用户的选择

**证据**：产品经理要求"句内逐字 + 句间光标不消失 + 回放速率 ≥ 生成速率 + 中止瞬时 snap"之后，

> **用户每毫秒看到的与 A 逐字节相同，唯一差别是中止落点是句边界。**

所以"选 B"**不是面向用户的选择，而是"服务端何时可以扣住文本"的投递策略** ——
而它给这笔账报价"0 行额外架构"，实际是 **4.5–6 天的客户端回放状态机**（抖动缓冲 + 速率控制 + snap + 两级 ack）。

**它的立场**：**要闸门，不要回放** —— 整单元显形 + 120ms 到达动画，`recvSeq ≡ renderedSeq`（两级 ack 自动消失）。

**并且它指出真正的优化方向被搞错了**：

| | TTFT | 首单元 |
|---|---|---|
| A | **0.9s** | — |
| B | — | 1.04s(p50) / 1.17s(p90) |

0.9s 的预算里 **TTFT 占 78–92%** ⇒ **该砍的毫秒在 TTFT（前缀/prefill），不在 A/B 之争的 0.27s**。
（它还指出产品经理用 200ms 当 TTFT，而 README 实测是 0.8–1.5s —— **把 TTFT 写成了 4 倍乐观**。）

### 3.3 它同时打掉了自己的旧估算（诚实度值得记录）

- `assistant/chunk` 落盘时**被抽样（约 1/200）** ⇒ 它第一轮说"2 小时零代码"的 V-A 方案**拿不到逐 chunk 时间戳**
- `dt` 是**缓冲排空时间**（1 字 chunk / 4ms gap → 瞬时 250 字/s，物理上不可能）⇒ 绝对速率可能**高估 2–3 倍**
- ⇒ **V-A 成本它自己说低了约 4 倍**（真实 ≈1 天），且必须先加"原始 chunk 到达时刻落盘"（约 5 行）

### 3.4 假前提误报率：样本量本身是个陷阱

| 要证明 | 需要样本 |
|---|---|
| 在 0 观测下断言误报 <5% | **n ≥ 59** |
| 区分 5% 与 15% | **≈120 条/组** |
| 产品经理提的"≤1/20"闸门 | **n=20 分不出 5% 与 15%**；真实 10% 的检测器有 **39% 概率通过** |

**最实用的一条降级**：
> **L1：只影响内容，永不产出 meta 语言** ⇒ **误报代价 = 0**

并把验收指标从"误报率"换成「**错前提污染率**」—— 构造集测 recall 合法、**测 precision 不合法**
（这正是产品经理 6/6 批评的精确形式）。

### 3.5 它修正了产品经理的一个事实错误

产品经理说"删声纹 ⇒ 合规归零"。**不对**：合规触发点是「**他人语音上传第三方 ASR**」，**不是生物特征**。
（但它也说：iOS 的系统麦克风指示器**白送**"它在听"的信号 ⇒ "用户不看屏幕"论证的一半失效。）

### 3.6 它当场认下的三条

| 认了什么 | 被谁说服 |
|---|---|
| 反馈层**两层同栈**（撤回自己的混合分配表，并用实测削掉自己的反驳：40 字首单元 ≈350ms 而非 600ms–1.2s） | 产品经理 |
| **用户中止必须全链路 cancel** | （推翻它自己的 C8） |
| **盲评排在机制之前**（顺序对但判据错："有没有换人"答不出"两块要不要合并"，应改成"**干等 7s vs 快答+深答**"两版对比 + 记录催促语） | 产品经理 |

**它还指出**：产品经理分三处提的"不可替代"要求，其实是**同一个机械锚点闸门的三条策略** ——
一次实现 1 天，不是三个功能。

---

## 四、我的原型被对辩挖出的三个真 bug（已修/待修）

| # | Bug | 位置 | 状态 |
|---|---|---|---|
| **B1** | **控制器在写台词**:三句固定承接语,其中两句正是 seamless §4.2 明令禁止的断裂式开头,且逐轮重复 ⇒ 必然成为口头禅 | `src/handoff.js` `withLead()` | ✅ **已修** —— 默认不再注入固定短语,承接交回反馈层生成;旧台词仅保留给回归对比 |
| **B2** | **"半句切换"是假宿主造出来的现象**:`FakeHost` 在任意时刻把文本拼进 `this.text`,而真实宿主 steer 只在 **step 边界**消费 —— **两个评审在争一个不存在的现象** | `src/host.js` `steer/inject` | ✅ 已标注 `simulated: true` + 文档写明偏差 |
| **B3** | **`spokenText()` 没有生产来源**:基类返回空串,只有 FakeHost 有实现;"用户实际看到的文本"今天不存在 | `src/host.js:22-24` | ⚠ 已知,是 C1 的落点(必须新增 `turn/text-committed` 事件) |

**B1 是活的用户可见 bug(每轮都会出现),B3 是最该先修的地基。**

---

## 五、说话人识别:我的实验给出了决定性负面结果

| 实验 | 结果 |
|---|---|
| 手工 MFCC 特征 + 余弦相似 | 区分裕度 **+0.002**(7 样本误判 1 个) |
| 按维度 z-norm 后 | 裕度跳到 **+0.759** —— 但这是**同一批数据算的,过拟合** |
| **留一验证(2 段注册,判断其余 5 段)** | **8/15 正确,7 次错误;有一次把主人自己的样本误判为访客** |

**结论:小样本 + 朴素特征的声纹识别不可靠**,即使在最有利工况(合成语音、无噪声、无重叠、无远场)。这**独立证实了工程实践师的判断**(他主张第一版只做自用 VAD)。

**如果将来要做**,正确路线是**预训练说话人嵌入模型**,而不是手工特征。我已定位到可用资源:
- **CAM++(`iic/speech_campplus_sv_zh-cn_16k-common`)**:中文说话人验证 SOTA,模型文件 **28MB**,ModelScope 可下载(HuggingFace 在本机不通)
- 仍需 FUNASR/ModelScope 运行时(数百 MB),本机磁盘已 85%,需要先清理

**建议**:按工程实践师的意见 —— **第一版只做自用 VAD**;声纹识别留到 P5 条件触发时再评估,且届时应直接上预训练模型。

---

## 六、需要你拍板的 6 件事(按影响排序)

| # | 决策 | 我的建议 |
|---|---|---|
| 1 | **反馈层投递拓扑**:混合(接收层 B / 反馈层 A)还是同栈 B | **同栈 B** —— 只有一套水位语义,失败模式更好修 |
| 2 | **A/B 的裁决方式**:直接选,还是按架构师建议做"每臂 20 次中止对照测量" | **做测量** —— 20 次中止约 1 小时,能把"残骸率 vs 卡死率"变成数字 |
| 3 | **L0 地基地基范围**:架构师九项 / 工程实践师四项+三行配置 | **四项 + 三行,但 `emittedText` 必须在 P4 之前** |
| 4 | **误报的处理**:出口断言(宁可漏答)还是确权问句(可撤销的沉默) | **确权问句** —— 误报代价不该单向转移给用户 |
| 5 | **会议文档 vs 合规义务**:被动聆听要不要做会议文档(它指出合规触发点是"他人语音上传第三方 ASR",不是声纹) | **第一版不做**,只做自用 VAD;文档能力等 P5 |
| 6 | **真实生成速率是多少**(50–80 字/s 还是 150–180 字/s) | **先测**(1 天)—— 它决定"两层同栈"是否成立,而这个数目前是估的 |

---

## 七、下一步(可直接开工的第一步)

不论上面 5 项怎么定,有一件事**必须先做且不可回填**:

> **写 `turn/text-committed` 事件**(20 行):`{ turnId, speaker, committedThroughSeq, sha256 }`
>
> 它同时是:① R5(反馈层承接用户看到的字)的物理前提 ② S4 纠偏的对象
> ③ 多用户审计的边界 ④ T4 任务归属的依据 ⑤ P4 那 21 天有没有信息量的前提
>
> **不写它,以后补不回来。**

第二条(1 小时,零埋点、不需真人):**V-D —— 50 条真实输入 + 新 role 段跑快层**,测
超限率 / 锚点命中率 / **锚点虚构率(必须为 0)** / 零信息形态占比。
其中"**锚点虚构率必须为 0**"是硬红线:长度上限 + "必须有具体信息"的组合会**逼模型虚构锚点**,
而**假记忆外发不可撤销** ⇒ 锚点必须做**机械字符串匹配校验**,不能靠模型自觉。
