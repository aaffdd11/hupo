# 工程评审报告：个人 AI 助理 v5（交付 / 运维 / 故障 / 成本视角）

> 评审人视角：15 年工程总监，带过 20–50 人团队，负责过多租户 SaaS 的架构与交付。
> 评审对象：`PLAN.md`、`ARCHITECTURE-v5.md`、`REVIEW-appdev.md`、`README.md`（v1 实测）。
> 本报告**不复述设计**，只回答四个问题：能不能交付、能不能运维、出了事能不能查、成本能不能控住。
> 评审时间：2026-09-13。所有判决基于**我在本机实测/实读的代码与数字**，不基于文档自述。

---

## 0. 我实测到的环境事实（这些数字后面每一条判决都用得上）

我没有只看文档，我在本机把关键假设都验了一遍：

| # | 我验的东西 | 实测结果 | 影响 |
|---|---|---|---|
| E1 | 本机算力 | **4 vCPU / 7.2GB RAM（当前 available 3.1GB）/ 磁盘 40G 已用 80%（剩 7.8G）** | 这里是硬上限，不是"未来要扩容" |
| E2 | 现有 DSH 进程常驻内存 | PID 862（3080，跑着 web UI + 会话历史）= **1734MB**；PID 59382（3081）= **1092MB** | 一个真活着的实例 ≈ 1–1.7GB |
| E3 | 全新 `DSH_HOME` 冷启动 | `DSH_HOME=/tmp/x dsh web --no-open --port 0` → **25s 内起来，RSS 203MB，端口自动分配成功**；该 home 磁盘仅 **1.9MB**（pnpm 从全局 store 硬链） | 进程级隔离**技术上完全可行**，磁盘不是问题 |
| E4 | `dsh web` 的绑定能力 | `--host` 只有 `127.0.0.1` / `0.0.0.0` 两个合法值（`dsh-host-webserver` zod schema 硬编码）；**没有 unix socket** | 想只开内网口，只能靠防火墙 |
| E5 | `/api` 的"鉴权" | `dsh-client-connection` 的 `isTrustedApiRequest` 注释原文：**"Network reachability and authentication stay out of scope: binding policy belongs to the webserver config, and this fence is not an auth layer."** 且服务**已在 `0.0.0.0:80` 上跑着 nginx** | D9 网关不是"防污染"，是防裸奔 |
| E6 | `agent.steer/inject/followup/cancel` 的可达性 | 实现在 `dsh-agent-loop/lib/index.js:394-410`，**是 Agent 实例方法**；全仓库唯一的外部调用点是 `dsh-host-apiproxy`，且**只能构造 `source.kind === "user"` 的消息** | 见第三节，这是控制器位置的判决依据 |
| E7 | LLM 重试策略 | `dsh-llm` 默认 `maxRetries = 5`，退避 500ms→10s，抖动 0.1，**可重试码包含 `EMPTY_RESPONSE`** | "禁止静默重试"与框架默认**冲突** |
| E8 | 子 agent 并发控制 | `dsh-tool-subagent` 的 `maxDepth` **默认是 3**；**全仓库没有任何子 agent 并发上限** | 扇出上限 = 模型自觉 |
| E9 | `subagent` 的"共用 system 前缀"假说 | `dsh-subagent` 会向**每个子 agent** 追加固定串：`SUBAGENT_DELEGATION_CONTEXT = "You are a delegated subagent: your permission scope was fixed when you were started…"`；父 agent 的 system 里**没有**这一句 | §5 约束 2 的落地方式必须改，见第五节 |
| E10 | 原型测试工具链 | `npm test`（`node --test test/`）**在 Node v24 上直接失败**：`Cannot find module '/home/deploy/projects/assistant/test'`；改用 `node --test "test/*.test.js"` 后 **20 通过 / 0 失败** | 测试本身是好的，**入口坏了**——所有"验收全绿"的声明目前无法在 CI 复现 |
| E11 | `package.json` 声明的入口 | `scripts/demo-fullstack.js` 与 `scripts/demo-fullstack:fake` **文件不存在**（`scripts/` 只有 `bench-real.js`、`demo.js`、`diagrams.mjs`） | 文档里"12 组件"有一部分在仓库里没有对应物 |
| E12 | trace 读 token 的真实代价 | `dsh-host-apiproxy` 注释里的实测：**一页 50 条消息 = 一个 turn 一个 chunk 事件 → 52k 事件 / 11MB，99% 是 `assistant/chunk`**；页上限 `MAX_PAGE_EVENTS = 8e3` | 单 chunk 平均 ≈ **220 字节**；跨进程读 chunk 的真实成本在这里 |
| E13 | 会话落盘膨胀 | 383 个会话文件共 231MB，最大**单会话 zstd 已 19.9MB** | 存储成本有量级可算 |

---

## 我的假设（不成立请驳回对应判决）

| 项 | 我的假设 | 依据 |
|---|---|---|
| 用户量 | 创始人 1 人 + **朋友 5–15 人**；不是公开产品 | 项目形态（本机 credentials、无鉴权设计、`PLAN.md` 原话"自己+几个朋友"） |
| 团队 | **1 人，10–20 小时/周**；没有 SRE、没有值班 | 全部文档一人撰写；本机是唯一环境（nginx 与开发同机） |
| 预算 | **创始人明确说"成本暂时不是考量"——我按这条采信**，但我不接受"因此不做成本熔断" | 你给了这条授权，但它只授权"多花钱"，不授权"烧到失控" |
| "上线"的定义 | 朋友能在自己手机上用、连续 30 天不炸、出事能查到原因 | PLAN 的 M6 口径 |
| 运维能力 | 能写 systemd / cron / nginx 配置，**但不会去盯 20 个手工进程** | 单人团队 |
| 基础设施 | **就这一台机器**（nginx + DSH + 待建设的网关同机） | E1/E5 |

---

# 一、这套东西最可能在哪里先炸（按"先炸的顺序"，不按严重度）

排序依据是**爆炸的时间窗**：什么时候第一个用户/第一周就会撞上。

### 炸点 1 — 第一周，测试入口已坏 → 所有"已通过"的声明不可复现

- **触发条件**：任何人执行 `npm test`。
- **实测**：`node --test test/` 在 Node v24 报 `MODULE_NOT_FOUND`；换 `node --test "test/*.test.js"` 后 20/20 通过（E10）。`scripts/demo-fullstack.js` 根本不存在（E11）。
- **爆炸半径**：不是"跑不起来"这么轻。它意味着**PLAN 里 12 个场景的验收证据无法在自动化里复现**。D1 要重构控制器、D14 要动级联——在这个前提下每次重构都是"改完看不出有没有破"，返工成本直接落在唯一的关键路径上。
- **如何提前发现**：把"`npm test` 在干净 checkout 上退出码 0"设成 D1 的**开工前置条件**（不是一个待办，是门禁）。这条 5 分钟能修，它排第一是因为**它会让后面所有炸点都晚发现**。

### 炸点 2 — 第 1–2 周，你给朋友用上真 UI 的那一刻，"双通道拦截"漏到用户屏幕上

- **触发条件**：在 DSH 自带的 web 对话界面里，控制器用 `steer`/`inject` 向接收层发指令（发言简报、续话通知、记忆简报）。
- **依据**：`dsh-client-ui-conversation` 渲染**所有** `user/message` 事件，其匹配逻辑是 `event.type === "user/message" && isAppendSurfaceEvent(event)`，**没有 source.kind 过滤**；`createUserMessage` 支持 `source: {kind:'plugin', plugin}`（`dsh-agent-loop` 里有按 `kind === "plugin"` 判定的先例）。也就是说：**今天你用 DSH 的 UI 打开这个会话，控制器注入的每一句"系统指令"都会显示成用户自己说的话。**
- **爆炸半径**：这正是 PLAN **D4"双通道拦截落地"**要解决的东西，而 D4 在表里排在地基之后——**但用户第一周就会看到它**。它是产品级穿帮（用户看到"用户：结论已到，立刻收尾"），也是数据污染（这些污染消息会被持久化进 session.jsonl，从此污染后续所有轮的上下文）。
- **如何提前发现**：在 D4 之前先做一条**会话级断言**：`session.jsonl` 里凡 `user/message` 且 `source.kind !== 'user'` 的事件，**一律不得进入外发流**。用一条测试跑：注入 3 条系统消息 → 断言外发流里恰好只有 1 条真实用户消息。同时立刻决定一件事：**客户端绝不能是 DSH 自带 web UI**，否则这个洞永远在你视野外。

### 炸点 3 — 第 2–4 周，首字 p50 在"有记忆 + 有级联"之后第一次破 1.5s

- **触发条件**：接收层被塞进更多前缀（记忆块 + 品类方法论 + 工具定义）之后，prefill 变长。
- **依据与算式**：你实测接收层首字 0.8–1.0s，其中**纯首个 token 的等待本身就是 0.8–1.5s**（README：主 agent 首 token 0.8–1.5s）。首字预算是 1.5s，**留给 prefill 的余量不到 0.5s**。而 `REVIEW-appdev` 已经指出：4 个 agent 每轮都要 POST 各自整份上下文，这是**每轮每 agent 的固定开销，跟缓存命中与否无关**——命中缓存省的是钱，**省不了这段上传时间**。前缀每加 8K tokens ≈ +32KB 请求体，在 prefill 侧是**百毫秒级**的确定性增加。
- **爆炸半径**：首字 p50 破 1.5s = 架构存在的理由（9 倍延迟差）在用户侧消失。这不是"慢一点"，是产品前提崩了。
- **如何提前发现**：每轮 trace 落 `prefix_tokens / first_token_ms`，**在首字 p50 超过 1.2s（不是 1.5s）时报警**——留 300ms 反应时间。这条必须在 D1 之前就上，否则你是"上了记忆才发现慢了"。

### 炸点 4 — 第 2–4 周，多用户并发时 429 被框架重试放大成雪崩

- **触发条件**：两个用户在同一分钟内提问，处理层各起一个会话，每个会话做 fan-out。
- **依据**：`dsh-llm` 默认 **每调用最多 5 次重试**，退避上限 10s，可重试码含 `RATE_LIMIT` / `SERVER` / `TIMEOUT` / `TRANSPORT` / **`EMPTY_RESPONSE`**（E7）。上游返回 429 时，**每个并发节点都独立重试**：20 个并发节点 × 6 次尝试 = **120 次请求**同时压向同一个 key。而此时 **DSH 没有任何按用户/按 key 的配额或队列**——`REVIEW-appdev` 第 2 部分第 1 条说的"成本熔断"在宿主侧**不存在**。
- **爆炸半径**：429 → 重试 → 更多 429 → 所有用户的接收层首字一起崩（因为接收层和处理层共用同一个 key）。**这是唯一一个会从"一个用户的问题"变成"所有用户的问题"的炸点。**
- **如何提前发现**：trace 里落 `retry_count / retryable_code`，**单轮重试次数 > 5 就报警**；并且**第一版就必须在网关侧按 userId 限并发**（不是按 token 记账，是按**在途请求数**），因为限并发才能挡住雪崩，限成本只能事后知道。

### 炸点 5 — 第 3–4 周，级联一开，磁盘和写盘延迟同时出事

- **触发条件**：级联 + "历史只追加、永不改写"。
- **依据（可算）**：`plans` 的"只追加"是**对**的（18 倍成本差是实测），但它有一个没人算过的副作用：**会话永不缩小**。E13 的实测是**单会话 zstd 已 19.9MB**——注意那是**一个会话**。个人助手如果保存原文、且用户是重度使用者，一条长会话天然会长到几十 MB；再乘上"每用户每会话 + 每用户多会话"。而磁盘**已经 80%，只剩 7.8G**。
- **爆炸半径**：磁盘写满 → session.jsonl append 失败 → **所有用户的所有会话全部写不进去**，且控制器的事实源（"唯一真源 = 控制器自己的记录"）同机同盘 → **真源和日志一起没**。这是"数据丢掉"级，不是"慢"级。
- **如何提前发现**：`du -sh ~/.dsh/sessions` 每天落一个数，**7 日斜率 > 500MB/天 就报警**；磁盘水位 **85% 硬告警**。这两条加起来是 4 行 cron，必须在上线前有。

### 炸点 6 — 第 4–8 周，成本熔断的缺失以"某天账单跳 20 倍"的形式出现

- **触发条件**：一次死循环/工具重试循环，或一次失控的 fan-out。
- **依据**：你明确说"成本暂时不是考量"——**我采信这条，但它的正确读法是"可以多花钱"，不是"不需要天花板"**。现实是：单轮 = 1 接收 + 1 分类 + N 专家 + 1 提炼 + 1 反馈，再乘级联的 fan-out 与 `maxRetries=5`。E7+E8 一起看：**没有并发上限、没有重试上限之外的任何刹车**。agent 类系统的标准事故就是"一个 bug 挂一夜烧掉一个月预算"，而本项目的独特放大项是**级联把单次事故的乘数从 1 变成 N×depth**。
- **爆炸半径**：不是破产级别（你的规模小），而是**它会摧毁你对成本的直觉**：一旦账单基线被污染，后面所有"级联到底值不值"的判断都没有数据可用。
- **如何提前发现**：`REVIEW-appdev` 已经给了正确形态（每轮 trace 落 tokens + 金额）。我要补一条工程纪律：**每轮 trace 必须落 `cascade_fanout / cascade_depth / total_llm_calls`**，并断言 `total_llm_calls ≤ 12`（超了当轮就断，不是记完继续）。**先有硬上限，再有告警顺序**——否则告警只是事后通知。

### 炸点 7 — 第 1–6 个月，"唯一真源"在控制器崩溃时以重复提交的形式污染记忆

- **触发条件**：控制器进程被杀在"处理层已提 `pending` 提案、控制器尚未 `committed`"之间。
- **依据**：`ARCHITECTURE-v5` 明确写"唯一对话真源 = 控制器自己的记录（不依赖任何 Agent 日志）"。`REVIEW-appdev` 已指出这等于把**单点**和**真源**绑成同一个东西，并给了正解（append-only JSONL + `turnId` 幂等 + 每日快照）。我要把它提到"工程必修"而不是"建议"：因为 `confidence` / `hits` 直接驱动 v5 §5 的经验权重，**双写一次 = hits 虚高 = 所有基于 hits 的排序全部错**，而且是**静默的错**。
- **爆炸半径**：记忆是唯一长期资产（v5 §5），一旦被静默污染，用户不会报错——他只会觉得"这 AI 越来越不准"，而你没有基线。
- **如何提前发现**：上线前手工演练一次 `kill -9`（`REVIEW-appdev` 已列），并且**在 trace 里落 `turnId` + `memory_commit_state`**，跑一条对账脚本：同一 `turnId` 出现两次提交 → 立即告警。**演练必须在上线前做一次，不是"计划做"。**

---

# 二、多用户方案：选哪一个

## 2.1 四个方案对照（用 E1–E5 的实测数据算，不用感觉）

| 维度 | **方案 A：每用户一个 DSH 进程 + 自建网关** | **方案 B：单进程多会话，网关做映射** | **方案 C：每用户一个容器/机器** | **方案 D：A 的变体——每用户一进程 + 共享只读 profile + systemd 模板**（我的建议） |
|---|---|---|---|---|
| 实现方式 | `DSH_HOME=data/u_xxx dsh web --port P --no-open`，N 个进程 | 1 个 DSH 进程，网关用 `sessionId` 路由 | 容器/k8s 或独立机器 | 同 A，但 `profiles/` 走共享只读目录、进程由 systemd 模板托管 |
| **隔离强度** | **强**（进程 + 文件系统 + 内存 + 会话全隔离；E3 已验证 25s 可起） | **弱**：同一进程内 N 个 agent 共享事件循环、共享 credentials、共享插件状态；一次 OOM 或一个死循环**全体阵亡** | 最强（内核级 + 资源配额） | **强**（同 A），且 systemd 给了 `MemoryMax`/`Restart=always` |
| **单机可承载** | 受内存限：E2 实测活跃实例 1.1–1.7GB。7.2GB 机器上，**扣掉现有 ~4.2GB 占用与 nginx，实际只剩 ~3GB** → **同时在线 3–6 人**；配合空闲回收可覆盖 15–20 名注册用户 | 受"事件循环 + 单点"限：**没有硬上限，但也没有隔离**；实际上限由模型并发决定（4 vCPU） | 受成本限：**2–3 人/机**（本机只有 1 台） | 同 A，加空闲回收后 **同时在线 8–12 人 / 注册 30–50 人**（需把常驻内存压到 <250MB，见 2.3） |
| **运维复杂度** | 中：N 个进程要起、要停、要回收、要看日志 | **低（起）→ 高（出事后）**：单进程好起，但**一个用户的问题会影响所有人**，且没有按用户的资源视图 | **高**：镜像、网络、证书、卷、配额，单人团队吃不下 | **中偏低**：`systemctl` 模板 + 一个回收脚本；**日志按用户天然分文件** |
| **故障影响面** | **1 个用户**（进程崩=他一个人断线，其他人无感） | **全部用户**（这是最致命的：它把"1 个朋友问了个怪问题"变成"所有人一起 502"） | 1 个用户 | 1 个用户 |
| **迁移成本** | 低：`DSH_HOME` 换目录即可增删用户 | **高（反向）**：从单进程拆到多进程要先做会话归属梳理 | 中 | 低（与 A 相同） |
| **凭证** | ⚠️ **每 home 一份 `.credentials.yaml`**（`resolveDshHome(config.dshHome)`）→ N 份密钥落在磁盘上 | 1 份 | N 份 | 用 `credentials.path` 指向**同一份只读文件**，密钥只有 1 份 |

**关键实测支撑（这三条决定了 A/D 赢）**：
- E3：`DSH_HOME` 隔离**真的干净**——全新 home 冷启动成功、自动选端口、只占 1.9MB 磁盘。**这条路不用发明任何东西。**
- E2：活跃实例内存是 1.1–1.7GB（不是文档说的"应该不大"）。这决定了单机人数，必须写进容量表，不能拍脑袋。
- E5：`/api` 的信任栅栏**自己声明"is not an auth layer"**，而 nginx 已经监听 `0.0.0.0:80`。**多用户的第一步不是隔离，是不裸奔。**

## 2.2 推荐：**方案 D**（= A + 共享只读 profile + systemd 模板 + 空闲回收）

推荐理由，按工程优先级：

1. **它把"故障影响面"压到 1 人**，这是多租户系统里最值钱的一条性质，而且**它今天就能拿到**（E3 证明可行）。
2. **它让"每用户"这个维度天然落在日志/数据/限额上**：`data/u_xxx/` 一个目录 = 一个用户的全部，删除用户 = `rm -rf`（D20 的账号删除入口因此变成 3 行脚本，而不是一条跨表联删）。
3. **它把 D10 和"多用户"这两个任务合并成一个**（见第六节：我把 D9/D10/D11 判成同一个交付物）。
4. 它**不排斥 B**：网关到 DSH 的协议（转发 `/api` + WS）在 A/D 与 B 上**完全一样**，所以将来真要切 B，网关不用重写——**这是"选错了也不锁死"的选择**。

**方案 B 我明确否掉**，理由是一条具体的、不可辩驳的：`agent.steer/inject/cancel` 是 Agent 实例方法（E6），而单进程多用户意味着**所有人的接收层在同一个事件循环里**。只要有一个用户触发工具循环（进程内 bash/subagent 都是同进程的），**所有人的首字一起被拖慢**——而你的产品前提就是首字 ≤1.5s。**B 把延迟 SLA 变成了全体共享的公共资源，这在架构上不可接受。**

**方案 C 我暂缓**：不是技术不行，是**本机只有 1 台、磁盘 80%、单人团队**，容器只会把故障排查变难（你要先学会在容器里看 session.jsonl）。**C 是信号驱动的**（见 2.3）。

## 2.3 什么信号出现时该换方案（给数字，不给感觉）

| 信号（可测量） | 阈值 | 换什么 |
|---|---|---|
| 同时在线用户数 | **> 12** | A/D → 加第二台机器做进程池分片（网关按 userId 取模路由）；**不是**切 B |
| 单机总 RSS | **> 5GB**（本机 7.2GB） | 同上；或上 C（容器 + 内存配额） |
| 活跃实例常驻内存 | **回落到 <250MB**（对齐 E3 的 203MB） | 不用换架构，此时 A/D 可撑 20+ 人 |
| 事件循环延迟（p99 turn 间隔） | 某用户的操作导致**其他用户**首字 p99 上升 > 300ms | 立刻切回纯进程隔离，禁止共享任何进程内资源 |
| 磁盘水位 | **> 85%** | 先加盘/加清理，**在换架构之前** |
| 单用户峰值上下文 | 真的超过窗口 50%（`REVIEW-appdev` 假设 A） | 说明"个人助理不会长会话"的前提错了 → 整个记忆/压缩设计要重做 |

**一句话**：`DSH_HOME` 一进程一用户 这条路，**在 30 个注册用户 / 12 个同时在线之前不需要换**；换了也是**加机器**，不是**改架构**。

---

# 三、控制器放在哪

**结论：必须是 (b) 作为 DSH 进程内的一个 cordis 插件，并且是"每用户进程内一个实例"的形态。**

我原来倾向于"独立进程通过网关通信"（很多系统的正确做法），但**在这个宿主上，那条路被三个具体的接口事实堵死了**。我把证据摆出来：

### 3.1 从进程外，你能拿到什么？（逐条对着 PLAN 的需求验）

| 控制器需要的能力 | 进程外能拿到吗 | 依据 |
|---|---|---|
| 读接收层的**实时 token 级输出** | **勉强能，但很贵** | `/api` 的 WS 下行（`MUX_EVENTS_PATH`/`HOST_EVENTS_PATH`）会推 `assistant/chunk`，且 `/api` 栅栏只需 loopback 或 `trustedHosts`（E5）。**但**：E12 实测单 chunk ≈220 字节、一页 50 消息 = 52k 事件 / 11MB。跨进程读每个 token = 每条 220 字节的帧。**做"300ms 速率估计"这种毫秒级判决，网络跳数 + JSON 解析本身就在你的预算里。** |
| 判"是否收尾/被打断" | 能 | `assistant/message` 带 `interrupted: true`，且是持久化事件 |
| `cancel` | **能** | `dsh-host-apiproxy` 暴露 `api.sessions.cancel` → 内部走 `agent.cancel({kind:"user"}, {keepInbox:true})`（E6） |
| `steer` / `followup` | **能，但语义被降级** | 只剩一个 `mode: "steer"` 的发送接口，而它**硬编码 `source = {kind:"user", rpcId}`**（E6） |
| `inject`（不唤醒、只喂上下文） | **不能** | 没有对应的远程语义；远程发送只有 steer/followup 两种模式，**都是"唤醒"** |
| 以**非用户身份**注入消息 | **不能** | 同上，`source.kind` 永远是 `user` |
| 拿到 `agentId → sessionId → 角色实例` 的映射 | 能（D11 本来就要建） | — |

### 3.2 拿不到会怎样——三条具体后果，不是"不方便"

**(1) `inject` 缺失 = 控制器失去了唯一不干扰 agent 的通道。**
`inject` 的语义是"放进 next-step 队列但**不唤醒**"（`dsh-agent-loop:405` 注释与实现），这正是"给接收层补充上下文但不要它现在说话"所需要的。进程外没有它，你只能用 `steer`——而 `steer` **会唤醒 idle 的 agent**（`send()` 里 `if (wakeup) this.wakeDriver()`）。**后果：一次"只想喂上下文"的操作，会让接收层多说一轮。** 这就是"控制器变成了第四个声音"的具体发生方式。

**(2) `source.kind` 永远是 `user` = PLAN 的 D4 在架构上不可能实现。**
PLAN D4 原话是"按 `source` 标记分路；**系统注入的回应永不外发**"。而从进程外注入的消息，**source 就是 `user`**——分路依据消失。更糟的是炸点 2：这些 `user/message` 会被 UI 渲染出来。**所以"控制器放进程外"直接导致 D1/D2/D4 这条关键路径上的任务需要重做。**

**(3) `cancel` 的默认行为会丢数据。**
`cancel(cause, options)` 里，**`keepInbox` 默认 false**，会把 inbox 清空（`this.inbox.clear()`）。远程 API 走的是 `keepInbox: true`（E6）——**这是对的**，但如果将来有人自己接一个进程外控制器去调 cancel，就有很实在的丢注入上下文的风险。**这条要写进代码评审清单。**

### 3.3 那记忆库和指标呢？——**这两个可以拆出去，而且应该拆**

不要把"控制器"当一个整体判决。按**是否在关键延迟路径上**拆：

| 组件 | 位置 | 理由 |
|---|---|---|
| 事件采集 / 判决 / steer / ETA / 续话 | **进程内插件** | 毫秒级判决 + 需要 `inject` + 需要 `source.kind` |
| 记忆库（读写） | **进程内插件，落盘到 `data/<userId>/`** | 粗查要"毫秒级"（v5 §⑧ 决策 4 自己定的），跨进程做不到 |
| 指标 / trace / 审计 | **可以进程外**（异步刷盘 + 只读分析） | 不阻塞主链路（v5 §② 组件 12 自己写的），且它天然是"事后查"的 |
| 网关 | 进程外（独立服务，反代到各 DSH） | 它只需要请求级信息，不需要 agent 内部状态 |

**判据一句话**：**凡是需要"在 agent 说话的同一毫秒里做决定"的，必须在进程内；凡是"事后才看"的，必须拆出去。**

---

# 四、级联的工程风险

## 4.1 `maxDepth` 设多少

**设为 2，并且必须显式设置。**

依据：

- DSH 的 `dsh-tool-subagent`，`maxDepth` **默认就是 3**（E8），所以"不设 = 失控"这个说法要修正为"**不设 = 它会自己选 3**"。这更危险，因为**它是静默的**。
- 深度语义是**绝对深度**（`resolveChildDepth` = `delegationDepthOf(parent) + 1`，且 `delegationDepthOf` 取 `session.header.delegationDepth` 与运行时的**最大值**）。你的处理层本身若是子 agent，它就是 depth 1；它再 fan-out 一次就是 depth 2；**depth 3 意味着"处理层 → 专家 → 子子任务"**。
- 而 **`PLAN.md` 里画的是"处理层 → 专家 → 子任务 → 子子任务"——那已经超过 2 了。** 文档画的图和文档建议的上限**自相矛盾**。

**改法**：处理层作为**顶层 agent** 起（depth 0），maxDepth = 2，于是允许的是"处理层 → 专家 → 子任务"，而**"子子任务"被结构性拒绝**（会抛 `SubagentDepthError`）。这条要写进配置 + 一条测试。

## 4.2 递归失控的真实后果（延迟 / 成本 / 死循环，分开说）

- **死循环：会被框架挡住，不必担心。** `delegationDepth` 单调递增 + `SubagentDepthError`（超限抛错，不是静默继续）。**所以"无限递归"不是真实风险。**
- **延迟：是真实风险，而且是产品级。** 实测基线：深层 p50 6.5–7.9s。级联**不是并行加速器**，它是**延迟串联器**——融合节点必须等所有兄弟节点（`subagent` 的 one-shot 完成才返回文本），所以 `p50(级联) ≈ max(p50(节点)) + 融合调用`。
  → **一次 3 节点 fan-out + 融合 = 6.5s + 1.3s ≈ 8s**，比不做级联**更慢**。而反馈层的职责是"承接已说的话并说出结论"——**它等 8s，接收层就得独白 8s，这就是"每条消息都要低延迟"被级联直接击穿的地方。**
- **成本：乘数效应是真实的。** 未命中 prefill 与命中差**50 倍**（$0.300/M vs $0.006/M）。每个 fan-out 节点都是一份独立上下文 → 一次 3 节点展开 = **3 份全量冷启动 prefill**（E9 也说明跨层前缀本来就不一致，见 4.3）。**这是"级联在成本上其实是乘法"的准确表述。**

## 4.3 "共用 system 前缀 + 尾部标记"——工程上怎么落地才不违反

**先说一个我刚实读到的、文档里没有的事实（E9）**：`dsh-subagent` 会给**每一个子 agent** 的上下文追加一条固定文本块
`SUBAGENT_DELEGATION_CONTEXT = "You are a delegated subagent: your permission scope was fixed when you were started…"`，
而**父 agent 的 system 里没有这一句**。

**推论（这是本报告里最有工程价值的一条）**：
- ✅ **兄弟节点之间**（同层、同 depth、同 persona）首字节前缀**可以**一致 → 90% 跨节点命中**成立**。
- ❌ **父子之间**永远多一段 → **跨层命中 0%**（必然，不是运气问题）。
- ⚠️ 而 v5 画的级联图里**融合节点是父、专家是子** → **融合节点与所有专家之间 0 命中**。而融合节点恰恰是"要快"的那个（它决定反馈层何时能开口）。

**所以这条实测结论必须改口径**：
> "级联节点共用 system 前缀"**只对同层兄弟节点成立**。工程目标应写成：**"同一深度的节点之间前缀逐字节相同"**，而不是"所有节点共用前缀"。

**落地四件套（缺一件就会在两周内被破坏）**：

1. **单一装配入口（唯一构造上下文的地方）**。一个函数 `assembleContext(role, nodeSpec, memory, tailBlocks)`，**它内部先放一个逐字节相同的 `systemPrefix` 常量块**（规则 + 工具 schema + 该用户记忆块），**再放角色差异**。任何人不得自己拼 prompt。
2. **差异只能放尾部，且尾部必须是"角色差异之外的固定块序列"**。禁止把"节点名/当前时间/随机 id"放进 system——**这正是 `REVIEW-appdev` 说的"缓存杀手"，它的破坏力比"记忆插在哪"大一个数量级**。
3. **前缀指纹不一致就在启动时抛错**（不是告警，是抛错）：启动每个节点时算 `sha256(systemPrefixBytes)`，与进程内已记录的父/兄弟指纹比对，**同一 depth 内不一致 → 拒绝启动该节点**。这条要在**生产也开**（成本是一次 sha256）。
4. **"不再有跨节点 cache"这条不能自动测**（DSH 不透出 cache 命中数）。所以**唯一可测的代理指标是轮级 token 使用量**：trace 里落每轮的 `prompt_tokens / cached_tokens`（若 provider 返回），**跨节点缓存一旦失效，prefill 会从"接近 0"跳到一个稳定的大值**——这个跳变是可观测的。**没有这个数字，你在 §5 约束 3 上是盲的。**

## 4.4 级联 vs "每条消息都要低延迟"——用数字说明冲突

**结论：冲突，而且冲突点在"反馈层什么时候能开口"。**

| 路径 | 延迟（用实测 p50） | 用户感知 |
|---|---|---|
| 单节点处理（v1 实测） | 6.5–7.9s | 接收层独白 ~7s，反馈层再开口 |
| 单节点 + 经验直出（第 3 次同类） | **1.5s** | 流畅 |
| **fan-out 3 节点 + 融合** | **≈ 8s**（max(6.5) + 1.3） | **比单节点更慢** |
| **纵向递归 2 层** | **≈ 8s + 子节点融合** | 更差；且每层都独立冷启动 |

**三个数字下的判决**：

1. **级联的延迟收益是负的**（除非节点间真正并行且融合极快——但融合本身是一次 LLM 调用，1.3s）。
2. **它的成本乘数是正的坏方向**：3 节点 = 3 份独立 prefill。**"共用前缀"能救兄弟节点的钱，救不了首次展开的钱**，因为**第一次展开必然是冷启动**。
3. **它和产品目标是根本冲突的**：v5 §③ 自己的时序是"处理层 0.9s 就出结论"——**那个 0.9s 是简单问题单节点的成绩**。级联恰恰用在**难题**上，而难题恰恰是**用户最愿意等**的场景。

→ **所以唯一自洽的用法是：级联只用在"用户明确要求深入"或"接收层已经说了足够多、等待成本已被吸收"的轮次上，绝不能用在首轮。** `PLAN` D14 的"先判难度再展开"方向是对的，但**判据不能是模型自觉**（E8：没有并发上限），必须是控制器里一条**可测量的准入规则**：例如"该品类 30 天纠正率 > 0.3 **且** 本轮用户输入长度 > 200 字 **且** 当轮预算未超限"。**否则第一版一定是"每个问题都 fan-out"。**

---

# 五、三个已实测约束：工程上怎么强制不被破坏

原则：**不靠人记住，靠"绕不过去"。** 每条给"不变量的位置 + 谁来校验 + 失败时会怎样"。

## 约束 1：记忆/摘要只能尾部追加 → 不靠人记住的做法

**核心手段：把"装配"变成一个只能追加的类型。** 不是加注释，是让"改写前缀"这件事**在代码里不可表达**。

1. **单一装配入口 + append-only 数据结构。** `ContextBlocks` 内部是 `readonly Block[]`，对外**只暴露 `append(block)` 和 `truncateOldestFromTurn(turn)`**，**不暴露 `splice`/`set`/`insert(index, …)`**。接收层/反馈层/处理层**没有任何 API 能改已有块**。
2. **不变量断言（每次装配都跑，生产也开）**：
   ```
   assemble(blocks, newTail) →
     assert(serialize(blocks[0..n]) === serialize(previousSerialize))
     // 前 n 个块序列化后必须逐字节等于上一轮
   ```
   失败 → **抛错并拒绝发请求**，不是记日志继续。理由：一个被污染的前缀会静默吃掉 18 倍成本，**失败要响亮**。
3. **单元测试（这是 `REVIEW-appdev` 提的那条，我把它具体化）**：构造 20 轮对话，断言第 K 轮序列化后的 prompt **前 K-1 轮的字节完全不变**（`Buffer.compare === 0`）。这条测试是**唯一能防住"三周后被某个新功能悄悄破坏"的东西**。
4. **压缩/摘要的形态强制为"新增一个尾部块"**——禁止改写、禁止删除旧块。旧块已经命中缓存，**留着几乎免费**（$0.006/M）。
5. **代码评审清单加一条**：任何触及 `assembleContext` / `ContextBlocks` 的 PR，**必须附带"字节稳定性测试仍然通过"的证据**。`REVIEW-appdev` 说的"评审清单"在这里才真正可执行——因为它指向一条能跑的测试，而不是一句原则。

## 约束 2：级联节点必须共用 system 前缀 → 谁来校验

**校验者必须是代码，且必须在"启动节点时"，不是"事后看指标时"。**

1. **前缀只在一个地方生成**：`buildSystemPrefix(userId)` → 返回**冻结的字节串**。所有节点（父/子/兄弟）都从这里取，**不允许任何节点自己写 system**。
2. **启动时校验（fail fast）**：节点创建前算 `sha256(prefixBytes)`，与**同 depth 的既有节点指纹**比对：不等 → `throw`，该节点起不来。**宁可起不来，也不要带着 0 命中跑。**
3. **配置版本号 + 变更即失效**：persona/工具集/前缀块的任何改动 → **版本号 +1 + 全量缓存作废**（这是正确的行为，不是 bug）。`REVIEW-appdev` 第 7 条（提示词版本化）在这里落地：**每轮 trace 落 `prompt_version`，出问题能一眼定位"改动 7 之后开始变差"。**
4. **E9 的修正必须写进设计**：父子之间**做不到**共用前缀，所以：
   - **不要**把"融合节点"设计成需要父级前缀命中的角色；
   - **同一层**的节点（专家 A/B/C）**必须**逐字节同前缀——这才是 90% 命中的适用面；
   - 把这条口径改成 **"同 depth 逐字节相同"**，并写成测试：`assert(prefixHash(nodeA) === prefixHash(nodeB))`，`assert(prefixHash(parent) !== prefixHash(child))`（后者是**预期行为**，防止有人误以为它该相等而去做"优化"）。

## 约束 3：缓存不跨 agent 共享 → 对"级联"意味着什么代价

**它意味着级联的成本结构是"每个节点各自冷启动"，且这个代价无法通过"共用 system"完全消除。**

- 兄弟节点：首字节前缀相同 → 90% 命中 → **第一个节点付全价，后续兄弟接近免费**（实测 90%）。这是你唯一拿到的优惠。
- **父/子/融合节点：0 命中 → 每次都全价 prefill。** 一次 fan-out-3 + 融合 = **1 次冷启动（第一个专家）+ 2 次近免费 + 1 次全价（融合）**。
- 推论：**级联的经济性取决于"单层并行度"**。`3 节点 + 1 融合` 的成本 ≈ `2 次全价 prefill + 3 次生成`；`2 层深链`（处理层 → 专家 → 子任务）≈ **每一级都全价**——**这就是"深度"比"宽度"贵得多的准确原因，也是 maxDepth=2 而不是 3 的经济理由。**
- **工程上必须做的记账**：trace 里落 `nodes_spawned / nodes_per_depth / est_prefill_full_price_calls`。**没有这三个数，"级联省钱"和"级联烧钱"你分不出来。**

---

# 六、`PLAN.md` 的工程层面问题

## 6.1 估时/依赖错的，和"其实是同一个任务"的

**(1) D1 不是一个任务，是三个；而且它是"不可测"的写法。**
D1 写的是"事实源 + 角色视图"，`ARCHITECTURE-v5` 组件表里挂着 8 个逻辑组件。**一个交付物挂 12 个组件，等于没有交付物。** 拆成：
- `D1a` 数据结构（事实源 schema + 3 个视图投影）→ **可单测，无外部依赖**
- `D1b` 上下文装配入口 + append-only 不变量（第五节约束 1）→ **可单测**
- `D1c` 后端骨架（会话生命周期 + 崩溃重放 + trace 落盘）

**并且：D1b 必须先于 D1a 完成。** 理由：D1a 产出的"事实源"最终要变成 prompt 里的尾部块；如果装配入口还没定，D1a 的数据结构会被后面反复改。PLAN 把 D1 当成"先动它就不会返工"，**正确，但要再往下切一层**——否则你会在 D1 里做两周，然后发现装配入口没定，全部重来。

**(2) D9 和 D10 其实是同一个任务，D11 是它们的字段，D20 是 D9 的一行。**
- D9（网关）+ D10（进程池）→ 一个交付物：**"一个能起/停/回收 `/api` 反代的进程池"**（E3 已证明 25s 能起一个，D10 的"难"被高估了）。
- D11（三段 ID 映射）→ 只是上面的**一张表 + 一条索引**，不是独立任务。它是 D10 的 schema。
- D20（凭证与合规）→ 密钥不下发客户端 = **网关的配置**；账号删除入口 = **`rm -rf data/<userId>`**。**不需要作为一个独立任务排期。**

**(3) D14（级联）和 `D3`（任务队列）是同一个任务。**
级联 = 多个在途任务 + 归属 + 合并。`D3` 的任务队列就是它的调度器。**分成两个任务会造成两套并发状态机**——而 v1 踩的 5 个坑里有 2 个就是"两条路径不一致"。**合并。**

**(4) D13 和 D19 是同一个任务。**
"切后台服务器继续跑，回前台拉取"和"处理层 10s+ 锁屏后推送"是**同一件事的两半**：都是"用户不在看的时候，事件怎么到达"。分两个任务 → 会出现"能拉取但不推送"或"推送了但拉不到"的错配。

**(5) D17（可观测性）被排在 M5，这是错的——它必须在 M0 之前。**
理由：`REVIEW-appdev` 第 2 部分第 2 条说"每一条裁定都依赖你得能看到真实数字"，我要更强：**M0–M4 的验收标准里有 4 条是"0 次"（不丢消息/不重复/不串数据），而"0 次"这个判据本身只能由 trace 提供。** 没有 trace，"12 个场景全绿"就是人工点一遍的结果，**下次点不出来。**

## 6.2 关键路径对不对——不对，有两处

**(1) 真正的最长路径不在 D1 那条。** D1 是**技术风险最高**的点，但不是**时间最长**的点。时间最长的是：

```
D9/D10(网关+进程池) → D12(流式与重连) → D13/D19(生命周期+推送) → M3 → M6(真机 3 天)
```

`D12` 是真正的时间黑洞：**WebSocket + SSE 回退 + 心跳 + 断点续传 + 去重**，这五样每一样都有独立的失败模式（半开连接、乱序、重复投递、消息 ID 在重连后失效）。它还要同时满足"**用户看到的是一条连续消息**"（PLAN 六之二 第 2 条"两段文本自然衔接"）——这意味着**服务端必须给出一个"消息分组"语义**，而这不是 WebSocket 能白送的。**我估 D12 是 D1 的 1.5 倍工作量。**

**(2) 隐藏的串行依赖（PLAN 没有画出来的）**：
- **`D14` 依赖 `D17`**：级联的诊断需要 trace。**并行做会做出一个无法调试的级联。**
- **`D14` 依赖 `D10`**：每个用户的进程池决定了级联节点跑在**谁的进程里**。这不是"共享同一个"，是**每用户一套**。
- **`D4` 依赖 `D12`**：拦截必须发生在"外发流"上，而外发流就是 D12 建的。PLAN 把 D4 排在 D12 之前（D4 在 D0 地基，D12 在 D9–D20），**顺序反了**。
- **`M0` 的验收"两台设备登录不同账号各自对话互不串"依赖 `D17`**（不然怎么证明不串）。
- **`M2`（含 D14）依赖 `D15`（成本熔断）**：没有熔断，级联的一次失控会**污染你所有后续的成本基线**（炸点 6）。

## 6.3 被漏掉的任务（每条都给"不做会怎样"）

| 漏掉的任务 | 不做会怎样（具体） |
|---|---|
| **测试工具链修复**（E10） | 所有"全绿"不可复现。**5 分钟的事，先做。** |
| **生产发布与回滚 SOP** | 现在只有"改代码 → 跑 `dsh web`"。一次坏 persona 上线 → **所有用户缓存作废 + 所有对话变傻**，而且**没有回滚路径**（E2：你已经在生产 3080 上跑了 6 天）。 |
| **进程资源上限** | 没有 `MemoryMax`/`TasksMax`，一个用户的工具循环可以**吃掉全机内存，把 nginx 一起拖死**（E1：只剩 3.1GB）。 |
| **存储成本与保留策略** | E13 实测单会话 19.9MB、磁盘 80%。**没有保留策略 = 迟早写满 = 全员写盘失败。** |
| **`turnId` 幂等 + 记忆提交对账** | 崩溃重启双写 → `hits` 虚高 → 所有基于 hits 的权重**静默错**。 |
| **密钥轮换与本地不落盘** | N 个 `DSH_HOME` = **N 份 `.credentials.yaml`**（E3/2.1 表）。轮换一次 = 改 N 个文件，漏一个就是"某个用户突然全崩"。**必须用 `credentials.path` 指同一份只读文件。** |
| **版本协商（服务器 ↔ 客户端）** | PLAN 有 D18 但排在 M5。实际上"两段文本拼成一条"是**服务端语义**（对话页 6 条 第 2 条），**客户端一升级就必须同时兼容新旧两种分片方式**——这是第一个版本就要有的字段。 |
| **崩溃恢复演练** | `REVIEW-appdev` 列了，我要强调它是**验收项**不是"建议"：不做演练就等于没有崩溃恢复。 |
| **用户级速率限制（不是 token 记账）** | 炸点 4 的唯一解。**限"在途请求数"，因为那才是雪崩的量。** |
| **data 目录的备份与恢复** | "记忆是唯一长期资产"（v5 §5）。**只有一份、还在 80% 的盘上、没有备份**，这是本报告里最贵的单条漏项。 |

## 6.4 里程碑验收标准够不够硬

**不够。全部是"人点一遍"级的，不是"机器能判"级的。**

| 里程碑 | 现验收 | 我改成（可自动判定） |
|---|---|---|
| **M0** | "两台设备登录不同账号，各自对话互不串；断网重连不丢消息" | ① 两台设备**并发**发消息，断言 A 的 `session.jsonl` 里**不出现任何 B 的文本**（字符串级断言，不是看界面）；② `kill -9` WS 后重连，断言**消息集合完全相等**（既不丢也不重，含顺序）；③ 每次重连后 `messageId` 去重表大小不变 |
| **M1** | "三个视图各自正确且能追溯到同一份事实" | 一条单测：给定一轮对话 → 断言三个视图的**字段结构**（schema）与**关键字段值**，且三者的 `factId` 指向同一条记录 |
| **M2** | "场景 1/2/3/4/11 全绿；级联按难度展开正确" | 场景断言要求**来自 trace**（不是人看）：`接管路径 ∈ {steer,inject,followup}`、**`total_llm_calls ≤ 12`**、**`cascade_fanout == 0` 对简单问题**（这条才叫"按难度展开正确"） |
| **M3** | "场景 5/6/7/10/12 全绿；切后台不丢内容" | 追加：**场景 12 必须跑"两个结论同时到达"的冲突合并**（PLAN 自己写了"合并错"是失败态，但没给合并规则）。**没有合并规则 = 这条验收无法判定。** |
| **M5** | "限额生效、trace 可查、推送到达、凭证不外泄" | "限额生效"要改成**人为触发一次，断言：① 请求真的被拒；② 用户看到明确提示（不是静默降级）；③ 该用户之外的人完全不受影响**（隔离性要在限额路径上验，因为限额代码最容易写错 userId） |
| **M6** | "真机连续 3 天：崩溃/丢消息/重复话说 0 次" | 保留，但补一条**基线**：同时落 `first_token_p50`、`cache_hit_rate`、`cost_per_turn`。**否则"不崩"只是没崩，你不知道它是不是已经慢了一半。** |

---

# 七、如果我说了算，第一个可上线版本是什么

## 7.1 工程 MVP 边界（做出来的东西）

**一句话**：**把"接收层先开口、反馈层接着说结论"这条链路做成一个朋友能在手机上用、崩了能查、账单看得到的系统。**

**做：**
1. **网关**：nginx 反代 + **token 鉴权**（不是 DSH 的 `/api` 栅栏）+ `userId → 端口` 路由 + **按 userId 限在途请求数** + 强制 `trustedHosts`。
2. **每用户 DSH 进程**（方案 D）：`DSH_HOME=data/u_xxx`，systemd 模板单元，`MemoryMax` + `Restart=always` + 空闲回收（15 分钟无活动释放）。**profile 共享只读。**
3. **控制器（进程内 cordis 插件）**：事实源 + **装配入口（append-only 不变量）+ 两条接管路径（`steer` 软接手 / `followup` 直接接管）**。**不做 ETA 三档、不做续话话题集合、不做打断禁区表。**
4. **三个角色**：接收层（flash + 关闭思考）、处理层（**单会话 + 上层对话池上限 2**）、反馈层。**三者共用一段逐字节相同的 `systemPrefix`，角色差异全在尾部。**
5. **记忆最小版**：JSONL 追加、`userId` 强制第一参数、尾部注入。
6. **每轮 trace**：`turnId / userId / prompt_version / first_token_ms / prompt_tokens / total_llm_calls / handoff_path / cost` + 4 个告警。
7. **崩溃恢复**：append-only 提案 + `turnId` 幂等 + 每日快照 + **演练一次**。
8. **客户端**：Flutter 三页（会话列表 / 对话 / 设置）+ 断线重连去重 + 账号删除入口。

**不做（第一版明确不做）：**
- **级联（D14）整条**——见 7.3。
- **ETA / 续话 / 软硬打断判决 / 禁区表**（`REVIEW-appdev` 已裁，我同意，理由是它们没有数据支撑）。
- 升格管理器、热品类池、跨用户共享、压缩管线、本地消息缓存、灰度平台、k8s。

## 7.2 上线检查清单（每一条都是"能判定的"，不是"要注意"）

**门禁类（不满足不允许上线）**
- [ ] `npm test` 在干净 checkout 上**退出码 0**（含 E10 的入口修复）
- [ ] **字节稳定性测试**通过：20 轮对话，第 K 轮前 K-1 轮字节 `Buffer.compare === 0`
- [ ] **同 depth 前缀指纹相等** / **父子前缀指纹不等**（两条都断言，后者是防误"优化"）
- [ ] **跨用户隔离测试**：`u1` 写入、`u2` 检索 → 返回空；**且 `u1` 的文本不出现在 `u2` 的 `session.jsonl`**
- [ ] **系统消息不外发**：注入 3 条 `source.kind !== 'user'` → 外发流恰好 1 条
- [ ] **没有无 `userId` 的读接口**（用类型签名检查/grep 断言，不靠人看）

**演练类（手工做一次，记录结果）**
- [ ] `kill -9` 控制器 → 重启 → 记忆零丢失、**同 `turnId` 零重复提交**
- [ ] 人为触发限额 → 该用户被拒且看到提示，**其他用户无感**
- [ ] 磁盘打满演练 → 写盘失败时用户看到明确语义（不是静默卡住）
- [ ] 恢复演练：从快照恢复一个用户的 `data/` → 对话可继续

**成本与容量类**
- [ ] 单轮 `total_llm_calls ≤ 12` 断言生效；单日成本上限生效且**当天真的停**
- [ ] **容量表写在运维文档里**：本机 4 vCPU / 7.2GB / 磁盘水位，**同时在线上限 8–12 人**，并写"超过 12 人加机器"
- [ ] `~/.dsh/sessions` 日增量与磁盘水位各有告警阈值
- [ ] 密钥只有 1 份（`credentials.path` 指向同一只读文件）；日志落盘前断言不含 `sk-`

**客户端与发布类**
- [ ] 弱网重试**不产生重复消息**（`messageId` 幂等，服务端去重表可见）
- [ ] 切后台 30s 回前台 → 内容补齐且顺序正确
- [ ] 服务端与客户端的**协议版本字段**存在于每条响应（D18 前置，不能等）
- [ ] **回滚路径验证过**：改坏的 persona 版本能在 5 分钟内回退（`prompt_version` + 一键切换）

## 7.3 直接回答："接收层 + 反馈层 + 控制器"这套机制，该不该第一版就上？

**该上，但只上它的骨架，不上它的机制。**

理由（三条，都是"用你的实测数字推出来的"）：

1. **"接收层先说一句"这件事本身不是假设，是实测。** README 的 9 倍延迟差（0.8s vs 7.9s）意味着：**如果只有一个 agent，用户在难题上会干等 6.5–7.9s。** 这不是"用户能不能感知"的问题，是**7–8 秒的沉默在移动端就是死机**。所以"先开口"必须有——**这一条我支持，且它不需要再验证。**
2. **但"由第二个 agent 来接管"是可以被砍掉的**——`REVIEW-appdev` 第 3.2 条给了正确形态（接收层被 `steer` 说出结论），v1 的 `hybrid` **已经跑通了**（README：跑着就 steer）。我同意他的判决，并补一条**工程上的硬理由**：**每多一个独立 agent，就多一份永不共享的缓存前缀**（你实测跨 agent 0%），而"反馈层"的输入**本来就是当轮的**（接收层已输出全文 + 结论），**它不需要自己的长历史**——所以它的前缀永远只有几 K，**它是最便宜的那个 agent，也是最不值得为它多担一份缓存风险的那个**。第一版让它独立起，是为了**能测"承接质量"**（A/B），不是为了架构完整。
3. **控制器必须第一天就有，但只能有"分发器"那么小。** 因为 `steer`/`followup`/`cancel` 只能从进程内调（E6），**控制器不是可选项，它是宿主接口的唯一消费方**。但它的第一版只能做三件事：**持有事实源、装配上下文（含 append-only 不变量）、决定谁说话**。`REVIEW-appdev` 说"v5 那句'控制器是结构必然'是偷换概念"——**我同意一半**：**分发器是必然的，那 12 个组件不是。** 第一版控制器代码量目标：**< 400 行**，超了就是又在上机制。

**一句话判据**：**第一版上"三个角色 + 一个薄控制器"，不上"级联 + ETA + 续话 + 升格"。** 骨架是实测支持的，机制全是未验证的。

---

# 工程结论（5 行以内）

1. **最该先做**：修 `npm test` 入口（E10，5 分钟）、写"装配入口 + 逐字节 append-only"不变量与其测试、落每轮 trace、给级联与重试加硬上限——**这四样都不依赖任何未验证的假设，且不做它们后面全是盲调**。
2. **最大的工程风险**：**控制器的"唯一真源"没有持久化与幂等保证**（炸点 7 + 6.3），一次崩溃双写会**静默污染全部记忆权重**，而这是用户唯一带不走的长期资产。
3. **第二个风险**：`/api` 明确自述不是鉴权层（E5），而 nginx 已在 `0.0.0.0:80`——**多用户第一步是不裸奔，不是做隔离**。
4. **架构选择**：**每用户一进程（方案 D），控制器做成进程内插件**；级联**第一版不做**，要做则 `maxDepth=2` 且只对"用户要求深入"的轮次开启。
5. **改变方案的信号**：跨节点缓存命中率长期 <80%、20 问题盲评"三层"胜率 <60% → 砍掉级联与独立反馈层；同时在线 >12 人 或 单机 RSS >5GB → **加机器分片（不是改架构）**；单用户峰值上下文真超窗口 50% → 说明"个人助理不会长会话"的前提错了，整套记忆与压缩设计重做。
