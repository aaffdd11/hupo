# 工程实践师对架构师的逐条回应（对辩稿）

> 作者：工程实践师（流式对话 / 移动端全链路）。
> 反驳对象：`DISCUSS-arch.md`。我的原立场：`DISCUSS-eng.md`。第三方实测：`REVIEW-eng.md`。
> 规则：能驳倒的驳，驳不倒的当场认，并说明**被哪一句说服**。不客套。
> 本文只在两处偏离上面的文件：我**新核实了几个宿主事实**（下面 §0），因为架构师有 3 条主张的前提取决于它们，而他在 G1–G5 里把这 5 条假设全标成"不成立请驳回"，**其中两条不成立**。

---

## 〇、进入辩论前先钉死的事实（我这轮实测/实读，构成本文所有数字的支点）

| # | 事实 | 来源 | 推翻了什么 |
|---|---|---|---|
| **F1** | `cancel` 的停止粒度**只到 chunk 边界**，没有字符级暂停。中止点在 `for await (const chunk of stream)` 里的 `signal.throwIfAborted()`，且每收一个 chunk 就先 `session.append("assistant/chunk")` —— **中止时已经落进 session 的 chunk 不可撤** | `dsh-agent-loop/lib/index.js:615-648` | 直接反驳"推迟窗口不需要缓冲"（见主张 11） |
| **F2** | 宿主**没有 per-chunk 钩子**。`agent/pre-step`（每 step 一次，可 `enter`/`reject`，可注入 messages，`index.js:501-512`）与 `agent/turn-stopping`（每 turn 结束一次，`index.js:565`）是**step 级与 turn 级**，不是 token 级；`agent/request`（`index.js:708`）只返回**请求配置**，**拿不到 `preparedCall`**（`preparedCall` 由 `llm.prepareCall()` 内部产生，`index.js:718`），所以**不能靠它包住 stream** | 同上 | 修正 G2 |
| **F3** | 但进程内**能逐帧观察**：`ctx.on("session/event", (session, event) => …)` 可订阅每一个事件，包括每条 `assistant/chunk` | `dsh-session/lib/index.js:1471-1476` | 支持"进程内零网络成本逐帧判决"，同时说明"进程外不可行"又多一条理由 |
| **F4** | 宿主**自己**已经把 token 级增量批处理了：reasoning 不是每 token 一条事件，而是 `reasoning-chunks` 一条事件带 `{dt:[...], texts:[...]}` 两个数组（实测量级：**284 B/事件**，某会话 803 条） | 实读 `~/.dsh/sessions` 真实会话 | 为"逐轮落盘"提供**宿主自己的先例**，也让"每帧落盘"变成明显违反宿主习惯的做法 |
| **F5** | `assistant/chunk` 实测 **185 B / 481 B**（两个会话），text-delta 实测 **139 B/事件**（431 事件 = 60 100 B） | 同上 | 写放大的分母（见主张 5） |
| **F6** | 一段 16 045 行的真实会话里，`assistant/message` + `assistant/chunk` 两类事件合计 **5.61 MB JSON**（`472` 条 message 均 8 015 B、`3 800` 条 chunk 均 481 B），而它承载的**真实文本只有 31 153 字 ≈ 93 KB**。**文本占 1.67%，信封开销 60.0 倍**（未计更大的 tool/result 事件） | 同上 | 把"存不存文本"这个问题**从成本问题降级为噪声问题**，真正的成本在"落几条事件" |
| **F7** | 日志**会被折叠**：`dsh-compaction-basic` 用 `surfaceOp:'replace'` 把一段 span 替换成摘要（`lib/index.js:582-607`） | 实读 | "反正 chunk 都在日志里"有一个**时间地平线**，这正是"不可回溯"的机制（见主张 5） |
| **F8** | 清空/替换面只对模型可见，人类转写以 append-origin 为耐久源 —— 这条我复核后**确认无误**，不再争 | 双方一致 | — |

---

# 一、逐条回应

### 他的主张 1：v6"两块回答"的边界谓词自相矛盾 —— "能立刻确定"的判据是研究结果，而接收层拿不到研究结果，所以 S4 纠偏是这条边界定义自己制造出来的

**判定**：成立（这一条我当场认，且这是我今年读到的最干净的一处"谓词级"驳论）

**我的回应**：
我在 `DISCUSS-eng.md:31-44` 把"三份文本不一致"当成第一难点，架构师指出的是**更上游的原因**：如果接收层只能靠**类别先验**猜"什么能立刻确定"，那么它猜错的产物**不是废话，是一句需要被推翻的话**，而推翻它就是 S4。也就是说我原来把 S4 当成"衔接形态里的一种"，架构师把它还原成"**边界定义的一个必然输出**"。
这一句我要明确认下来，因为它改了我的实现顺序：我原来打算把 S1–S4 当成提示词/形态表问题（P2 做），现在**S4 的产生率是划界方式的函数**，所以划界方式（P0/P1）必须先定。

**不过他的论证有一处滑步**（我要指出来，因为它影响"能否修"的判断）：谓词互斥**只在"接收层必须主动断言结论"这个读法下成立**。如果接收层的输出被约束为**只描述"我要做什么"、不断言"事实是什么"**，那两条禁令不再互斥 —— "先去确认连接池"不是"未经研究的结论"，它是**一个承诺**。也就是说：**这个矛盾不是"划界方式"制造的，是"接收层被允许说断言句"制造的。** 两者的区别有工程后果：按他的读法，修法是"换划界依据 + 控制器独占停止"；按我的读法，修法是"**在出口处对断言句式做硬门控**"，成本低一个数量级（见主张 2 的 (c)）。**我认为两者都要做，但出口门控是更便宜的那一半，而他在 1.1 里一个字没写输出侧。**

**实现的真实代价**：出口断言 ≈ 半天（闭集触发词表 + 20 条回归用例）。

**如果他是对的，我该先写哪段代码**：`src/controller/emit-gate.js` —— 接收层 turn 内**禁止断言句式**（"结论是/应该是/根本原因是/问题出在"）+ 长度上限（≤80 token）。**在写任何提示词之前先写它**，因为它是"接收层说错话"的**唯一机器可判定的防线**，而提示词防线不可判定。

---

### 他的主张 2：划界依据从"内容类型"改为**信息来源（能力门控）**：接收层的可用输入 = 用户本轮原话 + 粗查记忆 + 分类标签；它不持有工具、不持有精查记忆、不持有处理层任何产物

**判定**：部分成立。方向对，**但他给的机制（能力门控）只覆盖了输入侧的一半，而且"入参类型"在 TS 结构化类型下不构成他以为的那种硬墙** —— 这条我要顶。

**我的回应**：
**(a) "入参类型里根本没有这一项"是无效论证。** DSH 的 agent 上下文不是一个"你传进去的对象"，而是**一条可变的 session 事件流**（`agent/pre-step` 收到的是 `claimed` messages + `context` section，`index.js:501-512`）。工具产物、子 agent 返回、注入块**全都是 session 事件**，会进入同一条装配流水线。所以"显式参数类型"约束不了 `assembleContext` 从 session 里读到什么 —— **越界不发生在你传参的那一刻，发生在你装配的那一刻。**
要真的做成硬边界，形态必须是**投影而不是过滤**：

```
project(role, sessionEvents) → Block[]      // deny-by-default + 白名单
```

区别很大：**白名单**会在宿主某天新增一种事件类型时**启动即报错**；**过滤器**（allow-by-default）会让它**静默通过**。架构师说的"能力门控"如果落成"装配时不传处理层产物"，那它就是一个过滤器 —— **三个月后宿主加一类 `assistant/artifact` 事件，接收层就又能看见"结论"了，而没有任何东西会响。**

**(b) "接收层不持有工具"这笔钱他没算。** 工具 schema 是 prompt 里最大的一块（REVIEW-eng 也是这么说的），而 v6 要求**三个角色共用一段逐字节相同的 `systemPrefix`**。二选一：
- 工具 schema 放进共享前缀 → 接收层**看得见工具**（只是不给调用权），"不持有工具"落空；
- 给接收层单独做一份无工具的 prefix → **跨角色前缀命中直接废掉**，而接收层⇄反馈层的前缀命中是这个系统里最值钱的一份（同用户、同会话、每轮都发生）。

**我的判决：第一版让接收层"看得见工具、不给调用权"** —— 工具留在共享前缀里，用 role 段禁调用 + 出口断言"接收层 turn 内不得出现 tool call"。理由：①"看不见"本来也挡不住模型说"我查一下"（它靠 role 段，不靠 schema）；②架构师的方案为纯洁性多付**一份全价冷启动前缀**，而这份钱是每轮都付的。**这是我的原则上最不同意他的一处：他把"能力门控"从"接口设计手法"升级成了"纯洁性要求"，而每一条纯洁性都要用前缀命中率来买单。**

**(c) 输入侧门控不够，必须配输出侧门控。** 置信度门控我不推荐 —— 它要再调一次 LLM，而首字预算是 1.5s、接收层首字已占 0.8–1.0s，**没有钱给第三次分类调用**。可执行的输出侧门控是**结构性的**：硬 token 上限 + 断言句禁式表 + 禁用跨句指代开头（"这个/它/另外"）。零延迟代价，可机器判定，而且**它才是 S4 的真正止血点**。

**实现的真实代价**：`project()` 白名单投影 + 角色化装配 = 2–3 天（含未知事件类型拒启的启动自检）。比"传参类型"贵，但它是唯一能挡住"三个月后新增事件类型"的形态。

**如果他是对的，我该先写哪段代码**：先写 `project(role, events)` 的**白名单 + 未识别事件抛错**，再做角色化装配。顺序不能反：先做装配再补投影，等于把所有 prompt 构造点写两遍。

---

### 他的主张 3：**停止权归控制器独占**，接收层"只负责停在哪都不难看"，不得自我收尾（不得输出"我先去确认"后自行结束），控制器独占"什么时候停"

**判定**：**部分成立，表述过度设计。** 我把这条拆成两件事，其中只有一件是权利，另一件是结构。

**我的回应（这是议题 1 的核心，我用实测代码回答"怎么实现"和"会不会每 chunk 一次往返"）**：

**① 宿主里"停止"只有两个原语，粒度分别是 step 和 chunk —— 没有"停在句子边界"，没有"暂停后恢复"：**

| 原语 | 粒度 | 语义 | 代价 |
|---|---|---|---|
| `agent/pre-step` 返回 `reject` | **step** | 阻止**下一个** LLM 调用开始 | 零延迟（本来就要过这个 hook） |
| `agent.cancel(cause, {keepInbox})` | **chunk 边界**（F1） | 中止**当前** step；已 append 的 chunk 不可撤 | 一次函数调用 + 等 `assistant/message{interrupted:true}` 固化 |
| ~~暂停 / 恢复 / seek~~ | — | **不存在** | — |

所以"控制器要停顿在任意 chunk 边界吗？"这个问题的答案是：**它连 chunk 边界都停不住精确 —— 它只能"要求停"，实际停在下一个 chunk 到达时**（F1：`throwIfAborted()` 在 `for await` 循环体内，每收一个 chunk 才检查一次）。要求发出到真正停下之间的**那一个 chunk，正是"半句"的来源**。这不是实现细节 —— **它决定了"推迟窗口"必须自己持有缓冲**（见主张 11）。

**② "会不会把流式搞成每 chunk 一次往返"—— 进程内不会，进程外会，而且这是他论证里最有价值的一条副产品。**
- **进程内**：`ctx.on("session/event", …)`（F3）每帧一次回调，判标点只需对**新增字符**做 O(1) 尾部检查，~µs 级。60–70 tok/s 的帧率下**开销可忽略**。所以"控制器逐帧判决"这个动作本身是免费的。
- **进程外**：每帧 = 一个 ~185 B 的 WS 帧（F5），一页 50 条消息实测 52 k 事件 / 11 MB（REVIEW-eng E12）。**毫秒级判决要跨网络 + JSON 解析，每一次都在 1.5s 首字预算里扣钱。**
→ **结论：架构师要求的"控制器逐帧判决"是一条新的、独立的、要求控制器必须进程内的论证**，比 `source.kind` 那条更贴近延迟 —— 因为 `source.kind` 还可以靠"网关补标记"绕过，而**逐帧判决绕不过去**。这一条我加进自己的结论，署名归他。

**③ 但他的表述有问题：他把"中止权"和"投递门控"捆成一个"停止权"。**
- **中止权**（谁能调 `cancel`）→ 是权利，控制器独占，**已被 E6/F2 锁死**，无争议。我完全同意。
- **投递门控**（到哪一帧为止再往后不投递）→ **不是权利，是实现结构**。它必须落在**投递路径**上，而不是生成路径上。
- **"接收层不得自我收尾"在工程上不可执行**：模型什么时候结束一个 turn 是模型自己的决定。控制器能做的只有"它结束后不投递"或"它结束前 cancel"。而 `cancel` 会固化成 `interrupted:true` 的消息 —— **"接收层自我收尾"和"控制器中止接收层"在用户可见面上常常是同一条文本**（"我先去确认一下"）。控制器唯一能真正阻断的，是"接收层说完这句之后又自己接着往下说第二段"，**那是一条投递门控**。
→ 所以正确的契约表述是三条，不是一条：**`cancel` 只有控制器能调；`emit()` 出口只接受 `Utterance`（控制器无嘴）；投递水位由控制器推进，且只能前进。**

**实现的真实代价**：按他的表述实现 → 实现者会去找"暂停原语"，**白花 1–2 天**（这个原语不存在）。按三条契约实现 → 0.5 天。

**如果他是对的，我该先写哪段代码**：单例 `abortAuthority`（全仓库 grep 断言 `cancel(` 只有一处调用点）+ `emit()` 出口 gate。**先写 grep 断言，再写实现** —— 断言是这里唯一真正的护栏。

---

### 他的主张 4："接收层说到哪停"现在有三个 owner（控制器交接判决 / 接收层"末句留扣" / 反馈层首句锚点），三条都在等于谁都没负责

**判定**：成立，**但他说漏了第四个 owner，而且第四个才是第一版真正的责任人**。

**我的回应**：
第四个 owner 是**宿主自己**。v6 时序表（`ARCHITECTURE-v6.md:73-80`）假设的处理层 0.9s 出结论在实际难题档是 p50 6.5–7.9s，也就是说**大多数难题轮次里，前三个 owner 谁都不在，接待用户的只有接收层，它必须独白 5–7s**。这暴露的是我 §1.1 矛盾 1 那条：**接收层没有可外发的进度素材**，所以"说到哪停"这件事在难题档不是判决问题，是**素材供给问题**。
架构师把"三 owner"归并到"唯一出口 gate"很干净，但 **gate 只能决定"这些字不投递"，不能变出"接下来投什么"**。如果不在 P0 就给处理层加一个"可外发进度信号"字段（`{stage, found, querying}`），第一版难题档会退化成"接收层硬撑 7 秒 + 反馈层一句话结论"，也就是 v6 想消除的那种体验，只是换了个说法。
**这一条是我要给他的方案补的、他文档里没有的一环：`emit()` gate 解决了"谁有权不说话"，没解决"没话说的时候说什么"。**

**实现的真实代价**：处理层输出 schema 加一个字段 + 接收层 role 段消费它 = 1 天；事后补要动协议（因为接收层的续说会被 trace/历史记录）。

**如果他是对的，我该先写哪段代码**：处理层 `{stage, progress}` 的 schema 定义（**字段先有，填充后做**），因为它命中他自己的 L0 判据 1（历史形状）。

---

### 他的主张 5：R5 的"已输出"没有单一权威（模型已生成 / 控制器已放行 / 客户端已渲染三者可能不同）；唯一权威必须是"**控制器已放行**"，契约名改 `emittedText` 并**逐轮落盘**；**这条不落盘以后无法回溯**

**判定**：**权威口径成立（我改立场）；"逐轮落盘"必要且便宜（我认）；但"不可回溯"是"部分成立"，而他的措辞会让实现者做错一件事。**

**我的回应（议题 2 的核心，全是数字）**：

**(a) 权威口径：我原来选错了，被哪句说服？**
我 `DISCUSS-eng.md:163` 写的权威顺序是"客户端 ack 水位 > 网关投递日志 > 宿主 `assistant/message`"。架构师 `DISCUSS-arch.md:213` 说："选第三个需要客户端回执（多一跳、且弱网下不可靠）"。
**这一句说服了我，而且理由比他自己写的更硬**：我的假设是 RTT p99 400–800ms、丢包 0.5–2%（我 §0 自己写的）。**在 p99 上，"已渲染"这个事实要穿越一条 400–800ms 且会丢包的链路才能到达控制器，而承接判决发生在 150ms 内。** 我拿一个"最晚到、还可能永远不到"的信号当唯一权威，是错的。
**所以我认：唯一权威 = `committed`（控制器已放行）。** 但我要**保留** ack 水位，只是**降级、换岗位**：

- `committedSeq`：**权威**，落盘，用于 R5 / 审计 / S4 纠偏对象。
- `ackedSeq`：**不是权威，是裁剪依据**，不落盘、只在轮内存活。`handoff.text = committed.prefix(acked)`,若 ack 落后超过 N 个单元 → 按 S3 形态开口而不是假装承接。
- 不变量：`acked ≤ committed`，且 `handoff.text` 必须是 `committed` 的前缀。

**为什么必须双水位**：他的单权威解决的是"反馈层承接了用户没看到的字"（**正向错位**）。但还有**反向错位** —— `committed` 了但客户端还没画出来的那 100–300ms 里，反馈层以为自己承接到了，用户却**在它说完之后才看到被承接的那句**。弱网下这就是"它重复我说过的话"的机制。**他只堵了一个方向。**

**(b) 必要还是过度？最坏后果具体化（要求我给一个 bug 场景，我给了）**

> **场景**：用户问"线上支付偶发超时怎么办"。接收层生成两句：「这个我熟，先看连接池。**下游超时也不能排除。**」——第 2 句生成快的那个瞬间，控制器判定深答已就绪、`cancel`。`cancel` 停在 chunk 边界（F1），第 2 句的一两帧已经 append 进 session 但**没投递**。
> **如果控制器用宿主 `assistant/message` 的 content 当"已输出"**（这是默认实现最容易走的路，因为它是唯一全量的那份），交接包里就带上了第 2 句。反馈层首句写："对，下游先排除——不过结论是连接池。" **用户屏幕上从来没出现过"下游超时也不能排除"。他听到的是助手在反驳一句没人说过的话。**
> 这就是 S4 纠偏打空。而**这一轮之后没有任何数据能证明发生过**：宿主消息里两句都在，你不知道哪句投递了。

**这个 bug 的可怕之处在它的分布**：它只在"生成快于投递"时发生 —— 也就是**队列积压 / GC / 网络抖动**时。**开发机上永远不复现，线上千分之几，无证据，永久查不出。** 这就是"不可回溯"的真实含义，架构师这一点是对的。

**(c) 写放大量化（这是他要的答案，我用实测分母算）**

| 口径 | 每条记录 | 每轮条数 | 每轮字节 | 单人 30 轮/天/年 | 15 用户/年 |
|---|---|---|---|---|---|
| **逐轮聚合（他的本意）** | 文本 150–350 字 ≈ 0.5–1.1 KB | 1 | **~0.6–1.1 KB** | **~8–12 MB** | **~0.12–0.18 GB** |
| **只存水位（我的实现）** | `{turnId, speaker, committedThroughSeq, sha256}` ≈ 120 B | 1 | **~120 B** | **~1.3 MB** | **~20 MB** |
| **每帧落一条（危险误读）** | text-delta 实测 **139 B**（F5） | 200–300 | **~30–40 KB** | **~380–440 MB** | **~5.7–6.6 GB** |

对照 REVIEW-eng E13 的实测基线：**383 个会话 231 MB，单会话 zstd 已 19.9 MB**。磁盘 80%、剩 7.8 GB。
→ **逐轮聚合 = 现有日志的 ~5% 增量，可忽略。每帧一条 = 现有日志的 2.5 倍，7.8 GB 的盘撑不过两年。**

**所以"逐轮落盘"必要、且便宜 —— 但"逐轮"这两个字必须写死进契约。** 架构师写了"逐轮落盘"，但他没有给"一条记录 = 一轮，不是一帧"这个约束。**按他的文字实现，最常见的读法是"每次 emit 落一条"，因为 `emit()` 是唯一出口而出口是逐帧调的。** 这是他的方案里最可能被实现错的一处，代价 30–40 倍写放大。
**宿主自己给了正确形态**（F4）：reasoning 不是每 token 一条，而是 `reasoning-chunks` 一条事件带 `{dt:[], texts:[]}`。**"聚合 + 时序数组"是这个代码库的既有习惯**，照抄它即可。

**(d) 最省的实现：不新增表，不复用 session log 的**全文**，只加一个整数。**
关键实测（F6）：那段 5.6 MB 的会话日志，**真实文本只占 1.7%，信封开销 60 倍**。也就是说：**"文本在不在日志里"根本不是成本问题。** 真正的问题是"落几条事件"。
而全量文本**已经在日志里两份**了：`assistant/message`（均 8 015 B，含完整 content）与 chunk 事件；宿主的中止消息还带 `sourceEventSeqs: chunkSeqs`（F1，`index.js:631-648`），**精确指认"生成了哪些 chunk"**。
→ 所以 `emittedText` 的持久化形态应该是：

```
{ turnId, speaker, committedThroughSeq, sha256 }     // 每轮一条，~120 B
emittedText := replay(assistant/chunk, seq ≤ committedThroughSeq)   // 读时重建
```

**成本：单人 1.3 MB/年。重建代价：一次事件扫描（内存，无网络）。** 这是架构师方案的最省实现，比"存一份文本副本"省一个数量级，比"每帧一条"省 300 倍。

**(e) "不可回溯"我同意吗？—— 部分同意，而且我必须说清楚哪部分能恢复，因为说错会导致实现者去存全文副本。**

**同意的部分（而且我给他的理由比他更硬）**：他认为"不记就永久丢失"。我原来的想法是"chunk 都在日志里，能重放"，**这个想法是错的**，被两条事实否掉：
1. **日志会被折叠**（F7：`dsh-compaction-basic` 用 `surfaceOp:'replace'` 把 span 换成摘要）。所以"chunk 都在日志里"**有一个时间地平线**，不是永久保证。
2. **投递日志（网关侧）必然是滚动的** —— 我自己在 `DISCUSS-eng.md:155`（C6）就写了"服务端保留每 turn 最近 N=200 个单元用于断线续传"。**一旦投递水位是滚动窗口，历史轮次的"投递了多少"就真的没了**，而宿主侧只有"生成全文"，**尾巴长度无从判定**。
→ **"不可回溯"成立，但成立的机制是"记录这件事的地方是滚动窗口，不是耐久源"，不是"没人记"。** 这个区别重要：它意味着**解决办法是"把水位也写进耐久日志"（120 B/轮），而不是"存全文副本"。**

**不同意的部分（可重放恢复的两类）**：
1. **完整投递的轮次**（`committedThroughSeq == 最后一个 chunk seq`）：此时 `emitted ≡ generated`，从 `assistant/message` 即可精确恢复。
2. **中止轮次的边界**：宿主中止消息带 `sourceEventSeqs: chunkSeqs`（F1），**中止点精确落在 chunk 边界**，所以"生成了哪些 chunk"是可精确重建的 —— 真正缺的只有"其中哪些投递了"。**这两者之差就是那个整数。**
→ **所以正确的表述是"部分可回溯，缺的就是一个整数，而这个整数只在滚动窗口里"。** 结论：落那个整数（120 B/轮），**不要落全文**。

**实现的真实代价**：0.5 天（一个 append-only 事件 + 一条不变量 + 一条重放函数）。**做错的代价是 30–40 倍写放大。**

**如果他是对的，我该先写哪段代码**：`src/controller/watermark.js` —— 每轮一条 `turn/text-committed{turnId, speaker, committedThroughSeq, sha256}`，加三条断言：①`committedSeq` 单调；②`handoff.text` 是 `committed` 的前缀；③`turn/stop` 之后 `committedSeq` 不再增长（除显式 ack 补投）。**20 行，但它是 R5 的物理前提。**

---

### 他的主张 6：凡用户可见文本必须携带 `speaker ∈ {receptive, reflective}`；`speaker = controller` 在类型上不存在；`Decision` 与 `Utterance` 用 branded type 隔离，于是"控制器顺手说一句话"是编译不过

**判定**：成立，我照抄。**但要加两条他没想到的工程约束，其中一条是关于 JS 的。**

**我的回应**：
**(a) 技术对，但"编译不过"这句话只在编辑期成立 —— brand 在运行期只是一个属性。**
TypeScript 的 brand 是**类型层**技法，`tsc`/`tsgo` 一编译就抹掉；DSH 宿主本身是 JS（我实读的 `lib/index.js`，类型只在并行的 `lib/types/*.d.ts` 里），而插件运行期**构造一个裸对象、或者 `delete u.__brand`，没有任何东西会拦**。所以 brand 的真实价值是**编辑期与 CI（类型检查）期**，它必须配一条**运行期断言**才有牙齿：`emit()` 入口 `assert(u.__brand === 'utterance')`，并要求**唯一出口是 `emit()`**（否则别人绕过出口自己发，断言连执行机会都没有）。
→ **这一条我要把架构师的"类型系统"改成"类型 + 运行期断言 + CI grep 三件套"**，因为"编译不过"这句话在运行期是假的，而运行期才是决定用户看到什么的那一层。

**(b) `Decision.action = 'silent'` 必须携带 reason，且 reason 必须是闭集枚举。** 他的 L0 第 6 项只要求"`noReply` 可表达"。但被动模式上线后，**"它为什么没说话"是唯一的调试入口**（人在会议里被助手沉默对待，你唯一能查的就是这条 reason）。如果 reason 是自由字符串，**"它今天沉默了 400 次，为什么"这个统计问题在自由字符串下不可回答**。闭集枚举 + 计数器，零成本。

**(c) 他说"控制器越薄越容易偷偷说话"，这句我很喜欢，且它有一个具体的实现含义**：REVIEW-eng 把第一版控制器压到 <400 行。**400 行里只要有一行在做字符串拼接然后投递，brand 就被绕过了。** 所以线上门禁应该是 grep：`emit(` 的调用点必须全部落在 `receptive`/`reflective` 两个模块内，控制器模块里出现任何字符串字面量长度 > 4 就报警。**粗，但可执行。**

**实现的真实代价**：类型 + 断言 + 两条 grep = 0.5 天。

**如果他是对的，我该先写哪段代码**：`src/types/utterance.js`（brand + 运行期校验）+ `emit()` 唯一出口 + CI 那两条 grep。**顺序：先 brand 和出口，后控制器正文** —— 他自己的话是对的（"类型要先有，行数可以后长"）。

---

### 他的主张 7：S3 的两段文本无人认领 —— "轻量重启信号"归反馈层，"轻量续说"归接收层且必须计入**续说预算**（每轮 ≤ N 次，**预算写在控制器里、不写在接收层指令里**）

**判定**：成立，**且"预算写在控制器里"这一条是整份架构稿里最有工程品味的一句。**

**我的回应**：
"预算写在控制器里、不写在接收层指令里"这句话直接命中一个我踩过的坑：**把配额写进提示词的组件，不是被约束的组件，是被请求的组件。** 提示词里写"最多续说 3 次"，模型会理解为"你可以续说 3 次"，第 4 次的概率不是 0 而是"取决于上下文压力"。**配额必须是投递路径上的一个计数器，超了就丢弃并落 trace。**
我要补一点：**"丢弃"这个动作必须可见**（trace 落 `suppressed_continuation`），否则你无法回答"它这轮为什么停住了"。

**实现的真实代价**：0.2 天（一个计数器 + 一条 trace）。

**如果他是对的，我该先写哪段代码**：`emit()` 里的 `continuationBudget` 计数器（**在出口，不在 role 段**）。

---

### 他的主张 8：记忆写权被捆成了一个权利，必须拆成**决定权**（语义判断 → 处理层，它有 confidence 与类别路径）与**提交权**（工程不变量 → 控制器）；只允许 `commit(turnId, proposals[])`，**不存在 `write(entry)`**；读接口按 `(userId, asOfTurn)` 取

**判定**：拆分成立（我认，且这是他对 REVIEW-eng 炸点 7 的**正解补充**）；**`asOfTurn` 是过度设计**。

**我的回应**：
**(a) 拆分我认，被哪句说服**：REVIEW-eng 给的是"幂等键 = turnId"，架构师 `DISCUSS-arch.md:97` 补的是"它天然落在边界上"。**这一句是关键**：幂等键必须有 owner，而**唯一知道"一轮关闭了没有"的就是控制器** —— 处理层不知道自己的产物是哪一轮的、什么时候该被视为最终。所以"决定权/提交权"不是理论上漂亮，是**幂等键的 owner 归属问题**。这个论证比"v1 踩过坑"硬得多（v1 的坑是经验事实，这个是结构事实）。

**(b) 但要加一条防倒退断言，因为这条边界最容易被"防御性编程"破坏**：
`commit()` 的拒绝原因**只允许两种**（闭集）：①幂等重复；②schema 非法。**不允许"这条看起来不重要所以不提交"。** 一旦控制器能因语义理由拒绝一个 proposal，**决定权就偷偷回到控制器了** —— 而这会长得像"我在做防御性编程"，评审时几乎没人会拦。
→ 断言：`commit()` 的返回值是 `{accepted: n, rejected: [{id, reason: 'duplicate'|'schema'}]}`，**reason 是闭集**（和主张 6 的 reason 闭集同一个手法）。

**(c) `asOfTurn` 我判过度设计。** 理由：**单进程、单写者、控制器串行提交 —— 读到的永远就是"已提交的"**。`asOfTurn` 解决的问题（读到未提交半成品）在这个执行模型下**不存在**。加这个参数 = 引入一个**永远只有一个取值的参数**，并要求存储层支持时间旅行（MVCC 或版本化快照），**为没有发生的并发付费**。
正确形态：**签名里预留 `asOfTurn`，默认 `'latest'`，传其他值直接抛 `NotImplemented`。** 零成本，不阻碍将来，且**它把"现在没有 MVCC"这件事变成一条显式断言而不是一个隐性缺口。**

**实现的真实代价**：拆分 + 幂等单测 = 1 天。`asOfTurn` 按他说的真做 = 3–5 天 + 一个新失败模式（时间旅行读到的快照与当前不一致，而没人知道该信哪个）。

**如果他是对的，我该先写哪段代码**：`commit(turnId, proposals)` + 幂等单测（同 `turnId` 提交两次断言 hits 不变）。**先写单测，后写实现** —— 这条边界的全部价值在"提交两次"这个场景上。

---

### 他的主张 9：「该不该答」的归属含糊，但最容易做成硬边界 —— `decide(signals, mode) → Decision` 是**纯函数**、无 IO、无 LLM、可穷举单测；规则必须**全序**（被点名 > 低置信保守）；**强制点在出口不在入口**（`emit()` 处断言 `mode==='active' || pointedAt===true`）

**判定**：**全部成立，且这是整份架构稿里唯一一处我不打算改一个字的地方。**

**我的回应**：
"判决必须是纯函数"这个论证我没法驳，而且他给的理由是**可测试性**（"没有任何测试能钉住两个 owner 的行为"），不是纯洁性 —— 这是我唯一一次看到他**用工程代价而不是用不变量**来论证架构选择。
**唯一补充：出口断言的位置必须再往前挪一格。** 他写 `assert(mode === 'active' || pointedAt === true)`。这个断言在**网关**（多用户共享件）上不够 —— 因为**同一个网关服务所有用户的模式**，`mode` 必须携带 `userId` 一起进断言，否则一个用户的 `active` 会让另一个用户的 `passive` 断言通过（**这是限额类代码最经典的写错方式，REVIEW-eng 在 M5 验收里也点了同一条**）。→ 断言写成 `assert(d.turnId === u.turnId && (d.mode === 'active' || d.pointedAt))`，即**决策与 utterance 的 turnId 必须相等**。**这一条他不是不知道，是没写。**

**实现的真实代价**：纯函数 + 穷举表 + 回放 fixtures = 1 天（无外部依赖，我 §4 也是这么排的）。

**如果他是对的，我该先写哪段代码**：`src/controller/mode.js` 纯函数 + `test/mode-replay.test.js` 回放。**这条和我原来的计划完全重合，一天做完，我可以立刻开工。**

---

### 他的主张 10：交付形态档位由处理层的分类节点判定是危险的（一次 LLM 输出直接决定分钟级资源）；必须拆成 `deliveryHint`（建议）与 `delivery`（裁决）；trace 落两者的**不一致率**

**判定**：成立；**"不一致率是几乎免费的诊断指标"这一条我要给他加分，并且我把它的地位再提高一级。**

**我的回应**：
分类器是这套系统里**最没被验证的组件**（`deliveryHint`/`delivery` 的不一致率是它唯一的外部证据），而架构师说"这个率不需要任何新机制"是对的 —— **它是我能做到的最便宜的"模型判档是否可信"的度量**。我把它从"诊断指标"提升为**P0 阶段的验收项**：不一致率 > 15% 就说明分类器不可用，此时任何依赖分类的机制（ETA、级联准入、T3/T4 准入）都不该上。
**补一条工程约束**：第一版**没有 T3/T4**，所以准入闸门第一版只有 T1/T2 两个分支。**不要为不存在的档位写状态机** —— 这是最常见的"为了架构完整性而提前实现"的浪费。字段要有（判据 1：历史形状），状态机不要有。

**实现的真实代价**：两个字段 + 一条 trace + 一条告警 = 0.3 天。

**如果他是对的，我该先写哪段代码**：`deliveryHint` / `delivery` 两个字段进 trace，**准入闸门只写 `if (delivery >= T3) reject('not-in-v1')` 一行**。

---

### 他的主张 11：A/B 不该由单层决定；宿主里没有"句子"这个单位，选 B 会被四层各自发明标点规则；推荐 **A 做通路 + B 降级为"生成纪律" + 深答已就绪时最多再等一句完成（≤~400ms）**

**判定**：**部分成立 —— 这是他整份文档里最强的一条，也是我让步最大的一条；但他的方案描述里有一个会被实现错的硬错误（"不引入缓冲"），以及一个未区分的概念（软目标 vs 硬截止）。**

**我的回应（议题 3 的核心）**：

**(a) 先说我对的部分：他找到了一条零首字代价的路径，我的 B 方案不是。**
我的 B 方案从首字预算里拿走 250–350ms（我 §2.3 自己算的）。他的推迟窗口**只在"深答已就绪"时生效**，而首字在接收层开口时就发生了 —— **它不吃首字预算。** 用我自己的判据"优先消除不可逆失败，而不是优化可优化的指标"来检验：**他同时做到了消除不可逆失败 + 不付首字代价，这比我的 B 严格更优。** 我被这一句说服：`DISCUSS-arch.md:266` —— "**不引入缓冲 → 不引入沉默期**（B 方案最大的副作用：为了凑一个句号，用户会看到'打字停了'）"。
但注意**说服我的是他的结论**（零首字代价），**不是他的理由**（不引入缓冲）—— 见 (b)。

**(b) 他的"不引入缓冲"是错的，而且按他的文字实现会复现他要消除的那个 bug。**
推理链（全部基于 F1，实测代码）：
1. 宿主 `cancel` 停在 **chunk 边界**，不是句子边界（F1）。
2. 客户端**不能撤回**已渲染的字（双方共识）。
3. 于是"最多再等一句完成"这个动作，**必然要求把当前句的尾部先扣住不投递**，否则字已经出去了，等与不等没有区别（你已经切在半句了）。
4. **扣住不投递 = 缓冲**。而在扣住的这段时间里，客户端屏幕上的字**不再增加** —— **那就是"打字停了"，也就是他自己攻击 B 的那个副作用，只是更短。**
→ **正解：他的窗口与我的 B 在机制上是同一个东西（投递缓冲 + flush 判据），区别只有"何时 flush"**：B 是"永远在边界 flush"，他的是"平时逐帧 flush，只在窗口内等边界"。**所以他是对的（做法），他是错的（描述）。**
**为什么这个描述错误有实际代价**：照他文字实现的人会写"深答就绪 → 立即 `cancel`"（因为"不引入缓冲"），而 `cancel` 停在 chunk 边界 → **产出恰好是"半句接管"，正是这个方案存在的理由被自己消掉。** 这属于我题目里要指出的那类：**不变量很漂亮，但按字面实现会得到反向结果。**

**(c) 400ms 这个数字从哪来？—— 他没给推导。我的数字与依据：**
「等一句」的期望时长 = `E[到下一个硬边界的剩余字数] / 生成速率`。
- 生成速率支点（我 `DISCUSS-eng.md:19` 的 [待测]，**这仍然是全项目最该先测的那个数**）：60–70 tok/s × 1.2 token/字 → **50–58 字/秒**。
- 中文口语化短句的硬边界间距 ≈ **12–20 字**；均匀假设下"当前句剩余"期望 = 半个间距 ≈ **6–10 字**。
- → **E[等待] ≈ 6–10 字 ÷ 55 字/秒 ≈ 110–180ms**（p50）。
- **但 p90 不是均值**：模型在长从句、代码块、列表项内部时硬边界被抑制，剩余可到 **40–60 字** → **p90 ≈ 0.7–1.1s**。
→ **判决："≤400ms"作为 p50 偏保守（真实 ~150ms），作为"上限"是错的。**
**正确形态是双截止**：
```
软目标 200ms：等不到硬边界就……继续等（因为你还想等）
硬截止 400ms：到点强制接管，并且【直接走 S4 纠偏模板】
```
**为什么这个区分有工程后果**：只有**硬截止**存在时，"切在半句"这件事才是**可枚举的**（全部落在 S4 路径上），才能写成测试（"强制接管轮次的可见文本必须以非边界字符结尾 → 必须命中 S4 模板"）。他给了一个数，没给这个区分，于是"半句"仍然是一个**偶发、不可复现**的状态 —— 也就是他自己在 3.2 里反对的那种状况。

**(d) 边界情况：半句怎么处理？** 按我 §2.5 的分档，但要改一处：
- 残词（结尾是"的/了/和/就"这类功能词）→ **丢**（服务端丢，不能客户端丢）。
- 完整从句（以 `，` 结尾、≥8 字）→ **发**，标 `soft_end`，让深答按 S1/S2 接着它说。
- **触发硬截止 400ms 强制接管** → **发 + 强制 S4 模板**（新增，架构师没有这一档）。
- 并且：**丢掉的尾巴仍进日志，但带 `delivered: false`** —— 有了主张 5 的 `committedThroughSeq`，这一条变成"`committed < generated`"，**一个比较，不需要额外字段**。这就是"水位"这个设计同时解决四件事的证明。

**(e) "谁判句边界"归协议（他 3.2：只做 B 时切分只在控制器算一次，`Segment` 作为协议字段下发）—— 工程上第一版不可行，而且理由来自他自己的判据。**
时间兜底（600ms）意味着**同一个模型输出在不同负载下会切出不同的 `Segment`**。所以：
- 如果你**存 `Segment`** → 切分规则变成**历史数据的形状**（命中他自己的 L0 判据 1）→ 你欠了一笔永久的协议债；
- 如果你**重算** → **重连后客户端与历史切分不一致**（这是 C6 的失败模式，我 §3 已列）。
→ **他推荐"不做 B、不下发 Segment"是自洽且最省的，我接受：句边界只存在于控制器进程内，不进协议、不进历史。** 这也正是我 §2.2 那套规则的正确用法 —— **我的规则只用于决定"何时 flush"，我原来没说清楚它不落盘，现在说清楚。**
**但必须补一条他没说的**：句边界不进历史之后，**"客户端渲染的文本 === committed 流前缀"这条断言在重连后无法用 `Segment` 校验，只能用字节/seq 水位**。他砍掉了 `Segment`，**但砍不掉水位** —— 水位是绕不过去的那一个（这又把主张 5 的那个整数顶了回来）。

**(f) 我仍然要顶住的那一半：他的窗口对"用户 barge-in"完全无效。**
推迟窗口只在**系统接管**时生效。用户打断是**用户发起的**，你不可能让用户等 400ms。而 barge-in 恰恰是最常见的"露出半句"的场景（用户打断往往就是因为"它开始说错方向了"）。
- **他的方案：系统接管永不露半句。**
- **我的方案：任何中止都不露半句（可见文本永远结束在投递单元边界）。**
- **正确的合并**：**系统接管用推迟窗口（零首字代价）；用户 barge-in 用客户端打字机回放 + snap 到已收单元末尾**（我 §2.4，要求服务端按单元投递）。
→ 我原来的结论"若只能选一个全局选 B"是**过度概括**，我撤回；**但"第一版只做 A"会留下 barge-in 这半个洞，这一半我顶住。**
**我的最终立场**：**A + 推迟窗口上线（他的方案），但水位必须做成"每 turn 的字节/seq 水位"（我 C6 那个），并且在 trace 里加一个数：`bargein_visible_ends_midunit` 的比例。** 用这个数决定要不要付 B 的代价。**这是我在这场辩论里能走到的最后一步。**

**(g) 附：他这一条的复杂度估计低估了一半。**
他说"控制器需要字符级流 + 标点判定 → 复杂度 +5%"。**代码行数确实 +5%，但工作量不是**：标点判定必须**识别代码块/行内代码/列表/引号内部**（否则代码注释里的 `。` 会误判边界），这是一个**有真实 bug 面的小解析器**，必须配边界样例单测。**我估 +15%（约 1–1.5 天），其中 60% 花在测试上，不是花在代码上。** 架构师估复杂度时习惯估代码行数，这是通病。

**实现的真实代价**：投递缓冲 + 双截止 + 边界解析器 + 单测 ≈ **2 天**（不是他暗示的"极低成本"）。

**如果他是对的，我该先写哪段代码**：`src/delivery/pacing.js` —— 一个 `Pacer{ push(chunk), shouldFlush(), deadline }`，纯函数可单测。**在它之前先做 `V-A` 的离线重放**（我 §5：用现有 session.jsonl 的 chunk 事件按"纯标点 / +25 字 / +600ms"三套规则切，直接算出首单元延迟分布与软/硬截止的触发率）。**这是全项目最便宜、最值钱的一次测量，它就在今天的数据里，零新代码。**

---

### 他的主张 12：T4 让"一轮"延长到分钟级，与 R2/R5 冲突（任务完成时前缀里已追加了后面若干轮；R2 不许改前缀）→ 要么违反 R2，要么为任务单开一轮（于是结论无法严格承接）；建议 turn boundary 可关闭 + 任务携带 `originTurnId` + 跨轮机制显式化 + 检查清单加一列"这条门禁在 T4 下还成立吗"

**判定**：**冲突被高估；但他的产出物（`originTurnId` + T4 门禁检查列）成立。**

**我的回应**：
**(a) 冲突点找错了。** 他说"任务完成时要装配上下文，前缀里已追加了后面若干轮的内容，而 R2 不许改前缀"。
**但 R2 只约束"已有前缀不得改写"，不约束"新起一个 turn 用哪些块"。** 任务完成时的结论是**一次新的输出**，它的上下文完全可以是一个**新 turn**的装配：尾部块 = `originTurn` 的 committed 文本 + 任务产物 + 这中间轮次的摘要。**这里没有一处需要改写前缀。**
→ **所以这不是 R2 冲突，是"什么算一轮"的定义缺口** —— 架构师自己后半句说对了（"为任务单开一轮"），但他把它判成了"无法严格承接"。**"无法严格承接"不是 R2 的问题，是 R5 的语义在跨轮时退化**：R5 说"反馈层上下文必含接收层已输出全文"，跨轮时应该是"***originTurnId* 那轮的全文**"，而不是"最近一轮的全文"。
→ **一行契约就解决了：R5 的口径改成"必含 `handoff.turnId` 指定的那一轮的 committed 全文"**（这就是主张 5 那个 `turnId` 字段的第二个用途）。**他把它上升为"需要架构层拍板的取舍"，是对这个冲突的严重性估计过高。**

**(b) 他漏了最省的解法，而它同时改善体验：`handoff.mode`。**
3 分钟后用户已经不问那件事了。此时用 S2/S3 的"轻量续说"语气承接（"接着说，结论是……"）会**很诡异**。
→ **T4 完成时的形态应该是"报告"而不是"承接"**：新起一轮，`speaker=reflective`，`handoff.mode='report'`（不是 `'continue'`），上下文 = originTurn 的 committed 文本 + 产物 + 中间轮摘要（尾部追加）。
**结果：R2 不破、R5 无需改写、用户不会在 3 分钟后听到"接着说"。** 代价：`handoff.mode` 一个字段（**判据 1：历史形状，必须现在有**）。
他没想到，因为他把 R5 当成公理 —— **而 R5 应该有一个形态字段。这是他的架构洁癖造成的盲点：他把"不该破的不变量"和"该参数化的契约"混在一起了。**

**(c) `originTurnId` 我认**（判据 1，历史形状）。
**(d) "T4 下这条门禁还成立吗"这一列我认，而且我要把它推广**：REVIEW-eng 的 7 条炸点里有 4 条（炸点 3/4/5/7）都是**为"短轮次"设计的**。这一列的真正价值不是 T4，是**提醒"所有门禁都有隐含的轮长假设"**。我把我的 C1–C10 竞态表也过一遍：**C1（50–300ms 窗口）、C2（100–500ms 在途 chunk）、C5（300ms 判决窗口）全部假设轮长秒级**，T4 下这三条的时间参数要重新标定。**这是我要给他的这一条加的注脚，也是他给我的一个真实收获。**

**实现的真实代价**：`originTurnId` + `handoff.mode` 两个字段 = 0.1 天（字段先有，T4 后做）。

**如果他是对的，我该先写哪段代码**：`{originTurnId, handoff: {turnId, mode}}` 三个字段进 `Utterance` 类型与 trace，**T4 的实现一行不写**。

---

### 他的主张 13：R2 的唯一例外（整块重建）在代码里与常规改写**无法区分**，所以它一定会被当后门用 → 契约改成显式 `rebuild(reason)`，且需要**由控制器签发的窗口令牌**（窗口 = 用户思考间隙，控制器观测空闲时长产生），没有令牌 → 拒绝

**判定**：**问题成立，机制过度设计 —— 用一个更便宜的形态达到同样效果。**

**我的回应**：
他说"这个例外与常规改写无法区分，所以一定会被当后门用"—— **成立**。这也正是我在 `DISCUSS-eng.md:132-136` 为 `surfaceOp:'replace'` 提的落地口径（只在轮末、只对最后一条消息、只做一次），**我当时没意识到"它和常规改写在代码上长得一样"这个隐患，他补上了这一刀，我认。**

**但他的机制太重**：令牌需要「控制器观测空闲时长 → 产生令牌 → 校验 → 过期」这一整套授权机制。代价不只在 1–2 天工时：
- **新增一个失败模式**：令牌过期 → **合法的重建被拒** → 用户看到一个上下文已经过时的对话（"它忘了我们刚才说的"），而这个失败**与"重建成功但内容不对"在用户侧无法区分**。
- **控制器多了一个状态机**（空闲计时器），而架构师自己在风险 3 里主张"控制器无内存态" —— **这两条互相牵制**：空闲计时器就是一个内存态，它的准确值取决于上次活动时间，重启后丢失。→ **他的两个主张在这里打架。**

**我的替代形态**（达到同样效果，零令牌、零计时器、零过期语义）：
```
rebuild({ reason, expectedPrefixHash, promptVersion })
  → assert(sha256(currentPrefix) === expectedPrefixHash)   // 不匹配 → 拒绝
  → assert(reason ∈ 闭集)
  → 落一条 turn/prefix-rebuild 事件（含 reason + 前后 hash）
```
**为什么等价**：后门之所以危险，是因为它**看起来像常规追加**。加了 `expectedPrefixHash` + 闭集 reason + 强制落一条事件之后，**重建与原追加在"日志形状"上就再也混淆不了**（每一次重建都是一条独立事件，可计数、可告警）。**"次数异常"是一个可报警的量，"是否持有令牌"不是。**
→ **这就是我给他的通用手法（"例外必须有独立的表达形式"）的一个更省的实现。我认手法，不认实现。**

**实现的真实代价**：他的令牌方案 1.5–2 天 + 1 个新失败模式；我的形态 0.3 天 + 0 个新失败模式。

**如果他是对的，我该先写哪段代码**：`rebuild({reason, expectedPrefixHash})` + 一条 `turn/prefix-rebuild` 事件 + 一条告警（每 100 轮重建次数 > 0 就报）。**先写告警，先于实现** —— 因为"重建"这件事最可能死在"没人注意到它在发生"。

---

### 他的主张 14（风险 3）："进程内"被误当成"状态在进程内" → 把"进程内"限定为**执行位置**，控制器**无内存态**，所有决策从落盘事实源 + 本轮输入重算；唯一真源 = append-only JSONL + `turnId` 幂等；容量表要加**"重启恢复时间"**一列

**判定**：核心成立（故障边界 ≠ 隔离边界，这条我认）；**但"控制器无内存态"作为绝对命题与他自己要求的"逐帧判决"直接矛盾，而这一条被他列为"最架构的一条"，严重性被他高估了。**

**我的回应**：
**(a) 直接矛盾**：他在主张 11 里要求"深答已就绪时最多再等一句（≤400ms）"，**这个判决必须在每一个 chunk 上跑**（F1/F3）。而"每帧从磁盘重算"是不可能的 —— 那意味着每 15ms 读一次 session.jsonl。
→ 所以正确的表述是：**"控制器无内存态"必须被限定为"内存里只允许有能从落盘事实源在本轮内重建的工作集，且每个 turn 结束必须把可持久部分落盘"**。绝对化的"无内存态"是一条**无法实现的**不变量，而**无法实现的不变量比没有不变量更危险**（实现者会选择忽略它）。

**(b) 他的数量级判断错了**：他说"8–12 人的容量上限来自内存，而内存里装的应该是可再生工作集，不是事实源"。
**REVIEW-eng E2/E3 实测：一个活跃 DSH 实例 1.1–1.7 GB，全新 home RSS 203 MB。** 控制器的工作集是 **KB 级**。
→ **容量上限的 99% 是 DSH 进程本身，不是控制器状态。** 他把这条列为"最架构的风险"，但**它在数量级上不成立**，而真正的内存问题 REVIEW-eng 已经用 `MemoryMax` + 空闲回收解决了。**这是他的典型盲区：不变量正确，量级没算。**

**(c) 他说对的两件硬事，我认，其中一件我要加强**：
1. **故障边界与隔离边界不重合**：网关按 userId 路由、进程边界隔离凭证，但**重启即失忆** —— "每用户一进程"这个隔离承诺在重启时失效。成立。
2. **容量表要加"重启恢复时间"一列** —— 成立，而且我给一个下限数字：**E3 实测冷启动 25s**，加上重放 session 与记忆的时间 = **恢复时间 ≥ 25s**。这个数字必须写进运维文档，因为它是"崩溃时用户等多久"的唯一答案，而**现在是空白**。他这条列得对。
3. 我要加强的：**"唯一真源 = append-only JSONL"有一个地平线**（F7：compaction 用 `surfaceOp:'replace'` 折叠 span）。所以真源不是"一个文件"，而是**文件 + 折叠历史**。→ 契约要写成："**折叠只允许由 compaction 产生，且每次折叠必须落一条 `turn/compaction` 事件（含被折叠的 seqRange）**"，否则**折叠就是一次静默的历史销毁**，而 compaction 正是 REVIEW-eng 最担心的那类"静默"操作。**这一条是架构师没看到的，它比我上面任何一条都更接近"不可回溯"的真实机制。**

**实现的真实代价**：把"无内存态"改成"工作集可重建"= 0（是措辞与验收口径的改动）；加 `turn/compaction` 事件 = 0.2 天。

**如果他是对的，我该先写哪段代码**：容量/运维文档里的**"重启恢复时间"一行**（`≥25s，实测冷启动基线`）+ `turn/compaction` 事件。**文档那行先写，因为它不依赖任何代码。**

---

### 他的主张 15：L0 八项地基（1 事实源+`emittedText` 落盘 / 2 单一装配入口+append-only / 3 唯一出口 `emit()` / 4 `turnId`+幂等+trace / 5 `openMemory` 能力句柄 / 6 `Decision`/`Utterance`+`noReply` / 7 `maxDepth`+并发上限+重试预算 / 8 协议版本+`attachments[]`），判据是三条（①历史数据的形状 ②公共必经之路 ③缺了某条 R 纪律结构性不可实现），"不这样就会被返工"

**判定**：**八项我都同意要做（没有一项我判伪地基）—— 但"八项一起做"这个打包方式是过度设计，而且是这份文档里最贵的单笔工程代价：它会把 P0 从 3 天膨胀到 2 周。**

**我的回应（议题 4 的核心）**：
他的三条判据很好，好到**只要严格执行它，就会得到和我一样的分组**。逐项对着他自己的判据过一遍：

| # | 项 | 命中判据 | 真实性质 | 我的排期 |
|---|---|---|---|---|
| 1 | `emittedText` 落盘 | **①+③** | **不可回填** | **P0**（0.5 天，见主张 5） |
| 4 | `turnId`+幂等+trace | **①** | **不可回填**（历史轮次没有幂等键 → REVIEW-eng 炸点 7 无法对账） | **P0**（0.5 天） |
| 6 | `Decision`/`Utterance`+`noReply` | **①+③** | **不可回填**（字段+类型形状） | **P0**（0.5 天） |
| 8 | 协议版本+`attachments[]` | **①** | **不可回填**（客户端消息模型一旦是纯文本就改不动） | **P0**（0.3 天） |
| 2 | 单一装配入口+append-only | ② | **改动面大，但可回填**（prompt 历史不需要保留） | P0.5（2–3 天） |
| 3 | 唯一出口 `emit()` | ②（他说）/ **①+③（我说）** | 视理由而不同，见下 | P0（**只定义类型 + 落 trace**，gate 放 P0.5） |
| 5 | `openMemory` 能力句柄 | ②+③ | **改动面大，可回填** | P0.5（1 天） |
| 7 | `maxDepth`/并发/重试预算 | ③ | **必须做，但极便宜**（3 个配置值 + 1 个闸门） | **P0（2 小时）** |

→ **真正"不可回填"的只有 4 项（1/4/6/8），而它们加起来是 1.8 天。** 另外 4 项（2/3/5/7）是"改动面大"或"必须做但便宜"，**它们可以在一周内渐进，不需要塞进 P0。**
→ **架构师用同一条判据给 8 项背书，但其中 4 项命中的是判据②（改动面），而判据②和判据①的工程含义完全不同**：判据①是"永远补不回来"，判据②是"补回来要重写一批调用点"。**把两者打包成"现在必须做，否则一定返工"，会让 P0 的 3 天变成 2 周 —— 这是这份架构稿为纯洁性付的最大一笔账。**
**而且这不利于是他的目标**：P0 拖长 → P4 的 21 天真人验证延后 → **"两块回答到底值不值得做"这个最贵的判断被推后一个月**。架构师的洁癖在这里直接伤害了他自己的验证节奏。

**第 3 项我要单独说，因为他的理由会导出错误的实现顺序。** 他说 `emit()` 命中的是判据②（公共必经之路，后加 = 满仓库找调用点）。
**但实测上没这么可怕**：外发路径本来就收敛在网关（REVIEW-eng 已定"D4 拦截必须发生在外发流上"，而外发流就是网关建的），**后加 `emit()` 只需要改网关一处**。真正让它成为地基的是**①+③**：`speaker`/`turnId` 是历史数据的形状（不记就没了），被动模式的"沉默"要靠出口断言。
→ **理由变了，先写的代码就变了**：按判据②，你会先去重构所有输出点（几天）；按判据①+③，你会**先定义 `Utterance` 的字段并落 trace**（几小时，今天就能做完）。**这是"判据选错会直接导致动作选错"的一个实例。**

**第 8 项我要补一个他没有的洞察，它改变实现顺序**：`attachments[]` 与"两块回答"**本来就是同一个渲染机制的两个消费者**（"一条消息多个 block"）。所以第一版做"两块 = 两个 block"时，**block 化渲染是免费的**；而如果第一版做"字符串拼接"，**两块和 `attachments[]` 要一起返工**。
→ **正确顺序：先做"两块 = 两个 block"，`attachments[]` 自然落位。** 他把第 8 项判成"必须现在有"是对的，**但真正的理由（与两块共用渲染）他没看到，而这个理由决定了先写哪一行。**

**实现的真实代价**：按他的打包 = P0 **10–14 天**（且 P0 的验收"能跑、能看 trace"被淹没）；按我的分组 = P0 **2–3 天（1/4/6/8/7）** + P0.5 **4–5 天（2/3/5）**，**且 P4 的 21 天验证能提前一周开始**。

**如果他是对的，我该先写哪段代码**：`src/types/utterance.js` + `src/controller/watermark.js` + trace schema + 客户端"一条消息两个 block"。**四样，1.8 天，全部命中判据①。**

---

### 他的主张 16：**`noReply` 必须可表达**（L0 第 6 项里他强调"这是最容易被低估的一条"：如果第一版到处默认"接收层一定产出 utterance"，加被动模式时要满仓库找"哪些地方假定了一定会说话"）

**判定**：**成立。这条我完全认，而且我认为它是他整份文档里第二强的一条论证（仅次于主张 11 的零首字代价）。**

**我的回应**：
**为什么我认**：他给的不是"未来可能要被动模式"，而是一个**具体的、三个月后才爆的机制** —— 我把它具体化到代码上：
- 接收层的调用形态如果是 `receive() → Utterance`，那"不说话"只能靠**不调用它**，或者**返回空 Utterance**。
- **空 `Utterance` 与"忘了填"在出口断言处不可区分**。于是 trace 里 `spoken` 这类必填字段会出现一批**假的空值**。
- **假的空值在被动模式上线时会变成假证据**："它今天沉默了 400 次" —— 其中 398 次是 bug、2 次是真的沉默。**而你没有办法区分，因为你从来没有把"沉默"当成一个有类型的返回值。**
→ 这和我自己在 C5 里写的"**第一版：把'要不要说话'做成唯一一个前置闸门**"是同一件事的两个说法，但**他说得更早、更结构化**：我是"做一个闸门"，他是"让沉默成为一种返回值"。**后者才是可组合的**（闸门可以被绕过，返回值不能）。
**加上主张 6 的补充（`silent` 必须带闭集 reason），这条才完整。**

**顺带我要指出他的关联论证里有一处弱**：他说"被动语料不逐轮注入这条要求装配入口支持'有上下文但不产生输出'的形态"。**这不需要特殊形态** —— 装配和投递本来就是两件事，"装配了上下文"和"投递了 utterance"之间没有任何耦合。**他为了支持"第 3 条最容易被低估"而给了一个不必要的技术依赖，这会让实现者以为装配入口要改造。** 真正需要的只有"沉默可表达"这一条。

**实现的真实代价**：0.2 天（联合类型 + 出口断言 + 闭集 reason）。

**如果他是对的，我该先写哪段代码**：`type Reply = {kind:'utterance', …} | {kind:'silent', reason: SilentReason}`，**并且第一版的所有调用点都用它**（即使第一版永远返回 `utterance`）。**关键是"用"，不是"有"** —— 一个定义了但没被使用的联合类型不会产生任何约束力。

---

### 他的主张 17：所有用户可见文本（**含 T2/T3 内的文案、错误态、状态提示**）都必须经控制器唯一出口 `emit()`，否则"一个声音"不可审计

**判定**：**不成立 / 过度设计。而且它违反了他自己在风险 2 里立的原则。**

**我的回应**：
**他自己的话**：`DISCUSS-arch.md:371` —— "**状态提示是 UI 事件不是消息** —— 两者在类型与协议上就是两种东西，不能互相转换（否则'状态代替语言'会在第一次方便时变成'状态加一句话'）"。
**那么 T2/T3 卡片内的列名、标签、按钮文案，与状态提示在性质上是同一类东西：它们是人机界面的词条，不是助手说过的话。** 它们不进人类转写（transcript），用户不会认为"助手说过'支付方式'"。
**如果把 UI 词条也拉到 `emit()`**：
- 一张表格卡片的列名要从 Flutter 搬到服务端、走 i18n、多一跳网络；
- 而**收益是零** —— 审计要回答的问题是"助手说过什么"，不是"屏幕上出现过哪些字"。**架构师把"审计完整"和"审计有用"混为一谈。**
**正确口径（我顶住这一条）**：**`emit()` 覆盖"会进入人类转写的一切文本"** —— 接收层、反馈层、S4 纠偏、错误态（"查询失败，我大概知道原因"这类**是助手说的**，要过）、T4 报告。**UI 词条与状态提示不过 `emit()`，但必须是另一种类型（`UiLabel` / `UiStatus`），且与 `Utterance` 之间没有互相转换的构造器。**
→ **这才是他通用手法（"类型比断言强"）的正确应用**：不是"全走一个出口"，而是"**两种东西在类型上不能互转**"。**他把'统一出口'当成了目标，而真正的目标是'不可互转'。** 前者贵且无用，后者免费且正好。

**实现的真实代价**：按他的方案（UI 词条服务端化 + i18n + 一跳）= **3–5 天**，且 Flutter 端渲染被迫走服务端词条，**每次改一个标签都要发版**。按我的方案 = **0.2 天**（两个 brand type，无转换器）。

**如果他是对的，我该先写哪段代码**：**我不写**。只写两个 brand type 和一条 CI grep（断言服务端不存在 `UiLabel → Utterance` 的转换函数）。

---

### 他的主张 18：把"地基破了"做成 CI grep + 评审清单（第二处构造 prompt / 控制器产出用户可见文本 / 未携带 speaker 的对外文本 / 向非尾部位置追加 / 客户端自行切分 / 读记忆接口同时接受 userId 与句柄 / 跨轮的尾部追加）

**判定**：成立，**这是整份文档里投入产出比最高的一节，我要加两条，并且指出其中一条现在是错的。**

**我的回应**：
**错的这条**："出现**读记忆的接口同时接受 userId 与句柄** → R1 能力式隔离"。这条 grep 在评审清单里是**死条文**：`openMemory(userId)` 本身接受 `userId`，所以"同时接受 userId 与句柄"的字面匹配会命中它自己。正确写法是**方向性断言**：`openMemory` 必须是"接受 `userId`、返回句柄"的**唯一**函数；**除它之外任何接受 `userId` 的记忆相关函数都是违规**（用导出面清单做断言，不是用 grep 做正则）。
**加两条**：
1. **"出现第二处 `cancel(` 调用点"** → 中止权被分散（主张 3）。这条 grep 是一行，价值等于主张 3 的全部。
2. **"出现未被 `turn/compaction` 事件记录的 `surfaceOp:'replace'`"** → 静默历史销毁（我的主张 14(c)）。**这是我最想加进他清单的一条**，因为 compaction 是唯一一个"正常运作时就在销毁历史"的机制，而它现在完全不在任何人的检查表上。

**实现的真实代价**：0.2 天（三个 grep + 一个人工看的清单）。

**如果他是对的，我该先写哪段代码**：CI 脚本（`scripts/check-invariants.js`），**先写三条（cancel 唯一调用点 / 无 UiLabel→Utterance 转换 / replace 必须配 compaction 事件）**，因为它们对应的实现都还没写 —— **断言先于实现是唯一能防止"实现完了才发现断言不成立"的顺序。**

---

# 二、我改变了的立场

按"被哪一句说服"逐条列出，不客套：

1. **`emittedText` 的权威口径：客户端 ack 水位 → 控制器已放行（commit 流）。**
   被 `DISCUSS-arch.md:213` 说服："选第三个需要客户端回执（多一跳、且弱网下不可靠）"。我自己的假设（RTT p99 400–800ms、丢包 0.5–2%）使这句话成立，而承诺判决窗口只有 150ms。**我原来拿一个最晚到、还可能永远不来的信号当唯一权威。**
   **保留**：ack 仍作为"承接包裁剪依据"（不是权威），因为它堵的是**反向错位**（committed 了但客户端还没画出来 → 反馈层提前引用 → 用户觉得"它重复了我说的话"）。他堵了一个方向，我堵两个。

2. **B 从"若只能选一个就选 B"降级为"只管 barge-in 那一半"。**
   被 `DISCUSS-arch.md:266` 说服：推迟窗口只在"深答已就绪"时生效，**不吃首字预算**，而我的 B 要吃 250–350ms（我 §2.3 自己算的）。**他同时做到消除不可逆失败 + 零首字代价，严格优于我。** 这是我最大的一次让步。
   **顶住**：他的窗口对**用户 barge-in** 完全无效，而 barge-in 恰恰是最常见的露半句场景。这一半我顶住，并要求把它变成一个**可测量的数**（`bargein_visible_ends_midunit`）再决定付不付 B 的代价。

3. **"句边界"不进协议。** 被我自己推的推论说服（方向来自他的 3.2/3.3）：时间兜底让同一输出在不同负载下切出不同 `Segment`，**存则变成历史形状（命中他自己的判据①），重算则重连后与历史不一致**。我 §2.2 的规则**只用于决定何时 flush，不落盘**。我原来没说清这一点。

4. **`turnId` 的地位被我低估了。** 我原来把它放在"投递水位三件套"里当配角。现在承认它是**记忆幂等键的 owner 归属问题**（必须由知道"轮关闭了没有"的组件持有），而不只是投递问题。

5. **`noReply` / "沉默可表达"我原来没有明确写。** 被 `DISCUSS-arch.md:319` 的具体机制说服：**空 `Utterance` 与"忘了填"在出口断言处不可区分 → trace 里出现假空值 → 被动模式上线时变成假证据。**

6. **`source.kind` 之外，我多了两条"控制器必须进程内"的论证。** 原本靠 `source.kind` 硬编码（可以被"网关补标记"绕）；现在加上 **F3（逐帧判决要在 µs 级）+ F5（进程外每帧 185 B 帧，一页 52k 事件/11MB）**。前者绕不过去，后者有实测分母。**辩护更牢了，但功劳不是我的。**

7. **R2 例外"与常规改写无法区分，所以会被当后门用"这一刀，我原来漏了。** 我 `DISCUSS-eng.md:132-136` 只写了 `replace` 的落地口径，没意识到它在代码形状上与常规改写在日志里混淆。**他补上了，我认问题；但他的令牌机制我不认（见主张 13）。**

8. **"T4 门禁检查列"推广到我的竞态表。** C1/C2/C5 的时间参数全部假设轮长秒级，T4 下要重标定。这是我从他那里拿到的真实收获。

# 三、僵持不下的

1. **接收层的投递方式（B 的那一半）。**
   我：**用户 barge-in 是用户发起的，不能让用户等 400ms，而"露出半句"在两个场景里同源；可见文本应该永远结束在投递单元边界。**
   他：**A 是唯一数据通路，引入第二套渲染路径的代价大于收益。**
   我的最后一步：**A + 推迟窗口先上线（认他的方案），但水位必须是每 turn 的字节/seq 水位，并且 trace 落 `bargein_visible_ends_midunit` 比例。用这个数决定要不要付 B 的代价。** 在这之前，**这一半我顶住。**

2. **`emit()` 的覆盖范围。**
   他：所有用户可见文本（含 T2/T3 文案、状态提示）。
   我：**所有进入人类转写的文本**；UI 词条与状态提示是 `UiLabel`/`UiStatus`，**与 `Utterance` 之间不可互转 —— 这才是真正的不变量**。他的口径是"统一出口"，我的口径是"不可互转"；前者贵 3–5 天且每次改标签要发版，后者 0.2 天。**且他的口径与他自己的"状态是 UI 事件不是消息"冲突。**

3. **L0 的打包粒度（这是实际分歧，影响 P0 工期）。**
   他：8 项一起，"现在必须做否则一定返工"。
   我：**真正不可回填的只有 4 项（1/4/6/8），加 1 个 2 小时的 7 → P0 = 2–3 天；另外 3 项（2/3/5）放 P0.5。** 8 项打包会让 P0 从 3 天变 2 周，**并把他自己的 P4 21 天真人验证推迟一周以上 —— 这是为纯洁性付的最贵一笔账。**

4. **`asOfTurn` 读快照 / 令牌式 `rebuild`。**
   他：现在就要。
   我：**都是为一个不存在的并发付费**（单进程单写者；令牌的失败模式是"合法重建被拒"，且令牌状态与他自己主张的"控制器无内存态"打架）。**签名预留 + 传参抛 `NotImplemented`；`expectedPrefixHash` + 闭集 reason + 一条事件** —— 同样达到效果，代价 1/5，新失败模式 0。

5. **"接收层不持有工具"的代价。**
   他：能力门控含"不持有工具"。
   我：**工具 schema 是前缀里最大的一块，且 v6 要求三角色共用逐字节相同前缀。** 要"看不见"就得给接收层一份独立前缀，**等于每轮多付一份全价冷启动**。第一版应"看得见、不给调用权"（role 段 + 出口断言）。**这一条我等他给一个前缀命中率的数字再来谈。**

6. **"控制器无内存态"是绝对命题还是限定命题。**
   他：绝对（所有决策从落盘事实源 + 本轮输入重算）。
   我：**他的主张 11 要求逐帧判决，而每 15ms 读一次磁盘不可能。** 必须限定为"工作集可重建"。**无法实现的不变量比没有不变量更危险。**

---

# 四、净结果（≤5 行）

1. **他赢的两条我写进代码**：`emittedText` 唯一权威（控制器已放行）+ 逐轮落盘（**但只存 `committedThroughSeq` 一个整数，~120 B/轮，不存全文**）；A + 软 200ms / 硬 400ms 双截止推迟窗口（**但它必须有投递缓冲 —— 他的"不引入缓冲"是错的，照字面实现会得到他要消除的半句**）。
2. **我赢的三条**：`emit()` 只覆盖人类转写（UI 词条是不可互转的另一种类型，不是同一个出口）；`asOfTurn`/令牌 `rebuild` 判过度设计，用签名预留 + `expectedPrefixHash` 替代；**L0 八项里只有 4 项不可回填 → P0 = 2–3 天（1.8 天代码），不是 2 周。**
3. **双方都没提、我这轮实测补上的**：宿主**没有 per-chunk 钩子、`cancel` 只停在 chunk 边界**（所以"停在句子边界"必须自带缓冲）；**`committed` 单水位堵不住反向错位**（弱网下需要 ack 水位做裁剪）；**compaction 用 `surfaceOp:'replace'` 折叠历史 → 唯一真源有地平线**（必须落 `turn/compaction` 事件，这是最接近"不可回溯"的真实机制）。
4. **他这笔账的代价**：8 项打包 + 全量 `emit()` + 令牌机制 + `asOfTurn` ≈ **多花 6–8 天**，并**把 P4 的 21 天真人验证推迟一周以上** —— 架构洁癖在这里伤的是他自己的验证节奏。
5. **最该先写的一行**：`turn/text-committed{turnId, speaker, committedThroughSeq, sha256}` —— 20 行，同时是 R5 的物理前提、S4 的纠偏对象、审计的边界、T4 的归属依据，**而且是这里唯一一件"不写就永远补不回来"的事。**
