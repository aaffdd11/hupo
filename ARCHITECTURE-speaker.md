# 说话人识别与主被动对话（Speaker ID & Passive Listening）

> 输入侧的一次结构性升级:**输入不再只有"用户在说话",而是"多人在说话,而且要知道谁在跟我说话"。**
> 对应新增的三件事:① 主人识别 ② 主/被动对话 ③ 被点名响应。

---

## 一、角色定义

| 角色 | 定义 | 系统对待方式 |
|---|---|---|
| **主人 (owner)** | 唯一的主体用户,有注册声纹 | **默认只对主人回应**;以主人为记忆主体 |
| **访客 (guest)** | 场景中的其他人 | **默认不回应**;发言进入会话语料,不进主人的对话记忆 |
| **被点名** | 任何人对助手直接说话(非主人也算) | **例外通道**:必须回应,否则用户会觉得"它不理我" |

**关键纪律**:默认只回主人,但"被点名"是硬例外 ——
因为一个"喊了也不理"的助手,在现场会立刻被判定为坏掉的。

---

## 二、说话人识别(技术方案)

| 环节 | 做什么 | 说明 |
|---|---|---|
| **VAD** | 检测"有人在说话" | 客户端本地做,省流量、省算力 |
| **声纹注册** | 主人录一段(建议 10–30s),存模板 | ⚠ 声纹是**生物特征**,必须加密存储、可删除 |
| **说话人分离 (diarization)** | 切分"谁在什么时候说" | 云端做;需要处理重叠发言、远场、噪声 |
| **主人判定** | 与声纹模板比对,输出相似度 | 输出**置信度**,不是二值 —— 低置信度走保守策略 |
| **说话人聚簇** | 给非主人分配稳定 ID(guest-1/guest-2) | **粒度建议:主人 / guest-N 两类即可**,不必识别"张三李四" |

**为什么粒度只到"主人 / guest-N"**:
- 区分"是谁"在法律上是**处理他人生物特征**,合规负担陡增
- 而产品只关心一件事:**这句话该不该我回应** —— "主人 vs 非主人"已经够用

---

## 三、主/被动对话(核心状态机)

```
                    ┌──────────────┐
        ┌──────────►│  主动对话模式  │◄──────────┐
        │           │  (Active)     │           │
        │           └──────┬───────┘           │
        │   主人说话/被点名  │                   │ 明确结束语
        │                  │ 进入多人场景        │ 或主人示意
        │                  ▼                   │
        │           ┌──────────────┐           │
        └───────────│  被动聆听模式  │───────────┘
      被点名或主人明确提问│ (Passive)  │
                    └──────┬───────┘
                           │ 累积
                           ▼
                    ┌──────────────┐
                    │ 会议文档生成   │
                    │ (只出文档，   │
                    │  不主动说话)  │
                    └──────────────┘
```

| 模式 | 谁在说 | 助手行为 | 输出 |
|---|---|---|---|
| **主动对话** | 主人对助手说 | 正常闭环(接收层 → 反馈层) | 对话回答 |
| **被动聆听** | 多人互相聊 | **不出声**;只记 + 结构化成文档 | 状态提示(正在听) |
| **被动但被点名** | 有人对助手说 | **必须回应** | 对话回答(即使不是主人) |

### 3.1 被动模式下"不出声"是硬要求
被动模式若触发了接收层的自动开口,**会议现场会变成事故** —— 助手不停插话。
所以被动模式下:
- 接收层**不产生语言**(它的"在场感"改为**状态提示**:界面上显示"正在聆听")
- 但**持续维护一份滚动摘要**(谁说了什么、议题、决定、待办)
- 累积到阈值或用户要求时,输出**会议文档**

### 3.2 被点名检测(例外通道)
判断依据(按可靠度排序):
1. **称呼词**:助手名 / "喂" / "在吗" / "小X"
2. **直接提问句**:疑问语气 + 第二人称(你/你们)
3. **情绪与语气信号**(复用你要求的情绪检测):"你怎么不回答呀""我跟你说话呢" → **高强度 + 指向性 = 被点名**
4. **停顿等待**:说完后有明显停顿,像在等人接话

**这三条与情绪检测天然咬合** —— 情绪强度在这里不是用来决定"怎么答",而是用来决定"**要不要答**"。

### 3.3 主人是否在等助手回答(必经判定)
这是你提的最细也最关键的一条。主人在多人场景里说话有三种可能:

| 情况 | 判据 | 助手行为 |
|---|---|---|
| **在对别人说** | 第二人称指向现场其他人("你把这个发我") | **不介入** |
| **在对助手说** | 称呼助手 / 直接提问 / 命令式 | 进入主动对话 |
| **自言自语/思考** | 陈述句、无指向、无停顿等待 | 不介入,但记录 |

**这条直接决定"助手是不是在乱插话"** —— 判断错了,要么漏答主人的问题,要么在会议里不停抢话。

---

## 四、记忆的隔离(这条不做会立刻出事)

被动聆听会灌入**大量属于"他人"的内容**。处理不当会让主人的对话记忆被污染,表现为:
- 助手开始回应"别人的话题"
- 主人问"我上次说的事",助手答成 guest 说的话

**三层措施**:

| 层 | 规则 |
|---|---|
| **不进主人的对话记忆** | 访客发言只进**会话语料**,不进主人的经验/档案 |
| **不注入每轮提示词** | 被动语料**不逐轮注入**(否则上下文爆炸 + 缓存前缀反复失效)。只在**要求生成文档时**一次性喂入摘要 |
| **出声需主人授权** | 会议文档要回复给谁、能不能替主人说话,**必须由主人确认**(默认只给主人) |

---

## 五、新增组件与任务

| ID | 任务 | 依赖 | 说明 |
|---|---|---|---|
| **D29** | 客户端 VAD + 音频采集 + 说话人分离上行 | D12 | 分段上传,不是流式音频全传 |
| **D30** | 声纹注册与比对服务 | D29 | 加密存储、可删除、相似度带置信度 |
| **D31** | 说话人聚类(guest-N) | D29 | 只做"主人/非主人"粒度 |
| **D32** | 模式状态机(主动/被动/被点名) | D31 | 含"主人是否在等回答"判定 |
| **D33** | 被点名检测(称呼/指向/情绪/停顿) | D32 | 与情绪检测共用一次调用 |
| **D34** | 滚动摘要 + 会议文档生成 | D32 | 独立于对话记忆 |
| **D35** | 被动模式的状态提示 UI | D32 | 不出声也有在场感 |
| **D36** | 被动语料的隔离存储与保留策略 | D34 | 不进主人记忆 |
| **D37** | 录音合规:提示音/告知/授权/删除入口 | D29 | **法律必需**:录制他人语音须告知 |

---

## 六、必须先说的三个风险

### 风险 1:录制他人语音的法律与伦理
多人场景录音涉及**他人语音(生物特征)**。多数司法辖区要求**事先告知并取得同意**。
**必须做**:录音开始有明确提示(声音或灯光)、可一键停止、可删除、有可见的隐私说明。
**这条不做,产品不能上线。**

### 风险 2:说话人识别在真实场景的准确率
远场、重叠说话、噪声、方言、设备差异 —— 会议室场景是 diarization 的**最难工况**。
**对策**:主人判定输出**置信度**,并定义三档行为:
- 高置信是主人 → 正常
- 低置信 → **保守策略:不主动回应,但记录**(宁可漏答,不可乱答)
- 明确非主人 → 被动处理

### 风险 3:被动模式会诱使"什么都记"
会议语料是**低价值高噪声**的。如果全量保留,存储和隐私都是负担。
**对策**:会话语料**强保留期**(建议 7–30 天自动清除),只把**提炼出的结论/待办**长期保留,且需主人确认。

---

## 七、与已有设计的关系

| 已有设计 | 变化 |
|---|---|
| 情绪检测 | **职责扩展**:除"怎么答"外,新增"要不要答"(被点名检测的一部分) |
| 接收层 | 新增"静默模式":被动模式下不产生语言,只维护摘要 |
| 控制器 | 新增:模式状态机 + 被点名检测 + "主人是否在等回答"判定 |
| 记忆库 | 新增:会话语料区(隔离于主人记忆) + 会议文档区 |
| 客户端 | 新增:VAD、录音权限与提示、聆听状态 UI、说话人标记显示 |
| 12 场景验收 | 需新增多人与被动场景(建议 4 条:被动不插话 / 被点名必答 / 主人对他人说话不介入 / 会议文档正确) |

---

## 八、待你拍板

1. **说话人粒度**:只分"主人 / guest-N"(我建议),还是要识别具体身份?
   —— 后者合规负担大得多,且对"该不该回应"没有额外价值
2. **被动模式是否保留音频原文**:我建议**只留文本转写 + 保留 7 天**,音频不留
3. **被动模式怎么结束**:自动(一段时间没人说话)/ 主人手动 / 两者都要?
4. **被点名时,如果不是主人**:回应到什么程度?我建议**简短回应 + 引导给主人**("这个问题我来不及展开,等 X 说完我一起说"),避免喧宾夺主
