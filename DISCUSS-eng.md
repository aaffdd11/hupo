# 工程实践讨论稿：v6 能不能跑起来、先跑哪段、哪里先崩

> 作者视角：15 年客户端 + 服务端全链路，做过流式对话、音视频、移动端。
> 本文不复述 v6 与三份专文的设计，只回答四件事：**能不能跑、先写哪段代码、什么时序会崩、代价是多少。**
> 所有判决基于已核实的工程事实（宿主行为、实测延迟、本机容量），凡是我没有实测依据的地方我标 **[待测]**。
> 本文与 `REVIEW-eng.md` 的分工：那份评审管"交付/运维/故障/成本"，这份管"**投递、时序、中止、竞态**"。

---

## 0. 我的假设（不成立请直接驳回对应结论）

| 项 | 假设 | 依据 |
|---|---|---|
| 团队 | **1–2 人，10–20 h/周**，无专职测试、无值班 | 与 REVIEW-eng 一致 |
| 客户端 | **自研 Flutter（不用 DSH 自带 web UI）** | REVIEW-eng 炸点 2：DSH UI 渲染所有 `user/message`，无 `source.kind` 过滤 |
| 网络 | 4G/Wi-Fi 混合：RTT p50 60–120ms、p99 400–800ms、丢包 0.5–2% | 移动端常态 |
| 输出形态 | 文本为主，**语音输出（TTS）在 v6 里没有明确，但说话人识别那条隐含了语音** | 我按"文本 + 可选 TTS"评估，TTS 会显著改变结论（见 §2.4） |
| 模型 | `deepseek-flash` 关思考（接收层）+ `deepseek-v4-pro`（处理/反馈层） | README 实测 |
| 生成吞吐 | **60–70 tok/s**、中文 ≈1.2 token/字 —— 从 README 反推：快层整句 0.98s 总时长 − ~0.6s TTFT ≈ 0.38s 生成 ~20 字 | **[待测]**，是 §2 全部估算的支点，V-A 第一件事就是量它 |
| 宿主关键事实（我已复核代码） | 中止时 `assistant/message{interrupted:true}` 的 content = `assembler.interruptedBlocks()`，且带 `sourceEventSeqs: chunkSeqs`（`dsh-agent-loop/lib/index.js:631-648`）；中止点在 **chunk 边界**（`for await` 里逐步 `throwIfAborted`） | 意味着"生成到哪"是**可精确重建**的 |
| `surfaceOp` | `dsh-session/lib/types/surface.js` 原文：landed replacement **会遮蔽用户已看到的对话**，append-origin 事件才是"人类转写的耐久源"，replacement **只对模型可见** | 不能用来擦字，但**可以合法地用来"从模型视角删掉没投递的尾巴"**（见 §2.3） |

---

## 一、把 v6 拆成"能跑的"和"跑不起来的"

判据：✅ 有现成接口/无新研究，今天就能写；⚠️ 能做但必须在某处做取舍；❌ 第一版做不了（缺接口或缺前提数据）。

| # | 机制 | 判定 | 最关键的一个实现难点 | 我会先写哪段代码验证它 |
|---|---|---|---|---|
| 1 | **两块回答的无缝衔接** | ⚠️ | **不是提示词问题，是"三份文本一致性"问题**：宿主里有一份"生成全文"（`assistant/message`，含没投递的尾巴）、客户端有一份"用户实际看到的"、交接包里要有一份"已输出全文"。**这三份一旦不等，反馈层就会锚定到用户没看见的话 → 用户觉得它说了句我没看到的话（露馅）。** 第二难点：**难题档两块间隔 5–7s**，接收层要独白 5–7s，而它的素材只有"我准备怎么做"，继续说就必须**新造可外发的进度信号**（v6 没有这个接口，见 §1.1 矛盾 1） | `src/delivery/watermark.js`：一个 `DeliveredText` 单一真源 + 三条字节级断言：①`delivered` 是 `generated` 的前缀；②`handoff.text === delivered.text`；③`turn.stop` 之后 `delivered` 不再增长（除显式 ack 补投）。20 行代码，能立刻把最贵的不一致顶出来 |
| 2 | **切换判定（ETA × 时效性 × 打断禁区）** | ⚠️ 降级为"定时器 + 概率查表" | **ETA 在物理上不可测。** LLM 生成没有剩余时间信号，你唯一能拿到的是"已经跑了多久"；"还要多久"取决于模型什么时候决定停。所以"ETA × 时效性"这个乘法第一版无法实现，只能退化成"按已耗时查历史分布" | `scripts/eta-table.js`：**用现成的 `data/metrics.jsonl` + `~/.dsh/sessions/*.jsonl` 里每轮的 `deepLatency` 生成 `P(深层已完成 \| 已耗时桶)` 直方图**，再做离线回放：假设按这张表判决，会误判多少轮（把 6.5s 的当 2s 的）。**零新功能、半天出结果**。判据：p90 误差 > 1.5s → ETA 不可用，接管改成纯定时器（"到 3s 没结论就直接接"） |
| 3 | **被点名检测** | ⚠️ 能做，但**不能作为唯一依据** | **误报与漏报的代价严重不对称**：漏答 = 用户觉得它坏了；误答 = 会议事故（"它开始对着别人说话"）。而检测链路本身有 **≥1.7s 的固有延迟**（ASR 端点 500–700ms + 语义判定 400–1100ms + "停顿等待" 800ms），这段时间里**主人可能已经开始下一句**了 → 判决一到就已经过期 | `test/addressed.test.js`：30–50 条**手工构造**的语句（称呼词/指向性/自言自语/对他人说/情绪型指责），跑判定器出混淆矩阵。目的不是调准，是**先看误报出现在哪种句式上**。第一版不接 LLM，只用"称呼词命中 或 (第二人称 AND 停顿≥800ms)"的双条件规则 |
| 4 | **主被动状态机** | ✅ **最值得先做的一条** | 难点不在于状态机，而在于**入口条件用的是不可靠信号**（说话人判定 p50 1.5–2.5s、置信度分布很宽）。上游不可靠时，状态机必须做**滞回 + 置信门限 + 可撤销默认值**：默认主动（宁可多答，不可漏答），只有"连续 N 秒多说话人 且 主人未直接提问"才进被动；退出走乐观路径（一次被点名就退出） | `src/controller/mode.js`：**纯函数状态机**（输入事件序列 → 输出模式 + 是否允许出声），配 `test/mode-replay.test.js` 回放 12 条场景。**不需要模型、不需要网络、不需要真麦克风**，一天能做完整并回归 |
| 5 | **交付形态四档** | T1 ✅ / T2 ✅ / T3 ⚠️（容器+合规 2–3 周）/ **T4 ❌ 第一版** | T4 的难点不是"生成应用"，是**失败是异步的**：用户在 5 分钟后收到失败，而这时他已经因为"几分钟就好"的承诺把期待拉高了，**没有任何回滚路径**。所以 T4 的第一段代码必须写**失败路径**（超时/自检失败/磁盘满时用户看到什么、产物怎么回收），不是生成器 | `src/delivery/t4-failure.js`：任务状态机的**失败分支** + 时间承诺的"实测分布查表"（误差 >50% 主动更正）。跑通后才写生成器 |
| 6 | **级联** | ⚠️ 能做 / **❌ 第一版不做** | **它的瓶颈恰好长在最贵最慢的位置**：融合节点是父、专家是子 → 父子前缀命中必然 0% → 融合是全价冷启动；而融合恰恰决定"反馈层何时能开口"。延迟上 `p50(融合) ≈ max(p50(节点)) + 1.3s`，比单节点更慢 | **不改任何生成代码**：把现有 `CONCIERGE_EXPERT_ANGLES` 从 2 调到 4，跑同一批问题，看 p50/p95 与 `total_llm_calls` 怎么涨。**这是 README 里已经存在的旋钮，一次实验约 1 小时**，直接给出"每多一个节点，用户多等多少毫秒" |
| 7 | **记忆隔离三层** | ✅ 必须做 | 难点不是"隔离"，是**"共享面的清单化"**：`每用户一 DSH_HOME` 之外，还存在**共享只读 profile**（工具 schema / persona）、插件配置、日志目录、`~/.dsh/sessions`。任何一处可写 = 一个静默的跨用户通道。所以要做的是"把共享路径写成白名单 + 强制只读（`chmod a-w`/`ro` 挂载）+ 启动时可写即拒启" | `test/isolation.test.js`：u1 写入 → ①u2 检索为空；②u2 的 `session.jsonl` 不含 u1 任意 20 字片段（字符串级断言）；③**共享目录在整个测试期间 mtime 不变**；④断言不存在无 `userId` 的读接口（grep 签名） |
| 8 | **说话人识别** | ❌ 第一版（`§4` 详述） | 真实工况（远场/重叠/噪声/方言）是 diarization 的最难档，**且它的输出直接驱动"要不要出声"这个不可撤销的动作** | 只做**自用 VAD**：`src/audio/vad.js` —— 端点检测 + barge-in 打断，不录他人、无合规成本。先量：端上 VAD 的 CPU/电量/端点延迟 **[待测]** |

### 1.1 拆 v6 时发现的三个文档级矛盾（都不是措辞问题，都会变成返工）

**矛盾 1：`§三` 的时间轴只覆盖了"简单档"，而核心目标恰恰在另一档。**
`ARCHITECTURE-v6.md:73-80` 画的是 `t≈0.9s 处理层出结论 → t≈1.5s 深思回答`。但 README 的实测是 `p50 7.2–7.9s`。**难题档下两块间隔是 5–7s**，而 seamless 专文自己写"间隔超过约 4 秒才允许视觉分隔"——等于承认**难题档常态就是"两段"**，"两块拼成一条无缝的话"这个核心承诺在最需要的档位失效。
→ 必须补一个 v6 没有的接口：**处理层要吐出"可外发的进度信号"**（已分类到哪类 / 已召回 N 条经验 / 在查第几处），接收层才有素材续说。用自由文本续说一定会编或重复。这不是提示词能解决的，是**处理层的输出 schema 要多一个字段**，事后补要动协议。

**矛盾 2："只追加不改前缀"(R2) 与"一小时会议的滚动摘要"不可兼得。**
滚动摘要必然压缩 → 压缩必然改写前缀 → 缓存从改写点起全失效。解法不是妥协 R2，而是**明确豁免**：被动语料**不进主对话上下文**，滚动摘要跑在**独立短上下文**里（会议头 + 最近 10 分钟摘要 + 最近 3 分钟转写），每次跑完即弃，**不依赖跨调用的前缀缓存**。这条豁免必须白纸黑字写进架构，否则实现者一定会把会议摘要挂到主 prompt 上。

**矛盾 3："每用户一进程 + 共享只读 profile" 与 "提示词前缀首段必须是用户标识" 相互牵制。**
共享 profile 里装的是工具 schema 与 persona——**这是 prompt 里最大的一块**。而按 delivery 专文的实测，把用户标识放最前意味着**跨用户永远 0 命中**（这是刻意的）。代价可算且不大（每个用户每个会话第一次总是冷的）；但要注意运维后果：**profile 一变（prompt_version+1），所有用户的缓存同时作废**，全员首字一起变慢。→ 上线纪律：**persona 变更分批灰度，且变更窗口避开高峰**，这条在单人运维下比技术本身更容易出事。

---

## 二、方案 A vs 方案 B：工程结论

### 2.1 先纠正一个前提：这不是"全局二选一"，是"按角色分配"

| 角色 | 输出特征 | 会不会被**系统中止** | 我的选择 |
|---|---|---|---|
| **接收层（快答）** | **很短**（README：快层整句 0.98s 就说完） | **会，而且是常态**——它的存在意义就是在结论到达前占住 0.9–8s，而结论到达时刻不可预测 | **B：按单元投递** |
| **反馈层（深答）** | 长（100–300 字） | **不会被系统中止**（它是最终发言），只可能被用户打断 | **A：逐字流式** |

理由是**收益/代价比在两层上完全反向**：
- 接收层只有一句，**按句投递的代价 ≈ 0.3s，而且这 0.3s 不是"多等"，只是"从'开始看到'推迟到'这句写完了'——用户拿到完整快答的总时间一秒没变。** 换来的是"永不露半句"。
- 反馈层要写几百字，按句投递会把首字推迟 600ms–1.2s，**而这正是用户等了 8 秒之后最想要的那一个字**。它的中止只可能来自用户 barge-in，伤害也小（用户已经听到了大部分）。

**如果只能选一个：选 B。** 判断原则只有一条——**优先消除不可逆的失败，而不是优化可优化的指标。** 露半句是不可逆的（已显示的字撤不回来）；首字慢 300ms 是可优化的、渐进的、而且能从别处省回来（接收层首句提示词压短、分类器并行、情绪检测不阻塞首字）。

### 2.2 "句"怎么切：三条边界规则

纯标点切分是错的——模型不会按你的期望写句子长度。必须**标点 + 长度上限 + 时间兜底**三条一起上，缺任何一条都会出现尾部延迟失控。

| 规则 | 内容 | 作用 |
|---|---|---|
| **硬边界** | `。！？…；\n` 与"块结束" | 正常路径 |
| **长度上限** | 累积 ≥ **25 字** 未遇硬边界 → 强制切 | 防"模型一句话写 60 字"（这是首单元延迟的**唯一**失控源） |
| **时间兜底** | 首个 token 之后 **600ms** 未遇硬边界 → 强制切 | 防模型在长从句里卡住（吞吐抖动、上游排队） |
| **软切点** | 强制切时，**回退到最近 8 字内的 `，/、`** 切；没有就原地切 | 让切断点尽量落在呼吸处 |
| **逗号本身不算边界** | 除非触发上面两条 | 见下 |

**"这个我熟，先看连接池"算一句还是两句？→ 算一个投递单元。**
理由不是语法，是**投递规则**：没有硬边界就不 flush。而且它在语义上自成一个完整说法（"我知道这是什么 + 我要去看什么"），而切在第一个逗号会得到"这个我熟，"，**一个零信息的碎片**。真正的问题是：如果用户在 1.5s 打断，客户端只收到并播放了"这个我熟，"——**这比 A 方案还难看**。所以第一单元必须"自成一个有信息的完整句"，这直接否决了"用逗号做首单元边界"。

### 2.3 首句到底要等多久（估算，支点假设已标）

```
t_first_visible(B) = TTFT + tokens(第一单元)/吞吐 + 标点确认(1 token)
t_first_visible(A) = TTFT
增量 = tokens(第一单元)/吞吐 + ε
```
代入 §0 假设（TTFT p50 ≈0.6s、吞吐 65 tok/s、中文 1.2 token/字）：

| 边界规则 | 第一单元长度 | 首单元就绪（p50 估算） | 相对 A 的增量 | 评价 |
|---|---|---|---|---|
| 只用 `。！？` | 可能 40+ 字 | ~1.6s | **+0.7–0.9s** | ❌ 超出首字预算 |
| `。！？` + 25 字上限 | ≤25 字 | ~1.25s | +0.3s | ⚠️ 可接受，尾部风险仍在 |
| **+ 600ms 时间兜底** | ≤25 字 或 600ms | **~1.15s** | **+0.25s** | ✅ **推荐** |
| 逗号也切 | ≤8 字 | ~1.05s | +0.1s | ✅ 快，但首单元零信息 ❌ |

**结论：+"200–400ms" 是乐观值**（它假设第一句只有 12–20 字且吞吐稳定）。**实际必须靠"25 字上限 + 600ms 兜底"把增量钉死在 ≤0.35s**；不做长度上限，p90 会到 +0.9s，那就等于把接收层存在的理由（首字快）自己拆掉。
**这条估算必须实测**：V-A 里用现有 `session.jsonl` 的 chunk 事件按这三套规则离线重放，直接算出首单元延迟分布与"用户最终看到文本"的差异率。**这是全项目最便宜、最值钱的一次测量。**

### 2.4 用户自己插话（barge-in）：B 是不是反而更好？—— 是，但有前提

**是更好的，三个理由：**
1. **视觉上**：A 会留下"连接池打"这种半词；B 停下来时，可见文本永远结束在一个**完整句子**上。人的对话被打断时本来就是"把这句话说完再停"。
2. **记账上**：B 让"这一轮接收层到底说了什么"变成一个**可寻址的整数**（单元 seq 水位），可以直接进下一轮上下文；A 结束时你手里只有半句话，下一轮模型可能接着半句说，而用户没看到那半句。
3. **对 TTS 是必需的**：语音合成不可能合成"半个词"，**一旦输出走语音，按句投递就不是选项而是必需项**；而语音模式下 barge-in 的延迟预算（VAD 200ms + 网络 100ms + 停止播放）本身就比显示预算宽松得多。

**前提（做不到就白搭）：中止时客户端必须"瞬时 snap 到已收单元末尾"，不能真的把剩余字慢慢打完。**
- 客户端本地做"打字机回放"（服务端按单元投递 + 客户端按字回放），好处是**用户看到的仍然是一条连续的流**，B 的分段对用户不可见。
- 收到 `turn/stop` 时：**停**（软接管，不赶时间，让它把当前单元播完，最自然）；收到 **barge-in** 时：**snap**（当前单元文本已完整在手，瞬间补完 → 然后停）。这样 barge-in 的"它还在说"时长 = 0，而不是 600ms。
- **回放速率必须 ≥ 生成速率**，否则会出现"播完单元 1 等单元 2"的空档——这就是播放器的抖动缓冲问题：`playback_rate = max(base, observed_gen_rate × 1.15)`。

**一个 B 更差的场景（要正视）**：用户打断是因为"它开始说错方向了"。B 会多显示最多 25 个字（一个单元）才停。缓解办法不是撤回（做不到），而是：**打断时给接收层"止损"的权力是零**——所有补偿都必须是**追加式**（见 §3.0）。

### 2.5 中间状态：缓冲里那半句，丢掉还是发出去？

**分档，但默认"丢"，而且"丢"必须发生在服务端。**

| 缓冲内容 | 处理 | 理由 |
|---|---|---|
| 残词（<8 字 / 结尾是"的、了、和、就"这类功能词） | **丢** | 发出去就是露馅 |
| 完整从句（以 `，` 结尾、≥8 字、语义自足） | **发**，并把自己标记为 `soft_end`，让深答按 S1/S2 形态**接着它说** | 少一次"话没说完"的断裂 |
| 已触发兜底切分（≥25 字/600ms） | 早就发了，不存在这个问题 | — |

**三条硬纪律：**
1. **"丢"只能在服务端发生**——投递路径不能"边生成边转发"，否则半句已经在客户端屏幕上。这正是 B 的定义，也是它必须落在**网关**（而不是客户端）的原因：多端一致 + 服务端必须知道"用户实际看到了什么" + 断线续传按单元寻址。
2. **丢掉的文本仍要进会话记录，但必须带 `delivered: false` 标记**（宿主的中止消息里 `sourceEventSeqs: chunkSeqs` 已经给了"生成了哪些 chunk"的精确依据，可以直接拿来算尾巴）。
3. **不要用"丢掉"去暗示"我没说完"。** 用户看不到的东西不该被暗示；要表达"话没说完"只能用**追加**（"——我先不说了"）。

**关于 `surfaceOp: 'replace'` 的正确用法（这是我读 `dsh-session/lib/types/surface.js` 得到的一个非显然结论）：**
它**可以**用来解决"模型视角 vs 用户视角分叉"——把"没投递的尾巴"从**模型可见表面**上遮蔽掉，让模型看到的历史与用户看到的一致。它有两条必须遵守的限制：
- **它不能用来擦用户的字**（原文写死：replacement 只对模型可见，append-origin 事件才是人类转写的耐久源）。所以文档给的那条限制是对的，但**结论不要下成"replace 没用"**——它在"让模型别引用一句用户没看见的话"上是唯一正解。
- **它是一次前缀改写**，与 R2 冲突。落地口径：**只在轮末、只对最后一条 assistant 消息、只做一次，做完立刻重拍前缀指纹快照**，并把"这是一次授权的前缀改写"写进 R2 的例外清单。因为它落在尾部，缓存损失的只是"那一条消息 + 之后的尾部"，不是整段前缀。
- 必须配一条测试断言"replace 之后，模型可见表面 = 用户可见文本"。宿主升级可能改变语义。

---

## 三、时序与竞态：把一轮对话的竞态列全

### 3.0 一条贯穿原则（先立它，后面所有防御都从它推导）

> **已显示的字撤不回来，但可以追加。所以所有纠错动作都必须设计成"追加"，绝不设计成"擦除"。**

这条原则直接决定了：S4 纠偏要显式说"不是 X 是 Y"（追加）、打断之后要追加止损语、被点名误答之后要追加"我插一句，你们继续"、双答竞态要追加合并语。**任何需要"撤回"的方案在这个宿主上都是不可实现的**，不要浪费一天去验证它。

| # | 竞态 | 时间窗 | 坏结果（具体） | 防御 | 第一版怎么做 |
|---|---|---|---|---|---|
| **C1** | **交接包定稿时接收层又吐了一句** | 深答就绪 → cancel 生效之间，**50–300ms** | 反馈层承接的是"用户看过的上一句"，而接收层已经又说了新的一句 → 用户看到**两段重叠**、语义打架 | 接管必须是**原子三步**：①标记 turn 为 stopping ②`cancel(cause, {keepInbox:true})`（cause 用 `{kind:'plugin', plugin:'controller'}`；**`keepInbox` 默认 false，不传就会清空 inbox，丢掉所有待注入上下文**）③等 `assistant/message{interrupted:true}` 固化后再定稿交接包。**顺序不能反**（先定稿再 cancel，接收层一定会在窗口里多吐一句） | 必须带 150ms 超时兜底：`cancel` 在 `phase.kind === 'idle'` 时**是 no-op，根本不会产生 `interrupted` 消息**（`dsh-agent-loop/lib/index.js:405-412`），**死等就会卡住**。超时后改用**网关侧投递水位**定稿（权威是投递水位，不是宿主消息） |
| **C2** | **中止信号与 chunk 同时到** | 在途 chunk **100–500ms** | 服务端以为只投递到 seq=12，客户端又收到 13/14 并渲染 → **用户看到的文本比服务端记录的 delivered 多 2 个单位**，反馈层锚点就偏了（露馅的经典来源） | 无法在 A 方案下根治（字已显示）→ **B 方案天然根治**：投递水位永远落在单元边界，客户端只显示"已收单元"。服务端权威 = 客户端 ack 的单元 seq | 第一版：`turn/stop` 后服务端停止转发 + 客户端在收到 stop 后丢弃未 ack 单元；对**已显示**的部分追加止损语 |
| **C3** | **情绪检测与首句同时到** | 两者都在 **0.75–1.1s** 附近落地，几乎同到 | 设计要求"情绪 ≥0.7 先接情绪再讲逻辑"，但情绪信号到的时候**接收层首 token 可能已经在飞**（TTFT p10 可能 <0.75s）→ 大约 20–30% 的轮次里"先接情绪"物理上做不到 | **把接收层首句设计成"情绪无关"的**（无论什么情绪都成立的中性承接句），情绪信号只作用于**第二句之后 + 反馈层**；情绪块作为**尾部追加块**注入（不破前缀）。情绪晚到不阻塞首字 | 首句模板化约束写进接收层 role 段；情绪块追加时带 `emotion_seq` 以便回溯 |
| **C4** | **用户在两块之间插话** | 快答结束 → 深答到达，**间隔可达 5–7s** | ①用户输入被误当接收层的 steer 上下文混进交接包；②反馈层产出的是**旧问题的深答**，用户已经问到别处 → 答非所问；③旧 turn 已 `interrupted` 固化但用户已被计费 | **turn 血缘**：`turnId + parentTurnId`；用户插话 → 新 turn 建立、旧 turn 标 `superseded`；反馈层产出与旧 turn 绑定，**turn 已 superseded 则不投递深答**（宁可丢，不可串），只写记忆 | 丢弃 + 落 trace（判断"是否仍相关"要再调一次模型，不值得）。处理层**继续算完**（算力已花），产物写记忆备复用 |
| **C5** | **被点名与主人发言同时** | 判决窗口 **≤300ms** | 两条判定都触发 → 要么答两次，要么答错对象；更常见的是**判定晚到**：接收层已经因为"主人说话"进入长回答，而现场需要的是短答 | **单一判决点 + 优先级**（被点名 > 主人明确提问 > 被动）；**300ms 判决窗口内冻结输出**（接收层不开口）。窗口内没定稿 → **默认保守不答** | 第一版：把"要不要说话"做成**唯一一个前置闸门**，所有开工路径都必须先过它 |
| **C6** | **断线重连时两块正在流** | 重连 **1–10s** | 缺字 / 重复渲染 / 顺序错 / **两块被渲染成两条消息**（拼接语义丢失）；用 A 方案还要按字节偏移续传，而 chunk 平均只有 ~220 字节、一页 50 条消息就有 52k 事件 / 11MB | **以 turn 为单位、以单元 seq 为地址的续传**：服务端保留每 turn 最近 N=200 个单元（≈5–8KB）；客户端上报 `lastAckSeq`，只重放未 ack 的部分。**消息分组语义由服务端给**（`turnId + block: fast\|deep`），客户端不做拼接推断 | 重连**绝不重放整轮**（否则一个长回答重连一次就重传几千 chunk）。这条在 B 方案下是免费的，在 A 方案下要做字节水位 |
| **C7** | **接收层和处理层同时"回来"** | 深答可能 **比快答首字更早**（README：0.9s vs 1.2s） | 反馈层抢在接收层开口前就绪 → 交接包里"已输出全文"为空 → 反馈层锚点无处可锚 → 说出一段断裂的话 | 必须区分**两种就绪**：`deep_ready`（结论就绪）与 `handoff_allowed`（可以接管）。就绪太早时**延迟接管到达接收层开口之后**（`deferWhileThinking` 已经在 v1 里实现过） | 交接包允许 `delivered` 为空，此时反馈层按 **S3 形态**（轻量重启信号）开口，而不是假装承接 |
| **C8** | **两块被 barge-in 中止 + 处理层还在算** | 打断后 **数秒** | 处理层继续烧 token、产出没人要的深答；或深答在 30s 后落到一个已被替换的 turn 上 | 打断 → 反馈层生成立即 cancel；**处理层不 cancel**（沉没成本已付），产物写记忆并标 `superseded` | trace 落 `superseded_turns`，用来评估"白花的算力占比" |
| **C9** | **多设备同账号** | 常态 | 两台设备各自渲染同一轮 → 一台显示了、另一台没显示，ack 水位互相覆盖 | 水位是**每设备**的（`(userId, deviceId, turnId) → lastAckSeq`）；服务端保留窗口按最慢设备对齐，窗口超时降级为"拉取历史" | 第一版单设备优先，多设备只保证"最终一致（可回看）" |
| **C10** | **prefix 快照 vs `replace`** | 轮末 | replace 之后，模型可见历史变了，而"前 K-1 轮字节不变"的断言立刻失败 → 有人会去"修断言"，把唯一的护栏拆了 | 把 replace 明确列为**唯一授权的前缀改写**，断言改成"除轮末 replace 外逐字节不变"，并在 replace 后立即重拍快照 | 一条单测：replace 一轮 → 断言前 K-1 轮（除被 replace 的那条消息）字节不变 |

**竞态防御的工程归纳（写进代码评审清单）**：
1. **单一写入者**：只有控制器能 steer/inject/followup/cancel，任何其他组件想影响对话必须走它。
2. **权威顺序**：客户端 ack 水位 > 网关投递日志 > 宿主 `assistant/message`。**永远不要用宿主消息当"用户看到了什么"的依据**（它包含没投递的尾巴）。
3. **每个终态都要有超时兜底**（cancel 等待 150ms、深答等待 ≤25s 皮层超时、重连窗口 60s）。没有超时的等待就是死锁。

---

## 四、说话人识别与被动聆听：工程可行性

### 4.1 VAD + 说话人分离 + 声纹比对：真实复杂度与延迟

**结构性结论：端上只做 VAD 与分段，diarization 与声纹必须在云端；而"分段 vs 流式"不是偏好问题，是你的延迟预算决定的。**

| 环节 | 端上/云端 | 真实代价 | 延迟 |
|---|---|---|---|
| VAD | 端上（Silero/WebRTC 级） | 手机 CPU 上极低（<2% 单核），10–30ms 帧 | 即时 |
| **端点检测（分段方案特有）** | 端上 | 需要静音 300–700ms 才能确定一段结束 | **+0.5–0.7s，省不掉** |
| diarization（分离/聚类） | 云端 | CPU 上跑 pyannote 级模型 ≈ **每 1 小时音频要几十小时算力**；**本机 4 vCPU 上做流式 diarization 是不可能的** | 只在"段级 + 候选段"上做，不做全流 |
| ASR | 云端 | 段级 2s 音频，首字 300–800ms；流式方案 300–500ms | — |
| 声纹比对 | 云端 | 嵌入模型 CPU 约 **50–150ms/段**；**可靠比对需要 ≥1.5–3s 语音**：3s 语音 EER 约 2–5%，**<1s 时 EER 会升到 10–15%** **[文献量级，需自测]** | 80–150ms |
| 判定 LLM（33–35 token，关思考） | 云端 | 已实测（假前提检测同规格）：**395–1077ms** | 与上面并行 |

**端到端"要不要回应"的判定延迟：**

| 方案 | p50 | p90 | 能不能做到"被点名 1.5s 内开口" |
|---|---|---|---|
| **分段上传（推荐第一版）** | **1.5–2.3s** | **3.0s+** | ❌ |
| 流式音频 | 0.9–1.4s | 2.0s | ⚠️ 勉强 |

**所以：第一版分段（2s 段 + opus，有声音才传）。如果产品真的要"被点名后马上回应"，那必须单独开一条流式音频的技术线，那是另一个量级的工作。**

**移动端还有两条非技术性的硬约束**（会直接决定这条功能能不能上线）：
- **电量**：连续录音 + VAD ≈ **8–15%/小时**（[待测]）。
- **平台限制**：iOS 后台持续录音需要 audio background mode，**并且系统的麦克风指示器会常亮**。用户看得见的"一直在录音"，是产品问题，不只是法律问题。

### 4.2 被动模式下"不出声但维护滚动摘要"的资源代价（长会议 1 小时）

| 项 | 1 小时会议的量 | 评价 |
|---|---|---|
| 上传音频 | 说话率按 40% × 3600s × 16kbps opus ≈ **26–53MB** | 可接受，前提是"只在有人说话时传" |
| ASR | 24 分钟音频 | 按分钟计费 |
| 声纹比对 | ~200–400 段 × 100ms ≈ **20–40s CPU** | 单机可接受 |
| 滚动摘要 LLM | 每 60–120s 一次 → **30–60 次调用**，每次输入 ~1.5k 字 / 输出 200 字 | **这是主要成本项**，不是 ASR |
| 客户端内存/电量 | 连续录音 + VAD **8–15%**；文本量约 1.5–2 万字/小时（60–80KB） | 文本可忽略，**电量是主要代价** |
| 磁盘 | 只留转写、7 天保留 → **几个 MB** | 正确决策：音频不留 |

**真正的技术风险不在存储，在"上下文增长"**：如果滚动摘要挂进主 prompt，1 小时会议会有 30–60 个摘要块、前缀涨到 15k–30k tokens，**每次摘要调用都要重新 prefill 几秒**——这正好命中 REVIEW-eng 炸点 3（首字 p50 破 1.5s）。
→ **必须用独立短上下文**（会议头 + 最近 10 分钟摘要 + 最近 3 分钟转写），跑完即弃，**不依赖跨调用缓存**。这就是 §1.1 矛盾 2 的豁免，必须写进架构。

### 4.3 被点名检测的误报率：我能接受的阈值与降级

**先算代价**：一场 1 小时的会有 **50–200 个发言段**。如果段级 FP = 2%，就是**每小时 1–4 次误答**——在会议里这不是"不准确"，这是**事故**（助手突然对着别人说话）。
→ **我要求的阈值：段级 FP ≤ 0.5%，recall ≥ 85%。** FN 可以宽松（<10% 可接受），因为漏答的代价只是"用户再喊一次"，而误答不可撤销。

**怎么做到：**
1. **双条件触发**（不是单一信号）：称呼词命中 **或**（第二人称指向 AND 停顿等待 ≥800ms）。单靠"疑问语气 + 你"在会议里必然高 FP（"你这个方案怎么想的"是对同事说的）。
2. **LLM 只做第二意见，不做唯一依据**：规则先过一遍，只有规则命中才调 LLM 确认。这样既省一次调用，也把 FP 压在规则上。
3. **确认窗口**：判定为"被点名"后，**要求 ≥800ms 的静默**才开口（真人被点名后本来就会给一点反应时间）。这 800ms 顺便把 p90 延迟推到 3s，所以只适合"被动模式"，不适合主动对话。

**降级阶梯（三级，每级都能上线）：**
| 级别 | 行为 | 适用 |
|---|---|---|
| L1 自动 | 高置信 → 短答一句（**绝不允许触发 T2/T3/T4**） | 目标形态 |
| L2 半自动 | 判定为可能被点名 → **屏幕状态提示**（不发声）+ **主人一键"回答"** | 推荐的第一版 |
| L3 手动 | 主人长按才答 | 兜底，永远可用 |

**L2 是关键**：它用"主人点一下"这个物理动作替代不可靠的检测，代价几乎为零，而在会议室里手机屏幕亮一下完全不算打扰。**这条应该是第一版的形态，不是降级。**

### 4.4 这条功能第一版该不该做？——**不该。**

三条理由，都是工程性的：
1. **它的输入端（可靠说话人判定）本身就是 1–2 个月的攻关**，而它输出的动作（出声/不出声）是不可撤销的。
2. **它的合规成本是"不做就不能上线"级别**（告知 + 提示音/灯 + 可停 + 可删 + 隐私说明），这几件事没有一件能靠技术债拖。
3. **P4 是 21 天真人自用验证**。现在连"主人愿意主动用"都还没验证，**被动的边际价值根本无法评估**。

**第一版只做"自用 VAD"**：端点检测 + barge-in 打断。收益（打断体验、端点触发首字、静音检测）远大于复杂度，**而且不录他人、零合规负担**。

---

## 五、我会强烈要求先做的 3 个技术验证

每条都能在 1 天内完成，而且**失败会推翻一条已写进架构的设计**。

### V-A：投递边界验证 —— "能不能扣住 chunk"+"按单元投递要付多少延迟"

**做法（半天–1 天）**
1. **离线测算（2 小时，零新代码）**：拿现有 `~/.dsh/sessions/*.jsonl` 里的 `assistant/chunk` 事件按时间戳重放，用 §2.2 的三套边界规则（纯标点 / +25 字 / +600ms）切单元，算出：①首单元相对首 chunk 的延迟分布；②用单元投递后，**用户最终看到的文本**与逐字流式的差异率；③接收层真实首句长度分布（这是 §2.3 全部估算的支点）。
2. **在线验证（半天）**：起一个网关代理 `/api` + WS 事件流，只扣**接收层的文本 chunk**（块边界、工具事件、状态事件必须照常放行），确认客户端能在单元边界渲染、且 ack 水位可回传。

**失败会推翻什么**
- 如果**扣不住**（客户端直连宿主 WS、或宿主依赖 chunk 级事件推进状态）→ B 的执行点只能挪到客户端 → 那么"多端一致""断线续传按单元寻址""服务端知道用户看到什么"这三条全部要重做，§3 里 C2/C6/C9 的防御都要重写。
- 如果离线测算出**首单元延迟 p90 > 600ms** → 说明接收层的首句必须被硬性压到 20 字以内（提示词约束 + 兜底切成 15 字），"无缝"的代价高于预期，需要重新评估 B 的收益。

### V-B：接管时序闭环 —— 五种时序场景

**做法（1 天）**
写一个"假宿主 + 真模型"的场景驱动脚本，强制复现五种时序，每条都断言"用户视角的输出"：
1. 正常：接收层说完 → 深答接上；
2. **接管时接收层正在吐半句**（人为在 chunk N 处 cancel）；
3. 用户在两块之间插话（深答到达前 200ms）；
4. 反馈层生成中用户又插话；
5. **cancel 之后仍有 2–3 个在途 chunk**（人为注入延迟）。
断言：不出现"两段重叠"、不出现"用户看到未投递文本"、不出现"turn 串味"、不出现"两条消息"（渲染必须还是同一条）。

**失败会推翻什么**
- 如果**无法在 150ms 内拿到固化的 `interrupted` 文本** → 交接包不能以宿主消息为准，"已输出全文"必须改成**网关投递日志**为准 → R5（反馈层上下文必含接收层已输出全文）的载体换了，v6 §三 的时序表与 P2 的验收标准都要改。
- 如果第 5 条在 A 方案下无法防住 → **C2 这条竞态成了"必须选 B"的决定性证据**（我会把它作为推荐 B 的第三理由写进最终结论）。

### V-C：前缀不变性与"情绪不阻塞首字"的实测

**做法（1 天）**
20 轮真实对话，中途注入情绪块与其他尾部块，落两份数据：
1. 每轮 `prompt_tokens / cached_tokens / first_token_ms`，以及"前 K-1 轮序列化字节 `Buffer.compare === 0`"的断言；
2. A/B 两种首句策略的首字 p50：**首句等情绪信号** vs **首句情绪无关（情绪只作用于第二句+）**。

**失败会推翻什么**
- 如果**尾部追加块也会让 `cached_tokens` 塌陷**（例如上游按整段哈希，或工具 schema 参与前缀）→ R2 的落地方式（"尾部追加即可保住缓存"）是错的，记忆注入与情绪注入的方案都要重做，这会直接影响 P1/P2 的工期。
- 如果"首句等情绪"把首字 p50 从 0.9s 推到 1.6s+ → **"情绪只影响怎么说"这条纪律在第一版不可实现**，必须降级为"情绪只影响反馈层"，R3 要改写。

> **补一条不成体系的便宜验证（2 小时，不做也行，但收益/成本比极高）**：`CONCIERGE_EXPERT_ANGLES` 从 2 调到 4，看 p50/p95 与 `total_llm_calls` 的涨幅 → 直接得到级联延迟曲线的斜率，给"第一版不做级联"提供自己的实测依据，而不是引用别人的推论。

---

## 六、给另外两位同行的提醒

**给产品经理的一条：延迟预算是一个固定的盘子，不是每一项都能"并行掉"。**
`ARCHITECTURE-v6.md:146-148` 说得对——情绪检测和假前提检测必须并行，串行会把首字推到 1.6–2.3s。但**并行只是把"串行等待"换成了"对上游并发数的需求"**，它并没有变出时间：4 vCPU 的单机上，分类器、情绪、假前提、记忆粗查、按句投递的额外 0.25–0.35s、说话人判定 1.5–2.5s，全都在同一个 1.5s 首字预算里分钱；而上游的在途请求数一旦堆起来，**429 会被框架默认的 5 次重试放大成所有人的首字一起崩**（那就是 REVIEW-eng 炸点 4，唯一一个会从"一个用户的问题"变成"所有用户的问题"的炸点）。所以你要盯的不是"这个功能体验好不好"，而是**"它从首字预算里拿走了多少毫秒，以及它失败时是变慢还是变成事故"**。按句投递就是最好的例子：它从预算里拿走 250–350ms，换的是"永不露半句"——**这是一个需要你拍板的产品指标变更（首字 p50 从 ~0.9s 变成 ~1.15s），不是实现细节。**

**给架构师的一条：你设计的抽象边界和宿主的物理边界不是一回事，而物理边界有三条是你绕不过去的。**
① **跨 agent 缓存 0% 是物理事实**（且父子之间必然 0%，REVIEW-eng E9），所以任何"父等子"的节点都是全价冷启动——**你的协调/融合/汇总角色必须是最便宜的那个**，而 v6 里最贵最慢的融合节点恰恰决定了用户何时听到结论。② **只有能被安全中止的角色才配拥有"开口权"**：接收层被中止是它的常态而不是异常，所以角色应该按"**能否干净地停下来**"来划分，而不是按职责划分——这一条比"谁说什么"更硬，按它重划会得到和我 §2.1 一样的分层（短句 + 单元投递 / 长文 + 逐字流式）。③ **"用户看到的"和"模型看到的"是两份不同的文本，而架构里没有任何一处指定谁是权威**（宿主的中止消息含没投递的尾巴、客户端只显示收到了的部分、`surfaceOp: replace` 又让模型可见面可以偏离人类转写面）。这个洞不补，**所有"承接/不重复/锚点校验"都会建在流沙上**。

---

## 结论（5 行以内）

1. **我推荐：接收层用 B（按单元投递，25 字上限 + 600ms 兜底 + 服务端缓冲 + 客户端打字机回放与中止对齐），反馈层用 A（逐字流式）**；若只能全局选一个，**选 B**——优先消除不可逆的失败，而不是优化可优化的指标。
2. **三方最可能达成一致的三件事**：① 说话人识别第一版只做自用 VAD（不录他人、无合规成本）；② 级联第一版不做（延迟是负收益、父子前缀必 0 命中）；③ 先落"投递水位 + 三份文本一致性断言 + 每轮 trace"这三件基础设施，再谈无缝。
3. **我最坚持的一件事**：**在写下任何提示词之前，先把"用户实际看到的文本"变成一个单一、可断言、可续传的真源**——没有它，无缝衔接、不重复、锚点校验、断线重连、露馅防御全部是空中楼阁。
