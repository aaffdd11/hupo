# 评审报告：个人 AI 助理 v5 架构（10 个决策点）

> 评审人视角：15 年产品/APP 开发 + 技术负责人。目标是**上线**，不是把架构画得更漂亮。
> 评审对象：`ARCHITECTURE-v5.md`（权威）、`ARCHITECTURE-v4.md`、`ARCHITECTURE-v3.md`、`README.md`（v1 实测）。
> 评审时间：2026-09。所有判决基于文档里写出来的**实测数字**，不基于设计者自己写的"必然""妙处"。

---

## 0. 先说我读到的三个前提问题（不解决，下面 10 条都白判）

### 0.1 决策 3/4/5/6 在任何一份文档里都不存在

`grep 40% / 400K / 8-12 / 热品类 / 尾部追加 / 前缀` 在 `ARCHITECTURE*.md` 和 `README.md` 里**零命中**。
前缀缓存的实测数据（18.5% vs 95.6%、3383 vs 184 tokens）也**没有落盘**，只存在于口头/对话里。

这意味着：`ARCHITECTURE-v5.md` 自称"当前唯一权威版本"，但它比真实决策状态**落后了至少 4 条**。
一个 331 行、明确声明"合并定稿、唯一权威"的文档，一周内就被口头决策超出，这本身就是流程问题。
**上线前必须解决的元问题：决策只认落盘的文档。** 否则三个月后没人知道为什么要 40%。

### 0.2 "控制器是结构必然"是一个偷换概念的论证

v3 的推导链是：

> 接收层不能分发 → 分发必须在接收层之外发生 → 所以必然存在一个**在它之外看着它的进程** → 这就是控制器 → 控制器不是可选组件，是架构能成立的前提。

前半段对，后半段是跳跃。**"接收层之外有个东西决定分发"推出的是一个分发器（dispatcher），不是一个带 ETA、心跳续话、停滞检测、降级评估、记忆独占写权的有状态控制器。**
前者是宿主的事件循环 + 一个路由函数（<200 行）；后者是 v5 组件表里的 12 个组件。

这个跳跃的代价很实在：它把"控制器"从一个**可以砍到最小**的组件，论证成了**不可挑战的地基**，于是所有挂在它上面的机制（ETA/续话/禁区表/升格管理器）都顺势获得了"结构性必要"的光环。**v5 §② 那句"其中 4 号是结构必然"建议直接删掉。**

### 0.3 "9 倍延迟差"是真的，但它证明的是一个更弱的结论

README 实测：Reflex 0.8s vs Cortex 7.9s，~9x。**这是全项目最扎实的数据，我完全认可。**
但它证明的是：**"快速确认"和"深度求解"应当分开。**
它没有证明：**这两个东西必须是两个不同会话、不同 persona 的两个 Agent，中间还要一个控制器做接管判决。**

README 第 144 行已经把反证写在墙上了：

> 折中：Reflex 只提供**话术与时机**，仍由 Front agent 用自己的口吻说出。好处是"一个声音"，坏处是快层受主 agent 首 token 速度限制。

也就是 **v1 已经跑通了"同一个 agent 先确认、再被 steer 说出结论"这条路**——零额外会话、零缓存分裂、天然一个声音。
v5 换成了两个/三个独立 Agent 会话，多出来的部分是"承接质量"的理论收益，**而这部分恰恰没有任何实测支撑**。
我下面会在决策 2 和 MVP 里反复回到这一点。

---

## 需要我明确声明的事实假设

你说不知道的事实要说"需要假设"。以下是我全文的假设，**如果任一条不成立，请把对应的判决驳回**：

| 项 | 我的假设 | 依据 |
|---|---|---|
| 用户量 | 1 个真人（你自己）+ 最多 10–20 个朋友；不是公开产品 | 项目形态（本机 credentials、单机 JSONL、无鉴权设计） |
| 团队 | 1 人，每周投入 10–20 小时 | 文档全部由一人撰写、无分工痕迹 |
| 预算 | 月 $50–200 量级，**没有"烧得起"的余量** | 单用户自用项目，成本敏感 |
| "上线"的定义 | 自己每天真的用、朋友能用、能连续跑 30 天不炸 | 后面 MVP 的成功判据按这个定义设计 |
| 模型上下文窗口 | 未知。`40% = 400K` 反推出约 1M 窗口，**这个前提必须去核** | 决策 3 的口径 |
| 合规 | 无。个人数据、本机存储、无第三方数据出境要求 | 若给朋友用，需重新评估 |
| 延迟预算 | 首字 <1.5s 是硬需求（这是整个架构存在的理由） | README 实测目标 |

---

# 第一部分 · 逐条裁定

## 决策 1：反馈层与接收层共用同一套 persona

**裁定：定错了。应改成"共用同一套【风格块】+ 各自独立的【角色指令块】"。**

**理由（可执行依据）：**

字面执行这条会产生一个**确定的 bug**：v3 §2.4 把三条硬约束写进了接收层 persona——"只说自己正在做什么，**不说结论**"。
如果反馈层共用这同一套 persona，它继承的第一条就是"不说结论"。
而反馈层的唯一职责是**说出结论**。**你等于给唯一该说话的角色下了封口令。**

这不是理论风险，是照文档抄就会踩的坑。

第二，说"共用 persona 是为了保一个声音"——但这个理由**在缓存上不成立**，而缓存是你唯一量化的成本杠杆。
你自己实测过：**缓存不跨 agent 共享，同一段内容换个前缀命中 0**。
所以两个 agent 靠"persona 相同"拿不到任何缓存收益。真正让反馈层"像同一个人"的，是 v3 已经设计好的两样东西：
① 承接包里带**接收层已输出全文**；② 承接包里带**最近 3 句的语感**。
**有这两样就够"一个声音"了，跟 persona 是否同一份文件无关。**

第三，真正该共用的东西是"**怎么说**"（语气、长度偏好、禁用词、用户档案里的 verbosity），
真正必须分开的是"**说什么**"（接收层：只报进度不报结论；反馈层：报结论、承接、不重复）。这两者本来就是两个 prompt 段。

**最小可行版本：**
- 一个 `voice.md` 共享段（语气/长度/称呼/禁用词/用户偏好注入点），两个 agent 都加载，内容逐字节相同（顺便让它成为稳定前缀，对缓存友好）。
- 两个各自独立的 `role-receiving.md` / `role-feedback.md`，各 15 行以内，只写职责与禁区。
- 加一条单元测试：**断言接收层 persona 含"不得给出结论"、反馈层 persona 不含该条，且含"必须给出结论"。** 5 行代码，永久防复发。

**判断依据：** 这条想解决的是**想象出来的问题**（"用户会觉得是两个人"——没有任何实测、没有任何用户反馈支撑）。
但它**会制造一个实测得到的真问题**（反馈层被封口）。方向对、口径错。

---

## 决策 2：处理层按类别使用多个 agent（分类器只分发、不扇出、一次一个）

**裁定：现在不做（过度设计）。**

**理由：**

先肯定对的部分：**"不扇出、一次只唤醒一个"是对的**，扇出会让延迟和成本同时爆掉，这条纪律保持。

但"按类别用多个 agent"这件事，**零实测支撑，而且和 v1 的实测结论方向相反**。

v1 的数据说的是另一回事：
- 第三次问同类问题（改述版）**1.5s，0 位专家，经验直出**；
- 整条链的性能提升来自**经验复用 + 品类树**（分类→召回→提炼换快模型），**没有一处来自"某类别有自己的 agent"**。

也就是说：**你已经用"经验包"拿到了 90% 的收益。** v4 §3.2 自己把"经验包 → 专属 Agent"类比成"视图 → 物化视图"——那就按数据库的常识判断：**没有查询压力的时候不做物化视图。**

再看这条的**代价**（这些是确定要付的）：
1. N 个独立会话 = N 份独立上下文 = N 份**互不共享的缓存**（你实测过跨 agent 命中 0）。
   每个品类 agent 的第一轮都是**全量冷启动**，处理层 p50 从 6.5s 往上走——**和你的核心指标直接对撞**。
2. 路由面从"1 个脑子"变成"N 个脑子 + 一个分类器 + 一张品类树 + 一套升格状态机"。
   v1 踩的 5 个坑里，**2 个（经验复用链整条失效、沉淀路径与召回路径不一致）纯粹是分类路径不匹配造成的**。
   每加一个 agent 就再加一条路径，**你是在主动扩大自己已经栽过跟头的那类 bug 的面积**。
3. 上下文隔离的收益（不互相污染）在**每轮只唤醒一个**的前提下约等于零——反正一次只有一个在跑，串行复用同一个会话不会污染。
4. 运维：N 个会话的生命周期、冷启动、失败重试、状态回收，全部要写。

**关键反驳：** 支持这条的唯一硬论据是"不同品类需要不同的**工具/权限**"。
文档里没有任何地方说某个品类需要专属工具。**在出现第一个"这个品类必须调一个别的品类调不了的 API"之前，这条没有存在的理由。**

**最小可行版本（如果一定要做点什么）：**
不要按**品类**分池，按**并发**分池。真正会咬人的场景不是"品类不专精"，而是
**用户在 Q1 深研期间又问了 Q2，而处理层只有一个会话 → Q2 排队 → 反馈层无话可说。**
所以：
- MVP：**1 个处理会话**（品类只影响 prompt 组装 + 经验包检索，不影响选谁干活）。
- 加一个**按负载的会话池（上限 2）**：处理会话在忙且新消息到达 → 起第二个通用处理会话，任务结束即回收。
- 提前定死一条**回收规则**：同一会话连续 N 轮无任务 → 释放。避免 v4 §3.1 担心的"300 个僵尸会话"，只不过在 MVP 里是 2 个。

**判断依据：** **想象出来的问题。**
"用户会问 300 个不同品类、每个品类都值得一个专属 agent"——v1 的品类树只长出 55 个叶子，
而且真实使用中**绝大部分问题应该落在少数几个大类里**。这是典型的"先建一个能装 300 个东西的柜子，再去找 300 个东西"。

---

## 决策 3：软阈值 40%（400K tokens）触发上下文整块重建 / 压缩

**裁定：定错了，而且这是 10 条里错得最贵的一条。应改成"按角色分预算 + 只追加 + 只在窗口边界截断"。**

**理由：这条把"成本问题"和"窗口问题"搞混了，然后用一个动作同时把两边都搞坏。**

**(a) 400K 这个数字本身没有依据。** 它反推出模型窗口约 1M。**如果窗口是 128K，40% 就是 51K，整条设计的前提全变。**
先确认窗口大小，再谈阈值。

**(b) "整块重建"= 主动把自己最便宜的杠杆砸了。**
你的实测：命中缓存 $0.006/M，未命中 $0.300/M，**50×**。
一次 400K 的整块重建 = 400K 全部按未命中算 = **$0.12 一次调用**；而稳态命中时 400K 的成本是 **$0.0024**。
**一次重建 = 50 次稳态调用的钱。** 更糟的是重建之后缓存要重新预热，接下来每轮都在部分未命中区间爬坡。

**(c) 更致命的是：重建会挑在"最不该慢"的时刻发生。**
上下文到 40% 一定是**长会话的中段**，也就是用户正在连续提问、对延迟最敏感的时候。
一次 400K 的 prefill + 400K（约 1.6MB JSON）的上传，在你的接口上至少是**秒级**的额外延迟。
**你的接收层首字目标是 0.9–1.5s。这个动作会让它当场破功。**

**(d) 你根本不需要重建，因为——旧的上下文留在前缀里几乎不要钱。**
这是被漏掉的关键算术：**缓存命中的 token 是 $0.006/M，是未命中的 1/50。**
所以"保留旧内容"的成本极低，真正的约束**只有模型窗口**。
结论：**只要不撞窗口，就永远不要动前缀。** 撞了窗口，截断也应该发生在**轮的边界**，且只截最老的一段，而不是"整块重建"。

**(e) 还有一个被整份文档忽略的成本项：上传带宽。**
所有 4 个 agent 每轮都要把各自的整份上下文 POST 上去。400K tokens ≈ 1.6MB。
**这是每轮、每 agent 的固定开销，跟缓存命中与否无关。** 接收层要 0.9s 出首字，就不可能背着 400K 的包。
**v5 让四个角色共享同一套记忆库和同一套上下文预算——这是设计错误：接收层和处理层的上下文预算应该差一到两个数量级。**

**最小可行版本（可直接落成代码）：**
1. **按角色定预算**（这是本条的真正修正）：
   - 接收层：**≤ 8K tokens，硬上限**。只装：风格块 + 角色块 + 本轮记忆简报 + 最近 3–5 轮 + 当前用户话。**永不压缩、永不摘要**，超了就丢最老的轮次。
   - 反馈层：**≤ 16K**。额外装：接收层已输出全文 + 结论 + 承接点。
   - 处理层：**≤ 窗口的 50%**，允许大。
2. **唯一的上下文装配入口**（一个函数，一个文件），任何人都不能绕过它拼 prompt。
3. **只追加**：所有新内容（记忆简报、摘要、结论）作为**新的尾部块**push 进去，**永不修改已有块**。
4. **要压缩时，压缩结果是"追加一条新的摘要块"，而不是"改写旧块"。** 旧块留着，它们已经命中缓存，几乎免费。
5. **只有在撞窗口时才截断**，且：只在轮边界执行、只截最老的一段、执行前先打日志。
6. **每轮记录 `cache_hit_tokens / miss_tokens / 成本`**（见第二部分）。
7. **两个必须防的隐性缓存杀手**：① prompt 里注入的**时间戳/动态状态**（每次调用都变 → 永远 0 命中）；② agent 框架对历史消息做的**重新序列化/normalize**（哪怕字段顺序变一点，前缀就废了）。**这两个比"记忆插在哪里"更容易吃掉你的 18 倍，必须专门写测试盯住。**

**判断依据：** **没有实测支撑的预防性优化，且前提可能不成立。**
"会话会长到 400K"是一个纯假设：8–12 轮原文 + 摘要的个人助理会话，量级是 **5K–20K tokens**。
要自然长到 400K，需要同一会话里塞进几百轮或超长粘贴。**你正在为一个可能永远不会出现的工况，设计一个会破坏现有性能的机制。**

---

## 决策 4：原文保留 8–12 轮，其余压成摘要

**裁定：可以做但可延后。而且口径要改：按 token 预算，不按轮数。**

**理由：**

**"轮数"是坏代理指标。** 一轮可以是"帮我看看这段日志"+ 3000 行粘贴。12 轮 × 平均 200 tokens ≠ 12 轮 × 有一个 10K 的。
**用轮数当阈值，等于把变量交给了用户的手速和粘贴习惯。** 改成"**保留最近 N tokens 的原文**"（接收层 3K / 处理层 20K 之类），
或者更简单：**轮数下限 + token 上限双约束，谁先到就听谁的**。

**"压成摘要"这件事本身不是免费的**，它是一个额外 LLM 调用。好消息是你自己的实测支持它很便宜：
提炼阶段换 `deepseek-flash` + `effort=none`，单次 ~1.3s，p50 12.0s → 6.5s，降级率 100% → 0%。
**所以"摘要用快模型"这条是有实测背书的，可以直接复用。** 但要注意它仍然是**每轮多一次调用 × 4 个 agent**的成本。

**最小可行版本（按角色分开，这是我建议的核心简化）：**
- **接收层：根本不摘要，只滚动丢弃。** 它不需要历史，它需要的是**记忆简报**（v4 §2.2 已经设计好了）。
  一个只需要"对这个人的了解"的角色，你给它历史摘要纯属浪费。**这条能直接砍掉 1/3 的摘要调用。**
- **反馈层：不摘要。** 它的输入是"接收层刚说了什么 + 处理层的结论"，本来就都是当轮的。
- **处理层：唯一需要摘要的角色。** 用 flash/none，摘要作为**新的尾部块追加**（见决策 3），旧块保留。

**判断依据：** **半个已实测问题（提炼用快模型 = 实测）、半个想象问题（8–12 这个数字没有任何耗时/成本数据支撑）。**
延迟成本低，所以"可以做"；但**对接收层和反馈层做摘要属于纯粹的浪费，应该直接砍掉。**

---

## 决策 5：记忆/摘要一律尾部追加，绝不改写前缀

**裁定：必须做。这是 10 条里证据最硬的一条，也是唯一一条应该升级成"代码级不变量"而不只是"策略"的。**

**理由：**

实测数据是全项目最干净的因果链：插到最前 18.5% 命中 / 3383 未命中；追加到尾部 95.6% 命中 / 184 未命中；**代价差 18 倍**。
机制清楚（前缀缓存的物理性质），方向明确（尾部追加），**没有可争论的余地**。

但要修正三处**执行口径**，否则"决定了"和"生效了"是两回事：

**(a) 把它做成不变量，而不是一条设计原则。**
"绝不改写前缀"必须落到：① 上下文装配只有一个入口函数；② 该函数**只接受 append 操作，不暴露任何修改/替换 API**；
③ 一个单元测试：构造 20 轮对话，断言第 K 轮序列化后的 prompt **前 K-1 轮的字节完全不变**。**没有这个测试，这条三周后必然被某个新功能悄悄破坏。**

**(b) 承认绝对金额现在很小——做它的理由是"防止悬崖"，不是"省一大笔"。**
18 倍听起来震撼，但绝对差是 3383−184 ≈ 3200 tokens ≈ **$0.00096/次调用**。
按每天 1000 次调用算是 **$1/天**。**不是靠这条省出商业模式。**
真正的价值是：① 它是**免费的**（追加和删除的实现难度一样）；② 当上下文从 3K 长到 100K 时，未命中成本从 $0.001 变成 $0.03 而且**分布是突变的**（一个 bug 就能让命中率归零）。**这是防悬崖，不是抠小钱。**

**(c) 真正会吃掉这 18 倍的，大概率不是"记忆插在哪"，而是这三件事——文档里一句没提：**
1. **prompt 里的动态字段**（当前时间、"今天是几号"、随机的 session 状态）→ 每次调用前缀都不同 → **永久 0 命中**。
2. **框架对历史消息的重新序列化**（消息对象字段顺序、tool_call 格式归一化）→ 字节级前缀被破坏。
3. **把所有 agent 的上下文混装**——你已实测**缓存不跨 agent 共享**，所以**每一个 agent 都要独立维护自己的稳定前缀**，
   而"给某个 agent 加一句更聪明的系统提示"这类改动，会让**那个 agent 的缓存全部作废**。改动要收敛到版本切换，不要随时改。

**最小可行版本：**
- 上下文装配单一入口 + append-only + 上述单元测试（约半天）。
- **每轮落一条指标：`hit_tokens / miss_tokens / 该轮成本 / 所属角色`。**
  **命中率 <80% 就报警。** 没有这个数字，你既不知道这条是否生效，也永远调不了参——这是唯一能让这条从"信仰"变成"工程"的东西。
- 禁止在稳定前缀区放时间戳；时间放到尾部（或干脆不给模型）。

**判断依据：** **已实测问题，且是对的方向。**
补一句不客气的：**你只有一个成本杠杆的实测数据，这条是它唯一的落地物。**
相比之下决策 3 会把这个杠杆反向砸掉——**两条放在一起看是自相矛盾的：一边用 18 倍的数据说要稳定前缀，一边每 40% 就把前缀整块重建一次。**

---

## 决策 6：热品类池上限 3–5 个常驻 agent，其余降级为经验包，被问 N 次后升格

**裁定：现在不做（过度设计）。这条是决策 2 和 9 的附属品，砍了 2 和 9，它自动消失。**

**理由：**

**(a) 它依赖三个尚未验证的量，每一个都不存在：**
① 品类访问频次分布（"哪些品类会热"）——你只有 55 个叶子的原型，**没有真实使用数据**；
② "N 次"里的 N——没有任何数据支撑；
③ 常驻 agent 的单位收益——**没有任何测量表明"专属 agent 比经验包答得更好"**。

**(b) 成本方向是反的。** "常驻"意味着会话常开 = 上下文常驻 = **缓存要一直保温**。
而你实测过**缓存不跨 agent 共享、跨会话更不共享**。3–5 个常驻 agent = 3–5 份要独立维护的稳定前缀 + 3–5 份冷启动风险。
**在一个"要压首字延迟"的系统里，主动增加常驻的、各自冷启动的、互不共享缓存的会话，是自伤。**

**(c) 收益侧的证据在反方向。** v1 实测：**重复问题 0 位专家、1.5s 直出**，靠的是经验检索，不是专属 agent。
**经验包已经是那个"便宜覆盖全部"的方案了。** 你要做的是**把经验包做好（检索质量、路径归一化、相似度闸门）**，
而不是在它还只有 55 个叶子的样本量上，去建它的"物化视图"。

**(d) 复杂度直接落在你最脆弱的地方。** 升格状态机（experience-pack → agent → frozen）、
父子经验继承、降级冻结、常驻池的回收策略——**全都要状态、要持久化、要测试，而它们只管一个还没发生的场景。**

**最小可行版本：** 没有最小可行版本。**这条不存在"砍小一点"的版本，因为它整体就不该现在存在。**
如果一定要留个口子：在品类节点里**保留 `agentState` 字段并恒为 `"experience-pack"`**（成本 0），
把"要不要升格"变成**一个每月看一次的人工判断**，而不是一个自动系统。

**判断依据：** **想象出来的问题，且是双重推导的（依赖 2 和 9 成立）。**
真实优先级排序里，这条排在"把经验检索的召回率从当前水平提上去"之后**很远**。

---

## 决策 7：硬打断默认关闭，只在"结论有时效性 且 接收层还很久"时启用

**裁定：必须做（判断正确）。但 MVP 建议更激进：连软打断都别做。**

**理由：**

**(a) 判断本身正确，而且依据是你自己测出来的。**
v3/v5 反复引用的那条反直觉结论——**处理层 ~0.9s，常常比接收层首字 0.8–1.5s 还快**——直接说明：
**绝大多数轮次根本走不到"要不要打断"，走的是"接收层刚开口、结论就到了"。**
硬打断默认关闭是对的：它是**不可逆**的（宿主固化成 `interrupted: true` 的半截消息），
成本高、收益场景窄，**默认关闭这个动作本身零成本**。

**(b) 但 v5 §③ 自己的时序例子暴露了更值得砍的东西。**
那条旅程里，`t≈900ms 处理层出结论` → `t≈1.3s 软打断` → `t≈1.5s 反馈层接管`。
**处理层在 900ms 就有了完整结论，接收层在 1200ms 才说出第一句"这个我熟，你上次也是这个接口"。**
那么理性问一句：**用户从这 900ms 的"接收层先说话"里得到了什么？**
答案是"在场感"。而这个"在场感"的代价是：**一次额外的大模型调用、一套 ETA/续话/打断/禁区子系统、以及一句随后被反馈层接管的话。**

**这是整个架构最该被严肃质疑的地方，而文档里没有任何一段在质疑它——它只被反复称为"妙处"。**

**(c) MVP 的正确做法：**
**先只实现一条路：处理层就绪 → 若接收层在流式输出 → `steer` 让它收尾 → 反馈层接管；若接收层已停 → 直接接管。**
软/硬打断的区分、ETA 三档、禁区表，**全部推迟到有真实数据说明"用户干等超过 X 秒"之后再谈**。
理由：v1 的 `hybrid`（跑着就 steer、调工具就 inject、空闲就 followup）**已经跑通了这条路，是已验证的代码**；
而 ETA/续话那一整套是**未验证的新代码**。**用已验证的，砍掉未验证的。**

**最小可行版本：** 保留 `agent.cancel()` 的**能力**（宿主接口现成），**但不接任何自动判决逻辑**，
只在用户显式说"别想了"时由人触发（v3 §四 已经有这个场景）。

**判断依据：** **已实测问题（处理层常先到）→ 结论正确。**
但我要指出：**这条的正确性恰恰削弱了它周围那一大套机制的紧迫性。**

---

## 决策 8：处理层不能反向要求更多信息（接收层保持单向）

**裁定：必须做。而且这条的"最小可行版本"是免费的九个字：把反问放进结论里。**

**理由：**

**(a) 双向通道的复杂度是真实的，不是虚的。**
如果处理层能反向要信息，就出现：处理层 → 控制器 → 接收层 → 用户 → 接收层 → 控制器 → 处理层 的闭环。
这会**直接和 v3 §3.3 的打断禁区第 3 条对撞**："用户刚发新消息 1.5s 内"是禁区——而反向提问的本质就是**逼用户立刻说话**。
同时它把一个**单向数据流**变成**双向状态机**：处理层要能被"暂停等输入"、"从暂停恢复"、"输入到达时任务是否还有效"。
**这些全是真实的并发状态，要写对但不产生任何用户可见价值。**

**(b) 但不做双向 ≠ 不允许澄清。澄清是免费的。**
处理层的结论结构里**本来就有一栏"下一步动作"**（v3 §3.4 承接包：`{ 一句话, 要点≤3, 下一步动作, 置信度 }`）。
**"信息不足"就是结论本身的一种，澄清问题就写在"下一步动作"里，由反馈层说出来问用户。**
用户体验上完全一样（用户被问了问题），架构上零新增状态。
**这是我在整份评审里最推荐的一个"几乎免费的降维"：把协议问题降级成内容问题。**

**(c) 唯一的真实缺口：** 用户回答之后，新的一轮**必须能把上一轮的结论接上**。
这需要处理层会话能拿到"上一轮我问了什么、用户答了什么"——**这靠尾部追加新轮次就能做到**，不需要任何暂停/恢复机制。

**最小可行版本：**
- 结论结构里固定加一个字段：`澄清请求: string | null`。
- 反馈层规则：该字段非空 → 以问句收尾。
- 处理层 persona 一行禁令：**不得请求等待用户输入；信息不足时，直接给出"当前最佳判断 + 一个澄清问题"。**
- **不建**暂停/恢复；每轮都是完整独立任务。

**判断依据：** **半个真问题。**
"处理层遇到信息不足"是真的；"所以要建双向通道"是想象出来的。
**v3 §六.3 把这个问题提出来问，是对的；答案是"用内容层解决，不碰协议层"。**

---

## 决策 9：升格为专属 agent 采用"提议 + 人工确认"，不全自动

**裁定：现在不做（过度设计）。它是决策 6 的附属品。**

**理由：**

**(a) 不自动是对的，但"提议 + 人工确认"这个折中方案是最差的选择——它付了自动化的全部成本，只拿到一半的收益。**
"提议"意味着要：判定逻辑 + 状态机 + 阈值配置 + **一个能承载提议的 UI/通知渠道** + 确认回执 + 确认后的建 agent 流程。
"人工确认"意味着**这条链路的价值完全取决于你每周会不会去看那个提议**。
对一个 1 人团队、10–20 小时/周的项目，正确答案是**根本没有提议队列**：
**你想给某个品类建专属 agent，就去配置文件里加一行。** 成本 5 分钟，价值等价。

**(b) 更本质的：连"要不要有专属 agent"都还没被验证（决策 2 已裁定不做），
所以"如何升格"是在给一个不存在的功能设计管理界面。**

**(c) 阈值本身也是编的。** v4 §3.3 给的"30 天 ≥ 8 次""纠正率 ≥ 0.3""p50 超全局 2 倍"——三个数字，
**没有一个是实测出来的**，全是"建议，可调"。**用猜出来的阈值驱动一个自动状态机，比纯人工更容易出错，因为它还会自己偷偷做决定。**

**最小可行版本：** 无。**保留一个字段（`agentState` 恒为 experience-pack）+ 在文档里写清楚"人工判断"就够了。**
真正该做的是**一个只读的品类统计页**（哪些品类被问了多少次、纠正率多少），**给人看，不给机器用**，成本极低。

**判断依据：** **想象出来的问题，双重依赖。** 而且它是"为了一个没有被验证的假设，提前建管理后台"的典型。

---

## 决策 10：跨用户经验不共享（方法论共享、个人偏好隔离）

**裁定：必须做——但要做的是【隔离】，不是【共享】。共享那一层现在不做。**

**理由：**

**(a) 你提出的"分两层"听起来很合理，实际是把一个不存在的需求和一个真实的安全需求混在一起。**
- **"通用方法论共享"**：在 1 个真人 + 少量朋友的规模下，**共享池里不会有足够的样本量产生任何价值**。
  "接口故障排查的通法"——**你自己的记忆库里那几条经验，本来就足够覆盖**。
  跨用户共享要的是：全局经验库 + 隐私审核 + 归属追踪 + 冷启动合并策略。**成本很高，收益在 20 个用户时接近于 0。**
  **这是典型的"先建平台，再找用户"。**
- **"个人偏好严格隔离"**：这不是一个产品取舍，**这是一个正确性/安全要求**——它必须在 MVP 第一天就成立。

**(b) 而且隔离是**低成本前置**，不是后置补丁。**
在一个 per-user 记忆库里，隔离的正确实现是：
**每个存储/检索 API 的第一参数永远是 `userId`，所有检索在存储层强制带 userId 过滤，任何不带 userId 的读接口不允许存在。**
这条**今天写进去是 10 行代码；等出过一次"A 的记忆出现在 B 的对话里"再补，就是一次数据事故 + 一次全量数据迁移。**

**(c) 需要说清楚的现实：** 按我的假设（1 个主用户 + 少量朋友），**跨用户共享今天是一个纯假设问题**，
但你**不能因此不做隔离**——因为**一旦有第二个用户，隔离就从"优化项"变成"事故项"，且不可逆**。

**最小可行版本：**
- `userId` 作为**强制分区键**：单机目录 `data/<userId>/…`，检索函数签名 `search(userId, query)`，**没有无 userId 的版本**。
- **不做**全局经验库、不做共享合并、不做方法论分级。
- 加一条测试：以 u1 写入、以 u2 检索，**断言返回空**。5 行代码。

**判断依据：** **隔离部分 = 已实测问题（v1 踩过"经验张冠李戴"，隔离是同一类错误的边界版）；**
**共享部分 = 想象出来的问题。**

---

# 第二部分 · 你漏掉的：不上线就卡脖子的东西

按"不做就上不了线"的紧迫度排序。我只列真正会**让服务挂、让钱烧光、让数据丢掉**的，不列"最好有"的。

## 🔴 1. 成本熔断与上游限流退避（唯一一条"不做会破产"的）

**文档里对成本的讨论只有一句**（v1 ARCHITECTURE.md 第 149 行："成本约为朴素对话的 3–8 倍"），
**然后 v5 整份文档没有任何一处讲"钱花完了怎么办"。这是最大的空白。**

现实风险是具体的：
- 一次用户提问 = 1 次接收层 + 1 次分类 + N 次专家 + 1 次提炼 + 1 次反馈 = **3–8 次上游调用**；
- 如果处理层进入**工具调用循环**（agent 反复调用工具自我纠错），**单轮可以无上限地烧**；
- 上游会返回 **429 / 5xx**，而你现在没有任何退避、队列或重试策略；
- **一个死循环的 bug 挂一夜，可以烧掉一个月的预算。** 这不是理论风险，是 agent 类系统的标准事故。

**最小可行版本（半天到一天）：**
1. **每轮硬上限**：单轮最多 N 次上游调用（建议 12）+ 硬超时（v1 已有 `CORTEX_DEADLINE_MS=25000`，复用它）。
2. **每日熔断**：累计成本 > 阈值（比如 $5/天）→ 处理层降级为"直接回答"，只保留接收层 + 单次模型调用；
   > 上线（比如 $15/天）→ 拒绝新任务并**如实告诉用户**（不要静默降级成傻回答，那比报错更伤信任）。
3. **429/5xx 退避**：指数退避 + 抖动，最多 3 次；仍失败 → 走 v5 已设计的降级兜底话术。
4. **成本记账**：每轮落一行（tokens in/out、命中/未命中、金额）。**没有这个数，上面三条都是瞎设的。**

## 🔴 2. 轮级可观测性（trace）—— 包括缓存命中率

**v5 有"12 号组件：指标与审计"，但只写了"四指标 + evidence 可追溯"，没有说清"一次出问题你怎么查"。**

v1 已经用血的教训证明了这件事的必要性：**"指标被稀释"这个 bug，是因为纠正率的分母混进了没有用户反馈的轮次。**
**这种错误靠读代码是发现不了的，只能靠指标发现。**

第一部分的每一条裁定都依赖同一句话："**你得能看到真实数字**"——ETA 阈值、续话触发率、缓存命中率、成本/轮、
哪个角色慢、哪个品类老走兜底路径。**没有 trace，上面 10 条决策全是不可验证的信仰。**

**最小可行版本（一天）：**
每轮一个 JSONL，至少含：`userId / turnId / 用户原话 / 各角色耗时 / 各角色 tokens(命中+未命中) / 缓存命中率 /
本轮成本 / 接管路径(steer|inject|followup|none) / 是否降级 / 命中经验id / 用户是否纠正`。
再加 3 个告警：**缓存命中率 <80%、单轮成本 > P95×3、降级率 > 20%**。
**这个文件比 v5 组件表里的 8 个逻辑组件加起来都重要。**

## 🟠 3. 持久化与崩溃恢复（控制器是唯一真源 = 它也是唯一单点）

**v5 明确写了"唯一对话真源 = 控制器自己的记录（不依赖任何 Agent 日志）"。**
这句话在架构上很干净，**但它同时意味着：控制器进程一挂，唯一真源就没了，而且没有任何备份路径。**

必须回答的三个问题（文档里一个都没有）：
1. 进程在 `pending` 提案写了一半时被杀 → 重启后这条提案算什么？
2. 用户正说话时崩溃 → 用户看到什么？会不会重复回答？
3. 记忆库文件写坏了 → 用户几个月的积累怎么办？

**最小可行版本（一天）：**
- 记忆与提案**只走 append-only JSONL**（不做原地修改文件），启动时重放；
- **幂等**：每轮有 `turnId`，提交记忆前检查"这个 turnId 是否已提交过"——**防止崩溃重启后同一轮结论被双写**（写两次 = 经验 hits 虚高 = 决策 6 的升格阈值全部算错）；
- **每日快照**：整个 `data/` 目录打包留 7 份（cron 一行）。
- **导出/导入命令**：用户的记忆是他唯一的长期资产，**必须能一键带走**。

## 🟠 4. 失败的用户可见语义（不只是"超时兜底话术"）

**v5 有"降级兜底：四种终态话术"，这解决的是"处理层失败"。**
没解决的是：上游 429、网络断、密钥过期、模型返回空 content（**v1 README 明确踩过："带思考的档位会产出空 content"**）。

**空 content 是最阴险的**：它不报错，它让 agent 产出**一条空消息**，用户看到"对方什么都没说"。
**这必须在流层做拦截：首字超时 X 秒仍无 chunk → 判定失败 → 走兜底。** 而不是等它自然结束。

**最小可行版本：**
- 每个角色一条"首字看门狗"（接收层 3s / 反馈层 3s / 处理层按 deadline）；
- 一条规则：**任何错误都必须对用户可见地结束**（宁可说"我这轮没答上来，原因是 X，你可以这样做 Y"）；
- **禁止静默重试**（静默重试会重复计费，且用户看到两次回答）。

## 🟠 5. 多用户隔离与鉴权（成本前置，几乎为 0）

见决策 10 的裁定。**MVP 不做登录、不做鉴权**（假设：本机自用），
**但 `userId` 必须是每个存储/检索 API 的强制第一参数，且不允许存在无 userId 的读接口。**
今天 10 行代码，将来省一次数据事故。

## 🟡 6. 密钥不进日志、不进前端

密钥在 `~/.dsh/.credentials.yaml`，请求由本地宿主代理转发——**架构上已经是对的**。
需要的只是纪律 + 一个测试：**任何日志/trace 落盘前，断言不包含 `sk-` 前缀的字符串**。
成本：10 行。**收益：避免一次"把密钥打进 JSONL 又发给朋友调试"的事故。**

## 🟡 7. 配置/提示词的版本化与回滚

你一定会遇到"**昨天还好好的，今天变傻了**"。可能原因：改了 persona（→缓存全废）、换了模型、改了阈值。
**没有版本号，你只能靠记忆猜。** 最小版本：把 persona、模型配置、阈值放**一个带 version 字段的文件**，
每轮 trace 里记下这个 version。**出问题能一眼定位到"改动 7 之后开始变差"。** 半天工作量。

## ⚪ 暂不需要（明确说不做，避免被"完整性"绑架）
灰度发布、A/B 平台、K8s、正式 SLA、法务合规、等保——**在 1 主用户 + 少量朋友的规模下，全部不做。**
真要给朋友用，只补一条：**能一键删除某个用户的全部数据**（合规的最小可行形态）。

---

# 第三部分 · 2 周 MVP

## 3.1 做什么（按天）

| 天 | 交付 | 为什么是这个顺序 |
|---|---|---|
| **D1–D3** | **3 个角色的真 Agent + 最简时序**：用户说话 → 接收层开口 → 处理层结论到 → `steer` 接管。**ETA/续话/打断判决一律不写**，只用超时兜底。 | 先证明"三层真的能在一个进程里跑起来"。v1 已跑通 `hybrid`（跑着 steer/工具 inject/空闲 followup），**直接复用已验证机制，别重写。** |
| **D4–D7** | **记忆最小版**：JSONL 存储（`userId` 强制分区）→ 品类树 → 经验召回（**直接复用 v1 已验证的相似度闸门 + 5 级路径归一化**）→ **结构化记忆简报**（尾部追加） | **这是唯一被实测证明有效的部分**（第 3 次改述 1.5s / 0 位专家）。v1 的 5 个坑有 3 个在这一层，**复用已验证代码比自己重写值钱得多**。 |
| **D8–D10** | **上下文装配单一入口 + append-only 不变量 + 每轮 trace**（命中/未命中/成本/耗时/接管路径） | 决策 5 的落地物 + 第二部分第 2 条。**没有这个，后面全是盲调。** |
| **D11–D12** | **成本熔断 + 429 退避 + 幂等 + 崩溃重放 + 每日快照** | 第二部分第 1、3 条。**上线的门票，不是优化项。** |
| **D13–D14** | **真机连续用 + A/B 对拍**：三层架构 vs 单 agent + 同一记忆简报，同一批真实问题 | **这 2 天的产出决定整个项目要不要继续（见 3.3）。** |

## 3.2 明确不做什么

**从这 10 条里砍掉的：** 决策 2（多品类 agent）、6（热品类池）、9（升格 + 人工确认 UI）**整条链**；
决策 3（40% 重建）；决策 4 对接收层/反馈层的摘要；决策 7 的 ETA 三档 + 软/硬打断判决 + 打断禁区表。

**其他不做：** 升格管理器、一致性守卫（**降级成 persona 里一句"发现自己说错了要显式纠正"**）、
跨用户共享层、鉴权/登录、灰度/回滚平台、指标审计 UI、做假 Agent 的桩、
v5 §⑦ 的 P0–P4 分期表**（它是按"架构完整度"排的，不是按"上线风险"排的，这份排期应该重写）**。

**一个重要的额外建议：MVP 的反馈层先不要独立 Agent。**
用 v1 已验证的写法——**接收层 agent 自己被 `steer` 说出结论**（README 第 144 行的折中方案）。
理由：① 零额外会话；② 天然一个声音；③ **少一份独立缓存前缀**（你已实测缓存不跨 agent 共享）；
④ 承接质量的差异**可以从 D13 的对拍里实测出来**，而不是靠架构论证。
**如果对拍显示"承接明显不自然"，再拆独立反馈层——那时你是有证据地拆，不是按理想要拆。**

## 3.3 MVP 成功判据（全部可测量）

**基线是 v1 的实测值，目标是"不退化 + 拿到架构收益"。**

| 指标 | 目标 | 怎么测 |
|---|---|---|
| 接收层首字 p50 | **≤ 1.5s**（v1: 0.8–0.98s，**这是不能退化的底线**） | trace 每轮 |
| 结论交付 p50 | **≤ 7s**（v1: 6.5s） | trace 每轮 |
| 重复问题（第 3 次改述） | **≤ 2s 且专家调用 = 0**（v1: 1.5s / 0 位） | 20 轮里含 5 轮重复问题 |
| 纠正率 | **≤ v1 基线**（只在有用户反馈的轮次里算——**别再稀释指标**） | trace |
| 缓存命中率 | **≥ 80%**（低于这个数说明前缀被污染，决策 5 没落地） | trace |
| 平均成本/轮 | **≤ 3× 单 agent 基线**（v1 说朴素方案 3–8×，**压到 3× 以内才算架构划算**） | trace |
| 崩溃恢复 | `kill -9` 后重启，**记忆零丢失、零重复提交** | 手工演练 1 次 |
| 成本熔断 | 人为触发一次，**当天真的停止烧钱且用户看到明确提示** | 手工演练 1 次 |
| 隔离 | 以 u1 写入、u2 检索 → **返回空** | 单元测试 |

**外加一条非技术但最重要：**
**A/B 主观偏好 —— 同一批 20 个真实问题，"三层" vs "单 agent + 同一记忆简报"，你自己（或朋友）盲评 1–5 分：
"哪个更像在跟一个记得我的人干活"。**

**判据：三层胜率 ≥ 60% 才继续投入这套架构。**
低于 60% 就说明：**9 倍的延迟差可以通过"单 agent 先给一句确认再流式作答"拿到，而三层带来的复杂度不值这个钱。**

## 3.4 直接回答："有几条是在为未验证的假设提前优化？最该先验证哪个？"

**10 条里有 5 条（决策 2、3、4、6、9）建立在同一个未经检验的假设上：**

> **假设 A："这是一个长会话、多品类、高频次的重度使用场景。"**

决策 3（40%/400K）预设会话会长到几十万 token 并反复触发压缩；
决策 4（8–12 轮 + 摘要）预设会话长到需要压缩；
决策 6（热品类池）预设品类访问频次会呈现明显的长尾；
决策 2、9 预设"品类专精"能带来用户可感知的收益。

**这四条里最脆弱的是决策 3**：8–12 轮原文 + 摘要的个人会话，量级是 5K–20K tokens。
**要长到 400K，需要同一会话几百轮。个人助理的使用形态里，这几乎不会发生。**
**你在为一个大概率不存在的工况设计机制，而这个机制在真发生时又会摧毁你唯一的成本杠杆（前缀缓存）。**

### 但假设 A 不是最该先验证的。

**最该先验证的是这个（我称它假设 B）：**

> **假设 B："用户能感知到【接收层先说一句、反馈层再接管】比【一个 agent 先给一句确认、再自己说出结论】更好。"**

理由很直接：

1. **v1 已经把"单 agent 说完整件事"跑通了**（README 第 144 行：Reflex 只给话术与时机，仍由主 agent 用自己的口吻说出）。
   **也就是说：假设 B 的对照组不用你写，它已经存在，而且能跑。**
2. **假设 B 不成立的话，本项目的核心复杂度全部失去理由**：不需要独立反馈层、不需要接管判决、不需要 ETA/续话/打断禁区、
   不需要跨会话的缓存一致性——**v5 组件表 12 个里能删掉 6 个以上。**
3. **假设 B 有一个致命的先验支持它不成立**：V5 §③ 自己的时序里，**处理层 900ms 就出了完整结论，接收层 1200ms 才说出第一句。**
   那么这 300ms 的"在场感"值不值得一次额外调用 + 一整套控制器机制？**这是全项目最该被量化的问题，文档里一次都没被问过。**
4. **验证成本极低**：D13–D14 那 2 天，同一批 20 个问题，两种实现，盲评。**代价是 2 天，收益是可能省掉 60% 的复杂度。**

**排序（最该先验的在前）：**
**① 假设 B（三层 vs 单 agent 的用户可感知差异）→ ② 假设 A 的一个子集："真实用起来，一轮上下文到底多大、多少次会话会超过 20K tokens"（一天就能测出来）→ ③ 品类频次分布是否长尾（需要真实使用 2–4 周，急也急不来）。**

---

# 第四部分 · 我最想砍掉的三样

## 第一刀：续话子系统（ETA 三档 + 续话话题集合 + 打断禁区表 + 硬打断判决）

**砍什么：** v5 §④.2、§④.3、§④.4 全部，v3 §二、§三全部。相关组件：`进度估算器 ETA`、`心跳续话器 KeepAlive`、以及控制器一半的状态机。

**为什么砍它（不是砍别的）：**
**这是文档里篇幅最大、最精巧、最能体现设计者智力、也最没有数据支撑的部分。**
v1 的实测数据显示它要解决的场景**很少发生**：**处理层 0.9s 常常比接收层首字 0.8–1.5s 还快**。
也就是说，绝大多数轮次里，**根本不存在"用户干等"这个窗口——因为结论比第一句话来得还早。**
为一个小概率场景建了一整套状态机（三档 ETA + 话题耗尽判定 + 禁区查表 + 续话通知结构体），
**而且它的所有阈值（"最近 300ms 速率""回复长度中位数""2T(S)"）都需要先有大量真实数据才能算准——你现在一条都没有。**

**风险：** 处理层真的卡住时，用户会干等（比如 max 25s 的 deadline）。
**缓解（成本很低）：**
1. 复用 v1 的 `CORTEX_DEADLINE_MS=25000` 硬超时 + 一条降级交付话术——**这一条就覆盖了 90% 的卡死场景**；
2. 保留"处理层停滞告警"进 trace（**只观测、不动作**），**用两周真实数据来判断到底该不该做续话**；
3. 如果数据真的显示"每 10 轮就有 1 轮用户干等 >5s"，**再按真实数字设计续话**——那时阈值是量出来的，不是猜的。

## 第二刀：处理层按品类多 agent + 升格管理器（决策 2 + 6 + 9 整条链）

**砍什么：** 品类专属 agent、热品类常驻池、升格/降级状态机、父子经验继承、升格提议 UI。涉及组件 11（升格管理器）。

**为什么砍它：**
**收益零证据，成本全是确定的。**
v1 已经证明**经验复用**能带来"第 3 次问 1.5s / 0 位专家"的效果——**收益来自检索，不来自专属 agent。**
而成本是可数的：N 份互不共享的缓存（跨 agent 命中已被实测为 0）、N 份冷启动、
路由复杂度上升（**v1 五个坑里两个就是路径不匹配**）、一套要持久化的状态机、一个要运营的确认队列。
它还是**双重推导**的：假设 2 成立 → 才需要 6 → 才需要 9。

**风险：** 某些品类确实需要专属工具或长期积累的领域状态，通用 agent 答不好。
**缓解：**
1. **先用数据找出来**：trace 里按品类统计 p50 耗时、纠正率、降级率。**哪个品类真的差，数据会喊出来**（一个只读统计页，半天工作量）；
2. 真出现了，**人工加一个 agent，配置文件一行**——不需要自动升格系统；
3. 更便宜的中间形态：**把"品类专精"做成 prompt 注入 + 经验包检索**（成本≈0），
   实测不够再说。**先证伪，再花钱。**

## 第三刀：40% 上下文整块重建 / 压缩管线（决策 3，连带决策 4 的摘要调用）

**砍什么：** 40% 软阈值、整块重建、以及为"压缩"服务的整套机制。

**为什么砍它（我在第一部分说过，这里再直白一遍）：**
**它和决策 5 直接自相矛盾。**
你刚从实测里学到"前缀稳定 = 18 倍成本差"，转头就设计了一个**每 40% 就把前缀整块抹掉**的机制。
**一次 400K 的重建 = 50 次稳态调用的钱，而且发生在用户最需要速度的长会话中段。**
更根本的是：**旧内容留在前缀里，命中价 $0.006/M，几乎免费——真正的约束是模型窗口，不是钱。**
所以正确的策略是**"永不重写、只追加；只在撞窗口时于轮边界截断最老的一段"**，这件事的代码量**比一个压缩管线还少**。

**风险：** 会话真的长到接近窗口上限时，出现硬失败（超窗报错）或性能陡降。
**缓解：**
1. **加一条告警**：会话 token 数超过窗口 50% → 报警（**你不可能在两周内悄悄越线，会先看到告警**）；
2. **按角色定预算**（接收层 ≤8K 硬上限），**让接收层从结构上不可能进入这个工况**；
3. 真触发时，**先做最笨的："截断最老的一段 + 追加一条摘要块"**，仍然是 append-only；
4. 只有在实测证明"截断+摘要不够用"时，才谈重建。**到那时你有数据，可以正确设计。**

## 如果只能砍一样：砍第一刀（续话子系统）

因为它是**唯一一个"投入产出比确定很差"**的东西：代码量最大、状态最多、阈值全靠猜、要解决的场景被自己的实测数据证明是少数情况。
**第二、三刀砍掉的是"还没长出来的功能"，第一刀砍掉的是"已经写进权威文档的复杂度"——后者更贵。**

---

# 总结

## 10 条裁定汇总

| # | 决策 | 裁定 |
|---|---|---|
| 1 | 反馈层与接收层共用同一套 persona | **定错了** → 共用【风格块】+ 独立【角色指令块】（否则反馈层被封口） |
| 2 | 处理层按类别用多个 agent | **现在不做（过度设计）** → 1 个处理会话 + 按负载的会话池（上限 2） |
| 3 | 40%（400K）触发整块重建/压缩 | **定错了** → 按角色分预算（接收层 ≤8K 硬上限）+ 只追加 + 只在窗口边界截断 |
| 4 | 原文 8–12 轮，其余摘要 | **可以做但可延后** → 改按 token 预算；**接收层/反馈层不做摘要，只滚动丢弃** |
| 5 | 记忆/摘要一律尾部追加 | **必须做** → 升级为代码级不变量 + 命中率指标 + 单元测试 |
| 6 | 热品类池 3–5 个常驻 agent | **现在不做（过度设计）** → 无最小版本，整条不成立 |
| 7 | 硬打断默认关闭 | **必须做（判断正确）** → MVP 更激进：连软打断判决也推迟 |
| 8 | 处理层不能反向要信息 | **必须做** → 澄清降级为结论里的一个字段（免费） |
| 9 | 升格提议 + 人工确认 | **现在不做（过度设计）** → 人工改配置一行 |
| 10 | 跨用户经验不共享 | **必须做（仅隔离部分）** → `userId` 强制分区键；**共享层不做** |

## 数量统计（你要求的）

- **必须做：4 条** —— 第 5、7、8、10 条（其中第 10 条只做"隔离"，共享部分不做）
- **可以做但可延后：1 条** —— 第 4 条
- **应该砍掉（现在不做）：3 条** —— 第 2、6、9 条（且 6、9 是 2 的附属，属连锁砍）
- **定错了、必须改口径：2 条** —— 第 1、3 条

**即：10 条里真正必须做的只有 4 条（40%），有 5 条（第 2、3、4、6、9）在为一个未被验证的假设——"长会话 / 多品类 / 高频次"——提前优化。其中第 3 条最危险，因为它一边承认前缀缓存值 18 倍，一边每 40% 就把它抹掉一次。**

## 最后三句话

1. **你最值钱的东西是那几组实测数字**（9 倍延迟差、提炼换快模型 12.0s→6.5s、缓存 18 倍、3 个真 bug）。
   **v5 把"实测支撑的设计"和"没实测支撑的设计"混在同一份权威文档里，用同样的语气陈述——这是这份文档最大的问题。**
   **建议 v5 每一节强制标注【已实测】/【假设未验证】。标完你会发现，能标【已实测】的大概只有三节。**
2. **你在系统性地为一个"重度、长会话、多品类"的重度用户设计，而你的真实场景是"一个人每天问几个问题"。**
   两周的真机 A/B（假设 B：三层 vs 单 agent + 同一记忆简报）能把这个偏差一次性拉回正轨——**这是我现在最推荐你做的一件事。**
3. **文档里唯一该被质疑但从未被质疑的一句话是**："接收层先说一句、反馈层再接管，比一个 agent 自己说完整件事更好。"
   **它被写了四遍，被夸了三次（"妙处"），一次都没被验证过。而验证它的成本是 2 天。**
