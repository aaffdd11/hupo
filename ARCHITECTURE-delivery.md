# 第三形态：回答的"交付面"（H5 / 动态页面 / 完整应用）

> 本文件补充 v5 架构：**回答不只有"几句话"一种形态**。
> 判断标准不是"内容多少"，而是**这个回答是否需要一次交互**。

---

## 一、为什么要加这一层

现有架构假设"处理层的结论 → 反馈层用几句话说出来"。这覆盖不了三类真实回答：

| 用户的请求 | 几句话够吗 | 实际需要 |
|---|---|---|
| "接口为什么偶发 500" | ✅ 够 | 对话 |
| "帮我把这半年的开销理一理" | ❌ 不够 | **一张能看能点的页面**（按类别看、按月份看、看趋势） |
| "给我做一个记账工具" | ❌ 完全不够 | **一个完整的小应用**（增删改查、本地持久化） |

**关键判断：这不是"内容长度"问题，而是"是否需要交互"问题。**
20 行文字能说清的事不需要页面；但"3 个数 + 1 张图 + 能切换维度"就必须有页面。

---

## 二、四档回答形态（正式定义）

| 档 | 形态 | 载体 | 生成成本 | 典型场景 |
|---|---|---|---|---|
| **T1** | 几句话 | 对话流 | 即时 | 问答、建议、判断 |
| **T2** | 结构化展示块 | **Schema 驱动的卡片**（Flutter 原生渲染） | 即时 | 表格、列表、指标卡、简单图表 |
| **T3** | 动态页面（有层级、可少量交互） | **H5 容器**（WebView + 预置运行时） | 秒级 | 多维筛选、下钻、对比分析 |
| **T4** | 完整小应用 | **H5 容器 + 持久化** | **分钟级（异步任务）** | 工具类：记账、清单、打卡 |

**档位由处理层的分类节点判定**（与难度判定同一处，天然合并）：
```
输出: { 难度: 0|1|2, 交付形态: 'T1'|'T2'|'T3'|'T4' }
```

---

## 三、框架选型（按档位）

### T2 用 Server-Driven UI（SDUI），不要生成 HTML

| 方案 | 说明 | 评价 |
|---|---|---|
| **Stac**（Flutter SDUI） | 用 JSON 描述 Flutter 界面，服务端下发即可渲染 | ✅ **首选 T2**：无代码执行风险、版本可控、离线可缓存 |
| schema_build 类 | JSON → 组件树 | ✅ 同类选择 |
| 自研受控 schema | 自己定义 10–15 个组件（卡片/表格/图表/列表/标签） | ✅ 最可控，工作量可控 |

**T2 绝不生成 HTML。** 理由：T2 是"展示"性质，SDUI 已经够用，且**零代码执行风险**。

### T3 / T4 用 WebView 容器

- **Flutter `webview_flutter`**（双端统一）
- 页面由**服务端生成并托管**，客户端只加载**已白名单的域名**
- T4 需要：本地存储（IndexedDB / localStorage）、与原生桥接（读用户数据、存结果）

**参考框架：[Stac](https://github.com/StacDev/stac)（Flutter SDUI，JSON 驱动界面）**

---

## 四、合规：这是最大的风险，必须按 Apple 规则做

Apple 审核指南 **4.7** 专门管这件事（HTML5 / Mini Apps / Bots），并且 Apple 已推出 **[Mini Apps Partner Program](https://developer.apple.com/news/?id=06212016b#2)**，说明这条路径**是被官方支持的**。

**4.7 的核心要求（必须逐条满足）**：
1. 嵌入的软件（HTML5/JS）**必须使用 Apple 提供的 WebKit 与 JavaScriptCore**（不能用自带 JS 引擎）
2. 不得下载并执行**改变 App 主要功能**的代码
3. 必须提供**年龄分级、隐私政策、内容审核机制**
4. 内购必须走 Apple 通道

**设计落点**：
- 客户端只用系统 WebView（iOS = WKWebView，满足第 1 条）
- 生成页面**不得访问未白名单的域名**（满足第 2 条的可控性）
- 必须做**生成内容的审核与举报入口**（满足第 3 条）
- 生成的东西**不能绕开内购**（若未来有付费）

> 已确认依据：[App Review Guidelines 4.7 讨论](https://developer.apple.com/forums/thread/117933#1)

---

## 五、对现有架构的改动

### 5.1 链路多一条分支

```
用户 → 接收层（照旧，先说话）
         │
    处理层（级联）
         │
    ┌────┴─────────────────────────────┐
    │ 交付形态判定                       │
    ├─ T1 → 反馈层：说出来（照旧）        │
    ├─ T2 → 反馈层说一句 + 附展示块       │
    ├─ T3 → 反馈层说一句 + 开 H5 页面     │
    └─ T4 → 反馈层**承诺时间** + 建异步任务
              │
         任务完成 → 推送通知 + 给用户入口
```

### 5.2 T4 引入了"任务"这个新概念

这是与之前最大的不同：**T4 不是一次响应，是一个异步任务**。因此需要：

| 需要 | 说明 |
|---|---|
| 任务状态机 | 排队 / 生成中 / 自检中 / 待发布 / 已发布 / 失败 |
| 时间承诺 | 反馈层要能说"这个要几分钟"，**且这个估计要靠谱**（否则比不做更糟） |
| 进度可见 | 用户能离开 App，完成后推送 |
| 交付物管理 | 生成的应用要能被找到、再次打开、删除 |
| 版本与废弃 | 用户说"改一下" → 生成 v2，v1 归档 |

### 5.3 生成物需要"页面仓库"

| 数据 | 内容 |
|---|---|
| pageId / appId | 生成物标识 |
| 来源会话 | 哪一轮对话产生的 |
| schema / html | T2 存 schema，T3/T4 存 HTML+资源 |
| 版本 | 每次修改一版，可回滚 |
| 生命周期 | 保留策略（建议 90 天未访问则归档） |
| 权限 | 能读哪些用户数据（必须声明） |

### 5.4 反向输入（这一条容易被忽略，但价值很大）

**生成的页面可以往回反馈信息。** 用户在页面上筛选、点击、下钻 → 这些动作**成为新的输入**，
回流到处理层："我在看 3 月的明细，为什么这类涨了 40%？"

**这让架构从"对话"升级为"对话 + 交付 + 操作闭环"** —— 也是 H5 容器相对纯文本唯一不可替代的价值。
必须在协议里预留：`页面事件 → 控制器 → 处理层` 这条上行通道。

---

## 六、工程含义（新增任务）

| ID | 任务 | 依赖 |
|---|---|---|
| **D21** | 交付形态判定（分类节点扩展：难度 + 形态一起出） | D14 |
| **D22** | T2 的 SDUI 渲染器（受控 schema + 15 个组件） | — |
| **D23** | H5 容器（WebView + 域名白名单 + 运行时注入） | D23 前置：D9 网关 |
| **D24** | 页面仓库（生成物存储、版本、生命周期） | D23 |
| **D25** | T4 异步任务队列 + 状态机 + 时间承诺校验 | D24 |
| **D26** | 上行通道（页面事件 → 处理层） | D23 |
| **D27** | 生成内容审核 + 举报入口（**合规必需**） | D23 |
| **D28** | 完成推送（复用 D19 的 APNs/FCM） | D19, D25 |

---

## 七、我作为 PM 要提的三个风险

### 风险 1：T3/T4 会诱使"什么都生成页面"

页面的诱惑很大，但**对话的价值恰恰是快**。如果每个回答都变成页面，产品就从"助理"退化成"低代码平台"。

**对策——把档位判定的默认值设成 T1**：
> 只有当"**用户必须对结果做一次操作才能得到他想要的**"时，才允许升到 T2 以上。

### 风险 2：T4 的时间承诺是双刃剑

承诺"几分钟"会让用户走开，**但如果超时，信任损失比一开始就说"这个做不了"更大。**

**对策**：时间承诺必须来自**实测分布**（不给乐观值），且**误差超过 50% 就要主动推送更正**。

### 风险 3：生成物是长期的负债

每个生成的页面/应用都会：占存储、需要安全边界、需要审核、可能出错、可能需要维护。

**对策**：页面仓库必须有**强生命周期**（90 天归档、用户可删、上限配额），且**生成即明确"这是为你生成的临时工具，不是长期产品"**。

---

## 八、与现有计划的关系

- 与 `PLAN.md` 的 **M0–M6**：D21–D28 属于 **M4 之后**，不进第一版
- 与 PM 评审的 **P0–P4**：属于 **P4 的条件触发项**（先验证"有人愿意用"，再谈交付形态）
- 但**架构上现在就要预留**：分类节点输出加一个 `交付形态` 字段、协议留一条上行通道 —— 这两件事**成本近乎为零，事后补则要动协议**

---

## 九、待定（需要你拍板）

1. **T2 用现成 SDUI 框架还是自研受控 schema？** 我建议自研 15 个组件的受控 schema —— 生成式 UI 的可控性比功能丰富更重要
2. **T4 的生成产物是否允许被"保存为长期工具"？** 若允许，就变成应用商店，合规与运维复杂度陡增；我建议**只做临时交付物**
3. **上行通道第一版要不要做？** 我建议在协议里预留字段但不实现，等 T3 真跑起来再加

---

## 十、多用户记忆隔离：三层硬约束（不可协商）

> 澄清一个概念混淆：**"缓存"不等于"记忆共享"**。
> 前缀缓存只是"同一段提示词前缀被重复发送时，上游不重复计费"，它不存储、不检索、不跨请求传递语义内容。
> 但既然记忆隔离是产品底线，就要**在每一层都强制**，而不是依赖"缓存本来就不会共享"这个推论。

| 层 | 隔离机制 | 强制手段 |
|---|---|---|
| **数据层** | 每用户一份记忆（档案/品类树/经验/指标），`userId` 是检索函数的**必填参数** | 检索函数签名 `search(userId, query)`，**不存在无 userId 的重载** |
| **进程层** | 每用户一个 DSH 进程 + 独立 `DSH_HOME` | 进程边界天然隔离凭证、会话、存储 |
| **提示词层** | **用户标识放在提示词最前面**，其后才是该用户的记忆块 | 组装函数强制注入，且有断言校验 |

### 为什么把用户标识放在最前（而不是把公共规则放最前）

实测数据（同一套内容，仅用户块不同）：

| 前缀排布 | 用户A 重复 | 用户B（他人） |
|---|---|---|
| 公共规则在前 + 用户记忆块在后 | 命中 1408 | **命中 512（公共规则部分）** |
| **用户标识在最前** | 命中 1408 | **命中 0** |

**取"用户标识在最前"**：代价是省不到公共规则那一块的钱，收益是**上游服务端连"两个用户的内容出现在同一前缀里"的可能性都不存在**。
既然成本暂不是考量，这笔账划算。

### 必须写成断言的两条不变量

```js
// 1) 提示词前缀的第一段必须是用户标识段，且只含该用户的 uid
assert(prefix[0].kind === 'user-identity' && prefix[0].userId === ctx.userId)

// 2) 发给上游的任何请求，都不得包含非本用户的记忆条目
assert(!promptContainsForeignUserId(prompt, ctx.userId))
```

**为什么必须断言而不是靠人记住**：这类违规**不会报错**，只会静默地把一个用户的记忆混进另一个用户的请求里 ——
等发现时已经是数据事故。断言是唯一能在 CI 阶段拦住它的手段。

