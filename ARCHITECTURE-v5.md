# 个人 AI 助手 · 总架构（v5 · 合并定稿）

> 本文合并 v1–v4 四份规划，是**当前唯一权威版本**。
> 结构：① 一张总图 → ② 组件全表 → ③ 一条消息的完整旅程 → ④ 四条机制深挖 →
> ⑤ 数据模型 → ⑥ 宿主映射 → ⑦ 落地分期 → ⑧ 待定项。
>
> 被本文取代：`ARCHITECTURE.md`(v1 插件式双通道)、`ARCHITECTURE-v2.md`、`ARCHITECTURE-v3.md`、`ARCHITECTURE-v4.md`。

---

## ① 总图

```
┌──────────────────────────────────────────────────────────────────────┐
│                        应用层（唯一控制者 + 唯一记忆）                  │
│                                                                      │
│   ┌──────────┐   ┌──────────────┐   ┌───────────┐   ┌────────────┐  │
│   │ 控制器    │   │ 进度估算器    │   │ 一致性守卫 │   │ 记忆库      │  │
│   │Controller│◄─►│ ETA          │   │Consistency│◄─►│ (每用户一份) │  │
│   │          │   │              │   │           │   │            │  │
│   │ · 谁说话  │   └──────────────┘   └───────────┘   │ · 档案      │  │
│   │ · 何时接  │   ┌──────────────┐   ┌───────────┐   │ · 品类树    │  │
│   │ · 记什么  │◄─►│ 心跳续话器    │   │ 降级兜底   │   │ · 经验      │  │
│   │ · 检索    │   │ KeepAlive    │   │ Fallback  │   │ · 指标      │  │
│   └────┬─────┘   └──────────────┘   └───────────┘   └────────────┘  │
└────────┼─────────────────────────────────────────────────────────────┘
         │  发简报 / 发续话通知 / 发打断指令 / 收事件
         │
   ┌─────┴──────┬──────────────────┬──────────────────┐
   ▼            ▼                  ▼                  ▼
┌───────┐  ┌────────┐        ┌────────┐        ┌──────────┐
│接收层  │  │处理层   │        │反馈层   │        │ 品类专属  │
│Agent  │  │Agent    │        │Agent   │        │ Agent    │
│       │  │(独立会话)│        │        │        │ (按需升格)│
│接收+回应│  │深度思考  │        │承接+说出│        │ 高频大类/ │
│快、在场 │  │不对用户说│        │有最终发 │        │ 中类专属  │
│       │  │         │        │言权    │        │          │
└───┬───┘  └────┬───┘        └───┬────┘        └────┬─────┘
    │           │                │                  │
    └───────────┴────────────────┴──────────────────┘
                        │
                        ▼
                     用户
```

**一句话读法**：用户只跟"接收层"和"反馈层"打交道；**处理层永远不直接对用户说话**；
控制器不生产语言，只决定谁在什么时候说、以及记什么。

---

## ② 组件全表

| # | 组件 | 类型 | 职责（一句话） | 绝对不做 |
|---|---|---|---|---|
| 1 | **接收层** | Agent | 接收用户信息并**立刻**回应，维持在场感 | 不判断、不分发、不给结论 |
| 2 | **处理层** | Agent（独立会话） | 真正深度思考，产出结构化结论 | 不对用户说话 |
| 3 | **反馈层** | Agent | **承接**接收层已说的话，说出结论 | 不自己思考、不重复已说内容 |
| 4 | **控制器** | 进程 | 监控两环、估算进度、决定谁在何时说、独占记忆写权 | **不能有表达欲**（有表达欲就变成第四个声音） |
| 5 | **进度估算器 ETA** | 逻辑 | 把"还要多久"输出成三档相对判断 | 不输出秒数（伪精确） |
| 6 | **心跳续话器 KeepAlive** | 逻辑 | 处理层未就绪时，给接收层**真实素材**让它继续说 | 不生成填充废话、不承诺时间 |
| 7 | **一致性守卫** | 逻辑 | 比对"接收层说过的"与"处理层的结论" | 不放过冲突 |
| 8 | **降级兜底** | 逻辑 | 处理层超时/失败时的四种终态话术 | 不允许"用户干等" |
| 9 | **记忆库** | 数据 + 逻辑 | 每用户一份：档案 / 品类树 / 经验 / 指标 | 不让 Agent 直接写 |
| 10 | **检索器** | 逻辑 | 二级检索：粗查给接收层（毫秒级）、精查给处理层 | 不让粗查拖慢首字 |
| 11 | **升格管理器** | 逻辑 | 判定品类是否值得升格为专属 Agent | 不自动建 300 个 Agent |
| 12 | **指标与审计** | 逻辑 | 四指标 + 每条记忆的 evidence 可追溯 | 不阻塞主链路 |

> **四角色 + 一进程** 是骨架（1–4），其余 8 个是让骨架跑起来必需的配套。
> 其中 4 号（控制器）是**结构必然**：因为接收层没有分发能力，所以分发必须发生在它之外。

---

## ③ 一条消息的完整旅程（含时序）

以"我的接口偶发 500，日志里看不出原因，帮我定位一下"为例。

```
t=0ms      用户说话
             │
t≈5ms      控制器捕获 →【查会话档案 + 粗查记忆】关键词级，毫秒级
             │
t≈10ms     控制器并行做两件事：
             ├─► ① 给接收层发【记忆简报】+ 发言简报
             └─► ② 给处理层派任务（携完整记忆包）
             │
t≈1.2s     接收层开口（它要过一次大模型才有首字）
             说的不是"我听到了"，而是：
             "这个我熟，你上次也是这个接口的问题，我按上次那条路再看一遍"
             （因为简报里有：小类=接口故障排查，命中经验1条，3天前）
             │
             │  ← 同时处理层在跑：分类 → 召回 → 专家 → 提炼
             │     控制器持续观察它的活动（步数/工具调用/是否停滞）
             │
t≈900ms    处理层出结论（比接收层首字还快！）→ 写【记忆提案 pending】
             │
t≈1.2s     控制器收到结论 → 问 ETA：接收层还要多久？
             判定 = 还要一会 + 允许打断 + 结论有时效性
             │
t≈1.3s     【接管】软打断：通知接收层"结论已到，立刻收尾"
             接收层说完当前句就停
             │
t≈1.5s     反馈层接管（收到承接包）：
             · 接收层已输出全文  ← 没有它就会重复
             · 接管点（截断在第 N 字）
             · 一致性：extend
             · 结论 + 置信度
             它说："想到答案了：先查连接池和线程池有没有打满，八成在这儿。"
             │
t≈3s       反馈层收尾 → 控制器通知记忆库提交提案（committed）
             │
t+后续      控制器观察用户反应：
             未纠正 → 经验加固（hits+1，置信度↑）
             追问   → 权重再↑，触发更深检索
             纠正   → 标记 retracted 或降权，回写修正后的经验
```

**关键时序结论（实测得出）**：**处理层经常比接收层的第一个字还快**
（处理层简单问题 ~0.9s，接收层首字 0.8–1.5s）。
所以最常见的形态是"**接收层说一句话，反馈层紧接着接管**"，
而不是"接收层说很久，反馈层来救场"。

**由此推出一个必须处理的分支：处理层先到怎么办？**
此时接收层还没开口或刚开口，控制器应当**只让接收层说简短承接语**（不复述、不铺垫），
把发言预算留给反馈层。否则等于用两次发言说一件事。

---

## ④ 四条机制深挖

### 4.1 监控什么（对应"必须有个进程监控"）

| 监控对象 | 监控的**不是** | 监控的**是** |
|---|---|---|
| 接收层 | "它收到了什么"（控制器自己就是分发者） | 是否已开口 / 说了多少多快 / 是否在收尾 / 是否停了 |
| 处理层 | "完成百分之几"（伪精确） | 状态转移 + **是否停滞** |

**"漏接"的定义**：不是"时间过去了"，而是**"接收层停下来了，且处理层还没好"**。
两个条件同时成立才叫缺口 —— 用户在读、在思考时的安静是正确的，不该被填。

**为什么停滞检测比百分比重要**：因为停滞会**改变策略**，百分比不会。

```
未开始 → 已受理 → 推理中 ─┬─→ 已就绪
                        ├─→ 停滞（无新活动超阈值）→ 启动续话 + 评估降级
                        └─→ 失败/超时 → 兜底通道
```

### 4.2 进度估算（三档，不给秒数）

| 档位 | 判定 | 动作 |
|---|---|---|
| `快说完` | 字数接近该类回复均值，或末句命中收尾模式 | 等它自然收尾 |
| `还要一会` | 仍稳定输出，距均值尚有距离 | 软打断（引向收尾） |
| `还很久` | 刚开口 / 在工具调用中 / 速率骤降 | 硬打断，反馈层接管 |

处理层同理：`即将就绪` / `正常推进` / `停滞或超时`。

**计算式**：
```
接收层：剩余时间 ≈ (该类回复长度中位数 − 已输出字数) ÷ 最近 300ms 实测速率
处理层：当前状态 S 的历史 p50 为 T(S)，已停留 t：
        t < T(S)          → 正常推进
        T(S) ≤ t < 2T(S)  → 偏慢，准备续话素材
        t ≥ 2T(S)         → 停滞，启动降级评估
```
> 用 v1 原型实测的先验：分类 0.5–1.4s、召回 ~0ms、专家 2.5–5.7s、提炼 1.1–1.6s。

### 4.3 续话策略（不用固定次数）

**停止条件是"话题耗尽"，不是"次数用尽"。**

接收层的三条硬约束（写进 persona，不靠事后过滤）：
1. **只说自己正在做什么，不说结论**（抢答会制造打脸）
2. **不承诺时间**
3. **不重复**（每次必须带新信息）

控制器的续话通知是结构化的：
```
{ 剩余安全话题: [复述确认, 说明在做什么, 提一个推进性问题],
  已用过: [复述确认],
  禁止: [给出结论, 承诺时间],
  本轮要点: "用户的问题涉及两个可能的品类，正在确认是哪一个" }
```
**妙处**：话题耗尽时不是"硬撑到期"，而是**自然进入降级**（"这个我需要更久，先给你一个初步方向"）。
降级不是外挂异常分支，是续话策略的自然终点。不需要计数器。
安全阀：话题过快耗尽 → 立即触发降级交付。

### 4.4 接管判决（等 / 软打断 / 硬打断）

| 接收层 ETA | 允许打断？ | 结论有时效性？ | 判决 |
|---|---|---|---|
| 快说完 | — | — | **等** |
| 还要一会 | 是 | 是 | **软打断** |
| 还要一会 | 是 | 否 | **等**（自然交接更顺） |
| 还要一会 | 否（禁区） | 任意 | **等**（等它离开禁区） |
| 还很久 | 是 | 任意 | **硬打断** |
| 还很久 | 否（工具调用中） | 任意 | **等一个边界**再判 |

**打断禁区（查表，不现算）**：① 正在输出代码/数字/列表中间 ② 正在调工具且结果未说完
③ 用户刚发新消息 1.5s 内 ④ 当前句已到最后一个分句（等 0.3s 就自然结束）

**软 vs 硬的本质区别**：
- 软打断 = **改指令不改流程**，接收层仍是一轮，只是被通知"立刻收尾" → 无半截话
- 硬打断 = `cancel` + 换人 → **已输出 token 撤不回来**（宿主会固化成 `interrupted: true` 的半截消息）
  → 所以硬打断的真实语义是"**就此闭嘴，反馈层接着说**"，不是"抹掉刚才那句"。必须写进产品预期。

---

## ⑤ 数据模型（每用户一份）

```jsonc
// 记忆库根
{
  "userId": "u_xxx",
  "profile": {                        // 稳定偏好：影响"怎么答"
    "verbosity": "步骤优先，不喜欢长解释",
    "expertise": { "技术/编程": 0.8 },
    "bannedTopics": []
  },
  "taxonomy": [ /* 见下 */ ],
  "experiences": [ /* 见下 */ ]
}

// 品类节点（大类/中类/小类共用一张表）
{
  "path": "技术/编程/接口故障排查",
  "level": "subcategory",              // domain | category | subcategory
  "visits30d": 9,
  "correctionRate": 0.11,
  "p50DurationMs": 6400,
  "agentState": "experience-pack",      // experience-pack | agent | frozen
  "agentId": null,                      // 升格后填 sessionId
  "createdFrom": "dialogue"             // seed | dialogue
}

// 经验条目（记忆的最小单元）
{
  "id": "exp_123",
  "path": "技术/编程/接口故障排查",
  "level": "subcategory",
  "pattern": "接口高峰期偶发 500",
  "solution": "先查连接池与线程池是否打满",
  "pitfall": "先别改代码，容易误判",
  "hits": 3,
  "confidence": 0.86,
  "state": "committed",                 // pending | committed | retracted
  "evidence": ["turn_18", "turn_42"]    // 可追溯：每条记忆都能查到是哪几轮支撑的
}
```

**三条设计要点**：
1. **写权独占**：只有控制器能写，采用**两段式**（处理层提 `pending` → 用户未纠正 → 控制器提交 `committed`）。
   这样答错的记忆不会污染后续所有轮次。
2. **`evidence` 是防幻觉的落点**：接收层引用了记忆，就能一路查到证据；说了记忆里没有的，就是幻觉。
3. **记忆归属用户，不归属 Agent**：四个角色都只是读写方，控制器是唯一写入者。

### 记忆如何改变接收层的回应

**它引用的不是知识，是"对这个人的了解"** —— 这是"智能响应"和"复读机"的分界线。

| 场景 | 无记忆 | 有记忆简报 |
|---|---|---|
| 小类有高置信经验 | "我听到了，我看看" | "这个我熟，你上次也是这个接口，我按上次那条路再看" |
| 老用户有偏好档案 | "我听到了" | "收到，我直接给你步骤，不绕弯子" |
| 该小类首次遇到 | "我听到了" | "这类偶发故障我看过不少，先看是不是高峰期才出现？" |

**三条防幻觉约束**：① 只引用控制器筛过的"可安全引用"清单；② 引用必须可追溯到具体记忆；
③ **低置信记忆只能影响"怎么答"，不能出现在"说什么"里**。
**防过度拟合**：设引用频次与新鲜度门限，引用过多时主动衰减该记忆权重。

---

## ⑥ 宿主映射（已核实，不是设想）

| 需要 | 宿主接口 |
|---|---|
| 建三个角色 Agent | `ctx.agents.create({ sessionId, agentOptions, setup })` |
| 角色人格隔离 | agent preset + `dsh-persona`（scope 级，三角色三套 persona） |
| 拿任意 Agent | `ctx.agents.get(id)` / `list()` / `roots()` |
| 读接收层输出增量 | `assistant/chunk`（token 级） |
| 判断"是否收尾/被打断" | `assistant/message`，被打断时带 `interrupted: true` |
| 观察处理层活动 | 步边界 + 工具调用事件（用于停滞检测） |
| 硬打断 | `agent.cancel(cause, { keepInbox })` |
| 续话通知 / 发言简报 / 注入上下文 | `agent.steer()`（在跑）/ `followup()`（已停）/ `inject()`（不唤醒，只喂上下文） |
| 用户输入 | `session/event` 的 `user/message`（靠 `source` 区分真实用户与注入） |
| 唯一对话真源 | 控制器自己的记录（不依赖任何 Agent 日志） |

**两个实现要点**：
- 控制器不该把接收层要说的话写死（那就成了控制器在说话），应给**意图与约束**，语言由 Agent 组织。
- 模型分工（v1 原型实测）：接收层用 `deepseek-flash` + **关思考**；
  处理层专家用强模型，但**提炼阶段必须用快模型**（实测 p50 从 12.0s 降到 6.5s，降级率 100%→0）。

---

## ⑦ 落地分期

| 阶段 | 交付 | 验收标准 |
|---|---|---|
| **P0** | 应用层骨架：真实创建三个角色 Agent + 只读 transcript 采集 + 控制器唯一对话记录 | 用户说一句，三个 Agent 各自真实产生内容，控制器正确记录 |
| **P1** | 记忆库最小版：品类树 + 经验 CRUD + 二级检索 | 接收层能拿到记忆简报，第一句话不再是"我听到了" |
| **P2** | 控制器核心：ETA + 续话（话题耗尽制）+ 接管三路 | 三种接管路径各复现一次，反馈层正确承接 |
| **P3** | 一致性守卫 + 降级兜底 | 构造"接收层猜测被推翻"与"处理层超时"两个场景，处置正确 |
| **P4** | 升格管理器 + 指标审计 | 高频品类自动提议升格；每条记忆可追溯到证据 |

> 每阶段只用真实现，不写假 Agent。

---

## ⑧ 待你拍板（8 项，定完即可开工）

| # | 决策点 | 我的建议 |
|---|---|---|
| 1 | 反馈层与接收层是否共用同一套 persona | 共用（保"一个声音"）；输出内容不同、语气一致 |
| 2 | 硬打断默认策略 | **默认关闭**，只在"结论有时效性 且 接收层还很久"时启用 |
| 3 | 处理层能否反向要求更多信息 | 第一版**不做**（接收层保持单向），做稳后再说 |
| 4 | 接收层拿完整记忆还是粗查结果 | **粗查**（毫秒级），否则反噬首字延迟 |
| 5 | 升格触发方式 | **提议 + 人工确认**，不全自动 |
| 6 | 跨用户经验是否共享 | **分两层**：通用方法论可共享，个人偏好严格隔离 |
| 7 | 处理层先到时接收层的发言预算 | 只让接收层说简短承接语，预算留给反馈层 |
| 8 | 记忆检索放在控制器还是独立进程 | 放控制器内部（P1–P4），过载后再拆 |

---

## 附：v1–v4 演进摘要（存档用）

| 版本 | 核心结论 |
|---|---|
| v1（插件式） | 做出可跑原型；实测双层架构值 9 倍延迟差；踩到 5 个真 bug（经验张冠李戴、复用链断、超时降级、指标被稀释、沉淀路径与召回路径不一致） |
| v2（应用层） | 推翻 v1 的插件形态：应用层之上可"决定谁说话"，约束消失 |
| v3（三层定义） | 正式定名接收/反馈/处理；**控制器是结构必然**（因接收层无分发能力）；废除固定续话次数 |
| v4（用户记忆） | 记忆按用户隔离、写权独占控制器、记忆回流改变接收层第一句话 |
