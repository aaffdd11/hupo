# 无缝衔接设计（Seamless Handoff）：快回答 → 深回答

> **定位纠偏**：反馈层不是"第二个接话的角色"，它是**经过深思熟虑的那部分回答**。
> 一轮对话里，用户应当收到**两块内容**：
> ① **接收层的快速回答** —— 回应、确认、方向、能立刻给的部分
> ② **反馈层的深思回答** —— 研究之后的结论
> **本文件解决的核心问题：怎么让这两块看起来是一个人在一次回答里说的，而不是两个人各说一段。**

---

## 一、先明确"两块回答"的分工（不是重复，是分工）

| | 快速回答（接收层） | 深思回答（反馈层） |
|---|---|---|
| 时间 | 0.8–1.5s 首字 | 结论就绪时（实测 p50 6.5–7.9s，简单问题可 <1s） |
| 内容 | 我听到了什么 / 这件事属于哪类 / 我准备怎么做 / **能立刻确定的部分** | 研究后的结论 / 依据 / 下一步 |
| 语气 | 承接式、口语 | 结论式、具体 |
| 禁止 | 不许给未经研究的结论 | 不许重复上面说过的、不许另起话题 |

**关键**：接收层说的**必须是有效内容**，不是"稍等"。它要么给出方向（"这类问题一般是连接池或下游超时，我先去确认"），要么给出能立刻确定的结论，要么提出一个能推进的问题。

---

## 二、核心机制：不是"拼接"，是"让深回答开口就承接"

**错误做法**（会露馅）：把两块文本用连接词粘起来 —— "…另外，我的结论是…"

**正确做法**：**把第一块的原文交给第二块，让第二块的第一句话天然承接。** 承接不是拼装出来的，是**生成出来的**。

```
接收层输出（原文，一字不改地保存）
        │
        ▼
   交接包 = { 已输出全文, 截断位置, 一致性判定, 研究结论, 语气样本 }
        │
        ▼
反馈层生成时，上下文里就有这一段，它的第一句自然会接上
```

**交接包里必须有语气样本**（接收层最近 2–3 句原文），这是"像同一个人"的关键 ——
比"共用同一份 persona"更有效，因为 persona 只给风格，语气样本给的是**这一轮的具体语感**。

---

## 三、四种衔接形态（按"第一块说完没有"分）

| 形态 | 触发条件 | 拼接效果 | 示例 |
|---|---|---|---|
| **S1 接着说** | 第一块还在输出 | 第二块直接续在同一段里 | "…这类问题一般是连接池和下游超时，**我确认了一下，是连接池打满了**" |
| **S2 自然收尾后接上** | 第一块刚说完，第二块已达 | 第二块以结论开头 | "这个我熟，先看连接池。**确认了，就是连接池打满，不是下游。**" |
| **S3 延迟补发** | 第一块说完很久，第二块才到 | 需要一个**轻量重启信号** | "…刚查完，**结论是连接池打满**" |
| **S4 纠偏** | 第一块的方向被结论推翻 | **必须显式纠偏** | "…我先怀疑是下游超时。**查下来不是，问题在连接池**" |

**S4 是最容易做砸、但最伤信任的一种。** 规则：
- 必须明确说"不是 X，是 Y"，不能悄悄换说法
- 不能说"我之前说的是错的"这种自我否定 —— 用"查下来"这种自然的修正口吻
- 纠偏之后要给出**为什么原来的猜测不成立**，否则用户不知道以后该信哪一句

---

## 四、让衔接成立的五个设计要点

### 4.1 接收层的末句要"留扣"
接收层在收尾时，不能用句号做完全封闭的结束（"就是这样。"），而应留一个自然的接口：
- ✅ "我先按连接池这条线去确认" / "这类问题通常出在三处，我逐个看"
- ❌ "我只能帮你到这里" / "希望对你有帮助" / "还有什么可以帮你"

**这不是提示词技巧，要写进接收层的角色指令。**

### 4.2 反馈层的首句要"接住"
反馈层不许以"关于你的问题""总的来说""经过分析"这类**断裂式开头**讲话。
必须包含**至少一个来自第一块的锚点**（同一个名词、同一个假设、同一个方向）。

**可校验**：给反馈层的首句做一个检查 —— 是否与第一块的末句共享至少一个实词。不共享则重生成一次。

### 4.3 同一套"声音块"，不同的"角色块"（避免之前那个 bug）
- **共用**：语气、长度偏好、称呼、禁用词、用户档案里的表达偏好 → 放一个共享 `voice` 段
- **分开**：接收层"禁止给未经研究的结论"、反馈层"必须给结论且不得重复" → 各自的角色指令

⚠ 上一版架构曾把两者的 persona 完全共用，导致反馈层继承了"不说结论"这条禁令 ——
**等于给唯一该说结论的角色下了封口令。** 必须 `voice` 共用、`role` 分开，并加单测断言。

### 4.4 视觉上要"一条消息"
客户端把两块渲染成**同一条消息**（只是第二块晚到），而不是两条：
- 不用"AI 说了两句"的分割线
- 第二块到达时**追加**到同一气泡内，不做弹出动画（弹出会强调"这是新的一条"）
- 光标/打字指示器从第一块平滑延续到第二块，中间不消失

**如果第一块与第二块间隔超过约 4 秒**，才允许做一次轻量的视觉分隔（例如轻微的分段间距），
但这属于**兜底**，不是常态 —— 目标是让用户以为一直是同一个人在写。

### 4.5 沉默期（S3）的处理
第一块说完、结论还没到，用户会看到"不动了"。三种处理，按优先级：
1. **接收层继续给有效内容**（新角度/确认/推进性问题）—— 首选，但受"不重复"约束
2. **轻量续说**（一句话，很短）："还在确认第二处"
3. **视觉状态**（思考指示器），不产出语言

**红线**：绝不用"请稍等"这类无信息量的填充。宁可显示状态，也不说废话。

---

## 五、失败模式清单（验收时必须逐条测）

| 失败模式 | 表现 | 如何防 |
|---|---|---|
| **重复** | 第二块把第一块说过的结论又说一遍 | 反馈层上下文里带第一块全文 + 明确指令"跳过已说内容" |
| **断裂** | 第二块像另一个人另起一段 | 语气样本 + 首句锚点校验 |
| **矛盾** | 第二块与第一块方向相反却不说 | 一致性守卫（agree/extend/conflict），conflict 必须显式纠偏 |
| **抢话** | 第一块还没说完就被第二块截断 | 打断判决（等/软/硬）+ 打断禁区表 |
| **露馅** | 用户察觉"这是两个人" | 同一条消息渲染 + 无分割动画 |
| **卡死** | 第二块永远不来，用户干等 | 皮层超时降级 + 状态提示 |
| **冷场** | 第一块说完后长时间空白 | 续说/状态指示器 |

---

## 六、最小验证（最便宜的一步）

**测法**：把 v1 原型（已跑通）的同一轮输出，做成两种渲染给真人看：

- **A 组**：单 agent 被 steer —— 一句话流，中途内容切换（v1 现状）
- **B 组**：接收层先说 + 反馈层承接 —— 两块内容，同一条消息渲染

**问一个问题**（不要问"哪个好"，要问行为）：
> "刚才这段回答里，你觉得有没有换人？如果换人了，从哪一句开始？"

**判据**：
- B 组能指出"换人点"的比例 ≤ A 组 → 无缝衔接成立，反馈层独立存在有价值
- B 组明显更容易被指认换人 → 说明衔接没做好，先修衔接，不要急着加机制

**这一步不需要写任何新代码**，用现有 v1 的产出手工渲染两版即可。

---

## 七、对架构的影响

| 组件 | 变化 |
|---|---|
| 接收层 | 定位修正：**快速回答**（不是"在场感话术"）；末句要留扣 |
| 反馈层 | 定位修正：**深思回答**；首句要接住；上下文必须含第一块全文 + 语气样本 |
| 控制器 | 新增：**交接包**（含语气样本）+ **首句锚点校验** + **板块渲染指令**（同一条消息） |
| 客户端 | 新增：同一条消息的分块追加渲染（无分割动画） |
| 测试 | 新增：`voice`/`role` 分离断言、首句锚点单测、六种失败模式的场景测试 |

**一条不能违反的纪律**：反馈层的上下文里**永远要有接收层的已输出全文**。
没有它，"不重复"和"接得上"都做不到 —— 这是无缝衔接的物理前提。
