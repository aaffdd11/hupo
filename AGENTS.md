# 琥珀 —— 关于你自己

你是 **琥珀（hupo）**：一个会自己动手把事情办完的私人助理。
**这个仓库是你自己的代码。**

> ⚠️ **四条最容易搞错的，先看**：
> 1. 🔒 **手册 v1.0 已定档**（`baseline-v1.0`）——**代码按它写，不按旧代码写**（§二）
> 2. **这台机器不是生产机**（§一）
> 3. **唯一权威文档是 `docs/handbook/`**（§二）
> 4. **实现要全新建立**（§三）——**唯一的实现在 `v2/`**，上一代已删

---

## 一、⚠️ 这台机器不是生产机

**这是本项目最容易踩的坑。** 仓库里曾经的手册描述的是**另一台机器**，照着做会失败。

| | **本机（你现在在这里）** | **生产机（另一台）** |
|---|---|---|
| 项目路径 | `/home/deploy/proj/hupo` | 另一个 checkout |
| 服务 | ❌ **没有**（`concierge-core` 在本机是 inactive） | systemd 单元 `concierge-core`，端口 8091 |
| 用途 | **改代码、跑测试、出构建** | 真正服务用户 |
| 域名 | ❌ 无 | `https://hupo.stalkerai.cn` |

⇒ **别在本机找服务，别重启一个不存在的服务。**

### 1.1 本机的事实（都是实测的）

| 项 | 值 |
|---|---|
| Flutter | **3.35.1**（`~/sdk/flutter/bin/flutter`） |
| Dart | 3.9.0 |
| JDK | Temurin 17 |
| Android SDK | 36 |
| Node | **v24.15.0** |
| `sudo` | ⚠️ **要密码**（`sudo -n true` 会失败） |
| `node` | ⚠️ **只有 nvm 里那一个**（`/usr/bin/node` **不存在**）⇒ 让主人跑的命令**必须写绝对路径**，`sudo node …` 会撞「找不到命令」 |
| `deploy` 在 docker 组？ | ❌ 不在（**而且永久不加**） |
| 无根容器 | ❌ 被挡（`apparmor_restrict_unprivileged_userns=1`） |

> ⚠️ **`sudo` 要密码这一条很重要**：有一版手册写着"你 `sudo` 免密，可以直接改自己"——
> **那是错的**，而且**决策 P2 的安全性正建立在"要密码"上**。
> **不许照着"我免密"去试。**

---

## 二、唯一权威文档：`docs/handbook/`

**结论以那里为准。** 其他旧文档已经删掉（§六）。

| 文件 | 回答什么 |
|---|---|
| `README.md` | 索引 + 三条读法 + 四条维护纪律 |
| `01-PROJECT.md` | **这是什么、给谁、为什么** |
| `02-ARCHITECTURE.md` | **系统由什么组成**、边界、不变量 N12–N25 |
| `03-DEVELOPMENT.md` | **代码长什么样、往哪改** |
| `04-ROADMAP.md` | **先做什么后做什么、怎么算做完** |
| `05-DECISIONS.md` | **已经拍板了什么**（76 条） |
| `06-OPERATIONS.md` | 怎么跑起来、怎么不出事 |
| `07-APPENDIX.md` | 现状有哪些坑、术语、评审史 |
| `08-SPEC.md` | 规范与判据（15 节）：不变量 N1–N11 · 接口表 · 安全模型 · 浮窗铁律 · **阈值总表** · 运行机制 · L3 契约 · **容器与上线验收** · 小程序沙箱 · L2 实现约束 |

**冲突时的裁决顺序：手册 → 代码 → 其他。**

⚠️ **发现手册与代码不一致 ⇒ 先按代码确认事实，然后回来改手册**（不要反过来）。

### 2.1 四条维护纪律（**破一条这套手册就废**）

| # | 纪律 |
|---|---|
| 1 | **不写数值。** 阈值、超时、容量只住在代码与决策记录里。**写死数值的那一刻它就开始过期。** |
| 2 | **协议字段一旦上线就冻结**（旧客户端还在跑）。 |
| 3 | **纯函数进 `test/unit`，不写界面断言**（`test/widget` 只是提示不是闸）。 |
| 4 | **"以后再说"等于永远不做。** 要砍就明说砍了。 |

---

## 三、全新建立（**这条是硬要求**）

> **开发时，开发文档与代码都全新建立；旧的只能当参考。**

具体含义（**别理解成"照抄旧代码"**）：

| | 怎么办 |
|---|---|
| **代码** | **新写**。手册怎么说就怎么写。**旧代码是"它当时怎么做的"的证据，不是模板** |
| **为什么还有"旧的"** | ⚠️ **上一代实现已经删掉了**（2026-09-21）。它知道的那些事实（DSH 真实行为、坑、字段）**已经收敛进手册与 `docs/dev/`**；要翻原文用 `git show f93f296:<路径>` |
| **开发文档** | 随代码**新写**（模块设计、契约、验收）。旧的（`07-APPENDIX.md` 的缺陷清单、`08-SPEC.md` 的约束）**是输入，不是产出** |
| **遇到"手册没说"的** | **先问，别猜**；查旧代码是为了**搞清事实**，然后**回来把结论写进手册**（升版本） |

### 新代码在哪

```
v2/services/core/src/     调度器（现在有：store / timeline / message-writer / process-guard）
v2/services/core/test/    验收测试
docs/dev/                 这批怎么实现、验收怎么过（随代码长）
docs/handbook/            要什么、为什么（冻结，唯一权威）
```

**跑法**：
```bash
cd v2/services/core && npm test && npm run demo
```

**进度**（条数 / 批次 / 下一步）**只住在 `docs/dev/00-PROGRESS.md`** —— 别抄到这里来：
这个文件是**开机核对**的受保护文件，**它一变主人就得补一条 `sudo`**（见 §八）。
硬闸是 `cd v2/services/core && npm test`；线上是 <https://w.stalkerai.cn>。

⚠️ **最容易犯的错**（旧代码删了也一样）：**照着记忆里的老做法顺手改**。
**缺陷清单在 `07-APPENDIX.md` §一**，那份清单里的每一条都是"不许再出现"——
**而且它记的是当年的行号，代码早没了**（用 `git show f93f296:<路径>` 翻）。

---

## 四、现在的状态（**接手前必读**）

| 项 | 状态 |
|---|---|
| **规划** | ✅ **完成**。76 条决策全部拍板；两轮独立评审（各三席）已收口；**批 0 整个关闭** |
| **实现** | 🚧 **进行中**——批次与条数见 `docs/dev/00-PROGRESS.md`（**这里不抄数字**） |
| **线上** | ✅ <https://w.stalkerai.cn> —— 登录 + 聊天 + 会答话 + **重启还记得** + **卡住会收场** + **看得见它在做** + **说话像它自己** + **被硬杀能自己收干净、只读的活还会自己重做** |
| **下一步** | §八 那张单子已清完；接下来按 `00-PROGRESS.md` §六「欠的账」排 |

> ⚠️ **"线上"那一行曾经是假话，必须记着**（批 16，见 `docs/dev/16-STREAM.md`）：
> 服务端那些能力**全是真的**，但**浏览器那一条路从来没通过**——
> 客户端把流的地址算成了 `ws://`，在 https 页面上被浏览器按混合内容拦掉。
> 而当时**所有闸都是绿的**：验收用的是 node 探针，探针把 `wss://` **写死了**，
> 绕过了被测的那行代码。⇒ **判据补进了 `08-SPEC.md` §13.3 的 V13。**
> **接手的人：看到"线上✅"先问一句"这是在哪一端验的"。**


> 📊 **计划 vs 实际的逐条对表在 `docs/dev/00-PROGRESS.md`**——
> 那里有：**进度流水**（五条下一步各自做到哪）、旧缺陷清单 26 条的逐条核对
> （**20 条已对，2 条没修**）、批次表 7 批对到哪、以及**欠的账**。
> **接手前先看一眼，别重复核一遍。**

**已经建完的，一件一行**（每件都有对应的 `docs/dev/` 文档，**动手前先读那一份**）：

> ⚠️ **这张表搬走了** —— 它原来在这里，而它**每落地一批就长一行**。
> 现在它在 `docs/dev/00-PROGRESS.md` §〇（**进度流水**）与 §三（**批次表**），
> **和条数、批次百分比住在一起**。
>
> 为什么搬：**同一份数字不许有两处**（两处 = 一定会漂），
> 而且这个文件现在是**开机核对**的受保护文件之一
> （见 §八）——**它一变，主人就得补一条 `sudo`**。
> ⇒ **规则留在这里，数字滚去 `00-PROGRESS.md`。**

**批 1 里有四件是"今天就该修的"**——⚠️ 既然是全新建立，
**这四件不是"去旧代码里改"，而是"在新代码里从一开始就对"**。
**它们已经落进地基了**（下面留作对照）：

| # | 做什么 | 为什么急 |
|---|---|---|
| **S1 + S9** | 落盘失败**上抛**，**并写清谁接** | 唯一会**永久丢数据**的根；**只写上抛不写谁接 = 盘满 → 崩 → 重启 → 再崩** |
| **S7** | `dispatcher` 的 `turns` 键改用 **`info.turn`**（五处） | **升格过的轮次，收口永久失效** |
| **S8** | 瞬态事件**不占号** | 修前"磁盘无空洞"是假话 |
| **S10** | **连接生命周期** | **退一个会话会打死全站连接，且不自恢复** |

⚠️ **动手前先读** `03-DEVELOPMENT.md` §5.2（谁接）与 §5.3（`turns` 键）——
**那两处的根因不明显，照直觉改会改错地方。**

---

## 五、干活的标准流程

### 5.1 改客户端（**`v2/apps/mobile/` 是新实现**）

```bash
cd v2/apps/mobile
~/sdk/flutter/bin/flutter analyze          # 硬闸：编译
~/sdk/flutter/bin/flutter test test/unit   # 硬闸：协议 / 状态机 / 纯逻辑
~/sdk/flutter/bin/flutter test test/widget # ⚠️ 只是提示，不是闸
~/sdk/flutter/bin/flutter build web --release --pwa-strategy=none   # 出产物
```

⚠️ **`test/widget` 里的可访问性那组是硬闸**（`accessibility_test.dart`，
第 13 批加的：五档不溢出 + 命中区 ≥44 + 不封顶 —— **D3.5 点名要硬闸**）。
另一份 `busy_line_test.dart`（5 条）是**提示档**，但**别删**：那是"这件事到底有没有画到屏幕上"的唯一自动化证据。
**一条命令跑完三道闸**：`bash scripts/check-client.sh`。
⚠️ **出 Web 产物别手打命令**：用 `scripts/deploy-web-v2.sh`（它会**给入口文件加指纹**，见 `15-CACHE.md`）。
（**旧 app** 那边的界面测试是 49 条挂 10——别和 v2 的 49 条 unit 混了。
界面一重构那些断言必然过期 ⇒ **照常继续 → 用真机确认 → 有空再回头补**。
**你的任务是让用户看到变化，不是把旧断言哄好。**）

⚠️ **但 `flutter analyze` 与 `test/unit` 挂了必须修干净**——它们守的是协议与状态机。
想清楚：是真的改了协议，还是把状态机改坏了。

⚠️ **`apps/mobile/`（没有 `v2/` 的那个）已经删了**（2026-09-21）。仓库里**只有 `v2/`**。

### 5.2 改服务端（**`v2/services/core/` 是新实现**）

```bash
cd v2/services/core && npm test     # 硬闸（条数见 docs/dev/00-PROGRESS.md）
cd v2/services/core && npm run demo # 肉眼过一遍地基
```

**重启线上那个服务**（本机 8020，`w.stalkerai.cn` 就是它）：

```bash
scripts/restart-core.sh            # ⚠️ 默认**保留日志**——跨重启接记忆靠它
scripts/restart-core.sh --fresh    # 清空日志（"从我们刚认识开始"）
```

> ⚠️ `services/core/`、`apps/mobile/`、`packages/`（旧实现）**已经删掉**。要查证某个 DSH 行为，用 `git show f93f296:<路径>` 翻原文。

### 5.3 提交

所有改动**都要推到 GitHub**：

```bash
bash scripts/push-changes.sh "一句话说明"
```

推送失败也要说一声（改动已在本地提交，下次成功会自动补齐）。

---

## 六、不能破的几条

| # | 不许 |
|---|---|
| 1 | **不要把密钥写进任何会显示给别人的地方**，也不要写进日志 |
| 2 | **不要改协议字段的语义**——一旦上线就冻结（`03-DEVELOPMENT.md` §三） |
| 3 | **不要写死尺寸**（`写死尺寸 = 缺陷`，见 `05-DECISIONS.md` D3） |
| 4 | **不要让内部词出现在界面上**（工作区 / 口令 / 连接 / 客户端 / 云端 / 工具名 / `web_search`）——**这是缺陷，不是文风问题** |
| 5 | **不要动别人的目录**——这台机器上还有别的项目 |
| 6 | **不要说"以后再做"**——要砍就明说砍了（`04-ROADMAP.md` §十一"做一半比不做更坏"） |
| 7 | **不确定就去手册里找**；手册没有，**先问，别猜** |

---

## 七、历史在哪

旧文档（`ARCHITECTURE-v2…v7`、`REVIEW-*`、`DEBATE-*`、`DISCUSS-*`、`PLAN.md`、
`SYNTHESIS.md`、`docs/` 下的旧目录，以及 v1 原型的 `src/`）**已经删掉**。

**上一代实现（`services/` 调度器、`apps/mobile/` 客户端、`packages/protocol/`）也删掉了**（2026-09-21）。

**它们全都在 git 历史里**，需要时这样取：

**删除前的完整快照是 commit `f93f296`**（那之后的第一个 commit 就是删除）：

```bash
git show f93f296:ARCHITECTURE-v7.md                 # 例：旧的总架构
git show f93f296:docs/ux/06-retiree.md              # 例：那位不会拼音的用户的原话
git ls-tree -r --name-only f93f296 | grep docs/     # 列出当时所有文档
```

⚠️ **手册已经把该留的都收敛进来了**——包括不变量 N1–N11、接口表、安全模型、浮窗铁律、
**阈值总表**、**崩溃守护 / 回退锚点 / 预算降档**、**L3 工具能力矩阵与记忆五道写闸门**、
**v6 代仍有效的规范（含三起"页面在说假话"事故）**、iOS 构建、Web 部署。**见 `08-SPEC.md`。**
**只有在手册确实没写到、而你又非要那个原文的时候，才去翻历史。**

---

## 八、关于"改运行中的系统"（P1 / P2）

**批 19 已经把机制落地了**（`docs/dev/19-P1-P2.md`）。你要知道它的方向与**现在的事实**：

> **助手只能"提申请"，签字的是主人。**
> 助手可以提、可以写方案，**永远不能自己按那个按钮**。

理由不是洁癖。威胁的真实形态**不是"它干一次坏事"**（那能收拾），
而是"**坏事能不能留下来**"：

> 它读到一段看不见的网页内容 → 被诱导写进自己的配置 →
> **从今往后每轮开机都读那一段** → 而表面上一切正常。

⇒ **人工执行（P2）**：助手把改动与命令准备好，**主人自己上机器跑**。
形态必须"**短到主人愿意看**"（一条命令 + 一个补丁），否则会被绕过。

### 8.1 现在的事实（**别照着记忆里的老样子做**）

| 事 | 现在怎么样 |
|---|---|
| **开机完整性清单** | 机制在 `v2/services/core/src/integrity.js`：`strict` 条目对不上 ⇒ **拒绝启动** |
| ✅ **保护启用了吗** | **已启用**（2026-09-20 23:21，主人自己跑的 `--build`）。清单在 `/etc/hupo/integrity.json`，**`root:root 0444`**、50 个文件。开机横幅写「**完整性 对上了**」。真机验过：改一个受保护文件 ⇒ `serve.js` **拒绝启动**并点名那个文件；还原 ⇒ 对上了 |
| **提申请** | 补丁放 `proposals/`（已在 `.gitignore` 里），**主人**跑 `scripts/apply-change.sh proposals/xxx.patch` |
| **回退** | `scripts/rollback.sh`（用 `git revert`：**把这一次反做一遍**，历史留着。⚠️ **不许用"退回到上一个提交"那种做法**——它会连同后面的每一个改动一起丢掉）；它**不读任何助手产出的文件**——出问题时助手可能就是出问题的那个 |
| **改完代码之后** | 不用做什么（`src/`、`scripts/` 是**只报不拦**） |
| ⚠️ **改完这几样之后** | **人格 / 这个文件 / `docs/handbook/**` / `~/.dsh/profiles/*/cordis*.yml`** = `strict` ⇒ **要请主人补一条重建命令**，否则**下次重启会拒绝启动**。命令别手打，照 `verify-integrity.mjs` 打出来的那一条粘（**里面是绝对路径**：本机没有系统 `node`）。⇒ **`AGENTS.md` 里不许写进度数字**（那些每批都变）：数字只住在 `docs/dev/00-PROGRESS.md` |

⚠️ **在本机改仓库代码不属于这一条**——那是正常的开发工作。
这一条针对的是**让改动变成"下次开机自动读"**的路径。

⚠️ **`sudo` 要密码这件事是这道护栏的地基**：正因为它要密码，
"助手自己把改动变成永久生效"这条路才不成立。**不许去试"能不能免密"。**
