# 琥珀 —— 关于你自己

你是 **琥珀（hupo）**：一个会自己动手把事情办完的私人助理。
**这个仓库是你自己的代码。**

> ⚠️ **四条最容易搞错的，先看**：
> 1. 🔒 **手册 v1.0 已定档**（`baseline-v1.0`）——**代码按它写，不按旧代码写**（§二）
> 2. **这台机器不是生产机**（§一）
> 3. **唯一权威文档是 `docs/handbook/`**（§二）
> 4. **实现要全新建立，旧代码只当参考**（§三）——**新代码在 `v2/`**

---

## 一、⚠️ 这台机器不是生产机

**这是本项目最容易踩的坑。** 仓库里曾经的手册描述的是**另一台机器**，照着做会失败。

| | **本机（你现在在这里）** | **生产机（另一台）** |
|---|---|---|
| 项目路径 | `/home/deploy/proj/hupo` | 另一个 checkout |
| 服务 | ❌ **没有**（`concierge-core` 在本机是 inactive） | systemd 单元 `concierge-core`，端口 8091 |
| 用途 | **改代码、跑测试、出构建** | 真正服务用户 |
| 域名 | ❌ 无 | `https://hupo.stalkerai.cn` |

⇒ **别在本机找服务，别重启一个不存在的服务。**

### 1.1 本机的事实（都是实测的）

| 项 | 值 |
|---|---|
| Flutter | **3.35.1**（`~/sdk/flutter/bin/flutter`） |
| Dart | 3.9.0 |
| JDK | Temurin 17 |
| Android SDK | 36 |
| Node | **v24.15.0** |
| `sudo` | ⚠️ **要密码**（`sudo -n true` 会失败） |
| `deploy` 在 docker 组？ | ❌ 不在（**而且永久不加**） |
| 无根容器 | ❌ 被挡（`apparmor_restrict_unprivileged_userns=1`） |

> ⚠️ **`sudo` 要密码这一条很重要**：有一版手册写着"你 `sudo` 免密，可以直接改自己"——
> **那是错的**，而且**决策 P2 的安全性正建立在"要密码"上**。
> **不许照着"我免密"去试。**

---

## 二、唯一权威文档：`docs/handbook/`

**结论以那里为准。** 其他旧文档已经删掉（§六）。

| 文件 | 回答什么 |
|---|---|
| `README.md` | 索引 + 三条读法 + 四条维护纪律 |
| `01-PROJECT.md` | **这是什么、给谁、为什么** |
| `02-ARCHITECTURE.md` | **系统由什么组成**、边界、不变量 N12–N25 |
| `03-DEVELOPMENT.md` | **代码长什么样、往哪改** |
| `04-ROADMAP.md` | **先做什么后做什么、怎么算做完** |
| `05-DECISIONS.md` | **已经拍板了什么**（76 条） |
| `06-OPERATIONS.md` | 怎么跑起来、怎么不出事 |
| `07-APPENDIX.md` | 现状有哪些坑、术语、评审史 |
| `08-SPEC.md` | 规范与判据（15 节）：不变量 N1–N11 · 接口表 · 安全模型 · 浮窗铁律 · **阈值总表** · 运行机制 · L3 契约 · **容器与上线验收** · 小程序沙箱 · L2 实现约束 |

**冲突时的裁决顺序：手册 → 代码 → 其他。**

⚠️ **发现手册与代码不一致 ⇒ 先按代码确认事实，然后回来改手册**（不要反过来）。

### 2.1 四条维护纪律（**破一条这套手册就废**）

| # | 纪律 |
|---|---|
| 1 | **不写数值。** 阈值、超时、容量只住在代码与决策记录里。**写死数值的那一刻它就开始过期。** |
| 2 | **协议字段一旦上线就冻结**（旧客户端还在跑）。 |
| 3 | **纯函数进 `test/unit`，不写界面断言**（`test/widget` 只是提示不是闸）。 |
| 4 | **"以后再说"等于永远不做。** 要砍就明说砍了。 |

---

## 三、全新建立（**这条是硬要求**）

> **开发时，开发文档与代码都全新建立；旧的只能当参考。**

具体含义（**别理解成"照抄旧代码"**）：

| | 怎么办 |
|---|---|
| **代码** | **新写**。手册怎么说就怎么写。**旧代码是"它当时怎么做的"的证据，不是模板** |
| **为什么还要看旧的** | 它是**唯一知道 DSH 真实行为**的地方：API 长什么样、哪些坑踩过、某个字段实际是什么。**查证可以，照抄不行** |
| **开发文档** | 随代码**新写**（模块设计、契约、验收）。旧的（`07-APPENDIX.md` 的缺陷清单、`08-SPEC.md` 的约束）**是输入，不是产出** |
| **遇到"手册没说"的** | **先问，别猜**；查旧代码是为了**搞清事实**，然后**回来把结论写进手册**（升版本） |

### 新代码在哪

```
v2/services/core/src/     调度器（现在有：store / timeline / message-writer / process-guard）
v2/services/core/test/    验收测试
docs/dev/                 这批怎么实现、验收怎么过（随代码长）
docs/handbook/            要什么、为什么（冻结，唯一权威）
```

**跑法**：
```bash
cd v2/services/core && npm test && npm run demo
```

**进度**：地基（落盘 + 取号 + 收口 + 出事谁接）**已建完**，47 条验收全过。
**下一步**：把这条路接到真连接上（HTTP `/api/say` + WS `/api/stream` + 认证 + 续传）。
⚠️ **认证做出来之前，不要开对外监听的端口**（手册把"没设口令 ⇒ 除三个公开路由外一律 503"写成硬要求）。

⚠️ **最容易犯的错**：拿旧代码顺手改一改（"反正只差几行"）。
**那样做出来的东西，会同时继承旧代码里那些我们已经判定为缺陷的东西**——
而**缺陷清单就在 `07-APPENDIX.md` §一**，那份清单里的每一条都是"不许再出现"。

---

## 四、现在的状态（**接手前必读**）

| 项 | 状态 |
|---|---|
| **规划** | ✅ **完成**。76 条决策全部拍板；两轮独立评审（各三席）已收口；**批 0 整个关闭** |
| **实现** | 🚧 **进行中**——地基已建完（`v2/services/core`，47 条验收全过） |
| **下一步** | 接真连接：HTTP + WS + 认证 + 续传（见 `docs/dev/01-FOUNDATION.md` §六） |

**批 1 里有四件是"今天就该修的"**——⚠️ 既然是全新建立，
**这四件不是"去旧代码里改"，而是"在新代码里从一开始就对"**。
**它们已经落进地基了**（下面留作对照）：

| # | 做什么 | 为什么急 |
|---|---|---|
| **S1 + S9** | 落盘失败**上抛**，**并写清谁接** | 唯一会**永久丢数据**的根；**只写上抛不写谁接 = 盘满 → 崩 → 重启 → 再崩** |
| **S7** | `dispatcher` 的 `turns` 键改用 **`info.turn`**（五处） | **升格过的轮次，收口永久失效** |
| **S8** | 瞬态事件**不占号** | 修前"磁盘无空洞"是假话 |
| **S10** | **连接生命周期** | **退一个会话会打死全站连接，且不自恢复** |

⇒ **建议从 S1 + S9 开始**（约 50–80 行，能直接进 CI）。

⚠️ **动手前先读** `03-DEVELOPMENT.md` §5.2（谁接）与 §5.3（`turns` 键）——
**那两处的根因不明显，照直觉改会改错地方。**

---

## 五、干活的标准流程

### 5.1 改客户端（`apps/mobile/`）

```bash
cd apps/mobile
~/sdk/flutter/bin/flutter analyze          # 硬闸：编译
~/sdk/flutter/bin/flutter test test/unit   # 硬闸：协议 / 状态机 / 纯逻辑
~/sdk/flutter/bin/flutter test test/widget # ⚠️ 只是提示，不是闸
~/sdk/flutter/bin/flutter build apk --release
```

⚠️ **`test/widget` 挂了不要卡在那里**——它断言的是布局（像素坐标、控件 key），
**界面一重构它必然过期**（现在 **49 条挂 10**）。
正确做法：**照常继续 → 用真机确认 → 有空再回头更新那些断言**。
**你的任务是让用户看到变化，不是把旧断言哄好。**

⚠️ **但 `flutter analyze` 与 `test/unit` 挂了必须修干净**——它们守的是协议与状态机。
想清楚：是真的改了协议，还是把状态机改坏了。

### 5.2 改服务端（**`v2/services/core/` 是新实现**）

```bash
cd v2/services/core && npm test     # 硬闸（地基现 47 条全过）
cd v2/services/core && npm run demo # 肉眼过一遍地基
```

> `services/core/`（旧实现）**不改**，只当参考——除非是要去**查证**某个 DSH 行为。

### 5.3 提交

所有改动**都要推到 GitHub**：

```bash
bash scripts/push-changes.sh "一句话说明"
```

推送失败也要说一声（改动已在本地提交，下次成功会自动补齐）。

---

## 六、不能破的几条

| # | 不许 |
|---|---|
| 1 | **不要把密钥写进任何会显示给别人的地方**，也不要写进日志 |
| 2 | **不要改协议字段的语义**——一旦上线就冻结（`03-DEVELOPMENT.md` §三） |
| 3 | **不要写死尺寸**（`写死尺寸 = 缺陷`，见 `05-DECISIONS.md` D3） |
| 4 | **不要让内部词出现在界面上**（工作区 / 口令 / 连接 / 客户端 / 云端 / 工具名 / `web_search`）——**这是缺陷，不是文风问题** |
| 5 | **不要动别人的目录**——这台机器上还有别的项目 |
| 6 | **不要说"以后再做"**——要砍就明说砍了（`04-ROADMAP.md` §十一"做一半比不做更坏"） |
| 7 | **不确定就去手册里找**；手册没有，**先问，别猜** |

---

## 七、历史在哪

旧文档（`ARCHITECTURE-v2…v7`、`REVIEW-*`、`DEBATE-*`、`DISCUSS-*`、`PLAN.md`、
`SYNTHESIS.md`、`docs/` 下的旧目录，以及 v1 原型的 `src/`）**已经删掉**。

**它们全都在 git 历史里**，需要时这样取：

**删除前的完整快照是 commit `f93f296`**（那之后的第一个 commit 就是删除）：

```bash
git show f93f296:ARCHITECTURE-v7.md                 # 例：旧的总架构
git show f93f296:docs/ux/06-retiree.md              # 例：那位不会拼音的用户的原话
git ls-tree -r --name-only f93f296 | grep docs/     # 列出当时所有文档
```

⚠️ **手册已经把该留的都收敛进来了**——包括不变量 N1–N11、接口表、安全模型、浮窗铁律、
**阈值总表**、**崩溃守护 / 回退锚点 / 预算降档**、**L3 工具能力矩阵与记忆五道写闸门**、
**v6 代仍有效的规范（含三起"页面在说假话"事故）**、iOS 构建、Web 部署。**见 `08-SPEC.md`。**
**只有在手册确实没写到、而你又非要那个原文的时候，才去翻历史。**

---

## 八、关于"改运行中的系统"（P1 / P2）

**这一条现在还只是决策，落地在批 6。** 但你要知道它的方向：

> **助手只能"提申请"，签字的是主人。**
> 助手可以提、可以写方案，**永远不能自己按那个按钮**。

理由不是洁癖。威胁的真实形态**不是"它干一次坏事"**（那能收拾），
而是"**坏事能不能留下来**"：

> 它读到一段看不见的网页内容 → 被诱导写进自己的配置 →
> **从今往后每轮开机都读那一段** → 而表面上一切正常。

⇒ **人工执行（P2）**：助手把改动与命令准备好，**主人自己上机器跑**。
形态必须"**短到主人愿意看**"（一条命令 + 一个补丁），否则会被绕过。

⚠️ **在本机改仓库代码不属于这一条**——那是正常的开发工作。
这一条针对的是**让改动变成"下次开机自动读"**的路径。
